// Generated by tools/behavior_tests.ts from the `behavior` block of the Slider doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Slider } from '../../packages/react/src/Slider';
import type { SliderProps } from '../../packages/react/src/Slider';
import meta from '../../packages/react/src/Slider.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<SliderProps> = {}) {
  const events = {
    onChange: vi.fn(),
    onChangeEnd: vi.fn(),
  };
  const props = { ...meta.args, ...given, onChange: events.onChange, onChangeEnd: events.onChangeEnd };
  const utils = render(<Slider {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Slider"]') ?? screen.queryByRole('slider') ?? utils.container.firstElementChild) as HTMLElement,
    label: () => (screen.queryByRole('slider') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<SliderProps>) => utils.rerender(<Slider {...props} {...next} />),
  };
  return s;
}

describe('Slider', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-show-value-always', async () => {
    const s = setup({"showValue": "always"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-show-value-hover', async () => {
    const s = setup({"showValue": "hover"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-show-value-never', async () => {
    const s = setup({"showValue": "never"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('slider', { name: s.props.label })).toBeInTheDocument();
  });
  test('control-is-focusable', async () => {
    const s = setup({});
    act(() => (s.label()).focus());
    expect(s.label()).toHaveFocus();
  });
  test('error-is-identified', async () => {
    const s = setup({"error": "Fix this before continuing."});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeInTheDocument();
    expect(s.label()).toHaveAttribute('aria-invalid', 'true');
  });
});
