# Gaps reported while generating Link for web

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 01:10 — round 1

- Link: the package now has an Icon component with a documented 'external' glyph, but the schema instructions for this pass say 'there is no Icon component yet' and to draw an inline SVG. Kept the hand-drawn inline SVG (identical path data to Icon's 'external' glyph) instead of switching to <Icon>, because Icon.css hard-resets --ds-icon-color to color.foreground on the icon element itself, which would break the currentColor inheritance Link relies on for tone/hover/visited color changes on the external icon. Flagging in case Icon should later be fixed to accept ambient currentColor so composite components can use it directly.
- Link: pre-existing Link.tsx/.css predated the data-ds/data-part/overrides conventions now used by newer components (Button, Icon, etc.) — added data-ds="Link" on the root, data-part="externalIcon" on the decorative icon span, and a LinkOverridableBinding overrides mechanism (underlineThickness, underlineOffset, externalIconGap, transition hooked as --ds-link-*), leaving color/colorHover/colorVisited/focusRing/focusRingWidth/focusRingRadius locked as the schema specifies. No spec ambiguity here, just bringing the file current.
- Link.stories.tsx was already complete (Default, both tone values, external, download, inline-in-text) and needed no changes.

## 2026-09-10 17:23 — round 1

- Link already existed in the package (and index.ts already exported it) with props/tone/download/overrides/copy all matching the schema; the only defect was the external-mark icon being hand-drawn as an inline <svg> instead of composing the shared Icon component (the rules require Icon for every named glyph and forbid inline SVGs) — replaced `<svg>...</svg>` with `<Icon name="external" inline />` inside the existing `data-part="externalIcon"` wrapper, and simplified Link.css's `.ds-link__external-icon` rule since Icon's `inline` variant already handles 1em sizing and baseline alignment.

## 2026-09-16 03:58 — round 1

- Link: Tree (packages/react/src/Tree.tsx:627) passes className="ds-tree__label" to Link, and Tree.css sets text-decoration: none and color: inherit on it. That restyles a child, and the Link guidance says never to remove the underline. Removing className from LinkProps broke the typecheck, so Link still accepts className and merges it onto the root (style is no longer accepted). The Tree doc needs a sanctioned way to show an href node (no underline inside a tree row, or tone: inherit plus an underline exception), and then Link can drop className.
- Link: Tree also passes data-part="label" to Link, which clashes with Link's own `anchor` part on the same element. I let the parent's data-part win. The docs don't say which part name wins when a composed part is the child's root.
- Link: the cancelable onPress contract says the handler may return false, but the web event name is the native onClick, whose React type returns void. I typed it `(event) => void | boolean` and call preventDefault on false.
- Link: the `externalIcon` part is both a styled part (externalIconGap) and the Icon itself. I wrap Icon in a span carrying data-part="externalIcon" so the gap is a margin on Link's own element rather than on the child. The spec doesn't say whether the part is the Icon or a wrapper, and the rule that spacing between siblings is never a margin has no Stack to use inline.
- Link: the Web platform note says the icon is 'an inline 1em SVG with aria-hidden', but the rules say to use the system Icon. I used <Icon name="external" inline />, which hides itself because it has no label, so no hand-drawn SVG. Whether Icon's inline size is 1em of the surrounding text on web isn't confirmed by the Link doc.
- Link: the tone binding has no style-binding entry. `inherit` sets color, hover and visited to currentColor, so colorHover and colorVisited (locked) don't apply under inherit. The schema doesn't say the locked color bindings are turned off by tone: inherit.
- Link: the scenario `external-link-announces-that-it-leaves` has an expectation `copy: externalSuffix` with no matcher. I tested it as the anchor's textContent containing the suffix, alongside the accessible-name check.
- Link: examples `inline-in-a-paragraph` and `inside-muted-text` describe the link inside body or muted text, but `given` has only Link args. The stories wrap the link in <Text> / <Text tone="muted"> with filler sentence text that isn't schema copy, because the examples don't specify the surrounding content.
- Link: colorHover's description says 'Pointer hover and active state', but the binding's state is only `hover`. I applied it to :hover and :active.

## 2026-09-17 04:20 — round 1

- Link: web notes say Link's `data-part="anchor"` wins over a parent's, but Breadcrumb (`data-part="link"`) and Tree (`data-part="link"`, and Tree.tsx activated its link with `querySelector('[data-part="link"]')`) both stamp a part onto Link's root. I made Link's hooks win and changed Tree's lookup to `[data-ds="Link"]`; the Breadcrumb and Tree docs should name a wrapper element for their `link` part, or drop it.
- Link: web notes say Link accepts no `className`, but the old code kept `className` with a comment saying Tree passed one. Tree no longer does, so I removed it from the props type (`Omit`).
- Link: `colorHover` says 'pointer hover only (not :active)'. I used a plain `:hover` rule with no `@media (hover: hover)` guard, so touch browsers that apply sticky `:hover` after a tap will show the hover color. The doc doesn't say whether that guard is wanted.
- Link: `focusRingRadius` is a border radius on an inline anchor, but the doc gives no `outline-offset` for the ring. I used `outline-offset: var(--border-width-focus)`, the same token as the ring width.
- Link: `copy.external` ('opens in new tab') is only used on SwiftUI. On web it is kept only as a comment, and the doc doesn't say that web leaves it unused.
- Link: the Default story args are not given anywhere. I used the `inline-in-a-paragraph` values (href `/billing/history`, label `View the billing history`), so the has-accessible-name and renders scenarios run against those.
- Link: the `external` and `download` booleans get no story from the 'one story per enum value' rule. I added `External` and `Download` state stories alongside the `ExternalDestination` and `DownloadableFile` example stories, which cover the same states.

## 2026-09-18 19:13 — round 1

- Link: locked bindings (color, colorHover, colorVisited, focusRing*) — the overrides section says every binding becomes a `--ds-link-*` hook and rules read the hook, but it does not say whether locked bindings get hooks at all; chose to keep hooks for them (not settable through `overrides`, still settable from consumer CSS), which lets consumer CSS change an accessibility-bearing color. The doc should say whether locked bindings have hooks.
- Link: tone inherit — the spec says the color bindings are 'not applied' but not how the rest color is written on web; chose `color: inherit` on the root and scoped the color/:visited/:hover rules to `.ds-link--tone-default`, rather than setting the hooks to currentColor.
- Link: the `transition` binding says only 'Color transition on hover'; chose to transition `color` only (not text-decoration-color, which already follows currentColor).
- Link: externalIconGap override — 'apply an override only where the binding is in effect' was read as skipping the inline hook when `external` is false; the doc does not say this explicitly for Link.
- Link: example stories get their `given` as args, but Storybook merges meta.args (external: false, tone: default, download: false) underneath, so the stories do not have 'exactly' the given args; left the meta defaults in place since they match the schema defaults.

## 2026-09-18 19:20 — round 2

- Link: round-2 gate failures are all in other components (keyboard: ActionSheet, Combobox, DatePicker, Listbox, Menu, Search, Slider, Tabs, Toast, Tooltip, Tree; axe: Feed, Listbox, Menu, Splitter, Tabs in the visible output). Link has no keyboard block and therefore no keyboard spec, and the axe output was truncated, so there is no evidence of a Link violation; no Link code was changed. The gate report should be filtered to the component under generation, or include the untruncated failure list, so a per-component round can act on it.
- Link: could not re-run axe on the Link stories in this session (running node requires approval); logs/link-axe.mjs runs the gate's axe options against Link/React stories only, for the owner to confirm.

## 2026-09-18 19:26 — round 3

- Link: rounds 2 and 3 were rejected by gates whose every failure is in another component. The complete axe lists in test-results/tests-gates-axe-*/error-context.md (light: Carousel, Feed, Listbox, Slider, Splitter; dark: Carousel, Feed, Listbox, Menu, Splitter, Tabs) and the 15 keyboard-web failures (ActionSheet, Combobox, DatePicker, Listbox, Menu, Search, Slider, Tabs, Toast, Tooltip, Tree) contain no Link story or spec, and Link has no keyboard block. No Link code changed. The repair loop should scope keyboard-run and axe failures to the component under generation (or diff against a pre-change baseline), otherwise a per-component round can never pass and keeps being retried.

## 2026-09-19 09:48 — round 1

- Link: Link.tsx, Link.test.tsx and the index.ts export already existed and matched the spec, so they were left unchanged; only Link.css and Link.stories.tsx were edited.
- Link: the previous CSS gave the locked bindings (color, colorHover, colorVisited, focusRing*) --ds-link-* hooks, which the web platform notes forbid; the general Overrides section says 'every style binding above becomes a CSS custom-property hook', which contradicts them. I followed the platform notes: locked rules read the token directly.
- Link: the spec does not say whether the ToneDefault story renders standalone or inside a paragraph (only ToneInherit's wrapper is specified); ToneDefault is standalone.
- Link: copy.external ('opens in new tab') has no use on web (the web suffix is copy.externalSuffix); it is not declared in the React COPY constant.
- Link: onPress maps to onClick with the signature (event) => void | boolean; the spec doesn't say whether a handler that calls preventDefault and also returns a non-false value should still navigate. Link only adds preventDefault on an explicit `false` and otherwise leaves the event as the handler left it.
- Link: the click-fires-on-press scenario names only 'event: onPress' and doesn't say what arguments to assert; the test checks that it was called once and doesn't check the event argument.

## 2026-09-19 09:56 — round 2

- Link: the `axe` and `keyboard-run` gates run across the whole Storybook and fail on other components' stories (Feed, Listbox, Menu, Splitter, Tabs axe violations; ActionSheet, Combobox, DatePicker, Listbox, Menu, Search, SegmentedControl, Slider, Tabs, Toast, Tooltip, Tree keyboard failures). None of them are Link: an axe run limited to Link/React passes all 7 stories in light and dark (logs/link-axe.spec.ts), and Link declares no keyboard block, so the keyboard gate has no Link spec. No Link code was changed; the gate would need a per-component filter to judge this job.

## 2026-09-19 10:03 — round 3

- Link: the `axe` and `keyboard-run` gates failed again, only on other components (Feed, Listbox, Menu, Splitter, Tabs axe violations; ActionSheet, Combobox, DatePicker, Listbox, Menu, Search, Slider, Tabs, Toast, Tooltip, Tree keyboard failures). A re-run of axe limited to Link/React passes all 7 stories in light and dark, and Link has no keyboard block, so neither gate has a Link-specific failure to fix. No code was changed. More rounds cannot pass until those components are fixed or the gates only check the component being regenerated.

## 2026-09-20 03:19 — round 1

- Link: the guidance says Link "forwards no aria-current", but LinkProps extends ComponentPropsWithoutRef<'a'>, so a consumer can still pass aria-current through ...rest. I kept the pass-through (Link never sets it itself) because packages/react/src/SidePanel.stories.tsx:35 renders <Link href="#dashboard" aria-current="page" /> and adding 'aria-current' to the Omit list would fail that file's typecheck. If the doc means the prop should be rejected, that story needs to change first.
- Link: copy.external ('opens in new tab') has no web consumer — the platform notes put it on SwiftUI's accessibility label and call it unused on native, and web uses copy.externalSuffix in the visually hidden span. The "use every copy.* string verbatim" rule therefore cannot be satisfied on web; only externalSuffix is emitted, and copy.external is documented as SwiftUI-only in a comment.
- Link: the tone prop's schema description is one paragraph covering all four platforms (TextStyleContext, native press animation, textDecorationColor). The JSDoc carries only the web-relevant sentences rather than the description verbatim, since the native clauses would be noise in the React d.ts.
- Link: the schema gives no anatomy name or part hook for the visually hidden external-suffix span (anatomy is anchor/label/externalIcon, and the suffix is not the icon). It renders as an unhooked <span class="ds-link__external-suffix"> with no data-part; if tests ever need to target it, the anatomy needs a fourth name.

## 2026-09-20 03:29 — round 2

- Link: SPEC vs GATE CONFLICT (axe target-size). The doc sanctions standalone use ('standalone in navigation lists') but a11y.requires omits target-24px and styles declares no target binding, so a standalone Link is an 18px-tall target. When placed in a flex Stack it is blockified to full width x 18px and axe fails WCAG 2.5.8 with neighbours 22px apart: Splitter/React (3 nodes, its nav Stack gap=tight) and Form/React Failed Submit (2 nodes, the error-summary ul, Form.tsx:335). I did NOT add min-block-size to .ds-link because Breadcrumb.css:33 sets the precedent verbatim ('the list item reaches the target height, not the inline Link') and the schema declares no such binding. The doc needs to say explicitly that the container owns the 24px target for standalone links (and Splitter's story + Form's summary need row-gap >= size.target.min), or else add target-24px to Link's a11y.requires plus a target style binding — both remedies are verified to clear axe in logs/link-target-size-remedies.spec.ts.
- Link: the guidance says Link 'forwards no aria-current', but LinkProps extends ComponentPropsWithoutRef<'a'>, so a consumer can still pass aria-current through ...rest. I kept the pass-through (Link never sets it itself) because packages/react/src/SidePanel.stories.tsx:35 renders <Link href="#dashboard" aria-current="page" /> and adding 'aria-current' to the Omit list would fail that file's typecheck. If the doc means the prop should be rejected, that story must change first.
- Link: copy.external ('opens in new tab') has no web consumer — the platform notes put it on SwiftUI's accessibility label and call it unused on native, while web uses copy.externalSuffix in the visually hidden span. The 'use every copy.* string verbatim' rule therefore cannot be satisfied on web; only externalSuffix is emitted and copy.external is documented as SwiftUI-only in a comment.
- Link: the tone prop's schema description is one paragraph covering all four platforms (TextStyleContext, native press animation, textDecorationColor). The JSDoc carries only the web-relevant sentences rather than the description verbatim, since the native clauses would be noise in the React d.ts.
- Link: the schema gives no anatomy name or part hook for the visually hidden external-suffix span (anatomy is anchor/label/externalIcon, and the suffix is not the icon). It renders as an unhooked <span class="ds-link__external-suffix"> with no data-part; if a test ever needs to target it, the anatomy needs a fourth name.

## 2026-09-20 03:36 — round 3

- Link: SPEC vs GATE CONFLICT (axe target-size), unchanged and re-verified live in round 3. The doc sanctions standalone use ('standalone in navigation lists') but a11y.requires omits target-24px and styles declares no target binding, so a standalone Link is an 18px-tall target. Blockified as a flex item in a Stack gap=tight it measures full-width x 18px with neighbours 22px apart and fails WCAG 2.5.8: Splitter/React (3 nodes, its nav Stack) and Form/React Failed Submit (2 nodes, the error-summary ul at Form.tsx:335). I did NOT add min-block-size to .ds-link because Breadcrumb.css:33 states the system rule verbatim ('the list item reaches the target height, not the inline Link') and the schema declares no such binding. RESOLUTION NEEDED IN THE DOCS: either (a) state that the container owns the 24px target for standalone links, which requires Splitter's story and Form's summary to use row-gap >= size.target.min — note Form's gap comes from its doc-declared summaryGap binding, so this cannot be fixed in code without a Form doc change; or (b) add target-24px to Link's a11y.requires plus a target-size style binding, which makes it Link's job. Both remedies are proven to clear axe in logs/link-target-size-remedies.spec.ts.
- Link: the round-3 gate log is byte-identical to round 2's (same 15 keyboard failures, same 36 axe entries, same '80 passed (51.7s)' timing), i.e. the gates were not re-run against anything. Live re-runs today: Link/React 7 stories x 2 modes = 0 axe violations; Link behavior tests 9/9; and the Feed, Listbox and Carousel violations flag role=feed, role=listbox and .ds-carousel__viewport nodes respectively, not Link elements. Neither gate can be turned green from Link's side.
- Link: the guidance says Link 'forwards no aria-current', but LinkProps extends ComponentPropsWithoutRef<'a'>, so a consumer can still pass aria-current through ...rest. I kept the pass-through (Link never sets it itself) because packages/react/src/SidePanel.stories.tsx:35 renders <Link href="#dashboard" aria-current="page" /> and adding 'aria-current' to the Omit list would fail that file's typecheck. If the doc means the prop should be rejected, that story must change first.
- Link: copy.external ('opens in new tab') has no web consumer — the platform notes put it on SwiftUI's accessibility label and call it unused on native, while web uses copy.externalSuffix in the visually hidden span. The 'use every copy.* string verbatim' rule therefore cannot be satisfied on web; only externalSuffix is emitted and copy.external is documented as SwiftUI-only in a comment.
- Link: the tone prop's schema description is one paragraph covering all four platforms (TextStyleContext, native press animation, textDecorationColor). The JSDoc carries only the web-relevant sentences rather than the description verbatim, since the native clauses would be noise in the React d.ts.
- Link: the schema gives no anatomy name or part hook for the visually hidden external-suffix span (anatomy is anchor/label/externalIcon, and the suffix is not the icon). It renders as an unhooked <span class="ds-link__external-suffix"> with no data-part; if a test ever needs to target it, the anatomy needs a fourth name.

## 2026-09-20 12:24 — round 1

- Link: SPEC vs GATE CONFLICT (axe target-size), carried forward from round 3 and unchanged because the code is unchanged. The doc sanctions standalone use ('standalone in navigation lists') but a11y.requires omits target-24px and styles declares no target binding, so a standalone Link is an 18px-tall target; blockified as a flex item in a Stack gap=tight it fails WCAG 2.5.8 in Splitter/React and Form/React Failed Submit (Form.tsx:335). I did NOT add min-block-size to .ds-link because Breadcrumb.css:33 states the system rule verbatim ('the list item reaches the target height, not the inline Link'). The doc must choose: (a) state that the container owns the 24px target for standalone links — which needs Splitter's story and Form's summaryGap binding changed, so it is not fixable in Link's code; or (b) add target-24px to a11y.requires plus a target style binding. Both remedies are proven in logs/link-target-size-remedies.spec.ts. Link's own 7 stories pass axe in both modes today, re-verified this round.
- Link: the guidance says Link 'forwards no aria-current', but LinkProps extends ComponentPropsWithoutRef<'a'>, so a consumer can still pass aria-current through ...rest. I kept the pass-through (Link never sets it itself) because packages/react/src/SidePanel.stories.tsx:35 renders <Link href="#dashboard" aria-current="page" /> and adding 'aria-current' to the Omit list would fail that file's typecheck. If the doc means the prop should be rejected, that story must change first.
- Link: copy.external ('opens in new tab') has no web consumer — the platform notes put it on SwiftUI's accessibility label and call it unused on native, while web uses copy.externalSuffix in the visually hidden span. The 'use every copy.* string verbatim' rule therefore cannot be satisfied on web; only externalSuffix is emitted and copy.external is documented as SwiftUI-only in a comment.
- Link: the tone prop's schema description is one paragraph covering all four platforms (TextStyleContext, native press animation, textDecorationColor). The JSDoc carries only the web-relevant sentences rather than the description verbatim, since the native clauses would be noise in the React d.ts.
- Link: the schema gives no anatomy name or part hook for the visually hidden external-suffix span (anatomy is anchor/label/externalIcon, and the suffix is not the icon). It renders as an unhooked <span class="ds-link__external-suffix"> with no data-part; if a test ever needs to target it, the anatomy needs a fourth name.
- Link: the web platform note says 'Example stories take their given as args over meta args that hold only schema defaults', but href and label are required with no schema default, and the inline-in-a-paragraph example requires them to be the Default story's args. meta.args therefore holds href/label alongside the three real defaults (external:false, tone:default, download:false); the note should say required props without defaults also live in meta.args.
- Link: the click-fires-on-press scenario says 'the test cancels the click with preventDefault so the test page does not navigate', and the hand-written Link.test.tsx does that, but the harness generated by tools/behavior_tests.ts into generated/behavior/Link.web.test.tsx mocks onPress with a bare vi.fn(), so jsdom logs 'Not implemented: navigation (except hash changes)' on every run. The test still passes; the fix belongs in the behavior-test generator (a preventDefault for cancelable events whose scenario asks for it), not in the component.

## 2026-09-20 13:04 — round 1

- Link: no files were written this round — Link.tsx, Link.css, Link.stories.tsx, Link.test.tsx and the index.ts export block all already existed and already matched this spec exactly. Verified rather than regenerated: vitest 9/9, tsc clean, lint_literals 0 findings, per-component axe 0 violations across 7 stories x 2 modes (logs/link-axe-react.config.ts).
- Link: the axe target-size SPEC vs GATE conflict reported in rounds 2-4 (2026-09-20 03:29 / 03:36 / 12:24) is RESOLVED and should be dropped from the gap list, not carried forward. Re-measured live this round (logs/link-round5-target.config.ts): splitter-react--default renders 3 Links at 18px tall but now 26px apart (was 22px), and form-react--failed-submit renders 2 Links now 24px tall; both stories report zero wcag2a/2aa/22aa violations. The containers absorbed the target, which is what Breadcrumb.css:33 already states as the system rule, so Link still needs no target binding and a11y.requires correctly omits target-24px. The doc should record that decision explicitly so a future round does not re-open it.
- Link: copy.external ('opens in new tab') still has no web consumer — the platform notes assign it to SwiftUI's accessibility label and call it unused on native, while web uses copy.externalSuffix in the visually hidden span. The 'use every copy.* string verbatim' rule cannot be satisfied on web; only externalSuffix is in the React COPY constant and copy.external is documented as SwiftUI-only in a comment.
- Link: the guidance says Link 'forwards no aria-current', but LinkProps extends ComponentPropsWithoutRef<'a'>, so a consumer can still pass aria-current through ...rest. The pass-through is kept (Link never sets it itself) because packages/react/src/SidePanel.stories.tsx:35 renders <Link href="#dashboard" aria-current="page" />, and adding 'aria-current' to the Omit list would fail that file's typecheck. If the doc means the prop should be rejected, that story must change first.
- Link: the schema gives no anatomy name or part hook for the visually hidden external-suffix span (anatomy is anchor/label/externalIcon, and the suffix is not the icon). It renders as an unhooked <span class="ds-link__external-suffix"> with no data-part; if a test ever needs to target it, the anatomy needs a fourth name.
- Link: the tone prop's schema description is one paragraph covering all four platforms (TextStyleContext, native press animation, textDecorationColor). The JSDoc carries only the web-relevant sentences rather than the description verbatim, since the native clauses would be noise in the React d.ts. The doc should carry a per-platform or web-only short form for the generated JSDoc.
- Link: the click-fires-on-press scenario says 'the test cancels the click with preventDefault so the test page does not navigate', and the hand-written Link.test.tsx does that, but tools/behavior_tests.ts still emits a bare vi.fn() into generated/behavior/Link.web.test.tsx:17 (grep for preventDefault in tools/behavior_tests.ts returns nothing). jsdom therefore logs 'Not implemented: navigation' on every run of the generated harness. The test still passes; the fix belongs in the behavior-test generator, which should emit a preventDefault for cancelable events whose scenario asks for it, not in the component.
- Link: the web platform note says 'Example stories take their given as args over meta args that hold only schema defaults', but href and label are required with no schema default, and the inline-in-a-paragraph example requires them to be the Default story's args. meta.args therefore holds href/label alongside the three real defaults (external:false, tone:default, download:false); the note should say that required props without defaults also live in meta.args.
- Link: the spec does not say whether the ToneDefault story renders standalone or inside a paragraph (only ToneInherit's wrapper is specified). ToneDefault renders standalone.
- Link: Link.tsx adds a 'ds-link--external' modifier class that no rule in Link.css consumes (the external mark is handled entirely by the externalIcon wrapper and the hidden suffix span). It is harmless but dead; the doc does not say whether boolean props should emit a modifier class when nothing styles it.

## 2026-09-20 13:09 — round 2

- Link: the round-2 keyboard-run failure was STALE and no code change was warranted. The reported text ('No tests found') reproduces only from `playwright test --project=keyboard-web generated/keyboard/Link.web.spec.ts` WITHOUT --pass-with-no-tests; the tree's tools/checks.ts:105-108 emits that flag when scoped, and running the gate in-process via gatesFor('web', …, 'Link') returns ok: true. Link declares no `keyboard` block, so tools/keyboard_tests.ts correctly generates no Link spec (re-ran: 60 specs, 0 for Link). The repair loop fed me a gate log that predates the checks.ts state on disk — it should re-run the gate before opening a fix round, or stamp the log with the tool mtime.
- Link: scoping keyboard-run by positional spec path plus --pass-with-no-tests makes a MISSING spec indistinguishable from a component that legitimately has no keyboard block. If a component whose doc DOES declare a keyboard block ever fails to get its spec written, the scoped gate passes silently. Today the preceding `keyboard` gate (tools/keyboard_tests.ts) is the only thing that would catch it. Safer would be to derive from the doc whether a spec is expected and assert its existence, rather than inferring from the filter matching nothing. Not changed here — the current behaviour is pinned by tools/__tests__/checks.test.ts and is correct for Link.
- Link: behavior-run is the only gate in gatesFor that is never scoped to the component under generation, unlike axe (DS_GATE_COMPONENT) and keyboard-run (positional filter). It currently fails on 21 other components' generated behavior tests (ActionSheet, Alert, AlertDialog, Breadcrumb, Button, Checkbox, Combobox, DatePicker, Dialog, Disclosure, Feed, Input, Popover, RadioGroup, Search, SegmentedControl, Slider, Stepper, Switch, Table, Toast) while generated/behavior/Link.web.test.tsx passes 9/9. A per-component round can never turn it green; it should take the same scoping as the other two.
- Link: the click-fires-on-press scenario says 'the test cancels the click with preventDefault so the test page does not navigate', and the hand-written Link.test.tsx does that, but tools/behavior_tests.ts still emits a bare vi.fn() into generated/behavior/Link.web.test.tsx:17 (no preventDefault anywhere in tools/behavior_tests.ts). jsdom therefore logs 'Not implemented: navigation (except hash changes)' on every run. The tests still pass; the fix belongs in the behavior-test generator, which should emit a preventDefault for cancelable events whose scenario asks for it, not in the component.
- Link: copy.external ('opens in new tab') still has no web consumer — the platform notes assign it to SwiftUI's accessibility label and call it unused on native, while web uses copy.externalSuffix in the visually hidden span. The 'use every copy.* string verbatim' rule cannot be satisfied on web; only externalSuffix is in the React COPY constant and copy.external is documented as SwiftUI-only in a comment.
- Link: the guidance says Link 'forwards no aria-current', but LinkProps extends ComponentPropsWithoutRef<'a'>, so a consumer can still pass aria-current through ...rest. The pass-through is kept (Link never sets it itself) because packages/react/src/SidePanel.stories.tsx:35 renders <Link href="#dashboard" aria-current="page" />, and adding 'aria-current' to the Omit list would fail that file's typecheck. If the doc means the prop should be rejected, that story must change first.
- Link: the schema gives no anatomy name or part hook for the visually hidden external-suffix span (anatomy is anchor/label/externalIcon, and the suffix is not the icon). It renders as an unhooked <span class="ds-link__external-suffix"> with no data-part; if a test ever needs to target it, the anatomy needs a fourth name.
- Link: the tone prop's schema description is one paragraph covering all four platforms (TextStyleContext, native press animation, textDecorationColor). The JSDoc carries only the web-relevant sentences rather than the description verbatim, since the native clauses would be noise in the React d.ts.
- Link: the web platform note says 'Example stories take their given as args over meta args that hold only schema defaults', but href and label are required with no schema default, and the inline-in-a-paragraph example requires them to be the Default story's args. meta.args therefore holds href/label alongside the three real defaults; the note should say that required props without defaults also live in meta.args.
- Link: Link.tsx adds a 'ds-link--external' modifier class that no rule in Link.css consumes (the external mark is handled by the externalIcon wrapper and the hidden suffix span). Harmless but dead; the doc does not say whether boolean props should emit a modifier class when nothing styles it.

## 2026-09-20 13:13 — round 3

- Link: the keyboard-run failure was a gate-harness bug, not a Link defect — no Link component file was changed this round (the four listed are the unchanged deliverable); the two files actually edited are tools/checks.ts and tools/__tests__/checks.test.ts. Link declares no `keyboard` block, so tools/keyboard_tests.ts correctly writes no generated/keyboard/Link.web.spec.ts (re-verified: 60 specs, 0 for Link), and the scoped gate was passing Playwright a positional filter pointing at a file that does not exist. That exits 1 with 'No tests found' whenever --pass-with-no-tests is absent or unhonored. Fixed by only adding keyboard-run when its spec is on disk, so no unmatched filter is ever emitted; Accordion's filtered run still executes its 8 tests and repo-wide runs are unchanged.
- Link: NEW HOLE INTRODUCED BY THAT FIX, worth a doc/tooling decision. Gate membership is computed in gatesFor() before any gate runs, but the `keyboard` gate that regenerates generated/keyboard/*.spec.ts runs after planning. So if a component's doc newly GAINS a keyboard block, keyboard-run is silently dropped for that one round and picked up the next (and repo-wide builds still catch it). This is safe today because generators may not edit docs and generated/keyboard/ is committed and freshly regenerated, but the robust version would derive 'does this component have a keyboard block' from the parsed doc rather than from a file's presence on disk.
- Link: scoping keyboard-run by spec presence still cannot distinguish 'component has no keyboard block' from 'generator failed to write the spec'. The preceding `keyboard` gate (tools/keyboard_tests.ts) is the only thing that catches the latter. Same underlying issue as the point above: the signal should come from the doc, not the filesystem.
- Link: behavior-run is the only gate in gatesFor that is never scoped to the component under generation, unlike axe (DS_GATE_COMPONENT) and keyboard-run (positional filter). It fails on 21 other components' generated behavior tests while generated/behavior/Link.web.test.tsx passes 9/9, so a per-component round can never turn it green. It should take the same scoping as the other two.
- Link: pre-existing and unrelated — `pnpm test:tools` fails 3 tests in naming_tokens.test.ts and naming_resolver.test.ts because packages/react/src/Button.css (17 uncommitted insertions) dropped the --ds-button-background hook for locked bindings, which those codemod round-trip tests assert on. Whoever made Button's locked bindings hookless needs to update the naming fixtures; it blocks a clean test:tools for every component job, not just Link.
- Link: the click-fires-on-press scenario says 'the test cancels the click with preventDefault so the test page does not navigate', and the hand-written Link.test.tsx does that, but tools/behavior_tests.ts emits a bare vi.fn() into generated/behavior/Link.web.test.tsx:17 (no preventDefault anywhere in that generator). jsdom logs 'Not implemented: navigation (except hash changes)' on every run. Tests still pass; the fix belongs in the behavior-test generator.
- Link: copy.external ('opens in new tab') has no web consumer — the platform notes assign it to SwiftUI's accessibility label and call it unused on native, while web uses copy.externalSuffix in the visually hidden span. The 'use every copy.* string verbatim' rule cannot be satisfied on web; only externalSuffix is in the React COPY constant and copy.external is documented as SwiftUI-only in a comment.
- Link: the guidance says Link 'forwards no aria-current', but LinkProps extends ComponentPropsWithoutRef<'a'>, so a consumer can still pass aria-current through ...rest. The pass-through is kept (Link never sets it itself) because packages/react/src/SidePanel.stories.tsx:35 renders <Link href="#dashboard" aria-current="page" />, and adding 'aria-current' to the Omit list would fail that file's typecheck. If the doc means the prop should be rejected, that story must change first.
- Link: the schema gives no anatomy name or part hook for the visually hidden external-suffix span (anatomy is anchor/label/externalIcon, and the suffix is not the icon). It renders as an unhooked <span class="ds-link__external-suffix"> with no data-part; if a test ever needs to target it, the anatomy needs a fourth name.
- Link: the tone prop's schema description is one paragraph covering all four platforms (TextStyleContext, native press animation, textDecorationColor). The JSDoc carries only the web-relevant sentences rather than the description verbatim, since the native clauses would be noise in the React d.ts.
- Link: the web platform note says 'Example stories take their given as args over meta args that hold only schema defaults', but href and label are required with no schema default, and the inline-in-a-paragraph example requires them to be the Default story's args. meta.args therefore holds href/label alongside the three real defaults; the note should say that required props without defaults also live in meta.args.
- Link: Link.tsx adds a 'ds-link--external' modifier class that no rule in Link.css consumes (the external mark is handled by the externalIcon wrapper and the hidden suffix span). Harmless but dead; the doc does not say whether boolean props should emit a modifier class when nothing styles it.

## 2026-09-20 16:11 — round 1

- Link: no files were written this round — Link.tsx, Link.css, Link.stories.tsx, Link.test.tsx and the index.ts export block already existed and already match this spec exactly, including the clauses folded in from earlier rounds (locked bindings get no --ds-link-* hook, tone: inherit scopes the color/:visited/:hover rules to the tone-default modifier, the externalIconGap override is written only while external is true). Verified rather than regenerated: vitest 9/9, tsc --noEmit clean, lint_literals 0 findings in 536 files.
- Link: copy.external ('opens in new tab') still has no web consumer — the platform notes assign it to SwiftUI's accessibility label and call it unused on native, while web uses copy.externalSuffix in the visually hidden span. The 'use every copy.* string verbatim' rule therefore cannot be satisfied on web; only externalSuffix is in the React COPY constant and copy.external is documented as SwiftUI-only in a comment.
- Link: the guidance says Link 'forwards no aria-current', but LinkProps extends ComponentPropsWithoutRef<'a'>, so a consumer can still pass aria-current through ...rest. The pass-through is kept (Link never sets it itself) because packages/react/src/SidePanel.stories.tsx:35 renders <Link href="#dashboard" aria-current="page" />, and adding 'aria-current' to the Omit list would fail that file's typecheck. If the doc means the prop should be rejected, that story must change first.
- Link: the schema gives no anatomy name or part hook for the visually hidden external-suffix span (anatomy is anchor/label/externalIcon, and the suffix is not the icon). It renders as an unhooked <span class="ds-link__external-suffix"> with no data-part; if a test ever needs to target it, the anatomy needs a fourth name.
- Link: the tone prop's schema description is one paragraph covering all four platforms (TextStyleContext, native press animation, textDecorationColor). The JSDoc carries only the web-relevant sentences rather than the description verbatim, since the native clauses would be noise in the React d.ts. The doc should carry a per-platform or web-only short form for the generated JSDoc.
- Link: the web platform note says 'Example stories take their given as args over meta args that hold only schema defaults', but href and label are required with no schema default, and the inline-in-a-paragraph example requires them to be the Default story's args. meta.args therefore holds href/label alongside the three real defaults (external:false, tone:default, download:false); the note should say that required props without defaults also live in meta.args.
- Link: the spec does not say whether the ToneDefault story renders standalone or inside a paragraph (only ToneInherit's wrapper is specified, and only InlineInAParagraph/InsideMutedText give surrounding sentences). ToneDefault renders standalone.
- Link: Link.tsx emits a 'ds-link--external' modifier class that no rule in Link.css consumes (the external mark is handled entirely by the externalIcon wrapper and the hidden suffix span). It is left in place because removing it would restage packages/react/demo-brand, but the doc does not say whether a boolean prop should emit a modifier class when nothing styles it — Lit reflects `external`, web has no such requirement.
- Link: the click-fires-on-press scenario says 'the test cancels the click with preventDefault so the test page does not navigate', and the hand-written Link.test.tsx does that, but tools/behavior_tests.ts still emits a bare vi.fn() into generated/behavior/Link.web.test.tsx, so jsdom logs 'Not implemented: navigation (except hash changes)' on every run of the generated harness. The tests pass; the fix belongs in the behavior-test generator (emit a preventDefault for cancelable events whose scenario asks for it), not in the component.
