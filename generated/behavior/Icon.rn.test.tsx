// Generated by tools/behavior_tests.ts from the `behavior` block of the Icon doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Icon } from '../../packages/rn/src/Icon';
import type { IconProps } from '../../packages/rn/src/Icon';
import meta from '../../packages/rn/src/Icon.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<IconProps> = {}) {
  const events = {};
  const props: IconProps = { ...(meta.args as IconProps), ...given };
  const tree = (p: IconProps) => (
    <ThemeProvider mode="light">
      <Icon {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Icon') ?? screen.UNSAFE_root,
    glyph: () => screen.queryByRole('img') ?? s.root(),
    rerender: (next: Partial<IconProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Icon', () => {
  test('unlabelled-icon-is-hidden-from-assistive-technology', () => {
    const s = setup({});
    expect(s.glyph()).toHaveProp("accessibilityElementsHidden", true);
  });
  test('label-makes-the-icon-meaningful', () => {
    const s = setup({"name": "warning", "label": "Warning: over quota"});
    expect(screen.getByRole('img', { name: "Warning: over quota" })).toBeOnTheScreen();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-check', () => {
    const s = setup({"name": "check"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-dash', () => {
    const s = setup({"name": "dash"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-chevron-right', () => {
    const s = setup({"name": "chevron-right"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-chevron-down', () => {
    const s = setup({"name": "chevron-down"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-chevron-up', () => {
    const s = setup({"name": "chevron-up"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-chevron-left', () => {
    const s = setup({"name": "chevron-left"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-close', () => {
    const s = setup({"name": "close"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-plus', () => {
    const s = setup({"name": "plus"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-minus', () => {
    const s = setup({"name": "minus"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-info', () => {
    const s = setup({"name": "info"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-success', () => {
    const s = setup({"name": "success"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-warning', () => {
    const s = setup({"name": "warning"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-danger', () => {
    const s = setup({"name": "danger"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-external', () => {
    const s = setup({"name": "external"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-ellipsis', () => {
    const s = setup({"name": "ellipsis"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-search', () => {
    const s = setup({"name": "search"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-arrow-right', () => {
    const s = setup({"name": "arrow-right"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-arrow-left', () => {
    const s = setup({"name": "arrow-left"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-calendar', () => {
    const s = setup({"name": "calendar"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-menu', () => {
    const s = setup({"name": "menu"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-list', () => {
    const s = setup({"name": "list"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-grid', () => {
    const s = setup({"name": "grid"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-play', () => {
    const s = setup({"name": "play"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-pause', () => {
    const s = setup({"name": "pause"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-folder', () => {
    const s = setup({"name": "folder"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-name-file', () => {
    const s = setup({"name": "file"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-xs', () => {
    const s = setup({"size": "xs"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-sm', () => {
    const s = setup({"size": "sm"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-md', () => {
    const s = setup({"size": "md"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-lg', () => {
    const s = setup({"size": "lg"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-xl', () => {
    const s = setup({"size": "xl"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({"label": "Accessible name"});
    expect(screen.getByRole('img', { name: s.props.label })).toBeOnTheScreen();
  });
});
