Port mcp/server.py, mcp/index.py and mcp/smoke.py to TypeScript on the official MCP TypeScript SDK (`@modelcontextprotocol/sdk`, latest), same tool names, same JSON shapes, same index file format. `pnpm mcp`, `pnpm mcp:index`, `pnpm mcp:smoke` call the TS versions. Update process/mcp-server.md's command lines. Gate: `pnpm mcp:smoke` passes with the identical transcript. Delete the Python files afterwards. Do not modify packages/*/src or generated/.
