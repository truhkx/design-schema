// Generated by tools/behavior_tests.ts from the `behavior` block of the Stepper doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Stepper } from '../../packages/rn/src/Stepper';
import type { StepperProps } from '../../packages/rn/src/Stepper';
import meta from '../../packages/rn/src/Stepper.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<StepperProps> = {}) {
  const events = {
    onStepSelect: jest.fn(),
  };
  const props: StepperProps = { ...(meta.args as StepperProps), ...given, onStepSelect: events.onStepSelect };
  const tree = (p: StepperProps) => (
    <ThemeProvider mode="light">
      <Stepper {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Stepper') ?? screen.UNSAFE_root,
    list: () => s.root(),
    rerender: (next: Partial<StepperProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Stepper', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-orientation-horizontal', () => {
    const s = setup({"orientation": "horizontal"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-orientation-vertical', () => {
    const s = setup({"orientation": "vertical"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-navigable-none', () => {
    const s = setup({"navigable": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-navigable-completed', () => {
    const s = setup({"navigable": "completed"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-navigable-all', () => {
    const s = setup({"navigable": "all"});
    expect(s.root()).toBeTruthy();
  });
  test.skip('has-accessible-name — then.name: role \'none\' cannot be queried; name the landmark/text role in the doc', async () => {});
});
