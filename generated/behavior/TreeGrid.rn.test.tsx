// Generated by tools/behavior_tests.ts from the `behavior` block of the TreeGrid doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { TreeGrid } from '../../packages/rn/src/TreeGrid';
import type { TreeGridProps } from '../../packages/rn/src/TreeGrid';
import meta from '../../packages/rn/src/TreeGrid.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<TreeGridProps> = {}) {
  const events = {
    onExpandChange: jest.fn(),
    onExpand: jest.fn(),
    onSortChange: jest.fn(),
    onSelectionChange: jest.fn(),
    onCellChange: jest.fn(),
    onEditStart: jest.fn(),
    onColumnResize: jest.fn(),
  };
  const props: TreeGridProps = { ...(meta.args as TreeGridProps), ...given, onExpandChange: events.onExpandChange, onExpand: events.onExpand, onSortChange: events.onSortChange, onSelectionChange: events.onSelectionChange, onCellChange: events.onCellChange, onEditStart: events.onEditStart, onColumnResize: events.onColumnResize };
  const tree = (p: TreeGridProps) => (
    <ThemeProvider mode="light">
      <TreeGrid {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('TreeGrid') ?? screen.UNSAFE_root,
    container: () => screen.queryByRole('treegrid') ?? s.root(),
    rerender: (next: Partial<TreeGridProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('TreeGrid', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-none', () => {
    const s = setup({"selectable": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-row', () => {
    const s = setup({"selectable": "row"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-cell', () => {
    const s = setup({"selectable": "cell"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-density-compact', () => {
    const s = setup({"density": "compact"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-density-comfortable', () => {
    const s = setup({"density": "comfortable"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-height-content', () => {
    const s = setup({"height": "content"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-height-viewport', () => {
    const s = setup({"height": "viewport"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-height-fixed', () => {
    const s = setup({"height": "fixed"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('treegrid', { name: s.props.caption })).toBeOnTheScreen();
  });
});
