Add a `swift` platform to tokens/build.mjs (Style Dictionary 4) per process/ios-platform.md "Tokens".

Output per theme: packages/swiftui/Sources/DesignSchemaTokens/<ThemeName>.swift with `public enum <ThemeName> { public enum Light { … } public enum Dark { … } }`, static lets in camelCase (name/camel-no-default), types: colors `Color(red:green:blue:opacity:)` from hex, dimensions `CGFloat` (px → pt number), durations `TimeInterval` (ms → s), easing `Animation.timingCurve`, fontFamily `Font.Design`/family string mapping (`system-ui` → `.default`, `ui-monospace` → `.monospaced`), fontWeight `Font.Weight`, lineHeight Double, opacity Double, layer Double, shadow `ShadowToken(color:x:y:blur:spread:)`. Also generate `TokenRef.swift`: `public enum TokenRef: String, CaseIterable` with one case per token (dotted name as raw value, camelCase case name) and a `Theme` extension resolving each case for the active mode. Wire `pnpm tokens` to emit it; add a unit test in tokens/ that the Swift file lists exactly as many tokens as json/tokens.light.json.
Gate: `pnpm tokens` succeeds; the swiftui-gates workflow builds. Do not modify packages/*/src or generated/.
