# Gaps reported while generating Select for web

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 09:59 — round 1

- Select: the schema gives Select no controlled `open` prop (unlike Menu/Popover/Dialog), but the testability convention requires a `Keyboard` story rendered open/present with ≥3 focusable children. Resolved by rendering three separate Select instances side by side (matching the existing workaround in Tooltip.stories.tsx) rather than inventing an undocumented prop.
- Select: platform notes say focus 'visually stays on the trigger' while the popup's Listbox keyboard model applies, implying an aria-activedescendant-on-trigger pattern. Listbox is a self-contained widget whose own onKeyDown only fires when it itself holds real DOM focus, and exposes no imperative API to forward keys to it externally, so I moved real focus onto the Listbox root when the popup opens (mirroring Menu.tsx's precedent of focusing into its popup) and restore focus to the trigger on Escape/Tab/commit-close, satisfying focus-restore even though DOM focus briefly leaves the trigger.
- Select: passed `selectionFollowsFocus={false}` to the nested Listbox, diverging from Listbox's own default of `true`. Listbox's default auto-commits on ArrowUp/Down, which would fire onChange (and close the popup for single-select) on every arrow press — conflicting with the spec's own row 3, which makes Enter the sole committer/closer for single-select.
- Select: 'Opens the popup with the selected (or first) option active' cannot be fully implemented — Listbox has no prop to seed its initial `activeValue` (always starts `null`), only reachable via user interaction (arrow/typeahead/click). The popup currently opens with no option pre-highlighted; the selected option remains visually distinguished (checkmark + bold) via Listbox's own selected-state styling, just not the 'active' row highlight.
- Select: the keyboard table lists Space only on the closed-trigger row (to open); once the popup is open only Enter/Arrows/Home/End/a-z are listed. Because the popup reuses Listbox's own keydown handler verbatim (per 'never reimplement a composed child'), Space also commits/toggles there — inherited from Listbox rather than suppressed, a small superset of the documented table.
- Select: for `native: 'always'`, used the real HTML `disabled` attribute on the `<select>` (unfocusable when disabled) instead of the package's general `aria-disabled` + 'stays focusable' convention, since that escape hatch exists specifically for forms that must work without JavaScript — `aria-disabled` alone wouldn't block interaction there. `forwardRef` type is `HTMLButtonElement | HTMLSelectElement` accordingly, resolving to whichever element the current `native` value actually renders.
- Select: popupSurface/popupBorder/popupRadius overrides are forwarded into the composed Listbox via the sanctioned CSS custom-property escape hatch (targeting Listbox's own --ds-listbox-surface/-border/-radius from `.ds-select__popup > .ds-listbox`) rather than through Listbox's `overrides` prop — Listbox's `surface` binding is locked (no prop path exists), and doing border/radius through props while surface goes through CSS would have split one visual box across two override mechanisms and let the wrapper's shadow-clipping radius drift out of sync with Listbox's own.
- Select: the schema doesn't specify a join string for the `multiple` trigger's 'labels when two or fewer' text; used ', ' (comma + space), matching enumeration conventions used elsewhere in the package.
- Select: `container` (portal target) isn't in the component's own schema prop list but is added per the package-wide convention that every portaled overlay accepts it.

## 2026-09-10 18:40 — round 1

- Select: the doc now includes a real `open` prop (controlled popup state) and a `size` (sm/md) prop; these were absent from the existing implementation, so I added them — `open` as a standard controlled/uncontrolled boolean mirroring `value`, and `size` following Input's sm/md convention (`triggerPaddingBlockSm`, `minTargetSm`, and `fontSize` swapped to `font.size.sm` via a `.ds-select--sm` modifier class).
- Select: with `open` now available, updated the `Keyboard` story so the first of three trigger instances renders with `open` — this satisfies 'renders it open/present with its trigger' literally, while the other two closed triggers keep the ≥3-focusable-children count, since a single open Select still only exposes two focusable nodes (trigger + the embedded Listbox's one aria-activedescendant tab stop) and three separately-focusable children inside one instance remain structurally impossible.
- Select: the guidance text says the composed Listbox is `embedded` and receives `defaultActiveValue` set to the current selection, but the existing implementation wired neither. Wired both now, and moved the popup's surface/border/radius to be drawn by the `.ds-select__popup` wrapper itself (an embedded Listbox draws none of its own), removing the previous CSS custom-property forwarding hack into `--ds-listbox-*`, which would have been a no-op once `embedded` zeroes those rules out.
- Select: added the missing `hideLabel` prop (listed in the schema's prop table but absent from the existing code) and the package's standard `data-ds-field` attribute on the root (present on Input/Checkbox but missing on both of Select's render paths) — both plain omissions, not doc ambiguities.
- Pre-existing, out of scope: the generated behavior test (generated/behavior/Select.web.test.tsx) throws when jsdom's `HTMLElement.scrollIntoView` (unimplemented in jsdom) is hit by Listbox's own focus-handling code once the popup opens and focus moves to the embedded Listbox — this call path predates this change (Select already moved focus onto the Listbox on open) and lives in Listbox.tsx, not Select.tsx.

## 2026-09-16 08:26 — round 1

- Select: the guidance says the composed Listbox receives `defaultActiveValue`, but Listbox's prop is `initialActiveValue`, and it only applies when the list receives DOM focus. With focus staying on the trigger there is no clean way to open with the selected option active; I dispatch a synthetic `focusin` on the Listbox root when the popup opens. Listbox needs a focus-independent active prop (or a controlled `activeValue`).
- Select: web notes and guidance say focus stays on the trigger with keys forwarded to the Listbox, but `platforms.web.attributes` omits `aria-activedescendant`, which the APG select-only combobox needs on the trigger for the active option to be announced. I added it, pointing at the Listbox option id (`<listboxId>-option-<value>`), which ties Select to a Listbox internal id format the Listbox schema does not declare.
- Select: 'forward keydown from the trigger to the Listbox's handler' names no mechanism, and Listbox exposes no handler. I replay a native KeyboardEvent on the Listbox root and mirror its defaultPrevented back onto the trigger event. A guard flag stops the popup wrapper's own keydown handler from handling the replay twice.
- Select: Listbox commits only when the value changes, so Enter, Space or a click on the already-selected option would not close a single select through onChange. Select closes itself after those keys and on option click, whether or not the value changed. The schema does not say whether re-picking the current value closes; I chose to close.
- Select: Parts and slots say the listbox part receives exactly `options`, but the guidance also requires `embedded`, `selectionFollowsFocus: false`, the default active value and `labelledBy`. I passed those too, plus `multiple`, `value`, `onChange` and `onActiveChange`, which Select cannot work without. The composition block should list them.
- Select: the `fontWeight` binding covers 'trigger and option text', but Listbox has no fontWeight override and nothing is declared as forwarded. I set the weight on the trigger and on the popup wrapper (options inherit it; Listbox's selected-option weight still wins). Likewise `fontFamily`, `fontSize` and `lineHeight` are not forwarded to Listbox's own overrides, since the schema declares no forwards.
- Select: `labelWeight` and `helperSize` have no declared child binding to forward into. I forward them to Text's `fontWeight` and `fontSize` overrides on label (labelWeight) and on description and error (helperSize). `helperSize`, `partGap`, `placeholderColor`, `errorText`, `layer`, `fontFamily`, `fontSize`, `fontWeight`, `lineHeight`, `disabledOpacity` and `enter` have no `part`.
- Select: `triggerBorderFocus` and `focusRingWidth` are locked yet both are 'part trigger' bindings with no `state`. I applied them under :focus-visible and made focus win over the invalid border colour; the schema does not say which wins.
- Select: form `valueType: string[]`, but a single select submits one string. I submit a string for single and an array for `multiple`, omitting the key when empty; the conventions' FormContext getValue type (`string | boolean | undefined`) does not list arrays either.
- Select: `copy.done` is only used by the RN phone sheet; web has no use for it, so it is declared in COPY but unused.
- Select: `native: always` with `multiple` renders <select multiple>, which has no dropdown, so I omit the chevron there. The guidance says to render 'appearance: none styling plus the chevron' without covering the multiple case.
- Select: disabled 'not submitted' — for the popup mode I omit the hidden inputs when disabled; the native mode uses the real `disabled` attribute, per the web notes.
- Select: the popup enter transition — the `enter` binding is described only as 'Popup fade'. I fade opacity only, with no slide, following 'prefer no animation'. `popupOffset` is applied as a block margin on the fixed popup in the direction it opens.
- Select: the Keyboard story needs 'at least three focusable children', but the popup's options are reached through aria-activedescendant and are not tab stops. The story renders one Select open (six options) with its own open state so Escape and Tab actually close it; whether the gate counts activedescendant options as focusable children is unspecified.
- Select: `aria-controls` is set only while open, because the Listbox is not mounted while closed and an id reference to a missing element fails a11y lint. The web notes list aria-controls without saying whether it must be present while closed.
- Select: the placeholder `copy.placeholder` is rendered in the trigger, but the doc does not say whether `native: always` single mode needs a placeholder option. I render a disabled empty-value <option> showing the placeholder.

## 2026-09-17 11:51 — round 1

- Select: the web notes say to replay keys and dispatch `focusin` on 'the Listbox's list element', but React Listbox has its key and focus handlers on the wrapper (its ref), and its own doc says a host dispatches on the ref. Chose the wrapper ref.
- Select: Behavior says 'the embedded list is never a tab stop', but Listbox always renders its list with tabIndex=0 and has no prop to turn that off. Select can't fix it without restyling or reaching into the child, so it keeps a popup keydown fallback for when focus lands on the list. Listbox needs a non-tabbable mode for `embedded`.
- Select: Behavior says Enter with `multiple` toggles and stays open, but Listbox's Enter does nothing with `multiple`. Select handles Enter itself instead of forwarding it. The doc should say whether Enter is forwarded or handled by the host.
- Select: `fontSize`, `fontFamily`, `lineHeight` and `fontWeight` are forwarded into the composed parts, but the overrides contract still wants a --ds-select-* hook on the root for every binding. Kept the root hooks because the `native: always` <select> has no Text to forward into. The doc doesn't say whether these hooks exist, as it does for labelWeight/helperSize.
- Select: the locked `chevron` binding (color.foreground.muted) is forwarded as Icon overrides.color, but Icon's `color` is overridable while `chevron` is locked. Chose to always pass the locked token and give consumers no way to change it.
- Select: `valueColor`, `placeholderColor` and `chevron` are locked bindings realised through child tones, so their --ds-select-* hooks are declared on the root but read by nothing. The doc should say whether locked bindings realised by a child get a hook at all.
- Select: 'Clicking the label focuses the trigger and does not open the popup' conflicts with a native <label for> on a <button>, where the browser turns the label click into a button click. Chose to preventDefault the label click and focus the trigger by hand (native: always excluded). The doc should name this.
- Select: the doc doesn't say where data-part='label' goes when a native <label> wraps the label Text. Put it on the composed Text and gave the <label> only the class.
- Select: the chevron needs placing (absolutely positioned over the native <select>), and 'never restyle a child' rules out a class on Icon. Wrapped the Icon in a layout-only span. The native select's end padding reserves var(--font-size-sm) for the glyph, because Icon size sm is one font.size.sm wide; the doc gives no binding for that reserve.
- Select: disabledOpacity is 'applied to the whole field group', but a portaled popup can't inherit it. Moot for now because a disabled Select never opens; noting it in case a controlled `open` is passed with `disabled`.
- Select: the error message precedence (error → Form message → copy.invalid) is written 'as Input', and `copy.required` only shows through Form validate(). Chose not to show copy.required in the region when `invalid` is set and nothing is selected, which Listbox does. The doc should state which one.

## 2026-09-21 13:21 — round 1

- Select: a11y.requires lists `target-44px` (size.target.comfortable) but the `minTargetSm` binding lowers the trigger's block-size floor to size.target.min at `size: sm` — the two directly contradict. Implemented the declared binding, so an `sm` trigger does not meet the 44px floor the a11y block asserts. The doc should say which wins (likely: target-44px applies at md, and sm is the documented exception for toolbar/calendar-header pickers).
- Select: the Behavior guidance says the error region's `role="alert"` and id sit on a wrapper with `data-part="errorMessage"` on the composed Text inside, but the same paragraph says errors work 'exactly as Input', and Input puts role, id and data-part all on the Text itself. Followed Select's own explicit sentence, which adds a `.ds-select__error` span that is not in the anatomy list. The doc should either add the wrapper to anatomy or drop the sentence and match Input.
- Select: `chevronReserve` is documented as `native: always` on web only, yet it is listed as overridable, so it gets a root hook that has no effect in popup mode — which collides with the overrides rule 'apply an override only where the binding is in effect'. Chose to always define the hook and read it only in the native wrap; an override passed in popup mode is silently inert. The doc should mark it native-only, as it already does in prose.
- Select: `popupOffset` is specified as 'the popup's block margin on both sides', which means a non-flipped popup also carries a bottom margin. Harmless for a `position: fixed` popup, but it makes the margin box taller than the visual gap, which matters if the flip test measures the popup's height rather than its resolved margin. Implemented the literal reading (`margin-block`) since the doc says the flip test reads the resolved margin.
- Select: the composition block gives the label Text both `weight: medium` (a static prop) and a `labelWeight` forward that resolves to font.weight.medium, and gives description/errorMessage both `size: sm` and a `helperSize` forward resolving to font.size.sm. Each value is expressed twice and the two would silently disagree if either token changed. Sent both, as the block lists both; the doc should say which is authoritative.
- Select: on web `native: auto` and `native: never` both render the popup, so the derived scenarios `renders-native-auto` and `renders-native-never` assert identical output and only `renders-native-always` is distinguishing. Not wrong, but two of the three native stories/tests carry no web signal.
- Select: `copy.done` is declared but, per the platform notes, is only rendered by the React Native phone sheet. Kept it in the web COPY constant unused, so the web build ships a string it never shows. The doc could scope the key to the platforms that render it.

## 2026-09-23 15:55 — round 1

- Select: the rules say `ref` goes on the root, but the root is the field-group div and DatePicker/Form focus the control; I kept `ref` on the control (the trigger button, or the <select> with `native: always`), typed `Ref<HTMLButtonElement | HTMLSelectElement>`. The doc should say which element the ref reaches.
- Select: valueColor has no --ds-select-* hook (it is realised by the value Text's tone), but the `native: always` <select> draws its own text and has no Text to carry a tone. I read `var(--color-foreground)` directly on `.ds-select__native`. The doc should either give the native text color a hook or bless reading the token there.
- Select: the Guidance says a toggled value lands 'in option order, with values that match no option appended', but the composed Listbox's inOptionOrder drops values that match no option. Select's own Enter toggle (multiple) appends them; a click or Space toggle through Listbox still drops them, so the three paths disagree when the value holds an unknown entry. Listbox's doc/impl needs the same rule.
- Select: `initialActiveValue` (selectedValues[0]) and a controlled `activeValue` are both passed, as the Guidance asks, but Listbox's `activeValue` wins over `initialActiveValue` whenever it is set, so `initialActiveValue` has no effect on web. The doc could drop it for web or say it is parity-only.
- Select: the doc gives no order for the rAF visible flip versus the first position measurement; I measure in the same layout effect (keyed on open and hydrated) and flip `--visible` a frame later, so the fade starts at the final position.
- Select: focusout with a null relatedTarget (the window losing focus, e.g. switching tabs) counts as 'focus moved outside' and closes the popup; the doc does not say whether a window blur should close it.
- Select: the doc does not say whether a Form's validate-on-blur should run when the trigger blurs while the popup is open; I skip validation while open (focus never leaves the trigger during a pick), and Tab closes first, then blurs.
- Select: `native: always` with `disabled` uses the real `disabled` attribute, which takes the <select> out of the tab order and conflicts with 'stays focusable'; I followed the platform note (real disabled) over the prop description.

## 2026-09-23 15:56 — round 2

- Select: the spec's Behavior section says bindings realised by a child's tone (valueColor, placeholderColor, descriptionText, errorText) 'declare no --ds-select-* hook', but the hooks gate (tools/check_hooks.ts) requires every locked binding to declare one and only exempts forwarded bindings. I declared all four on the root (the gate wins, as the Overrides section's 'locked closes the override API, not the styling hook' agrees). valueColor and placeholderColor are now read by the `native: always` <select> (text color, and placeholder color while nothing is selected, which fixes the round-1 gap where the native select read the token directly). descriptionText and errorText are declared but read by no rule, since the only element they style is the composed Text, which may not be restyled. The doc should drop the 'declare no hook' sentence and say which hooks are declaration-only.
- Select: chevron is locked and forwarded into the Icon's overrides.color; the gate treats it as a forwarded binding and exempts it, so it still has no hook, matching the spec. The doc should say forwarded locked bindings are the one exception, as the gate does.
- Select: the doc never says that minTargetSm needs its own hook read by the sm modifier (`--ds-select-min-target: var(--ds-select-min-target-sm)`) rather than the sm class setting the minTarget hook straight from the token; the gate requires it. The doc should state the pattern for size-keyed locked bindings.
- Select: the doc does not say how the native single select shows its placeholder color; I added a `ds-select__native--placeholder` modifier set while nothing is selected, and reset <option> text to valueColor so the list itself is not muted.
- Select: the hooks gate also fails for 49 other components, including Select (lit), which is outside this job; `pnpm gates:hooks` exits 1 overall even though Select (web) is clean.

## 2026-09-23 19:20 — round 1

- Select: the guidance says bindings realised by a child's tone (valueColor, placeholderColor, descriptionText, errorText) and chevron 'declare no --ds-select-* hook', while the package convention says locked bindings keep their hook. I kept the hooks on the root for page CSS, the naming codemod and the native <select>, which reads value and placeholder color directly. The doc should say which rule wins.
- Select: the Behavior prose lists Listbox's `initialActiveValue` as the current selection, and the web notes also drive `activeValue` controlled. I pass both (`initialActiveValue` = first selected value, `activeValue` = state). The doc does not say which one wins on first paint.
- Select: the doc does not say when Form validation runs relative to the value write for a trigger that stays focused, or whether blur validation should be skipped while the popup is open. I skip blur validation while open, and Tab commits and closes before the blur fires.
- Select: `ref` type is unspecified because the root is a wrapper div but the control is a button or select. I chose `ref` reaches the control (trigger button, or native <select>); the doc should state what `ref` targets.
