// Generated by tools/behavior_tests.ts from the `behavior` block of the Container doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Container } from '../../packages/rn/src/Container';
import type { ContainerProps } from '../../packages/rn/src/Container';
import meta from '../../packages/rn/src/Container.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<ContainerProps> = {}) {
  const events = {};
  const props: ContainerProps = { ...(meta.args as ContainerProps), ...given };
  const tree = (p: ContainerProps) => (
    <ThemeProvider mode="light">
      <Container {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Container') ?? screen.UNSAFE_root,
    column: () => s.root(),
    rerender: (next: Partial<ContainerProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Container', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-width-prose', () => {
    const s = setup({"width": "prose"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-width-content', () => {
    const s = setup({"width": "content"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-width-page', () => {
    const s = setup({"width": "page"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-width-full', () => {
    const s = setup({"width": "full"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gutter-narrow', () => {
    const s = setup({"gutter": "narrow"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gutter-default', () => {
    const s = setup({"gutter": "default"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gutter-wide', () => {
    const s = setup({"gutter": "wide"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gutter-none', () => {
    const s = setup({"gutter": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-center', () => {
    const s = setup({"align": "center"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-start', () => {
    const s = setup({"align": "start"});
    expect(s.root()).toBeTruthy();
  });
});
