// Generated by tools/behavior_tests.ts from the `behavior` block of the Text doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Text } from '../../packages/rn/src/Text';
import type { TextProps } from '../../packages/rn/src/Text';
import meta from '../../packages/rn/src/Text.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<TextProps> = {}) {
  const events = {};
  const props: TextProps = { ...(meta.args as TextProps), ...given };
  const tree = (p: TextProps) => (
    <ThemeProvider mode="light">
      <Text {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Text') ?? screen.UNSAFE_root,
    text: () => s.root(),
    rerender: (next: Partial<TextProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Text', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-xs', () => {
    const s = setup({"size": "xs"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-sm', () => {
    const s = setup({"size": "sm"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-md', () => {
    const s = setup({"size": "md"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-lg', () => {
    const s = setup({"size": "lg"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-xl', () => {
    const s = setup({"size": "xl"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-weight-regular', () => {
    const s = setup({"weight": "regular"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-weight-medium', () => {
    const s = setup({"weight": "medium"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-weight-semibold', () => {
    const s = setup({"weight": "semibold"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-weight-bold', () => {
    const s = setup({"weight": "bold"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-default', () => {
    const s = setup({"tone": "default"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-strong', () => {
    const s = setup({"tone": "strong"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-muted', () => {
    const s = setup({"tone": "muted"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-danger', () => {
    const s = setup({"tone": "danger"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-on-action', () => {
    const s = setup({"tone": "onAction"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-start', () => {
    const s = setup({"align": "start"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-center', () => {
    const s = setup({"align": "center"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-end', () => {
    const s = setup({"align": "end"});
    expect(s.root()).toBeTruthy();
  });
});
