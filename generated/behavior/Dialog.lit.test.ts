// Generated by tools/behavior_tests.ts from the `behavior` block of the Dialog doc. Do not edit.
import { beforeEach, describe, expect, test, vi } from 'vitest';
import { userEvent } from 'vitest/browser';
import '../../packages/lit/src/Dialog.js';
import meta from '../../packages/lit/src/Dialog.stories.js';

function deep(root: ParentNode, selector: string): Element | null {
  const direct = root.querySelector(selector);
  if (direct) return direct;
  for (const el of Array.from(root.querySelectorAll('*'))) {
    const shadow = (el as HTMLElement).shadowRoot;
    if (shadow) {
      const found = deep(shadow, selector);
      if (found) return found;
    }
  }
  return null;
}

/** Every element that is "active" from the document down through shadow roots: the host, then the inner one.
 *  document.activeElement alone is a shadow host while focus sits inside its shadow tree. */
function activeChain(): Element[] {
  const chain: Element[] = [];
  let el: Element | null = document.activeElement;
  while (el) {
    chain.push(el);
    el = el.shadowRoot?.activeElement ?? null;
  }
  return chain;
}

async function setup(given: Record<string, unknown> = {}) {
  const el = document.createElement('ds-dialog');
  const props = { ...meta.args, ...given };
  for (const [key, value] of Object.entries(props)) {
    if (value === undefined) continue;
    // `children` is slotted light-DOM content, and `Element.children` is getter-only:
    // assigning it throws and takes the whole file down at setup. Slot a string into the
    // default slot the way the stories do; a richer value cannot be expressed here.
    if (key === 'children') {
      if (typeof value === 'string') el.append(value);
      continue;
    }
    (el as unknown as Record<string, unknown>)[key] = value;
  }
  const events = {
    onClose: vi.fn(),
    onOpened: vi.fn(),
  };
  el.addEventListener('close', events.onClose as unknown as EventListener);
  el.addEventListener('opened', events.onOpened as unknown as EventListener);
  document.body.append(el);
  await (el as unknown as { updateComplete: Promise<boolean> }).updateComplete;
  const root: ParentNode = el.shadowRoot ?? el;  // some elements render in light DOM
  const s = {
    el,
    root,
    events,
    props,
    root_: () => el,
    scrim: () => (deep(root, '[role="dialog"]') ?? deep(root, '[part="scrim"]') ?? deep(root, '[data-part="scrim"]') ?? root.firstElementChild) as HTMLElement,
    closeButton: () => (deep(root, '[part="closeButton"]') ?? deep(root, '[data-part="closeButton"]')) as HTMLElement,
  };
  return s;
}

beforeEach(() => {
  document.body.replaceChildren();
});

describe('ds-dialog', () => {
  test('close-button-fires-on-close', async () => {
    const s = await setup({"open": true});
    await userEvent.click(s.closeButton());
    expect(s.events.onClose).toHaveBeenCalled();
  });
  test('non-dismissible-still-reports-escape', async () => {
    const s = await setup({"open": true, "dismissible": false});
    s.el.focus();
    await userEvent.keyboard('{Escape}');
    expect(s.events.onClose).toHaveBeenCalled();
  });
  test('non-dismissible-scrim-click-does-nothing', async () => {
    const s = await setup({"open": true, "dismissible": false});
    await userEvent.click(s.scrim());
    expect(s.events.onClose).not.toHaveBeenCalled();
  });
  test('initial-focus-lands-on-the-close-button', async () => {
    const s = await setup({"open": true, "initialFocus": "close"});
    expect(activeChain()).toContain(s.closeButton());
  });
  test('hidden-heading-is-still-the-accessible-name', async () => {
    const s = await setup({"open": true, "hideHeading": true});
    expect(s.scrim()).toHaveAccessibleName(s.props.heading);
  });
  test('closed-dialog-renders-nothing', async () => {
    const s = await setup({"open": false});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(false);
  });
  test('renders', async () => {
    const s = await setup({"open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-size-sm', async () => {
    const s = await setup({"size": "sm", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-size-md', async () => {
    const s = await setup({"size": "md", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-size-lg', async () => {
    const s = await setup({"size": "lg", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-initial-focus-first', async () => {
    const s = await setup({"initialFocus": "first", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-initial-focus-title', async () => {
    const s = await setup({"initialFocus": "title", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-initial-focus-close', async () => {
    const s = await setup({"initialFocus": "close", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('has-accessible-name', async () => {
    const s = await setup({"open": true});
    expect(s.scrim()).toHaveAccessibleName(s.props.heading);
  });
  test('escape-fires-on-close', async () => {
    const s = await setup({"open": true});
    s.el.focus();
    await userEvent.keyboard('{Escape}');
    expect(s.events.onClose).toHaveBeenCalled();
  });
});
