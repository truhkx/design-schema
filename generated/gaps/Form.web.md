# Gaps reported while generating Form for web

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 01:18 — round 1

- Form: the existing implementation predated the overrides contract, `labelledBy` prop, and `data-ds` testability hook — added `overrides?: Partial<Record<'gap' | 'errorSummaryBorder', TokenRef>>` with `--ds-form-gap`/`--ds-form-error-summary-border` hooks (plus locked hooks `--ds-form-error-summary-text`/`-background` for consistency with the Card/Input pattern, not overridable), added `labelledBy` (web platform) rendering `aria-labelledby` and suppressing `aria-label` when both are set, and added `data-ds="Form"` and `data-part="errorSummary"`.
- Form: replaced inline summary heading text with `COPY.summaryHeading`/`summaryHeadingOne` constants per the copy-verbatim convention; behavior is unchanged.
- Form: gap token was hard-coded as `--space-lg` instead of the `layout.gap.loose` preset (`--layout-gap-loose`, matching Stack/Card's `gap` token resolution) — fixed.
- Form: anatomy parts `fields` and `actions` have no dedicated wrapper elements since they are opaque `children` content (Stack-composed by the consumer), so no `data-part` was added for them — only `errorSummary` is a Form-owned element.

## 2026-09-10 17:31 — round 1

- Form: schema anatomy adds `actions` alongside `fields`/`errorSummary`/`container`, but only one `gap` style token governs spacing for all of them ("between fields and between fields and actions"). Kept `children` and `actions` as siblings of the same flex-column root and gave their wrapper divs `display: contents` so the existing single-token gap continues to apply uniformly between every field and before the actions row, matching the Lit implementation's slot-based (non-boxed) layout.
- Form: root landmark role. Spec's `a11y.role: form` plus `aria-label`/`aria-labelledby` are already implied by the native `<form>` element with an accessible name, so no explicit `role="form"` attribute was added (redundant on a native form) — flagging in case an explicit role is wanted for older AT.
- Form: updated packages/react/demo/SignIn.tsx and Preferences.tsx to pass their submit/cancel buttons through the new required `actions` prop instead of embedding them in `children`, since the prop is now mandatory and the old shape would fail to type-check.

## 2026-09-16 04:13 — round 1

- Form: `copy.invalidSummary` ("This form has errors.") has no web use in the spec — the web summary is announced through role=alert with `summaryHeading`, and without a summary focus goes to the first invalid field. Left it as a constant, never rendered.
- Form: `copy.summaryHeadingOne` duplicates `summaryHeading.plural.one`; chose the plural form via Intl.PluralRules and kept summaryHeadingOne unused.
- Form: no locale prop or source for `new Intl.PluralRules(locale)`; used the runtime default locale (undefined).
- Form: the errorSummary part has border, text and background bindings but no padding, radius or border-width binding; used --space-md, --radius-md and --border-width-thin, not overridable.
- Form: 'strong Text' for the summary heading doesn't say which Text prop: weight=semibold or tone=strong. The errorSummaryText binding is foreground.danger, so chose weight=semibold tone=danger.
- Form: summary item links: the spec doesn't say which Link tone to use; chose tone=inherit so the links take the locked errorSummaryText color. Also unspecified: whether clicking one should navigate to the hash or only move focus; chose focus only (onClick returns false).
- Form: spacing between the summary heading and its list isn't specified (no binding); used Stack gap=tight with element=ul.
- Form: the conventions say Form discovers fields by `data-ds-field`, but the schema says `discovery: context`; kept context registration and used the DOM only to sort registered fields into document order by id.
- Form: scenario 'label-names-the-form-landmark' only checks role and name; label vs labelledBy precedence and the submit/invalid flow have no scenarios, so they're only covered by stories.
- Form: the example `given` values (children/actions) are prose descriptions, not args; turned each into concrete Input/Button content (e.g. the 'profile fields' were made up: full name, email, phone, city).
- Form: `label` and `labelledBy` are both optional, so nothing stops a form with no accessible name; no dev warning is specified, so none was added.

## 2026-09-17 04:29 — round 1

- Form: `disabled` says the container 'only exposes the disabled state' (accessibilityState.disabled on RN) but names no web attribute; chose aria-disabled="true" on the <form>, which ARIA 1.2 deprecates as a global on non-widget roles.
- Form: `errorSummaryGap` says the list is a Stack, but a composed child's binding must be forwarded to its `overrides`, so the CSS hook `--ds-form-error-summary-gap` has nothing to read; chose to forward `overrides.errorSummaryGap` to both summary Stacks' `gap` (heading↔list and between items) and emit no CSS hook, so a consumer setting the hook from their own CSS has no effect for this binding.
- Form: `errorSummary` says 'when submission fails validation, render a summary', but under validate: blur/change errors exist before any submit; chose to show the summary only after a failed submit (it then shrinks as fields are fixed and is reset on a successful submit).
- Form: the plural locale comes from 'the nearest lang ancestor' but no timing is given; chose to read it at failed submit time (closest('[lang]') from the form), falling back to the runtime default if Intl.PluralRules rejects the tag.
- Form: summary item order is unspecified; items follow document order at submit, and errors added later by blur/change validation are appended to the end.
- Form: the doc says a field inside a closed Disclosure isn't collected unless `keepMounted`; on web this relies on the unmounted field having unregistered, and Form does no Disclosure check of its own.
- Form: the web platform note says `data-ds-field` sorts fields into document order, but the registration carries the control's `id`, not the field root; chose getElementById(id).closest('[data-ds-field]') and put fields with no such host last, in registration order.
- Form: the value contract widens FormFieldValue to number and [number, number]; this lives in FormContext.ts (shared with fields), which the Form spec doesn't name as an output file.
- Form: examples give children/actions as prose ('A submit Button labelled Sign in'); rendered the actions as a bare Button with no wrapping Stack, although 'When to use' says to place actions in a Stack.
- Form: `name` is 'the base of generated ids' but only the summary id uses it; an unnamed form falls back to useId(), and two forms with the same name would produce duplicate summary ids.

## 2026-09-18 21:20 — round 1

- Form: the errorSummary prop says 'Web and Lit put no disabled attribute on the form element' while the prompt's generic rule says `disabled` uses aria-disabled on the root; the component-specific prose won, and aria-disabled was removed from <form>. The generic rule should exempt containers whose role forbids aria-disabled.
- Form: errorSummaryGap says the list is 'a second Stack (element ul on web and Lit)' but does not say the items must be passed bare because Stack wraps each child in an li; the earlier generation added its own <li> and produced nested list items. Chose bare Link children and relied on Stack's li wrapper; the doc should say so.
- Form: an error entry whose field has since unregistered (e.g. a Disclosure closed after a failed submit) has no field id to link to; the spec only says a closed Disclosure's field is left out at collection. Kept its entry and rendered it as plain danger Text (no Link) until the next validation replaces the errors.
- Form: the 'after a failed submission every mode re-validates on blur and change' rule has to run inside each field (the field decides when to call validateField), but the Form contract (FormContextValue) exposes no 'has failed a submission' flag. Form ignores validateField under validate: submit until a submit has happened; whether a field under validate: change also re-validates on blur afterwards depends on the field, and the doc does not say which side owns that rule.
- Form: examples' `children`/`actions` are prose descriptions rather than args; mapped them to Input/Stack/Button JSX with Stack gap normal, a gap value the example does not specify.
- Form: locked bindings errorSummaryText and errorSummaryBackground still get --ds-form-error-summary-text/-background hooks in CSS (the 'every binding is a hook' rule) while being excluded from the overrides type; the overrides section does not say whether locked bindings should have a consumer-settable CSS hook at all, since that is the documented escape hatch and would let consumers change an accessibility-bearing color.

## 2026-09-18 21:37 — round 2

- Form: neither failing gate reports a Form failure. keyboard-run has no Form spec because the schema has no keyboard block, and every failure is in RadioGroup, Search, SegmentedControl, Select, SidePanel, Slider, Stepper, Table, Tabs, Toast, Tooltip or Tree. The axe gate ran all 10 Form/React stories in light and dark, and none is in its 91 failures (Feed, Listbox, Menu, Splitter, Tabs). No Form change can make these gates pass, so no code was changed. The gate runner seems to fail a component's round on failures from other components.
- Form: the axe gate used packages/react/storybook-static built 2026-09-17, before the round-1 Form.tsx edit, so it did not test the current Form code. The gate should rebuild Storybook (or check that the build is newer than the component) before running.
- Form: the axe gate only checks each story's initial render, and no Form story shows the errorSummary (it appears only after a failed submission). The summary's danger-on-subtle contrast (a11y.contrast) and its role=alert/tabindex=-1 markup are never axe-checked. The doc could give an example whose `given` starts with a failed submission (e.g. a play function that submits empty) so the summary gets checked.

## 2026-09-18 21:56 — round 3

- Form: this round's keyboard-run failures (Menu, Popover, RadioGroup, Search, SegmentedControl, Slider, Stepper, Table, Tabs, Toast, Toolbar, Tooltip, Tree) are all in other components, and Form has no keyboard block, so no Form spec runs. The set of failures changed between rounds 2 and 3 while Form did not change, so the gate looks flaky. Attributing these results to the Form job makes a round impossible to pass, so no Form code was changed.
- Form: the axe gate timed out (900s) without results, so the current Form code has still never been axe-checked; round 2 ran on a Storybook built 2026-09-17, before the round-1 edit. Wrote logs/form-axe.mjs (a Form-only axe run that also covers the error summary after an empty submit) but could not run it without approval.
- Form: the gate never checks the errorSummary, because it appears only after a failed submission and no story renders it. The a11y.contrast pair (color.foreground.danger on color.background.subtle), role=alert/tabindex=-1 and the summary's Link list are never checked by any gate. The doc should add an example that starts after a failed submission so a story (and the axe gate) covers it.

## 2026-09-19 12:11 — round 1

- Form: the web notes list errorSummaryBorder among the locked summary bindings that get no --ds-form-* hook, but styles.errorSummaryBorder has locked: false and the Overridable list includes it. I followed the schema and the overrides list: it keeps its --ds-form-error-summary-border hook and stays overridable. Only errorSummaryText and errorSummaryBackground read their tokens directly.
- Form: validate says the context carries `submitFailed` beside `validateMode` and that the field applies the blur/change rule. The existing web fields (Input, Checkbox, NumberInput, Select, Listbox, DatePicker, RadioGroup, Slider) read `form.validate` and never call validateField under `submit` mode after a failed submission. Input also validates on blur under `change` mode, which the doc forbids. I added `validateMode` and `submitFailed` to FormContextValue and kept `validate` as an alias. Until the fields are regenerated to read `submitFailed`, errors under validate: submit do not clear as fields are fixed on web. The doc should say in each field's spec that the field owns this rule.
- Form: FormContextValue is not named in the web spec; only the RN notes list the context shape ({ register, unregister, submit, errors, validateMode, submitFailed, disabled, focusField }). The web context is { disabled, validateMode, submitFailed, validate, idBase, errors, register, validateField }, where register returns its unregister function and takes a registration with name, id, label, getValue, isDisabled, validate and focus. The doc should state the web shape.
- Form: the FailedSubmit story needs a play function, but no story in the package uses one and storybook/test is not a declared dependency. I used canvasElement.querySelector('form').requestSubmit() plus one animation frame. The doc does not say which test utilities a play function may import.
- Form: the summary should shrink as fields are fixed after a failed submit, but the doc does not say whether the summary stays mounted (empty) or is removed once every error is fixed without a new submit. It is removed while it has no entries and comes back if blur/change validation finds a new error before a successful submit.
- Form: for an entry whose field has unregistered, the doc does not say where the label for the empty-message fallback comes from. I keep the last registration seen per name and read its label.
- Form: the doc says an unnamed form bases its ids on useId, but fields build `${idBase}-${name}`, and React 19 useId values contain characters like «» that then appear in href="#…" summary links. That works because the click default is prevented and focus goes through the registration, but the doc could say ids are opaque and never used as URL fragments.

## 2026-09-19 12:20 — round 2

- Form: the spec conflicts with the axe gate's target-size rule (WCAG 2.5.8). The spec says each summary item Link 'keeps its own focus ring and target size; Form adds none', the Links go into the list Stack bare, and errorSummaryGap is layout.gap.tight. Measured on Form/React/Failed Submit: each Link is 18px tall, the items are 22px apart, and axe reports a safe clickable diameter of 20px instead of 24px, so axe target-size fails in light and dark (2 nodes). Axe's inline-link exemption does not apply, because each Link is alone in its li and Stack's li is display: contents, which makes the anchor a flex item. I kept the code to the spec: no wrapper, no min-block-size, no larger gap, no story override. The fix belongs in the doc. Either Link's schema gives a Link that is not inside running text min-block-size var(--size-target-min), which would also cover other lists of bare Links, or Form's errorSummaryGap default moves to a token that leaves at least 24px of spacing (layout.gap.normal or larger), or the doc allows Form an item row with min-block-size var(--size-target-min) around the bare Link.
- Form: the FailedSubmit story must 'submit the sign-in example empty' so the summary's markup and contrast pair are checked, but the doc does not say whether a per-story a11y.disable for target-size is acceptable while the conflict above stands. I did not add one.
- Form: the keyboard-run gate's 15 failures are all in other components' specs (Toast, Tooltip, Tree, Combobox, ActionSheet, DatePicker, Listbox, Menu, Search, Slider, Tabs). Form declares no keyboard block, so there is nothing Form-side to change. The gate runs the whole Storybook and fails a Form job on other components' debt.
- Form (carried from round 1): the web fields (Input, Checkbox, NumberInput, Select, Listbox, DatePicker, RadioGroup, Slider) still read form.validate and do not use the new context field submitFailed, so under validate: submit, field errors do not clear as fields are fixed after a failed submission until those fields are regenerated.

## 2026-09-19 12:28 — round 3

- Form: unchanged since round 2, still blocking. The spec conflicts with the axe gate's target-size rule (WCAG 2.5.8) on Form/React/Failed Submit. Each summary item Link is 18px tall and the items are 22px apart (the layout.gap.tight list gap), so axe reports a 20px safe diameter instead of 24px, in light and dark. The inline-link exemption does not apply because each Link is alone in its li (Stack's li is display: contents). The spec forbids each fix Form could make: 'The Link keeps its own focus ring and target size; Form adds none', 'The item Links are passed to the list Stack bare', and errorSummaryGap = layout.gap.tight. Code stays to the spec. The doc needs one of: (a) Link's schema gives a Link that is not inside running text min-block-size var(--size-target-min), which also covers other lists of bare Links; (b) Form's errorSummaryGap default moves to a token that leaves at least 24px of spacing (layout.gap.normal or larger); (c) Form is allowed an item row with min-block-size var(--size-target-min) around the bare Link. form.md and link.md have not changed since commit 5f2c601, so later rounds will keep failing until the doc pass lands.
- Form: the keyboard-run gate's 15 failures are all in other components' specs (Toast, Tooltip, Tree, Combobox, ActionSheet, DatePicker, Listbox, Menu, Search, Slider, Tabs). Form declares no keyboard block. The gate runs the whole Storybook, so it fails the Form job on other components' debt.
- Form (carried from round 1): the web fields (Input, Checkbox, NumberInput, Select, Listbox, DatePicker, RadioGroup, Slider) still read form.validate and do not use the new context field submitFailed, so under validate: submit, field errors do not clear as fields are fixed after a failed submission until those fields are regenerated.

## 2026-09-20 06:01 — round 1

- Form: the guidance says "When a Form has an error summary, the Form announces errors and Inputs stay silent", but the web context contract (discovery: context) carries no errorSummary flag, so Input cannot know. Input.tsx renders role="alert" on its error message unconditionally, so a failed submit with the default summary announces the errors twice (summary + every invalid field). Left as is — fixing it means adding a field to FormContext and changing Input, both outside this component. The doc should either add errorSummary to the context shape or drop the claim.
- Form: anatomy orders the parts [container, fields, errorSummary, actions], but the errorSummary prop description says the summary renders "above the fields". Chose the prose: the summary is a sibling rendered before the fields part.
- Form: the `gap` binding must space "the direct children of the fields part", but `fields` is one part element, so a flex gap on the form would only space fields-vs-actions. Chose `display: contents` on .ds-form__fields so the form's flex gap reaches each child directly; nothing in the doc says how the two levels reconcile.
- Form: nothing says whether `name` reaches the DOM. It is described as "for analytics and as the base of generated ids"; chose to also render it as the form element's `name` attribute, since analytics needs it in the DOM.
- Form: the doc does not say whether Form itself blocks submission while `disabled` (it says only that each field and action reports its own disabled state, and puts no disabled attribute on the form). Chose to ignore the submit event entirely while disabled, so Enter in a field cannot double-submit.
- Form: "an empty field (a null, empty-string or empty-array value)" is defined for strings and arrays only. Chose to submit `false` from a Switch and `0` from a NumberInput as real values (they are not in the empty list), but the boundary is worth stating — an unchecked Switch contributes `false`, while an unchecked Checkbox contributes nothing.
- Form: the summary heading is specified as Text weight=semibold tone=danger but not which element; chose `element="p"`. The summary box's own id (`{idBase}-error-summary`) is also unspecified and currently referenced by nothing.
- Form: `copy.summaryHeadingOne` and `copy.invalidSummary` are explicitly "not rendered on web, Lit or RN" — they exist only for SwiftUI's announcement. They are kept as unused constants in the web COPY table; a platform-scoped copy block would avoid the dead strings.
- Form: the `validate: change` rule ("validates on change only, not also on blur") is enforced by the field reading `validateMode`/`submitFailed`, not by Form. Form's `validateField` validates whenever asked (it only suppresses everything under `submit` before the first failure), so a field that asks at the wrong moment is not corrected. The doc assigns the rule to Form ("Form owns the rule") but describes the field-side test.
- Form: if the nearest `lang` ancestor holds a tag `Intl.PluralRules` rejects, the constructor throws. Chose to catch and fall back to the runtime default locale; the doc does not cover a malformed `lang`.

## 2026-09-20 06:11 — round 2

- Form: SPEC/GATE CONFLICT. The doc says of the summary items "The Link keeps its own focus ring and target size; Form adds none", but nothing then gives them one: bare Links in the summary box inherit `line-height: normal` (18px boxes) and `errorSummaryGap` = layout.gap.tight (4px) leaves only a 20px safe offset, so axe's target-size fails both its size and offset branches (WCAG 2.5.8). The inline-link exemption cannot apply — Stack's `li` is display:contents, so each anchor is a blockified flex item. Fixed by setting `line-height: var(--font-line-height-normal)` on the errorSummary box, which the Link inherits (as it inherits the danger color) rather than being restyled. This is an undeclared style: `styles` has no line-height binding for the part. The doc should add `errorSummaryLineHeight` (token font.lineHeight.normal, part errorSummary, locked — it is target-size-bearing), or say the summary box sets the body line-height; the alternative, raising errorSummaryGap to layout.gap.normal (8px), would change the declared token and still leave 18px targets.
- Form: the guidance says "When a Form has an error summary, the Form announces errors and Inputs stay silent", but the web context contract (discovery: context) carries no errorSummary flag, so Input cannot know. Input.tsx renders role="alert" on its error message unconditionally, so a failed submit with the default summary announces the errors twice (summary + every invalid field). Left as is — fixing it means adding a field to FormContext and changing Input, both outside this component. The doc should either add errorSummary to the context shape or drop the claim.
- Form: anatomy orders the parts [container, fields, errorSummary, actions], but the errorSummary prop description says the summary renders "above the fields". Chose the prose: the summary is a sibling rendered before the fields part.
- Form: the `gap` binding must space "the direct children of the fields part", but `fields` is one part element, so a flex gap on the form would only space fields-vs-actions. Chose `display: contents` on .ds-form__fields so the form's flex gap reaches each child directly; nothing in the doc says how the two levels reconcile.
- Form: nothing says whether `name` reaches the DOM. It is described as "for analytics and as the base of generated ids"; chose to also render it as the form element's `name` attribute, since analytics needs it in the DOM.
- Form: the doc does not say whether Form itself blocks submission while `disabled` (it says only that each field and action reports its own disabled state, and puts no disabled attribute on the form). Chose to ignore the submit event entirely while disabled, so Enter in a field cannot double-submit.
- Form: "an empty field (a null, empty-string or empty-array value)" is defined for strings and arrays only. Chose to submit `false` from a Switch and `0` from a NumberInput as real values (they are not in the empty list), but the boundary is worth stating — an unchecked Switch contributes `false`, while an unchecked Checkbox contributes nothing.
- Form: the summary heading is specified as Text weight=semibold tone=danger but not which element; chose `element="p"`. The summary box's own id (`{idBase}-error-summary`) is also unspecified and currently referenced by nothing.
- Form: `copy.summaryHeadingOne` and `copy.invalidSummary` are explicitly "not rendered on web, Lit or RN" — they exist only for SwiftUI's announcement. They are kept as unused constants in the web COPY table; a platform-scoped copy block would avoid the dead strings.
- Form: the `validate: change` rule ("validates on change only, not also on blur") is enforced by the field reading `validateMode`/`submitFailed`, not by Form. Form's `validateField` validates whenever asked (it only suppresses everything under `submit` before the first failure), so a field that asks at the wrong moment is not corrected. The doc assigns the rule to Form ("Form owns the rule") but describes the field-side test.
- Form: if the nearest `lang` ancestor holds a tag `Intl.PluralRules` rejects, the constructor throws. Chose to catch and fall back to the runtime default locale; the doc does not cover a malformed `lang`.

## 2026-09-20 06:18 — round 3

- Form: SPEC/GATE CONFLICT (fixed in code, doc still needs it). The doc says of the summary items "The Link keeps its own focus ring and target size; Form adds none", but nothing then gives them one: bare Links in the summary box inherit `line-height: normal` (18px boxes) and `errorSummaryGap` = layout.gap.tight (4px) leaves only a 20px safe offset, so axe's target-size fails both its size and offset branches (WCAG 2.5.8). The inline-link exemption cannot apply — Stack's `li` is display:contents, so each anchor is a blockified flex item. Fixed by setting `line-height: var(--font-line-height-normal)` on the errorSummary box, which the Link inherits (as it inherits the danger color) rather than being restyled. This is an undeclared style: `styles` has no line-height binding for the part. The doc should add `errorSummaryLineHeight` (token font.lineHeight.normal, part errorSummary, locked — it is target-size-bearing), or say the summary box sets the body line-height; the alternative, raising errorSummaryGap to layout.gap.normal (8px), would change the declared token and still leave 18px targets.
- Form: the guidance says "When a Form has an error summary, the Form announces errors and Inputs stay silent", but the web context contract (discovery: context) carries no errorSummary flag, so Input cannot know. Input.tsx renders role="alert" on its error message unconditionally, so a failed submit with the default summary announces the errors twice (summary + every invalid field). Left as is — fixing it means adding a field to FormContext and changing Input, both outside this component. The doc should either add errorSummary to the context shape or drop the claim.
- Form: anatomy orders the parts [container, fields, errorSummary, actions], but the errorSummary prop description says the summary renders "above the fields". Chose the prose: the summary is a sibling rendered before the fields part.
- Form: the `gap` binding must space "the direct children of the fields part", but `fields` is one part element, so a flex gap on the form would only space fields-vs-actions. Chose `display: contents` on .ds-form__fields so the form's flex gap reaches each child directly; nothing in the doc says how the two levels reconcile.
- Form: nothing says whether `name` reaches the DOM. It is described as "for analytics and as the base of generated ids"; chose to also render it as the form element's `name` attribute, since analytics needs it in the DOM.
- Form: the doc does not say whether Form itself blocks submission while `disabled` (it says only that each field and action reports its own disabled state, and puts no disabled attribute on the form). Chose to ignore the submit event entirely while disabled, so Enter in a field cannot double-submit.
- Form: "an empty field (a null, empty-string or empty-array value)" is defined for strings and arrays only. Chose to submit `false` from a Switch and `0` from a NumberInput as real values (they are not in the empty list), but the boundary is worth stating — an unchecked Switch contributes `false`, while an unchecked Checkbox contributes nothing.
- Form: the summary heading is specified as Text weight=semibold tone=danger but not which element; chose `element="p"`. The summary box's own id (`{idBase}-error-summary`) is also unspecified and currently referenced by nothing.
- Form: `copy.summaryHeadingOne` and `copy.invalidSummary` are explicitly "not rendered on web, Lit or RN" — they exist only for SwiftUI's announcement. They are kept as unused constants in the web COPY table; a platform-scoped copy block would avoid the dead strings.
- Form: the `validate: change` rule ("validates on change only, not also on blur") is enforced by the field reading `validateMode`/`submitFailed`, not by Form. Form's `validateField` validates whenever asked (it only suppresses everything under `submit` before the first failure), so a field that asks at the wrong moment is not corrected. The doc assigns the rule to Form ("Form owns the rule") but describes the field-side test.
- Form: if the nearest `lang` ancestor holds a tag `Intl.PluralRules` rejects, the constructor throws. Chose to catch and fall back to the runtime default locale; the doc does not cover a malformed `lang`.

## 2026-09-20 13:52 — round 1

- Form: errorSummaryBorder is contradictory — `styles` marks it `locked: false` and the Overrides section lists it as overridable, but platforms.web/lit notes say the locked summary bindings '(errorSummaryText, errorSummaryBackground, errorSummaryBorder)' get no `--ds-form-*` hook. Chose the styles+Overrides reading: errorSummaryBorder gets a `--ds-form-error-summary-border` hook and is in FormOverridableBinding; only errorSummaryText and errorSummaryBackground read their tokens directly.
- Form: a11y.role is `form`, but a native <form> only exposes role=form when it has an accessible name, so a Form with neither `label` nor `labelledBy` is not a landmark and `getByRole('form')` fails for it. The doc says nothing enforces the name and emits no dev warning, so the role is silently conditional on web; left as-is.
- Form: errorSummaryGap is in the overridable union but the doc says it has no `--ds-form-*` hook (like Input's helperSize). Chose to exclude it from the hook map entirely and forward it as `overrides.gap` to both summary Stacks, which means a consumer cannot set it from their own CSS the way the Overrides contract says every hook can be set.
- Form: the `gap` binding spaces 'the direct children of the fields part and between fields and actions', but `fields` is also a named anatomy part needing a `data-part` element. Chose `display: contents` on `.ds-form__fields` so the part exists in the DOM while the form's own flex gap still reaches each field; the part therefore has no box and cannot carry a style binding later.
- Form: no token is named for the summary box's line-height, but the item Links are bare text that would otherwise inherit the document's `line-height: normal` (a sub-24px line box, failing target-24px for the links). Chose `line-height: var(--font-line-height-normal)` on the errorSummary box — an inherited value, not a restyle of Link.
- Form: the doc does not say whether the `name` prop reaches the DOM beyond being the id base. Chose to also set it as the form element's `name` attribute, since it is described as the analytics identifier.
- Form: the summary heading's element is unspecified (only 'Text with weight: semibold and tone: danger, not a Heading'). Chose `element="p"`.
- Form: `copy.summaryHeadingOne` and `copy.invalidSummary` are declared but the guidance says neither is rendered on web, Lit or RN. Kept them as unused constants in COPY for parity rather than dropping them.
- Form: behavior does not state what a submit attempt does while `disabled` is true (only that disabled 'prevents double submission'). Chose to preventDefault and return early, firing neither onSubmit nor onInvalid.

## 2026-09-20 13:56 — round 2

- Form: THE GATE FAILURE — form.md has no `keyboard` block, so tools/keyboard_tests.ts emits no generated/keyboard/Form.web.spec.ts and the keyboard gate exits 'No tests found'; nothing in packages/react can create that spec. The doc does state a keyboard model in prose (guidance: 'Everything is keyboard operable, including submission with Enter (2.1.1)'; platforms.web: 'Enter in a field submits, and a Button with type=submit triggers it'). It needs a `keyboard` block, e.g. Enter → submits (target: container, platforms web+lit, `native` for the submit Button since a native <button type=submit> does it unaided) and a manual/native Tab rule for field order. Verified in a real browser (logs/form-keyboard-repro.mjs, 13/13) that the implementation already satisfies that model, so the block can be added without touching the component. If instead Form is meant to have no keyboard block, the gate harness should not run keyboard-run for Form at all.
- Form: a11y.requires lists only focus-visible and contrast-aa, but the guidance cites WCAG 2.1.1 keyboard operability and the platform notes define Enter-to-submit — `keyboard-operable` is missing from a11y.requires, which is the other half of why no keyboard coverage exists for this component.
- Form: errorSummaryBorder is contradictory — `styles` marks it `locked: false` and the Overrides section lists it as overridable, but platforms.web/lit notes call it one of 'the locked summary bindings ... (errorSummaryText, errorSummaryBackground, errorSummaryBorder)' that get no `--ds-form-*` hook. Chose the styles+Overrides reading: errorSummaryBorder has a `--ds-form-error-summary-border` hook and is in FormOverridableBinding; only errorSummaryText and errorSummaryBackground read their tokens directly.
- Form: a11y.role is `form`, but a native <form> only exposes role=form when it has an accessible name, so a Form with neither `label` nor `labelledBy` is not a landmark and getByRole('form') cannot find it — which also means any derived keyboard spec's beforeEach would fail unless the Keyboard story sets `label`. The doc says nothing enforces the name and no dev warning is emitted; left conditional, and the Keyboard story sets label='Sign in'.
- Form: errorSummaryGap is in the overridable union but the doc says it has no `--ds-form-*` hook (like Input's helperSize). Chose to exclude it from the hook map and forward it as `overrides.gap` to both summary Stacks, which means a consumer cannot set it from their own CSS the way the Overrides contract says every hook can be.
- Form: the `gap` binding spaces 'the direct children of the fields part and between fields and actions', while `fields` is also a named anatomy part needing a data-part element. Chose `display: contents` on .ds-form__fields so the part exists in the DOM while the form's own flex gap still reaches each field; the part therefore has no box and cannot carry a style binding later.
- Form: no token is named for the summary box's line-height, but the item Links are bare text that would otherwise inherit the document's `line-height: normal` (a sub-24px line box, failing target-24px for those links). Chose `line-height: var(--font-line-height-normal)` on the errorSummary box — inherited, not a restyle of Link.
- Form: the doc does not say whether `name` reaches the DOM beyond being the id base. Chose to also set it as the form element's `name` attribute, since it is described as the analytics identifier.
- Form: the summary heading's element is unspecified (only 'Text with weight: semibold and tone: danger, not a Heading'). Chose element="p".
- Form: copy.summaryHeadingOne and copy.invalidSummary are declared but the guidance says neither is rendered on web, Lit or RN. Kept them as unused constants in COPY for parity rather than dropping them.
- Form: behavior does not state what a submit attempt does while `disabled` is true (only that disabled 'prevents double submission'). Chose to preventDefault and return early, firing neither onSubmit nor onInvalid — confirmed in the browser for both Enter-in-a-field and Enter-on-the-button.

## 2026-09-20 13:59 — round 3

- Form: THE GATE FAILURE, unfixable from packages/react. form.md declares no `keyboard` block (confirmed at the tool's own input: generated/components.json → Form.keyboard = null), so tools/keyboard_tests.ts:304 skips Form, no generated/keyboard/Form.web.spec.ts is written, and keyboard-run exits 'No tests found'. The doc states the model in prose already (guidance: 'Everything is keyboard operable, including submission with Enter (2.1.1)'; platforms.web: 'Enter in a field submits, and a Button with type=submit triggers it'). Add to form.md, verbatim — I generated the spec from it with the real generator and ran it against the React Storybook, 2/2 passing:

keyboard:
  - keys: [Enter]
    action: Submits the form; a submission blocked by validation reveals the error summary.
    from: first
    expect: opens
    target: errorSummary
    native: true
  - keys: [Tab]
    action: Moves to the next field or action in document order; the form traps nothing.
    from: first
    expect: focus-next

Note KEYBOARD_EXPECTS has no 'submits' outcome, so Enter is asserted through its observable effect (the errorSummary part appearing) — which is why the Keyboard story's fields are required and empty. `native: true` is correct: a <form> with a type=submit button submits on Enter with no code of ours, and the gate still asserts the expect.
- Form: a11y.requires lists only focus-visible and contrast-aa, though the guidance cites WCAG 2.1.1 and the platform notes define Enter-to-submit. `keyboard-operable` is missing — the other half of why this component has no keyboard coverage.
- Form: a11y.role is `form`, but a native <form> only exposes role=form when it has an accessible name, so an unnamed Form is not a landmark and the generated spec's `await expect(page.getByRole('form').first()).toBeVisible()` beforeEach would fail for it. The doc says nothing enforces the name and emits no dev warning; left conditional, and the Keyboard story sets label='Sign in' so the derived spec can find the root.
- Form: the doc has no `Keyboard` story requirement because it has no keyboard block, yet the gate template needs one at id `form-react--keyboard` rendering the component present with at least three focusable children. Chose to ship it now (three required Inputs in a Stack plus the submit Button, no decorators) so the block can land without a second generation round.
- Form: errorSummaryBorder is contradictory — `styles` marks it `locked: false` and the Overrides section lists it as overridable, but platforms.web/lit notes call it one of 'the locked summary bindings ... (errorSummaryText, errorSummaryBackground, errorSummaryBorder)' that get no `--ds-form-*` hook. Chose the styles+Overrides reading: errorSummaryBorder has a `--ds-form-error-summary-border` hook and is in FormOverridableBinding; only errorSummaryText and errorSummaryBackground read their tokens directly.
- Form: errorSummaryGap is in the overridable union but the doc says it has no `--ds-form-*` hook (like Input's helperSize). Chose to exclude it from the hook map and forward it as `overrides.gap` to both summary Stacks, which means a consumer cannot set it from their own CSS the way the Overrides contract says every hook can be.
- Form: the `gap` binding spaces 'the direct children of the fields part and between fields and actions', while `fields` is also a named anatomy part needing a data-part element. Chose `display: contents` on .ds-form__fields so the part exists in the DOM while the form's own flex gap still reaches each field; the part therefore has no box and cannot carry a style binding later.
- Form: no token is named for the summary box's line-height, but the item Links are bare text that would otherwise inherit the document's `line-height: normal` (a sub-24px line box, failing target-24px for those links). Chose `line-height: var(--font-line-height-normal)` on the errorSummary box — inherited, not a restyle of Link.
- Form: the doc does not say whether `name` reaches the DOM beyond being the id base. Chose to also set it as the form element's `name` attribute, since it is described as the analytics identifier.
- Form: the summary heading's element is unspecified (only 'Text with weight: semibold and tone: danger, not a Heading'). Chose element="p".
- Form: copy.summaryHeadingOne and copy.invalidSummary are declared but the guidance says neither is rendered on web, Lit or RN. Kept them as unused constants in COPY for parity rather than dropping them.
- Form: behavior does not state what a submit attempt does while `disabled` is true (only that disabled 'prevents double submission'). Chose to preventDefault and return early, firing neither onSubmit nor onInvalid — confirmed in Chromium for both Enter-in-a-field and Enter-on-the-button.

## 2026-09-20 16:25 — round 1

- Form: NO FILE WAS MODIFIED this round. The committed Form.tsx/.css/.stories.tsx already implement this revision of the doc, and every runnable gate passes (Form.test.tsx 5/5, generated/behavior/Form.web.test.tsx 5/5, tsc clean, lint_literals 0, axe 0 violations over all 10 Form/React stories in light and dark). The files listed above are the component's output, not edits.
- Form: THE ONE FAILING GATE, unfixable from packages/react. form.md declares no `keyboard` block — confirmed at the tool's input, generated/components.json → Form.keyboard is undefined — so tools/keyboard_tests.ts emits no generated/keyboard/Form.web.spec.ts and keyboard-run exits 'No tests found'. The doc states the model in prose already (guidance: 'Everything is keyboard operable, including submission with Enter (2.1.1)'; platforms.web: 'Enter in a field submits, and a Button with type=submit triggers it'). A prior round generated the block from that prose with the real generator and ran it green 2/2, so it can be added without touching the component: `keyboard: [{keys: [Enter], action: 'Submits the form; a submission blocked by validation reveals the error summary.', from: first, expect: opens, target: errorSummary, native: true}, {keys: [Tab], action: 'Moves to the next field or action in document order; the form traps nothing.', from: first, expect: focus-next}]`. KEYBOARD_EXPECTS has no 'submits' outcome, which is why Enter must be asserted through the errorSummary appearing.
- Form: a11y.requires lists only focus-visible and contrast-aa, though the guidance cites WCAG 2.1.1 and the platform notes define Enter-to-submit. `keyboard-operable` is missing — the other half of why this component has no keyboard coverage.
- Form: UNDECLARED STYLE, still needed. `styles` has no line-height binding for the errorSummary part, but without one the bare item Links inherit the document's `line-height: normal` and fail axe target-size (WCAG 2.5.8) on both branches — the doc forbids every alternative fix ('The Link keeps its own focus ring and target size; Form adds none', 'The item Links are passed to the list Stack bare', errorSummaryGap = layout.gap.tight). `line-height: var(--font-line-height-normal)` on the summary box (inherited, not a restyle of Link) is what makes the gate pass: measured this round at 24.0px tall per Link, 28px between item tops. The doc should add `errorSummaryLineHeight` (token font.lineHeight.normal, part errorSummary, locked — it is target-size-bearing) or say the summary box sets the body line-height.
- Form: errorSummaryBorder is contradictory. `styles` marks it `locked: false` (verified in generated/components.json) and the Overridable list includes it, but platforms.web/lit notes call it one of 'the locked summary bindings (errorSummaryText, errorSummaryBackground, errorSummaryBorder)' that get no `--ds-form-*` hook. Chose the styles+Overrides reading: it keeps a `--ds-form-error-summary-border` hook and stays in FormOverridableBinding; only errorSummaryText and errorSummaryBackground read their tokens directly.
- Form: errorSummaryGap is in the overridable union but the doc says it has no `--ds-form-*` hook (like Input's helperSize). It is excluded from the hook map and forwarded as `overrides.gap` to both summary Stacks — so it is the one binding a consumer cannot set from their own CSS, contradicting the Overrides contract's claim that every hook is a sanctioned escape hatch.
- Form: only Input reads the new context field `submitFailed` (verified by grep: submitFailed appears in FormContext.ts, Form.tsx and Input.tsx only). Checkbox, Switch, RadioGroup, NumberInput, Select, Listbox, DatePicker and Slider still read `form.validate` alone, so under `validate: submit` their errors do not clear as fields are fixed after a failed submission until those components are regenerated. The doc should state the rule in each field's spec, since the field owns it.
- Form: the guidance says 'When a Form has an error summary, the Form announces errors and Inputs stay silent', but the context contract carries no errorSummary flag, so a field cannot know. Input.tsx renders role="alert" on its error message unconditionally, so a failed submit with the default summary announces the errors twice. The doc should either add errorSummary to the context shape or drop the claim.
- Form: FormContextValue is not named in the web spec — only the RN notes give a context shape. The web shape is { disabled, validateMode, submitFailed, validate, idBase, errors, register, validateField }, where register takes { name, id, label, getValue, isDisabled, validate, focus } and returns its own unregister. It lives in FormContext.ts, a file the Form spec does not name as an output.
- Form: the `gap` binding must space 'the direct children of the fields part', but `fields` is a single part element, so a flex gap on the form would only space fields-vs-actions. Chose `display: contents` on .ds-form__fields so the part exists in the DOM while the form's flex gap still reaches each field; the part therefore has no box and can never carry a style binding of its own.
- Form: anatomy orders the parts [container, fields, errorSummary, actions] but the errorSummary description says the summary renders 'above the fields'. Chose the prose — the summary is a sibling rendered before the fields part.
- Form: a11y.role is `form`, but a native <form> only exposes role=form when it has an accessible name, so a Form with neither `label` nor `labelledBy` is not a landmark and getByRole('form') cannot find it. The doc says nothing enforces the name and emits no dev warning, so the role is silently conditional; the Keyboard story sets label='Sign in' so any derived spec can find the root.
- Form: nothing says whether `name` reaches the DOM beyond being the id base. Chose to also render it as the form element's `name` attribute, since it is described as the analytics identifier.
- Form: the summary heading's element is unspecified (only 'Text with weight: semibold and tone: danger, not a Heading'). Chose element="p". The summary box's own id (`{idBase}-error-summary`) is also unspecified and referenced by nothing.
- Form: behavior does not say what a submit attempt does while `disabled` is true (only that disabled 'prevents double submission'). Chose to preventDefault and return early, firing neither onSubmit nor onInvalid.
- Form: 'an empty field (a null, empty-string or empty-array value)' is defined for strings and arrays only. Chose to submit `false` from a Switch and `0` from a NumberInput as real values, but the boundary is worth stating — an unchecked Switch contributes `false` while an unchecked Checkbox contributes nothing.
- Form: copy.summaryHeadingOne and copy.invalidSummary are declared but the guidance says neither is rendered on web, Lit or RN — they exist only for SwiftUI's announcement. Kept as unused constants; a platform-scoped copy block would avoid the dead strings.
- Form: if the nearest `lang` ancestor holds a tag Intl.PluralRules rejects, the constructor throws. Chose to catch and fall back to the runtime default locale; the doc does not cover a malformed `lang`.
- Form: for an entry whose field has since unregistered, the doc does not say where the empty-message fallback label comes from. Form keeps the last registration seen per name and reads its label.
- Form: the doc says an unnamed form bases its ids on useId, but fields build `${idBase}-${name}`, and React 19 useId values contain characters like «» that then appear in href="#…" summary links. It works only because the click default is prevented and focus goes through the registration; the doc could say ids are opaque and never used as URL fragments.
- Form: the examples give children/actions as prose ('A submit Button labelled Sign in'), not args. Mapped them to concrete Input/Stack/Button JSX with Stack gap=normal — a gap the examples do not specify — and rendered actions as a bare Button with no wrapping Stack, although 'When to use' says to place actions in a Stack.

## 2026-09-22 03:39 — round 1

- Form: the web context is declared as containing `order`, but the doc never says its type and no web field can consume it — Enter submits natively, so there is no `returnKeyType` equivalent. Tracking it as state would re-render every field on each register/unregister. Chose a stable `order(): readonly string[]` returning enabled fields in document order, read at call time, not reactive state.
- Form: `reportValidity` is listed in the context with no signature, no caller and no stated semantics. Chose `(): boolean` — validates every enabled field, updates `errors`, and on failure runs the full failed-submit report (locale capture, summary counter, `onInvalid`, focus to summary or first invalid field), but never fires `onSubmit` and never resets `submitFailed`, since the doc says only a successful submission resets that. Nothing in the package calls it.
- Form: the doc puts `errorSummary` in the context 'so a field can stay silent while the Form announces', but no field reads it and the doc does not say what a silent field does. Input renders its error text with an unconditional `role="alert"` (Input.tsx:351), so inside a Form with a summary each error is announced twice — once by the field, once by the summary. Exposed the flag; did not change Input, which is outside this generation.
- Form: the doc's validation rule ('a field validates on blur when the mode is `blur` or `submitFailed` is true' and '`change` validates on change only, not also on blur') contradicts the shipped Input, which treats `validateMode === 'change'` as validating on blur too (Input.tsx:284). Left Input as it is; the Form side implements the doc's rule.
- Form: 'an entry whose field has since unregistered stays as plain danger Text with no Link until the next validation replaces the errors' cannot be observed reliably on web — the registrations live in a ref, so unregistering does not re-render the Form and the summary keeps rendering the Link until some other state change. Chose not to add state churn on every unregister; the fallback is correct whenever the Form does re-render.
- Form: 'items follow document order at the failed submit; an error found later by blur or change validation is appended at the end' rests on JS object key insertion order, since the doc types `errors` as a plain `Record<string, string>` on both the event and the context. A field whose `name` is an integer-like string ('0', '12') would be hoisted to the front of the summary. Kept the declared Record rather than a parallel ordered list.
- Form: what `reportValidity()` should do on a disabled Form is unstated. Since a disabled Form disables every field, the validation pass skips them all and reports valid, so it returns true and fires nothing; the `disabled` early return in the submit handler is what keeps `onSubmit` from firing with an empty map, matching 'a submit attempted while disabled is cancelled early and fires neither event'.
- Form: `copy.summaryHeadingOne` and `copy.invalidSummary` are declared but the doc says neither is rendered on web, Lit or RN — they have no consumer on any of the three. Kept both in the `COPY` constant so the copy table stays complete and verbatim, but they are dead on web.
- Form: `errorSummaryGap` is forwarded to the two summary Stacks' `gap` override, but Stack's `gap` is an enum-valued binding whose override takes an arbitrary `TokenRef`; the doc does not say what happens if the forwarded token is not a `layout.gap.*` one. Passed it through unchecked, with the default left at `gap="tight"` (`layout.gap.tight`).

## 2026-09-23 14:15 — round 1

- Form: the spec says an error-summary entry whose field has unregistered becomes plain danger Text, but not what makes the Form re-render when a field registers or unregisters — the registry is a ref. Chose to bump a state counter on register and unregister so the summary updates; the context value does not depend on it, so fields do not re-render.
- Form: the platform notes list `validate` next to `validateMode` in the React context without saying why both exist. Kept both, with the same value (`validate` for fields generated before `validateMode`).
- Form: the doc asks for a `FailedSubmit` story but not how its play function should wait for the summary. Chose `form.requestSubmit()` and then one animation frame.
- Form: the web notes say the summary's link targets are opaque ids but not how the generated id base is formed for an unnamed form. Chose `ds-form` + useId().
- Form: the spec has no keyboard block, but a Keyboard story (three required Inputs and a submit Button) exists and was kept; the docs should either declare a keyboard block or drop the story from the requirements.
- Form: the `LabelledBy` story is not one of the doc's examples; kept it to show that `labelledBy` wins over `label`.

## 2026-09-23 14:16 — round 2

- Form: the web platform notes say the locked errorSummaryText and errorSummaryBackground 'get no --ds-form-* hook on web or Lit; their rules read the token directly', but the prompt's Overrides section, the package conventions and the hooks gate all say a locked binding keeps its hook. Chose the Overrides contract: all three locked bindings (text, background, lineHeight) declare hooks and the rules read them. They are still left out of the `overrides` type. The notes should drop that sentence, for Lit too.
- Form: after the Round 1 fix the spec is still unclear on what re-renders the summary when a field unregisters (it now bumps a registry counter), on the id base for an unnamed form (`ds-form` + useId()), on why the context carries both `validate` and `validateMode`, and on how the FailedSubmit play function waits for the summary (requestSubmit, then one animation frame).
- Form: the spec has no keyboard block but a Keyboard story exists; the docs should declare a keyboard block or drop the requirement.
