// Generated by tools/behavior_tests.ts from the `behavior` block of the Alert doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Alert } from '../../packages/rn/src/Alert';
import type { AlertProps } from '../../packages/rn/src/Alert';
import meta from '../../packages/rn/src/Alert.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<AlertProps> = {}) {
  const events = {
    onDismiss: jest.fn(),
  };
  const props: AlertProps = { ...(meta.args as AlertProps), ...given, onDismiss: events.onDismiss };
  const tree = (p: AlertProps) => (
    <ThemeProvider mode="light">
      <Alert {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Alert') ?? screen.UNSAFE_root,
    container: () => screen.queryByRole('status') ?? s.root(),
    rerender: (next: Partial<AlertProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Alert', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-info', () => {
    const s = setup({"tone": "info"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-success', () => {
    const s = setup({"tone": "success"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-warning', () => {
    const s = setup({"tone": "warning"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-danger', () => {
    const s = setup({"tone": "danger"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-live-status', () => {
    const s = setup({"live": "status"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-live-alert', () => {
    const s = setup({"live": "alert"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-live-off', () => {
    const s = setup({"live": "off"});
    expect(s.root()).toBeTruthy();
  });
});
