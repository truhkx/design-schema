# Gaps reported while generating SidePanel for web

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 03:25 — round 1

- SidePanel: `trigger` is typed `content` in the schema, but showing/hiding it requires cloning a single element to attach aria-expanded/aria-controls/onClick — typed it as `ReactElement` (matching this package's existing Popover convention) rather than a generic ReactNode.
- SidePanel: the web platform notes describe an `as: nav` switch 'through the Landmark component' for the body-is-navigation case, but the component's own prop table has no `as` prop and Landmark isn't in the composition map. Always render a plain `<aside>`; did not invent an `as` prop.
- SidePanel: `copy.openLabel` ('Open menu') has no render site in the component itself since `trigger` is fully consumer-supplied content with its own label — used it as the default trigger's label in the stories instead.
- SidePanel: the notes describe toggling the native `hidden` attribute on the panel once the exit transition ends; instead followed this package's established BottomSheet/Popover pattern of conditionally mounting/unmounting the panel, which gives the same accessibility-tree result but not the literal mechanism described.
- SidePanel: no anatomy part corresponds to BottomSheet's drag 'handle'. Implemented swipeable's drag-to-dismiss on the header (excluding the close button) as the closest non-interactive surface — the schema doesn't say what part should carry the gesture.
- SidePanel: `dismissible: false` doc text ('only the trigger and footer actions close it') has no stated Escape exception, unlike BottomSheet's explicit 'Escape still reports' carve-out — interpreted this as Escape and the close button both being fully gated by `dismissible`, intentionally differing from BottomSheet's precedent.
- SidePanel: added a `container?: HTMLElement` portal-target prop; it isn't in the component's own schema prop list but matches the existing convention on every other portal-based overlay in this package (Popover, BottomSheet).

## 2026-09-10 21:09 — round 1

- SidePanel: the file already existed with prop names `title`/`hideTitle` instead of the schema's `heading`/`hideHeading` — renamed throughout (props, ids, CSS classes, stories) to match the schema exactly.
- SidePanel: the `role` (complementary/navigation) prop was entirely missing from the existing implementation, along with its two required behavior-scenario stories. Added it, rendering the non-modal-open and persistent regions through the existing `Landmark` component (as the persistent-mode platform note names) rather than a raw `<aside role="complementary">`, so `navigation` renders as `<nav>` with native semantics; not applied to the modal `<dialog>` per the schema's own note that modal is a dialog, not a landmark.
- SidePanel `dismissible`: the schema's own field description ('the close button is not rendered ... Escape still reports ... as in Dialog') contradicts the simpler paraphrase that was in the existing prop JSDoc. Fixed to match Dialog's exact precedent (packages/react/src/Dialog.tsx): the close button is now hidden (not just inert) when `dismissible` is false, and `escape` now always calls `onOpenChange` regardless of `dismissible` (previously it was incorrectly blocked).
- SidePanel `scrim` default: the `modal` prop's own free-text description says non-modal is 'no scrim by default', but the `scrim` prop's structured field says `default: true` — a genuine contradiction in the schema. Kept the structured default (`scrim: true`), matching the existing implementation and how defaults are meant to be sourced.
- SidePanel keyboard model, Tab row ('from trigger, moves into the open panel... Tab flows trigger → panel → page'): the panel is rendered through a React portal to `document.body` (per the package's general portal convention), so it is not actually adjacent to the trigger in DOM order, and forward-Tab from the trigger will not naturally enter the panel. I added the Shift+Tab-to-trigger and Tab-out-of-last-element-to-next-page-element handling (mirroring Popover's existing tab-out simulation in this same package), but did not attempt to intercept forward Tab from the trigger into the panel — Popover has the identical limitation already, so this looks like an accepted systemic tradeoff of the portal approach rather than something to redesign unilaterally within a single-component task.

## 2026-09-16 07:34 — round 1

- SidePanel: DOM placement contradicts itself — the web platform note says the panel is portaled 'so Tab from the trigger does not walk into it by document order' with a Popover-style seam, while the Guidance body says the next Tab enters the panel 'because it sits right after the trigger in the document' and the Web platform-notes prose says 'directly after the trigger'. Chose the portal (schema note + overlay rule) and added handlers so Tab from the trigger enters the panel, Shift+Tab from the first control returns to the trigger, and Tab from the last control goes to the next tabbable element after the trigger.
- SidePanel: the keyboard section says Tab 'from the trigger, moves into the open panel', but the platform note implies it does not by DOM order; implemented the keyboard rule explicitly (Tab keydown on the trigger wrapper focuses the panel's first tabbable).
- SidePanel: reason `action` ('a footer action asked to close') has no mechanism — no context, callback or prop lets a footer Button request close with that reason. Not emitted; consumers close footer actions by flipping `open` themselves.
- SidePanel: an outside pointerdown (not on the scrim) has no reason of its own; the reasons list has no `outside`. Chose `scrim` for both scrim clicks and outside pointerdowns.
- SidePanel: dismissible:false + Escape in uncontrolled mode — 'reports, the consumer decides' is ambiguous about whether local state changes. Chose: fire onOpenChange(false, 'escape') without closing the uncontrolled panel.
- SidePanel: whether `dismissible:false` blocks `navigation`/`trigger`/`action` is not stated. Chose: only close-button, scrim/outside and swipe are gated; the trigger toggle and Link navigation always close.
- SidePanel: 'the switch must not lose content state' can't be kept for a modal panel, whose presentation changes from <dialog> to the non-modal element at the breakpoint (the tree root type changes). Kept for non-modal panels by rendering into a single host node that moves between the portal target and an in-page slot; modal panels remount children when crossing the breakpoint.
- SidePanel: the root is ambiguous — platforms.web.element is `aside`, but the note says Landmark (which renders the aside/nav) takes no className/style/ref and SidePanel's own positioned element carries them. Chose: `data-ds`, classes, hooks and ref on SidePanel's wrapper (a display:contents div holding scrim + fixed surface; the <dialog> in modal). `id`/aria-controls target is the surface div, not the landmark itself.
- SidePanel: `widthNarrow` computed `calc(var(--space-20) * 3)` vs the override contract (hook defaults to its token). Chose hook `--ds-side-panel-width-narrow: var(--space-20)` and rules read `calc(hook * 3)`, so an override sets the unit, not the final width.
- SidePanel: the breakpoint media query syntax differs between docs ('above this layout width' vs guidance `min-width`). Chose `(width > <token>)` as BottomSheet does, read from the token custom property at runtime.
- SidePanel: persistent-mode layout is underspecified ('page grid beside the content', 'grid column of the width token') — no rule for where the sidebar goes relative to the trigger, and no statement on whether the close button shows. Chose: the sidebar renders in place of the component (after the trigger wrapper, which gets display:none), inline-size from the width binding, natural height (the page scrolls, not the body region), and no close button in persistent mode.
- SidePanel: modal initial focus — the note says 'first control', the guidance 'first control (or the title)', and the guidance also says `autoFocus={modal ? 'first' : false}` though FocusScope has no `false` value (it uses 'none'). Chose FocusScope autoFocus='none' plus focusing the first tabbable after showModal() (FocusScope's own autoFocus runs before the dialog is shown), falling back to the title with tabindex -1.
- SidePanel: the `trigger` prop is `content` (ReactNode) in the schema but 'exactly one element, typed as such' in the web note. Typed as ReactElement; hiding it in persistent mode uses a display:contents wrapper span (data-part='trigger') so the Button itself is never restyled.
- SidePanel: copy.expanded ('Expanded') has no use on web (aria-expanded carries the state); it is kept in COPY but not rendered.
- SidePanel: the `hideHeading` wording 'do not render it' vs 'keep the title for assistive technology' — chose the visually-hidden clip pattern so aria-labelledby still resolves.
- SidePanel: examples give content props as prose ('A Form of filter controls'). There is no List component in the package and Form requires `actions`, so the stories use a Stack of Links (with aria-current) and a Stack of Checkboxes; the example stories wrap the component so `open: true` still closes when the panel asks.
- SidePanel: safe-area padding is given as env(safe-area-inset-left/right) without saying which edge. Chose the physical edge the panel touches (flipped under :dir(rtl)), plus top/bottom insets on the layout.

## 2026-09-17 11:13 — round 1

- SidePanel: the style hooks (`--ds-side-panel-*`) must live on the surface, but the scrim must be the surface's sibling, so it cannot inherit `--ds-side-panel-scrim`/`layer`/`enter`/`exit` from it. I put the hook defaults on `.ds-side-panel__scrim` as well and pass the same inline override style to the scrim; the doc should say where the scrim's hooks come from.
- SidePanel: 'the ref is null while closed' conflicts with 'the `hidden` attribute when closed'. The non-modal surface stays mounted with `hidden` (the renders and has-accessible-name scenarios need it in the DOM), and the ref reports null whenever the panel is closed and not persistent.
- SidePanel: the `partGap` binding names part `focusScope`, but FocusScope always overwrites `data-part` with `scope` and accepts no className/style, so no `data-part="focusScope"` element exists and the gap cannot be put on it. The gap is on an inner column div (`ds-side-panel__layout`) inside FocusScope (and inside Landmark when non-modal).
- SidePanel: the closeButton composition lists only `variant: ghost` and `iconOnly: true`, but an icon-only Button needs a `label` (copy.closeLabel) and the `close` Icon as `leadingIcon`. I passed both although they are not in the listed props.
- SidePanel: the body Box is `inset: lg` with `inset` forwarded to `paddingBlock`, and the doc says the header and footer get 'the same padding'. Combined with `partGap` between the parts, the space above and below the body doubles (gap + Box padding-block). I gave the header inline + block-start inset and the footer inline + block-end inset, and left the doubling in place; the doc should say whether the gap or the padding wins.
- SidePanel: Heading has its own `marginBlockEnd` (space.sm), which unbalances vertical centering against the close button in the header. There is no forward for it and restyling the child is not allowed, so it is left as is.
- SidePanel: the non-modal scrim closes on `pointerdown` as the notes say; the modal ::backdrop close is detected on `click` (target is the <dialog> and the point is outside its box), because ::backdrop has no pointerdown target of its own. The doc says 'Click on the backdrop' for modal and 'pointerdown on the scrim' for non-modal; confirm that the split is intended.
- SidePanel: the phone-width cap is written `min(width, 100vw - edgeGutter)` per the web Platform notes. `100vw` includes the scrollbar gutter on desktop; the doc could say whether that is intended.
- SidePanel: the notes say safe-area padding goes on the physical edge plus top/bottom, but not whether a persistent in-page sidebar keeps it. I applied it only in overlay mode (the persistent layout has no padding).
- SidePanel: persistent is read from the token through getComputedStyle on <html>, so with no token stylesheet loaded (jsdom, a bare Storybook) the panel is never persistent and renders-persistent-content/page only exercise the overlay path; the scenarios cannot observe the sidebar.
- SidePanel: the has-accessible-name scenario runs on the Default (closed) story, so the named region is `hidden` and the test must query with `hidden: true`; the doc could give that scenario `open: true`.

## 2026-09-21 09:59 — round 1

- SidePanel: the doc says the focusScope column is "directly inside FocusScope", but in modal mode the surface must sit between the <dialog> and the column (it is the edge-positioned, width-capped, sliding box). Chose <dialog> > scrim + surface > FocusScope > focusScope column, so modal and non-modal both end surface > FocusScope > ... > column.
- SidePanel: Button hardcodes data-part="container" AFTER its rest spread, so data-part="closeButton" cannot be placed on the composed Button. Put it on an overlay-owned <span> wrapper and forwarded a click landing on the wrapper to the Button — the generated behavior test clicks [data-part="closeButton"] with userEvent, which does not activate an inner button. The doc should say which element carries a composed part's data-part when the child already writes one.
- SidePanel: headingGap is listed both as an overridable binding and as a composition forward to Heading's marginBlockEnd, but not how the space.0 default travels. Chose the hook route the `inset` binding describes (--ds-heading-margin-block-end set from --ds-side-panel-heading-gap on the heading wrapper) rather than an unconditional Heading `overrides` prop, so an override needs no conditional prop.
- SidePanel: with hideHeading plus a close button, the doc asks for the close button to be end-aligned in the header, but the hidden-title mechanism it also specifies (the absolute clip pattern) removes the title from the flex flow, so space-between left-aligns the lone button. Added a `:has(> .ds-side-panel__visually-hidden)` rule to end-align it; the doc should state the alignment rule rather than leaving it to emerge from the layout.
- SidePanel: a modal panel's <dialog> unmounts when closed (as Dialog does), so the trigger's aria-controls={panelId} points at no element while closed. The doc only describes aria-controls under the non-modal disclosure. Kept aria-controls on the trigger in both modes; this may trip axe's aria-valid-attr-value on a story that renders a closed modal panel with a trigger.
- SidePanel: the doc says the persistent breakpoint token is "converted to px" but not how. Measured the resolved custom property once with an off-screen probe element and handed matchMedia `(width > <n>px)`, because a rem breakpoint in a media query resolves against the initial font size, not the one on <html>. Under jsdom there is no matchMedia, so the derived renders-persistent-* scenarios only ever exercise overlay mode.
- SidePanel: the doc says the modal panel gets scroll lock "through FocusScope's modal contract", but FocusScope implements no scroll lock. SidePanel locks <html> itself with a reference-counted class, as Dialog does.
- SidePanel: Box never scrolls, so an overlay-owned scroll wrapper sits between the focusScope column and the body Box. partGap therefore spaces that wrapper rather than the Box itself. The parts list has no element for it — the doc should either name it or say the body part owns its own scrolling.
- SidePanel: the doc's width cap is "100% of the fixed containing block (which excludes a scrollbar gutter)". For a position: fixed element the containing block is the initial containing block, which in most engines matches the viewport including the scrollbar area, so the stated exclusion does not hold. Used `100%` verbatim rather than substituting 100dvw.
- SidePanel: `action` is documented as never raised by the component on React Native, and the web notes only describe Lit's method="dialog" submit listener. Assumed the web raises it never either, so the reason exists purely for a consumer's own footer handler reusing onOpenChange. The doc should say so for web explicitly.
- SidePanel: `swipeable` is accepted and completely inert on web — no gesture, no rendered difference. A consumer has no way to tell it did nothing. Worth a note in the prop description that the web value is only for cross-platform parity.
- SidePanel: copy.expanded ('Expanded') is never rendered on web (aria-expanded carries the state). Kept it in the COPY constant, unused, so the string stays verbatim with the doc — but the doc should say whether non-SwiftUI platforms should carry the constant at all or omit it.
- SidePanel: the doc distinguishes `trigger` from `outside` for a pointer press on the trigger with no scrim, but does not say what a press that starts on the trigger and ends elsewhere reports. Implemented by excluding the trigger wrapper from the outside-pointerdown listener, so the trigger's own click reports `trigger`.

## 2026-09-23 04:27 — round 1

- SidePanel: the navigation reason requires a click that was not default-prevented, but client-side routers (and a Link with its own onClick that calls preventDefault) cancel the click, so a panel with router Links never closes with `navigation`. I followed the spec; the previous generation deliberately counted cancelled clicks. The doc should say which it wants.
- SidePanel: the navigation reason says nothing about modified or new-tab clicks. I kept the existing filter: ctrl/meta/shift/alt clicks, non-primary buttons, target=_blank and download links do not close the panel, since the page is not navigated.
- SidePanel: the partGap description says the focusScope column is 'directly inside FocusScope (and inside the Landmark when non-modal)' and 'both modes end surface > FocusScope > column', but it never says whether Landmark goes above or below FocusScope. I chose surface > Landmark > FocusScope > column.
- SidePanel: 'Persistent mode renders no FocusScope at all' is stated only for React Native, while web says only 'no trap'. On web I keep an untrapped FocusScope in the persistent sidebar, so the tree above the children is the same in both presentations and a non-modal panel keeps its children's state across the breakpoint, as the web notes require.
- SidePanel: the spec says a `navigation` close does not move focus to the trigger, but it doesn't say whether the modal FocusScope's restore-on-unmount counts. I turn FocusScope's restoreFocus off when the last close reason was `navigation`.
- SidePanel: the spec says a bare-string trigger 'silently never opens the panel' but not what is rendered. I render it as-is inside the trigger wrapper, with no wiring, plus the development warning.
- SidePanel: the Default-story guidance says the closed panel carries `hidden` on 'its wrapper (web)', but the web notes put `hidden` on the root/surface element itself. I kept `hidden` on the surface root, and the has-accessible-name test queries the complementary region with hidden: true, since a closed panel is out of the accessibility tree.

## 2026-09-23 04:29 — round 2

- SidePanel: the spec puts the Landmark (non-modal) and FocusScope between the edge-positioned surface and the partGap column, but neither takes className or style, so neither can be stretched to the surface's height. If the column fills the surface with position: absolute, the Landmark measures zero and the keyboard gate's getByRole('complementary') visibility check fails. I gave the column `block-size: 100dvh` in overlay mode (auto when persistent), so the wrappers take their size from it. The doc should say how the column gets its height through composed wrappers it may not style.
- SidePanel: the keyboard gate roots every rule at getByRole('complementary').first() and requires it to be visible, so it covers only the non-modal Keyboard story. A modal panel is a dialog with no complementary landmark, so the modal branches of the Enter/Space and Shift+Tab rules (focus moves in, wrap to the last element) are never exercised on web.
- SidePanel: the navigation reason requires a click that was not default-prevented, so a panel whose Links go through a client-side router that cancels the click never closes with `navigation`. I followed the spec (carried over from round 1).
- SidePanel: the spec doesn't cover modified or new-tab clicks for `navigation`. I don't close on ctrl/meta/shift/alt clicks, non-primary buttons, target=_blank or download links (carried over from round 1).
- SidePanel: 'Persistent mode renders no FocusScope' is stated for React Native only. Web keeps an untrapped FocusScope in the persistent sidebar so the tree above the children doesn't change and their state survives crossing the breakpoint (carried over from round 1).
