// Generated by tools/behavior_tests.ts from the `behavior` block of the Form doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Form } from '../../packages/react/src/Form';
import type { FormProps } from '../../packages/react/src/Form';
import meta from '../../packages/react/src/Form.stories';

function setup(given: Partial<FormProps> = {}) {
  const events = {
    onSubmit: vi.fn(),
    onInvalid: vi.fn(),
  };
  const props = { ...meta.args, ...given, onSubmit: events.onSubmit, onInvalid: events.onInvalid };
  const utils = render(<Form {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Form"]') ?? screen.queryByRole('form') ?? utils.container.firstElementChild) as HTMLElement,
    container: () => (screen.queryByRole('form') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<FormProps>) => utils.rerender(<Form {...props} {...next} />),
  };
  return s;
}

describe('Form', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-validate-submit', async () => {
    const s = setup({"validate": "submit"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-validate-blur', async () => {
    const s = setup({"validate": "blur"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-validate-change', async () => {
    const s = setup({"validate": "change"});
    expect(s.root()).not.toBeNull();
  });
});
