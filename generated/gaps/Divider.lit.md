# Gaps reported while generating Divider for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 01:31 — round 1

- Divider: schema says label is 'optional text in the middle of a horizontal divider' but doesn't forbid it on a vertical divider or define layout for that case; I render label between two line segments in a flex column for vertical too, same as horizontal, rather than rejecting the combination.
- Divider: the `semantic` prop and `label` both drive the same role/aria-orientation exposure (guidance text says a labelled divider 'becomes semantic'); I implemented `effectiveSemantic = semantic || Boolean(label)` since the schema gives no separate visual/AT toggle for a labelled-but-not-announced divider.
- Divider: labelSize/fontFamily overrides are wired into the composed `<ds-text>` by targeting its own public override hooks (`--ds-text-font-size`, `--ds-text-font-family`) from a `.label` class rule in Divider's shadow styles, rather than passing Text's `overrides` property object, since that avoids fighting Lit's property/attribute timing and stays within Text's documented CSS-variable contract; labelColor is left to Text's own locked `tone="muted"` default since it's excluded from Divider's overridable set.
- Divider: no keyboard model, form-field interface, or events apply to this component (purely presentational/APG separator), so none were implemented — confirming there was nothing ambiguous there, just absent by design.

## 2026-09-10 17:29 — round 1

- Divider: doc says label is 'ignored on a vertical divider, with a development warning' but doesn't specify whether that also suppresses the implied `semantic`; I chose to fully ignore the label (no render, no implied semantic) on vertical, only honoring an explicit `semantic` prop there, since the label truly has nowhere to render.

## 2026-09-16 04:31 — round 1

- Divider: the Lit platform notes and guidance say role and aria-orientation go on the host through ElementInternals, but the package convention says names and roles that tests must read are plain host attributes (dom-accessibility-api ignores ElementInternals). I chose plain attributes: role, aria-orientation, aria-hidden.
- Divider: the lit notes say 'Host is the line (:host { display: block })' but a labelled divider must lay out line, label, line. I kept display: block with the host drawing the line when unlabelled, and switch the host to flex through an internal data-labelled host attribute when a label is in effect.
- Divider: the guidance says labelled separators 'read their text', but ARIA separator children are presentational, so text inside the shadow root may not be announced. I also set aria-label to the label on the host. The doc should say whether the label is the separator's accessible name.
- Divider: spacing none has no token (layout.gap.{spacing} with none does not resolve), and the binding says none is the off state. I render no margin for none, not a token, and the override hook applies only when spacing is tight, normal or loose.
- Divider: the doc does not say which axis spacing applies to. I chose margin-block for horizontal and margin-inline for vertical.
- Divider: label composition says Text takes fontSize and fontFamily via overrides but does not say what text size or tone to pass. I used size=sm and tone=muted (labelColor is color.foreground.muted, locked) with element=span, and forwarded labelSize and fontFamily through Text's documented --ds-text-font-size and --ds-text-font-family hooks.
- Divider: the behavior list given to Lit omits decorative-divider-is-hidden-from-assistive-technology and semantic-divider-is-a-separator (their expectations are tagged web only), so Lit has no test for aria-hidden or role=separator even though the Lit notes require both. The doc should tag those expectations for lit as well.
- Divider: the label scenario's name says the label 'makes the divider semantic', but for Lit its then has only 'text: or' (role is web only), so the test asserts the text alone.
- Divider: a vertical divider stretches only when its parent is a flex or grid row. The doc does not say what happens in block flow, where it has no height. The ToolbarGroups and OrientationVertical stories place it in a horizontal ds-stack with align=stretch.

## 2026-09-17 04:46 — round 1

- Divider: the `fontFamily` binding says 'the root sets no font hook' and `labelSize` says 'Divider writes no label … size rule of its own', but the Overrides section says every overridable binding becomes a `--ds-divider-*` hook on :host. I kept `--ds-divider-label-size` and `--ds-divider-font-family` on :host, passed to ds-text's own `--ds-text-font-size` / `--ds-text-font-family` hooks so page CSS can still set them. The `overrides` property is also passed to Text's `overrides` (fontSize, fontFamily), and only the bindings the author actually overrode are passed on.
- Divider: `orientation` says a vertical divider uses 'block-size 100% and align-self stretch'. In CSS, flex stretch only applies when the cross size is `auto`, so `block-size: 100%` inside a flex row with no set height (like the ToolbarGroups example in a horizontal Stack) would give height 0. I used `block-size: auto; align-self: stretch; min-block-size: 100%`, which stretches in a flex/grid row and fills a parent with a set height. The doc should say this instead of block-size 100%.
- Divider: the Lit platform note says '`label` is a property', but the Lit guidance shows `<ds-divider semantic label="or">` as markup. I made it a property that also reads the attribute (`@property({ type: String })`, not reflected).
- Divider: the doc names the parts (`line`, `label`) but does not say whether the Lit shadow elements carry `part` as well as `data-part`, while the conventions say 'No ::part for styling'. I kept both, set to the anatomy names, as before.
- Divider: `spacing` renders nothing and sets no hook at `none`, but the doc does not say whether `overrides.spacing` given while spacing is `none` should still be written to the host. I always write it to `--ds-divider-spacing`; it does nothing because no margin rule reads the hook at `none`.

## 2026-09-20 15:08 — round 1

- Divider: the two `role: separator` scenarios are unsatisfiable on Lit. tools/behavior_tests.ts maps `then.role` for lit to `el.shadowRoot!.querySelector('[role="separator"]')`, but platforms.lit.notes requires role/aria-orientation/aria-label as plain host attributes 'because tests read them'. I followed the notes (host attributes, which the hand-written test and dom-accessibility-api do read) and left semantic-divider-is-a-separator and label-is-read-and-makes-the-divider-semantic failing in the generated gate; putting a second role=separator in the shadow root would expose two nested separators. This is a generator/doc fix, not an element fix, and it hits every component whose role lives on the host (Landmark, Toolbar).
- Divider: platforms.lit.notes says 'Host is the line when unlabelled', which reads as an empty shadow root, but the derived `renders` scenarios assert `shadowRoot.childElementCount > 0` and the decorative scenario resolves its target to `[part="line"]`. I render the `line` part as a span in the shadow root in every case (the host still carries the thickness, orientation and spacing), so the anatomy's `line` part has an element with `part`/`data-part` even unlabelled. The web note says the opposite for its unlabelled case ('the root paints itself and is the `line` part; it carries no data-part'), so web and Lit now differ; the doc should state which platform is right.
- Divider: styles.labelSize.description says labelSize and fontFamily have no --ds-divider-* hook and reach the label only through Text's `overrides`, but the generic overrides contract in the prompt says *every* binding becomes a hook on :host defaulting to its token. I followed the binding description (and React, which does the same), dropping the --ds-divider-label-size / --ds-divider-font-family hooks the previous Lit element had. The overrides contract should carve out forwarded bindings.
- Divider: labelColor is a locked binding with part `label`, but the composition already sets Text tone="muted", and the description says 'Divider writes no label color or size rule of its own'. The element therefore contains no rule for its one locked, accessibility-bearing binding — nothing enforces it if Text's `muted` tone changes. Flagging rather than adding a rule, since adding one would restyle a composed child.
- Divider: a vertical divider with an ignored label and `semantic: true` is left with no accessible name (role=separator, aria-orientation=vertical, no aria-label). The doc says the ignored label 'implies nothing either' about semantics but never says whether it should still name an otherwise-semantic divider; I chose not to name it, since a name with no visible text contradicts the label being ignored.
- Divider: nothing says whether the single unlabelled line segment is aria-hidden. I mark it aria-hidden="true" in both orientations, consistent with the labelled pieces and harmless under role=separator (whose children are presentational) — and it is what the decorative scenario's resolved target asserts.
- Divider: `spacing` is described as space on the cross axis of the line, but with a label in effect the host is the row, so margin-block spaces the whole labelled row rather than the line pieces. Assumed intended (React does the same); the doc does not address spacing + label.
- Divider: the toolbar-groups example names 'the Texts "Bold Italic" and "Align left"' — ambiguous whether "Bold Italic" is one Text or two controls. I matched the React story: a single Text per side.

## 2026-09-20 15:12 — round 2

- Divider: the `keyboard-run` gate is unsatisfiable and was mis-scheduled. Divider's doc declares no `keyboard` block (confirmed in divider.md, and `keyboard: null` in generated/components.json), so tools/keyboard_tests.ts never writes generated/keyboard/Divider.lit.spec.ts, and the gate ran `playwright test --project=keyboard-lit generated/keyboard/Divider.lit.spec.ts` — an unmatched filter, which I reproduced exactly. Nothing in packages/lit can create that file. Two harness causes, both in tools/checks.ts: (1) the `existsSync` guard around keyboard-run is evaluated when the gate list is built, before the `keyboard` gate's freshOutDir() wipes and rewrites generated/keyboard/, so a stale spec from an older doc revision schedules a gate whose file is then deleted; (2) the failing invocation lacked the `--pass-with-no-tests` that the guarded branch always pairs with the filter. Same failure previously hit Card and Container (logs/card-keyboard-scope.mjs, logs/container-keyboard-gate.mjs).
- Divider: the two `role: separator` behavior scenarios are unsatisfiable on Lit. tools/behavior_tests.ts maps `then.role` for lit to `el.shadowRoot!.querySelector('[role="separator"]')`, but platforms.lit.notes requires role/aria-orientation/aria-label as plain host attributes 'because tests read them'. I followed the notes and left semantic-divider-is-a-separator and label-is-read-and-makes-the-divider-semantic failing in the generated gate; a second role=separator inside the shadow root would expose two nested separators. This hits every component whose role lives on the host (Landmark, Toolbar).
- Divider: platforms.lit.notes says 'Host is the line when unlabelled', which reads as an empty shadow root, but the derived `renders` scenarios assert `shadowRoot.childElementCount > 0` and the decorative scenario resolves its target to `[part="line"]`. I render the `line` part as a span in the shadow root in every case (the host still carries thickness, orientation and spacing), verified in Chromium for all three layouts. The web note says the opposite for its unlabelled case ('the root paints itself and is the `line` part; it carries no data-part'), so web and Lit now differ; the doc should state which is right.
- Divider: styles.labelSize.description says labelSize and fontFamily have no --ds-divider-* hook and reach the label only through Text's `overrides`, while the generic overrides contract says every binding becomes a hook on :host. I followed the binding description (and React), dropping the two hooks the previous Lit element had.
- Divider: labelColor is a locked, accessibility-bearing binding with part `label`, but its own description says Divider writes no label color rule (the color comes from Text tone="muted"). The element therefore contains nothing that enforces its one locked binding; adding a rule would restyle a composed child, so I left it.
- Divider: a vertical divider with an ignored label and `semantic: true` is left with no accessible name. The doc says the ignored label 'implies nothing either' about semantics but never says whether it should still name an otherwise-semantic divider; I chose not to name it, since an invisible name contradicts the label being ignored.
- Divider: nothing says whether the single unlabelled line segment is aria-hidden. I mark it aria-hidden="true" in both orientations, consistent with the labelled pieces and harmless under role=separator, whose children are presentational.
- Divider: `spacing` is described as space on the cross axis of the line, but with a label in effect the host is the row, so margin-block spaces the whole labelled row rather than the line pieces. Assumed intended (React does the same); the doc does not address spacing + label.
- Divider: the toolbar-groups example names 'the Texts "Bold Italic" and "Align left"' — ambiguous whether "Bold Italic" is one Text or two controls. I matched the React story: a single Text per side.

## 2026-09-20 15:15 — round 3

- Divider: `keyboard-run` is unsatisfiable for this component and the failure is a harness bug, now pinned down. Divider declares no `keyboard` block, so tools/keyboard_tests.ts never writes generated/keyboard/Divider.lit.spec.ts, and the gate ran a Playwright positional filter naming that file. Proof it was a filter and not an empty project: a project matching no files prints only `Error: No tests found` (verified with logs/nomatch.config.ts), while the gate output carries the extra `Make sure that arguments are regular expressions…` lines, which I reproduce exactly with `playwright test --project=keyboard-lit generated/keyboard/Divider.lit.spec.ts`. This tree's own composition passes (`--pass-with-no-tests` + the filter exits 0), and `git diff tools/checks.ts` shows that guard is an uncommitted working-tree change while HEAD runs the gate repo-wide with no filter — so the job runner uses a third copy of checks.ts that has the scoping filter but neither the existsSync guard nor --pass-with-no-tests. Two fixes are needed there: commit the guarded composition, and move the existsSync check after the `keyboard` gate, since freshOutDir() wipes generated/keyboard/ between the gate list being built and Playwright running. No change to packages/lit can affect this; writing the spec by hand is forbidden (generated/) and would be deleted by the next freshOutDir anyway. Same failure previously hit Card and Container (logs/card-keyboard-scope.mjs, logs/container-keyboard-gate.mjs).
- Divider: the two `role: separator` behavior scenarios are unsatisfiable on Lit. tools/behavior_tests.ts maps `then.role` for lit to `el.shadowRoot!.querySelector('[role="separator"]')`, but platforms.lit.notes requires role/aria-orientation/aria-label as plain host attributes 'because tests read them'. I kept the host attributes (which the hand-written test and dom-accessibility-api read) and left those two failing; a second role=separator inside the shadow root would expose two nested separators. This hits every component whose role lives on the host (Landmark, Toolbar).
- Divider: platforms.lit.notes says 'Host is the line when unlabelled', which reads as an empty shadow root, but the derived `renders` scenarios assert `shadowRoot.childElementCount > 0` and the decorative scenario resolves its target to `[part="line"]`. I render the `line` part as a span in the shadow root in every case (the host still carries thickness, orientation and spacing), verified in Chromium for all three layouts. The web note says the opposite for its unlabelled case ('the root paints itself and is the `line` part; it carries no data-part'), so web and Lit now differ; the doc should state which is right.
- Divider: styles.labelSize.description says labelSize and fontFamily have no --ds-divider-* hook and reach the label only through Text's `overrides`, while the generic overrides contract says every binding becomes a hook on :host. I followed the binding description (and React), dropping the two hooks the previous Lit element had.
- Divider: labelColor is a locked, accessibility-bearing binding with part `label`, but its own description says Divider writes no label color rule (the color comes from Text tone="muted"). Nothing in the element enforces its one locked binding; adding a rule would restyle a composed child, so I left it.
- Divider: a vertical divider with an ignored label and `semantic: true` is left with no accessible name. The doc says the ignored label 'implies nothing either' about semantics but never says whether it should still name an otherwise-semantic divider; I chose not to name it, since an invisible name contradicts the label being ignored.
- Divider: nothing says whether the single unlabelled line segment is aria-hidden. I mark it aria-hidden="true" in both orientations, consistent with the labelled pieces and harmless under role=separator, whose children are presentational.
- Divider: `spacing` is described as space on the cross axis of the line, but with a label in effect the host is the row, so margin-block spaces the whole labelled row rather than the line pieces. Assumed intended (React does the same); the doc does not address spacing + label.
- Divider: the toolbar-groups example names 'the Texts "Bold Italic" and "Align left"' — ambiguous whether "Bold Italic" is one Text or two controls. I matched the React story: a single Text per side.

## 2026-09-20 16:46 — round 1

- Divider: the two `role: separator` scenarios are unsatisfiable on Lit and stay red in behavior-run. tools/behavior_tests.ts maps `then.role` for lit to `el.shadowRoot!.querySelector('[role="separator"]')`, while platforms.lit.notes requires role/aria-orientation/aria-label as plain host attributes 'because tests read them'. I followed the notes; putting a second role=separator in the shadow root would nest two separators. This hits every component whose role lives on the host (Landmark, Toolbar) and is a generator/doc fix.
- Divider: platforms.lit.notes says 'Host is the line when unlabelled', which reads as an empty shadow root, but the derived `renders` scenarios assert `shadowRoot.childElementCount > 0` and the decorative scenario resolves its target to `[part="line"]`. I render the `line` span in every case (the host still carries thickness, orientation and spacing). The web note says the opposite for its unlabelled case ('the root paints itself and is the `line` part; it carries no data-part'), so web and Lit differ; the doc should state which is right.
- Divider: the notes give the host `display: block` / `inline-block`, but a line child sized `block-size: 100%` collapses inside a host whose own block-size is auto (the stretched vertical case in the toolbar-groups row). The host is `display: flex` / `inline-flex` with the line as `flex: 1 1 auto` instead — verified in Chromium for all three layouts. The doc should say flex, or say the host paints and carries no line element.
- Divider: styles.labelSize.description says labelSize and fontFamily have no `--ds-divider-*` hook and reach the label only through Text's `overrides`, while the generic overrides contract says every binding becomes a hook on :host. I followed the binding description (and React); the overrides contract should carve out forwarded bindings.
- Divider: labelColor is the one locked, accessibility-bearing binding, but its own description says Divider writes no label color rule (the color comes from the composed Text's tone="muted"). Nothing in the element enforces it, and it would silently follow a change to Text's muted tone. Adding a rule would restyle a composed child, so I left it.
- Divider: a vertical divider with an ignored `label` and `semantic: true` gets role=separator, aria-orientation=vertical and no accessible name. The doc says the ignored label 'implies nothing either' about semantics but never says whether it should still name an otherwise-semantic divider; I chose not to name it, since an invisible name contradicts the label being ignored.
- Divider: nothing says whether the single unlabelled line segment is aria-hidden. I mark it aria-hidden="true" in both orientations, consistent with the labelled pieces and harmless under role=separator (presentational children) — and it is what the decorative scenario's resolved target asserts.
- Divider: the development warning for an ignored label has no `copy.*` entry, so its wording is invented. I aligned it with React's sentence ('<ds-divider>: `label` is ignored on a vertical divider — a vertical line has no room for centered text.'); if the string is contractual or asserted by a test it belongs in the copy block.
- Divider: `spacing` is described as space on the cross axis of the line, but with a label in effect the host is the row, so margin-block spaces the whole labelled row rather than the line pieces. Assumed intended (React does the same, and or-between-alternatives pairs label with spacing normal); the doc does not address spacing + label.
- Divider: `flex-shrink: 0` on the host is not in the spec; added so a horizontal divider in a vertical flex Stack keeps its one-token thickness (React's CSS has the same rule). Its presence should be stated in the doc.
- Divider: the toolbar-groups example names 'the Texts "Bold Italic" and "Align left"' — ambiguous whether "Bold Italic" is one Text or two controls. I matched the React story: a single Text per side. React puts the wrapper in a Storybook decorator so `args` stays exactly the example's `given`; Lit cannot, because the docs-site snippet reader requires an inline render returning one `html` template, so OrientationVertical/ToolbarGroups repeat the Stack wrapper inside their own render. The doc should say whether the wrapper is part of the example's identity.
- Divider: the Default story's args are a generator choice — the doc fixes `given` only for the four named examples, yet the behavior tests spread `meta.args` as their baseline, so Default silently defines every scenario's starting state. Lit's meta also carries `label: ''` (React omits `label` entirely) so the control renders; an empty string is no label, so the rendered result is identical.
- Divider: the doc fixes the vertical cross-axis size but never the horizontal one. The Lit host is a block-level flex container, so a horizontal divider fills its container's inline size; worth stating, since it decides what happens inside a horizontal flex row.

## 2026-09-22 04:18 — round 1

- Divider: the two `role: separator` scenarios are unsatisfiable on Lit and stay red in behavior-run. tools/behavior_tests.ts:457 maps `then.role` for lit to `el.shadowRoot!.querySelector('[role="separator"]')`, but platforms.lit.notes requires role/aria-orientation/aria-label as plain host attributes 'because tests read them'. I kept the host attributes (which dom-accessibility-api and the hand-written test do read, verified passing) and left semantic-divider-is-a-separator and label-is-read-and-makes-the-divider-semantic failing; a second role=separator inside the shadow root would nest two separators. Generator/doc fix, and it hits every component whose role lives on the host (Landmark, Toolbar).
- Divider: styles.labelSize/fontFamily say they have no `--ds-divider-*` hook and reach the label only through the composed Text's `overrides`, while the prompt's generic overrides contract says *every* binding becomes a hook on :host defaulting to its token. I followed the binding descriptions (and React), so `overrides.labelSize`/`fontFamily` are forwarded to ds-text's `overrides` and page CSS reaches them only through Text's own hooks. The overrides contract should carve out forwarded bindings.
- Divider: labelColor is the one locked, accessibility-bearing binding, but its own description says Divider writes no label colour rule (the colour comes from the composed Text's `tone="muted"`). The element therefore contains nothing that enforces it, and the contrast claim silently follows any change to Text's muted tone. Adding a rule would restyle a composed child, so I left it — flagging that a locked binding with no enforcement point is a contract the code cannot keep.
- Divider: the docs snippet reader requires an inline render returning one `html` template, so OrientationVertical and ToolbarGroups repeat the Stack wrapper inside their own render, while React puts it in a Storybook decorator. The published snippets therefore differ in scenario, not just syntax: generated/examples/Divider.json has react `<Divider orientation="vertical" />` against lit `<ds-stack …><ds-text>Bold Italic</ds-text><ds-divider orientation="vertical"></ds-divider><ds-text>Align left</ds-text></ds-stack>`. The doc should say whether the wrapper is part of the example's identity (and if so, React's snippet is missing it).
- Divider: `label` is treated as in effect when it is truthy, so a whitespace-only label (`label=" "`) renders a Text with a space and names the separator ' '. The doc fixes only the empty string ('an empty string is no label'); I matched React's `Boolean(label)` rather than trimming, so the two platforms agree. The doc should say whether whitespace is a label.
- Divider: the Default story's args are a generator choice — the doc fixes `given` only for the four named examples, yet both the hand-written and generated behavior tests spread `meta.args` as every scenario's baseline, so Default silently defines the starting state. Lit's meta carries `label: ''` (React omits `label` entirely) so the Storybook control renders; an empty string is no label, so the rendered result is identical.
- Divider: a labelled line segment gets `flex: 1 1 0` rather than the notes' `flex: 1 1 auto`, so the two pieces are equal and the label sits centred. With empty spans the two resolve identically, but the doc states one value for a case (unlabelled, single line) that differs from the labelled row it does not describe.
