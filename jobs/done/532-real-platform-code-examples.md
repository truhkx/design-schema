Replace the exported-story source with real, per-platform copy/paste code per Tony's review, 2026-09-15 — flagged as important. Right now `tools/docs_examples.ts` (job 507) captures each React story's own source slice as `sourceText`, so every platform's "code" on a component page is just the React story. Tony wants a developer to pick React, React Native, Lit, or Swift and see what they would actually write in that system, not a description of the React one.

1. **React.** Already close: `sourceText` from the story is real React source, but review it for story-specific noise (test ids, decorator wrappers, `args` spread patterns) that shouldn't appear in copy/paste code — the goal is "what you'd paste into your app," not "what the story file contains." Trim to a clean function component usage: the import line(s) plus the JSX for that example's props.
2. **Lit.** `packages/lit/src/<Name>.stories.ts` exist (the Lit package has its own Storybook, referenced in `510-chromatic-storybooks.md`'s log). Extend `tools/docs_examples.ts` to also read the matching Lit story for each example (matched by title, the same way React examples are matched to their component) and emit its template-literal HTML usage (`<ds-button variant="primary">Save</ds-button>`-shaped) as that platform's snippet.
3. **React Native.** Same approach against `packages/rn/src/<Name>.stories.tsx`.
4. **Swift.** Not available yet — `packages/swiftui` has no per-component source (`ios-platform-plan.md`, jobs 400-460: only `apps/ios-gallery`'s skeleton exists, no `Button.swift` has been generated). Do not fabricate a Swift snippet. Render a fourth tab that says so plainly ("SwiftUI example ships once this component is generated for iOS — see the iOS platform plan") rather than omitting the tab silently, so the four-platform intent is visible even while incomplete. Wire it to flip to real content automatically once `packages/swiftui/Sources/DesignSchema/<Name>.swift` exists — a presence check, not a hard-coded "not yet" per component that needs manual upkeep.
5. **UI.** In `Examples.tsx` (or wherever the example's code panel renders — coordinate with job 508's `<Code>`/Shiki wiring), each example gets a platform switcher (React / React Native / Lit / Swift) alongside its existing live-preview + source view, not a separate page or tab dimension crossed with the existing scenario tabs — a reader picks a scenario (Default, Sizes, …) and, within it, a platform. Only the React (and eventually RN's `react-native-web`-rendered) version needs to actually *run* as the live island; Lit and Swift are code-only panels (no live render for Lit today — check whether the Lit package has a browser-safe build `apps/website` could mount as a web component island; if not, code-only is fine and is still the important deliverable here).
Gate: `generated/examples/<Name>.json` carries a per-platform snippet map (`{ react, lit, rn, swift }`, `swift` possibly `null`) for every component that has React, Lit, and RN stories; the component page's platform switcher shows real, distinct source per platform for at least one component that exists on all three today (Button is the safest first target); switching platforms never changes which scenario (Default/Sizes/…) is showing. Do not modify `packages/*/src` — every platform's snippet comes from that platform's own existing `.stories` file, never invented.
