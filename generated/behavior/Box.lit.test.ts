// Generated by tools/behavior_tests.ts from the `behavior` block of the Box doc. Do not edit.
import { beforeEach, describe, expect, test, vi } from 'vitest';
import { userEvent } from 'vitest/browser';
import '../../packages/lit/src/Box.js';
import meta from '../../packages/lit/src/Box.stories.js';

function deep(root: ParentNode, selector: string): Element | null {
  const direct = root.querySelector(selector);
  if (direct) return direct;
  for (const el of Array.from(root.querySelectorAll('*'))) {
    const shadow = (el as HTMLElement).shadowRoot;
    if (shadow) {
      const found = deep(shadow, selector);
      if (found) return found;
    }
  }
  return null;
}

/** Every element that is "active" from the document down through shadow roots: the host, then the inner one.
 *  document.activeElement alone is a shadow host while focus sits inside its shadow tree. */
function activeChain(): Element[] {
  const chain: Element[] = [];
  let el: Element | null = document.activeElement;
  while (el) {
    chain.push(el);
    el = el.shadowRoot?.activeElement ?? null;
  }
  return chain;
}

async function setup(given: Record<string, unknown> = {}) {
  const el = document.createElement('ds-box');
  const props = { ...meta.args, ...given };
  for (const [key, value] of Object.entries(props)) {
    if (value === undefined) continue;
    // `children` is slotted light-DOM content, and `Element.children` is getter-only:
    // assigning it throws and takes the whole file down at setup. Slot a string into the
    // default slot the way the stories do; a richer value cannot be expressed here.
    if (key === 'children') {
      if (typeof value === 'string') el.append(value);
      continue;
    }
    (el as unknown as Record<string, unknown>)[key] = value;
  }
  const events = {
  };
  document.body.append(el);
  await (el as unknown as { updateComplete: Promise<boolean> }).updateComplete;
  const root: ParentNode = el.shadowRoot ?? el;  // some elements render in light DOM
  const s = {
    el,
    root,
    events,
    props,
    root_: () => el,
    surface: () => ((el.matches('[part~="surface"], [data-part="surface"]') ? el : null) ?? deep(root, '[part="surface"]') ?? deep(root, '[data-part="surface"]') ?? root.firstElementChild) as HTMLElement,
  };
  return s;
}

beforeEach(() => {
  document.body.replaceChildren();
});

describe('ds-box', () => {
  test('nav-element-carries-navigation-semantics', async () => {
    const s = await setup({"element": "nav"});
    expect(s.el.matches('[role="navigation"]') || deep(s.root, '[role="navigation"]') !== null || deep(s.el, '[role="navigation"]') !== null).toBe(true);
  });
  test('article-element-carries-article-semantics', async () => {
    const s = await setup({"element": "article"});
    expect(s.el.matches('[role="article"]') || deep(s.root, '[role="article"]') !== null || deep(s.el, '[role="article"]') !== null).toBe(true);
  });
  test('renders', async () => {
    const s = await setup({});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-none', async () => {
    const s = await setup({"inset": "none"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-sm', async () => {
    const s = await setup({"inset": "sm"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-md', async () => {
    const s = await setup({"inset": "md"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-lg', async () => {
    const s = await setup({"inset": "lg"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-xl', async () => {
    const s = await setup({"inset": "xl"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-block-none', async () => {
    const s = await setup({"insetBlock": "none"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-block-sm', async () => {
    const s = await setup({"insetBlock": "sm"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-block-md', async () => {
    const s = await setup({"insetBlock": "md"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-block-lg', async () => {
    const s = await setup({"insetBlock": "lg"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-block-xl', async () => {
    const s = await setup({"insetBlock": "xl"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-inline-none', async () => {
    const s = await setup({"insetInline": "none"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-inline-sm', async () => {
    const s = await setup({"insetInline": "sm"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-inline-md', async () => {
    const s = await setup({"insetInline": "md"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-inline-lg', async () => {
    const s = await setup({"insetInline": "lg"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-inset-inline-xl', async () => {
    const s = await setup({"insetInline": "xl"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-surface-none', async () => {
    const s = await setup({"surface": "none"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-surface-default', async () => {
    const s = await setup({"surface": "default"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-surface-subtle', async () => {
    const s = await setup({"surface": "subtle"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-surface-strong', async () => {
    const s = await setup({"surface": "strong"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-radius-none', async () => {
    const s = await setup({"radius": "none"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-radius-sm', async () => {
    const s = await setup({"radius": "sm"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-radius-md', async () => {
    const s = await setup({"radius": "md"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-radius-lg', async () => {
    const s = await setup({"radius": "lg"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-radius-full', async () => {
    const s = await setup({"radius": "full"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-element-div', async () => {
    const s = await setup({"element": "div"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-element-section', async () => {
    const s = await setup({"element": "section"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-element-article', async () => {
    const s = await setup({"element": "article"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-element-aside', async () => {
    const s = await setup({"element": "aside"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-element-header', async () => {
    const s = await setup({"element": "header"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-element-footer', async () => {
    const s = await setup({"element": "footer"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-element-main', async () => {
    const s = await setup({"element": "main"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-element-nav', async () => {
    const s = await setup({"element": "nav"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
});
