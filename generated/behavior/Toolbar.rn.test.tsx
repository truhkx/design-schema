// Generated by tools/behavior_tests.ts from the `behavior` block of the Toolbar doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Toolbar } from '../../packages/rn/src/Toolbar';
import type { ToolbarProps } from '../../packages/rn/src/Toolbar';
import meta from '../../packages/rn/src/Toolbar.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<ToolbarProps> = {}) {
  const events = {};
  const props: ToolbarProps = { ...(meta.args as ToolbarProps), ...given };
  const tree = (p: ToolbarProps) => (
    <ThemeProvider mode="light">
      <Toolbar {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Toolbar') ?? screen.UNSAFE_root,
    container: () => screen.queryByRole('toolbar') ?? s.root(),
    rerender: (next: Partial<ToolbarProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Toolbar', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-orientation-horizontal', () => {
    const s = setup({"orientation": "horizontal"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-orientation-vertical', () => {
    const s = setup({"orientation": "vertical"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-overflow-wrap', () => {
    const s = setup({"overflow": "wrap"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-overflow-menu', () => {
    const s = setup({"overflow": "menu"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-overflow-scroll', () => {
    const s = setup({"overflow": "scroll"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-sm', () => {
    const s = setup({"size": "sm"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-md', () => {
    const s = setup({"size": "md"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-density-compact', () => {
    const s = setup({"density": "compact"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-density-comfortable', () => {
    const s = setup({"density": "comfortable"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('toolbar', { name: s.props.label })).toBeOnTheScreen();
  });
});
