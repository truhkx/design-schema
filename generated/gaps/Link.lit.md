# Gaps reported while generating Link for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 01:10 — round 1

- Link.ts/.stories.ts pre-existed from an earlier pass and were already spec-complete except for the overrides contract (hooks, `overrides` property, `data-ds`) and the `LinkOverridableBinding` type export, which I added; no ambiguity in the schema itself.
- download: not in `platforms.lit.reflect`, so left unreflected (a plain non-reflected boolean attribute) even though `external` and `tone` are reflected — the spec only lists the latter two under `reflect`.
- has-accessible-name scenario doesn't specify whether the external suffix is included in the assertion; used the plain `label` for the default (non-external) case, matching the scenario's default `given` (no `external: true`).

## 2026-09-16 04:00 — round 1

- Link: styles.colorHover's description says 'Pointer hover and active state' but its declared state is only `hover`; the declared contract won, so Lit applies color.link.hover on :hover only. The doc should either add an active state or drop 'and active' from the description.
- Link: href and label are `required: true` with no default, but a Lit property needs an initial value; both default to '' and nothing warns when either is missing. The doc could say whether an empty href should trigger a dev warning, since the guidance says a link with no href should be rendered as Text instead.
- Link: the `external-link-announces-that-it-leaves` scenario marks the `name` expectation web-only and gives Lit only `copy: externalSuffix`, so it's unclear whether Lit must reach the same accessible name through the shadow root. The Lit test checks both the suffix text and the full accessible name, and both pass.
- Link: examples give only props, but `inline-in-a-paragraph` and `inside-muted-text` describe a surrounding paragraph (muted text for the second). The doc has no copy for that paragraph, so the stories wrap the link in `<ds-text>` with short made-up sentences ('Invoices from the last year are listed.', 'For details, read …'). The doc should supply that text.
- Link: the underline on tone=inherit links has no color binding, so it follows currentColor; the doc doesn't say whether that is intended.
- Link: `tone: inherit` makes visited and hover inherit too, so an inherited link has no visited or hover color change. The doc implies this ('the underline alone marks it') but doesn't say it outright.
- Link: externalIcon's color isn't bound; it follows the anchor's currentColor, which gives it the hover and visited colors. The RN notes say the icon uses the link color, but nothing is declared for Lit or web.
- Link: onPress lists `fires: [user]` and cancelable, but on Lit it is the native click, which a script calling `.click()` also fires; there is no way to limit it to user clicks, so none was attempted.

## 2026-09-17 04:21 — round 1

- Link: the Overrides section says 'No ::part is exposed for styling' while the package convention says 'part and data-part values are the anatomy names verbatim'; kept part="anchor"/"label"/"externalIcon" alongside data-part, which does technically expose ::part to consumers.
- Link: `label` is in the anatomy but no style binding or platform note says what element carries it; rendered a <span data-part="label"> inside the anchor.
- Link: the focus ring's outline-offset has no binding (focusRing, focusRingWidth, focusRingRadius cover color, width, radius only); used var(--border-width-focus) as the offset.
- Link: focusRingRadius is described as applying to the ring, but a radius on an inline anchor also rounds its background/hit area at rest; applied border-radius only under :focus-visible.
- Link: the doc asks for one story per enum value plus notable states, but external and download are booleans with only example stories (ExternalDestination, DownloadableFile) covering them; did not add separate External/Download stories.
- Link: the web note's visually hidden pattern lists 'absolute, 1px box, clip-path inset 50%, white-space nowrap' but not margin/overflow/border; kept the standard margin:-1px, overflow:hidden, border:0 and dropped the legacy clip:rect.
- Link: the `renders`/`renders-tone-*` derived scenarios say only `renders: true` with no observable to assert for tone; the tests check the anchor part exists, which cannot distinguish tones.

## 2026-09-18 19:33 — round 1

- Link: the spec requires a story per enum value plus one per example, but doesn't say whether ExternalDestination and DownloadableFile also sit inside a Text paragraph like the other two examples; I render them standalone.
- Link: the click-fires-on-press scenario would navigate the test page with the Default href '/billing/history'; the spec doesn't say how a test stops that, so the test calls preventDefault on the retargeted click.
- Link: the `tone: inherit` enum story must use the muted wrapper, but the spec doesn't say whether it keeps the Default args or takes the inside-muted-text args; ToneInherit keeps the Default href and label inside the muted sentence.
- Link: whether the decorative ds-icon (no label) is hidden from assistive technology depends on Icon's own contract; the Link spec says 'no label (so it hides itself)' but doesn't state the attribute Lit's Icon uses, so Link passes only name and inline.

## 2026-09-18 19:40 — round 2

- Link: `label` is a required string prop and Link has no slot, so the common web-component form `<ds-link href>text</ds-link>` renders an unnamed anchor with no error; Feed and SidePanel stories did that and failed axe link-name. The doc should say plainly that Lit takes no slotted label content (and whether it should warn in development when label is empty, since the platform notes say it does not warn). I fixed the callers to use `label` and added no slot.
- Link: the SidePanel stories put `aria-current="page"` on the ds-link host for a nav drawer's current item, but Link has no current prop and doesn't forward it to its shadow anchor, so the state never reached assistive technology. I removed the attribute. The doc should say whether Link supports a current-page state (as Breadcrumb's last item and navigation drawers need) or whether consumers render Text for the current item.
- Link: the gate failures handed to this round were almost all other components (keyboard-run: Combobox, Tabs, Tree and others; axe: Tabs color-contrast, TreeGrid aria-hidden-focus and target-size); Link has no keyboard block and no Link story appears in either failure list, so I changed nothing for those.
- Link: I couldn't re-run axe in this session (running the check needs approval I couldn't get), so the Feed and SidePanel link-name fix is untested in a browser; logs/link-axe.mjs runs axe over the Link, Feed and SidePanel stories only.

## 2026-09-18 19:47 — round 3

- Link: the gates run over the whole Lit package, so their red status can't be cleared by changes to Link alone. This round's axe and keyboard-run failures are all in other components (Tabs color-contrast, TreeGrid aria-hidden-focus and target-size, Feed aria-required-children, and the keyboard specs of 18 other components); none names Link. The last Link-related failure, Feed and SidePanel link-name, is gone after round 2's story fix. The gate harness should scope each component's round to that component's stories and specs, or say that other components' failures are out of scope.

## 2026-09-19 10:12 — round 1

- Link: the reflect list names tone, external and download, but the Lit guidance says to 'reflect tone and external'; all three are reflected, as the reflect list is the declared contract.
- Link: the tone description scopes the link colours to 'the tone: default modifier' only for web; for Lit I scoped them with :host(:not([tone='inherit'])), so an unknown tone value falls back to the link colours rather than inherit.
- Link: the has-accessible-name and control-is-focusable scenarios don't say whether to assert on the host or the shadow anchor; the tests check the anchor's name and assert that focusing the host lands on the anchor through delegatesFocus.
- Link: externalIconGap says an override 'is written only while external is true', but not whether a stored override should be re-applied when external goes back to true later; the element re-applies it on any change to external or overrides.
- Link: the Lit notes say an empty href is the consumer's error and Link does not warn, so an empty href still renders <a href=""> (a link to the current page) rather than a non-link; left as written.

## 2026-09-19 10:21 — round 2

- Link: gates keyboard-run and axe run over the whole Lit Storybook and failed only on other components (keyboard: Combobox, DatePicker, Dialog, Feed, Listbox, Menu, Popover, Search, SegmentedControl, Select, SidePanel, Slider, Stepper, Tabs, Toast, Toolbar, Tooltip, Tree; axe: Tabs color-contrast in dark mode, TreeGrid aria-hidden-focus and target-size, among others). Link has no keyboard block so it has no keyboard spec, and all 7 Link/Lit stories pass axe in light and dark (checked with logs/link-axe.spec.ts). No Link change can make these gates pass; I left the code as it was rather than change another component's files in a Link job.
- Link: carried over from round 1: tone scoping on Lit uses :host(:not([tone='inherit'])), so an unknown tone value falls back to the link colours; the spec states the scoping only for web.

## 2026-09-19 10:30 — round 3

- Link: round 3 gave the same keyboard-run and axe output as round 2, and none of it is Link's. Link has no keyboard block, so there's no keyboard spec for it, and all 7 Link/Lit stories pass axe in light and dark (logs/link-axe.spec.ts, re-run this round). Both gates check the whole Lit Storybook, so Tabs' dark-mode color-contrast failures, TreeGrid's aria-hidden-focus and target-size failures, and the keyboard failures in 18 other components fail every job. The job loop should limit these gates to the component being generated (filter stories by title, as logs/link-axe.spec.ts does) or accept failures already in the baseline; no Link code change can make them pass.
- Link: carried over from round 1: tone scoping on Lit uses :host(:not([tone='inherit'])), so an unknown tone value falls back to the link colours; the spec states the scoping only for web.

## 2026-09-20 03:44 — round 1

- Link: the doc says the color/:visited/:hover rules are 'scoped to the tone: default modifier' but does not say which selector; since `tone` reflects and an invalid value would otherwise fall through to no colour at all, the rules are written `:host(:not([tone='inherit']))` so only the documented inherit case opts out.
- Link: the visually hidden span carrying copy.externalSuffix has no anatomy name (anatomy is anchor/label/externalIcon), so it gets no `part`/`data-part` — it is a plain class inside the anchor and after the `label` part. If tests or a theme need to address it, the doc needs a fourth anatomy entry.
- Link: externalIconGap is 'written only while external is true', but the doc does not say what happens when `external` flips back to false with an override still set; the inline custom property is removed from the host, so the hook falls back to its token default rather than persisting.
- Link: href and label start as '' and Link does not warn, but the doc does not say whether an empty href should still render the href attribute. It always renders (`href=""`), which keeps the anchor focusable so the derived control-is-focusable scenario passes on an element with no args set.
- Link: the doc specifies the muted-Text wrapper for ToneInherit/InsideMutedText but not for ToneDefault, so ToneDefault renders standalone from the meta render; the two tone stories therefore sit in different contexts on the component page, which makes them hard to compare side by side.
- Link: the doc gives no :host([hidden]) behaviour for an inline-by-default host; the package convention `display: none` is applied, which overrides `display: inline`.

## 2026-09-20 03:51 — round 2

- Link: the two failing gates (keyboard-run, axe) are whole-Storybook/whole-suite runs, so they report other components' pre-existing debt as this job's failure. Link has no keyboard block (no generated/keyboard/Link.lit.spec.ts) and all 7 Link/Lit stories pass axe in both modes with 0 violations; nothing in Link was changed, because no change to Link can turn those gates green.
- Link: the doc says the color/:visited/:hover rules are 'scoped to the tone: default modifier' but not which selector; since `tone` reflects and an unreflected or invalid value would otherwise get no link colour at all, the rules are written `:host(:not([tone='inherit']))` so only the documented inherit case opts out.
- Link: the visually hidden span carrying copy.externalSuffix has no anatomy name (anatomy is anchor/label/externalIcon), so it gets no `part`/`data-part` — it is a plain class inside the anchor, after the `label` part. A fourth anatomy entry would be needed for a test or theme to address it.
- Link: externalIconGap is 'written only while external is true', but the doc does not say what happens when `external` flips back to false with an override still set; the inline custom property is removed from the host, so the hook falls back to its token default rather than persisting.
- Link: href and label start as '' and Link does not warn, but the doc does not say whether an empty href should still render the attribute. It always renders (`href=""`), which keeps the anchor focusable so the derived control-is-focusable scenario passes on an element with no args set.
- Link: the doc specifies the muted-Text wrapper for ToneInherit/InsideMutedText but not for ToneDefault, which therefore renders standalone from the meta render; the two tone stories sit in different contexts on the component page and are hard to compare.
- Link: the doc gives no :host([hidden]) behaviour for an inline-by-default host; the package convention `display: none` is applied, overriding `display: inline`.

## 2026-09-20 03:58 — round 3

- Link: the two failing gates (keyboard-run, axe) are whole-suite runs and report other components' debt as this job's failure — round 3's output is byte-identical to round 2's. The gate artifacts name only DataGrid, Feed, Listbox, Select, Slider and TreeGrid (axe, both modes) and 18 other components (keyboard); Link has no keyboard block and all 7 Link/Lit stories pass axe with 0 violations in both modes. No change to Link can make either gate pass, so none was made.
- Link: the doc says the color/:visited/:hover rules are 'scoped to the tone: default modifier' but not which selector; since `tone` reflects and an unreflected or invalid value would otherwise get no link colour at all, the rules are written `:host(:not([tone='inherit']))` so only the documented inherit case opts out.
- Link: the visually hidden span carrying copy.externalSuffix has no anatomy name (anatomy is anchor/label/externalIcon), so it gets no `part`/`data-part` — it is a plain class inside the anchor, after the `label` part. A fourth anatomy entry would be needed for a test or theme to address it.
- Link: externalIconGap is 'written only while external is true', but the doc does not say what happens when `external` flips back to false with an override still set; the inline custom property is removed from the host, so the hook falls back to its token default rather than persisting.
- Link: href and label start as '' and Link does not warn, but the doc does not say whether an empty href should still render the attribute. It always renders (`href=""`), which keeps the anchor focusable so the derived control-is-focusable scenario passes on an element with no args set.
- Link: the doc specifies the muted-Text wrapper for ToneInherit/InsideMutedText but not for ToneDefault, which therefore renders standalone from the meta render; the two tone stories sit in different contexts on the component page and are hard to compare.
- Link: the doc gives no :host([hidden]) behaviour for an inline-by-default host; the package convention `display: none` is applied, overriding `display: inline`.

## 2026-09-20 13:16 — round 1

- Link: the 'Events' contract section says to dispatch each event as a CustomEvent under its emitted name with cancelable: true, which directly contradicts platforms.lit ('No custom event: the native click bubbles and retargets') and the events.onPress prose. Followed the platform note — nothing is dispatched, and the native click's own cancelability is the contract.
- Link: copy.external ('opens in new tab') has no consumer on Lit — the web/Lit notes only ever use copy.externalSuffix — yet the rules say to use every copy.* string verbatim. Omitted it; it is SwiftUI's accessibility-label suffix. Either mark it platforms: [swiftui] or say explicitly that Lit ignores it.
- Link: href is required but starts as '' , and the doc does not say what the anchor renders then. Kept href="" on the <a> (still focusable, navigates to the current page) rather than omitting the attribute (which would make it a non-link <a>). If an empty href should degrade to a non-focusable element, the doc has to say so — it currently only says the consumer should have rendered Text.
- Link: the web note says the colour rules are 'scoped to the tone: default modifier'. Used :host(:not([tone='inherit'])) rather than :host([tone='default']) so the link colours also apply before the reflected attribute lands and for any unknown tone value; a literal [tone='default'] selector would leave an un-upgraded or mis-valued link uncoloured.
- Link: focusRingOffset and focusRingWidth resolve to the same token (border.width.focus) and both are locked, so the two bindings are indistinguishable in the emitted CSS. Harmless here, but if they are meant to be tunable separately the offset needs its own token.
- Link: anatomy lists externalIcon, and the note says the gap goes on Link's own wrapper span, never on the Icon — so the part name is on the wrapper and the composed ds-icon carries no part and receives no forwarded override. The doc never states a forward for the icon's colour binding; relied on currentColor inheritance instead.

## 2026-09-20 13:19 — round 2

- Link: the keyboard-run gate is unsatisfiable for this component. a11y.requires lists `keyboard-operable` (and the guidance says 'Links are keyboard operable with Enter (2.1.1)'), but the doc has no `keyboard` block, so tools/keyboard_tests.ts emits no Link spec and any component-scoped keyboard-run has nothing to match. Either the doc should carry the one native rule (`keys: [Enter], action: activate the link, from: any, expect: manual, native: true`) so the gate reports coverage as a test.skip, or `keyboard-operable` should be documented as satisfied by the native <a> and excluded from the gate. I changed no code: inventing the rule means editing the doc, which this job may not do.
- Link: the 'Events' contract section says to dispatch each event as a CustomEvent under its emitted name with cancelable: true, which contradicts platforms.lit ('No custom event: the native click bubbles and retargets') and the events.onPress prose. Followed the platform note — nothing is dispatched, and the native click's own cancelability is the contract.
- Link: copy.external ('opens in new tab') has no consumer on Lit — the web/Lit notes only use copy.externalSuffix — yet the rules say to use every copy.* string verbatim. Omitted it; it is SwiftUI's accessibility-label suffix. Either scope it `platforms: [swiftui]` or say explicitly that Lit ignores it.
- Link: href is required but starts as '', and the doc does not say what the anchor renders then. Kept href="" on the <a> (still focusable, navigates to the current page) rather than omitting the attribute, which would make it a non-link <a>. If an empty href should degrade to a non-focusable element the doc has to say so; it currently only says the consumer should have rendered Text.
- Link: the web note says the colour rules are 'scoped to the tone: default modifier'. Used :host(:not([tone='inherit'])) rather than :host([tone='default']) so the link colours still apply before the reflected attribute lands and for any unknown tone value; a literal [tone='default'] selector would leave an un-upgraded link uncoloured.
- Link: focusRingOffset and focusRingWidth resolve to the same token (border.width.focus) and both are locked, so the two bindings are indistinguishable in the emitted CSS. If they are meant to be tunable separately the offset needs its own token.
- Link: anatomy lists externalIcon, and the note says the gap goes on Link's own wrapper span, never on the Icon — so the part name sits on the wrapper and the composed ds-icon carries no part and receives no forwarded override. The doc never states a forward for the icon's colour binding; relied on currentColor inheritance instead.

## 2026-09-20 13:22 — round 3

- Link: keyboard-run is unsatisfiable for this component and the failure is harness-side, not code-side. Link's doc has no `keyboard` block, so tools/keyboard_tests.ts writes no spec and a component-scoped Playwright filter matches nothing (proven: the filter without --pass-with-no-tests exits 1 with the reported message; today's tools/checks.ts does not schedule the gate at all). The running generator holds a pre-fix copy of tools/checks.ts in memory. Fix is to restart the runner / commit tools/checks.ts. Separately, the doc could carry the one native rule (`keys: [Enter], action: activate the link, from: any, expect: manual, native: true`) so the gate reports coverage as a test.skip instead of having nothing to assert while a11y.requires claims keyboard-operable.
- Link: the 'Events' contract section says to dispatch each event as a CustomEvent under its emitted name with cancelable: true, which contradicts platforms.lit ('No custom event: the native click bubbles and retargets') and the events.onPress prose. Followed the platform note — nothing is dispatched, and the native click's own cancelability is the contract.
- Link: copy.external ('opens in new tab') has no consumer on Lit — the web/Lit notes only use copy.externalSuffix — yet the rules say to use every copy.* string verbatim. Omitted it; it is SwiftUI's accessibility-label suffix. Either scope it `platforms: [swiftui]` or say explicitly that Lit ignores it.
- Link: href is required but starts as '', and the doc does not say what the anchor renders then. Kept href="" on the <a> (still focusable, navigates to the current page) rather than omitting the attribute, which would make it a non-link <a>. If an empty href should degrade to a non-focusable element the doc has to say so; it currently only says the consumer should have rendered Text.
- Link: the web note says the colour rules are 'scoped to the tone: default modifier'. Used :host(:not([tone='inherit'])) rather than :host([tone='default']) so the link colours still apply before the reflected attribute lands and for any unknown tone value; a literal [tone='default'] selector would leave an un-upgraded link uncoloured.
- Link: focusRingOffset and focusRingWidth resolve to the same token (border.width.focus) and both are locked, so the two bindings are indistinguishable in the emitted CSS. If they are meant to be tunable separately the offset needs its own token.
- Link: anatomy lists externalIcon, and the note says the gap goes on Link's own wrapper span, never on the Icon — so the part name sits on the wrapper and the composed ds-icon carries no part and receives no forwarded override. The doc never states a forward for the icon's colour binding; relied on currentColor inheritance instead.

## 2026-09-20 16:14 — round 1

- Link: the 'Events' contract section says to dispatch each event as a CustomEvent under its emitted name with cancelable: true, which contradicts platforms.lit ('No custom event: the native click bubbles and retargets') and the events.onPress prose. Followed the platform note — nothing is dispatched, and the native click's own cancelability is the contract. This contradiction has now survived several rounds; the Events section should be scoped to exclude events whose lit name is a native event.
- Link: copy.external ('opens in new tab') has no consumer on Lit — the web/Lit notes only ever use copy.externalSuffix — yet the rules say to use every copy.* string verbatim. Omitted it; it is SwiftUI's accessibility-label suffix. Either scope it `platforms: [swiftui]` or state that Lit ignores it.
- Link: the tone description says the colour rules are 'scoped to the tone: default modifier' but not which selector. Used :host(:not([tone='inherit'])) rather than :host([tone='default']) so the link colours still apply before the reflected attribute lands and for any unknown tone value; a literal [tone='default'] selector would leave an un-upgraded link uncoloured.
- Link: href is required but a Lit property needs an initial value, so it starts as ''. The doc does not say what the anchor renders then. Kept href="" on the <a> (still focusable, navigates to the current page) rather than omitting the attribute, which would make it a non-link <a> and fail the derived control-is-focusable scenario. If an empty href should degrade to a non-focusable element the doc has to say so.
- Link: focusRingOffset and focusRingWidth both resolve to border.width.focus and are both locked, so the two bindings are indistinguishable in the emitted CSS. If they are meant to be tunable separately the offset needs its own token.
- Link: the visually hidden span carrying copy.externalSuffix has no anatomy name (anatomy is anchor/label/externalIcon), so it gets no part/data-part — it is a plain class inside the anchor, after the label part. A fourth anatomy entry would be needed for a test or theme to address it.
- Link: the doc never states an override forward for the composed ds-icon's colour binding; the note says externalIconGap goes on Link's own wrapper, never on the Icon, so the icon carries no part and receives no forwarded override — it relies on currentColor inheritance. Worth stating explicitly, since every other composed child in the package has a declared forward or an explicit 'none'.
- Link: a11y.requires lists keyboard-operable but the doc has no `keyboard` block, so tools/keyboard_tests.ts emits no Link spec and a component-scoped keyboard-run has nothing to match. Either carry the one native rule (keys: [Enter], action: activate the link, from: any, expect: manual, native: true) so the gate reports coverage as a skip, or document keyboard-operable as satisfied by the native <a> and exclude it from the gate.
- Link: the doc specifies the muted-Text wrapper for ToneInherit and InsideMutedText but not for ToneDefault, which therefore renders standalone from the meta render; the two tone stories sit in different contexts on the component page and cannot be compared side by side.
- Link: the doc gives no :host([hidden]) behaviour for an inline-by-default host; the package convention `display: none` is applied, overriding `display: inline`.

## 2026-09-22 03:19 — round 1

- Link: generated/behavior/Link.lit.test.ts fails and takes all 9 of its tests with it. Its click-fires-on-press does `await userEvent.click(anchor)` with no cancel, so Chromium navigates to the Default href '/billing/history' and Vitest reports 'Cannot connect to the iframe ... Received URL: http://localhost:63315/billing/history'. The scenario's own description says 'On web and Lit the test cancels the click with preventDefault so the test page does not navigate', but tools/behavior_tests.ts emits no preventDefault anywhere (grep finds none) — the instruction is in prose the generator never reads. Nothing in Link.ts can fix it: a real <a href> must navigate. The hand-written packages/lit/src/Link.test.ts runs the same 9 scenarios and passes 9/9 precisely because it cancels the retargeted click. Fix belongs in the generator: cancel the default for a scenario whose `when` is a click on a navigating element, or have the emitted setup attach a document-level capture listener that calls preventDefault for the duration of a behavior run. This is invisible to the web package because jsdom does not navigate.
- Link: the generic 'Events' contract section says to dispatch each event as a CustomEvent under its emitted name with cancelable: true, which contradicts platforms.lit ('No custom event: the native click bubbles and retargets') and the events.onPress prose ('Link emits no press CustomEvent'). Followed the platform note — nothing is dispatched and the native click's own cancelability is the contract. This contradiction has now survived many rounds; the Events section should exclude any event whose lit name is a native event name.
- Link: copy.external ('opens in new tab') has no consumer on Lit — the web and Lit notes only ever use copy.externalSuffix, and the rn notes now say plainly that copy.external is unused on native — yet the rules say to use every copy.* string verbatim. Omitted it. Either scope it `platforms: [swiftui]` or state that web and Lit ignore it, so the 'every copy string' rule stays checkable.
- Link: the web notes say Link passes a consumer's aria-current through `...rest`, but Lit has no rest-props mechanism and the doc does not say whether an aria-current set on the <ds-link> host should be forwarded to the shadow anchor. It is not forwarded, so a navigation that marks its own current page reaches assistive technology on web but not on Lit — an earlier round had to delete aria-current from the SidePanel stories for exactly this reason. The doc should either declare the forward for Lit or say the current item is rendered as Text on every platform.
- Link: externalIconGap's override 'is written only while `external` is true', but the doc still does not say what becomes of a stored override when `external` flips back to false. The inline custom property is removed from the host, so the hook falls back to its token default rather than persisting, and it is re-applied if external becomes true again.
- Link: the derived renders-tone-default and renders-tone-inherit scenarios give only `renders: true`, with no observable that distinguishes the two tones — both tests can only assert that the anchor part exists, so they would pass even if the tone scoping were dropped entirely. The one thing that actually differs (the anchor's resolved color: var(--color-link) versus inherit) would need a computed-style expectation the scenario format cannot express.
- Link: the `transition` hook (--ds-link-transition) carries only motion.duration.fast; motion.easing.standard is written into the rule. An override of `transition` therefore changes the duration but can never change the easing. Consistent with the binding's token being a duration, but worth stating if easing is ever meant to be tunable.

## 2026-09-23 13:52 — round 1

- Link: `current` has no example and no story (the React package has none either), so no story shows aria-current; I added it as a meta arg defaulting to false and a control, not a `Current` story, to keep parity with React's story list.
- Link: the web notes say `current: false` writes nothing so a consumer's `aria-current` passed through `...rest` still applies; Lit has no rest spread onto the shadow anchor, so a consumer cannot give the Lit anchor `aria-current="step"`/`"location"` at all. I chose to write only `page` when `current` is true. The doc should say whether Lit supports a non-page aria-current, and how.
- Link: the web notes say `external` writes its modifier 'class or attribute' on both web and Lit; on Lit that is the reflected `external` host attribute (it is in platforms.lit.reflect). I added nothing more, and the doc could name the attribute explicitly for Lit.
- Link: the `external-link-announces-that-it-leaves` scenario's `copy` check reads the anchor's textContent, which includes the leading space of `copy.externalSuffix`; the test asserts the verbatim suffix string including that space. The doc should confirm the check is verbatim rather than trimmed.

## 2026-09-23 13:53 — round 2

- Link: the web platform notes say the locked bindings (color, colorHover, colorVisited, focusRing, focusRingWidth, focusRingRadius, focusRingOffset) 'get no --ds-link-* hook on web or Lit: their rules read the token directly, so consumer CSS cannot swap an accessibility-bearing colour'. That contradicts the prompt's Overrides section, the package conventions and the check_hooks gate, which all require a :host hook for every locked binding. I followed the gate and the Overrides section: :host now declares --ds-link-color, --ds-link-color-hover, --ds-link-color-visited and --ds-link-focus-ring(-width,-radius,-offset), each defaulting to its token, the rules read the hooks, and the bindings stay out of LinkOverridableBinding. The doc's notes should drop the 'no hook' sentence (React's Link fails the same gate for the same reason).
- Link: `current` has no example and no story (the React package has none either), so no story shows aria-current; I added it as a meta arg defaulting to false and a control, not a `Current` story, to keep parity with React's story list.
- Link: the web notes say `current: false` writes nothing so a consumer's `aria-current` passed through `...rest` still applies; Lit has no rest spread onto the shadow anchor, so a consumer cannot give the Lit anchor `aria-current="step"`/`"location"` at all. I chose to write only `page` when `current` is true. The doc should say whether Lit supports a non-page aria-current, and how.
- Link: the `external-link-announces-that-it-leaves` scenario's `copy` check reads the anchor's textContent, which includes the leading space of `copy.externalSuffix`; the test asserts the verbatim suffix string including that space. The doc should confirm the check is verbatim rather than trimmed.
