// Generated by tools/behavior_tests.ts from the `behavior` block of the Switch doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Switch } from '../../packages/rn/src/Switch';
import type { SwitchProps } from '../../packages/rn/src/Switch';
import meta from '../../packages/rn/src/Switch.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<SwitchProps> = {}) {
  const events = {
    onChange: jest.fn(),
  };
  const props: SwitchProps = { ...(meta.args as SwitchProps), ...given, onValueChange: events.onChange };
  const tree = (p: SwitchProps) => (
    <ThemeProvider mode="light">
      <Switch {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Switch') ?? screen.UNSAFE_root,
    track: () => screen.queryByRole('switch') ?? s.root(),
    label: () => screen.getByText(props.label),
    description: () => screen.getByText(props.description),
    rerender: (next: Partial<SwitchProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Switch', () => {
  test('click-on-track-toggles-on', () => {
    const s = setup({});
    fireEvent.press(s.track());
    expect(s.events.onChange).toHaveBeenCalledWith(true);
    expect(s.track()).toBeChecked();
  });
  test('click-on-label-toggles', () => {
    const s = setup({});
    fireEvent.press(s.label());
    expect(s.events.onChange).toHaveBeenCalledWith(true);
    expect(s.track()).toBeChecked();
  });
  test('click-on-description-toggles', () => {
    const s = setup({"description": "Sends a daily summary at 9:00."});
    fireEvent.press(s.description());
    expect(s.events.onChange).toHaveBeenCalledWith(true);
    expect(s.track()).toBeChecked();
  });
  test('toggles-back-off', () => {
    const s = setup({"defaultChecked": true});
    fireEvent.press(s.track());
    expect(s.events.onChange).toHaveBeenCalledWith(false);
    expect(s.track()).not.toBeChecked();
  });
  test('disabled-does-not-toggle', () => {
    const s = setup({"disabled": true});
    fireEvent.press(s.track());
    expect(s.events.onChange).not.toHaveBeenCalled();
    expect(s.track()).not.toBeChecked();
    expect(s.track()).toBeDisabled();
  });
  test('controlled-follows-prop', () => {
    const s = setup({"checked": false});
    fireEvent.press(s.track());
    expect(s.events.onChange).toHaveBeenCalledWith(true);
    expect(s.track()).not.toBeChecked();
  });
  test('controlled-updates-on-set', () => {
    const s = setup({"checked": false});
    s.rerender({"checked": true});
    expect(s.track()).toBeChecked();
  });
  test('description-is-rendered', () => {
    const s = setup({"description": "Sends a daily summary at 9:00."});
    expect(screen.getByText(new RegExp("Sends\\ a\\ daily\\ summary\\ at\\ 9:00\\."))).toBeOnTheScreen();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-label-position-start', () => {
    const s = setup({"labelPosition": "start"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-label-position-end', () => {
    const s = setup({"labelPosition": "end"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('switch', { name: s.props.label })).toBeOnTheScreen();
  });
});
