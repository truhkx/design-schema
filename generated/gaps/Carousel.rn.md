# Gaps reported while generating Carousel for rn

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 13:01 — round 1

- Carousel: "Previous and Next move one slide (or one page of perView)" doesn't define page alignment, so Next/Prev/autoplay/swipe-detection all advance exactly one slide index at a time; perView only changes how many slides are visible at once (item width = viewport/perView), never the step size.
- Carousel: styles.controlBackground (color.overlay.surface, locked) and controlShadow can't be applied to prevButton/nextButton directly — the composed Button has no plain 'background' override slot, and restyling a child is forbidden. Implemented as Button variant="ghost" (transparent) inside a small wrapper View that carries controlBackground/controlShadow and a radiusFull backdrop, so the 'overlaid arrows sit on a readable surface' intent is preserved without touching Button internals.
- Carousel: the play/pause control is rendered only when autoplay && !reducedMotion, since 'Starts only when the user has not asked for reduced motion' makes rotation permanently impossible under reduced motion — judged a visible-but-dead control worse than omitting it; a persistently-rendered disabled pause button may have been intended instead.
- Carousel: 'stops on hover, focus, touch' — hover doesn't apply on native. Touch is handled (onTouchStart on the region, onScrollBeginDrag on the track). Focus-based pausing could only be wired for the hand-built picker items; prevButton/nextButton/playButton are composed Buttons with no onFocus prop exposed, so focusing them cannot pause autoplay.
- Carousel: platforms.rn asks for accessibilityRole="adjustable" with increment/decrement actions on the region while also requiring prevButton/nextButton/picker to be individually reachable in tab order. Implemented without accessible={true} on the region so descendants stay individually focusable, with adjustable role/actions layered on as the documented swipe alternative — exact VoiceOver behavior for an adjustable-but-not-collapsed container is unverified.
- CarouselSlide.heading is a plain string prop (not rendered) used only to build each slide's accessibility name and its tabs-picker label, since RN can't extract text from arbitrary children; the visible content must repeat its own heading (e.g. inside a Card), mirroring the web model's separate aria-label.
- Carousel: snap=false only disables native paging/snapToInterval (scrolling/swiping still works); the spec only describes snap's 'on' behavior.
- Carousel: non-loop autoplay that reaches the last page simply stops advancing (no bounce/reverse), since the spec doesn't define wrap-less autoplay's end behavior.

## 2026-09-10 19:40 — round 1

- Carousel: "Previous and Next move one slide (or one page of perView)" doesn't define page alignment, so Next/Prev/autoplay/swipe-detection all advance exactly one slide index at a time; perView only changes how many slides are visible at once (item width = viewport/perView), never the step size.
- Carousel: styles.controlBackground (color.overlay.surface, locked) and controlShadow can't be applied to prevButton/nextButton directly since the composed Button has no plain 'background' override slot and restyling a child is forbidden. Implemented as Button variant="ghost" inside a small wrapper View carrying controlBackground/controlShadow and a radiusFull backdrop; the wrapper reuses the prevButton/nextButton testID rather than a separate controlSurface testID since the anatomy doesn't map controlSurface to its own composed element.
- Carousel: the play/pause control is rendered only when autoplay && !reducedMotion, since rotation can never start under reduced motion makes a live pause control meaningless; a persistently-rendered disabled pause button may have been intended instead.
- Carousel: 'stops on hover, focus, touch' — hover doesn't apply on native. Touch is handled (onTouchStart on the region, onScrollBeginDrag on the track). Focus-based pausing could only be wired for the hand-built picker items; prevButton/nextButton/playButton are composed Buttons with no onFocus prop exposed, so focusing them cannot pause autoplay.
- Carousel: platforms.rn asks for accessibilityRole="adjustable" with increment/decrement actions on the region while also requiring prevButton/nextButton/picker to be individually reachable in tab order. Implemented without accessible={true} on the region so descendants stay individually focusable, with adjustable role/actions layered on as the documented swipe alternative — exact VoiceOver behavior for an adjustable-but-not-collapsed container is unverified.
- CarouselSlide.heading is a plain string prop (not rendered) used only to build each slide's accessibility name and its tabs-picker label, since RN can't extract text from arbitrary children; the visible content must repeat its own heading (e.g. inside a Card), mirroring the web model's separate aria-label.
- Carousel: snap=false only disables native paging/snapToInterval (scrolling/swiping still works); the spec only describes snap's 'on' behavior.
- Carousel: non-loop autoplay that reaches the last page simply stops advancing (no bounce/reverse), since the spec doesn't define wrap-less autoplay's end behavior.

## 2026-09-10 19:40 — round 2

- Carousel: "Previous and Next move one slide (or one page of perView)" doesn't define page alignment, so Next/Prev/autoplay/swipe-detection all advance exactly one slide index at a time; perView only changes how many slides are visible at once (item width = viewport/perView), never the step size.
- Carousel: styles.controlBackground (color.overlay.surface, locked) and controlShadow can't be applied to prevButton/nextButton directly since the composed Button has no plain 'background' override slot and restyling a child is forbidden. Implemented as Button variant="ghost" inside a small wrapper View carrying controlBackground/controlShadow and a radiusFull backdrop; the wrapper reuses the prevButton/nextButton testID rather than a separate controlSurface testID since the anatomy doesn't map controlSurface to its own composed element.
- Carousel: the play/pause control is rendered only when autoplay && !reducedMotion, since rotation can never start under reduced motion makes a live pause control meaningless; a persistently-rendered disabled pause button may have been intended instead.
- Carousel: 'stops on hover, focus, touch' — hover doesn't apply on native. Touch is handled (onTouchStart on the region, onScrollBeginDrag on the track). Focus-based pausing could only be wired for the hand-built picker items; prevButton/nextButton/playButton are composed Buttons with no onFocus prop exposed, so focusing them cannot pause autoplay.
- Carousel: platforms.rn asks for accessibilityRole="adjustable" with increment/decrement actions on the region while also requiring prevButton/nextButton/picker to be individually reachable in tab order. Implemented without accessible={true} on the region so descendants stay individually focusable, with adjustable role/actions layered on as the documented swipe alternative — exact VoiceOver behavior for an adjustable-but-not-collapsed container is unverified.
- CarouselSlide.heading is a plain string prop (not rendered) used only to build each slide's accessibility name and its tabs-picker label, since RN can't extract text from arbitrary children; the visible content must repeat its own heading (e.g. inside a Card), mirroring the web model's separate aria-label.
- Carousel: snap=false only disables native paging/snapToInterval (scrolling/swiping still works); the spec only describes snap's 'on' behavior.
- Carousel: non-loop autoplay that reaches the last page simply stops advancing (no bounce/reverse), since the spec doesn't define wrap-less autoplay's end behavior.
- Carousel: the dev warning for interval < 5000 could not mention the literal '5000ms' in its message string without tripping the literals gate (it scans string/comment text for duration-shaped tokens, not just style properties) — reworded to 'raised to the minimum' instead of naming the value.

## 2026-09-16 11:02 — round 1

- Carousel: the RN platform note says the composed arrow and play Buttons expose no focus callback, so focus cannot pause rotation. The package Button does forward onFocus/onBlur, so I wired them (and the picker items' focus) to pause autoplay while focus lasts, as the Behavior section asks for web.
- Carousel: the platform notes say autoplay is 'cleared on any touch' / 'setInterval cleared in onTouchStart', but Behavior says touch pauses 'only while they last' and only the pause button stops it for good. I followed Behavior: the region's onTouchStart/onTouchEnd/onTouchCancel pause and resume, and only the pause button (or reaching the end without loop) stops rotation.
- Carousel: the region is accessibilityRole=adjustable but deliberately not `accessible`, as the notes require. On iOS a non-accessible View is not a VoiceOver element, so its increment/decrement actions may never be reachable. The spec doesn't reconcile keeping the controls individually reachable with a working adjustable region. I kept the note's form and added accessibilityValue (copy.slideLabel).
- Carousel: the accessibility action labels for increment/decrement are not in copy; I reused copy.next and copy.previous.
- Carousel: the spec gives no rule for where Next lands when fewer than perView slides remain, or what loop does with a partial last page. I chose: Next clamps to the last full page (total - perView), Next from there wraps to 0 with loop, and Prev from 0 wraps to total - perView. Picker indices are clamped the same way.
- Carousel: a dots picker with perView > 1 has one dot per slide (copy.goTo is per slide), but the spec doesn't say which dots are 'current'. I marked every slide on the current page as selected.
- Carousel: the Tab order in the keyboard rules (play/pause, previous, next, picker, then slide content) can't be matched without extra layout tricks, because the picker sits visually below the slides. Native focus follows view tree order, so the RN order is play, previous, next, slides, picker.
- Carousel: the tabs picker has no tab typography, colour or selected-indicator tokens (only dot tokens exist). I composed the package Text: size sm, weight semibold when selected and regular otherwise, tone strong when selected and muted otherwise, padding space.sm, with a size.target.comfortable target (the minTarget binding). Dots use dotTarget (size.target.min).
- Carousel: the `transition` binding covers 'dot and tab state changes'. Tabs change weight and tone, which can't animate, so only the dot background colour animates (motion.easing.standard, instant under reduced motion).
- Carousel: the controlSurface radius is described only in the web notes ('a full radius') and has no binding; I used radius.full on RN.
- Carousel: controlOffset has no `part`; I applied it as horizontal padding on the overlay that holds the two controlSurfaces inside the viewport. The play button's distance above the viewport is not specified; I used layout.gap.tight as padding (not a margin).
- Carousel: prevButton, nextButton and controlSurface parts can't carry `Carousel.<part>` testIDs on the composed Button (Button hard-codes testID="Button"), so each part testID sits on a wrapper View: controlSurface wraps the prevButton/nextButton View, which wraps the Button. Both controlSurface wrappers share one testID.
- Carousel: 'fewer are shown as the viewport narrows (one below the prose width)' comes with no RN measurement rule; I compared the viewport's onLayout width with t.layoutMaxWidthProse. Before the first layout, perView applies as given.
- Carousel: the notes use onViewableItemsChanged for the index, but it also fires during scrolls started by code (arrows, picker, controlled prop changes), which would be misreported as `swipe`. I only honour it between onScrollBeginDrag and onMomentumScrollEnd; this isn't specified.
- Carousel: the interval dev warning is only shown when autoplay is on (the value is unused otherwise); the spec doesn't say.
- Carousel: the liveRegion accessibilityLiveRegion switches to 'none' while rotating, following the web aria-live rule; the RN notes only mention announceForAccessibility, which I also call on iOS for user-initiated changes.
- Carousel: the AmbientHero and ThreeUpGallery examples call for photographs and images, but the package has no image assets or Image component; the stories use strong-surface Boxes with a visible heading as stand-ins.
- Carousel: the Keyboard story has no `given` to follow; it turns autoplay on so the play/pause control is present, and puts a focusable Button in each slide (demo text 'Add to cart' is story content, not copy).

## 2026-09-17 13:36 — round 1

- Carousel: the RN notes say pagingEnabled at perView 1, but with slideGap > 0 each slide plus gap is wider than the viewport, so native paging drifts by one gap per page; chose pagingEnabled only when the page is 1 and the gap is 0, and snapToInterval (itemWidth + gap) otherwise.
- Carousel: pressing play on the last slide without loop 'restarts rotation from the first slide', but the doc gives no onChange reason for that move; chose 'autoplay' (so it is not announced).
- Carousel: the web notes say pressing play clears hover/focus/touch pauses, but the RN notes say rotation pauses while the play button has focus, which on react-native-web would block rotation right after a keyboard press of play; chose to clear the touch and focus pauses on play (the next focus change pauses again).
- Carousel: 'reaching the last slide without loop counts as stopped' doesn't say whether that covers the arrows or a swipe reaching the end while autoplay is paused, or only an autoplay step; chose any arrival at the last page while playing.
- Carousel: an RN picker item's focus ring has no radius binding (dotTarget/minTarget/focusRing give size and colour only); chose a square ring (no radius) rather than borrowing a token.
- Carousel: the tabs picker's label has no lineHeight binding, so none is set (platform default); the doc doesn't say whether it should follow font.lineHeight.normal.
- Carousel: dots have no RN equivalent of aria-current; chose accessibilityState.selected on every dot of the current page, and the picker row is role='group' (dots) or 'tablist' (tabs) labelled copy.pickerLabel.
- Carousel: the doc doesn't say whether a slide's increment/decrement actions should stay listed at an end without loop; they stay and do nothing, since the Button-style disabled state has no per-action equivalent.
- Carousel: accessible={visible} on each slide makes the visible slide one VoiceOver element, so its inner focusable content (e.g. a Card's Button) is no longer reachable one by one on iOS; the doc asks for the slide as the adjustable element and doesn't address its interactive content.
- Carousel: onMomentumScrollEnd doesn't fire on iOS when a drag ends without momentum, so the swipe window (onScrollBeginDrag → onMomentumScrollEnd) can stay open; the doc names only those two events, so no onScrollEndDrag fallback was added.
- rn conventions digest: toLineHeight is documented as (lineHeight, fontSize), but theme.tsx declares toLineHeight(fontSize, multiplier).

## 2026-09-21 19:02 — round 1

- Carousel: `dotRadius` is declared with part `pickerItem` but its description says a tab has no radius, so the binding only ever reaches dots. Applied it to both the dot shape and the dot Pressable's focus ring; the tab's focus ring keeps no radius. An override of `dotRadius` therefore silently does nothing in a `tabs` picker.
- Carousel: `tabLineHeight` is a `font.lineHeight.*` multiplier and React Native needs an absolute lineHeight. Resolved it against the *effective* `tabFontSize`, so overriding `tabFontSize` also rescales the line height; the doc doesn't say whether the two should be coupled.
- Carousel: the onChange contract asks for `onScrollEndDrag when no momentum follows` and forbids a timer, but React Native fires `onMomentumScrollBegin` *after* `onScrollEndDrag`, so 'no momentum follows' is unknowable at that instant. Chose to settle at `onScrollEndDrag` and reopen the swipe window in `onMomentumScrollBegin` when a drag had just ended; a programmatic animated scroll (which fires the same momentum events) clears the flag first so it is never read as a swipe. Consequence the doc doesn't address: a controlled carousel runs its scroll-back twice for a flick — harmless because scrollToOffset to the same offset is idempotent.
- Carousel: the web platform notes say 'the icon set has no play or pause glyph', so the play control is a text-labelled Button. tools/icon-paths.json (and packages/rn/src/paths.ts) DO define `play` and `pause` glyphs. Followed the composition contract (playButton = Button, variant secondary, no iconOnly) and kept copy.play/copy.pause as the visible label, but the stated reason is stale.
- Carousel: 'a click that lands on the part wrapper itself runs the same action the Button runs' has no React Native meaning — a plain View with no onPress receives nothing, and adding a Pressable wrapper around a Button would create a second accessibility element. The prevButton/nextButton/playButton wrappers are inert and carry only the part testID.
- Carousel: `minTarget` says the arrow's part wrapper 'stretches to fill that surface'. The wrapper now stretches, but the composed Button inside it keeps its own intrinsic size and cannot be stretched without restyling a child, so the pressable area is still the Button's, not the full 44px controlSurface. Either Button needs a `fill` prop in its schema or the binding's promise cannot be kept on native.
- Carousel: a11y.requires lists `arrow-navigation`, and the guidance describes a roving tab stop with ArrowLeft/Right/Home/End in the picker, but the resolved keyboard section for rn keeps only the native Enter/Space rule. Implemented no arrow handling, so under react-native-web the picker is N tab stops rather than one — the two sections disagree and the keyboard section was taken as authoritative.
- Carousel: each visible slide is `accessibilityRole="adjustable"`, which VoiceOver announces with a value, but no copy string or binding defines one (copy.slideLabel is spent on the accessible name). Left accessibilityValue unset; the slide announces its name and its increment/decrement actions only.
- Carousel: the `has-accessible-name` scenario cannot use getByRole on native — the region is deliberately not `accessible` so its controls stay individually reachable, which keeps it out of RNTL's role queries. The test asserts `role="region"` and the accessible name on the `Carousel` testID root instead; the scenario's platform-neutral wording assumes a queryable role.
- Carousel: `pickerOffset` is described as the gap between the picker row and the viewport *and* between the play/pause row and the viewport, so it is the root View's single `gap` — which also spaces the play row from the viewport at the same value. The doc names no separate token for the play row's offset.

## 2026-09-21 19:08 — round 2

- Carousel: the rn notes require accessibilityRole="adjustable" with increment/decrement actions on each visible slide, but the rn build also ships on react-native-web (where the axe gate runs), and RNW maps that role to role="slider" — which requires aria-valuenow and must not contain another control. That made every story fail aria-required-attr and any slide with a focusable child fail nested-interactive. Chose a Platform.OS branch: native keeps adjustable + the actions + accessibilityValue, web renders the slide as the role="group" the web platform notes describe. The doc gives one rn implementation and never says which platform's semantics win under react-native-web.
- Carousel: the web notes say a slide is role="tabpanel" when picker is `tabs` and role="group" otherwise, but the rn notes never mention tabpanel and RN wires no aria-controls between picker and slides. Chose role="group" for every slide on react-native-web rather than an unreferenced tabpanel.
- Carousel: the track is a scroll container, so react-native-web fails axe `scrollable-region-focusable` (WCAG 2.1.1) unless it is a tab stop — the gate only passed on the Keyboard story, whose slides happen to hold Buttons. Added tabIndex 0 on web only, as packages/react does on its viewport. The keyboard rules list Tab as play/pause → previous → next → picker → the current slide's content and never mention a viewport stop, so this inserts one the doc does not describe.
- Carousel: `accessibilityElementsHidden` and `importantForAccessibility` are native-only and dropped by react-native-web, so 'hidden slides are inert' had no effect there — a hidden slide's links and buttons stayed in the tab order. Added `inert` on non-visible slides on web. The rn notes name only the two native props.
- Carousel: an `adjustable` slide announces a value in VoiceOver but the doc specifies none (copy.slideLabel is spent on the accessible name). Chose accessibilityValue { min: 1, max: total, now: index + 1, text: copy.slideLabel } on native.
- Carousel: `dotRadius` is declared with part `pickerItem` but its description says a tab has no radius, so the binding only ever reaches dots — applied to the dot shape and the dot's focus ring, leaving the tab's ring square. An override of `dotRadius` silently does nothing in a `tabs` picker.
- Carousel: `tabLineHeight` is a font.lineHeight multiplier and React Native needs an absolute lineHeight; resolved it against the *effective* `tabFontSize`, so overriding `tabFontSize` rescales the line height too. The doc doesn't say whether the two are coupled.
- Carousel: the onChange contract asks for `onScrollEndDrag when no momentum follows` and forbids a timer, but React Native fires onMomentumScrollBegin *after* onScrollEndDrag, so 'no momentum follows' is unknowable at that instant. Chose to settle at onScrollEndDrag and reopen the swipe window in onMomentumScrollBegin when a drag had just ended; a programmatic animated scroll (which fires the same momentum events) clears the flag first so it is never read as a swipe. A controlled carousel therefore runs its scroll-back twice for a flick — harmless because scrollToOffset to the same offset is idempotent.
- Carousel: the web notes justify the text-labelled play Button with 'the icon set has no play or pause glyph', but tools/icon-paths.json and packages/rn/src/paths.ts both define `play` and `pause`. Followed the composition contract (playButton = Button, variant secondary, not iconOnly) with copy.play/copy.pause as the visible label; the stated reason is stale.
- Carousel: 'a click that lands on the part wrapper itself runs the same action the Button runs' has no React Native meaning — a plain View with no onPress receives nothing, and a Pressable wrapper around a Button would add a second accessibility element. The prevButton/nextButton/playButton wrappers are inert and carry only the part testID.
- Carousel: `minTarget` says the arrow's part wrapper 'stretches to fill that surface'. The wrapper stretches, but the composed Button inside keeps its intrinsic size and cannot be stretched without restyling a child, so the pressable area is still the Button's, not the full controlSurface. Button would need a `fill` prop in its schema for the binding's promise to hold on native.
- Carousel: a11y.requires lists `arrow-navigation` and the guidance describes a roving tab stop with ArrowLeft/Right/Home/End in the picker, but the resolved keyboard section for rn keeps only the native Enter/Space rule. Implemented no arrow handling, so under react-native-web the picker is N tab stops rather than one; the two sections disagree and the keyboard section was taken as authoritative.
- Carousel: the `has-accessible-name` scenario cannot use getByRole on native — the region is deliberately not `accessible` so its controls stay individually reachable, which keeps it out of RNTL's role queries. The test asserts role and name on the `Carousel` testID root instead.
- Carousel: `pickerOffset` is described as the gap between the picker and the viewport *and* between the play/pause row and the viewport, so it is the root View's single `gap`; no separate token is named for the play row's offset.

## 2026-09-23 14:51 — round 1

- Carousel: NOT A DOC GAP, job status: the generation is unfinished and unverified. A second session (ds-regen-rn-d5) edited packages/rn/src/Carousel.tsx in the same worktree at the same time. Some of the aria-* mirrors in the file are its edits, not mine. Carousel.stories.tsx (Keyboard story still autoplay, not picker: tabs), Carousel.test.tsx and index.ts were not touched. jest and tsc were not run.
- Carousel: 'Mirror every accessibility prop as aria-*' conflicts with the web contract for dots. accessibilityState.selected would mirror as aria-selected, which is not allowed on role=button (axe aria-allowed-attr). Chose accessibilityState.selected on native and aria-current="true" on every dot of the current page, following web's aria-current. The doc should say that on the rn platform the dot's mirror is aria-current, not aria-selected.
- Carousel: the web notes give slides aria-roledescription="slide" and the region aria-roledescription="carousel", but the rn notes don't say whether react-native-web should carry them. RN's View types omit both. Chose to spread them as DOM-only attributes on web builds (Record<string, unknown>, as Breadcrumb/DatePicker do).
- Carousel: the web notes make slides role="tabpanel" when picker is tabs, with tabs carrying aria-controls. The rn notes don't mention tabpanel or aria-controls, and RN 0.87 has no aria-controls prop (Combobox/Select set it on the DOM node). Chose role tabpanel on web when picker is tabs, and no aria-controls on the tabs. The doc should say whether rn-web needs the idref and how to guarantee the FlatList renders every panel it names.
- Carousel: onChange says a swipe 'settles' at onScrollEndDrag/onMomentumScrollEnd, but the rn notes also say onViewableItemsChanged is 'honoured only during a user drag'. That leaves open whether each viewability change during a drag reports immediately or only the index at settle. Chose to record the first visible index during the drag and report it once at settle.
- Carousel: the rn notes ask for accessibilityValue on each visible slide. The digest rule wants aria-valuenow/min/max/text mirrored beside every accessibilityValue, while the same notes forbid a value on the react-native-web build. Left the native branch without aria-value* mirrors. The doc should state that the mirror rule is waived when the native-only branch never runs on web.
- Carousel: the Keyboard story is specified two ways. Guidance says 'those slides with picker: tabs'; the generic rule asks for 'rendering it open with its trigger and at least three focusable children'. The existing story uses autoplay and a Button inside each slide. Not reconciled: I planned the default four slides with picker: 'tabs' and nothing else, but did not write it.
- Carousel: the missing-CarouselSlide-label development warning has no stated frequency. interval's warning is explicitly 'once ever per instance'; the label warning is left firing on every children change.
- (report recovered after a second request)

## 2026-09-23 14:51 — round 1

- Carousel: the web notes ask for aria-controls from the previous/next Buttons to the track's id and from each dot to its slide, but the composed Button exposes no aria-controls prop and a child is never restyled or reached into; omitted on rn (react-native-web). Button would need an aria-controls/controls prop in its schema.
- Carousel: the web notes make each slide role="tabpanel" when picker is `tabs`, but the tabs have no aria-controls and the panels no aria-labelledby on rn, so the tab/tabpanel pairing is conveyed only by aria-selected and the announcement (as Lit does). Chose tabpanel for tabs and group otherwise on react-native-web.
- Carousel: aria-roledescription (region 'carousel', slide 'slide') and a dot's aria-current are missing from React Native's View/Pressable types, so they are spread from Record<string, unknown> constants on react-native-web only; the rn notes don't say which web-only attributes to emit under react-native-web.
- Carousel: the rn notes say a dot carries accessibilityState.selected as native's form of aria-current, but the mirror rule says to write aria-selected alongside accessibilityState, which axe rejects on role=button. Chose aria-current="true" on web for dots and aria-selected for tabs.
- Carousel: the Keyboard story is 'those slides with picker: tabs'; its focusable children are previous, next, the tabs and the track (a tab stop on react-native-web). The previous story's per-slide Buttons were dropped, because on native the visible slide is one VoiceOver element and its inner controls are unreachable anyway.
- Carousel: this job's file was edited concurrently by a second generator session on the same job (it added pendingSwipe, which reports a swipe at settle rather than mid-drag, plus a duplicate attribute-constant block). Kept its swipe change and its IS_WEB-guarded constants, and removed the duplicate.

## 2026-09-23 19:25 — round 1

- Carousel: the tabs picker is described as a Tabs-style tablist controlling slides, but the RN notes give no aria-controls or accessibilityControls wiring between a tab and its slide; I set role=tab and selected state only.
- Carousel: 'slide movement is instant under reduced motion' is written for web scroll-snap; for RN I chose scrollToOffset with animated=false under reduced motion and on first positioning, since the doc names no RN equivalent.
- Carousel: the RN notes say the arrow wrapper is inert, but not what a disabled arrow looks like beyond Button's own disabled state; I rely on Button's disabled handling (opacity.disabled) and add nothing on the wrapper.
- Carousel: on RN, what a controlled activeIndex above total minus page does is not stated; I clamp it to total minus page, as the picker rule does.
- Carousel: the Keyboard story's focusable count is described for the web (controls, tabs, viewport); the RN doc says nothing is focusable natively, so the story only exists for the axe gate on react-native-web.
- Carousel: the tab label's a11y name uses the slide label, but the doc does not say whether a missing label should also drop the aria-selected mirror; I keep both and fall back to copy.goTo as the text.
