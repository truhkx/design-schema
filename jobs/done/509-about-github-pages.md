Finish the remaining pages and wire the deploy per website-plan.md ("About and GitHub", "Where this lands relative to the existing Pages plan").

1. `apps/website/src/pages/about.astro`: `Container(width="prose")` → `Stack` of `Heading`/`Text` content adapted from `vision-and-decisions.md`'s "Who it is for" (Adopter / Owner) and `guides/two-ways-in.md`.
2. Confirm the header's GitHub entry (job 503, sourced from `generated/nav.json`) is a plain external `Link` (new tab) to the repo, not a route — no page to build here beyond that check.
3. `apps/website/src/components/Footer.astro`: `Landmark(role="contentinfo")`, a `Divider`, a `Stack` of `Link`s (GitHub, npm, License), and a `Text(tone="muted")` copyright line; wire into `Layout.astro` below the page slot.
4. `.github/workflows/deploy-pages.yml` (hand-delivered): now builds and deploys `apps/website` only — the three Storybooks moved to Chromatic (job 510) and are no longer part of this artifact, which also fixes the single-point-of-failure the combined build used to have (a Storybook build breaking no longer blocks the docs site from deploying).
5. Update `vision-and-decisions.md` §3 ("Pages site") to say docs at `/docs` means `apps/website`'s docs section (this plan), not a Starlight build, and that Storybooks are published to Chromatic rather than folded into the Pages artifact.
Gate: `astro build` for `apps/website` succeeds and deploys on its own; the deployed site serves `/`, `/about`, and all 51 `/docs/components/<slug>` routes, and a broken Chromatic publish (job 510) does not fail this workflow. Do not touch `.github/workflows/*` by any means other than the delivery method `vision-and-decisions.md` already names for protected workflow files (hand delivery, not a direct commit from this job if that constraint still applies at the time this runs).
