// Generated by tools/behavior_tests.ts from the `behavior` block of the Splitter doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Splitter } from '../../packages/react/src/Splitter';
import type { SplitterProps } from '../../packages/react/src/Splitter';
import meta from '../../packages/react/src/Splitter.stories';

function setup(given: Partial<SplitterProps> = {}) {
  const events = {
    onSizeChange: vi.fn(),
    onSizeChangeEnd: vi.fn(),
    onCollapseChange: vi.fn(),
  };
  const props = { ...meta.args, ...given, onSizeChange: events.onSizeChange, onSizeChangeEnd: events.onSizeChangeEnd, onCollapseChange: events.onCollapseChange };
  const utils = render(<Splitter {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Splitter"]') ?? screen.queryByRole('separator') ?? utils.container.firstElementChild) as HTMLElement,
    container: () => (screen.queryByRole('separator') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<SplitterProps>) => utils.rerender(<Splitter {...props} {...next} />),
  };
  return s;
}

describe('Splitter', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-orientation-horizontal', async () => {
    const s = setup({"orientation": "horizontal"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-orientation-vertical', async () => {
    const s = setup({"orientation": "vertical"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-stack-below-prose', async () => {
    const s = setup({"stackBelow": "prose"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-stack-below-content', async () => {
    const s = setup({"stackBelow": "content"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-stack-below-never', async () => {
    const s = setup({"stackBelow": "never"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('separator', { name: s.props.label })).toBeInTheDocument();
  });
});
