// Generated by tools/behavior_tests.ts from the `behavior` block of the Fieldset doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Fieldset } from '../../packages/rn/src/Fieldset';
import type { FieldsetProps } from '../../packages/rn/src/Fieldset';
import meta from '../../packages/rn/src/Fieldset.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<FieldsetProps> = {}) {
  const events = {};
  const props: FieldsetProps = { ...(meta.args as FieldsetProps), ...given };
  const tree = (p: FieldsetProps) => (
    <ThemeProvider mode="light">
      <Fieldset {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Fieldset') ?? screen.UNSAFE_root,
    group: () => screen.queryByRole('group') ?? s.root(),
    rerender: (next: Partial<FieldsetProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Fieldset', () => {
  test('the-legend-names-the-group', () => {
    const s = setup({"legend": "Delivery window"});
    expect(screen.getByText(new RegExp("Delivery\\ window"))).toBeOnTheScreen();
  });
  test('the-description-is-rendered', () => {
    const s = setup({"description": "We only ship within the EU."});
    expect(screen.getByText(new RegExp("We\\ only\\ ship\\ within\\ the\\ EU\\."))).toBeOnTheScreen();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gap-tight', () => {
    const s = setup({"gap": "tight"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gap-normal', () => {
    const s = setup({"gap": "normal"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gap-loose', () => {
    const s = setup({"gap": "loose"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('group', { name: s.props.legend })).toBeOnTheScreen();
  });
  test('error-is-identified', () => {
    const s = setup({"error": "Fix this before continuing."});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeOnTheScreen();
  });
});
