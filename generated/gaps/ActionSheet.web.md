# Gaps reported while generating ActionSheet for web

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 03:16 — round 1

- ActionSheet: the wide/Menu presentation must anchor 'to the element that was focused when open became true' (an arbitrary external DOM node), but Menu owns and renders its own internal trigger Button — there's no API to anchor Menu to a foreign element. Chose to render Menu with its own trigger positioned (via the sanctioned style-prop passthrough) at a fixed rect captured from document.activeElement at open time, made invisible (opacity 0, pointer-events none) so Menu's popup positions itself relative to that point. This leaves a zero-opacity, pointer-events-none but still keyboard-focusable proxy button in the DOM whenever the wide sheet is open — not a perfect substitute for anchoring to the real opener.
- ActionSheet: Menu's onOpenChange(false) fires for both a real dismissal (Escape/outside click) and, just before onAction, for a chosen action — it doesn't say which, and ActionSheet's contract requires onClose to fire only for the former. Worked around with a same-tick setTimeout that the paired onAction call cancels. Also, Menu doesn't distinguish escape from an outside click, so both collapse to onClose('escape') in the wide presentation (the only one of the four reasons — escape/scrim/cancel/drag — that fits a non-modal, scrim-less, cancel-row-less surface).
- ActionSheet: general generation instructions mention overlay portals may accept a `container` prop override, but the component's own YAML props list has no `container` prop (only open/title/actions/cancelLabel). Followed the schema as source of truth and hardcoded `document.body` as the portal target, unlike BottomSheet/Popover which do declare `container`.
- ActionSheet: anatomy lists no 'handle' part, but the platform notes say the phone presentation reuses BottomSheet's <dialog> mechanics 'as BottomSheet', which includes a drag handle enabling the drag-to-dismiss gesture referenced by the 'drag' close reason and the gesture-alternative requirement. Rendered a decorative grab handle (aria-hidden, no data-part since it isn't a named anatomy part) to support this, matching BottomSheet visually.
- ActionSheet: styles list gives `itemGap` the description 'Between icon and label' only (no separate token for vertical spacing between rows). Interpreted this literally — rows are stacked with no gap and get their rhythm entirely from `itemPaddingBlock`, unlike Menu.css which reuses its `itemGap` for both row spacing and icon-label spacing.
- ActionSheet: `fontFamily` and `lineHeight` are generic overridable bindings but the title renders through a composed `Text`, which always sets its own font-family/line-height (never inherits). Forwarded both bindings into Text's own `overrides` (alongside `titleSize`→Text `fontSize`) as well as applying them as root CSS hooks for the item rows, so a single override value affects both consistently — mirroring the `Divider → Text fontSize` precedent, extended by analogy to `fontFamily`/`lineHeight` since no other component schema exercises that exact combination.
- ActionSheet: the forwardRef exposes the phone presentation's `HTMLDialogElement` only; in the wide/Menu presentation the consumer's ref is not attached to anything (Menu's root is an `HTMLDivElement`, an incompatible type), unlike BottomSheet's wide branch which forwards the same ref to Dialog because both render a native `<dialog>`.

## 2026-09-10 20:41 — round 1

- ActionSheet: the file already existed but was generated against a stale schema draft (prop named `title`, no `dismissible`, no `header`/`handle` anatomy parts, no `headerPaddingBlock` binding, no `container` portal prop) — updated it to match the current schema: renamed `title`→`heading`, added `dismissible` (gated via the same requestClose pattern as BottomSheet/Dialog, Escape still reports), added the `header` anatomy part wrapping handle+heading with `headerPaddingBlock` (also applied to the cancel row per the token's description), added `data-part="handle"`/`data-part="heading"`, and added the `container?: HTMLElement` portal-target prop required by the platform-wide portal convention (forwarded to the wide-mode Menu too).
- ActionSheet: fixed a pre-existing bug in the wide (Menu) presentation's onOpenChange mapping — it collapsed every non-action reason to 'escape', but the Behavior section explicitly maps Menu's 'outside' reason to 'scrim'; now 'outside'→'scrim', 'escape'→'escape', and the unused 'trigger'/'controlled' reasons (which shouldn't fire in this anchor-only mode) fall back to 'escape'.
- ActionSheet: the schema's keyboard rule for Tab on the wide-screen (Menu) presentation is `expect: manual` and isn't testable/enforceable in code (a menu popup already isn't a tab-stop container by virtue of not being in the tab order when using roving tabindex plus outside-click-to-close) — no explicit handler was added; flagging since it's an 'expect: manual' item rather than one with concrete pass/fail behavior.
- ActionSheet: the prompt's Behavior scenarios section implies scenarios become tests, but the Output section only requests `.tsx`/`.stories.tsx` and this package's test-generation rollout is opt-in per component (per prior session notes, `--adopt`/test generation is not to be run without being asked) — no `ActionSheet.test.tsx` was written; both derived scenarios (`renders`, `has-accessible-name`) are satisfiable by the existing `Default` story and `aria-label`/visible heading text, so nothing here is expected to fail if tests are added later.

## 2026-09-16 07:18 — round 1

- ActionSheet: platforms.web.notes says Menu 'takes no external anchor' and asks for the Menu host to be moved to the opener's position with its own trigger hidden (opacity 0, pointer-events none, tabindex -1, aria-hidden). The package's Menu already has `anchor?: RefObject<HTMLElement | null>`, which renders no trigger and puts focus back on the opener, so I passed the opener ref as `anchor` instead. The note needs updating to match Menu.
- ActionSheet: Button always sets its own data-part="container", so a data-part on the composed Button is overwritten. I put data-part="cancelButton" on the row that wraps the Button (as AlertDialog/Dialog do for their buttons); the behavior test clicks `[data-part=cancelButton] button`. The doc does not say where a composed Button's part hook goes.
- ActionSheet: the `maxWidth` override cannot move the breakpoint. The breakpoint is read from `--layout-max-width-prose` on <html> through matchMedia, before any root element exists to carry an inline hook, so `overrides.maxWidth` only sets a hook that nothing reads. The doc lists maxWidth as overridable without saying how an override reaches a media query.
- ActionSheet: `overrides` has no effect in the wide presentation. The doc says nothing about forwarding them to Menu (radius/shadow/itemPadding*/fontFamily/layer map naturally onto Menu bindings); I forwarded nothing.
- ActionSheet: `dismiss` in the overlay block lists `close-button` and `swipe`, while the onClose reasons are `cancel` and `drag`. I treated close-button as the cancel row and swipe as the drag and reported the event's reason names.
- ActionSheet: the `titleColor` (locked) and `titleSize` bindings have no `part`. I read them as the heading Text: tone=muted size=sm, with titleSize forwarded as Text `fontSize`. The doc does not name the heading as their part or say that titleSize goes through Text.
- ActionSheet: the doc has `itemGap` apply to the item row but `fontFamily`, `fontSize` and `lineHeight` have no part. I applied them to each item row, not the list.
- ActionSheet: `divider` covers 'above the danger group and above the cancel row', but nothing says whether a sheet whose actions are all danger (or none) still gets the first divider. I draw it only when both groups are non-empty; the wide Menu gets a separator under the same rule.
- ActionSheet: the `enter`/`exit` durations have no easing binding. The rules say motion.easing.standard for both directions, so I used it for both, although an exit easing token may exist.
- ActionSheet: the doc says Enter/Space 'chooses the focused action and closes', while onAction says the consumer closes. Enter/Space only fire onAction through the native button click, and the sheet stays open until `open` changes. For a disabled item, Enter/Space do nothing.
- ActionSheet: the Tab rule is 'wide-screen menu presentation' only; in the sheet presentation the FocusScope traps Tab between the menu's single tab stop and the Cancel button. The doc does not say what Tab does in the sheet.
- ActionSheet: the UnnamedSheet example's `given` has no heading, but the stories' default args include `heading: "Photo.jpg"`. To keep 'exactly its given', the story sets `heading: undefined` explicitly. The doc does not say whether example args are applied on top of the Default story's args or replace them.
- ActionSheet: the doc names no drag start (distance or angle) that tells a drag apart from a tap. Any pointerdown on the header starts a drag, and a release is scored on distance/height > 0.25 or speed > 1.5 px/ms.
- ActionSheet: the `anatomy` list includes `focusScope` and `itemIcon`, but the rules for the wide presentation give no part hooks. Menu owns them there, so no ActionSheet data-part values appear when wide; in the sheet `itemIcon` is a span wrapping Icon, since the doc does not say whether Icon passes data-* attributes through.

## 2026-09-17 11:04 — round 1

- ActionSheet: the `divider` description ends 'decorative and hidden from assistive technology' after describing the danger divider as role="separator" inside the menu; unclear whether 'hidden' covers both. Chose: the menu separator is an exposed role=separator, and the divider above the Cancel row is aria-hidden.
- ActionSheet: with dismissible=false and no heading the header has no content (the handle is not rendered). Chose: skip the header element entirely, so no empty padded strip remains.
- ActionSheet: Menu's overridable bindings include none of the locked names the Behavior section lists as forwarded (surface, itemHover, itemColor, itemDangerColor, minTarget, focusRing, focusRingWidth), and those are not in ActionSheet's override type either. Chose: forward only the overridable shared names (shadow, radius, itemPaddingBlock, itemPaddingInline, itemGap, fontFamily, fontSize, lineHeight, layer, enter) plus divider → separator.
- ActionSheet: 'Controlled state' says uncontrolled from its initial state when omitted, but the `open` prop says controlled only and required. Chose controlled only; the stories' wrapper plays the consumer.
- ActionSheet: the schema has no enum props (tone lives inside the actions shape), so 'one story per enum value' produces none. Added DismissibleFalse and Closed instead.
- ActionSheet: no `transition` binding exists for the row hover paint. Chose motion.duration.fast + motion.easing.standard (the rule's default), removed under reduced motion.
- ActionSheet: the wide Menu fires onOpenChange(false,'action') before onAction; the doc says a choice's close never fires onClose 'whatever order', but a later focus-out after unmount is possible. Chose: a per-opening flag set by the 'action' reason or onAction suppresses every mapped close until open next becomes true.
- ActionSheet: the drag 'holds the released position' after a dismissing release is BottomSheet behavior not stated here. Chose to hold the dragged offset until the consumer closes, so the exit slides on from there; if the consumer ignores onClose('drag') the sheet stays offset until the next pointer down.
- ActionSheet: 'the rest of the page inert' relies on native showModal(); in the setAttribute('open') fallback (no showModal) nothing is made inert.

## 2026-09-21 09:17 — round 1

- ActionSheet: the titleSize/fontFamily/lineHeight forwards say the value reaches Text 'through Text's CSS hook set to --ds-action-sheet-title-size in the sheet's stylesheet', but Text sets --ds-text-font-size itself from its own size class (.ds-text--size-sm) on the same element, so a one-class rule loses. The doc also never says how the sheet selects the composed Text node (Text overrides any className passed through rest). Chose: pass data-part="heading" through Text's rest spread and target it with a two-class descendant selector so the sheet's hook out-specifies .ds-text--size-sm.
- ActionSheet: the ref contract ('the sheet presentation's <dialog>, null while closed; in the wide presentation the ref stays null') cannot be typed — Ref<HTMLDialogElement> has no null in its value type and useImperativeHandle must return one. Chose: return dialogRef.current cast to HTMLDialogElement, so the ref is null at runtime as specified but typed non-null.
- ActionSheet: dismissible is documented as gating the sheet presentation only, but nothing says what a consumer passing dismissible: false should expect above maxWidth — Menu always closes on outside click and has no scrim/cancel/drag. Chose: dismissible is simply inert in the wide presentation (no warning, no behavior change), which is what the prose implies but never states.
- ActionSheet: the Controlled state section says open is 'controlled when given, uncontrolled from its initial state when omitted (local state)', contradicting the schema's required: true and the prop description's 'Controlled only — there is no uncontrolled mode'. Chose: required and controlled only, with no local open state, per the prop description.
- ActionSheet: overlay.dismiss lists escape alongside scrim/close-button/swipe, but the dismissible description and the non-dismissible-still-reports-escape scenario single Escape out as still firing when dismissible is false. Chose: requestClose suppresses scrim/cancel/drag when dismissible is false and always reports escape.
- ActionSheet: the drag lives on the header, and with dismissible true but no heading the header renders containing only the handle (an otherwise empty padded strip). The doc describes the empty-header case only for dismissible: false with no heading. Chose: render the header whenever dismissible || heading, so the handle always has its drag surface.
- ActionSheet: velocity is defined as the slope between the last two move samples, which is undefined when a flick produces only one pointermove before release. Chose: velocity 0 in that case, so such a gesture can dismiss only by passing dismissDistance.
- ActionSheet: dragSlop resolves from the --space-1 custom property, which is absent in jsdom (no theme stylesheet), making the slop 0 so any downward move claims the drag in tests. The doc does not say what an unresolvable token means. Chose: treat it as 0, matching BottomSheet's documented fallback.
- ActionSheet: the doc requires example stories to 'start from blank args, never from Default's or meta args', but Storybook always merges meta.args into every story and the Default story is required to carry the photo-actions args (which must therefore live in meta). Chose: restate every meta arg explicitly in each example story (including heading: undefined for unnamed-sheet), which is equivalent but not literally blank.
- ActionSheet: the component declares no enum props (open and dismissible are booleans), so the 'one story per enum value' rule yields nothing. Chose: DismissibleFalse and Closed as the notable-state stories alongside Default, the three examples, and Keyboard.
- ActionSheet: with no trigger in any story (the doc forbids one, there being no copy key), document.activeElement at open time is the body, so the wide presentation anchors Menu to document.body and there is no visible anchoring to review in Storybook. The doc sanctions body as the fallback but the result is that no story exercises real anchor placement.
- ActionSheet: the a11y guidance says a danger row is distinguished 'by color and position and an icon when given', which reads as the icon carrying meaning, yet the anatomy makes itemIcon a composed Icon with no label field on the action shape. Chose: render itemIcon as decorative (aria-hidden), leaning on the label text and the danger grouping to carry the distinction without color alone.

## 2026-09-21 09:32 — round 2

- ActionSheet: the keyboard block specifies the sheet's model (Escape, Arrow, Home/End, Enter/Space over the roving items) and marks only the Tab rule as the wide presentation, but the generated gate loads the Keyboard story at a desktop viewport where the doc's own maxWidth rule (572px vs a 1280px gate viewport) renders the wide Menu. The gate therefore asserts Menu's keyboard model, not the sheet's, and passes only because the two coincide. The doc gives no way for a story to pin a presentation, so I left it: the sheet's keyboard model is effectively unasserted by the browser gate and only covered by the jsdom tests.
- ActionSheet: the doc says the wide Menu anchors to 'the element that was focused when open became true' but never says what that means when the presentation switches while open — by then the sheet has moved focus into itself and the originally focused element may be gone. Chose: capture the opener exactly once, the moment open turns true, and never re-capture for that opening; a presentation switch reuses that same element.
- ActionSheet: Menu raises `focus-out` both for focus moving out of the popup and for the window losing focus (its window-blur listener), but the doc's mapping table treats `focus-out` as a single reason mapped to `scrim`. Taken literally, the browser window losing focus reports a scrim tap the user never made. Chose: keep the documented mapping as written and report this, since suppressing it would contradict the doc.
- ActionSheet: nothing says what the breakpoint comparison should do when `--layout-max-width-prose` is unresolvable (no theme stylesheet yet, or jsdom). Chose: treat it as not-wide but keep retrying while the document is still loading, so a late stylesheet corrects the presentation instead of pinning it; once loading completes an unresolvable token stays not-wide, which is what keeps the jsdom tests on the sheet.
- ActionSheet: the doc sanctions the `open`-attribute fallback only for 'where showModal() does not exist (jsdom)', and says nothing about showModal() existing but refusing (an already-top-layer or detached dialog). Chose: catch the failure and fall back to the attribute, since letting it throw out of a layout effect unmounts the whole component and renders nothing at all.
- ActionSheet: the titleSize/fontFamily/lineHeight forwards say the value reaches Text 'through Text's CSS hook set to --ds-action-sheet-title-size in the sheet's stylesheet', but Text sets --ds-text-font-size itself from its size class (.ds-text--size-sm) on the same element, so a one-class rule loses; the doc also never says how the sheet selects the composed Text node (Text overrides any className passed through rest). Chose: pass data-part="heading" through Text's rest spread and target it with a two-class descendant selector.
- ActionSheet: the ref contract ('the sheet presentation's <dialog>, null while closed; in the wide presentation the ref stays null') cannot be typed — Ref<HTMLDialogElement> has no null in its value type and useImperativeHandle must return one. Chose: return dialogRef.current cast to HTMLDialogElement, so the ref is null at runtime as specified but typed non-null.
- ActionSheet: dismissible is documented as gating the sheet presentation only, but nothing says what a consumer passing dismissible: false should expect above maxWidth — Menu always closes on outside click and has no scrim/cancel/drag. Chose: dismissible is simply inert in the wide presentation, with no warning.
- ActionSheet: the Controlled state section says open is 'controlled when given, uncontrolled from its initial state when omitted (local state)', contradicting the schema's required: true and the prop description's 'Controlled only — there is no uncontrolled mode'. Chose: required and controlled only, with no local open state.
- ActionSheet: the drag lives on the header, and with dismissible true but no heading the header renders containing only the handle (an otherwise empty padded strip); the doc describes the empty-header case only for dismissible: false with no heading. Chose: render the header whenever dismissible || heading, so the handle always has its drag surface.
- ActionSheet: velocity is defined as the slope between the last two move samples, which is undefined when a flick produces only one pointermove before release. Chose: velocity 0 in that case, so such a gesture dismisses only by passing dismissDistance.
- ActionSheet: dragSlop resolves from the --space-1 custom property, absent in jsdom, making the slop 0 so any downward move claims the drag in tests. Chose: treat an unresolvable token as 0, matching BottomSheet.
- ActionSheet: the doc requires example stories to 'start from blank args, never from Default's or meta args', but Storybook always merges meta.args into every story and Default must carry the photo-actions args (which therefore live in meta). Chose: restate every meta arg explicitly in each example story, including heading: undefined for unnamed-sheet.
- ActionSheet: the component declares no enum props (open and dismissible are booleans), so 'one story per enum value' yields nothing. Chose: DismissibleFalse and Closed as the notable-state stories alongside Default, the three examples, and Keyboard.
- ActionSheet: with no trigger in any story (the doc forbids one, there being no copy key), document.activeElement at open time is the body, so the wide presentation anchors Menu to document.body and no story exercises real anchor placement. It also means Menu's outside/focus-out guards, which test anchor containment, never fire in the gate — body contains everything.
- ActionSheet: the a11y guidance says a danger row is distinguished 'by color and position and an icon when given', which reads as the icon carrying meaning, yet the action shape has no icon label field. Chose: render itemIcon as decorative (aria-hidden), leaning on the label text and the danger grouping.

## 2026-09-23 04:33 — round 1

- ActionSheet: itemIconSize is 'delivered the way titleSize describes', but Icon has no data-part to select (its root carries data-part="glyph") and titleSize's recipe names data-part="heading"; I selected the composed Icon as `.ds-action-sheet .ds-action-sheet__icon [data-ds='Icon']`. The doc should name the selector.
- ActionSheet: Icon ignores `size`/`overrides.size` while `inline`, and the web platform note (`<Icon>` in the row) doesn't say whether the row glyph is inline; for itemIconSize to take effect I render it non-inline at Icon's default size md. The doc should say the row Icon is not inline.
- ActionSheet: itemIconSize isn't in the list of bindings forwarded to the wide Menu, nor among those 'with no effect there'; I didn't forward it (Menu has no matching binding here). The doc should list it in one of the two.
- ActionSheet: Menu reports both 'focus moved elsewhere' and 'the window lost focus' as the same `focus-out` reason, so ActionSheet can't tell them apart from the reason alone; I report `scrim` only when `document.hasFocus()` is true at the callback. The doc (or Menu's reason set) should give the window-blur case its own signal.
- ActionSheet: the Keyboard story's trigger is described only as 'an overflow Button labelled "More actions"' (iconOnly per When to use); the glyph and variant aren't specified. I chose leadingIcon `ellipsis` and variant `secondary`.
- ActionSheet: the Keyboard story renders open with its trigger, so the opener captured at mount is document.body rather than the trigger, and at a phone width showModal() makes the trigger inert. The doc doesn't say whether the story should start closed and open from the trigger so the opener is real; I kept open: true as the gate rule requires.

## 2026-09-23 15:38 — round 1

- ActionSheet: composition says focusScope receives exactly trapped/restoreFocus/autoFocus none/active, but the web implementation also passes FocusScope's `returnFocusTo` (the opener captured once when `open` turned true), because otherwise FocusScope restores to whatever was focused when it activated, which can be wrong after a switch from the wide presentation to the sheet while open. The doc should either allow `returnFocusTo` or say the restore target is FocusScope's own capture.
- ActionSheet: the `focusScope` anatomy part can't sit on FocusScope itself on web, because FocusScope stamps its own data-part="scope" after its rest spread. I put it on a wrapper div inside the scope, as BottomSheet does. The platform note should say this, as it does for cancelButton and itemIcon.
- ActionSheet: the doc doesn't say what happens when `open` goes false before the enter frame has run. I finish the exit at once (close() and unmount) instead of waiting for a transitionend that never comes, matching the overlay first-frame Escape fix; the exit binding's description should cover this case.
- ActionSheet: the scrim dismisses only for a click whose target is the scrim element itself (a click that bubbled up from a child doesn't count), matching BottomSheet. The doc says only 'the scrim was clicked'.
- ActionSheet: the doc says the drag claims 'a pointer' but not which: non-primary pointers and secondary mouse buttons never claim the gesture (as BottomSheet). The doc could say so.
- ActionSheet: the `with-an-unavailable-action` example has only one enabled action, so the Keyboard story (not that example) is the one with at least three focusable children; no story exercises roving over a disabled row in the browser, only the unit test does.

## 2026-09-23 15:39 — round 2

- ActionSheet: maxWidth is locked and 'read from the theme token, not per instance', yet the hooks gate requires `--ds-action-sheet-max-width`. The breakpoint is decided before the sheet's <dialog> exists, so the hook on the root can never affect it. I declare it on the root and have the logic read a `--ds-action-sheet-max-width` set on :root first, falling back to `--layout-max-width-prose`. The doc should say whether a page-wide hook on :root is the sanctioned way to re-theme the breakpoint.
- ActionSheet: titleColor is 'realized by the composed heading Text's tone="muted"; Text owns its color', but the gate requires a hook. I declare `--ds-action-sheet-title-color` (default color.foreground.muted) and point Text's `--ds-text-color` at it from the sheet's two-class heading rule, the same delivery titleSize uses. The doc should say that titleColor is delivered like titleSize (through Text's CSS hook, never through `overrides`) rather than 'not overridable'.
- ActionSheet: minTarget, focusRing and focusRingWidth have no `part` in the schema. I apply them to the item rows (min-block-size, the :focus-visible outline); the Cancel row's Button keeps its own target and ring.
- ActionSheet: FocusScope receives `returnFocusTo` (the opener captured when `open` turned true) on top of the listed composition props; the doc should allow it or name the restore target (carried over from round 1).
- ActionSheet: the focusScope part is on a wrapper inside FocusScope, because FocusScope stamps its own data-part="scope" (carried over from round 1).
- ActionSheet: closing before the enter frame finishes the exit immediately rather than waiting for a transitionend that never fires; the exit binding's description should cover this (carried over from round 1).

## 2026-09-23 19:15 — round 1

- ActionSheet: the doc says dismissDistance and dismissVelocity are 'marked `literal-ok`' but does not say where that marker goes for a TypeScript constant (the lint gate only checks CSS); I put a `// literal-ok: …` trailing comment on each constant.
- ActionSheet: the wide-presentation breakpoint is read from `--layout-max-width-prose`, but the doc does not say whether the locked maxWidth hook (`--ds-action-sheet-max-width`) is also consulted before the sheet's <dialog> exists; I read that hook from :root first and fall back to the token.
