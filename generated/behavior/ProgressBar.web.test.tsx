// Generated by tools/behavior_tests.ts from the `behavior` block of the ProgressBar doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { ProgressBar } from '../../packages/react/src/ProgressBar';
import type { ProgressBarProps } from '../../packages/react/src/ProgressBar';
import meta from '../../packages/react/src/ProgressBar.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<ProgressBarProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<ProgressBar {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="ProgressBar"]') ?? screen.queryByRole('progressbar') ?? utils.container.firstElementChild) as HTMLElement,
    container: () => (screen.queryByRole('progressbar') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<ProgressBarProps>) => utils.rerender(<ProgressBar {...props} {...next} />),
  };
  return s;
}

describe('ProgressBar', () => {
  test('the-bar-reports-its-value-and-range', async () => {
    const s = setup({"value": 42, "min": 0, "max": 100});
    expect(screen.getByRole('progressbar')).toBeInTheDocument();
    expect(s.container()).toHaveAttribute("aria-valuenow", "42");
    expect(s.container()).toHaveAttribute("aria-valuemin", "0");
    expect(s.container()).toHaveAttribute("aria-valuemax", "100");
  });
  test('the-bar-is-never-focusable', async () => {
    const s = setup({});
    act(() => (s.container()).focus());
    expect(s.container()).not.toHaveFocus();
  });
  test('a-hidden-label-is-still-the-accessible-name', async () => {
    const s = setup({"hideLabel": true});
    expect(screen.getByRole('progressbar', { name: s.props.label })).toBeInTheDocument();
  });
  test('the-label-names-the-task', async () => {
    const s = setup({"label": "Importing contacts"});
    expect(screen.getByText(new RegExp("Importing\\ contacts"))).toBeInTheDocument();
  });
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-tone-neutral', async () => {
    const s = setup({"tone": "neutral"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-tone-success', async () => {
    const s = setup({"tone": "success"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-tone-danger', async () => {
    const s = setup({"tone": "danger"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-announce-none', async () => {
    const s = setup({"announce": "none"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-announce-milestones', async () => {
    const s = setup({"announce": "milestones"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-announce-complete', async () => {
    const s = setup({"announce": "complete"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('progressbar', { name: s.props.label })).toBeInTheDocument();
  });
});
