# Gaps reported while generating Tooltip for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 02:27 — round 1

- Tooltip: the schema has no `events` section (unlike Menu/AlertDialog), so no CustomEvent is dispatched — all behavior is expressed through native aria-describedby/aria-labelledby, focus, and pointer events; treated the absence as intentional rather than an omission.
- Tooltip: 'delay: none / a shared warm toolbar state' is described only qualitatively (no numeric grace window given for how long a tooltip stays 'warm' after closing). Chose motion.duration.base as that grace window (same token the default-delay formula already reads) — a module-level `warmUntil` timestamp set on every close.
- Tooltip: because the Popover API sets `display: none` while closed, the popup (and its aria-describedby/aria-labelledby target) technically leaves the accessibility tree between shows, which sits in tension with the platform notes' 'the description is still in the accessibility tree' / 'never hover-only anywhere' language. Mitigated by always showing synchronously on focus (so AT users get it at the same moment they'd query it), matching how comparable production tooltips (e.g. Radix) handle this, but it's not literally 'always present'.
- Tooltip: fontFamily/fontSize/lineHeight are overridable bindings, but the composed <ds-text> renders its own self-contained font hooks rather than inheriting CSS custom properties (unlike ds-link, per the Breadcrumb precedent's `font: inherit` comment). Forwarded these three via ds-text's own `overrides` prop (driven by Tooltip's JS `overrides` property), which works — but the parallel CSS escape hatch (`ds-tooltip.foo { --ds-tooltip-font-size: ... }`) described in the overrides contract will NOT reach the rendered text for these three bindings specifically.
- Tooltip: 'shows ... immediately when the trigger receives keyboard focus' — implemented as 'any focus event shows immediately' since reliably distinguishing keyboard-origin focus from mouse-origin focus across arbitrary composed trigger types (ds-button, native <button>, ds-input, ds-link) isn't practical; this also matches how the pointer path already shows on hover, so the only behavioral difference is the delay, not the focus source.
- Tooltip: `placement: start/end` is implemented as literal left/right (LTR only, not logical/RTL-aware), matching the existing Menu component's own `bottom-start/bottom-end` positioning code in this package, which is also not RTL-aware.

## 2026-09-10 17:51 — round 1

- Tooltip: the schema's `open` prop ("controlled visibility, for stories and tests only ... the Keyboard story renders the tooltip open with it") was missing from the prior implementation; added `@property({ type: Boolean }) open?: boolean` (unreflected, matching platforms.lit.reflect which lists only placement/describes) that forces showPopup()/hidePopup() on change, and updated the Keyboard story to render `<ds-tooltip open>` instead of relying on native-button autofocus.
- Tooltip: Escape hiding the popup does not reset the `open` property back to false (only the internal `visible`/popover-open DOM state changes) — acceptable since the schema calls `open` a one-way initializer for stories/tests, not a fully two-way controlled prop like Popover's, but noting the asymmetry in case a future scenario expects `open` itself to flip.

## 2026-09-16 05:58 — round 1

- Tooltip: aria-describedby/aria-labelledby is set on the slotted trigger host (e.g. <ds-button>), but the accessible element is the <button> inside that host's shadow root, so the ID reference never reaches it. The Lit notes assume a light-DOM trigger. ds-button has no way to forward describedby/labelledby (e.g. ariaDescribedByElements on its inner button). Chose to set the attributes on the host as the notes say; the doc (or Button's schema) needs a forwarding contract.
- Tooltip: the `delay` prop description says the warm window is one motion.duration.loop, but the `warmWindow` constant says motion.duration.base. Used the constant (base), as the contracts section says it wins.
- Tooltip: `open` is described as controlled, but Tooltip has no event, so a controlled element can never report Escape/blur/leave. Yet the keyboard rule and the Keyboard story expect Escape to close a tooltip rendered with open. Chose: open=true shows, open=false hides, and user interaction can still hide it while the property stays true.
- Tooltip: the keyboard rule scopes Escape to `from: trigger`, but WCAG 1.4.13 (cited in the Accessibility section) needs a tooltip opened by hover alone to be dismissable while focus is elsewhere. Added a document-level Escape listener that runs only while the bubble is visible and the trigger does not have focus.
- Tooltip: the Parts section gives `text` (Text) no props or forwards, yet fontSize (font.size.sm), fontFamily and lineHeight are Tooltip bindings that only take effect through Text. Chose size="sm" element="span" on <ds-text> and forwarded the fontFamily/fontSize/lineHeight overrides to Text's `overrides`, as the old element did; the doc should list these.
- Tooltip: the examples name glyphs that tools/icon-paths.json lacks (bold, italic, a copy/link glyph). The stories use stand-ins (menu, list, external) with the right labels; add the glyphs or change the examples.
- Tooltip: the warm-toolbar example puts tooltips inside a Toolbar, but Toolbar's docs don't say whether its roving tabindex and overflow menu see through a ds-tooltip wrapper around each Button. Wrapped the buttons anyway; not verified in the browser.
- Tooltip: 'popup is the root, already found by data-ds' does not map to Lit, where data-ds is on the display:contents host, not the bubble. The bubble carries no data-part or data-ds; tests find it as the host's aria-hidden child.
- Tooltip: the web notes and guidance disagree on how touch is detected: the pointer:coarse media query vs 'never shown on touch'. Chose pointerType === 'touch' on pointerenter, which leaves focus-driven showing intact on hybrid devices.
- Tooltip: the delay-before-show and warm-window timings have no reduced-motion rule for web/Lit (only SwiftUI says 'none under reduced motion'). Kept the delays under reduced motion; only the fade transition is removed.

## 2026-09-17 09:55 — round 1

- Tooltip: the lit notes say system triggers (ds-button, ds-link, ds-input) forward aria-label/aria-description from their host to the inner control, but packages/lit/src/Button.ts does not (its inner button's aria-label comes only from accessibleName/iconOnly label), and aria-label on a role-less host is ignored by Chromium. Tooltip sets the attributes as specified; the link does not reach the accessibility tree until Button/Link/Input's docs require the forwarding. The scenario tests only assert the attribute on the host.
- Tooltip: 'custom element with a shadow root' has no stated detection rule; chose `trigger.shadowRoot !== null` (open roots only). A custom element with a closed shadow root, or one not yet upgraded at slotchange, falls back to aria-describedby, which won't resolve.
- Tooltip: Guidance and the web notes say the bubble is mounted only while shown, while the lit notes describe a popover='manual' bubble appended to the host. Mounting and unmounting would kill the `exit` fade and re-fire slotchange on every show, so lit keeps the bubble mounted, closed (display: none, aria-hidden) while hidden. The doc should say which one lit follows.
- Tooltip: controlled `open` only says Escape hides and it stays hidden until `open` changes. It does not say whether hover, focus or blur still change visibility while `open` is set. Chose: while `open` is defined, only Escape and changes to `open` affect visibility.
- Tooltip: 'stops that Escape in the capture phase' does not say whether to call preventDefault. A native <dialog> closes on Escape unless keydown is default-prevented, so the listener calls both stopPropagation and preventDefault.
- Tooltip: the lit notes say the host carries data-ds, while the web notes and Guidance put data-ds on the visually-hidden role=tooltip copy. Lit puts it on the host only.
- Tooltip: the offset has to be read in px for positioning, but a custom property's computed value may be in rem. The bubble carries `scroll-margin-top: var(--ds-tooltip-offset)` and logic reads it resolved from there. The doc names no mechanism.
- Tooltip: the warm-toolbar example's given names one tooltip, but showing the warm state needs a sibling tooltip. The story adds a 'List view' sibling (not in the given) inside the 'View' toolbar, and 'Grid view' keeps the default describes: true as given, so an icon-only button with the same name also gets it as aria-description.
- Tooltip: the Default story's trigger variant is not specified (only 'a Button labelled "Items"'); chose variant secondary.

## 2026-09-21 05:17 — round 1

- Tooltip: BLOCKING keyboard gate, and the web-side fix does not carry over. `from: trigger` emits `[aria-haspopup], [aria-expanded], [aria-controls]`, which matches 0 elements in the Lit Keyboard story (verified: locator.focus times out at 30s). The web gap list proposes extending that selector with `[aria-describedby]` — on Lit that also matches 0, because platforms.lit mandates that a custom-element trigger gets the text as `aria-description` rather than an idref (verified: 3 `[aria-description]`, 0 `[aria-describedby]`). The only fix that works on both platforms is `from: any` (Escape is a document-level listener). Paired with `target: popup` (the doc's third-time-reported fix), the rule passes against the unchanged component — verified in logs/tooltip-lit-kb.spec.ts.
- Tooltip: BLOCKING behavior gate, pre-existing and structural. generated/behavior/Tooltip.lit.test.ts asserts `el.shadowRoot.querySelector('[role="tooltip"]')` for both role scenarios, but platforms.lit requires that node to live in the LIGHT DOM (appended to the host) so the idref crosses the boundary; it can never be in the shadow root, and a second copy inside it would break 'exactly one role=tooltip'. The generator needs to search the host as well as the shadow root for lit. Its `trigger()` fallback also resolves to the shadow `<slot>` and times out on hover, because `children` cannot be expressed in lit story args — 2 of 9 fail; the other 7 pass.
- Tooltip: platforms.lit asserts 'system triggers (ds-button, ds-link, ds-input) forward both to their inner control', but none of them do — ds-button's inner <button> gets aria-label only from its own `accessibleName`/`iconOnly`, and no element forwards host `aria-description`. So on a ds-button trigger the text lands on the custom-element host (a generic node), not on the focusable control a screen reader lands on. Either those three components' schemas grow a forwarding rule, or Lit Tooltip needs a different mechanism. Left as the doc describes.
- Tooltip: story parity forced a choice on the placement stories. React's PlacementTop/Bottom/Start/End pass only `placement`, so the Lit ones now do too — which means all four render a bare button with no visible bubble, and the docs page shows four identical-looking samples. The previous Lit stories set `open: true` to make the placement visible. If the doc wants these to demonstrate placement, the example/story contract should say the enum stories for a hover-driven overlay are rendered open.
- Tooltip: example `warm-toolbar` names a sibling 'List view' Button with its own Tooltip, which is the whole point of the warm window, but it is not expressible as args. React renders only the primary trigger; the Lit story had a real second tooltip and I removed it for story parity. The doc should either drop the sibling from the example's `children` or give examples a way to name extra elements.
- Tooltip: the Overrides contract says every binding becomes a `--ds-tooltip-<binding>` hook, while Guidance says fontFamily/fontSize/lineHeight are forward-only with no hook. Followed Guidance (and React). Consequence the doc does not address: on Lit a consumer can no longer set tooltip typography from document CSS via `ds-tooltip { --ds-tooltip-font-size: ... }`, only through Text's `--ds-text-*` hooks, and those are inherited by every ds-text in the subtree.
- Tooltip: `pointerGrace` is described as pointer-only ('while the pointer crosses the offset gap'), but the doc also says the tooltip hides 'when focus leaves the trigger' without saying whether that is delayed. Used one shared hide timer, so blur also waits pointerGrace and the guard re-checks hover and focus — matching React.
- Tooltip: the doc says an Escape-dismissed uncontrolled tooltip 'stays hidden until the trigger has lost both hover and focus, even if it is re-hovered first'. Implemented by clearing the dismissal inside the pointerGrace timer once neither hover nor focus is active; the doc does not say whether the dismissal should also clear when the trigger is replaced or the element is disconnected (it does not, today).
- Tooltip: nothing says what `data-placement` (or any attribute) should carry the resolved, flipped side. React writes `data-placement={side}` on the bubble, so Lit now does too, mapping the physical side back to the logical `start`/`end` under RTL. If that is not part of the contract it should be removed from both, or named in parts.
- Tooltip: the doc calls a non-focusable child 'an error', but a custom-element trigger's focusability is not readable from the host (ds-button's host has tabIndex -1 with delegatesFocus). The dev warning now fires only for a plain element with tabIndex < 0, so the most common wrong usage — a hyphenated tag that is not focusable — goes unwarned.
- Tooltip: constants and `offset` are resolved by temporarily setting the token expression on the always-present visually-hidden description node and reading it back (transition-duration for durations, padding-left for the length). The doc says 'through getComputedStyle' but names no property, and on Lit an appended probe would re-fire slotchange on the host, so the existing hidden node is used instead.

## 2026-09-21 05:21 — round 2

- Tooltip: BLOCKING keyboard gate, round 2, no code fix exists — the fix is two fields on the keyboard rule in the doc. (1) `from: trigger` emits `[aria-haspopup], [aria-expanded], [aria-controls]` (tools/keyboard_tests.ts:185), which matches 0 elements on an APG tooltip trigger, so `locator.focus` times out at 30s. Use `from: any` — Escape is a document-level listener, and `any` is the only value in FROM_BLOCKS that emits no focus step. Do NOT extend the selector with `[aria-describedby]` as proposed for web: on Lit a custom-element trigger takes `aria-description` instead of an idref, so that selector matches 0 here too (measured: 0 aria-describedby, 3 aria-description in the Keyboard story). (2) `closes` needs `target: popup`. Verified: with both fields the rule passes against the unchanged component (logs/tooltip-lit-kb.spec.ts).
- Tooltip: the `closes`-on-root conflict is now proven unsatisfiable by any component code, not merely awkward. The generated beforeEach asserts `getByRole('tooltip').first()` is VISIBLE, and `closes` asserts the same locator is HIDDEN after Escape. Because a11y.role is `tooltip`, that locator resolves to the always-present visually-hidden copy the doc mandates (1x1 clip pattern → Playwright reads it visible, before and after), and the bubble is excluded from getByRole because the doc requires it aria-hidden. The only architecture the gate accepts is a single non-aria-hidden role=tooltip node that is display:none when closed — which the doc forbids in three places (two nodes; bubble aria-hidden; description always in the accessibility tree). Either the rule takes `target: popup`, or the doc drops the two-node design.
- Tooltip: adding any of `aria-haspopup` / `aria-expanded` / `aria-controls` to the trigger to satisfy the gate was rejected as a spec violation, not just an omission — it is absent from APG tooltip and platforms.lit, and it contradicts the Behavior section's promise that a tooltip-wrapped Button inside a Toolbar keeps its own semantics. If the doc actually wants an opener attribute on the trigger, it must say so in platforms.lit.attributes, and on Lit it should point at the bubble (which stays mounted there, so unlike web the idref would never dangle).
- Tooltip: `focus-unchanged` is vacuous on this component even once the gate runs. focusIndex is scoped to `root`, and the root (the sr-only span) has no focusable descendants, so before and after are both -1 whatever focus does. A meaningful check would compare document.activeElement identity, or scope focusables to the story container.
- Tooltip: BLOCKING behavior gate, pre-existing and structural. generated/behavior/Tooltip.lit.test.ts asserts `el.shadowRoot.querySelector('[role="tooltip"]')` for both role scenarios, but platforms.lit requires that node in the LIGHT DOM so the idref crosses the boundary — it can never be in the shadow root. Its `trigger()` fallback also resolves to the shadow `<slot>` and times out on hover, because `children` cannot be expressed in lit story args. 2 of 9 fail; the other 7 pass. The generator needs to search the host as well as the shadow root on lit.
- Tooltip: platforms.lit asserts 'system triggers (ds-button, ds-link, ds-input) forward both to their inner control', but none of them do — ds-button's inner <button> takes aria-label only from its own `accessibleName`/`iconOnly`, and nothing forwards a host `aria-description`. On a ds-button trigger the text lands on the custom-element host, not on the focusable control. Either those components' schemas grow a forwarding rule, or Lit Tooltip needs another mechanism.
- Tooltip: the Overrides contract says every style binding becomes a `--ds-tooltip-<binding>` hook, while Guidance says fontFamily/fontSize/lineHeight are forward-only with no hook. Followed Guidance (and React). Consequence the doc does not address: on Lit a consumer can no longer set tooltip typography from document CSS, only through Text's `--ds-text-*` hooks, which every ds-text in the subtree inherits.
- Tooltip: story parity forced the placement stories to render nothing visible. React's PlacementTop/Bottom/Start/End pass only `placement`, so the Lit ones now do too, and all four show a bare button. If those stories are meant to demonstrate placement, the doc should say the enum stories for a hover-driven overlay render open.
- Tooltip: example `warm-toolbar` names a sibling 'List view' Button with its own Tooltip — the whole point of the warm window — but it is not expressible as args, so neither platform renders it. The doc should drop it from `children` or give examples a way to name extra elements.
- Tooltip: `pointerGrace` is described as pointer-only, but the doc also says the tooltip hides 'when focus leaves the trigger' without saying whether that is delayed. Used one shared hide timer, so blur also waits pointerGrace with the guard re-checking hover and focus — matching React.
- Tooltip: nothing says which attribute carries the resolved, flipped side. React writes `data-placement` on the bubble, so Lit now does too (mapping the physical side back to logical start/end under RTL). Name it in parts or drop it from both.
- Tooltip: the doc calls a non-focusable child 'an error', but a custom-element trigger's focusability is unreadable from the host (ds-button's host has tabIndex -1 with delegatesFocus). The dev warning now fires only for a plain element with tabIndex < 0, so the most common mistake — a hyphenated tag that is not focusable — goes unwarned.
- Tooltip: constants and `offset` are resolved by temporarily setting the token expression on the always-present visually-hidden node and reading it back (transition-duration for durations, padding-left for the length). The doc names no property, and on Lit an appended probe would re-fire slotchange on the host, so the existing hidden node is reused.

## 2026-09-21 05:25 — round 3

- Tooltip: BLOCKING keyboard gate, THIRD occurrence on lit (and the fourth across platforms — web reported the same thing in its rounds 1-3). No component code can pass it; the fix is two fields on the keyboard rule in tooltip.md. Patch: add `from: any` and `target: popup`. Round 3 changed no code and the generated spec is byte-identical to round 2, so this loop will not converge until the doc changes. Verified against the unchanged component in logs/tooltip-lit-kb.spec.ts (3/3 passing).
- Tooltip: why `from: any` and not the selector fix proposed for web. `from: trigger` emits `[aria-haspopup], [aria-expanded], [aria-controls]` (tools/keyboard_tests.ts:185) and matches 0 elements page-wide in the Keyboard story — Playwright pierces open shadow roots, so ds-button's inner <button> is included in that count. The web gap list proposed extending the selector with `[aria-describedby]`; on lit that also matches 0, because platforms.lit requires a custom-element trigger to take the text as `aria-description` rather than an idref (measured: 0 aria-describedby, 3 aria-description). `any` is the only FROM_BLOCKS value that emits no focus step, and it is correct here because Escape is a document-level listener, not a trigger-scoped one.
- Tooltip: why `target: popup` is required as well, proven rather than argued. The generated beforeEach asserts `getByRole('tooltip').first()` is VISIBLE and `closes` asserts the SAME locator is HIDDEN after Escape. Because a11y.role is `tooltip`, that locator can only resolve to the always-present visually-hidden copy (1x1 clip pattern → Playwright reads it visible before and after); the bubble is excluded from getByRole because the doc requires it aria-hidden. The only shape that satisfies both lines is one non-aria-hidden role=tooltip node that is display:none when closed, which contradicts the doc in three places (two nodes; bubble aria-hidden; description always in the accessibility tree). Consequence: clearing the `from` failure alone does not make the gate pass — it just moves the failure from a 30s timeout to an assertion failure at line 76.
- Tooltip: adding an opener attribute to the trigger was considered and rejected again this round. `aria-haspopup`/`aria-expanded` change the trigger's announced semantics, against the Behavior section's promise that a tooltip-wrapped Button keeps its own role inside a Toolbar. `aria-controls` is benign to screen readers and would not dangle on lit (the bubble stays mounted, unlike web) — but it is absent from platforms.lit.attributes and from APG tooltip, and it still would not make the gate pass. If the doc does want an opener attribute, it must say so explicitly and it should be `aria-controls` pointing at the bubble.
- Tooltip: `focus-unchanged` is vacuous on this component even once the gate runs. focusIndex is scoped to `root`, and the root (the sr-only span) has no focusable descendants, so before and after are both -1 whatever focus does. A meaningful check would compare document.activeElement identity, or scope focusables to the story container.
- Tooltip: BLOCKING behavior gate, pre-existing and structural. generated/behavior/Tooltip.lit.test.ts asserts `el.shadowRoot.querySelector('[role="tooltip"]')` for both role scenarios, but platforms.lit requires that node in the LIGHT DOM so the idref crosses the boundary — it can never be in the shadow root. Its `trigger()` fallback also resolves to the shadow `<slot>` and times out on hover, because `children` cannot be expressed in lit story args. 2 of 9 fail; the other 7 pass. The generator needs to search the host as well as the shadow root on lit.
- Tooltip: platforms.lit asserts 'system triggers (ds-button, ds-link, ds-input) forward both to their inner control', but none of them do — ds-button's inner <button> takes aria-label only from its own `accessibleName`/`iconOnly`, and nothing forwards a host `aria-description`. On a ds-button trigger the text lands on the custom-element host, not on the focusable control a screen reader lands on. Either those components' schemas grow a forwarding rule, or Lit Tooltip needs another mechanism.
- Tooltip: the Overrides contract says every style binding becomes a `--ds-tooltip-<binding>` hook, while Guidance says fontFamily/fontSize/lineHeight are forward-only with no hook. Followed Guidance (and React). Consequence the doc does not address: on Lit a consumer can no longer set tooltip typography from document CSS, only through Text's `--ds-text-*` hooks, which every ds-text in the subtree inherits.
- Tooltip: story parity forced the placement stories to render nothing visible. React's PlacementTop/Bottom/Start/End pass only `placement`, so the Lit ones now do too, and all four show a bare button. If those stories are meant to demonstrate placement, the doc should say the enum stories for a hover-driven overlay render open.
- Tooltip: example `warm-toolbar` names a sibling 'List view' Button with its own Tooltip — the whole point of the warm window — but it is not expressible as args, so neither platform renders it. The doc should drop it from `children` or give examples a way to name extra elements.
- Tooltip: `pointerGrace` is described as pointer-only, but the doc also says the tooltip hides 'when focus leaves the trigger' without saying whether that is delayed. Used one shared hide timer, so blur also waits pointerGrace with the guard re-checking hover and focus — matching React.
- Tooltip: nothing says which attribute carries the resolved, flipped side. React writes `data-placement` on the bubble, so Lit now does too (mapping the physical side back to logical start/end under RTL). Name it in parts or drop it from both.
- Tooltip: the doc calls a non-focusable child 'an error', but a custom-element trigger's focusability is unreadable from the host (ds-button's host has tabIndex -1 with delegatesFocus). The dev warning now fires only for a plain element with tabIndex < 0, so the most common mistake — a hyphenated tag that is not focusable — goes unwarned.
- Tooltip: constants and `offset` are resolved by temporarily setting the token expression on the always-present visually-hidden node and reading it back (transition-duration for durations, padding-left for the length). The doc names no property, and on Lit an appended probe would re-fire slotchange on the host, so the existing hidden node is reused.

## 2026-09-22 01:28 — round 1

- Tooltip: BLOCKING behavior gate, fourth round on lit, and no component code can pass it — but the generator fix is now measured, not argued. generated/behavior/Tooltip.lit.test.ts needs three changes, all verified green against the unchanged component in logs/tooltip-lit-behavior-probe.test.ts.txt (2/2). (A) Both role scenarios assert `el.shadowRoot!.querySelector('[role="tooltip"]')`, but platforms.lit requires that node in the LIGHT DOM so the idref crosses the boundary; use `el.shadowRoot!.querySelector(sel) ?? el.querySelector(sel)`. (B) `setup` skips a `children` value that is not a string, so a slot-only component is rendered with nothing slotted; create the story's slotted element. (C) `trigger()` falls back to `root.firstElementChild`, which on lit is the shadow `<slot>` — a <slot> has no box, so Playwright's actionability check never calls it visible and `userEvent.hover` times out at 15s. A `kind: slot` part must resolve to `slot.assignedElements({flatten:true})[0]`.
- Tooltip: platforms.lit asserts 'system triggers (ds-button, ds-link, ds-input) forward both to their inner control', but none of them do — ds-button's inner <button> takes aria-label only from its own `accessibleName`/`iconOnly`, and nothing forwards a host `aria-description`. On a ds-button trigger (every example in the doc) the text lands on the custom-element host, a generic node, not on the focusable control a screen reader lands on. Unchanged from round 3: either Button/Link/Input's schemas grow a forwarding rule, or lit Tooltip needs a different mechanism. Left as the doc describes.
- Tooltip: the Overrides contract says every style binding becomes a `--ds-tooltip-<binding>` hook, while Guidance says fontFamily/fontSize/lineHeight are forward-only with no hook. Followed Guidance (and React). Consequence the doc still does not address: on lit a consumer cannot set tooltip typography from document CSS, only through Text's `--ds-text-*` hooks, which every ds-text in the subtree inherits.
- Tooltip: `data-placement` is now named in platforms.web.attributes, but the resolved side is only known once the bubble is measured, and on lit the bubble is mounted (popover-closed) from the start. So `data-placement` is absent until the first show and then retains the last resolved side while hidden. The doc does not say what the attribute should read before the first show; chose absent.
- Tooltip: Guidance mandates `open: true` for the four `placement` stories but says nothing about `DelayDefault`/`DelayNone`, which have no visible difference without it (a hover delay cannot be seen in a static story). Left them closed, so they render identically to `Default` — same choice as web.
- Tooltip: the doc does not say whether the internal show/hide timers should keep running while `open` is controlled. Lit returns early from `requestShow`/`scheduleMaybeHide` when `open` is defined (the hover and focus flags still update, so state stays consistent); React chose the opposite — to keep arming them and mask the result. Both satisfy 'only Escape and changes to `open` affect visibility', but the platforms differ, which the doc should settle.
- Tooltip: the behavior scenario `the-visible-tooltip-carries-the-tooltip-role` says 'the bubble is a role=tooltip element', while Guidance says the bubble is `aria-hidden` and 'only a visible copy' and puts role=tooltip on the visually-hidden span. Followed Guidance — the bubble carries no role. The scenario's prose should be reworded to name the visually-hidden copy, or it reads as a contradiction.
- Tooltip: `focus-unchanged` on the Escape rule is vacuous as generated. focusIndex is scoped to the rule's `root`, which resolves to the sr-only role=tooltip span; it has no focusable descendants, so before and after are both -1 whatever focus does. The gate now passes, but that expectation proves nothing. A meaningful check would compare document.activeElement identity, or scope the focusables to the story container.
- Tooltip: example `warm-toolbar`'s Guidance note now acknowledges the sibling is not expressible as args, but the result is that neither platform's story demonstrates the warm window at all — the story is a single tooltip with `delay: none`, indistinguishable from `DelayNone`. If the warm window is worth a constant and a prop value, examples need a way to name extra elements.
- Tooltip: the doc calls a non-focusable child 'an error', and platforms.lit scopes the dev warning to 'a plain element whose tabindex is below 0'. So the most common wrong usage — a hyphenated tag that is not focusable — is explicitly left unwarned by the contract itself. Implemented as written; flagging that the stated rule cannot catch the case the prose calls an error.

## 2026-09-23 14:23 — round 1

- Tooltip: the web notes name the light-DOM classes (`ds-tooltip` on the bubble, `ds-tooltip__description` on the visually-hidden span) but the lit notes don't; chose the web names so the naming codemod finds the same classes on both platforms.
- Tooltip: the lit notes put `data-ds` on the host (display: contents) while the web notes put it on the visually-hidden role=tooltip span; the `renders` scenario is platform-neutral, so what `renders` should locate differs by platform. Kept `data-ds` on the host per the lit notes, and the test finds the role=tooltip span inside it.
- Tooltip: the locked `surface` and `text` bindings must keep a :host hook, but the bubble lives in the light DOM (a child of the host, and in the top layer when it is a popover). Chose `--ds-tooltip-surface` / `--ds-tooltip-text` on :host, inherited by the bubble through its DOM parent; the doc does not say whether hooks on a display:contents host are expected to reach a top-layer light-DOM child.
- Tooltip: the `text` binding names part `text` but its description says the foreground is re-scoped on the bubble container (not set on the Text part). Implemented as `--color-foreground: var(--ds-tooltip-text)` on the popup, so the binding is not applied to the `data-part="text"` element itself.
- Tooltip: the bubble's rules can't live in the element's shadow stylesheet because the bubble sits in the light DOM; the doc doesn't say how to style it. Chose a single <style> injected once per document or shadow root, built from token/hook vars only.
- Tooltip: `renders` scenarios for placement/delay cannot distinguish values (the bubble is hidden without `open`); tests only assert the element and its role=tooltip copy render.
