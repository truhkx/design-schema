# Gaps reported while generating Slider for web

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 10:55 — round 1

- Slider: schema props have no `required`/`invalid` fields and `copy` only defines minimumLabel/maximumLabel, unlike Input/Checkbox/RadioGroup's required+invalid+copy.required/copy.invalid pattern. Implemented `validate()` as just returning the `error` prop, with no required-field enforcement.
- Slider: marks doc says 'Values snap to marks when step is omitted,' but `step` always carries a default of 1 (props can't structurally distinguish 'omitted' from 'explicitly 1'). Chose to always snap by `step` for drag/click/arrow keys (so arrows never get stuck between sparse marks), and only used marks for PageUp/PageDown 'next mark' navigation as separately and explicitly specified.
- Slider: platforms.web `attributes` list for the thumb doesn't include aria-describedby, but a11y.requires lists error-identification and label-association, which need the description/error text linked to something. Added aria-describedby on each thumb referencing description/error ids (same mechanism Input/Checkbox/RadioGroup use) as the minimal way to satisfy that requirement; did not add aria-invalid since no token/requirement calls for a visual invalid state.
- Slider: composition says `valueText: Text`, but the same anatomy part also renders as a hover/drag bubble with an inverse-surface pill background, which the Text component can't express (no background/padding, no 'inverse' tone). Rendered the beside-label value as a Text component (matching composition) but the bubble as plain markup (like Tooltip's popup), styled directly from the bubbleSurface/bubbleText/valueSize tokens — the bubble's font-size is hardcoded to font-size-sm rather than going through the `valueSize` override contract.
- Slider: no copy template is given for how a range's beside-label value text should combine the two thumb values. Joined them as `${formatValue(low)} – ${formatValue(high)}` (en dash) — a judgment call, not a copy string from the schema.
- Slider: APG multithumb convention (referenced via `apg: slider-multithumb`) suggests each range thumb's aria-valuemin/aria-valuemax should reflect the live constraint from its sibling thumb (min thumb's max = current high value, and vice versa) rather than the global min/max on both. Implemented it that way even though the schema's flat attribute list doesn't spell this out.
- Slider: the prompt's generic Keyboard-story guidance asks for 'at least three focusable children'; a range Slider only ever has two thumbs (a single slider has one). Used the two-thumb `range` story for Keyboard since three isn't achievable for this component.
- Slider: pointer capture and the drag/click handlers are attached to a wrapper (`.ds-slider__body`, needed so `trackPaddingBlock` can size the 44px hit region without visually inflating the thin rail) rather than literally the `track` element as the platform note phrases it; the rail (`data-part="track"`) is used only for the horizontal position math, which is equivalent since the wrapper has no inline padding.

## 2026-09-10 18:47 — round 1

- Slider: the existing file was missing the `required`, `invalid`, and `snapToMarks` props entirely (schema declares them); added them with copy.required/copy.invalid validation precedence (error → required → invalid) matching Input/NumberInput's pattern, `required` treated as 'current value equals the effective default' per the schema description ('must have a value other than the default to submit').
- Slider: platforms.web.attributes for the thumb role=slider list does not include aria-invalid or aria-required (unlike Input/NumberInput's own attribute lists), so I did not add them to the thumb divs; invalid state is instead surfaced via a `ds-slider--invalid` root class (no dedicated visual token exists for it in styles) plus the existing aria-describedby link to the error text. Flagging in case aria-invalid was intended to be implicit.
- Slider: copy.rangeText was previously not used (the beside-label range text was hardcoded as `${low} – ${high}`); now built from COPY.rangeText.replace, matching the 'use copy.* verbatim' rule.
- Slider: the file never called `form.validateField` on interaction, so `required`/`invalid` (and `error`) never re-validated live in `validate: 'change'`/`'blur'` Forms, only at submit. Added `validateField` calls on commit (change mode) and on interaction-end/onChangeEnd (blur or change mode), mirroring Input/NumberInput, since the spec's blur-note ('validate: blur means on-change for toggles') implies drag/key-release should count as the commit point for a non-typed control like Slider.
- Slider: `snapToMarks` only affects pointer drag/click (per spec: 'keys still move by step, PageUp/Down by mark'); implemented as a separate snap function passed into `updateThumb`, defaulting to step-snapping for the keyboard path.

## 2026-09-16 09:12 — round 1

- Slider: `marks` says 'Values snap to marks when `step` is omitted', but `step` defaults to 1 and `snapToMarks` exists to turn mark snapping on. Chose: only `snapToMarks` switches drag/click snapping to marks; omitting `step` changes nothing.
- Slider: the conventions say a field's label is a native `<label for>`, but the thumbs are `div role=slider`, and `<label for>` can't name a div. Chose: label is a Text `<span id>` referenced by each thumb's `aria-labelledby` (range thumbs use hidden minimumLabel/maximumLabel spans).
- Slider: the Form value is described as a number or `[low, high]`, but FormFieldValue is `string | string[] | boolean | undefined`. Chose: getValue returns `String(value)` or `[String(low), String(high)]`; hidden inputs carry the same strings (two inputs with the same name for a range).
- Slider: `showValue: hover` is named 'hover' but described as 'only while dragging or focused'. Chose the description: the bubble shows while the thumb is pressed or focused, not on plain pointer hover.
- Slider: `trackPaddingBlock` is bound to part `track`, but padding the track would thicken the coloured rail. Chose: the padding goes on an unparted body wrapper around track and thumbs, which is also the pointer hit area.
- Slider: `errorText` is overridable, but the error message is a composed Text with tone=danger whose color binding is locked, and the child may not be restyled. Chose: accepted and ignored (no-op).
- Slider: `valueSize`/`fontFamily`/`fontSize`/`labelWeight`/`helperSize` have no part; they style composed Texts. Chose: forward to the label/value/description/error Text `overrides` (fontWeight/fontSize/fontFamily); `valueSize` and `fontFamily` are also root hooks read by the bubble and mark labels.
- Slider: `mark`, `markSize`, `markLabelSize`, `markLabelColor` have no part, and 'label' on a mark has no anatomy name. Chose: dots live in `data-part=tickMarks`; mark labels are an unparted aria-hidden row under the track, spaced by space.2 (a token the spec doesn't name).
- Slider: the spacing between the label and the value text in the header, and the bubble's padding and offset, have no binding. Chose space.2 gap, space.1/space.2 bubble padding, bubble flush above the 44px thumb hit box.
- Slider: onChangeEnd 'once when the interaction ends' does not say whether an interaction that changed nothing (a click on the thumb, End at max) counts. Chose: onChange and onChangeEnd fire only when the value actually changed.
- Slider: `validate: blur` for a pointer control is only defined in the RN notes. Chose the same on web: in blur mode the field validates when an interaction ends (pointer up / key up), not on thumb blur.
- Slider: the halo 'thumbSize larger on each side (space.2)' is contradictory (thumbSize is space.5). Chose: halo = knob size + 2 × space.2, fill colour at thumbActiveScale opacity.
- Slider: PageUp/PageDown 'to the next mark' does not say what happens past the last mark. Chose: move to max/min.
- Slider: the behavior scenarios' `then: event: onChange` gives no expected payload. Tests also assert the value (55, 45, 60, 0, 100, onChangeEnd 51), derived from the keyboard table.
- Slider: nothing says which element takes the `id`/registration id for a range. Chose: the low thumb (the first tab stop).

## 2026-09-17 12:16 — round 1

- Slider: the `name` description says the form value types have no number, but the web FormContext's FormFieldValue includes number and [number, number] (its JSDoc even says 'Slider a number'). I followed the Slider doc and register String(value) or [String(low), String(high)] (string[]); FormContext's JSDoc is now wrong about Slider.
- Slider: the tickMarks part must hold both the dots (on the track centre line) and the labels (below the track area, markLabelGap after trackPaddingBlock), and the slider grows only when a mark has a label. No layout is given. I made tickMarks an in-flow sibling after the track area: dots are absolutely positioned back up by trackPaddingBlock + trackHeight/2, and labels sit in a one-cell grid positioned by percentage. Pointer presses on the label line do not move a thumb.
- Slider: the web platform notes don't say whether the errorMessage wrapper is role=alert. The Lit notes say it is. I added no role on web.
- Slider: the description/error Text `element` for web is span per the composition; the previous file used p. I followed the composition.
- Slider: `copy.pageUpAction`/`pageDownAction`/`homeAction`/`endAction` are RN-only accessibility action labels. The rule says to use every copy string, but web has no place for them. They sit unused in the COPY constant.
- Slider: the `showValue: hover` bubble shows on 'pressed or focused'. The doc doesn't say whether focus means any focus or :focus-visible only. I show it on any focus of the thumb (including focus moved there by a pointer press).
- Slider: errorText is overridable (not locked), yet its description says an override has no effect. It stays in SliderOverridableBinding and is ignored at runtime; the type offers a knob that does nothing.
- Slider: the Form's focus-on-error and registration id is the low thumb for a range. The id is also where the generated id lands, so the high thumb carries no id. The doc doesn't say whether a user-supplied `id` prop belongs on the root or the thumb; I put it on the (low) thumb.
- Slider: 'a controlled component shows the new state only once the prop changes', but during a drag or key-repeat the next step must diff against the value just emitted. I track the last emitted value internally until the next render resets it from the prop, so a parent that rejects a change still gets onChangeEnd with the rejected value.
- Slider: the behavior scenarios don't cover a range (Tab between thumbs, no crossing, live aria-valuemin/max), snapToMarks, pointer drag or the form value. Only the 17 listed scenarios are tested.

## 2026-09-21 14:54 — round 1

- Slider: the styles block says an override of `errorText` "has no effect" (implying it is accepted and ignored) while the Overrides section lists it as Locked ("not in the type"). Chose Locked — removed it from `SliderOverridableBinding`, a breaking type change from the previous generation.
- Slider: a mismatch between `range` and the value's shape (`range: true` with `defaultValue: 30`, or a number `value`) is unspecified. Chose a silent fallback to `[min, max]` with no dev warning; only `max <= min` warns.
- Slider: Home/End are specified as "sets the minimum/maximum" with no range carve-out, but thumbs cannot cross. Chose the no-cross clamp — End on the low thumb stops at the high thumb's value, not `max` — so the clamp wins over the literal key action.
- Slider: nearest-thumb selection on a track press is specified only for coincident thumbs. Chose the low thumb on an exact midpoint tie between two distinct thumbs.
- Slider: `aria-valuetext` is specified as coming from `formatValue`, but `formatValue` defaults to the plain number, duplicating `aria-valuenow`. Chose to always emit `aria-valuetext` (`String(value)` when unformatted) rather than omitting it in the default case.
- Slider: the doc says the form value types "have no number" so a slider registers decimal strings, but `FormContext.ts`'s `FormFieldValue` JSDoc still says "NumberInput and Slider a number … a range Slider or DatePicker a pair". Implemented strings per the Slider doc; the shared comment in FormContext.ts is stale and left unedited (other component's file).
- Slider: the Slider doc predates the Form's `submitFailed`/`validateMode` contract and specifies only "`validate: blur` validates when an interaction ends". Chose Input's current idiom — change-mode and post-failed-submit validate on every commit, blur-mode at interaction end.
- Slider: the bubble is described only under `showValue: hover`; whether `always` should also show a drag bubble is not stated. Chose to render the bubble element only for `hover` (kept mounted at opacity 0 so `transition` can fade it), so `always` has no bubble in the DOM at all.
- Slider: `data-part="tickMarks"` covers both the dot layer and the label row, but the doc calls the dots the part and the label row "unparted". One element cannot be both, so the part is a wrapper holding the dot layer plus the unparted label row.
- Slider: `markSize` and `trackHeight` are both `space.1`, so mark dots are exactly as tall as the rail and read as barely visible bumps on it rather than ticks. Implemented as specified; the doc may want a larger `markSize` token.

## 2026-09-21 15:03 — round 2

- Slider: the keyboard gate cannot express the Tab rule. tools/keyboard_tests.ts locates the gate's `root` by the doc's a11y.role (`page.getByRole('slider').first()`), assuming the role sits on the component root, but Slider's role is on the `thumb` part — so `root` is the low thumb, which has 0 focusable descendants, and `focus-next` (focusIndex inside root) is -1 forever. Verified Tab does move low→high and Shift+Tab back. Fix in the doc: give the Tab rule `expect: manual` with `native: true` (Tab between two tab stops is browser behavior); or fix in the tool: fall back to `[data-ds="<Name>"]` when the resolved role belongs to an anatomy part rather than the root. Left the code spec-true — adding role=slider to the root would nest two sliders and break `the-thumb-is-the-slider`.
- Slider: the doc lists `aria-disabled` only among the thumb's attributes, but `disabledOpacity` dims the whole root including the label row and messages. Any dimmed text that no disabled control references via aria-labelledby then fails axe color-contrast (the value text measured 2.98:1 in light mode) even though WCAG 1.4.3 exempts inactive components. Chose to also set `aria-disabled="true"` on the root while disabled so the exemption is machine-visible. The doc should either say this or say the value text keeps full opacity; the same trap applies to every component whose disabledOpacity dims text.
- Slider: the styles block says an override of `errorText` "has no effect" (implying accepted-and-ignored) while the Overrides section lists it as Locked ("not in the type"). Chose Locked — removed from `SliderOverridableBinding`, a breaking type change from the previous generation.
- Slider: a mismatch between `range` and the value's shape (`range: true` with `defaultValue: 30`, or a number `value`) is unspecified. Chose a silent fallback to `[min, max]` with no dev warning; only `max <= min` warns.
- Slider: Home/End are specified as "sets the minimum/maximum" with no range carve-out, but thumbs cannot cross. Chose the no-cross clamp — End on the low thumb stops at the high thumb's value, not `max`.
- Slider: nearest-thumb selection on a track press is specified only for coincident thumbs. Chose the low thumb on an exact midpoint tie between two distinct thumbs.
- Slider: `aria-valuetext` is specified as coming from `formatValue`, which defaults to the plain number and so duplicates `aria-valuenow`. Chose to always emit it rather than omitting it in the default case.
- Slider: the doc says the form value types "have no number" so a slider registers decimal strings, but `FormContext.ts`'s `FormFieldValue` JSDoc still says "NumberInput and Slider a number … a range Slider or DatePicker a pair". Implemented strings per the Slider doc; that shared comment is stale and left unedited.
- Slider: the doc predates the Form's `submitFailed`/`validateMode` contract and specifies only "`validate: blur` validates when an interaction ends". Chose Input's current idiom — change-mode and post-failed-submit validate on every commit, blur-mode at interaction end.
- Slider: the bubble is described only under `showValue: hover`; whether `always` should also show a drag bubble is unstated. Chose to render the bubble element only for `hover` (kept mounted at opacity 0 so `transition` can fade it).
- Slider: `data-part="tickMarks"` covers both the dot layer and the label row, but the doc calls the dots the part and the label row "unparted". One element cannot be both, so the part is a wrapper holding the dot layer plus the unparted label row.
- Slider: `markSize` and `trackHeight` are both `space.1`, so mark dots are exactly as tall as the rail and read as bumps on it rather than ticks. Implemented as specified; the doc may want a larger `markSize`.

## 2026-09-21 15:09 — round 3

- Slider: UNFIXABLE FROM THE COMPONENT — the keyboard gate's Tab rule. tools/keyboard_tests.ts:87 builds the root as page.getByRole('<a11y.role>').first(), assuming the role is on the component root (keyboard_tests.ts:15), but Slider's role is on the `thumb` part, so root IS the low thumb: 0 focusable descendants, focusIndex -1 forever. Measured: same helpers against [data-ds="Slider"] give before=0 afterTab=1 and the rule passes. Injecting role="slider" on the root (the only shape that redirects the locator) produces three wcag2a axe violations — nested-interactive, aria-required-attr, aria-input-field-name — and makes getByRole('slider') match 3 elements, breaking the behavior scenario `the-thumb-is-the-slider`. Fix in the doc: give the Tab rule `expect: manual` with `native: true` (Tab between two tab stops is browser behavior). Or fix in the tool: fall back to [data-ds="<Name>"] when the resolved role belongs to an anatomy part rather than the root. No third option exists; code left spec-true and unchanged this round.
- Slider: the harness's FOCUSABLE selector (`input:not([disabled])`) counts the hidden <input> elements the web platform notes require for native form submission, so the root's focusable list is [thumb, thumb, input[hidden], input[hidden]]. Harmless for `from: first`/`focus-next`, but a `from: last` or `focus-last` rule would target a hidden input once the root locator is fixed. The selector should exclude `input[type="hidden"]`.
- Slider: the doc lists `aria-disabled` only among the thumb's attributes, but `disabledOpacity` dims the whole root including the label row and messages. Any dimmed text that no disabled control references via aria-labelledby then fails axe color-contrast (the value text measured 2.98:1 in light) even though WCAG 1.4.3 exempts inactive components. Chose to also set aria-disabled="true" on the root while disabled so the exemption is machine-visible; the doc should say this, or say the value text keeps full opacity. Same trap for every component whose disabledOpacity dims text.
- Slider: the styles block says an override of `errorText` "has no effect" (implying accepted-and-ignored) while the Overrides section lists it as Locked ("not in the type"). Chose Locked — removed from `SliderOverridableBinding`, a breaking type change from the previous generation.
- Slider: a mismatch between `range` and the value's shape (`range: true` with `defaultValue: 30`, or a number `value`) is unspecified. Chose a silent fallback to `[min, max]` with no dev warning; only `max <= min` warns.
- Slider: Home/End are specified as "sets the minimum/maximum" with no range carve-out, but thumbs cannot cross. Chose the no-cross clamp — End on the low thumb stops at the high thumb's value, not `max`.
- Slider: nearest-thumb selection on a track press is specified only for coincident thumbs. Chose the low thumb on an exact midpoint tie between two distinct thumbs.
- Slider: `aria-valuetext` is specified as coming from `formatValue`, which defaults to the plain number and so duplicates `aria-valuenow`. Chose to always emit it rather than omitting it in the default case.
- Slider: the doc says the form value types "have no number" so a slider registers decimal strings, but FormContext.ts's `FormFieldValue` JSDoc still says "NumberInput and Slider a number … a range Slider or DatePicker a pair". Implemented strings per the Slider doc; that shared comment is stale and left unedited.
- Slider: the doc predates the Form's `submitFailed`/`validateMode` contract and specifies only "`validate: blur` validates when an interaction ends". Chose Input's current idiom — change-mode and post-failed-submit validate on every commit, blur-mode at interaction end.
- Slider: the bubble is described only under `showValue: hover`; whether `always` should also show a drag bubble is unstated. Chose to render the bubble element only for `hover` (kept mounted at opacity 0 so `transition` can fade it).
- Slider: `data-part="tickMarks"` covers both the dot layer and the label row, but the doc calls the dots the part and the label row "unparted". One element cannot be both, so the part is a wrapper holding the dot layer plus the unparted label row.
- Slider: `markSize` and `trackHeight` are both `space.1`, so mark dots are exactly as tall as the rail and read as bumps on it rather than ticks. Implemented as specified; the doc may want a larger `markSize`.

## 2026-09-22 01:21 — round 1

- Slider: the declared onChange/onChangeEnd payload is a single positional `value`, but the web behavior-test emitter (tools/behavior_tests.ts:365) unconditionally writes `toHaveBeenCalledWith(<value>, expect.anything())`, assuming a React `(value, event)` handler. All six value-bearing scenarios in generated/behavior/Slider.web.test.tsx therefore fail with the correct value and no second argument. This is systemic — 19 components fail the same way. I followed the schema's payload contract; fixing the emitter is out of scope for a single component job and would change every platform's generated output.
- Slider: `error` is specified as a web `setCustomValidity` custom validity that 'blocks submission like the listed flags', but the web rendering has no constraint-validatable control — the value reaches a native form through `<input type="hidden">`, which is barred from constraint validation, so setCustomValidity there can never block a submit. I report `error` first in the FormContext `validate` callback (matching the stated precedence) and left native submission unguarded. The doc should name what carries custom validity on web or drop the claim.
- Slider: the doc never says whether the bubble's inner Text also carries `data-part="valueText"`. `bubbleText` says it 'is a Text (size sm, tone default) with valueSize forwarded', and the composition table gives valueText its own part. I gave data-part="valueText" only to the beside-label Text — so `showValue: hover` renders no valueText part at all — and left the bubble's Text unparted inside `data-part="bubble"`, because a range would otherwise emit two valueText parts.
- Slider: three bindings (`markLabelSize`, `markLabelColor`, `markLabelGap`) declare `part: tickMarks`, but the `marks` prose puts the labels in 'an unparted aria-hidden row below the track area' — outside the tickMarks part they name. I implemented the prose (dots are the part, labels are an unparted sibling row); the style-binding `part` field for those three is then describing a part that does not contain what they style.
- Slider: `snapToMarks` says drag and click 'snap to the marks instead of step' but never says which mark. I chose the nearest mark by absolute distance, with ties resolved to the lower value (stable over the sorted mark list).
- Slider: `required` compares against 'the default' = `defaultValue` when set, else `min` / `[min, max]`. The doc only considers uncontrolled mode. With a controlled `value` and no `defaultValue`, the baseline is still `min`, so a controlled slider mounted at any other value satisfies `required` at mount. The doc should say whether a controlled slider's baseline is its first `value` instead.
- Slider: `marks` entries outside [min, max] are unspecified. The doc rules out clamping for mark *labels* ('the labels at min and max hang past the track') but says nothing about a mark whose `value` is out of range. I clamp dot position to 0-100% (so an out-of-range mark stacks on the end) and let `commit` clamp a snapped out-of-range mark value back into the bounds.
- Slider: under `showValue: hover`, the bubble shows on 'pressed or focused' and `disabled` thumbs 'stay focusable'. The doc does not say whether focusing a disabled thumb shows its bubble. I show it (the root is dimmed by disabledOpacity anyway), since suppressing it would hide the value the doc says is 'always available as text'.

## 2026-09-23 16:04 — round 1

- Slider: web notes say pointer capture is on 'the track and thumbs' and 'the track click moves the nearest thumb', but only the RN notes say a press inside a thumb's hit area drags from its current value instead of jumping. I applied the RN rule on web too, keeping a grab offset so the drag doesn't jump; the web notes should say so.
- Slider: onChange says 'within one interaction the comparison is against the last value emitted', but no platform says what counts as the end of a key interaction when focus leaves before key-up. I end it on blur.
- Slider: during held-key autorepeat on a controlled slider whose owner never updates `value`, the doc doesn't say whether each keydown steps from the displayed (prop) value or from the last value emitted. I step from the displayed value, so the owner gets the same target once rather than a run of increasing values.
- Slider: bubbleText says web 're-scopes --color-foreground on the bubble', but the bubble also has its own locked `--ds-slider-bubble-text` hook. I kept both (the hook feeds the re-scoped variable); the doc could say which name a theme should set.
- Slider: `required` compares the value against a default that the doc says is clamped to [min, max], but it doesn't say whether a range default is also reordered when defaultValue[0] > defaultValue[1]. normalize() clamps the high value up to the low one.

## 2026-09-23 16:05 — round 2

- Slider: the spec says valueColor, markLabelColor, descriptionText and errorText are 'realised by the composed Text's tone; no hook of its own', but check_hooks requires a hook for every locked binding, and the digest says locked bindings keep their hook. I followed the gate and the digest: each hook is declared on the root with its token as the default, and it sets the tone's colour variable on a wrapper element with no data-part. The schema descriptions for these four bindings (and the same wording on Input/NumberInput errorText) should be changed to say the hook exists and how it is applied.
- Slider: the web notes say the value and description Texts carry their own data-part with 'no wrapper taking the part name', but they don't say whether an unparted wrapper is allowed. Re-scoping each colour separately needs one around the value and the description, because the value sits in the label row beside the label Text, which uses the same default tone. I added `span.ds-slider__value` and `div.ds-slider__description`, both without a data-part.
- Slider: bubbleText re-scopes --color-foreground on the bubble, and valueColor now re-scopes --color-foreground on the value wrapper. The bubble is not inside that wrapper, so valueColor never reaches the bubble's Text. The doc doesn't say whether valueColor should also apply there; I chose no, because the bubble is an inverse surface.

## 2026-09-23 19:22 — round 1

- Slider: none material — the spec is complete for web, no contradictions found. I did not re-derive the existing file from scratch; I checked it against the spec's contracts and ran its tests (17/17).
