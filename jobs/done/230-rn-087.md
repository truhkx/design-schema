React Native to the current release (0.87 at the time of writing; use `npm view react-native version`) — Workstream B step 4.

1. packages/rn: `react-native` latest, `react-native-svg` latest, `react-native-web` latest, `@react-native/babel-preset` matching, `react-test-renderer` matching the bundled React (0.87 bundles React 19.2), Jest 30 with the RN preset, `@testing-library/react-native` latest. Peer: `react-native: >=0.87`, `react: >=19`.
2. RN 0.87 makes the strict TypeScript API default: fix type errors it surfaces without changing behavior (imports from `react-native` that moved).
3. apps/ (job 130's Expo gallery): bump to the Expo SDK that targets this RN.
4. prompts/conventions/rn.md: note the strict TS API and `role` prop availability.
Gate: `pnpm --filter @design-schema/rn typecheck`, `test`, `build-storybook`, `pnpm storybook:device` boots. Type-only fixes are the only permitted edits under packages/rn/src; do not modify generated/.
