// Generated by tools/behavior_tests.ts from the `behavior` block of the Link doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Link } from '../../packages/react/src/Link';
import type { LinkProps } from '../../packages/react/src/Link';
import meta from '../../packages/react/src/Link.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

const INTERACTIVE = "button, a[href], input:not([type=\"hidden\"]), select, textarea, [role=\"button\"], [role=\"checkbox\"], [role=\"switch\"], [role=\"radio\"], [role=\"textbox\"], [role=\"searchbox\"], [role=\"spinbutton\"], [role=\"combobox\"], [role=\"slider\"], [role=\"link\"], [role=\"tab\"], [role=\"menuitem\"], [role=\"menuitemcheckbox\"], [role=\"menuitemradio\"], [role=\"option\"], [role=\"treeitem\"], [role=\"scrollbar\"]";
const OWN_ROLE = "dialog, [role=\"dialog\"], [role=\"alertdialog\"], [role=\"combobox\"], [role=\"grid\"], [role=\"listbox\"], [role=\"menu\"], [role=\"menubar\"], [role=\"radiogroup\"], [role=\"tablist\"], [role=\"tree\"], [role=\"treegrid\"]";
function activatable(el: Element | null, root: Element | null): HTMLElement {
  if (el === null || el === root || el.matches(INTERACTIVE) || el.matches(OWN_ROLE)) return el as HTMLElement;
  return (el.querySelector(INTERACTIVE) ?? el) as HTMLElement;
}
function focusedPart(el: Element | null, root: Element | null): Element | null {
  return el === null || document.activeElement === el ? el : activatable(el, root);
}

function setup(given: Partial<LinkProps> = {}) {
  const events = {
    onPress: vi.fn(),
  };
  const props = { ...meta.args, ...given, onClick: events.onPress };
  const utils = render(<Link {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Link"]') ?? screen.queryByRole('link') ?? utils.container.firstElementChild) as HTMLElement,
    anchor: () => (screen.queryByRole('link') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<LinkProps>) => utils.rerender(<Link {...props} {...next} />),
  };
  return s;
}

describe('Link', () => {
  test('click-fires-on-press', async () => {
    const s = setup({});
    await s.user.click(activatable(s.anchor(), s.root()));
    expect(s.events.onPress).toHaveBeenCalled();
  });
  test('external-link-announces-that-it-leaves', async () => {
    const s = setup({"external": true, "label": "View the billing history"});
    expect(screen.getByText(new RegExp("\\(opens\\ in\\ new\\ tab\\)"))).toBeInTheDocument();
    expect(screen.getByRole('link', { name: "View the billing history (opens in new tab)" })).toBeInTheDocument();
  });
  test('external-link-opens-a-new-tab', async () => {
    const s = setup({"external": true});
    expect(s.anchor()).toHaveAttribute("target", "_blank");
    expect(s.anchor()).toHaveAttribute("rel", "noopener noreferrer");
  });
  test('download-asks-the-browser-to-save', async () => {
    const s = setup({"download": true});
    expect(s.anchor()).toHaveAttribute("download", "");
  });
  test('current-marks-the-page', async () => {
    const s = setup({"current": true});
    expect(s.anchor()).toHaveAttribute("aria-current", "page");
  });
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-tone-default', async () => {
    const s = setup({"tone": "default"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-tone-inherit', async () => {
    const s = setup({"tone": "inherit"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('link', { name: s.props.label })).toBeInTheDocument();
  });
  test('control-is-focusable', async () => {
    const s = setup({});
    act(() => (s.anchor()).focus());
    expect(s.anchor()).toHaveFocus();
  });
});
