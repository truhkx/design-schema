// Generated by tools/behavior_tests.ts from the `behavior` block of the BottomSheet doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { BottomSheet } from '../../packages/react/src/BottomSheet';
import type { BottomSheetProps } from '../../packages/react/src/BottomSheet';
import meta from '../../packages/react/src/BottomSheet.stories';

function setup(given: Partial<BottomSheetProps> = {}) {
  const events = {
    onClose: vi.fn(),
    onDragDismiss: vi.fn(),
  };
  const props = { ...meta.args, ...given, onClose: events.onClose, onDragDismiss: events.onDragDismiss };
  const utils = render(<BottomSheet {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="BottomSheet"]') ?? screen.queryByRole('dialog') ?? utils.container.firstElementChild) as HTMLElement,
    scrim: () => (screen.queryByRole('dialog') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<BottomSheetProps>) => utils.rerender(<BottomSheet {...props} {...next} />),
  };
  return s;
}

describe('BottomSheet', () => {
  test('renders', async () => {
    const s = setup({"open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-height-content', async () => {
    const s = setup({"height": "content", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-height-half', async () => {
    const s = setup({"height": "half", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-height-full', async () => {
    const s = setup({"height": "full", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({"open": true});
    expect(screen.getByRole('dialog')).toHaveAccessibleName();
  });
});
