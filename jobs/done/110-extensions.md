Implement component extensions as specified in site/src/content/docs/process/extending-components.md (read it first; it is the contract).

1. Schema: schema/extension.schema.json and schema/extension.ts (Zod) for the `extension` frontmatter block: extends, name, and optional props / events / styles / copy / keyboard / behavior / modules, reusing the $defs from component.schema.json ($ref across files, or duplicate the defs with a comment). `modules` entries: { path, signature, wire, platforms? }.
2. tools/parse.py: read site/src/content/docs/extensions/*.md; validate; merge into the named component with the rules in the doc (add-only; no collisions with upstream or other extensions; no locked bindings; no contrast pairs; no a11y.requires); mark merged items with `source`; include the extension prose in the prompt under a "## Extensions" heading with the module contracts spelled out as import + call instructions; include extension docs in the prompt hash (the hash is of the prompt text, so this is automatic once they are in the prompt — confirm). Report errors as DocError naming the extension file.
3. Module stubs: for each module, write generated/modules/<platform>/<path>.d.ts declaring the export with the given signature; the `modules` gate (new, in tools/checks.py, always on, cheap) checks packages/<platform>/src/<path> exists, exports the name, and typechecks against the stub (a tiny tsc invocation or a project reference). Also fail if any file under packages/*/src/custom/ changed during the run (compare mtimes/hashes before and after the model call in generate.py).
4. generate.py: exclude packages/*/src/custom/ from the model's write scope (allowedTools patterns cannot express deny; instead check after the run and fail the round with a clear message if custom/ changed, restoring from git).
5. Templates: add an "Extensions" rule to prompts/templates/{web,lit,rn}.md: implement extension props/events like any other; import modules from `./custom/<path>` and call them exactly where `wire` says; never write into custom/.
6. Ship one real example: site/src/content/docs/extensions/Button.analytics.md exactly as in the process doc, and packages/react/src/custom/analytics.ts, packages/lit/src/custom/analytics.ts, packages/rn/src/custom/analytics.ts exporting `trackPress` that console.debug()s in __DEV__/import.meta.env.DEV and is a no-op otherwise. Do not regenerate Button; just make parse, the gate and the prompt correct.
7. Tests: tests/test_extensions.py covering merge, each collision/forbidden case, module stub generation and the gate.
Run `pytest -q` and `node tools/py.mjs tools/parse.py` (0 errors). Do not modify generated code under packages/*/src except the three custom/ files.
