# Gaps reported while generating TreeGrid for rn

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 14:17 — round 1

- TreeGrid's schema has no `selected`/controlled-selection prop (unlike DataGrid), so row/cell selection is uncontrolled-only — observable via onSelectionChange but not settable from outside.
- TreeGrid's schema has no `defaultSort` (unlike DataGrid), so `sort` was treated as fully caller-controlled: the grid re-sorts `data` from whatever `sort` is passed, but never maintains its own sort state; a sortable header click only fires onSortChange.
- TreeGrid's copy block only defines expand/collapse/level/childCount/loading/expandAll/collapseAll — there is no copy for the status bar (row count, selection count, editing message), the select-column labels (select all / select row), or the empty-state message, all of which DataGrid's own schema defines. Authored parallel English strings matching DataGrid's phrasing since none were given; these are called out in a source comment above the local COPY object.
- expandAll/collapseAll copy strings have no anatomy part or trigger described anywhere in the schema (no toolbar button, no keyboard binding beyond the web-only `*` for 'siblings of the focused row', which isn't the same scope as 'all'). Exposed them as custom accessibilityActions ('expandAll'/'collapseAll') on the root view as the best available accessible substitute, since native has no `*` key.
- `column.resizable` was not implemented (no drag handle) because TreeGrid's schema defines no `onColumnResize` event (DataGrid's does) to report the result through; columns render at their fixed `width`/`minWidth`, and a `__DEV__` warning fires if any column sets `resizable`.
- `expandButtonSize` override cannot resize the composed `Button`'s own internal minimum touch target (Button has no size-override prop, and children may not be restyled per the package rules); the override only changes the layout width reserved around the button, used for guide-line/text alignment.
- Guide lines are drawn as one segment per open ancestor level per row (mirroring the web implementation's `::before`-per-child-row technique), not as a single line spanning a parent's whole open subtree — FlatList rows are independent list items with nothing to anchor a spanning line to.
- Per the platform notes ('there are no arrow keys'), ArrowUp/Down/Left/Right/Home/End cell navigation, the `*` expand-siblings shortcut, and Ctrl+A select-all are not implemented on native; touch (expand button, cell tap, select-all checkbox) and accessibility actions/state stand in instead. F2/Escape editor shortcuts only work when a hardware keyboard is attached (react-native-web or an external keyboard), same as DataGrid.
- `date`-editor columns fall back to the same inline TextInput as `text`/`number` (no package DatePicker to compose), matching DataGrid's own documented gap.
- Select-all selects every currently loaded row across the whole tree (recursively, regardless of expansion/collapse), and `selectChildren` cascades over a row's already-loaded descendants only (a `children: "lazy"` subtree contributes nothing to the cascade until loaded) — the schema doesn't specify either behavior explicitly.

## 2026-09-10 19:58 — round 1

- indent (space.5) and expandButtonSize (size.target.min) are different-sized tokens, so 'guide lines aligned to the ancestor's expand button centre' can't be pixel-exact; implemented one guide-line slot sized to `indent` per ancestor level, followed by a separately-sized expand-button column — an approximation.
- The spec asks the row header cell itself to be `accessible` with a label/state/actions AND contain a separately real, focusable expand Button (and, when editable, a nested edit-start Pressable). Nesting focusable children inside an accessible parent risks VoiceOver double-exposure that RN has no clean fix for; implemented literally per spec rather than redesigning around it.
- Native accessibilityLabel format '{rowName}, level {n}, {count} items' is only specified for rows with children; leaf rows use '{rowName}, level {n}' with no count segment — a guess, since the doc doesn't give the leaf-row format.
- childCount is included in the row-header label only when `children` is a loaded array (count known); a still-`"lazy"` row's label omits the count segment since it's unknown before load — a guess.
- `defaultExpanded: ["*"]` and the root `expandAll` accessibility action expand every row with *loaded* (array) children only; `"lazy"` rows stay collapsed rather than firing `onExpand` for a potentially unbounded set at once — undocumented, a guess.
- The lazy-row loading placeholder renders as a single indented `Text` reading `copy.loading`, not one placeholder per column — the doc doesn't specify the placeholder row's column layout.
- Status-bar `rowCount`/`selectedRows` totals count currently visible (flattened, non-placeholder) rows rather than every loaded row in `data`, mirroring DataGrid's 'count what's in the list' convention — the 'as DataGrid' note doesn't resolve which total a collapsible tree should use.
- Dropped DataGrid's scroll-chrome effects (`headerShadow` on scroll, `pinnedShadow`) since those bindings aren't in TreeGrid's own overridable/locked token list or `styles` block; used static borders instead. Also omitted `rowCount`/`onEndReached` server-paging entirely since TreeGrid's prop list has no such prop — `data` is always the full loaded set.
- Trusted the caller to place the `isRowHeader` column first after any selection column, per the spec's requirement; the component does not detect or correct a violation.
- Keyboard rules involving Arrow keys, Home/End, `*`, Shift+Arrow and Ctrl+A have no hardware-keyboard equivalent on touch and are only reachable via the documented substitutes (expand Button, row-header accessibilityActions, root expandAll/collapseAll, tap-to-edit/select); these are exercised only through the `Keyboard` story for manual/axe checks, not as unit-testable behavior.

## 2026-09-16 12:17 — round 1

- TreeGrid: platforms.rn.notes gives the row-header label as "{rowName}, level {n}, {count} items" (lowercase 'level') but copy.level is "Level {level}"; used the copy verbatim, joined with ', '.
- TreeGrid: the rn row-header label always includes a child count, but a leaf has none and a "lazy" row's count is unknown; chose to leave the count out for leaves and unloaded lazy rows rather than say "0 items".
- TreeGrid: the keyboard table (ArrowUp/Down/Left/Right, Home/End, Ctrl+Home/End, *, F2, Space, Shift+Space, Control+a) can't be implemented on a React Native View, which gets no hardware key events. Only Enter (via Pressable) and Enter/Escape in the editor TextInput work. The root expandAll/collapseAll actions stand in for `*` but expand every loaded row, not just the focused row's siblings. The spec doesn't say what the native stand-in for `*` and the selection keys should be.
- TreeGrid: copy.selectedRows and copy.rowCount say 'how many rows the treegrid has' without saying whether that means visible rows or every loaded row; chose every loaded row at every level, so the selected count can't exceed the total when selected rows sit in a collapsed subtree.
- TreeGrid: onCellChange's `previous` is typed string | number | boolean, but a cell can hold no value; chose '' for a missing value. The number editor commits a number when the draft parses, otherwise the raw string; the spec doesn't say how an unparseable draft should be handled.
- TreeGrid: the spec doesn't say what pressing a row header should do when the row has children and its column is editable (Enter toggles expansion); chose press = toggle and long-press = edit.
- TreeGrid: the spec doesn't say whether a select-all without selectChildren covers only the top-level rows or every loaded level; chose every loaded row at every level, as it does with selectChildren.
- TreeGrid: the schema lists only TreeGrid's own bindings; grid lines, cell padding, row height, the fixed height, the resize handle width and step, and the header weight are 'as DataGrid' but not overridable here, so they use DataGrid's default tokens with no overrides.
- TreeGrid: the rn element is FlatList with role=grid (as DataGrid) rather than a treegrid role, and RN has no row-level level/setsize/posinset, so posinset/setsize aren't conveyed on native at all; only level and child count are, through the label.
- TreeGrid: the expand Button sits inside the focusable row-header Pressable, so on iOS VoiceOver reaches expansion only through the row header's expand/collapse actions, not the button itself; the spec asks for both without saying which one screen readers should reach.
- TreeGrid: whether the uncontrolled sort orders data inside the component isn't settled: the behavior text says 'the caller does the sorting' but DataGrid's rn implementation sorts when uncontrolled; followed DataGrid and sorts siblings within each level only when `sort` is not passed.

## 2026-09-17 15:02 — round 1

- TreeGrid: the rn notes require the expand Button to be hidden from accessibility (accessibilityElementsHidden / importantForAccessibility no-hide-descendants), but the package's Button exposes no `focusable`/tabIndex escape hatch, so on react-native-web that is aria-hidden wrapped around a focusable button — an axe `aria-hidden-focus` violation, where web's version passes because it also sets tabindex=-1. Implemented as written; the generated test has to query it with `includeHiddenElements: true`. The doc should either give Button a non-focusable option or drop the hiding on rn.
- TreeGrid: `defaultExpanded` says `"*"` never opens a `"lazy"` row (it would fire onExpand without a user act), while `onExpand` says `*` fires one onExpand per newly opened lazy row. Read as two different acts: `["*"]` in expanded/defaultExpanded opens only rows with a non-empty array of children, and the root expandAll action (the native stand-in for `*`) opens every loaded row including lazy ones, firing onExpand for each. Please confirm.
- TreeGrid: `A lazy id listed explicitly stays collapsed until the user opens it` does not say whether that id stays in the array onExpandChange reports. Chose to keep it (it is the caller's value) and track "opened by the user" in local state, so the row shows shut until pressed and the reported arrays are never rewritten behind the caller's back.
- TreeGrid: the notes say DataGrid's own bindings `apply at DataGrid's default tokens` but TreeGrid's doc never lists them, so the defaults are hard-coded from DataGrid's rn file (column width space.20 × 2, grid line color.border at border.width.thin, header font.weight.semibold / font.size.sm, resize handle space.1, resizeStep space.4, header and pinned shadow.raised, status bar padding and gap space.2, caption font.size.md / semibold). Nothing keeps the two in step: change DataGrid's defaults and TreeGrid drifts silently. Either restate them in TreeGrid's styles block or declare the sharing.
- TreeGrid: the copy block has no equivalent of DataGrid's `cellLabel` ({column}: {value}) or `editHint` (Double tap to edit), which the rn cells need for an accessible name and the tap-to-edit hint — the doc otherwise insists every platform package is self-contained in its copy. Reused DataGrid's two strings verbatim; they should be added to TreeGrid's copy.
- TreeGrid: the caption gap is contradictory across platforms — the web note says the caption Heading takes marginBlockEnd space.0 (the grid's captionGap does the spacing), while DataGrid on rn puts captionGap (space.2) on the Heading itself and TreeGrid declares neither binding. Used space.2 on the Heading so it matches its DataGrid sibling on this platform.
- TreeGrid: the accessible name of the lazy placeholder row is only specified as `copy.loading in the row-header column`, but every rn row header also announces its level. Rendered it as "Loading, Level {n}" (no child count, not selectable, not editable).
- TreeGrid: with `selectable: "cell"`, pressing the row header of a row with children is doubly specified — the rn notes say pressing a row header with children toggles it, and cell selection says the tapped cell is selected. Chose toggle only (no cell selection) for parents; leaves follow DataGrid's order (select the cell, then edit when editable). Long-press edits an editable parent row header, as the notes say.
- TreeGrid: the keyboard table has no core React Native path for the arrows, Home/End, `*`, F2, Space, Shift+Space, Control+A, Page keys, Delete/Backspace or Shift+arrow resize — View and Pressable have no key events. Covered by the row header's expand/collapse actions, the root expandAll/collapseAll, the select Checkboxes, the header's increment/decrement resize actions and the editing cell's activate/escape, which is also the only thing answering the `escape-dismiss` a11y requirement (TreeGrid has no overlay to dismiss).
- TreeGrid: two anatomy parts have no element here — `body` (FlatList gives the rows no wrapper, as DataGrid) and the guide lines, which are not a declared part and so carry no testID; they are drawn as an absolutely positioned decorative overlay inside the row header cell.

## 2026-09-21 21:53 — round 1

- TreeGrid: the copy block still has no equivalent of DataGrid's `cellLabel` ({column}: {value}), which the rn notes require on every cell ('Cells carry accessibilityLabel "{column}: {value}"'). `editHint` was added to TreeGrid's copy but `cellLabel` was not; reused DataGrid's string verbatim, flagged in a source comment. The doc otherwise insists each platform package is self-contained in its copy.
- TreeGrid: the caption bindings are undeclared and the platforms disagree — the web note says the caption Heading takes marginBlockEnd space.0 and the grid's captionGap does the spacing, while TreeGrid declares neither captionSize, captionWeight nor captionGap. Used DataGrid's rn defaults (font.size.md, semibold, space.2 on the Heading) so it matches its sibling on this platform.
- TreeGrid: 'every binding DataGrid declares that TreeGrid does not applies at DataGrid's own default token' has no machine-readable form, so those defaults are hard-coded a second time here (column width space.20 x 2, grid line color.border at border.width.thin, header font.weight.semibold / font.size.sm, row hover color.action.ghost.background.hover, resize handle space.1, resizeStep space.4, header and pinned shadow.raised, status bar padding and gap space.2). Nothing keeps the two files in step: change a DataGrid default and TreeGrid drifts silently. Either restate them in TreeGrid's styles block or declare the sharing.
- TreeGrid: `density` is 'as DataGrid' but the doc never states the consequence DataGrid's rn file had to solve — this package's Checkbox is a whole comfortable-target row whose height is not overridable, so at `density: compact` (row height = size.target.min) the select-column checkboxes of adjacent rows overlap and getItemLayout stops matching what a row measures. Raised the row to size.target.comfortable at both densities whenever `selectable: "row"`, as DataGrid does, and told the virtualizer the same height. minTarget is documented as 'the locked row-height floor', which this quietly exceeds.
- TreeGrid: `expandButtonSize` is locked at size.target.min and described as the width the guide lines align to, but it cannot size the composed Button's own touch target — Button has no size-override binding and a child is never restyled. The token only reserves the layout width around the button; the Button's real target is whatever `size: sm` gives it.
- TreeGrid: `focusRing` and `focusRingWidth` are locked bindings with no `part`, so where they are drawn is a guess. Used them twice: as the inset ring over a focused, selected, editing or invalid cell, and as the scroll region's own border (DataGrid's technique, so the horizontal region's overflow cannot clip a cell's ring). The doc should name the part.
- TreeGrid: `transition` is TreeGrid's own token for the chevron, while 'the row hover and editor-open transitions are DataGrid's, at DataGrid's own token'. Both resolve to motion.duration.fast today, so the distinction is real in the code (overriding TreeGrid's `transition` moves only the chevron) but unobservable in any theme shipped now, and untestable.
- TreeGrid: the whole keyboard table except Enter has no core React Native path — View and Pressable get no key events. The arrows, Home/End, Ctrl+Home/End, `*`, F2, Space, Shift+Space, Control+a, the Page keys, Delete/Backspace and Shift+arrow resize are replaced by the row header's expand/collapse actions, the root expandAll/collapseAll, the select Checkboxes, the header cell's increment/decrement resize actions and the editing cell's activate/escape — which is also the only thing answering `escape-dismiss`, TreeGrid having no overlay. The `*` substitute is the wrong scope: expandAll covers every loaded row, not the focused row's siblings, because native has no focused row to read the level from.
- TreeGrid: because Delete/Backspace has no native path, the only way onCellChange can report `value: undefined` is an emptied number editor — the 'undefined when cleared' half of the contract is unreachable on this platform. Similarly onCellChange's `previous` is undefined only when the row held no value.
- TreeGrid: `copy.position` is specified as 'the active cell's row number among the visible rows, so it agrees with aria-rowindex'. There is no aria-rowindex here (the rn element is a FlatList with role=grid, and RN conveys no row position in set), so the number is shown in the status bar with nothing to agree with. Kept the visible-row index + 1.
- TreeGrid: the expand Button sits inside the row-header Pressable, which is `accessible`. Now that the Button is no longer hidden (per the updated rn notes), react-native-web exposes both as focusable — which is what avoids the axe failure — but on iOS VoiceOver an accessible parent still swallows its children, so expansion is reachable there only through the row header's expand/collapse actions. The doc asks for both controls without saying which one wins where the two platforms differ.
- TreeGrid: the lazy placeholder row's layout across non-row-header columns is unspecified — rendered as empty role=cell Views at their column widths, with copy.loading in the row-header column, announced as 'Loading, Level {n}' (no child count), neither selectable nor editable, and the parent row marked busy.
- TreeGrid: the row-header accessibilityLabel format is given only for rows with children. Left the count out for leaves and for lazy rows whose children have not arrived, rather than saying '0 items'; the doc should give the leaf and unloaded-lazy forms.
- TreeGrid: `long-pressing an editable row header edits` is specified for a parent (press being the toggle), but not for a leaf, where press already edits. Left long-press unbound on leaves.
- TreeGrid: `loadingColor` is locked at color.foreground.muted and lands on the composed Text of the placeholder. Delivered through Text's own `muted` tone, which resolves to that token — there is no way to hand a composed child an arbitrary colour without restyling it, so a future re-point of the locked binding to a different token would silently not apply here.
- TreeGrid: copy.sortedAnnouncement takes `direction` as 'ascending or descending' — the raw enum value is passed straight into the sentence, so the announcement is English-shaped regardless of locale. Same as DataGrid; no copy string exists for the localized direction word.
- TreeGrid: two anatomy parts have no element here — `body` (FlatList gives the rows no wrapper, as DataGrid) and the guide lines, which are not a declared part and so carry no testID. Guide lines are drawn per row as an absolutely positioned decorative overlay inside the row header cell rather than as one segment spanning a parent's open subtree: FlatList rows are independent list items with nothing for a spanning line to anchor to.
- TreeGrid: `stickyHeader` has no observable effect on this platform — the header is the FlatList's sticky list header whenever the grid is bounded (`height` is not `content`), and with `height: content` the list does not scroll at all. The prop is accepted and documented as 'as DataGrid' but changes nothing.
