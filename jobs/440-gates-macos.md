Complete `.github/workflows/swiftui-gates.yml` (job 400's stub) on `macos-26`: `xcodebuild -scheme DesignSchema -destination 'generic/platform=iOS' build` (typecheck), `swift test` (Swift Testing), and for the target named by `inputs.target` an XCUITest that opens that component's gallery screen and runs `XCUIApplication().performAccessibilityAudit()` plus the keyboard-spec checks from generated/keyboard/<Name>.json on an iPad simulator with a hardware keyboard. Write `gates/<Name>.swiftui.json` in the shape job 430 expects and upload it as an artifact. Cache SwiftPM. Concurrency group per branch.
Gate: green on the skeleton and on the first generated Button. Do not modify packages/*/src or generated/.
