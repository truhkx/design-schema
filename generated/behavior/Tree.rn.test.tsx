// Generated by tools/behavior_tests.ts from the `behavior` block of the Tree doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Tree } from '../../packages/rn/src/Tree';
import type { TreeProps } from '../../packages/rn/src/Tree';
import meta from '../../packages/rn/src/Tree.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<TreeProps> = {}) {
  const events = {
    onSelectionChange: jest.fn(),
    onExpandChange: jest.fn(),
    onExpand: jest.fn(),
    onActivate: jest.fn(),
  };
  const props: TreeProps = { ...(meta.args as TreeProps), ...given, onSelectionChange: events.onSelectionChange, onExpandChange: events.onExpandChange, onExpand: events.onExpand, onActivate: events.onActivate };
  const tree = (p: TreeProps) => (
    <ThemeProvider mode="light">
      <Tree {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Tree') ?? screen.UNSAFE_root,
    container: () => screen.queryByRole('tree') ?? s.root(),
    rerender: (next: Partial<TreeProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Tree', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-2', () => {
    const s = setup({"headingLevel": "2"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-3', () => {
    const s = setup({"headingLevel": "3"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-4', () => {
    const s = setup({"headingLevel": "4"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-none', () => {
    const s = setup({"selectable": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-single', () => {
    const s = setup({"selectable": "single"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-multiple', () => {
    const s = setup({"selectable": "multiple"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('tree', { name: s.props.label })).toBeOnTheScreen();
  });
});
