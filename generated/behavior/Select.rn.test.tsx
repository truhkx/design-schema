// Generated by tools/behavior_tests.ts from the `behavior` block of the Select doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Select } from '../../packages/rn/src/Select';
import type { SelectProps } from '../../packages/rn/src/Select';
import meta from '../../packages/rn/src/Select.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<SelectProps> = {}) {
  const events = {
    onChange: jest.fn(),
    onOpenChange: jest.fn(),
  };
  const props: SelectProps = { ...(meta.args as SelectProps), ...given, onChange: events.onChange, onOpenChange: events.onOpenChange };
  const tree = (p: SelectProps) => (
    <ThemeProvider mode="light">
      <Select {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Select') ?? screen.UNSAFE_root,
    label: () => screen.queryByRole('combobox') ?? s.root(),
    rerender: (next: Partial<SelectProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Select', () => {
  test('renders', () => {
    const s = setup({"open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-sm', () => {
    const s = setup({"size": "sm", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-md', () => {
    const s = setup({"size": "md", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-native-auto', () => {
    const s = setup({"native": "auto", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-native-always', () => {
    const s = setup({"native": "always", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-native-never', () => {
    const s = setup({"native": "never", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({"open": true});
    expect(screen.getByRole('combobox', { name: s.props.label })).toBeOnTheScreen();
  });
  test('error-is-identified', () => {
    const s = setup({"error": "Fix this before continuing.", "open": true});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeOnTheScreen();
  });
});
