// Generated by tools/behavior_tests.ts from the `behavior` block of the Toast doc. Do not edit.
import { beforeEach, describe, expect, test, vi } from 'vitest';
import { userEvent } from 'vitest/browser';
import '../../packages/lit/src/Toast.js';
import meta from '../../packages/lit/src/Toast.stories.js';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function deep(root: ParentNode, selector: string): Element | null {
  const direct = root.querySelector(selector);
  if (direct) return direct;
  for (const el of Array.from(root.querySelectorAll('*'))) {
    const shadow = (el as HTMLElement).shadowRoot;
    if (shadow) {
      const found = deep(shadow, selector);
      if (found) return found;
    }
  }
  return null;
}

/** Every element that is "active" from the document down through shadow roots: the host, then the inner one.
 *  document.activeElement alone is a shadow host while focus sits inside its shadow tree. */
function activeChain(): Element[] {
  const chain: Element[] = [];
  let el: Element | null = document.activeElement;
  while (el) {
    chain.push(el);
    el = el.shadowRoot?.activeElement ?? null;
  }
  return chain;
}

async function setup(given: Record<string, unknown> = {}) {
  const el = document.createElement('ds-toast');
  const props = { ...meta.args, ...given };
  for (const [key, value] of Object.entries(props)) {
    if (value === undefined) continue;
    // `children` is slotted light-DOM content, and `Element.children` is getter-only:
    // assigning it throws and takes the whole file down at setup. Slot a string into the
    // default slot the way the stories do; a richer value cannot be expressed here.
    if (key === 'children') {
      if (typeof value === 'string') el.append(value);
      continue;
    }
    (el as unknown as Record<string, unknown>)[key] = value;
  }
  const events = {
    onAction: vi.fn(),
    onDismiss: vi.fn(),
  };
  el.addEventListener('action', events.onAction as unknown as EventListener);
  el.addEventListener('dismiss', events.onDismiss as unknown as EventListener);
  document.body.append(el);
  await (el as unknown as { updateComplete: Promise<boolean> }).updateComplete;
  const root: ParentNode = el.shadowRoot ?? el;  // some elements render in light DOM
  const s = {
    el,
    root,
    events,
    props,
    root_: () => el,
    region: () => (deep(root, '[role="status"]') ?? deep(root, '[part="region"]') ?? deep(root, '[data-part="region"]') ?? root.firstElementChild) as HTMLElement,
    actionButton: () => (deep(root, '[part="actionButton"]') ?? deep(root, '[data-part="actionButton"]')) as HTMLElement,
    dismissButton: () => (deep(root, '[part="dismissButton"]') ?? deep(root, '[data-part="dismissButton"]')) as HTMLElement,
  };
  return s;
}

beforeEach(() => {
  document.body.replaceChildren();
});

describe('ds-toast', () => {
  test('the-dismiss-button-fires-on-dismiss', async () => {
    const s = await setup({"dismissible": true});
    await userEvent.click(s.dismissButton());
    expect(s.events.onDismiss).toHaveBeenCalled();
  });
  test('the-action-button-fires-on-action', async () => {
    const s = await setup({"actionLabel": "Undo"});
    await userEvent.click(s.actionButton());
    expect(s.events.onAction).toHaveBeenCalled();
  });
  test('escape-dismisses-the-focused-toast', async () => {
    const s = await setup({});
    s.el.focus();
    await userEvent.keyboard('{Escape}');
    expect(s.events.onDismiss).toHaveBeenCalled();
  });
  test('danger-toasts-are-announced-assertively', async () => {
    const s = await setup({"tone": "danger"});
    expect(s.el.shadowRoot!.querySelector('[role="alert"]')).not.toBeNull();
  });
  test('the-message-is-rendered', async () => {
    const s = await setup({"message": "3 files moved to Archive"});
    expect(s.el.shadowRoot!.textContent).toMatch(new RegExp("3\\ files\\ moved\\ to\\ Archive"));
  });
  test('renders', async () => {
    const s = await setup({});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-tone-neutral', async () => {
    const s = await setup({"tone": "neutral"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-tone-success', async () => {
    const s = await setup({"tone": "success"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-tone-warning', async () => {
    const s = await setup({"tone": "warning"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-tone-danger', async () => {
    const s = await setup({"tone": "danger"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-duration-short', async () => {
    const s = await setup({"duration": "short"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-duration-long', async () => {
    const s = await setup({"duration": "long"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-duration-persistent', async () => {
    const s = await setup({"duration": "persistent"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('has-accessible-name', async () => {
    const s = await setup({});
    expect(s.region()).toHaveAccessibleName();
  });
});
