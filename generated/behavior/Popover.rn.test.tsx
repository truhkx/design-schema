// Generated by tools/behavior_tests.ts from the `behavior` block of the Popover doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Popover } from '../../packages/rn/src/Popover';
import type { PopoverProps } from '../../packages/rn/src/Popover';
import meta from '../../packages/rn/src/Popover.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<PopoverProps> = {}) {
  const events = {
    onOpenChange: jest.fn(),
  };
  const props: PopoverProps = { ...(meta.args as PopoverProps), ...given, onOpenChange: events.onOpenChange };
  const tree = (p: PopoverProps) => (
    <ThemeProvider mode="light">
      <Popover {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Popover') ?? screen.UNSAFE_root,
    trigger: () => screen.queryByRole('dialog') ?? s.root(),
    closeButton: () => screen.queryByTestId('Popover.closeButton') ?? s.root(),
    rerender: (next: Partial<PopoverProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Popover', () => {
  test('close-button-fires-on-open-change', () => {
    const s = setup({"open": true});
    fireEvent.press(s.closeButton());
    expect(s.events.onOpenChange).toHaveBeenCalled();
  });
  test('the-panel-is-named-by-its-heading', () => {
    const s = setup({"open": true, "heading": "Filters"});
    expect(screen.getByRole('dialog', { name: "Filters" })).toBeOnTheScreen();
  });
  test('renders', () => {
    const s = setup({"open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-2', () => {
    const s = setup({"headingLevel": "2", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-3', () => {
    const s = setup({"headingLevel": "3", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-4', () => {
    const s = setup({"headingLevel": "4", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-bottom-start', () => {
    const s = setup({"placement": "bottom-start", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-bottom', () => {
    const s = setup({"placement": "bottom", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-bottom-end', () => {
    const s = setup({"placement": "bottom-end", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-top-start', () => {
    const s = setup({"placement": "top-start", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-top', () => {
    const s = setup({"placement": "top", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-top-end', () => {
    const s = setup({"placement": "top-end", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-start', () => {
    const s = setup({"placement": "start", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-end', () => {
    const s = setup({"placement": "end", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-initial-focus-first', () => {
    const s = setup({"initialFocus": "first", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-initial-focus-none', () => {
    const s = setup({"initialFocus": "none", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({"open": true});
    expect(screen.getByRole('dialog')).toBeOnTheScreen();
  });
});
