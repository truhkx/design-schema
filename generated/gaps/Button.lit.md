# Gaps reported while generating Button for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 01:08 — round 1

- Button: `inverse` is a boolean prop but is not listed under platforms.lit.reflect (which only lists variant, size, type, disabled, icon-only, loading). Reflected it anyway (type: Boolean, reflect: true) since CSS attribute selectors are the only way to style it from the shadow stylesheet and every other boolean on this component is reflected — treating the omission as an oversight in the reflect list rather than an intentional exclusion.
- Button: the `inverse` description says ghost hover 'uses a translucent inverse foreground' but names no specific token/opacity for it. Used `color-mix(in srgb, var(--color-inverse-foreground) 12%, transparent)` (matches the codebase's existing color-mix-of-tokens exemption pattern) since no `backgroundHover`-style binding exists for the inverse case.
- Button: renamed the container part from the pre-existing `part="button"` to `part="container"` to match the anatomy name literally (consistent with Card/Heading/etc. convention) — confirmed correct since tools/behavior_tests.py's auto-generated harness queries `[part="container"]`.
- Button: `track`'s CustomEvent detail order relative to `press` — spec says trackPress/onTrack fire 'after onPress'; implemented as press dispatch, then (if track set) trackPress() call, then track dispatch, all synchronously within the same click handler.

## 2026-09-10 17:17 — round 1

- Button: schema lists `expanded`, `accessibleName`, and `overflowLabel` props and a `spinnerStroke` overridable binding that the pre-existing Button.ts didn't implement; added them (expanded as a non-reflected, attribute:false boolean since it's a tri-state undefined/true/false set by disclosure parents via JS property, not a plain HTML attribute; accessibleName maps to aria-label taking priority over the iconOnly fallback; overflowLabel is a plain data prop for Toolbar to read, with no rendering effect in Button itself).
- Menu.ts's trigger sets a raw `aria-expanded` attribute directly on the `<ds-button>` host element rather than setting the new `expanded` property — that attribute doesn't forward into the shadow-DOM `<button>`, so it currently has no effect on the accessible state. Out of scope for this Button-only pass; flagging so Menu (and any other disclosure parent: Popover, SidePanel, Disclosure) can be updated to use `.expanded=` instead.
- IconOnly story previously drew a hand-typed Unicode '✕' glyph in the leading-icon slot, violating the 'never draw a Unicode glyph by hand' rule; replaced with `<ds-icon name="close">`.

## 2026-09-10 19:47 — round 1

- Button (lit): the existing implementation hid the entire label/icon content via `visibility: hidden` and overlaid a centered absolute spinner while loading, contradicting the spec's 'hides trailingIcon, keeps the label visible' requirement. Fixed by placing the spinner in the leading-icon slot position (display toggled with `loading`), hiding only the leading/trailing icon slots via CSS while `loading`, and leaving the label always visible.
- Button (lit): `expanded` is `attribute: false` (JS-property only, not a reflected HTML attribute) since the spec says it's set by a disclosing parent programmatically and isn't in the `platforms.lit.reflect` list; kept as-is since this predates my pass and matches the 'consumers rarely set it directly' intent, but flagging since the spec doesn't explicitly say whether it should still be attribute-settable for the derived `renders` test.

## 2026-09-16 03:49 — round 1

- Button: `copy.loading` has no declared surface on web/Lit — only SwiftUI is told where it goes (`.accessibilityValue`). Chose visually-hidden text rendered after the label while `loading`, so the accessible name becomes "Save changes Loading". Consequence the doc does not address: when `accessibleName` or `iconOnly` sets `aria-label`, the string is dropped entirely, so an icon-only loading button never announces it.
- Button: `accessibleName` says "The visible label must be the start of it (WCAG 2.5.3 label-in-name)", but the doc's own example is label "Amount" -> "Sort by Amount, ascending", where the label is only *contained*. Because the rule contradicts its example, no dev-mode warning was emitted; the JSDoc says "part of it". Pick one and say which.
- Button: `spinnerStroke` is listed as locked in the overrides contract, but the previous Lit generation exposed it in `ButtonOverridableBinding` with a `--ds-button-spinner-stroke` hook. Removed it to match the contract — this silently breaks any consumer passing `overrides.spinnerStroke`. The doc says nothing about deprecating a binding that moves from overridable to locked.
- Button: the doc never says whether a click on a `disabled` or `loading` button should still let the native `click` escape the shadow root. The Lit guidance explicitly invites consumers to listen to that native click, so letting it through would fire their handlers (and submit a native form) on a disabled button. Chose `preventDefault()` + `stopPropagation()` so nothing leaks.
- Button: `type: submit` has two declared paths (ds-form listens for `press`; a directly-enclosing native `<form>` gets `requestSubmit()`) with no rule for a `<ds-form>` nested inside a native `<form>`, where both would fire. Kept `closest('form')?.requestSubmit()` unconditional.
- Button: parts vs slot names collide in casing. Anatomy is camelCase (`leadingIcon`) and 'part values are the anatomy names verbatim', but the platform notes and the Parts-and-slots contract spell the slots `leading-icon`/`trailing-icon`. Chose `part="leadingIcon"` with `name="leading-icon"`; the previous generation used `part="leading-icon"`, so external `::part()` selectors change.
- Button: `a11y.requires` includes `target-24px` and the guidance also demands 44x44 on touch platforms, but `minTarget` resolves to a single token (`size.target.min`) with no touch-sized counterpart and no coarse-pointer rule. Only the 24px floor is implemented on Lit; a coarse-pointer media query would need a token the doc does not name.
- Button: `inverse` states that only `ghost` changes its fill, so `primary`/`secondary`/`danger` keep their own background and `backgroundHover` on an inverse surface — but `a11y.contrast` only proves `color.inverse.link` and `color.inverse.focus` against `color.inverse.surface`. Nothing proves the non-ghost fills read on that surface; implemented as written.
- Button: `expanded` is declared `type: boolean` with no default, while the Lit notes require it to stay tri-state (`undefined` renders no `aria-expanded`). Typed `boolean | undefined` with `attribute: false`. Relatedly, scenario `expanded-is-reported` asserts `state: expanded is true` with no platform-neutral spelling; read as `aria-expanded="true"` on the `container` part.
- Button: `overflowLabel` is a prop on Button that only Toolbar reads, and it is absent from `platforms.lit.reflect`. Left as a non-reflected property — but then Toolbar cannot find it by attribute on a `<ds-button>` it did not create, only by reading the JS property. Say which Toolbar uses.
- Button: scenarios `enter-activates` and `space-activates` are listed for lit, but a native `<button>` already synthesises `click` for both and the doc asks for no key handling. No key code was written; the scenarios pass through the native path. Worth stating that they are `native` rules rather than implementable behaviour, as the keyboard contract does elsewhere.

## 2026-09-17 04:17 — round 1

- Button: loading says copy.loading is referenced by aria-describedby 'merged with any caller aria-describedby', but on Lit the caller's aria-describedby sits on the host and IDs cannot cross the shadow boundary. Only the internal id is referenced; the doc should say how (or whether) Lit merges the caller's value.
- Button: the Overrides section says every binding gets a hook, and spinnerStroke explicitly keeps its hook while locked. It doesn't say whether the other locked bindings (background, backgroundHover, foreground, focusRing, focusRingWidth, inverseForeground, inverseFocusRing, minTarget) also get document-overridable --ds-button-* hooks. I gave them all hooks, which means page CSS can still replace a locked colour; the doc should confirm or forbid this.
- Button: leadingIcon/trailingIcon are 'decorative — hidden from assistive technology', but the Lit notes don't say whether the button hides the slot or relies on an unlabelled ds-icon being decorative. I left the slots unhidden (aria-hidden on a <slot> is unreliable in Chromium) and rely on the Icon.
- Button: spinnerSize is font.size.{size} ('1em of the label font'), but a fontSize override doesn't move spinnerSize, so the ring and the label can drift apart. I kept them as separate hooks as the bindings list them.
- Button: 'Inside a disabled Form the button is disabled whatever this prop says' has no Lit mechanism in the Button doc. ds-form already sets .disabled on ds-button descendants, so Button does nothing itself; the Lit notes could say so.
- Button: the inverseHoverOpacity computed value is an opacity factor, but color-mix needs a percentage. I wrote calc(var(--ds-button-inverse-hover-opacity) * 100%); the doc doesn't give the conversion.
- Button: backgroundHover has state 'hover' but its description says 'hover and pressed'. I applied it to :hover and :active.
- Button: the press event has no payload and the Events contract says detail has 'exactly the listed keys' (none). Kept ButtonPressDetail = void with no detail.
- Button: the disabled-does-not-fire scenario 'click: container' can't run as written on Lit/web: Playwright's actionability check rejects an aria-disabled button, so the test uses click({ force: true }). The scenario schema could state that blocked-activation clicks are forced.

## 2026-09-18 18:31 — round 1

- Button: `inverseHoverOpacity` is described two ways. The prop text says the hook holds the base token and the rule that reads it multiplies (`calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)`). The generated 'Style bindings' section puts the ×0.25 in the binding itself (`calc(var(--opacity-disabled) * 0.25)`). I followed the prop text: hook = `var(--opacity-disabled)`, multiplied in the rule.
- Button: the loading spinner has to be drawn as a ring (round), but no binding names its corner radius. I used `var(--radius-full)` directly, with no hook. The doc should say whether the ring's roundness is a token or a binding.
- Button: `label` is `required: true` with no default, but a Lit property still needs an initial value. I defaulted it to '' and added no dev warning when it is missing. The doc doesn't say whether Lit should warn about a missing label.
- Button: the interpolated locked bindings (background, backgroundHover, foreground) get no hook, so the 'resolve per enum value with a lookup' rule can't go through a per-attribute hook. I wrote one rule per variant for each (4 × 2) that reads the token directly. The doc could say that locked interpolated bindings are written out per value.
- Button: the doc doesn't say whether hover and pressed fills (`backgroundHover`, `inverseBackgroundHover`) apply while the button is `disabled` or `loading`. I suppressed them in both states, since neither accepts a press.
- Button: the Lit guidance lists the reflected attributes as `variant, size, disabled, icon-only, loading`. `platforms.lit.reflect` also lists `type`, `inverse` and `overflow-label`. I followed `platforms.lit.reflect`; the guidance list should be updated to match.

## 2026-09-18 18:40 — round 2

- Button: neither failing gate points at Button. axe-lit's full report (logs/playwright.json) has no Button/Lit failure in light or dark. There is no Button keyboard spec because the schema has no `keyboard` block. The failures belong to Carousel, DataGrid, Feed, Listbox, NumberInput, Select, SidePanel, Slider, Splitter, Tabs, TreeGrid (axe) and to Combobox, DatePicker, Dialog, Feed, Listbox, Menu, Popover, Search, SegmentedControl, Select, SidePanel, Slider, Stepper, Tabs, Toast, Toolbar, Tooltip, Tree (keyboard). A rerun of the Toast and Toolbar Lit specs shows failures in each component's own key handling (Toast F6/Escape/Tab, Toolbar Home). Button's round-1 change was CSS-only, so I left Button.ts unchanged.
- Button: round-1 gaps still apply. (1) inverseHoverOpacity: the prop text puts the ×0.25 in the rule that reads the hook, but the Style bindings section puts it in the binding's computed value; I followed the prop text. (2) No binding covers the spinner ring's corner radius; I used var(--radius-full) directly. (3) `label` is required with no default; the Lit property defaults to '' and there is no dev warning. (4) Locked interpolated bindings have no hooks, so I wrote one rule per variant. (5) Whether hover fills apply while disabled or loading is unstated; I suppressed them. (6) The Lit guidance's reflected-attribute list omits type, inverse and overflow-label, which platforms.lit.reflect includes.

## 2026-09-18 18:47 — round 3

- Button: round 3 is the same as round 2. axe-lit's full report for this run (logs/playwright.json, 2026-09-18T22:41Z) has no Button/Lit failure. There is no Button keyboard spec because the schema has no `keyboard` block. The failing entries belong to Carousel, DataGrid, Feed, Listbox, NumberInput, Select, SidePanel, Slider, Splitter, Tabs, TreeGrid (axe) and to Combobox, DatePicker, Dialog, Feed, Listbox, Menu, Popover, Search, SegmentedControl, Select, SidePanel, Slider, Stepper, Tabs, Toast, Toolbar, Tooltip, Tree (keyboard). These are whole-Storybook gates, so they will stay red until those components are fixed; Button.ts is unchanged.
- Button: round-1 gaps still apply. (1) inverseHoverOpacity: the prop text puts the ×0.25 in the rule that reads the hook, but the Style bindings section puts it in the binding's computed value; I followed the prop text. (2) No binding covers the spinner ring's corner radius; I used var(--radius-full) directly. (3) `label` is required with no default; the Lit property defaults to '' and there is no dev warning. (4) Locked interpolated bindings have no hooks, so I wrote one rule per variant. (5) Whether hover fills apply while disabled or loading is unstated; I suppressed them. (6) The Lit guidance's reflected-attribute list omits type, inverse and overflow-label, which platforms.lit.reflect includes.

## 2026-09-19 09:04 — round 1

- Button: the Style bindings section gives inverseHoverOpacity's computed as `calc(var(--opacity-disabled) * 0.25)`, reading the token, while the prop prose says the rule reads the hook (`calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)`) so an override is multiplied too; I followed the prose (hook × 0.25 × 100% inside color-mix).
- Button: loading prose says `a spinnerSize circle with one quarter transparent` but not which quarter; kept the existing inline-end border transparent.
- Button: iconOnly says padding is `space.sm` on all sides but paddingInline `takes the resolved paddingBlock`; these only agree while paddingBlock is at its default. I implemented the latter (padding-inline reads --ds-button-padding-block), so a paddingBlock override changes all four sides.
- Button: example `icon-only-in-a-toolbar` gives `leadingIcon: "Icon name=close"` as a string shorthand; Lit has no content-typed prop, so the story arg is the icon name (`leadingIcon: 'close'`) and the render slots `<ds-icon slot="leading-icon" name=… inline>`. The example args are therefore not literally the `given`.
- Button: the doc does not say whether the `press` event or the native-form `requestSubmit()` comes first for `type=submit`, or whether `track` fires before the submit; kept press → trackPress → track → requestSubmit.
- Button: the Inverse story needs an inverse-surface wrapper, which uses inline `style` with tokens on a story-only div; the doc names no Surface/Box inverse option to compose instead.

## 2026-09-19 09:13 — round 2

- Button: gates `axe` and `keyboard-run` run over the whole Lit Storybook, so they fail on other components (Tabs contrast; TreeGrid aria-hidden-focus and target-size; keyboard specs for Combobox, DatePicker, Dialog, Feed, Listbox, Menu, Popover, Search, SegmentedControl, Select, SidePanel, Slider, Stepper, Tabs, Toast, Toolbar, Tooltip, Tree). The report has no Button/Lit entry; all 23 Button/Lit stories pass axe in light and dark when run alone, and Button has no keyboard block. The rerun failures are reproducible at 2 workers; SegmentedControl, Tabs and Select fail without using ds-button. No Button code changed.
- Button: gaps carried over from round 1 still apply: inverseHoverOpacity's computed reads the token in the Style bindings section but the hook in the prose (followed the prose); which quarter of the spinner ring is transparent is unspecified; iconOnly's 'space.sm on all sides' vs 'paddingInline takes the resolved paddingBlock' (implemented the latter); example icon-only-in-a-toolbar's `leadingIcon: "Icon name=close"` becomes the icon-name arg `'close'` on Lit; order of press / track / requestSubmit for type=submit is unstated; Inverse story uses a story-only inline-styled wrapper for the inverse surface.

## 2026-09-19 09:20 — round 3

- Button: gates `axe` and `keyboard-run` fail only on other components (Tabs contrast; TreeGrid aria-hidden-focus/target-size; keyboard specs for Combobox, DatePicker, Dialog, Feed, Listbox, Menu, Popover, Search, SegmentedControl, Select, SidePanel, Slider, Stepper, Tabs, Toast, Toolbar, Tooltip, Tree). Button has no keyboard block and all 23 Button/Lit stories pass axe in both modes. Example of the cause: generated/keyboard/Toolbar.lit.spec.ts collects light-DOM `[tabindex]` hosts (Toolbar's roving ds-button hosts) before shadow-root buttons, so index 0 is a host and `Home → focusIndex 0` cannot pass; that is tools/keyboard_tests.ts × Toolbar, not Button. Nothing in the Button doc or code can make these gates pass; they need scoping to the job's component or the other components fixed.
- Button: gaps carried over from round 1 still apply: inverseHoverOpacity's computed reads the token in the Style bindings section but the hook in the prose (followed the prose); which quarter of the spinner ring is transparent is unspecified; iconOnly's 'space.sm on all sides' vs 'paddingInline takes the resolved paddingBlock' (implemented the latter); example icon-only-in-a-toolbar's `leadingIcon: "Icon name=close"` becomes the icon-name arg `'close'` on Lit; order of press / track / requestSubmit for type=submit is unstated; Inverse story uses a story-only inline-styled wrapper for the inverse surface.

## 2026-09-20 01:46 — round 1

- Button: scenario `icon-only-keeps-its-name` contradicts its own description — `given` sets both `iconOnly: true` and `accessibleName: 'Open menu'` and expects the name "Open menu", while the description says "label becomes the accessible name". The doc never states precedence when both are set; implemented as accessibleName winning over the iconOnly label fallback, which is what makes the scenario pass. State the precedence in the prop text.
- Button: `inverseBackgroundHover` says "this color at inverseHoverOpacity over color.inverse.surface", but the ghost fill is transparent and the button may sit on a Toast or Tooltip panel that is not literally `color.inverse.surface`. Mixed against `transparent` so the real surface shows through, not against the inverse-surface token. Say which of the two the color-mix takes as its second colour.
- Button: `inverseHoverOpacity` is still described two ways — the Style bindings section computes `calc(var(--opacity-disabled) * 0.25)` (reading the token, so an override is not multiplied), the prop prose puts the x0.25 in the rule that reads the hook. Followed the prose, so the hook holds the bare token. This has been reported in four previous rounds and neither text has moved; pick one.
- Button: `iconOnly` says padding is `space.sm` on all sides and also that paddingInline takes the resolved paddingBlock. The two agree only while paddingBlock is at its default. Implemented the second (padding-inline reads `--ds-button-padding-block`), so a paddingBlock override moves all four sides.
- Button: the spinner is specified as a ring with "one quarter transparent" but neither which quarter nor its corner radius is a binding. Used the inline-end border and `var(--radius-full)` read directly with no hook.
- Button: `spinnerSize` is `font.size.{size}` and described as "1em of the label font", but it is its own binding that a fontSize override does not move, so the ring and the label can drift apart. Kept them as two independent hooks, as the bindings list them.
- Button: `type: submit` has no stated ordering for `press`, `track` and the native `requestSubmit()`, and no rule for a `ds-form` nested inside a native `<form>`. Kept press -> trackPress -> track -> `closest('form')?.requestSubmit()` guarded by `!this.closest('ds-form')`. A `submit` button with neither form present is a silent no-op, mirroring the rn note.
- Button: example `icon-only-in-a-toolbar` gives `leadingIcon: "Icon name=close"`, but Lit has no content-typed prop — the story arg is the icon name (`leadingIcon: 'close'`) and the inline render slots `<ds-icon slot="leading-icon" name=... inline>`. The example's args are therefore not literally its `given`.
- Button: the `Inverse` story needs an inverse-surface wrapper and the doc names no Box/Card surface value for it, so the story uses a story-only div with inline `style` reading `--color-inverse-surface` and `--space-lg`.
- Button: `a11y.requires` lists `target-24px` and the guidance also demands 44x44 on touch, but `minTarget` resolves to the single `size.target.min` token with no touch counterpart and the binding text explicitly rules out a coarse-pointer rule on web/Lit. Only the 24px floor exists; the 44px promise in the guidance is unimplementable as tokenised.
- Button: `inverse` keeps primary/secondary/danger fills unchanged on an inverse surface, but `a11y.contrast` only proves `color.inverse.link` and `color.inverse.focus` against `color.inverse.surface`. Nothing proves those three fills read there; implemented as written.
- Button: `label` is `required: true` with no default, and a Lit property needs an initial value. Defaults to `''` with no dev warning, per the platform note — but then an author who forgets `label` ships a button with no accessible name and no signal. The doc says explicitly not to warn; flagging that this is the one required prop with no enforcement on this platform.
- Button: no binding covers `cursor`. Chose `pointer`, `not-allowed` while `disabled` and `progress` while `loading`; none of the three is in the schema.
- Button: the host has no declared layout binding. Chose `display: inline-flex` + `vertical-align: middle` with the container at `inline-size: 100%`, so a stretched host can expose click area outside the inner button (a click there produces no `press`, correctly, but also is not swallowed while disabled).

## 2026-09-20 02:00 — round 2

- Button: the `axe` and `keyboard-run` gates are whole-Storybook runs, so a Button job is failed by other components' debt. This run: 0 Button/Lit entries in logs/playwright.json, and no generated/keyboard/Button.lit.spec.ts exists at all because the schema has no `keyboard` block. Scoped, Button/Lit passes axe on all 23 stories in light and dark. The gates need scoping to the job's component, or the job needs a per-component pass/fail, otherwise Button can never go green. This is the fourth round to report it.
- Button: the two dominant axe failures are other components wrapping a ds-button in an aria-hidden span (TreeGrid's `.expand-chevron`, NumberInput's `.steppers`) — 88 of the 127 entries. The Button doc says nothing about what a composing parent may do with a ds-button, and `tabindex="-1"` does not make one unfocusable. A line in the Lit notes saying a ds-button must never be placed inside an aria-hidden subtree (hide the decoration, or use `inert` + `aria-hidden` together) would put the constraint where composers read it.
- Button: Toolbar's keyboard spec fails because the generated harness collects light-DOM `[tabindex]` nodes before shadow-root buttons, so a roving tabindex on a `<ds-button>` host is counted separately from the inner `<button>` that actually takes focus. The doc requires delegatesFocus and an inner native button, so any composite that rovings tabindex over ds-button hosts double-counts. Either the harness should skip a shadow host whose shadow root contributes a focusable, or the doc should say how a roving-tabindex parent marks a ds-button as the tab stop.
- Button: scenario `icon-only-keeps-its-name` contradicts its own description — `given` sets both `iconOnly: true` and `accessibleName: 'Open menu'` and expects the name "Open menu", while the description says "label becomes the accessible name". The doc never states precedence when both are set; implemented as accessibleName winning over the iconOnly label fallback, which is what makes the scenario pass.
- Button: `inverseBackgroundHover` says "this color at inverseHoverOpacity over color.inverse.surface", but the ghost fill is transparent and the button may sit on a Toast or Tooltip panel that is not literally `color.inverse.surface`. Mixed against `transparent` so the real surface shows through. Say which of the two the color-mix takes as its second colour.
- Button: `inverseHoverOpacity` is still described two ways — the Style bindings section computes `calc(var(--opacity-disabled) * 0.25)` (reading the token, so an override is not multiplied), the prop prose puts the x0.25 in the rule that reads the hook. Followed the prose, so the hook holds the bare token.
- Button: `iconOnly` says padding is `space.sm` on all sides and also that paddingInline takes the resolved paddingBlock. The two agree only while paddingBlock is at its default. Implemented the second, so a paddingBlock override moves all four sides.
- Button: the spinner is specified as a ring with "one quarter transparent" but neither which quarter nor its corner radius is a binding. Used the inline-end border and `var(--radius-full)` read directly with no hook.
- Button: `spinnerSize` is `font.size.{size}` and also described as "1em of the label font", but it is its own binding that a fontSize override does not move, so the ring and the label can drift apart. Kept them as two independent hooks.
- Button: `type: submit` has no stated ordering for `press`, `track` and the native `requestSubmit()`, and no rule for a `ds-form` nested inside a native `<form>`. Kept press -> trackPress -> track -> `closest('form')?.requestSubmit()` guarded by `!this.closest('ds-form')`. A submit button with neither form present is a silent no-op, mirroring the rn note.
- Button: example `icon-only-in-a-toolbar` gives `leadingIcon: "Icon name=close"`, but Lit has no content-typed prop — the story arg is the icon name (`leadingIcon: 'close'`) and the inline render slots `<ds-icon slot="leading-icon" name=... inline>`. The example's args are therefore not literally its `given`.
- Button: the `Inverse` story needs an inverse-surface wrapper and the doc names no Box/Card surface value for it, so the story uses a story-only div with inline `style` reading `--color-inverse-surface` and `--space-lg`.
- Button: `a11y.requires` lists `target-24px` and the guidance also demands 44x44 on touch, but `minTarget` resolves to the single `size.target.min` token with no touch counterpart and the binding text explicitly rules out a coarse-pointer rule on web/Lit. Only the 24px floor exists.
- Button: `inverse` keeps primary/secondary/danger fills unchanged on an inverse surface, but `a11y.contrast` only proves `color.inverse.link` and `color.inverse.focus` against `color.inverse.surface`. Nothing proves those three fills read there; implemented as written.
- Button: `label` is `required: true` with no default, and the platform note says to default it to '' and not warn — so the one required prop has no enforcement on this platform and a forgotten label ships a nameless button silently.
- Button: no binding covers `cursor`. Chose `pointer`, `not-allowed` while `disabled` and `progress` while `loading`; none of the three is in the schema.
- Button: the host has no declared layout binding. Chose `display: inline-flex` + `vertical-align: middle` with the container at `inline-size: 100%`, so a stretched host can expose click area outside the inner button (a click there produces no `press`, correctly, but also is not swallowed while disabled).

## 2026-09-20 02:27 — round 3

- Button: the doc never says what a host `tabindex` means for a ds-button. `delegatesFocus` makes the host focusable but leaves the inner <button> independently tabbable, so `tabindex="-1"` on the host — which TreeGrid, NumberInput, DataGrid and Tree all already write — did nothing, keeping a tab stop inside their aria-hidden subtrees (88 axe aria-hidden-focus violations, every one in the package) and breaking the 'grid stays one tab stop' contract DataGrid.ts:261 and TreeGrid.ts:194 state in their own comments. I forward the host attribute to the inner button. The Lit platform notes should state this forward as part of the contract, since it is the only way a composer can take a ds-button out of the tab order without reaching into the shadow root.
- Button: forwarding is necessarily one microtask behind for *changes* to the host tabindex, because MutationObserver callbacks are microtasks — the initial value is read synchronously in connectedCallback, so parent-rendered markup is correct immediately, but a roving-focus parent that writes tabindex and synchronously measures the inner button sees the previous value. The doc gives no timing contract for host attributes that are not schema props.
- Button: the doc's reflect list and prop table cover only schema props, so it says nothing about global HTML attributes on the host (`tabindex`, and by the same argument `inert`, `hidden`, `lang`, `dir`). Only `tabindex` is forwarded; the rest are left to the host. A rule for which global attributes cross into the shadow root would settle this for every component with a shadow control, not just Button.
- Button: scenario `icon-only-keeps-its-name` contradicts its own description — `given` sets both `iconOnly: true` and `accessibleName: 'Open menu'` and expects the name "Open menu", while the description says "label becomes the accessible name". The doc never states precedence when both are set; implemented as accessibleName winning over the iconOnly label fallback, which is what makes the scenario pass.
- Button: `inverseBackgroundHover` says "this color at inverseHoverOpacity over color.inverse.surface", but the ghost fill is transparent and the button may sit on a Toast or Tooltip panel that is not literally `color.inverse.surface`. Mixed against `transparent` so the real surface shows through. Say which of the two the color-mix takes as its second colour.
- Button: `inverseHoverOpacity` is still described two ways — the Style bindings section computes `calc(var(--opacity-disabled) * 0.25)` (reading the token, so an override is not multiplied), the prop prose puts the x0.25 in the rule that reads the hook. Followed the prose, so the hook holds the bare token. Reported in five rounds now with neither text moving.
- Button: `iconOnly` says padding is `space.sm` on all sides and also that paddingInline takes the resolved paddingBlock. The two agree only while paddingBlock is at its default. Implemented the second, so a paddingBlock override moves all four sides.
- Button: the spinner is specified as a ring with "one quarter transparent" but neither which quarter nor its corner radius is a binding. Used the inline-end border and `var(--radius-full)` read directly with no hook.
- Button: `spinnerSize` is `font.size.{size}` and also described as "1em of the label font", but it is its own binding that a fontSize override does not move, so the ring and the label can drift apart. Kept them as two independent hooks.
- Button: `type: submit` has no stated ordering for `press`, `track` and the native `requestSubmit()`, and no rule for a `ds-form` nested inside a native `<form>`. Kept press -> trackPress -> track -> `closest('form')?.requestSubmit()` guarded by `!this.closest('ds-form')`. A submit button with neither form present is a silent no-op, mirroring the rn note.
- Button: example `icon-only-in-a-toolbar` gives `leadingIcon: "Icon name=close"`, but Lit has no content-typed prop — the story arg is the icon name (`leadingIcon: 'close'`) and the inline render slots `<ds-icon slot="leading-icon" name=... inline>`. The example's args are therefore not literally its `given`.
- Button: the `Inverse` story needs an inverse-surface wrapper and the doc names no Box/Card surface value for it, so the story uses a story-only div with inline `style` reading `--color-inverse-surface` and `--space-lg`.
- Button: `a11y.requires` lists `target-24px` and the guidance also demands 44x44 on touch, but `minTarget` resolves to the single `size.target.min` token with no touch counterpart and the binding text explicitly rules out a coarse-pointer rule on web/Lit. Only the 24px floor exists.
- Button: `inverse` keeps primary/secondary/danger fills unchanged on an inverse surface, but `a11y.contrast` only proves `color.inverse.link` and `color.inverse.focus` against `color.inverse.surface`. Nothing proves those three fills read there; implemented as written.
- Button: `label` is `required: true` with no default, and the platform note says to default it to '' and not warn — so the one required prop has no enforcement on this platform and a forgotten label ships a nameless button silently.
- Button: no binding covers `cursor`. Chose `pointer`, `not-allowed` while `disabled` and `progress` while `loading`; none of the three is in the schema.
- Button: the host has no declared layout binding. Chose `display: inline-flex` + `vertical-align: middle` with the container at `inline-size: 100%`, so a stretched host can expose click area outside the inner button (a click there produces no `press`, correctly, but also is not swallowed while disabled).
- Gates (not Button): the Lit vitest suite is 179 tests red independently of this job, and the failures are harness-shaped, not component-shaped — `generated/behavior/Toolbar.lit.test.ts:35` throws `Cannot set property children of #<Element> which has only a getter`, and `generated/behavior/Breadcrumb.lit.test.ts` dies on the browser-mode iframe navigating to /docs. tools/behavior_tests.ts appears to emit a `children` prop assignment that cannot work on a DOM element. Worth a pass of its own; no Button job can see it because Button's own behavior test passes.

## 2026-09-20 11:27 — round 1

- Button: the `inverseHoverOpacity` binding is specified twice and the two disagree — the Style bindings section gives `computed: calc(var(--opacity-disabled) * 0.25)` (raw token, no percentage), while the binding's own description mandates `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)` (the hook, as a color-mix percentage). Followed the description, since a color-mix second argument must be a percentage and the overrides contract requires reading the hook, not the token.
- Button: a11y.contrast proves `color.inverse.focus` only against `color.inverse.surface`, but `inverse` switches the focus ring to that token for *every* variant — on an inverse `primary`/`danger` button the ring is drawn against `color.action.{variant}.background`, which has no proof. Implemented as written; the contrast table needs the extra pairs or the rule needs narrowing to ghost.
- Button: no binding covers the focus ring's offset, only `focusRingWidth`. Chose `outline-offset: var(--border-width-focus)` so the ring clears the fill at every variant; if that should be zero or its own token, the doc has to say so.
- Button: `delegatesFocus` makes the host focusable but leaves the inner `<button>` a tab stop of its own, so a composer that writes `tabindex="-1"` on `<ds-button>` (ds-toolbar's roving focus, ds-tree-grid's chevron, ds-number-input's steppers) cannot take the control out of the tab order — and it cannot reach into this shadow root. The doc declares no contract for this. Implemented as a `MutationObserver` on the host's `tabindex` that mirrors it onto the inner button (read synchronously in `connectedCallback` so parent-written markup is never tabbable for a frame), covered by two tests that are not doc scenarios.
- Button: `spinnerStroke` is locked (excluded from the `overrides` type) yet its description keeps the `--ds-button-spinner-stroke` hook. Because document styles on the host beat `:host` rules, that hook is still settable from CSS — i.e. a locked, accessibility-bearing binding stays overridable through the sanctioned escape hatch. Kept the hook as the doc names it; the doc should say whether that is intended.
- Button: the scenario list flattens `loading-announces-busy-and-ignores-activation` without the `platforms: [web, lit]` key that the schema puts on its `aria-busy` assertion, so a native platform reading the flat list would be asked to assert a DOM attribute. No effect on Lit.
- Button: cursor is unspecified for every state. Chose `pointer` by default, `not-allowed` while `disabled` and `progress` while `loading`.
- Button: `iconGap` resolves to the primitive `space.2` while every other spacing binding uses a semantic step (`space.{size}`, `space.sm`). Used it verbatim, but it is the only primitive reference in the component and drifts if the semantic scale is retuned.

## 2026-09-20 11:41 — round 2

- Button: BLOCKING for the keyboard gate, not fixable from Button — `Toolbar.ts:460` guards its roving-tabindex write with `if (el.tabIndex !== next)`, but the `tabIndex` IDL getter returns -1 on a custom-element host with no `tabindex` attribute, so the write to inactive `<ds-button>` hosts is skipped entirely. The attribute never appears, ds-button has nothing to mirror, and the five shadow `<button>`s stay natively tabbable: the toolbar is 6 tab stops, breaking its own documented 'the toolbar is one tab stop' contract. Fix belongs in Toolbar (compare `getAttribute('tabindex')`, as TreeGrid.ts:2315 and Tree.ts:1040 already do), not in Button.
- Button: BLOCKING for `Toolbar > Home: First control`, not fixable from Button — the walker generated by tools/keyboard_tests.ts collects a delegatesFocus control twice, the host via `[tabindex]:not([tabindex="-1"])` and the shadow `<button>` via `button:not([disabled])`, so the first control resolves to index 1 and the test asserts 0. Any shadow-wrapped native control hits this; host-`0`/inner-`-1` does not help, since `button:not([disabled])` matches regardless of tabindex. The walker needs to skip a node whose shadow host is already listed.
- Button: the doc declares no tab-order contract for a shadow-wrapped control. `delegatesFocus` makes the host focusable but leaves the inner `<button>` an independent tab stop, so a composer that wants the control out of the tab order (ds-toolbar's roving focus, ds-tree-grid's chevron, ds-number-input's steppers inside an aria-hidden wrapper) cannot express it — it cannot reach into the shadow root. Implemented as a MutationObserver on the host's `tabindex` mirrored onto the inner button, read synchronously in connectedCallback so parent-written markup is never tabbable for a frame, covered by two tests that are not doc scenarios. The doc should state whether the host or the inner button is the tab stop.
- Button: the `inverseHoverOpacity` binding is specified twice and the two disagree — the Style bindings section gives `computed: calc(var(--opacity-disabled) * 0.25)` (raw token, no percentage), while the binding's own description mandates `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)` (the hook, as a color-mix percentage). Followed the description, since a color-mix second argument must be a percentage and the overrides contract requires reading the hook, not the token.
- Button: a11y.contrast proves `color.inverse.focus` only against `color.inverse.surface`, but `inverse` switches the focus ring to that token for *every* variant — on an inverse `primary`/`danger` button the ring is drawn against `color.action.{variant}.background`, which has no proof. Implemented as written; the contrast table needs the extra pairs or the rule needs narrowing to ghost.
- Button: no binding covers the focus ring's offset, only `focusRingWidth`. Chose `outline-offset: var(--border-width-focus)` so the ring clears the fill at every variant; if that should be zero or its own token, the doc has to say so.
- Button: `spinnerStroke` is locked (excluded from the `overrides` type) yet its description keeps the `--ds-button-spinner-stroke` hook. Because document styles on the host beat `:host` rules, that hook is still settable from CSS — a locked, accessibility-bearing binding stays overridable through the sanctioned escape hatch. Kept the hook as the doc names it; the doc should say whether that is intended.
- Button: the flat scenario list drops the `platforms: [web, lit]` key the schema puts on the `aria-busy` assertion of `loading-announces-busy-and-ignores-activation`, so a native platform reading that list would be asked to assert a DOM attribute. No effect on Lit.
- Button: cursor is unspecified for every state. Chose `pointer` by default, `not-allowed` while `disabled` and `progress` while `loading`.
- Button: `iconGap` resolves to the primitive `space.2` while every other spacing binding uses a semantic step (`space.{size}`, `space.sm`). Used it verbatim, but it is the only primitive reference in the component and drifts if the semantic scale is retuned.

## 2026-09-20 11:51 — round 3

- Button: BLOCKING for `Toolbar > Home: First control`, proven not fixable from Button — the walker generated by tools/keyboard_tests.ts counts a delegatesFocus control twice, the host via `[tabindex]:not([tabindex="-1"])` (Toolbar writes `tabindex="0"` on it) and its shadow `<button>` via `button:not([disabled])`. Measured both ways on the Toolbar keyboard story: 7 focusables and focusIndex 1 with Button's tabindex forwarding, and 7 focusables and focusIndex 1 with it stripped — identical, so Button is not the cause. Fix: after building the list, drop any entry whose `getRootNode()` is a ShadowRoot whose `.host` is already in the list.
- Button: BLOCKING for the keyboard gate, not fixable from Button — `Toolbar.ts:460` guards its roving-tabindex write with `if (el.tabIndex !== next)`, but the `tabIndex` IDL getter returns -1 on a custom-element host with no `tabindex` attribute, so the write to inactive `<ds-button>` hosts is skipped. The attribute never appears, ds-button has nothing to mirror, and the five shadow `<button>`s stay natively tabbable: the toolbar is 6 tab stops, breaking its own 'the toolbar is one tab stop' contract. Fix belongs in Toolbar (compare `getAttribute('tabindex')`, as TreeGrid.ts:2315 and Tree.ts:1040 already do).
- Button: `Table > Tab` is flaky in the gate, not a Button regression — it passes 5/5 in isolation. The generated test reads `before` with focus still on `body` (-1) and asserts Tab lands on index 0, so it depends on where focus starts; under the gate's 8-way parallelism it intermittently fails. Every generated Tab test that omits a `focusAt` seed has the same weakness.
- Button: the doc declares no tab-order contract for a shadow-wrapped control. `delegatesFocus` makes the host focusable but leaves the inner `<button>` an independent tab stop, so a composer that wants the control out of the tab order (ds-toolbar's roving focus, ds-tree-grid's chevron, ds-number-input's steppers inside an aria-hidden wrapper) cannot express it — it cannot reach into the shadow root. Implemented as a MutationObserver on the host's `tabindex` mirrored onto the inner button, read synchronously in connectedCallback so parent-written markup is never tabbable for a frame, covered by two tests that are not doc scenarios. The doc should state whether the host or the inner button is the tab stop.
- Button: the `inverseHoverOpacity` binding is specified twice and the two disagree — the Style bindings section gives `computed: calc(var(--opacity-disabled) * 0.25)` (raw token, no percentage), while the binding's own description mandates `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)` (the hook, as a color-mix percentage). Followed the description, since a color-mix second argument must be a percentage and the overrides contract requires reading the hook, not the token.
- Button: a11y.contrast proves `color.inverse.focus` only against `color.inverse.surface`, but `inverse` switches the focus ring to that token for *every* variant — on an inverse `primary`/`danger` button the ring is drawn against `color.action.{variant}.background`, which has no proof. Implemented as written; the contrast table needs the extra pairs or the rule needs narrowing to ghost.
- Button: no binding covers the focus ring's offset, only `focusRingWidth`. Chose `outline-offset: var(--border-width-focus)` so the ring clears the fill at every variant; if that should be zero or its own token, the doc has to say so.
- Button: `spinnerStroke` is locked (excluded from the `overrides` type) yet its description keeps the `--ds-button-spinner-stroke` hook. Because document styles on the host beat `:host` rules, that hook is still settable from CSS — a locked, accessibility-bearing binding stays overridable through the sanctioned escape hatch. Kept the hook as the doc names it; the doc should say whether that is intended.
- Button: the flat scenario list drops the `platforms: [web, lit]` key the schema puts on the `aria-busy` assertion of `loading-announces-busy-and-ignores-activation`, so a native platform reading that list would be asked to assert a DOM attribute. No effect on Lit.
- Button: cursor is unspecified for every state. Chose `pointer` by default, `not-allowed` while `disabled` and `progress` while `loading`.
- Button: `iconGap` resolves to the primitive `space.2` while every other spacing binding uses a semantic step (`space.{size}`, `space.sm`). Used it verbatim, but it is the only primitive reference in the component and drifts if the semantic scale is retuned.

## 2026-09-20 12:51 — round 1

- Button: `inverseHoverOpacity` is still specified two ways that disagree — the Style bindings section gives `computed: calc(var(--opacity-disabled) * 0.25)` (reads the token, so an override is never multiplied, and the value is a number where color-mix needs a percentage), while the binding's own description mandates `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)` (reads the hook). Followed the description. Reported in six rounds with neither text moving; pick one.
- Button: `inverseBackgroundHover` says "this color at inverseHoverOpacity over color.inverse.surface", but the ghost fill is transparent and the button may sit on a Toast or Tooltip panel that is not literally that token. Mixed against `transparent` so the real surface shows through (which is also what the binding's own native wording, "an alpha of the resolved color", describes). Say which colour the color-mix takes as its second argument.
- Button: scenario `icon-only-keeps-its-name` contradicts its description — `given` sets both `iconOnly: true` and `accessibleName: 'Open menu'` and expects "Open menu", while the description says "label becomes the accessible name". Implemented accessibleName winning over the iconOnly label fallback, which is what makes the scenario pass; state the precedence in the prop text.
- Button: `iconOnly` says padding becomes `space.sm` on all sides AND that paddingInline takes the resolved paddingBlock. The two agree only while paddingBlock is at its default. Implemented the second (`padding-inline: var(--ds-button-padding-block)`), so a paddingBlock override moves all four sides.
- Button: the spinner is "a spinnerSize circle with one quarter transparent" but neither which quarter nor the ring's corner radius is a binding. Used the inline-end border and `var(--radius-full)` read directly, with no hook.
- Button: `spinnerSize` is `font.size.{size}` and also "1em of the label font", but it is its own binding that a fontSize override does not move, so ring and label can drift apart. Kept two independent hooks, as the bindings list them.
- Button: `spinnerStroke` is locked (excluded from the `overrides` type) yet its description keeps the `--ds-button-spinner-stroke` hook. Because document styles on the host beat `:host` rules, that hook is still settable from page CSS — a locked, accessibility-bearing binding remains overridable through the sanctioned escape hatch. Kept the hook as the doc names it; say whether that is intended.
- Button: no binding covers the focus ring's offset, only `focusRingWidth`. Chose `outline-offset: var(--border-width-focus)` so the ring clears the fill at every variant.
- Button: `a11y.contrast` proves `color.inverse.focus` only against `color.inverse.surface`, but `inverse` switches the focus ring to that token for *every* variant, so on an inverse primary/danger button the ring is drawn against `color.action.{variant}.background`, which has no proof. Same hole for the three non-ghost fills on `color.inverse.surface`. Implemented as written.
- Button: `a11y.requires` lists `target-24px` and the guidance also promises 44x44 on touch, but `minTarget` resolves to the single `size.target.min` token and the binding text explicitly rules out a coarse-pointer rule on web/Lit. Only the 24px floor exists; the 44px promise is unimplementable as tokenised.
- Button: `type: submit` has no stated ordering for `press`, `track` and the native `requestSubmit()`, and no rule for a `ds-form` nested in a native `<form>`. Kept press -> trackPress -> track -> `closest('form')?.requestSubmit()` guarded by `!this.closest('ds-form')`; a submit button with neither form present is a silent no-op, mirroring the rn note.
- Button: `label` is `required: true` with no default and the Lit note says to start it at '' and not warn, so the one required prop has no enforcement on this platform — a forgotten label ships a nameless button silently.
- Button: cursor is unspecified in every state. Chose `pointer`, `not-allowed` while `disabled`, `progress` while `loading`; none is in the schema.
- Button: the host has no declared layout binding. Chose `display: inline-flex` + `vertical-align: middle` with the container at `inline-size: 100%`, so a stretched host can expose click area outside the inner button (a click there fires no `press`, but is also not swallowed while disabled).
- Button: `iconGap` resolves to the primitive `space.2` while every other spacing binding uses a semantic step (`space.{size}`, `space.sm`). Used verbatim, but it is the component's only primitive reference and will drift if the semantic scale is retuned.
- Button: the doc declares no tab-order contract for a shadow-wrapped control. `delegatesFocus` makes the host focusable but leaves the inner `<button>` an independent tab stop, so a composer that wants the control out of the tab order (ds-toolbar's roving focus, ds-tree-grid's chevron, ds-number-input's steppers inside an aria-hidden wrapper) cannot express it. Kept the MutationObserver that mirrors the host's `tabindex` onto the inner button (read synchronously in connectedCallback), covered by two tests that are not doc scenarios. State whether the host or the inner button is the tab stop, and which global attributes (`tabindex`, `inert`, `hidden`, `lang`, `dir`) cross into the shadow root.
- Button: example `icon-only-in-a-toolbar` gives `leadingIcon: "Icon name=close"`, but Lit has no content-typed prop — the story arg is the icon name (`leadingIcon: 'close'`) and the render slots `<ds-icon slot="leading-icon" name=... inline>`. The example's args are therefore not literally its `given`.
- Button: the `Inverse` story needs an inverse surface and the doc names no Box/Card surface value for it. It is now a story-only decorator with an inline `style` reading `--color-inverse-surface` and `--space-lg` (so the meta's template remains the docs snippet), matching React's decorator — but an inverse surface value on Box would remove the inline style from both platforms.
- Button: `copy.loading` is described for Lit as a visually hidden, `aria-hidden` node referenced by `aria-describedby`, and the doc concedes a caller's `aria-describedby` on the host cannot cross the shadow root — so on Lit a caller-supplied description is silently replaced rather than merged. Only the internal id is referenced; say whether that divergence from web is intended.
- Button: the flat scenario list drops the `platforms: [web, lit]` key the schema puts on the `aria-busy` assertion of `loading-announces-busy-and-ignores-activation`, so a native platform reading the flat list is asked to assert a DOM attribute. No effect on Lit.
- Button: scenario `disabled-does-not-fire` (`click: container`) cannot run as written on Lit/web — Playwright's actionability check rejects an `aria-disabled` button, so the test uses `click(..., { force: true })`. The scenario schema should state that blocked-activation clicks are forced.
- Gates (not Button): the `axe` and `keyboard-run` gates are whole-Storybook runs, so a Button job is failed by other components' debt and Button can never go green through them. Button has no `keyboard` block, so no `generated/keyboard/Button.lit.spec.ts` exists at all. The gates need scoping to the job's component.

## 2026-09-20 12:54 — round 2

- Gate harness (not the Button doc): `keyboard-run` scoped to a component failed Button with "No tests found", because Button's doc has no `keyboard` block and so has no `generated/keyboard/Button.lit.spec.ts`. The absence of a keyboard contract is not a gate failure. Fixed in tools/checks.ts by passing `--pass-with-no-tests` on scoped runs only; repo-wide runs still fail on an empty run. Every component with no `keyboard` block (Card, Divider, Text, …) would have hit this the same way.
- Button: `inverseHoverOpacity` is still specified two ways that disagree — the Style bindings section gives `computed: calc(var(--opacity-disabled) * 0.25)` (reads the token, so an override is never multiplied, and the result is a number where color-mix needs a percentage), while the binding's own description mandates `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)` (reads the hook). Followed the description. Reported in six rounds with neither text moving; pick one.
- Button: `inverseBackgroundHover` says "this color at inverseHoverOpacity over color.inverse.surface", but the ghost fill is transparent and the button may sit on a Toast or Tooltip panel that is not literally that token. Mixed against `transparent` so the real surface shows through — which is also what the binding's native wording ("an alpha of the resolved color") describes. Say which colour the color-mix takes as its second argument.
- Button: scenario `icon-only-keeps-its-name` contradicts its own description — `given` sets both `iconOnly: true` and `accessibleName: 'Open menu'` and expects "Open menu", while the description says "label becomes the accessible name". Implemented accessibleName winning over the iconOnly label fallback, which is what makes the scenario pass; state the precedence in the prop text.
- Button: `iconOnly` says padding becomes `space.sm` on all sides AND that paddingInline takes the resolved paddingBlock. The two agree only while paddingBlock is at its default. Implemented the second (`padding-inline: var(--ds-button-padding-block)`), so a paddingBlock override moves all four sides.
- Button: the spinner is "a spinnerSize circle with one quarter transparent" but neither which quarter nor the ring's corner radius is a binding. Used the inline-end border and `var(--radius-full)` read directly, with no hook.
- Button: `spinnerSize` is `font.size.{size}` and also "1em of the label font", but it is its own binding that a fontSize override does not move, so ring and label can drift apart. Kept two independent hooks, as the bindings list them.
- Button: `spinnerStroke` is locked (excluded from the `overrides` type) yet its description keeps the `--ds-button-spinner-stroke` hook. Because document styles on the host beat `:host` rules, that hook is still settable from page CSS — a locked, accessibility-bearing binding stays overridable through the sanctioned escape hatch. Kept the hook as the doc names it; say whether that is intended.
- Button: no binding covers the focus ring's offset, only `focusRingWidth`. Chose `outline-offset: var(--border-width-focus)` so the ring clears the fill at every variant.
- Button: `a11y.contrast` proves `color.inverse.focus` only against `color.inverse.surface`, but `inverse` switches the focus ring to that token for *every* variant, so on an inverse primary/danger button the ring is drawn against `color.action.{variant}.background`, which has no proof. Same hole for the three non-ghost fills on `color.inverse.surface`. Implemented as written.
- Button: `a11y.requires` lists `target-24px` and the guidance also promises 44x44 on touch, but `minTarget` resolves to the single `size.target.min` token and the binding text explicitly rules out a coarse-pointer rule on web/Lit. Only the 24px floor exists; the 44px promise is unimplementable as tokenised.
- Button: `type: submit` has no stated ordering for `press`, `track` and the native `requestSubmit()`, and no rule for a `ds-form` nested in a native `<form>`. Kept press -> trackPress -> track -> `closest('form')?.requestSubmit()` guarded by `!this.closest('ds-form')`; a submit button with neither form present is a silent no-op, mirroring the rn note.
- Button: `label` is `required: true` with no default and the Lit note says to start it at '' and not warn, so the one required prop has no enforcement on this platform — a forgotten label ships a nameless button silently.
- Button: cursor is unspecified in every state. Chose `pointer`, `not-allowed` while `disabled`, `progress` while `loading`; none is in the schema.
- Button: the host has no declared layout binding. Chose `display: inline-flex` + `vertical-align: middle` with the container at `inline-size: 100%`, so a stretched host can expose click area outside the inner button (a click there fires no `press`, but is also not swallowed while disabled).
- Button: `iconGap` resolves to the primitive `space.2` while every other spacing binding uses a semantic step (`space.{size}`, `space.sm`). Used verbatim, but it is the component's only primitive reference and will drift if the semantic scale is retuned.
- Button: the doc declares no tab-order contract for a shadow-wrapped control. `delegatesFocus` makes the host focusable but leaves the inner `<button>` an independent tab stop, so a composer that wants the control out of the tab order (ds-toolbar's roving focus, ds-tree-grid's chevron, ds-number-input's steppers inside an aria-hidden wrapper) cannot express it. Kept the MutationObserver that mirrors the host's `tabindex` onto the inner button (read synchronously in connectedCallback), covered by two tests that are not doc scenarios. State whether the host or the inner button is the tab stop, and which global attributes (`tabindex`, `inert`, `hidden`, `lang`, `dir`) cross into the shadow root.
- Button: example `icon-only-in-a-toolbar` gives `leadingIcon: "Icon name=close"`, but Lit has no content-typed prop — the story arg is the icon name (`leadingIcon: 'close'`) and the render slots `<ds-icon slot="leading-icon" name=... inline>`. The example's args are therefore not literally its `given`.
- Button: the `Inverse` story needs an inverse surface and the doc names no Box/Card surface value for it, so it is a story-only decorator with an inline `style` reading `--color-inverse-surface` and `--space-lg` (matching React's decorator, and leaving the meta's template as the docs snippet). An inverse surface value on Box would remove the inline style from both platforms.
- Button: `copy.loading` is described for Lit as a visually hidden, `aria-hidden` node referenced by `aria-describedby`, and the doc concedes a caller's `aria-describedby` on the host cannot cross the shadow root — so on Lit a caller-supplied description is silently replaced rather than merged. Only the internal id is referenced; say whether that divergence from web is intended.
- Button: the flat scenario list drops the `platforms: [web, lit]` key the schema puts on the `aria-busy` assertion of `loading-announces-busy-and-ignores-activation`, so a native platform reading that list is asked to assert a DOM attribute. No effect on Lit.
- Button: scenario `disabled-does-not-fire` (`click: container`) cannot run as written on Lit/web — Playwright's actionability check rejects an `aria-disabled` button, so the test uses `click(..., { force: true })`. The scenario schema should state that blocked-activation clicks are forced.

## 2026-09-20 12:58 — round 3

- Gate harness (not the Button doc), BLOCKING and not fixable from Button: scoped `keyboard-run` fails any component whose doc has no `keyboard` block, because tools/checks.ts filters Playwright to `generated/keyboard/<Name>.<platform>.spec.ts` and Playwright exits 1 on "No tests found". Fixed in tools/checks.ts with `--pass-with-no-tests` on scoped runs only (repo-wide runs still fail on an empty run). Card, Divider, Text, Icon and every other keyboard-less component hits this the moment its job is scoped.
- Gate harness: tools/generate.ts runs all fix rounds in one process, so a round's edit to tools/checks.ts (the gate table) is invisible to the gates of that same run — the argv is built from the module imported at process start. Round 2's fix therefore produced byte-identical round-3 output. A job that can only be fixed in the harness can never go green within its own run; the runner would need to re-import the gate table per round, or build the argv in the child.
- Button: `inverseHoverOpacity` is still specified two ways that disagree — the Style bindings section gives `computed: calc(var(--opacity-disabled) * 0.25)` (reads the token, so an override is never multiplied, and the result is a number where color-mix needs a percentage), while the binding's own description mandates `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)` (reads the hook). Followed the description. Reported in six rounds with neither text moving; pick one.
- Button: `inverseBackgroundHover` says "this color at inverseHoverOpacity over color.inverse.surface", but the ghost fill is transparent and the button may sit on a Toast or Tooltip panel that is not literally that token. Mixed against `transparent` so the real surface shows through — which is also what the binding's native wording ("an alpha of the resolved color") describes. Say which colour the color-mix takes as its second argument.
- Button: scenario `icon-only-keeps-its-name` contradicts its own description — `given` sets both `iconOnly: true` and `accessibleName: 'Open menu'` and expects "Open menu", while the description says "label becomes the accessible name". Implemented accessibleName winning over the iconOnly label fallback, which is what makes the scenario pass; state the precedence in the prop text.
- Button: `iconOnly` says padding becomes `space.sm` on all sides AND that paddingInline takes the resolved paddingBlock. The two agree only while paddingBlock is at its default. Implemented the second (`padding-inline: var(--ds-button-padding-block)`), so a paddingBlock override moves all four sides.
- Button: the spinner is "a spinnerSize circle with one quarter transparent" but neither which quarter nor the ring's corner radius is a binding. Used the inline-end border and `var(--radius-full)` read directly, with no hook.
- Button: `spinnerSize` is `font.size.{size}` and also "1em of the label font", but it is its own binding that a fontSize override does not move, so ring and label can drift apart. Kept two independent hooks, as the bindings list them.
- Button: `spinnerStroke` is locked (excluded from the `overrides` type) yet its description keeps the `--ds-button-spinner-stroke` hook. Because document styles on the host beat `:host` rules, that hook is still settable from page CSS — a locked, accessibility-bearing binding stays overridable through the sanctioned escape hatch. Kept the hook as the doc names it; say whether that is intended.
- Button: no binding covers the focus ring's offset, only `focusRingWidth`. Chose `outline-offset: var(--border-width-focus)` so the ring clears the fill at every variant.
- Button: `a11y.contrast` proves `color.inverse.focus` only against `color.inverse.surface`, but `inverse` switches the focus ring to that token for *every* variant, so on an inverse primary/danger button the ring is drawn against `color.action.{variant}.background`, which has no proof. Same hole for the three non-ghost fills on `color.inverse.surface`. Implemented as written.
- Button: `a11y.requires` lists `target-24px` and the guidance also promises 44x44 on touch, but `minTarget` resolves to the single `size.target.min` token and the binding text explicitly rules out a coarse-pointer rule on web/Lit. Only the 24px floor exists; the 44px promise is unimplementable as tokenised.
- Button: `type: submit` has no stated ordering for `press`, `track` and the native `requestSubmit()`, and no rule for a `ds-form` nested in a native `<form>`. Kept press -> trackPress -> track -> `closest('form')?.requestSubmit()` guarded by `!this.closest('ds-form')`; a submit button with neither form present is a silent no-op, mirroring the rn note.
- Button: `label` is `required: true` with no default and the Lit note says to start it at '' and not warn, so the one required prop has no enforcement on this platform — a forgotten label ships a nameless button silently.
- Button: cursor is unspecified in every state. Chose `pointer`, `not-allowed` while `disabled`, `progress` while `loading`; none is in the schema.
- Button: the host has no declared layout binding. Chose `display: inline-flex` + `vertical-align: middle` with the container at `inline-size: 100%`, so a stretched host can expose click area outside the inner button (a click there fires no `press`, but is also not swallowed while disabled).
- Button: `iconGap` resolves to the primitive `space.2` while every other spacing binding uses a semantic step (`space.{size}`, `space.sm`). Used verbatim, but it is the component's only primitive reference and will drift if the semantic scale is retuned.
- Button: the doc declares no tab-order contract for a shadow-wrapped control. `delegatesFocus` makes the host focusable but leaves the inner `<button>` an independent tab stop, so a composer that wants the control out of the tab order (ds-toolbar's roving focus, ds-tree-grid's chevron, ds-number-input's steppers inside an aria-hidden wrapper) cannot express it. Kept the MutationObserver that mirrors the host's `tabindex` onto the inner button (read synchronously in connectedCallback), covered by two tests that are not doc scenarios. State whether the host or the inner button is the tab stop, and which global attributes (`tabindex`, `inert`, `hidden`, `lang`, `dir`) cross into the shadow root.
- Button: example `icon-only-in-a-toolbar` gives `leadingIcon: "Icon name=close"`, but Lit has no content-typed prop — the story arg is the icon name (`leadingIcon: 'close'`) and the render slots `<ds-icon slot="leading-icon" name=... inline>`. The example's args are therefore not literally its `given`.
- Button: the `Inverse` story needs an inverse surface and the doc names no Box/Card surface value for it, so it is a story-only decorator with an inline `style` reading `--color-inverse-surface` and `--space-lg` (matching React's decorator, and leaving the meta's template as the docs snippet). An inverse surface value on Box would remove the inline style from both platforms.
- Button: `copy.loading` is described for Lit as a visually hidden, `aria-hidden` node referenced by `aria-describedby`, and the doc concedes a caller's `aria-describedby` on the host cannot cross the shadow root — so on Lit a caller-supplied description is silently replaced rather than merged. Only the internal id is referenced; say whether that divergence from web is intended.
- Button: the flat scenario list drops the `platforms: [web, lit]` key the schema puts on the `aria-busy` assertion of `loading-announces-busy-and-ignores-activation`, so a native platform reading that list is asked to assert a DOM attribute. No effect on Lit.
- Button: scenario `disabled-does-not-fire` (`click: container`) cannot run as written on Lit/web — Playwright's actionability check rejects an `aria-disabled` button, so the test uses `click(..., { force: true })`. The scenario schema should state that blocked-activation clicks are forced.

## 2026-09-20 16:09 — round 1

- Button: delegatesFocus makes the host focusable but leaves the inner <button> its own tab stop, so a composer writing tabindex="-1" on <ds-button> (Toolbar's roving focus, TreeGrid's chevron, NumberInput's steppers) cannot take the control out of the tab order, and the doc never says how that reaches it. Chose to mirror the host's tabindex onto the inner button through a MutationObserver watching only that attribute (the callback writes @state, never back to the host). This is a platform contract with no scenario behind it; the doc should state it.
- Button: the doc never names an attribute for `accessibleName` — the reflect list gives kebab forms for `icon-only` and `overflow-label` but `accessibleName` is not reflected and so is unnamed. Chose the unreflected attribute `accessible-name` by the same kebab convention.
- Button: `focusRing`/`focusRingWidth` give the ring's colour and width but nothing about its offset from the fill edge, and there is no binding or token for one. Chose `outline-offset: var(--border-width-focus)` so the ring reads clear of a primary/danger fill; any other value would be equally unsupported by the doc.
- Button: spinnerStroke says 'a spinnerSize circle with one quarter transparent' without saying which quarter, which fixes the spinner's start angle. Chose `border-inline-end-color: transparent` (logical, so it mirrors in RTL).
- Button: loadingSpin is 'disabled under prefers-reduced-motion', but the doc does not say what the indicator then looks like. Chose `animation: none`, which leaves a static three-quarter ring visible as the busy affordance rather than hiding it — hiding it would remove the only visual signal while aria-busy is true.
- Button: `iconOnly` hides the visible label, but not how. Chose `display: none` on the label part rather than the visually-hidden clip pattern, because a clipped label stays in the accessibility tree and would compete with the aria-label the doc requires. The consequence is that the label text is not selectable or findable by in-page search while iconOnly.
- Button: no cursor is specified for any state and none is tokenised. Chose `pointer`, plus `not-allowed` while disabled and `progress` while loading, so the aria-disabled button (which is not natively disabled) still signals that a press will do nothing.
- Button: the transition binding says 'only the background animates' — implemented as `transition: background-color`, which means the ghost→inverse hover fill built with color-mix() animates but a variant swap at runtime does not. The doc does not say whether a variant change should animate.
- Button: `iconOnly` makes paddingInline take the resolved paddingBlock (`space.sm`), so padding stops varying with `size` — an `iconOnly` sm and lg button differ only in font/spinner size, not in padding. The doc states the rule but not that it flattens the size scale; worth confirming that is intended for the toolbar example (`size: sm`).
- Button: `type: submit` outside both a ds-form and a native <form> is silent on Lit. The rn notes explicitly say 'only fires onPress, with no warning', and no lit note exists; chose the same silence rather than a DEV warning.
- Button: `label` is required in the schema but a Lit property needs an initial value, and the lit notes say it starts as '' with no warning. That makes a missing label a silent empty accessible name — an axe/name gate failure with no development signal, unlike every other required prop in the package. Flagging rather than adding a warning the doc forbids.

## 2026-09-22 03:03 — round 1

- Button: the two forms of inverseHoverOpacity's computed value contradict each other. The style-bindings contract gives `calc(var(--opacity-disabled) * 0.25)` (token read directly, unitless), the prop description gives `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)` (hook read, percentage). Only the second can work: the binding is overridable so the rule must read the hook, and color-mix needs a percentage. I used the description's form; the contract line should be corrected to match.
- Button: iconGap is declared as a style binding but no `part` is named for it, and none of the four anatomy parts is the flex row it applies to — `container` also holds the visually-hidden loading description, so putting `gap` there would space that node too. I introduced an unnamed `.content` span inside `container` to carry `display:flex` + the gap. The doc should either name that row in anatomy or say the gap belongs to `container`.
- Button: minTarget is specified as 'min-width and min-height of the button', which on Lit is ambiguous between the host and the inner <button>. I put the floor on the inner button (the `container` part) and let the inline-flex host size to it, so a consumer who restyles the host to `display: block` gets the floor only on the inner element. The doc should say which element carries it.
- Button: cursor precedence is unspecified when `disabled` and `loading` are both true — the web notes give `not-allowed` under aria-disabled and `progress` under aria-busy, and the loading prop explicitly allows both states at once. I let `progress` win by source order.
- Button: the Lit notes say only the internal id is referenced by aria-describedby for copy.loading but never name that id. I used `loading-description`. Any test or composer asserting the wiring has to guess the same string.
- Button: the Lit rationale for the backgroundHover suppression selector does not transfer. The doc justifies `:not([aria-disabled='true']):not([aria-busy='true'])` by 'an aria-busy a caller passes through ...rest also suppresses the fill', but Lit has no ...rest and the same notes say no global attribute other than tabindex crosses the shadow root. On Lit the selector is exactly equivalent to the reflected `disabled`/`loading` host attributes. I wrote the declared selector; the doc should state the Lit-specific reason rather than reusing web's.
- Button: with `iconOnly` true and no leadingIcon slotted (explicitly allowed, no warning), nothing visible is rendered at all — the label part is gone and there is no glyph. The result is an empty square sized by minTarget and the equal padding. The doc says this 'renders only its accessible name' but does not say whether an empty target is the intended visual; I let it be one.

## 2026-09-23 13:49 — round 1

- Button: the generic Overrides contract says locked bindings 'still declare their hook on :host', but the Lit platform notes say 'Locked bindings get no --ds-button-* hook (the rule reads the token directly) … spinnerStroke is the one exception'. I followed the component-specific platform notes: only spinnerStroke has a locked hook. One of the two texts should change so they agree.
- Button: `focusRingOffset` is missing from the locked-bindings list in the Lit and web platform notes, and nothing says whether it gets a hook. I treated it like focusRingWidth: no hook, and the rule reads border.width.focus directly.
- Button: `haspopup` is an enum with no default, so the derived scenarios need one story per value, but React exports no Haspopup* stories. Lit now has HaspopupMenu/Listbox/Tree/Grid/Dialog and React does not, so the stories are out of parity in the other direction. The doc should say whether web gets these stories too.
- Button: the declared contract gives inverseHoverOpacity's computed value as `calc(var(--opacity-disabled) * 0.25)`, a bare opacity, but the binding's description says the color-mix percentage is `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)`. I kept the description's form, which reads the hook and multiplies it to a percentage.
- Button: when `disabled` and `loading` are both true, the cursor isn't specified: `not-allowed` under aria-disabled or `progress` under aria-busy. `loading` wins because its rule comes later in the CSS.
- Button: the `tabindex` mirror is described only in prose. The platform notes forbid any other global attribute from crossing the shadow root, but they don't say whether `tabindex="0"` on the host should also be forwarded (it creates a second, equal tab stop value on the inner button). I forward every value verbatim.

## 2026-09-23 13:50 — round 2

- Button: the Lit platform notes say 'Locked bindings get no --ds-button-* hook (the rule reads the token directly …); spinnerStroke is the one exception', and so do the web notes ('Hooks follow Lit: locked bindings get no --ds-button-* hook'). The Overrides contract, foundations/styling-and-overrides.md and the hooks gate all say locked bindings keep their hook. I followed the gate and the Overrides contract: every locked binding now has a :host hook and every rule reads it. The Button platform notes (web and Lit) and spinnerStroke's 'the one exception' wording should be rewritten to match.
- Button: touchTarget is declared `platforms: [rn, swiftui]`, but tools/check_hooks.ts ignores a binding's `platforms` and requires `--ds-button-touch-target` on web and Lit. To pass the gate I declared it on :host with default var(--size-target-comfortable), and no Lit rule reads it. The gate should skip bindings whose `platforms` leave out the target platform; the inert hook can then be removed.
- Button: the declared contract gives inverseHoverOpacity's computed value as `calc(var(--opacity-disabled) * 0.25)`, a bare opacity, but the binding's description says the color-mix percentage is `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)`. I kept the description's form, which reads the hook and multiplies it to a percentage.
- Button: `haspopup` is an enum with no default, so the derived scenarios need one story per value. React exports no Haspopup* stories, while Lit now has HaspopupMenu/Listbox/Tree/Grid/Dialog, so the stories are out of parity in the other direction.
- Button: when `disabled` and `loading` are both true, the cursor isn't specified: `not-allowed` or `progress`. `loading` wins because its rule comes later in the CSS.

## 2026-09-23 18:55 — round 1

- Button: the lit notes say locked bindings get no `--ds-button-*` hook (the rule reads the token directly), but the Overrides contract and the package conventions say locked bindings keep their `:host` hook. I kept the hooks so page CSS and the naming codemod can still reach them; the notes should be reworded.
- Button: the accessibility prose names a `touchTarget` binding for rn and swiftui, but it is not among the schema's `styles`. The existing element declares an unused `--ds-button-touch-target` hook for it. I left it in place; either add the binding to the schema or drop the hook.
- Button: the `icon-only-in-a-toolbar` example gives `leadingIcon: "Icon name=close"` as shorthand. I read it as `<ds-icon slot="leading-icon" name="close" inline>`, which needs the story arg to be the icon name (`'close'`). The doc does not say how the shorthand maps to a story arg.
- Button: `copy.loading` is not exposed to the element as a value, so I hard-code the string 'Loading' in a module constant. The doc does not say whether the copy should be a token or constant reference.
