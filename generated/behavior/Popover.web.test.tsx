// Generated by tools/behavior_tests.ts from the `behavior` block of the Popover doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Popover } from '../../packages/react/src/Popover';
import type { PopoverProps } from '../../packages/react/src/Popover';
import meta from '../../packages/react/src/Popover.stories';

function setup(given: Partial<PopoverProps> = {}) {
  const events = {
    onOpenChange: vi.fn(),
  };
  const props = { ...meta.args, ...given, onOpenChange: events.onOpenChange };
  const utils = render(<Popover {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Popover"]') ?? screen.queryByRole('dialog') ?? utils.container.firstElementChild) as HTMLElement,
    trigger: () => (screen.queryByRole('dialog') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<PopoverProps>) => utils.rerender(<Popover {...props} {...next} />),
  };
  return s;
}

describe('Popover', () => {
  test('renders', async () => {
    const s = setup({"open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-heading-level-2', async () => {
    const s = setup({"headingLevel": "2", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-heading-level-3', async () => {
    const s = setup({"headingLevel": "3", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-heading-level-4', async () => {
    const s = setup({"headingLevel": "4", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-bottom-start', async () => {
    const s = setup({"placement": "bottom-start", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-bottom', async () => {
    const s = setup({"placement": "bottom", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-bottom-end', async () => {
    const s = setup({"placement": "bottom-end", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-top-start', async () => {
    const s = setup({"placement": "top-start", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-top', async () => {
    const s = setup({"placement": "top", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-top-end', async () => {
    const s = setup({"placement": "top-end", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-start', async () => {
    const s = setup({"placement": "start", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-end', async () => {
    const s = setup({"placement": "end", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({"open": true});
    expect(screen.getByRole('dialog')).toHaveAccessibleName();
  });
});
