// Generated by tools/behavior_tests.ts from the `behavior` block of the Form doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Form } from '../../packages/rn/src/Form';
import type { FormProps } from '../../packages/rn/src/Form';
import meta from '../../packages/rn/src/Form.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<FormProps> = {}) {
  const events = {
    onSubmit: jest.fn(),
    onInvalid: jest.fn(),
  };
  const props: FormProps = { ...(meta.args as FormProps), ...given, onSubmit: events.onSubmit, onInvalid: events.onInvalid };
  const tree = (p: FormProps) => (
    <ThemeProvider mode="light">
      <Form {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Form') ?? screen.UNSAFE_root,
    container: () => screen.queryByRole('form') ?? s.root(),
    rerender: (next: Partial<FormProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Form', () => {
  test('label-names-the-form-landmark', () => {
    const s = setup({"label": "Sign in"});
    expect(s.container()).toHaveProp("accessibilityLabel", "Sign in");
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-validate-submit', () => {
    const s = setup({"validate": "submit"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-validate-blur', () => {
    const s = setup({"validate": "blur"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-validate-change', () => {
    const s = setup({"validate": "change"});
    expect(s.root()).toBeTruthy();
  });
});
