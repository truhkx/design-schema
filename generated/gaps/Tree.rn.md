# Gaps reported while generating Tree for rn

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 14:28 — round 1

- No RN element expresses a11y.role 'tree'/'treeitem'/'group'; per the platform notes I used FlatList with accessibilityRole='list' and accessibilityLabel={label}, with per-row accessibilityState/accessibilityActions carrying expanded/selected/checked state instead.
- Anatomy's 'group' (nested container per open parent) has no separate wrapper: visible children are flattened into the single virtualized FlatList (matching the existing TreeGrid precedent) rather than rendered as nested per-parent group views.
- Arrow-key navigation, roving tabindex, Home/End, type-ahead, Shift+Arrow and Ctrl+A are web keyboard-model behaviors with no hardware-keyboard equivalent on most RN devices; only Enter-equivalent (tap to activate) and the always-visible expand chevron are implemented, with expand/collapse also exposed via accessibilityActions for assistive technology, per the 'implement the reachable subset' rule.
- In `multiple` mode the row has no outer Pressable — the composed Checkbox's own control and label are the entire hit target — so onActivate/href navigation is not reachable there (no native gesture distinguishes 'move' from 'select' the way Enter/Space do on web); activation only fires in `none`/`single` mode. Documented rather than adding a second overlapping touch target.
- Checkbox owns its own visible/accessible label, so in `multiple` mode the row's accessible name is the plain node label (no ', level N' suffix); the '{label}, level {n}' format from the platform notes is only applied in `none`/`single` mode where the row itself is a Pressable exposing accessibilityLabel.
- `selectOnFocus` (documented for `single` mode) is wired to the row Pressable's onFocus event, which fires for external-keyboard tab focus and assistive-technology (VoiceOver/TalkBack) navigation on RN/react-native-web — there is no separate 'move' input on touch, so this is the closest native analogue to the web behavior.
- href navigation combined with `selectable: 'multiple'` has no dedicated tap target (the row's only interactive area is the Checkbox, which toggles selection); href is only actionable in `none`/`single` mode via Linking.openURL.
- 'live-region' is implemented as the docs' explicit accessibility note describes: only `copy.selectedCount` announcements on selection change in `multiple` mode (AccessibilityInfo on iOS, a hidden polite-live-region Text on Android) — expand/collapse changes are conveyed via each row's own accessibilityState.expanded rather than a separate announcement, matching TreeGrid's existing precedent in this package.

## 2026-09-10 20:03 — round 1

- Tree: the file already existed but was missing the `headingLevel`/`headingSize` prop and binding entirely (Heading was hardcoded to level="2" with no size override); added `headingLevel` (TreeHeadingLevel = '2'|'3'|'4'|2|3|4, default '2') and forward `headingSize` (default 'font.size.md') as the Heading's `overrides.fontSize`, per the schema's 'its size is headingSize regardless' note.
- Tree: `checkboxSize`, `checkboxBackground` and `checkboxRadius` were missing from `TreeOverridableBinding` even though the schema lists them as overridable; added them.
- Tree: the existing multiple-select row composed the `Checkbox` component directly, which contradicts the RN platform note ('not the Checkbox component ... a tap toggles selection, a long press activates') and made `onActivate`/`href` unreachable in multiple mode. Rewrote it to draw the checkbox glyph by hand (View + check/dash Icon using the checkbox* bindings) inside the row's own Pressable, with onPress toggling selection and onLongPress activating.
- Tree (RN, a11y gap the schema doesn't resolve): RN's assistive-technology 'activate' gesture is routed to a Pressable's `onPress`, not `onLongPress`. In multiple mode that means onPress now toggles selection as specified, but screen-reader users have no non-literal-long-press path to `onActivate`/`href` — the schema names only `expand`/`collapse` accessibilityActions, none for activation. Implemented per the literal spec; flagging since it may need an accessibilityAction alternative for activation in multiple mode.
- Tree: `rowGap` ('between the expand button, icon, label and badge') and `checkboxGap` ('between the checkbox and the label') default to the same token (layout.gap.tight), so their distinction was invisible in the prior flex-gap-based layout. Replaced the row's single flex `gap` with explicit per-boundary margins so overriding one independently of the other now has a visible effect.
- Tree: `accessibilityState.selected` was previously set in both single and multiple mode; narrowed to single mode only (multiple mode now exposes `checked`/`'mixed'` instead), matching the web platform's aria-selected vs aria-checked split.

## 2026-09-16 12:31 — round 1

- Tree: the rn notes ask for an `activate` accessibility action beside expand/collapse, but `activate` is a standard RN action name that takes over the screen reader's double-tap (it would stop the tap from toggling selection), and copy has no label for a custom action. Chose the standard `longpress` action (no label needed) in `multiple` mode, which calls the same handler as a long press, i.e. activation.
- Tree: what a tap does in `single`/`none` is not stated for rn (the web pointer model is click = Space, double-click = Enter; rn only says tap toggles and long press activates in `multiple`). Chose tap = Enter: activate (onActivate or href) and, in `single`, also select. In `none` a tap only activates; it does not toggle expansion.
- Tree: 'follows href' has no navigation target on native, and Link skips its own `Linking` call when given an `onPress` handler. The row opens `href` with `Linking.openURL` itself and passes Link an `onPress` that returns `false`, so a tap on the link text still selects and toggles like the rest of the row. There is no router hook for in-app hrefs like `/settings/account`.
- Tree: `fontFamily`, `fontSize` and `lineHeight` are overridable but have no part, and the parts contract lists only `fontWeight` as the label Text's forward ('add no other'). Chose to forward them to the label Text's overrides only when the caller sets them; defaults come from `Text size="sm"`.
- Tree: the `link` part cannot take `fontSize` (Link exposes only `transition`), and standalone Link uses font.size.md. Chose to nest the Link in a label `Text size="sm"` so it inherits the tree's size; the selected weight is not applied to it, as `labelSelectedWeight` describes.
- Tree: `checkboxBorder` (color.control.border) has no width binding. Chose border.width.thin, and the border takes checkboxSelected's color when checked or mixed so the fill has no contrasting edge.
- Tree: `rowSelectedBorder` is a start-edge bar, but whether it adds to layout width is unspecified. Drew it as an absolutely positioned View at the row's start so selecting does not shift the content.
- Tree: `rowHover` on touch. There is no hover on native, so the row uses it for both pressed and react-native-web hover; the selected fill wins over it. `transition` only animates the chevron: hover swaps instantly (the theme prefers no animation).
- Tree: `onExpand` says 'first time', but TreeGrid fires on every expand while children are still lazy so a failed load can retry. Followed Tree's spec: once per id per mount, tracked in a ref.
- Tree: the chevron color is unbound (`iconColor` is the node glyph's part). Chose color.foreground to match TreeGrid's chevron; the Button is left accessible, named with copy.expand/collapse, because rn calls it 'a real target' while web hides it.
- Tree: with `selectChildren`, whether disabled or lazy descendants cascade is unspecified. Chose loaded, enabled descendants only. A parent's own id counts toward mixed only when some but not all descendants are selected.
- Tree: the `Keyboard` story needs 'its trigger' but a Tree has none. Chose showLabel plus `docs` expanded, which gives expand buttons and several focusable rows.
- Tree: `*` (open all siblings) is listed for rn in the keyboard table but the notes say no arrow keys or type-ahead, and there is no touch equivalent or copy for an 'expand all' accessibility action (TreeGrid invents one). Not implemented on rn.
- Tree: the `container` and `group` parts have no rn element (groups are flattened into the list). The root View carries `testID="Tree"` and the FlatList has no anatomy testID; the has-accessible-name test finds it by `accessibilityLabel`.
- Tree: the `typeaheadReset` constant is unused on rn, since there is no type-ahead.

## 2026-09-17 15:32 — round 1

- Tree: the Keyboard section excludes only 7 rules from rn, leaving Tab, Home, End, Enter, Space and Control+a as applicable — but React Native gives non-text views no key events, and the rn platform notes themselves say the row is a Pressable driven by tap/long-press. I implemented tap = Enter and nothing for Home/End/Space/Control+a. Those five rules should carry platforms [web, lit, swiftui] like the arrow rules, or the notes should say the rn subset is tap only.
- Tree: a11y.requires lists roving-tabindex and the Tab rule promises one tab stop, which is unreachable on this platform — on react-native-web every row Pressable and every chevron Button is its own tab stop, and RN has no tabIndex. Chose to leave focus to the platform order; the doc should scope roving-tabindex to web/lit/swiftui.
- Tree: defaultExpanded says a lazy id listed explicitly stays closed until the user opens it, but says nothing about the controlled `expanded` prop. I applied the same suppression to `expanded` (as TreeGrid does), so a controlled caller cannot force a still-lazy node open without a user act — state which prop the rule covers.
- Tree: the rn notes fix accessibilityLabel as "{label}, level {n}", which replaces the row's rendered content for a screen reader, so the badge (a count the sighted user sees) and the lazy placeholder's copy.loading inside the row are never announced. I followed the note verbatim; the doc should say whether the badge joins the row label.
- Tree: the guidance puts copy.selectedCount in a visually hidden role="status" region. RN has no role="status": I used an Android accessibilityLiveRegion="polite" View clipped to one point plus AccessibilityInfo.announceForAccessibility on iOS, announced on every fired selection change (so not when the set is unchanged). Worth stating as the rn form of live-region.
- Tree: selectChildren says the cascade covers "loaded, enabled descendants" but not whether the children of a *disabled* node count. I include them (a disabled node is skipped itself, its enabled children still cascade) — the opposite reading (a disabled node walls off its subtree) is equally available from the text.
- Tree: nothing says what a cascading parent's own checkbox shows when it has no loaded enabled descendants (children: "lazy", or only disabled children). I fall back to its own id, so such a parent behaves as a leaf checkbox rather than staying unchecked/mixed.
- Tree: rowSelected/rowSelectedBorder are specified for selected and aria-checked="true" nodes, leaving the indeterminate parent undefined. I give a `mixed` parent neither the fill nor the start-edge bar — only its checkbox glyph shows the dash.
- Tree: focusRing/focusRingWidth carry no `part`, and on rn the row is split between the tree-owned expandButton wrapper and the nodeRow Pressable. I draw the ring around the whole `node` (chevron included) rather than around nodeRow alone, since the row is one visual target.
- Tree: the guide lines have no geometry on native (`group` has no element). I draw one absolutely positioned line per ancestor level, centred on that ancestor's expandButton (rowPaddingInline + n*indent + (expandButtonSize - guideLineWidth)/2) — the offsets are my invention and would drift from the web ::before if either changes.
- Tree: href on rn is followed with Linking.openURL, but the navigation-sidebar example's hrefs are app-relative ("/settings/account"), which Linking cannot open on a device; the promise rejects and I swallow it. The example should use absolute URLs, or the doc should say relative hrefs are a web-only affordance and onActivate is the native path.
- Tree: the empty state is specified as a Text below the list inside the container (web). On rn I used FlatList's ListEmptyComponent, so copy.empty renders inside the list that carries accessibilityRole="list" and accessibilityLabel rather than as a sibling of it.
- Tree: the constant typeaheadReset (500 ms) is unreachable on rn — there is no type-ahead on this platform, so no code reads it. It should be marked web/lit/swiftui like the a-z rule.

## 2026-09-21 22:25 — round 1

- Tree: the `fontFamily`/`fontSize`/`lineHeight` bindings are described as setting 'the container's font', but RN has no cascade and a root View carries no typography — only the composed label Text receives them. The loading placeholder and emptyState Text keep their own size/tone defaults, which the spec never assigns.
- Tree: the `link` part had no rn structure. Mirrored web by nesting a `View testID="Tree.link"` inside the label `Text`; an inline View inside Text is supported but fragile on RN, and a flatter shape (link as a sibling, losing the label's font) was the alternative.
- Tree: `rowHover` declares `state: hover`, which native RN does not have. Bound to both `onHoverIn/Out` (react-native-web) and `onPressIn/Out` (the native pressed fill) per the binding's own prose; it is never animated, since that prose says 'swapped instantly' while `transition` claims to cover hover.
- Tree: `copy.selectedCount` is contradictory — 'the region exists from mount and always holds the current count' vs 'written on every user selection change'. Chose derived-from-`selected`, so a controlled prop change also updates the region; iOS `announceForAccessibility` still fires only on user acts.
- Tree: `defaultExpanded: ["*"]` reserves `"*"` as a sentinel 'so a node whose id is literally "*" is never matched by it', but the sentinel expands every loaded parent — a parent whose id is literally `"*"` therefore still opens. Left it opening; excluding it would need the doc to say so.
- Tree: the lazy placeholder's accessible name is specified as just `copy.loading`, with no level segment — unlike TreeGrid, whose placeholder announces 'Loading, level n'. Followed the Tree doc literally (`accessible` + `accessibilityLabel={COPY.loading}`); the inconsistency between the two docs is probably unintended.
- Tree: `checkboxGap` is defined only for `multiple` mode ('the gap after the checkbox'). The doc does not say what that slot's gap is in `single`/`none`, where no checkbox renders; the gap sits on the Pressable and has no effect there.
- Tree: `focusRing` is drawn around the whole `node`, but the doc does not say whether focusing the chevron Button (its own tab stop on react-native-web) should light the node's ring. It does not — only the row Pressable's focus does, so a keyboard user tabbing to the chevron sees only the Button's own focus treatment.
- Tree: behavior scenario `clicking-a-node-selects-it` (selectable single) asserts only `onSelectionChange`, but the rn platform notes require a tap to also activate — so the same tap fires `onActivate` too. The scenario does not mention it; the notes win.
- Tree: `onExpand` 'fires each time a node whose `children` is still "lazy" is opened' — read as firing again after a collapse and reopen, with the caller replacing `children` as the only stop condition. A reading where it fires once per mount is equally available from the sentence.
- Tree: `headingLevel` has no meaning on rn (no heading levels); it reaches the composed Heading for typography only, and `headingSize` overrides the size regardless — so all three enum values render identically apart from whatever Heading itself does with `level`.

## 2026-09-21 22:34 — round 2

- Tree: the rn notes give the FlatList `accessibilityRole="list"` but never say what the rows are. On react-native-web that is a real <ul>, and ARIA lets a list own only listitems, so every node row — plus the lazy placeholder and the empty state — needs `role="listitem"`, a role neither the anatomy nor the platform notes mention. Followed Table's precedent; the doc should name it, since `group` explicitly 'has no element' but the item role is left unsaid.
- Tree: `disabled` nodes are a direct conflict between two things the doc requires — `opacity.disabled` over the whole row, and the `contrast-aa` pair color.foreground on color.background. The composite is 2.98:1 and can never reach 4.5:1. WCAG 1.4.3 exempts inactive components, but react-native-web does not emit `aria-disabled` from `accessibilityState`, so it is set on the DOM node directly (Button/Listbox precedent). The doc should record that disabled nodes are exempt from the listed contrast pairs.
- Tree: the doc demotes the composed Link to `tabindex=-1` for web and lit only, and for rn says 'every row Pressable and chevron Button is its own tab stop' — silent about the anchor inside an `href` label, which react-native-web renders focusable. It is a second tab stop inside the row and a 20px target that fails axe target-size. Demoted it on the DOM to match web; the rn notes should say so explicitly.
- Tree: `size.target.min` is 24px while the label's line box (`font.lineHeight.normal` × `font.size.sm`) is 20px, so any inline interactive text inside a row is under the 24px floor by construction. The `target-24px` requirement is satisfiable only by the row itself, never by the composed Link — worth stating, since it forces the demotion above rather than a sizing fix.
- Tree: the `fontFamily`/`fontSize`/`lineHeight` bindings are described as setting 'the container's font', but RN has no cascade and a root View carries no typography — only the composed label Text receives them. The loading placeholder and emptyState Text keep their own size/tone defaults, which the spec never assigns.
- Tree: the `link` part had no rn structure. Mirrored web by nesting a `View testID="Tree.link"` inside the label `Text`; an inline View inside Text is supported but fragile on RN, and a flatter shape (link as a sibling, losing the label's font) was the alternative.
- Tree: `rowHover` declares `state: hover`, which native RN does not have. Bound to both `onHoverIn/Out` (react-native-web) and `onPressIn/Out` (the native pressed fill) per the binding's own prose; it is never animated, since that prose says 'swapped instantly' while `transition` claims to cover hover.
- Tree: `copy.selectedCount` is contradictory — 'the region exists from mount and always holds the current count' vs 'written on every user selection change'. Chose derived-from-`selected`, so a controlled prop change also updates the region; iOS `announceForAccessibility` still fires only on user acts.
- Tree: `defaultExpanded: ["*"]` reserves `"*"` as a sentinel 'so a node whose id is literally "*" is never matched by it', but the sentinel expands every loaded parent — a parent whose id is literally `"*"` therefore still opens. Left it opening; excluding it would need the doc to say so.
- Tree: the lazy placeholder's accessible name is specified as just `copy.loading`, with no level — unlike TreeGrid, whose placeholder announces 'Loading, level n'. Followed the Tree doc literally; the inconsistency between the two docs is probably unintended.
- Tree: `checkboxGap` is defined only for `multiple` mode ('the gap after the checkbox'). The doc does not say what that slot's gap is in `single`/`none`, where no checkbox renders; the gap sits on the Pressable and has no effect there.
- Tree: `focusRing` is drawn around the whole `node`, but the doc does not say whether focusing the chevron Button (its own tab stop on react-native-web) should light the node's ring. It does not — only the row Pressable's focus does.
- Tree: behavior scenario `clicking-a-node-selects-it` (selectable single) asserts only `onSelectionChange`, but the rn platform notes require a tap to also activate, so the same tap fires `onActivate` too. The scenario does not mention it; the notes win.
- Tree: `onExpand` 'fires each time a node whose `children` is still "lazy" is opened' — read as firing again after a collapse and reopen, with the caller replacing `children` as the only stop condition. A once-per-mount reading is equally available from the sentence.
- Tree: `headingLevel` has no meaning on rn (no heading levels); it reaches the composed Heading for typography only, and `headingSize` overrides the size regardless — so all three enum values render identically apart from whatever Heading itself does with `level`.

## 2026-09-23 15:01 — round 1

- Tree: platforms.rn gives each row accessibilityRole="button" (or "link") carrying accessibilityState.selected/checked, but the digest says to mirror them as aria-selected/aria-checked. ARIA allows neither on role=button or role=link, so axe's aria-allowed-attr would fail. Chose to mirror only aria-label/aria-expanded/aria-busy (as Button leaves out aria-valuetext) and leave selected/checked in accessibilityState, so react-native-web's DOM shows no selection state.
- Tree: an id that is literally "*" inside a controlled `expanded` cannot be told apart from the sentinel. The spec only defines the sentinel for defaultExpanded ('the same rule covers the controlled expanded' refers to held-lazy ids). Chose TreeGrid's approach: resolve "*" at render in both modes.
- Tree: 'When a controlled expanded change closes an ancestor of the focused node, focus moves up to that ancestor' has no rn form in platforms.rn (no roving focus). Not implemented; AccessibilityInfo.setAccessibilityFocus would be the candidate.
- Tree: selectOnFocus is 'wired to the Pressable's onFocus', but the global rule says Tab entry and pointer focus never select. On react-native-web, tabbing onto a row fires onFocus and selects it. Followed the rn note.
- Tree: the navigation-sidebar example uses app-relative hrefs (/settings/account), which the rn notes say Linking.openURL cannot open. The story renders them as given, the rejected promise is swallowed, and no __DEV__ warning is asked for.
- Tree: this run was stopped midway because another session (ds-regen-rn-19) was regenerating the same file at the same time; the file's final state belongs to that session, not this report.

## 2026-09-23 15:01 — round 1

- Tree: `"*"` is defined as a sentinel only for `defaultExpanded`; whether a controlled `expanded` containing `"*"` means the same is not stated. Chose to resolve it identically in both, since the doc says lazy-hold rules are shared.
- Tree: after a user opens a lazy node whose id is literally `"*"`, onExpandChange reports `"*"`, which the next render reads as the sentinel. The doc says `"*"` is never matched against an id but doesn't cover this round trip. Left as is.
- Tree: 'the first user toggle resolves `"*"` to the concrete ids then open' does not say where in the array those ids go relative to other listed ids. Chose to splice them in at the sentinel's position, keeping every other id (held-lazy and disabled ones included) in place.
- Tree: 'disabled nodes cannot be expanded by any means' — whether a disabled parent listed in `defaultExpanded`/`expanded` (or covered by `"*"`) renders open is only implied by the web note 'a disabled parent stays closed'. Chose: never renders open; `"*"` resolves to enabled loaded parents only; an explicitly listed disabled id stays in the reported array like a held-lazy id.
- Tree: selectChildren says 'a disabled node is skipped as a target' but also 'a parent's id is in selected exactly when all its enabled loaded descendants are'. It is unclear whether a disabled ancestor's own id joins the selection when its subtree fills. Kept the literal invariant: it does.
- Tree: expandButtonSize says 'the chevron takes the Button's own color', but RN has no currentColor, so Button cannot tint a caller-supplied leadingIcon. Passed `t.colorActionGhostForeground` (Button's ghost foreground) to the Chevron's Icon, as TreeGrid does; the doc could name that token for rn.
- Tree: the rn notes list accessibilityState {expanded, selected, checked, disabled} but the web notes put aria-busy on an open lazy parent. Kept `busy` in accessibilityState and mirrored aria-busy on rn too.
- Tree: fontFamily/fontSize/lineHeight 'set the container's font', but RN has no inheritance, so there is nothing to set on the root View. They are forwarded to the label, placeholder and empty-state Text only.
- Tree: rowGap sits between the chevron and the icon/label, and guideLine's position formula (rowPaddingInline + indent × (level − 1) + expandButtonSize / 2) only holds if nothing separates the indent from the chevron. Chose to group indent + chevron with no gap between them; the doc could state that the indent is not followed by rowGap.
- Tree: the web notes say the lazy placeholder has 'no data-part'; the rn notes are silent. Removed the `Tree.node`/`Tree.indent` testIDs from the rn placeholder for parity.
- Tree: the rn notes put the root `testID="Tree"` on the container View while the declared element is FlatList; the accessible name therefore lives on the FlatList (role list), not on the node carrying the root hook.

## 2026-09-23 15:01 — round 2

- Tree: platforms.rn gives each row accessibilityRole="button" (or "link") carrying accessibilityState.selected/checked, but the package convention says to mirror them as aria-selected/aria-checked. ARIA allows neither on role=button or role=link, so axe's aria-allowed-attr would fail. The mirrors stay limited to label/expanded/busy, so react-native-web's DOM shows no selection state; the doc should pick a role that allows them or drop the mirror requirement for this row.
- Tree: an id that is literally "*" inside a controlled `expanded` cannot be told apart from the sentinel; the doc defines the sentinel only for defaultExpanded. The code resolves "*" at render in both modes, as TreeGrid does.
- Tree: 'a controlled expanded change that closes an ancestor of the focused node moves focus to that ancestor' has no rn form in platforms.rn (no roving focus); not implemented.
- Tree: selectOnFocus is 'wired to the Pressable's onFocus', but the selectOnFocus description says Tab entry never selects. On react-native-web, tabbing onto a row fires onFocus and selects it. The rn note was followed.
- Tree: the navigation-sidebar example uses app-relative hrefs, which the rn notes say Linking.openURL cannot open; the story renders them as given and no __DEV__ warning is asked for.
- Tree: this round was fixed by the concurrent session ds-regen-rn-19 re-adding ICON_COLOR, not by this session; two generator sessions are writing the same file in one worktree.

## 2026-09-23 19:45 — round 1

- Tree: the guideLine description says a line runs 'down that parent's group', but rn flattens groups, so each row draws one full-height line per open ancestor. The result looks the same, but the doc never says how a flattened platform should draw it, and I kept the existing approach.
- Tree: Enter is listed with no platforms, so it applies to rn, but the rn notes say a tap does what Enter does and give no separate Enter path. I treated it as satisfied by the tap.
- Tree: the doc does not say whether the selected-count live region is rendered when `selectable` is `multiple` and `nodes` is empty. The existing code renders it ('0 selected'), following the web guidance, and I kept that.

## 2026-09-23 19:46 — round 2

- Tree: the rn notes require accessibilityState.selected/checked on rows of role button/link, and the conventions say to mirror every accessibility prop as aria-*. But aria-selected and aria-checked are not allowed on those roles, so axe's aria-allowed-attr fails, and react-native-web ignores accessibilityState. I kept native accessibilityState and dropped the DOM mirrors, so on react-native-web selection and checked state are visual only. The doc should either pick a role that allows them (a treeitem-like role that Role lacks) or allow aria-current/aria-pressed as the DOM form.
- Tree: the row's `aria-label` on the Pressable is still emitted; if the doc's reading of 'level n' in the label conflicts with aria-level on web, that is unspecified for rn.
