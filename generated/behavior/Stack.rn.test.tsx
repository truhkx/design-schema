// Generated by tools/behavior_tests.ts from the `behavior` block of the Stack doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Stack } from '../../packages/rn/src/Stack';
import type { StackProps } from '../../packages/rn/src/Stack';
import meta from '../../packages/rn/src/Stack.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<StackProps> = {}) {
  const events = {};
  const props: StackProps = { ...(meta.args as StackProps), ...given };
  const tree = (p: StackProps) => (
    <ThemeProvider mode="light">
      <Stack {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Stack') ?? screen.UNSAFE_root,
    container: () => s.root(),
    rerender: (next: Partial<StackProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Stack', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-direction-vertical', () => {
    const s = setup({"direction": "vertical"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-direction-horizontal', () => {
    const s = setup({"direction": "horizontal"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gap-none', () => {
    const s = setup({"gap": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gap-tight', () => {
    const s = setup({"gap": "tight"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gap-normal', () => {
    const s = setup({"gap": "normal"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gap-loose', () => {
    const s = setup({"gap": "loose"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-gap-section', () => {
    const s = setup({"gap": "section"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-start', () => {
    const s = setup({"align": "start"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-center', () => {
    const s = setup({"align": "center"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-end', () => {
    const s = setup({"align": "end"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-stretch', () => {
    const s = setup({"align": "stretch"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-justify-start', () => {
    const s = setup({"justify": "start"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-justify-center', () => {
    const s = setup({"justify": "center"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-justify-end', () => {
    const s = setup({"justify": "end"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-justify-between', () => {
    const s = setup({"justify": "between"});
    expect(s.root()).toBeTruthy();
  });
});
