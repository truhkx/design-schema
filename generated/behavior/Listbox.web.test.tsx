// Generated by tools/behavior_tests.ts from the `behavior` block of the Listbox doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Listbox } from '../../packages/react/src/Listbox';
import type { ListboxProps } from '../../packages/react/src/Listbox';
import meta from '../../packages/react/src/Listbox.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<ListboxProps> = {}) {
  const events = {
    onChange: vi.fn(),
    onActiveChange: vi.fn(),
  };
  const props = { ...meta.args, ...given, onChange: events.onChange, onActiveChange: events.onActiveChange };
  const utils = render(<Listbox {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Listbox"]') ?? screen.queryByRole('listbox') ?? utils.container.firstElementChild) as HTMLElement,
    list: () => (screen.queryByRole('listbox') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<ListboxProps>) => utils.rerender(<Listbox {...props} {...next} />),
  };
  return s;
}

describe('Listbox', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-max-visible-5', async () => {
    const s = setup({"maxVisible": "5"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-max-visible-8', async () => {
    const s = setup({"maxVisible": "8"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-max-visible-12', async () => {
    const s = setup({"maxVisible": "12"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-max-visible-all', async () => {
    const s = setup({"maxVisible": "all"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('listbox', { name: s.props.label })).toBeInTheDocument();
  });
  test('error-is-identified', async () => {
    const s = setup({"error": "Fix this before continuing."});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeInTheDocument();
    expect(s.list()).toHaveAttribute('aria-invalid', 'true');
  });
});
