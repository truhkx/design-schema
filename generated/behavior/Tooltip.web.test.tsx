// Generated by tools/behavior_tests.ts from the `behavior` block of the Tooltip doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Tooltip } from '../../packages/react/src/Tooltip';
import type { TooltipProps } from '../../packages/react/src/Tooltip';
import meta from '../../packages/react/src/Tooltip.stories';

function setup(given: Partial<TooltipProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Tooltip {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Tooltip"]') ?? screen.queryByRole('tooltip') ?? utils.container.firstElementChild) as HTMLElement,
    trigger: () => (screen.queryByRole('tooltip') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<TooltipProps>) => utils.rerender(<Tooltip {...props} {...next} />),
  };
  return s;
}

describe('Tooltip', () => {
  test('the-visible-tooltip-carries-the-tooltip-role', async () => {
    const s = setup({"open": true});
    expect(screen.getByRole('tooltip')).toBeInTheDocument();
  });
  test('the-text-stays-in-the-tree-while-hidden', async () => {
    const s = setup({"open": false});
    await s.user.hover(s.trigger());
    expect(screen.getByRole('tooltip')).toBeInTheDocument();
  });
  test('renders', async () => {
    const s = setup({"open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-top', async () => {
    const s = setup({"placement": "top", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-bottom', async () => {
    const s = setup({"placement": "bottom", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-start', async () => {
    const s = setup({"placement": "start", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-placement-end', async () => {
    const s = setup({"placement": "end", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-delay-default', async () => {
    const s = setup({"delay": "default", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-delay-none', async () => {
    const s = setup({"delay": "none", "open": true});
    expect(s.root()).not.toBeNull();
  });
});
