// Generated by tools/behavior_tests.ts from the `behavior` block of the TreeGrid doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { TreeGrid } from '../../packages/react/src/TreeGrid';
import type { TreeGridProps } from '../../packages/react/src/TreeGrid';
import meta from '../../packages/react/src/TreeGrid.stories';

function setup(given: Partial<TreeGridProps> = {}) {
  const events = {
    onExpandChange: vi.fn(),
    onExpand: vi.fn(),
    onSortChange: vi.fn(),
    onSelectionChange: vi.fn(),
    onCellChange: vi.fn(),
    onEditStart: vi.fn(),
    onColumnResize: vi.fn(),
  };
  const props = { ...meta.args, ...given, onExpandChange: events.onExpandChange, onExpand: events.onExpand, onSortChange: events.onSortChange, onSelectionChange: events.onSelectionChange, onCellChange: events.onCellChange, onEditStart: events.onEditStart, onColumnResize: events.onColumnResize };
  const utils = render(<TreeGrid {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="TreeGrid"]') ?? screen.queryByRole('treegrid') ?? utils.container.firstElementChild) as HTMLElement,
    container: () => (screen.queryByRole('treegrid') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<TreeGridProps>) => utils.rerender(<TreeGrid {...props} {...next} />),
  };
  return s;
}

describe('TreeGrid', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-selectable-none', async () => {
    const s = setup({"selectable": "none"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-selectable-row', async () => {
    const s = setup({"selectable": "row"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-selectable-cell', async () => {
    const s = setup({"selectable": "cell"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-density-compact', async () => {
    const s = setup({"density": "compact"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-density-comfortable', async () => {
    const s = setup({"density": "comfortable"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-height-content', async () => {
    const s = setup({"height": "content"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-height-viewport', async () => {
    const s = setup({"height": "viewport"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-height-fixed', async () => {
    const s = setup({"height": "fixed"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('treegrid', { name: s.props.caption })).toBeInTheDocument();
  });
});
