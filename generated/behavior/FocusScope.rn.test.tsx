// Generated by tools/behavior_tests.ts from the `behavior` block of the FocusScope doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { FocusScope } from '../../packages/rn/src/FocusScope';
import type { FocusScopeProps } from '../../packages/rn/src/FocusScope';
import meta from '../../packages/rn/src/FocusScope.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<FocusScopeProps> = {}) {
  const events = {
    onEscapeAttempt: jest.fn(),
  };
  const props: FocusScopeProps = { ...(meta.args as FocusScopeProps), ...given, onEscapeAttempt: events.onEscapeAttempt };
  const tree = (p: FocusScopeProps) => (
    <ThemeProvider mode="light">
      <FocusScope {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('FocusScope') ?? screen.UNSAFE_root,
    scope: () => s.root(),
    rerender: (next: Partial<FocusScopeProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('FocusScope', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-auto-focus-first', () => {
    const s = setup({"autoFocus": "first"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-auto-focus-last', () => {
    const s = setup({"autoFocus": "last"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-auto-focus-container', () => {
    const s = setup({"autoFocus": "container"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-auto-focus-none', () => {
    const s = setup({"autoFocus": "none"});
    expect(s.root()).toBeTruthy();
  });
});
