Two Lit test failures in generated/gaps/TEST-FAILURES.md (`control-is-focusable` in packages/lit/src/AlertDialog.test.ts and Dialog.test.ts) are harness, not component: with a shadow root, `document.activeElement` is the HOST while focus is inside its shadow tree, so "expected <ds-alert-dialog> to be <ds-button>" means the inner button IS focused. Fix the assertion helper used by tools/behavior_tests.py and the model-written Lit tests to resolve the deep active element (`let el = document.activeElement; while (el?.shadowRoot?.activeElement) el = el.shadowRoot.activeElement;`) before comparing, and add that helper to the Lit conventions digest (prompts/conventions/lit.md, one line) so future generated tests use it. Re-run `pnpm --filter @design-schema/lit test` and update TEST-FAILURES.md. Do not modify packages/*/src component code.
