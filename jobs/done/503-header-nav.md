Build the site header and mobile navigation per website-plan.md ("Global chrome").

1. `apps/website/src/components/Header.astro` (or `.tsx` island where interactivity is needed): desktop layout is `Landmark(role="banner")` → `Container(width="page")` → `Stack(direction="row", align="center", justify="space-between")` with the logo `Link` to `/` on the left, a `Stack(direction="row", gap="normal")` of nav `Link`s (Homepage, Docs, About, and GitHub as an external `Link`) plus a `SegmentedControl` for the theme in the middle/right, read from `generated/nav.json`'s `top` list — do not hand-write the nav items.
2. Below `layout.breakpoint.md` (job 502's token), swap the nav `Stack` and `SegmentedControl` for one icon-only `Button` (`accessibleName="Open menu"`, `aria-expanded` bound to open state) that opens a `SidePanel` with `modal` set. The panel repeats the same nav `Link`s stacked vertically plus the theme `SegmentedControl` at the bottom, and returns focus to the hamburger `Button` on close per `SidePanel`'s existing focus-return contract.
3. Wire `Header` into `apps/website/src/layouts/Layout.astro` above the page slot.
Gate: keyboard-only pass — Tab reaches the hamburger, Enter/Space opens the panel, focus moves inside it, Escape closes and returns focus to the hamburger; axe/accessibility audit clean on both the open and closed states. Do not modify `packages/*/src` or `generated/` (this job only reads `generated/nav.json`).
