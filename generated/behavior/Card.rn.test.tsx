// Generated by tools/behavior_tests.ts from the `behavior` block of the Card doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Card } from '../../packages/rn/src/Card';
import type { CardProps } from '../../packages/rn/src/Card';
import meta from '../../packages/rn/src/Card.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<CardProps> = {}) {
  const events = {};
  const props: CardProps = { ...(meta.args as CardProps), ...given };
  const tree = (p: CardProps) => (
    <ThemeProvider mode="light">
      <Card {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Card') ?? screen.UNSAFE_root,
    surface: () => s.root(),
    rerender: (next: Partial<CardProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Card', () => {
  test('heading-is-rendered-as-a-heading', () => {
    const s = setup({"heading": "Team plan"});
    expect(screen.getByText(new RegExp("Team\\ plan"))).toBeOnTheScreen();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-2', () => {
    const s = setup({"headingLevel": "2"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-3', () => {
    const s = setup({"headingLevel": "3"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-4', () => {
    const s = setup({"headingLevel": "4"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-5', () => {
    const s = setup({"headingLevel": "5"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-6', () => {
    const s = setup({"headingLevel": "6"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-sm', () => {
    const s = setup({"inset": "sm"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-md', () => {
    const s = setup({"inset": "md"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-lg', () => {
    const s = setup({"inset": "lg"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-surface-default', () => {
    const s = setup({"surface": "default"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-surface-subtle', () => {
    const s = setup({"surface": "subtle"});
    expect(s.root()).toBeTruthy();
  });
});
