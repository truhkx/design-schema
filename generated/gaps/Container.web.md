# Gaps reported while generating Container for web

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-09 21:07 — round 1

- Container: the `gutter: narrow`/`wide` values have no explicit spec statement on whether they're fixed or also responsive; I treated only `default` as responsive (per its own description) and made `narrow`/`wide` static single values, since giving every gutter value the same three-tier responsive behavior would make the enum redundant.
- Container: `width: full` and `gutter: none` have no 'no-op' token, so I omit the CSS custom-property hook entirely for those modifier classes and let the property fall back to the literal `none` (max-inline-size) / `0` (padding-inline), mirroring Box's `surface: none` pattern — not stated explicitly in the schema but consistent with the codebase convention.
- Container: `element` is web/lit-only per platforms metadata, so no RN handling was needed; not a gap, just confirming the field was read correctly.
- Container: no dev-only warnings were specified or obviously needed (unlike Card's single-interactive-child check), so none were added.

## 2026-09-09 21:08 — round 2

- build gate `parse`: fails on checkbox.md and switch.md ('behavior' property not allowed by the component frontmatter schema) — unrelated to Container, which parses without error. This is a pre-existing docs/schema mismatch outside the React generator's scope (site/ and schema/ are off-limits here); it needs to be fixed by editing checkbox.md/switch.md frontmatter or the schema itself in the separate docs-fixing pass, not in packages/react.

## 2026-09-09 21:08 — round 3

- build gate `parse`: still fails only on checkbox.md and switch.md ('behavior' property not allowed by the component frontmatter schema) — confirmed unchanged from round 2, and container.md continues to parse without error. This is a pre-existing docs/schema mismatch unrelated to Container and outside the React generator's editable scope (site/ and schema/); no change to packages/react/src/Container.* can affect this gate's outcome. Needs a fix to checkbox.md/switch.md frontmatter or the schema's allowed properties in the separate docs-fixing pass.

## 2026-09-09 21:53 — round 1

- Container: no changes were needed — the existing implementation already matched the spec exactly (props, tokens, media-query literals verified against the built token file at 960px/1280px, override contract, index.ts export).
- Container: found an existing Container.test.tsx in the tree, which conflicts with my stored memory that only Switch has tests and the other 75 test targets are intentionally deferred pending user decision — left it untouched since generating/modifying tests wasn't requested, but this may mean that decision has since changed.
- Container (package-wide): none of the existing components implement the `data-ds="<Name>"` root testability hook the generation instructions call for; since the digest/conventions doc doesn't mention it and no sibling component has it, I did not add it to Container alone to avoid inconsistency — flagging for a package-wide decision instead.

## 2026-09-09 21:54 — round 2

- Container: the reported `parse` gate failure (checkbox.md / switch.md frontmatter has an unexpected 'behavior' property) is unrelated to Container and to the React package entirely — those files live under site/src/content/docs/components/, which the docs-fix pass owns and I'm instructed not to edit. Container's own files parse and lint clean (verified with tools/parse.py and tools/lint_literals.py). No code change was made; this needs to be routed to whatever pass generates checkbox.md/switch.md frontmatter (likely a stray 'behavior' key added to their schemas that isn't in the component JSON Schema).

## 2026-09-09 21:54 — round 3

- Container: identical to Round 2 — the `parse` gate failure is still exclusively about checkbox.md and switch.md frontmatter ('behavior' property not allowed by the component schema), files under site/src/content/docs/components/ that I'm instructed not to edit. This is unrelated to Container or the React/web package; re-verified Container.tsx/Container.css independently pass tools/lint_literals.py and tools/parse.py reports no error for container.md. Since the same report recurred unchanged, flagging that whatever is meant to clear checkbox.md/switch.md's stray 'behavior' frontmatter key (or the schema itself) hasn't run yet — this needs to be fixed at the docs/schema layer, not in packages/react.

## 2026-09-09 22:23 — round 1

- Container.tsx, Container.css, Container.stories.tsx, and the index.ts export already existed and matched the spec; the only deviation was a missing data-ds="Container" testability hook on the root element, which I added. No ambiguity in the schema itself required a judgment call.

## 2026-09-10 01:19 — round 1

- Container: default paddingInline hook is only set per gutter modifier class (not on .ds-container base), consistent with other interpolated bindings in the package — chose to leave it unset at base so 'none' truly renders 0 with no fallback token flash.
- Container: overridable maxWidth hook has no effect for width:full since no modifier class sets it — treated as correct per the spec's 'overrides are no-ops where the binding is not in effect' rule.

## 2026-09-10 17:32 — round 1

- Container: schema doesn't specify whether `align: start` clears margin-inline entirely or just the inline-start side; chose margin-inline: 0 (both sides), matching Box/Card sibling-spacing convention.
- Container: `full` width intentionally emits no --ds-container-max-width hook (falls back to `none`) rather than a literal value, per the 'a resolved path ending in a no-op renders nothing' rule — worth confirming this is the intended reading for `width` (not just background/border/max-width bindings named in the general rule).

## 2026-09-16 04:18 — round 1

- Container: the breakpoint px values depend on the theme (calm-precise content/page = 960px/1280px, warm-sleek = 1040px/1387px), but the CSS is a single theme-independent file; I used the calm-precise values, the package's theme, so another theme's responsive gutter switches at the wrong widths.
- Container: the gutter.default description says 'narrow below content, default between, wide above page' but does not say which side a viewport exactly at the token width falls on; I used min-width media queries, so exactly 960px gets the base gutter and exactly 1280px gets the wide one.
- Container: layout.gutter.default resolves to --layout-gutter (the .default drop rule), and the web note names it, but the prop description only mentions the narrow and wide gutters for `default`; I used narrow → --layout-gutter → wide.
- Container: the anatomy names one part, `column`, but no binding names a `part`; I put data-part="column" on the root, since the column is the root.
- Container: the web platform block says `element: div` while the `element` prop allows main/section; I render the tag the prop chooses, with div as the default.
- Container: the example `given.children` values are prose descriptions ('A Stack of page regions') and not content; the stories pass them verbatim as string children, so ApplicationScreen shows no real Stack.
- Container: the `main-element-is-the-page-landmark` scenario says 'there must be exactly one', but a component can't enforce that across the page and the spec asks for no dev warning; the test only checks that one main role renders, and no warning is added.
- Container: the SwiftUI notes say gutters shrink below the *prose* width, while the web/RN notes key off the content and page widths; this is a cross-platform inconsistency I didn't act on for web.

## 2026-09-17 04:34 — round 1

- Container: the rule 'every example is a story with exactly its given as args' conflicts with the children description (a string children renders inside a Text); followed the children description and wrapped each example string in <Text element="p">, which makes the args differ from the literal given.
- Container: the spec doesn't say which Text element or variant should hold illustrative children; chose Text element="p" with defaults.
- Container: the 'renders' scenarios don't say what to assert on web; the tests check that the root element exists and nothing about styles (jsdom can't evaluate the media queries or custom properties).
- Container: the spec names no Default-story children; kept a single Text paragraph 'Container content.' as the Default arg, which isn't schema copy.
- Container: the spec doesn't say whether an override on gutter: default should also replace the value inside the media queries; the hook is set inline, which beats the class rules at every viewport, so it replaces the whole responsive gutter as the paddingInline description requires.

## 2026-09-19 13:23 — round 1

- Container: the Related section names Landmark and the `element` description says to use `main` 'when no Landmark wraps it', but nothing says whether Container should warn or change when `element: main` is inside a Landmark main; I chose no check and no warning, following the 'neither enforces it nor warns' clause.
- Container: the `main-element-is-the-page-landmark` scenario's `then: role: main` doesn't say whether it means 'exactly one main' or 'at least one'; the test asserts exactly one element with role main in the render.
- Container: the derived `renders` scenarios say nothing beyond `renders: true`, so for gutter/width/align/element they only assert that a root node exists; the spec gives no observable (class, computed max-inline-size, tag name) to assert per value, so jsdom can't check that the responsive gutter or the `full`/`none` literals apply.
- Container: `children` is declared `required: true` and typed `ReactNode`, which already allows `undefined`/`null`, so 'required' here only means the prop key must be present; I kept `children: ReactNode` (not optional).

## 2026-09-20 07:34 — round 1

- Container: the overrides contract says every binding's hook sits on the root defaulting to its token, but both of Container's bindings are interpolated and two enum values (width: full, gutter: none) render no hook at all. I defined the hooks only on the modifier classes, so the base rule's var() is undefined at those values (max-inline-size falls back to the initial `none`, and gutter: none writes a literal 0). The doc does not say what the base .ds-container defaults should be.
- Container: because gutter: none writes `padding-inline: 0` instead of reading the hook, the documented consumer escape hatch (setting --ds-container-padding-inline from your own CSS) is also dead at that value, not just the `overrides` prop. The doc only rules out the prop-level override.
- Container: the generic generation rules require focus-visible, target sizes and keyboard-operable semantics, but the schema declares `a11y.role: none` and `a11y.requires: []`. I implemented none of them (schema wins), so Container ships with no focus styling of its own even though `element: main` makes it a landmark that can receive programmatic focus from a skip link.
- Container: the two media-query breakpoints are authored into a hand-written CSS file, not read from the token JSON at build time as the platform note describes — nothing regenerates Container.css when layout.maxWidth.content/page changes, so a theme edit silently desyncs the gutter breakpoints from the cap. The doc calls the theme-independence a known limit but implies a build-time read that does not exist in this package.
- Container: `align` has no stated behaviour at `width: full`, where there is no cap so the margin can never be asymmetric. I left `ds-container--align-*` applied unconditionally (margin-inline: auto vs 0, both no-ops at full) rather than suppressing it.
- Container: `element: section` on web renders an unnamed <section>, which is generic rather than a region landmark. The Lit note spells this out ('a section is a region only when it is named') but the web note does not, and no prop exists to name it — a caller wanting a named region must pass aria-label through ...rest.
- Container: ContainerProps extends ComponentPropsWithoutRef<'div'> for all four element values, so div-only attributes typecheck under element: main|section (and <main>/<section>-specific ones do not). The doc gives no polymorphic typing rule; I matched Box's choice.

## 2026-09-20 07:41 — round 2

- Container: round 2 reported the `axe` and `keyboard-run` gates as rejecting Container, but both are whole-Storybook/whole-suite gates — every failure belongs to Carousel, Feed, Listbox, Slider, Splitter (axe) and ActionSheet, Combobox, DatePicker, Listbox, Menu, Search, Slider, Tabs, Toast, Tooltip, Tree (keyboard). Container has no keyboard spec at all and passes axe on all 17 stories in both modes. A component-scoped gate run would make this attribution correct; I changed nothing rather than weakening the spec to chase another component's debt.
- Container: the overrides contract says every binding's hook sits on the root defaulting to its token, but both of Container's bindings are interpolated and two enum values (width: full, gutter: none) render no hook at all. I defined the hooks only on the modifier classes, so the base rule's var() is undefined at those values (max-inline-size falls back to the initial `none`, and gutter: none writes a literal 0). The doc does not say what the base .ds-container defaults should be.
- Container: because gutter: none writes `padding-inline: 0` instead of reading the hook, the documented consumer escape hatch (setting --ds-container-padding-inline from your own CSS) is also dead at that value, not just the `overrides` prop. The doc only rules out the prop-level override.
- Container: the generic generation rules require focus-visible, target sizes and keyboard-operable semantics, but the schema declares `a11y.role: none` and `a11y.requires: []`. I implemented none of them (schema wins), so Container ships with no focus styling of its own even though `element: main` makes it a landmark that can receive programmatic focus from a skip link.
- Container: the two media-query breakpoints are authored into a hand-written CSS file, not read from the token JSON at build time as the platform note describes — nothing regenerates Container.css when layout.maxWidth.content/page changes, so a theme edit silently desyncs the gutter breakpoints (960px / 1280px, verified against packages/tokens/dist/calm-precise/css/tokens.css) from the cap.
- Container: `align` has no stated behaviour at `width: full`, where there is no cap so the margin can never be asymmetric. I left `ds-container--align-*` applied unconditionally (margin-inline: auto vs 0, both no-ops at full) rather than suppressing it.
- Container: `element: section` on web renders an unnamed <section>, which is generic rather than a region landmark. The Lit note spells this out ('a section is a region only when it is named') but the web note does not, and no prop exists to name it — a caller wanting a named region must pass aria-label through ...rest.
- Container: ContainerProps extends ComponentPropsWithoutRef<'div'> for all four element values, so div-only attributes typecheck under element: main|section (and <main>/<section>-specific ones do not). The doc gives no polymorphic typing rule; I matched Box's choice.

## 2026-09-20 07:48 — round 3

- Container: for the third round the `axe` and `keyboard-run` gates were reported as rejecting Container, but both are whole-Storybook/whole-suite gates and neither failure list contains a Container entry — Container has no keyboard spec at all and its 17 stories pass axe in both modes. The gate needs to be scoped per component (or carry a baseline) before a generator round can act on it; I changed nothing rather than editing unrelated components or weakening the spec.
- Container: the overrides contract says every binding's hook sits on the root defaulting to its token, but both of Container's bindings are interpolated and two enum values (width: full, gutter: none) render no hook at all. I defined the hooks only on the modifier classes, so the base rule's var() is undefined at those values (max-inline-size falls back to the initial `none`, and gutter: none writes a literal 0). The doc does not say what the base .ds-container defaults should be.
- Container: because gutter: none writes `padding-inline: 0` instead of reading the hook, the documented consumer escape hatch (setting --ds-container-padding-inline from your own CSS) is also dead at that value, not just the `overrides` prop. The doc only rules out the prop-level override.
- Container: the generic generation rules require focus-visible, target sizes and keyboard-operable semantics, but the schema declares `a11y.role: none` and `a11y.requires: []`. I implemented none of them (schema wins), so Container ships with no focus styling of its own even though `element: main` makes it a landmark that can receive programmatic focus from a skip link.
- Container: the two media-query breakpoints are authored into a hand-written CSS file, not read from the token JSON at build time as the platform note describes — nothing regenerates Container.css when layout.maxWidth.content/page changes, so a theme edit silently desyncs the gutter breakpoints (960px / 1280px, verified against packages/tokens/dist/calm-precise/css/tokens.css) from the cap.
- Container: `align` has no stated behaviour at `width: full`, where there is no cap so the margin can never be asymmetric. I left `ds-container--align-*` applied unconditionally (margin-inline: auto vs 0, both no-ops at full) rather than suppressing it.
- Container: `element: section` on web renders an unnamed <section>, which is generic rather than a region landmark. The Lit note spells this out ('a section is a region only when it is named') but the web note does not, and no prop exists to name it — a caller wanting a named region must pass aria-label through ...rest.
- Container: ContainerProps extends ComponentPropsWithoutRef<'div'> for all four element values, so div-only attributes typecheck under element: main|section (and <main>/<section>-specific ones do not). The doc gives no polymorphic typing rule; I matched Box's choice.

## 2026-09-20 14:16 — round 1

- Container: the doc never says whether the gutter padding sits inside or outside the max-width cap — `box-sizing` is unspecified, and it changes the usable measure by up to twice the wide gutter (at `width: content` + `gutter: wide`, border-box gives ~960px minus gutters of content, content-box gives 960px plus gutters of total column). I chose `box-sizing: border-box` to match the Behavior prose 'the full viewport width minus the gutter'. The schema should state this, because prose measure ('about 65 characters') is only correct under one of the two.
- Container: the baked-in breakpoints are the default theme's, but the repo ships three themes and the other two disagree — warm-friendly and warm-sleek both resolve layout.maxWidth.content/page to 1040/1387, not 960/1280. The doc calls this 'a known limit', but as written the `default` gutter switches at the wrong widths for two of the three shipped themes, not just for hypothetical custom ones. Either the CSS needs to be emitted per theme or the gutter breakpoints need their own theme-independent tokens; I implemented the doc as written (calm-precise numbers, one file).
- Container: `element: section` renders a `<section>` with no accessible name, which is a generic element and not a region — the Lit note acknowledges 'a section is a region only when it is named', but the web side offers no naming affordance. The doc doesn't say whether Container should take a label for this case, warn in development, or accept the silent no-op. I accepted it silently (a caller can still pass `aria-label` through `...rest`, which is untyped intent rather than a declared prop).
- Container: `align` produces CSS (`margin-inline: auto | 0`) but is not listed under `styles:`, so it has no binding, no token and no override hook. That makes `auto`/`0` bare CSS keywords written directly in the rule. It passes the literal gate, but the doc never states how `align` is expressed on web — I inferred `margin-inline` from the platform note. If align is intentionally non-overridable that is fine, but it should be said, as `locked` is said elsewhere.
- Container: the overrides contract says a hook defaults to its token on the root, but `maxWidth` and `paddingInline` are interpolated bindings whose `full`/`none` values render no hook at all. That means the base rule reads a custom property that is sometimes undefined, relying on an explicit literal in the modifier class to win. The doc describes the intent but not the mechanism; I wrote the explicit `max-inline-size: none` / `padding-inline: 0` in the modifier rules rather than depending on invalid-at-computed-value fallback, which would resolve to `unset`, not to the documented value.

## 2026-09-20 14:18 — round 2

- Container: the `keyboard-run` gate fails with 'No tests found' and cannot be made to pass from the React package. Container has no `keyboard` block, so tools/keyboard_tests.ts:304 generates no spec, and `playwright test --project=keyboard-web Container` matches zero files and exits 1. This is a gate-harness bug affecting every non-interactive component (only 30 of 60 have a web spec; Box, Stack, Text, Heading, Card, Divider and Landmark are all in the same position). The runner should skip when generated/keyboard/<Name>.<platform>.spec.ts is absent rather than failing — and should match the exact filename, since a bare substring filter makes `Box` spuriously pass by running Combobox's and Listbox's specs, hiding the gap. I left the code correct and did not invent a keyboard block or a Keyboard story to satisfy the runner.
- Container: the doc never says whether the gutter padding sits inside or outside the max-width cap — `box-sizing` is unspecified, and it changes the usable measure by up to twice the wide gutter (at `width: content` + `gutter: wide`, border-box gives ~960px minus gutters of content, content-box gives 960px plus gutters of total column). I chose `box-sizing: border-box` to match the Behavior prose 'the full viewport width minus the gutter'. The schema should state this, because the prose measure ('about 65 characters') is only correct under one of the two.
- Container: the baked-in breakpoints are the default theme's, but the repo ships three themes and the other two disagree — warm-friendly and warm-sleek both resolve layout.maxWidth.content/page to 1040/1387, not 960/1280. The doc calls this 'a known limit', but as written the `default` gutter switches at the wrong widths for two of the three shipped themes, not just for hypothetical custom ones. Either the CSS needs to be emitted per theme or the gutter breakpoints need their own theme-independent tokens; I implemented the doc as written (calm-precise numbers, one file).
- Container: `element: section` renders a `<section>` with no accessible name, which is a generic element and not a region — the Lit note acknowledges 'a section is a region only when it is named', but the web side offers no naming affordance. The doc doesn't say whether Container should take a label for this case, warn in development, or accept the silent no-op. I accepted it silently (a caller can still pass `aria-label` through `...rest`, which is untyped intent rather than a declared prop).
- Container: `align` produces CSS (`margin-inline: auto | 0`) but is not listed under `styles:`, so it has no binding, no token and no override hook, leaving `auto`/`0` as bare CSS keywords in the rule. It passes the literal gate, but the doc never states how `align` is expressed on web — I inferred `margin-inline` from the platform note. If align is intentionally non-overridable that is fine, but it should be said, as `locked` is said elsewhere.
- Container: the overrides contract says a hook defaults to its token on the root, but `maxWidth` and `paddingInline` are interpolated bindings whose `full`/`none` values render no hook at all, so the base rule reads a custom property that is sometimes undefined and relies on an explicit literal in the modifier class to win. The doc describes the intent but not the mechanism; I wrote the explicit `max-inline-size: none` / `padding-inline: 0` in the modifier rules rather than depending on invalid-at-computed-value fallback, which would resolve to `unset`, not to the documented value.

## 2026-09-20 14:20 — round 3

- Container: the `keyboard-run` failure reported in rounds 2 and 3 is STALE — the current runner never runs that gate for Container. tools/checks.ts:110-114 already drops keyboard-run on a scoped run when generated/keyboard/<Name>.<platform>.spec.ts is absent (fix credited in generated/gaps/Button.lit.md:210). I probed gatesFor('web', …, 'Container') directly: keyboard-run is DROPPED, while Menu gets '--pass-with-no-tests generated/keyboard/Menu.web.spec.ts'. Running the gate set the runner really builds for Container gives 7/7 green (parse, contrast, literals, typecheck, deps, modules, keyboard). The round loop is replaying a pre-fix failure verbatim instead of re-deriving it from the current gatesFor result — that is the bug, and it burned two fix rounds here. Other components in this phase may be stuck the same way.
- Container: the doc never says whether the gutter padding sits inside or outside the max-width cap — `box-sizing` is unspecified, and it changes the usable measure by up to twice the wide gutter (at `width: content` + `gutter: wide`, border-box gives ~960px minus gutters of content, content-box gives 960px plus gutters of total column). I chose `box-sizing: border-box` to match the Behavior prose 'the full viewport width minus the gutter'. The schema should state this, because the prose measure ('about 65 characters') is only correct under one of the two.
- Container: the baked-in breakpoints are the default theme's, but the repo ships three themes and the other two disagree — warm-friendly and warm-sleek both resolve layout.maxWidth.content/page to 1040/1387, not 960/1280. The doc calls this 'a known limit', but as written the `default` gutter switches at the wrong widths for two of the three shipped themes, not just for hypothetical custom ones. Either the CSS needs to be emitted per theme or the gutter breakpoints need their own theme-independent tokens; I implemented the doc as written (calm-precise numbers, one file).
- Container: `element: section` renders a `<section>` with no accessible name, which is a generic element and not a region — the Lit note acknowledges 'a section is a region only when it is named', but the web side offers no naming affordance. The doc doesn't say whether Container should take a label for this case, warn in development, or accept the silent no-op. I accepted it silently (a caller can still pass `aria-label` through `...rest`, which is untyped intent rather than a declared prop).
- Container: `align` produces CSS (`margin-inline: auto | 0`) but is not listed under `styles:`, so it has no binding, no token and no override hook, leaving `auto`/`0` as bare CSS keywords in the rule. It passes the literal gate, but the doc never states how `align` is expressed on web — I inferred `margin-inline` from the platform note. If align is intentionally non-overridable that is fine, but it should be said, as `locked` is said elsewhere.
- Container: the overrides contract says a hook defaults to its token on the root, but `maxWidth` and `paddingInline` are interpolated bindings whose `full`/`none` values render no hook at all, so the base rule reads a custom property that is sometimes undefined and relies on an explicit literal in the modifier class to win. The doc describes the intent but not the mechanism; I wrote the explicit `max-inline-size: none` / `padding-inline: 0` in the modifier rules rather than depending on invalid-at-computed-value fallback, which would resolve to `unset`, not to the documented value.

## 2026-09-20 16:31 — round 1

- Container: the component already existed in the package and already satisfied this spec — no file was written this pass. The four source files and the index.ts export block were verified against the spec and the gates instead. If this job was meant to produce a fresh generation, the prompt gave no signal that the target was already populated.
- Container: `a11y.role: none` and `a11y.requires: []` contradict the `main-element-is-the-page-landmark` behavior scenario, which asserts `role: main`. The a11y block records no landmark at all, so the only place the main-landmark contract exists is the scenario list. Kept the native `<main>` semantics (no explicit `role` attribute) and let the scenario be the contract.
- Container: the spec does not say what `align` means when `width: full`, where there is no cap for the column to sit inside and `margin-inline` has nothing to distribute. Chose to keep emitting the `ds-container--align-*` modifier at every width (so `align: start` still writes `margin-inline: 0`) rather than suppressing it, since the class is inert there anyway and suppressing it would make the DOM inconsistent across widths.
- Container: the `children` prop description mixes API documentation with story-authoring instructions (how a string `given` should be wrapped in a Text, what the Default story's children must be). Only the first sentence is usable as a JSDoc line; the rest was applied to the stories instead. Splitting story-construction guidance out of the prop description would remove the judgment call.
- Container: the spec fixes the two gutter breakpoints to calm-precise's resolved px and acknowledges the theme-independence limit, but does not say what should happen for a theme whose `layout.maxWidth.content`/`page` differ (warm-sleek is 1040 / 1387). Emitted the single calm-precise-keyed stylesheet as instructed; under warm-sleek the cap follows the theme while the gutter switches at the wrong widths. A per-theme CSS file or container queries would be the fix, but both are outside what the spec allows.
- Container: the spec does not state whether `...rest` may override the testability hooks. Placed `{...rest}` before `data-ds`/`data-part` so a consumer cannot clobber them, which is the opposite order from Box.tsx:127 (where `data-part` precedes the spread so a composing parent can rename the part). Container is never composed by another component in this package, so the stricter order was chosen.

## 2026-09-22 03:54 — round 1

- Container: the spec never mentions box-sizing, but max-inline-size plus padding-inline only produce the documented "full viewport width minus the gutter" under border-box; content-box would make the column wider than its cap by the gutter. Chose `box-sizing: border-box` on the root as an unstated requirement of the documented behavior.
- Container: the `children` schema description mixes the consumer-facing contract with generator instructions about how stories must render a string label. Chose to put only the first sentence in the prop's JSDoc and honour the story instruction in Container.stories.tsx, since the rest is not meaningful to a consumer reading the type.
- Container: the spec says `...rest` never forwards `className`/`style` but not whether passing them should be a type error or a silent drop. Chose to omit both from the `Omit<ComponentPropsWithoutRef<'div'>, …>` so a consumer gets a compile error rather than a silently ignored prop — unlike Box, which accepts `className` because composing components pass one.
- Container: `element` is div|main|section but props are typed against `'div'` only, so the component is not polymorphic and the tag is cast to `ElementType`. The spec doesn't say whether attribute props should narrow per element value; chose Box's approach (one prop type, cast) for consistency, which means e.g. no element-specific attributes are typed.
- Container: an override at a no-op value could either be skipped or written anyway (the literal rule would win regardless). The spec only states the result is a no-op with no warning; chose to skip writing the hook, so the inline style stays empty and the consumer's own CSS hook is not shadowed by a value that does nothing.
- Container: the spec describes the fixed gutters as outranking "the bare responsive rules inside both media queries", implying the media queries contain unscoped `.ds-container` rules. Chose instead to scope both media queries to `.ds-container--gutter-default`, which makes narrow/wide/none fixed by construction rather than by specificity — same observable result, fewer rules to reason about.

## 2026-09-23 14:17 — round 1

- Container: the `children` prop description mixes prop documentation with instructions for stories (render the label in a Text, the Default children string). Only the first sentence went into the JSDoc; the rest was applied to the stories.
- Container: the derived `renders*` scenarios name no element or attribute to assert on. The test checks that the render container has a first child, not the `data-ds` root specifically.
- Container: the web notes don't say whether `element: section` with no accessible name should warn or accept an `aria-label`. It renders a plain unnamed `<section>` that inherits `aria-*` from `...rest`, with no warning, matching the doc's 'author's responsibility' stance on `main`.
- Container: the example stories inherit the meta defaults (`gutter: default`, `align: center`, `element: div`) as well as their `given` args. I read 'exactly its given as args' as 'given layered on the Default args', the same way the scenarios work.

## 2026-09-23 18:58 — round 1

- Container: the spec doesn't say how a test should check the responsive gutter or the `full`/`none` no-hook cases. jsdom has no media queries and does not load the CSS, so the scenarios only assert render and the main landmark. The breakpoint behavior has no automated coverage.
- Container: the breakpoints (960 and 1280) are hard-coded in the CSS from calm-precise, as the notes say. The spec doesn't say what should happen when they drift from the tokens, so nothing checks them.
