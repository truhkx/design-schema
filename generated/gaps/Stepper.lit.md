# Gaps reported while generating Stepper for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 11:26 — round 1

- Stepper: composition table says `step: Button`, but ds-button's API only supports a single text label plus leading/trailing icon slots — it has no default slot for the label+description Text pair the platform notes describe as 'wrapped inside' the Button. Implemented the interactive step control as a native <button> (styled with the component's own tokens), matching the precedent in Disclosure.ts (which also declares its own locked focusRing/focusRingWidth bindings rather than relying on a composed Button's). ds-text is still used for the label/description parts.
- Stepper: the a11y section wants the accessible name to read 'Step 2: Payment, current step' and the web platform notes say this comes from 'a visually-hidden <span> inside the control'. I instead set a single `aria-label` on the control (built from copy.stepLabel + the status word) and left the visible label/description as decorative, with the description linked via aria-describedby instead of folded into the name. This avoids double-announcement and keeps the description available to AT, but is a different mechanism than the literal hidden-span instruction.
- Stepper: `label` (nav landmark accessible name, default 'Progress') is used in the web platform notes and accessibility section but is not declared in the `props` table, and 'Progress' is not in `copy`. Added it as an optional property defaulting to 'Progress', inferring the default from prose rather than a copy token.
- Stepper: navigable="completed" eligibility was decided as position-based (`index < currentIndex`, i.e. any visited step, including one explicitly marked `error`) rather than strictly `status === 'complete'`, since the doc frames it as 'go back, not skip ahead' and an error step is exactly the one a user most needs to revisit. Not explicit in the schema.
- Stepper: no token is given for the indicator's numeral/icon color on upcoming/current steps (only indicatorCompleteForeground/indicatorErrorForeground are defined). Used `color.foreground` as a reasonable default; the contrast pair list doesn't explicitly cover this combination against `color.control.background`.
- Stepper: the `styles` block has no hover-background or radius token for the step control (unlike Button/Disclosure/Tabs, which declare one), so the control renders with no hover affordance and square focus outline — treated as intentional minimalism rather than an omission to fill in.
- Stepper: `compact` prop and the auto-compact container query both toggle the same CSS (hide non-current labels, show "Step n of m"); the doc doesn't say whether manual `compact` should also apply to vertical orientation, so it was implemented to work for either orientation, while the automatic container-query switch is scoped to horizontal only per the platform notes.

## 2026-09-10 18:45 — round 1

- Stepper: stepHover and stepRadius were listed in the schema's styles and Overridable set but had no CSS binding in the file I found — added `--ds-stepper-step-hover` (color.action.ghost.backgroundHover, applied on button.control:hover/:active) and `--ds-stepper-step-radius` (radius.sm, applied as border-radius on .control) plus a background-color transition using the existing transition hook, since the spec didn't say whether radius applies to the non-navigable div control too — I applied it to both for a consistent hit-area shape since only the interactive affordance (hover/press) is button-only.

## 2026-09-16 09:45 — round 1

- Stepper: Behavior says a navigable step's accessible name is copy.stepLabel plus the status word set as aria-label, but the same section (and Lit notes) says on web/Lit the <ol> already gives the ordinal so the control shows its plain label plus a visually hidden status word. Chose the latter; copy.stepLabel is therefore unused on Lit — the doc should say stepLabel is native-only.
- Stepper: platforms.web.notes says each navigable <li> holds a Button (ghost), while navigable's description and Behavior say it is the component's own native <button>, not Button. Chose the native <button>; the web notes and Guidance > Platform notes > Web need updating.
- Stepper: the compact breakpoint is layout.maxWidth.prose, but a container-query condition cannot read a custom property, so the value (572px) is hard-coded with literal-ok. The doc should name how Lit gets the built token value (or accept the literal).
- Stepper: labelWeight, labelCurrentWeight, labelSize and descriptionSize are listed as parts label/description with their own hooks, but the label and description are composed Text. Chose to forward them to <ds-text>.overrides (fontWeight/fontSize) and to declare no --ds-stepper-label-* hooks; fontFamily is both a host hook and forwarded. The doc should state the forwards explicitly (child binding names).
- Stepper: labelColor/labelUpcomingColor/descriptionColor are expressed as Text tone default/muted; the doc does not map these bindings to tone values. Chose muted for upcoming (non-current) labels and all descriptions.
- Stepper: partGap, fontFamily, minTarget, focusRing and focusRingWidth have no part. Chose partGap = gap between indicator and label inside the control (also its inline padding), fontFamily on the host, minTarget as the control's min block/inline size, focus ring on the button's :focus-visible.
- Stepper: the stepOf count has no anatomy part or style binding (size, tone, placement). Chose a <ds-text size=sm tone=muted> after the <ol>, shown only when compact applies.
- Stepper: steps[].status 'current' on a step whose id is not `current` is undefined behavior. Chose: indicator/status word follow the status (says 'current step'), but aria-current and the compact reveal follow the id.
- Stepper: when `current` matches no step id, status derivation, navigability and 'Step n of m' are unspecified. Chose all steps upcoming, none navigable under `completed`, count shows 'Step 1 of m', and a dev warning.
- Stepper: 'Hidden labels are clipped' in compact — the doc does not say whether descriptions and the status word are clipped with the label. Chose to clip the whole label/description/status-word group of non-current steps.
- Stepper: the doc never says whether descriptions render in horizontal orientation (orientation says vertical shows them). Chose to render descriptions only when vertical.
- Stepper: the connector-complete condition is unspecified (by position before current, or by the step's status). Chose by position (index < current index), matching 'completed means visited'.
- Stepper: behavior scenario `compact-shows-the-step-count` only checks text; the Lit test also asserts the count is visible. The `copy:` scenarios are checked against shadow-root textContent because the words are visually hidden.

## 2026-09-17 12:42 — round 1

- Stepper: Text's own weight scale has no default for font.weight.medium/semibold, so the labelWeight/labelCurrentWeight forwards are always sent to the label <ds-text> overrides (the token by default), not only when an override is set; the doc says 'forwarded' without saying whether a default is forwarded too.
- Stepper: indicatorCompleteForeground and indicatorErrorForeground are locked, yet they are realised as the composed Icon's `color` override (with indicatorFontSize as its `size`); the composition block lists no Icon part or forwards, so these Icon props/overrides are outside the declared 'exactly the listed props' contract. I followed the style-binding descriptions.
- Stepper: the `step` part carries stepHover, stepRadius, stepPadding, minTarget and focusRing, which style the inner control (button/div), but stepGap styles the space between <li>s. I kept data-part="step" on the <li> as web does and styled the control, so the step part is not one element for all its bindings.
- Stepper: labelCurrentWeight is described as applying to 'the current step (the id match)' while tone and status word follow the explicit status; for a step whose explicit status is 'current' but whose id is not `current`, the label keeps labelWeight. Chosen per the id wording.
- Stepper: the doc says a dev warning is logged 'when no id matches' but `current` is required with no default; the Lit property needs a default, so an element whose `current` is still '' warns in development too.

## 2026-09-21 16:38 — round 1

- Stepper: the connector-centring formula `calc(stepPadding + (indicatorSize - connectorWidth) / 2)` is given as if one axis, but stepPadding is inline-only in horizontal (`block padding too when vertical`), so in horizontal adding it pushes the line below the indicator's centre. Used `calc((indicatorSize - connectorWidth) / 2)` horizontally and the full formula vertically; the doc should state the formula per orientation, using the padding on the offset axis.
- Stepper: `minTarget` is listed on part `step` with no condition, but a display-only `<div>` step is not a target and target-24px does not apply to it. Applied the minimum block/inline size to the navigable `<button>` only (as React does); the doc should say the binding is scoped to the navigable control, as `focusRing`/`stepHover` effectively are.
- Stepper: the lit notes say `aria-describedby` to the description, but on a non-navigable `<div>` control (no role) aria-describedby is not exposed, and aria-hiding the description there would silence it entirely. Referenced + hid the description only inside the navigable `<button>`; a non-navigable vertical step renders it as plain visible text, read once. The doc should say the describedby/aria-hidden pairing is button-only.
- Stepper: the order of the visually-hidden status word relative to the description inside the control is only implied ('the visually-hidden status span follows the label directly'). Rendered label -> status word -> description, so compact clips label and status word together and the description (vertical-only) never competes with them.
- Stepper: `count` is described both as 'present only while compact is in effect' and as 'always rendered and display: none outside the query'. Rendered it unconditionally and toggled `display` from `:host([compact]:not([orientation='vertical']))` and the container query; its `stepGap` spacing is the nav's flex gap, which collapses with it when hidden. The doc should drop the 'present only while' phrasing for web/Lit.
- Stepper: `orientation` is in `platforms.lit.reflect` with default `horizontal`, but a Lit default is not reliably on the element as an attribute before first update, so `:host([orientation='horizontal'])` is not safe. Wrote every horizontal rule as `:host(:not([orientation='vertical']))`. Worth stating as the package rule for reflected enum props that have a default.
- Stepper: `copy.stepLabel` is declared in the copy block but the lit notes explicitly forbid using it (the `<ol>` supplies the ordinal). Kept it in the module's COPY table, unused, so the copy set stays complete — but a copy string no platform on this target may render reads as a doc inconsistency; it should be marked native-only in the copy block itself, not only in prose.
- Stepper: `stepGap` says the connector 'grows with free space from a minimum of stepGap'. There is free inline space to grow into when horizontal, but a vertical column of auto-height steps has none, so vertically the connector is always exactly `stepGap` tall. The doc should say the growth is horizontal-only or name a vertical connector length.
- Stepper: `indicatorFontSize` (a `font.size.*` token) is forwarded as the check/danger Icon's `size` override, per the binding's description. That makes a font-size token drive an icon box size; it works because both resolve to a length, but the doc should confirm this is intended rather than `space.*`.

## 2026-09-23 15:19 — round 1

- Stepper: minTarget's description says both navigable and display-only step controls 'carry the transparent focusRingWidth border, so navigable and display-only steps occupy the same box', but focusRing says the reserved border is React Native only (web/Lit draw an outline outside), and the web/Lit connector offsets (calc((indicatorSize − connectorWidth) / 2), calc(stepPadding + …)) leave no room for such a border. Chose no border on web/Lit: the outline is outside the control and the connector calc is as written. The doc should scope the transparent border sentence in minTarget to React Native.
- Stepper: compact with no id match reveals the first step's label, but the doc does not say whether that step's visually-hidden status word, which sits in the same clipped block, is revealed with it. Chose to reveal the label and status word together as one block (the status word is still visually hidden), matching how compact reveals the selected step.
- Stepper: the doc gives the exact dev-warning text but not whether it fires once or on every change to steps/current. Chose to check again on each change to steps or current, so a later non-matching id warns again.
- Stepper: the keyboard block's Tab rule (from: first, expect: focus-next) appears in the schema but not in the resolved lit Keyboard section, which lists only Enter/Space as native. Chose native tab order only: non-navigable steps are <div>s and so are not focusable, and no Tab code was added.
- Stepper: the count is described as rendered always with display:none 'applied through Text's layout-only className'. Lit has no className prop, so the class='count' goes on the <ds-text> host inside Stepper's shadow root, where Stepper's own shadow styles hide and show it. That controls the host's layout only and never reaches into Text's shadow tree.

## 2026-09-23 15:20 — round 2

- Stepper: labelColor, labelUpcomingColor, descriptionColor and countColor each say 'Realised by the … Text's tone …; no --ds-stepper-* hook', but tools/check_hooks.ts requires every locked binding that is not in composition.forwards to declare its hook. The doc cannot have both. Chose to keep the tone props and also declare the four hooks on :host with the same default tokens, feeding them into Text's documented --ds-text-color hook on the label, description and count <ds-text>, per status for the label. The doc should either drop 'no --ds-stepper-* hook' or mark these bindings as exempt, for example as forwards to a Text color override. Text's overrides type has no color binding today.
- Stepper: minTarget's description says both navigable and display-only step controls 'carry the transparent focusRingWidth border', but focusRing says that reserved border is React Native only (web/Lit draw an outline outside), and the web/Lit connector offset calcs leave no room for such a border. Chose no border on web/Lit.
- Stepper: compact with no id match reveals the first step's label, but the doc does not say whether that step's visually-hidden status word is revealed with it. Chose to reveal the label and status word together as one block (the status word is still visually hidden).
- Stepper: the doc gives the exact dev-warning text but not whether it fires once or on every change to steps/current. Chose to check again on each change to steps or current.
- Stepper: the keyboard block's Tab rule (from: first, expect: focus-next) is in the schema but missing from the resolved lit Keyboard section, which lists only Enter/Space as native. Chose native tab order only: non-navigable steps are <div>s and so are not focusable.
- Stepper: the count is shown and hidden 'through Text's layout-only className'. Lit has no className prop, so class='count' goes on the <ds-text> host inside Stepper's shadow root, where Stepper's own shadow styles show and hide it. That controls the host's layout only.

## 2026-09-23 19:33 — round 1

- Stepper: the styles list labelColor, labelUpcomingColor, descriptionColor and countColor as locked and say 'no --ds-stepper-* hook', while the Lit convention says locked bindings keep a :host hook. Text's tone is locked and has no colour hook, so a Stepper hook would do nothing; I declared none. The doc should say plainly that these four are realised by Text's tone on every platform.
- Stepper: layout.maxWidth.prose is emitted as a literal 572px container-query breakpoint. The doc does not give the resolved number, so it has to be read from the built tokens and can go stale if the token changes.
