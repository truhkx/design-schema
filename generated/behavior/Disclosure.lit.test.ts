// Generated by tools/behavior_tests.ts from the `behavior` block of the Disclosure doc. Do not edit.
import { beforeEach, describe, expect, test, vi } from 'vitest';
import { userEvent } from 'vitest/browser';
import '../../packages/lit/src/Disclosure.js';
import meta from '../../packages/lit/src/Disclosure.stories.js';

function deep(root: ParentNode, selector: string): Element | null {
  const direct = root.querySelector(selector);
  if (direct) return direct;
  for (const el of Array.from(root.querySelectorAll('*'))) {
    const shadow = (el as HTMLElement).shadowRoot;
    if (shadow) {
      const found = deep(shadow, selector);
      if (found) return found;
    }
  }
  return null;
}

/** Every element that is "active" from the document down through shadow roots: the host, then the inner one.
 *  document.activeElement alone is a shadow host while focus sits inside its shadow tree. */
function activeChain(): Element[] {
  const chain: Element[] = [];
  let el: Element | null = document.activeElement;
  while (el) {
    chain.push(el);
    el = el.shadowRoot?.activeElement ?? null;
  }
  return chain;
}

async function setup(given: Record<string, unknown> = {}) {
  const el = document.createElement('ds-disclosure');
  const props = { ...meta.args, ...given };
  for (const [key, value] of Object.entries(props)) {
    if (value !== undefined) (el as unknown as Record<string, unknown>)[key] = value;
  }
  const events = {
    onToggle: vi.fn(),
  };
  el.addEventListener('toggle', events.onToggle as unknown as EventListener);
  document.body.append(el);
  await (el as unknown as { updateComplete: Promise<boolean> }).updateComplete;
  const root: ParentNode = el.shadowRoot ?? el;  // some elements render in light DOM
  const s = {
    el,
    root,
    events,
    props,
    root_: () => el,
    trigger: () => (deep(root, '[role="button"]') ?? deep(root, '[part="trigger"]') ?? deep(root, '[data-part="trigger"]') ?? root.firstElementChild) as HTMLElement,
  };
  return s;
}

beforeEach(() => {
  document.body.replaceChildren();
});

describe('ds-disclosure', () => {
  test('click-on-trigger-expands', async () => {
    const s = await setup({"open": true});
    await userEvent.click(s.trigger());
    expect(s.events.onToggle).toHaveBeenCalled();
    expect(s.trigger()).toHaveAttribute('aria-expanded', 'true');
  });
  test('open-disclosure-collapses-on-click', async () => {
    const s = await setup({"defaultOpen": true, "open": true});
    await userEvent.click(s.trigger());
    expect(s.events.onToggle).toHaveBeenCalled();
    expect(s.trigger()).toHaveAttribute('aria-expanded', 'false');
  });
  test('disabled-trigger-does-not-toggle', async () => {
    const s = await setup({"disabled": true, "open": true});
    await userEvent.click(s.trigger(), { force: true });
    expect(s.events.onToggle).not.toHaveBeenCalled();
    expect(s.trigger()).toHaveAttribute('aria-expanded', 'false');
    expect(s.trigger()).toHaveAttribute('aria-disabled', 'true');
  });
  test('disabled-trigger-stays-focusable', async () => {
    const s = await setup({"disabled": true, "open": true});
    s.el.focus();
    expect(activeChain()).toContain(s.el);
  });
  test('controlled-open-change-reports-controlled', async () => {
    const s = await setup({"open": false});
    Object.assign(s.el, {"open": true});
    await (s.el as unknown as { updateComplete: Promise<boolean> }).updateComplete;
    expect(s.events.onToggle).toHaveBeenCalled();
    expect(s.trigger()).toHaveAttribute('aria-expanded', 'true');
  });
  test('renders', async () => {
    const s = await setup({"open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-heading-level-2', async () => {
    const s = await setup({"headingLevel": "2", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-heading-level-3', async () => {
    const s = await setup({"headingLevel": "3", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-heading-level-4', async () => {
    const s = await setup({"headingLevel": "4", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-heading-level-5', async () => {
    const s = await setup({"headingLevel": "5", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-heading-level-6', async () => {
    const s = await setup({"headingLevel": "6", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('has-accessible-name', async () => {
    const s = await setup({"open": true});
    expect(s.trigger()).toHaveAccessibleName(s.props.summary);
  });
  test('control-is-focusable', async () => {
    const s = await setup({"open": true});
    s.el.focus();
    expect(activeChain()).toContain(s.el);
  });
});
