// Generated by tools/behavior_tests.ts from the `behavior` block of the Input doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Input } from '../../packages/rn/src/Input';
import type { InputProps } from '../../packages/rn/src/Input';
import meta from '../../packages/rn/src/Input.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<InputProps> = {}) {
  const events = {
    onChange: jest.fn(),
    onFocus: jest.fn(),
    onBlur: jest.fn(),
  };
  const props: InputProps = { ...(meta.args as InputProps), ...given, onChangeText: events.onChange, onFocus: events.onFocus, onBlur: events.onBlur };
  const tree = (p: InputProps) => (
    <ThemeProvider mode="light">
      <Input {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Input') ?? screen.UNSAFE_root,
    label: () => screen.queryByRole('textbox') ?? s.root(),
    rerender: (next: Partial<InputProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Input', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-type-text', () => {
    const s = setup({"type": "text"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-type-email', () => {
    const s = setup({"type": "email"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-type-password', () => {
    const s = setup({"type": "password"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-type-number', () => {
    const s = setup({"type": "number"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-type-search', () => {
    const s = setup({"type": "search"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-type-tel', () => {
    const s = setup({"type": "tel"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-type-url', () => {
    const s = setup({"type": "url"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-sm', () => {
    const s = setup({"size": "sm"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-md', () => {
    const s = setup({"size": "md"});
    expect(s.root()).toBeTruthy();
  });
  test('error-is-identified', () => {
    const s = setup({"error": "Fix this before continuing."});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeOnTheScreen();
  });
});
