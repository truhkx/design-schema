// Generated by tools/behavior_tests.ts from the `behavior` block of the Table doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Table } from '../../packages/rn/src/Table';
import type { TableProps } from '../../packages/rn/src/Table';
import meta from '../../packages/rn/src/Table.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<TableProps> = {}) {
  const events = {
    onSortChange: jest.fn(),
    onSelectionChange: jest.fn(),
    onRowPress: jest.fn(),
  };
  const props: TableProps = { ...(meta.args as TableProps), ...given, onSortChange: events.onSortChange, onSelectionChange: events.onSelectionChange, onRowPress: events.onRowPress };
  const tree = (p: TableProps) => (
    <ThemeProvider mode="light">
      <Table {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Table') ?? screen.UNSAFE_root,
    container: () => screen.queryByRole('table') ?? s.root(),
    rerender: (next: Partial<TableProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Table', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-caption-level-2', () => {
    const s = setup({"captionLevel": "2"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-caption-level-3', () => {
    const s = setup({"captionLevel": "3"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-caption-level-4', () => {
    const s = setup({"captionLevel": "4"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-none', () => {
    const s = setup({"selectable": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-single', () => {
    const s = setup({"selectable": "single"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-multiple', () => {
    const s = setup({"selectable": "multiple"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-responsive-stack', () => {
    const s = setup({"responsive": "stack"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-responsive-scroll', () => {
    const s = setup({"responsive": "scroll"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-max-height-none', () => {
    const s = setup({"maxHeight": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-max-height-viewport', () => {
    const s = setup({"maxHeight": "viewport"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-density-compact', () => {
    const s = setup({"density": "compact"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-density-comfortable', () => {
    const s = setup({"density": "comfortable"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('table', { name: s.props.caption })).toBeOnTheScreen();
  });
});
