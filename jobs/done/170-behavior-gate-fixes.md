The first full run of the package test suites (logs/test-react.log, test-lit.log, test-rn.log — read them; Out-File wrapped lines at console width, so rejoin before grepping) shows the behavior gate's generated tests failing systematically while the model-written src/*.test.tsx files largely pass. Fix the harness, not the components:

1. Components that are not generated yet (Accordion, Carousel, … 26 files on web, the "Failed to fetch dynamically imported module" set on lit) must not produce failing tests: tools/behavior_tests.py skips a component whose source file is absent in the package (or emits a single `test.skip` with the reason), and `pnpm test` in each package must pass with only generated components present.
2. Root location: the generated tests find the component by `getByRole(a11y.role)`. That fails for `img` (Icon is decorative by default: no role), `text` and `landmark` (not concrete roles), `separator` (Divider is aria-hidden unless semantic), `menu`/`dialog`/`alertdialog`/`tooltip` (closed at defaults), and inside Lit shadow roots (`expected null not to be null` in Stack/Heading/Container/Dialog/FocusScope lit tests). Locate by `[data-ds="<Name>"]` first (piercing shadow roots on lit, as tools/keyboard_tests.py's walker does), by role only as a fallback, and never by `text`/`landmark`/`none`.
3. Overlays and other closed-by-default components: derive `given: { open: true }` for components with a boolean `open` prop when a scenario needs the surface present (accessible-name, focusable, keyboard), or render the component's `Keyboard` story where one exists. Tooltip needs its trigger hovered/focused; treat `tooltip` as manual unless the story exists.
4. `has-accessible-name` on components whose name comes from an optional prop (Icon `label`, Divider `label`) must supply the prop in `given` (job 150 point 2 — do it here if 150 has not landed).
5. React Native: five src test files fail with `TypeError: Cannot read properties of undefined (reading 'then')` (Icon, AlertDialog, Menu, Stack, Dialog) — almost certainly `await act(() => {...})` returning undefined under react-test-renderer 18.3 or an Animated/timer mock missing. Fix the shared test setup (packages/rn/jest.setup or vitest equivalent) so the model-written tests run; if a test itself is wrong, fix it and record which in generated/gaps/<Name>.rn.md under a "harness" heading.
6. After the harness is fixed, list the remaining REAL failures per package (assertion mismatches between doc and code, e.g. the Lit `toHaveAccessibleName` and spy-not-called cases, the React `toHaveFocus` cases, the Button.test.tsx failure) in generated/gaps/TEST-FAILURES.md with the scenario name, the file, and a one-line diagnosis, so the docs or the targets can be fixed. Do not change component code under packages/*/src to make a test pass — the generator owns it; a real mismatch is either a doc fix or a regeneration.
Run `pnpm -r test` and `pytest -q`; the goal is zero harness-caused failures, real ones listed. Do not modify generated/ except the gap files and generated/behavior/*.
