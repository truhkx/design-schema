// Generated by tools/behavior_tests.ts from the `behavior` block of the Card doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Card } from '../../packages/react/src/Card';
import type { CardProps } from '../../packages/react/src/Card';
import meta from '../../packages/react/src/Card.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<CardProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Card {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Card"]') ?? utils.container.firstElementChild) as HTMLElement,
    surface: () => s.root(),
    rerender: (next: Partial<CardProps>) => utils.rerender(<Card {...props} {...next} />),
  };
  return s;
}

describe('Card', () => {
  test('heading-is-rendered-as-a-heading', async () => {
    const s = setup({"heading": "Team plan"});
    expect(screen.getByText(new RegExp("Team\\ plan"))).toBeInTheDocument();
    expect(screen.getByRole('heading')).toBeInTheDocument();
  });
  test('a-card-with-a-heading-is-an-article', async () => {
    const s = setup({"heading": "Team plan"});
    expect(screen.getByRole('article')).toBeInTheDocument();
  });
  test('interactive-adds-no-focus-stop', async () => {
    const s = setup({"heading": "September invoice", "children": "A Link to the invoice", "interactive": true});
    act(() => (s.surface()).focus());
    expect(s.surface()).not.toHaveFocus();
  });
  test('focusable-takes-scripted-focus-only', async () => {
    const s = setup({"focusable": true});
    expect(s.surface()).toHaveAttribute("tabindex", "-1");
    act(() => (s.surface()).focus());
    expect(s.surface()).toHaveFocus();
  });
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-heading-level-2', async () => {
    const s = setup({"headingLevel": "2"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-heading-level-3', async () => {
    const s = setup({"headingLevel": "3"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-heading-level-4', async () => {
    const s = setup({"headingLevel": "4"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-heading-level-5', async () => {
    const s = setup({"headingLevel": "5"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-heading-level-6', async () => {
    const s = setup({"headingLevel": "6"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-sm', async () => {
    const s = setup({"inset": "sm"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-md', async () => {
    const s = setup({"inset": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-lg', async () => {
    const s = setup({"inset": "lg"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-surface-default', async () => {
    const s = setup({"surface": "default"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-surface-subtle', async () => {
    const s = setup({"surface": "subtle"});
    expect(s.root()).not.toBeNull();
  });
});
