Add a parser check to tools/parse.py (keep the file's existing structure and the behavior-scenario code intact): for every style binding whose token contains `{prop}` interpolation, every value of that enum prop must resolve to a token that exists in the built token names (packages/tokens/dist/calm-precise/json/tokens.light.json keys, after dropping a trailing `.default`). Report as a DocError naming the component, binding, prop value and the missing token, e.g. "Toolbar: styles.itemGap 'layout.gap.{density}' → 'layout.gap.compact' is not a token". Non-enum interpolation targets are an error too. Two docs shipped with this bug today (Table, Toolbar) before it was caught by hand.

Add tests in tests/test_parse_checks.py in the existing style: a passing case (`font.size.{size}` with values [sm, md]), a failing enum value, and a non-enum prop. Run `pytest -q tests/test_parse_checks.py` and `node tools/py.mjs tools/parse.py` (must report 0 errors on the current docs). Do not modify anything under packages/ or generated/ by hand.
