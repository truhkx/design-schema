// Generated by tools/behavior_tests.ts from the `behavior` block of the Heading doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Heading } from '../../packages/rn/src/Heading';
import type { HeadingProps } from '../../packages/rn/src/Heading';
import meta from '../../packages/rn/src/Heading.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<HeadingProps> = {}) {
  const events = {};
  const props: HeadingProps = { ...(meta.args as HeadingProps), ...given };
  const tree = (p: HeadingProps) => (
    <ThemeProvider mode="light">
      <Heading {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Heading') ?? screen.UNSAFE_root,
    text: () => screen.queryByRole('heading') ?? s.root(),
    rerender: (next: Partial<HeadingProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Heading', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-level-1', () => {
    const s = setup({"level": "1"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-level-2', () => {
    const s = setup({"level": "2"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-level-3', () => {
    const s = setup({"level": "3"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-level-4', () => {
    const s = setup({"level": "4"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-level-5', () => {
    const s = setup({"level": "5"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-level-6', () => {
    const s = setup({"level": "6"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-4xl', () => {
    const s = setup({"size": "4xl"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-3xl', () => {
    const s = setup({"size": "3xl"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-2xl', () => {
    const s = setup({"size": "2xl"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-xl', () => {
    const s = setup({"size": "xl"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-lg', () => {
    const s = setup({"size": "lg"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-md', () => {
    const s = setup({"size": "md"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-start', () => {
    const s = setup({"align": "start"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-center', () => {
    const s = setup({"align": "center"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-align-end', () => {
    const s = setup({"align": "end"});
    expect(s.root()).toBeTruthy();
  });
});
