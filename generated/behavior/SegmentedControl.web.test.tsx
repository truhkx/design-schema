// Generated by tools/behavior_tests.ts from the `behavior` block of the SegmentedControl doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { SegmentedControl } from '../../packages/react/src/SegmentedControl';
import type { SegmentedControlProps } from '../../packages/react/src/SegmentedControl';
import meta from '../../packages/react/src/SegmentedControl.stories';

function setup(given: Partial<SegmentedControlProps> = {}) {
  const events = {
    onChange: vi.fn(),
  };
  const props = { ...meta.args, ...given, onChange: events.onChange };
  const utils = render(<SegmentedControl {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="SegmentedControl"]') ?? screen.queryByRole('radiogroup') ?? utils.container.firstElementChild) as HTMLElement,
    group: () => (screen.queryByRole('radiogroup') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<SegmentedControlProps>) => utils.rerender(<SegmentedControl {...props} {...next} />),
  };
  return s;
}

describe('SegmentedControl', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-sm', async () => {
    const s = setup({"size": "sm"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-md', async () => {
    const s = setup({"size": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('radiogroup', { name: s.props.label })).toBeInTheDocument();
  });
});
