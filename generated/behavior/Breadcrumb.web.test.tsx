// Generated by tools/behavior_tests.ts from the `behavior` block of the Breadcrumb doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Breadcrumb } from '../../packages/react/src/Breadcrumb';
import type { BreadcrumbProps } from '../../packages/react/src/Breadcrumb';
import meta from '../../packages/react/src/Breadcrumb.stories';

function setup(given: Partial<BreadcrumbProps> = {}) {
  const events = {
    onNavigate: vi.fn(),
  };
  const props = { ...meta.args, ...given, onNavigate: events.onNavigate };
  const utils = render(<Breadcrumb {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Breadcrumb"]') ?? screen.queryByRole('navigation') ?? utils.container.firstElementChild) as HTMLElement,
    nav: () => (screen.queryByRole('navigation') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<BreadcrumbProps>) => utils.rerender(<Breadcrumb {...props} {...next} />),
  };
  return s;
}

describe('Breadcrumb', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({"label": "Accessible name"});
    expect(screen.getByRole('navigation', { name: s.props.label })).toBeInTheDocument();
  });
});
