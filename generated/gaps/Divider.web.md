# Gaps reported while generating Divider for web

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 01:30 — round 1

- Divider: label is documented as text for a horizontal divider only ("Optional text in the middle of a horizontal divider"). When orientation is vertical and label is set, I render a plain vertical line (no label text, since there's no described layout for a vertical labelled divider) but still treat it as semantic (role=separator, aria-orientation=vertical) since a label was explicitly given — the doc doesn't cover this combination.
- Divider: labelSize and fontFamily are listed as overridable bindings using the standard --ds-divider-* CSS hook convention, but the label is rendered via the composed Text component, which already owns fontSize/fontFamily as its own overridable bindings. Rather than adding a parallel --ds-divider-label-size/--ds-divider-font-family hook that no rule would read, I forward overrides.labelSize/overrides.fontFamily into Text's own overrides prop (fontSize/fontFamily). Net effect for consumers is the same (the token applies), but there is no literal --ds-divider-label-size custom property to set from plain CSS as the generic override docs describe — only Text's own --ds-text-font-size hook, reachable by targeting Text's default class from outside, which isn't documented either.
- Divider: platform notes describe the vertical line as 'inline-size: var(--border-width-thin)' and the schema's thickness token is named border.width.thin, suggesting a CSS border. I implemented the line (both orientations, and both the plain-<hr> and labelled-<div> forms) as an explicit block-size/inline-size box with background-color instead of a border, so the single thickness/color hook pair works identically across the <hr> and the two line spans in the labelled layout without doubled-border rendering differences.

## 2026-09-10 17:35 — round 1

- Divider: the schema requires a development warning when `label` is set on a `vertical` divider ('no room for centered text'), but the existing implementation silently dropped the label with no warning and also incorrectly forced `isSemantic=true` from the ignored label (making a plain vertical <hr> announce role=separator with no visible/accessible text driving that semantics). Fixed both: added the console.warn gated on isDev, and changed isSemantic to derive from `showLabel` (label only takes effect when horizontal) rather than raw `Boolean(label)`.
- No story or test exercises the vertical+label combination (the dev-warning path) since it isn't an enum value in the behavior scenarios; left untested per the 'render every scenario, don't invent new ones' instruction — flagging in case a dedicated story/test is wanted for that guard.

## 2026-09-16 04:29 — round 1

- Divider: role=separator makes its children presentational in ARIA, so 'the label text is what gets read' is not guaranteed by containment alone; chose aria-labelledby on the separator pointing at the label Text (id from useId) so the text is its accessible name without duplicating it in aria-label.
- Divider: platforms.web.element is `hr` but the notes make every semantic divider a div; unclear whether an unlabelled semantic divider may stay <hr> (implicit separator role). Chose <div role=separator aria-orientation> for all semantic/labelled cases, <hr aria-hidden> only for decorative.
- Divider: anatomy lists `line` and `label`, but the unlabelled divider has no separate line element (the root paints itself); chose data-part="line" only on the two flanking spans of a labelled divider, and data-part="label" on the composed Text.
- Divider: `fontFamily` has no `part` and the root has no text of its own; the labelSize description says it travels 'along with fontFamily' to Text, so chose to forward fontFamily to Text's overrides only, never setting a root hook.
- Divider: `labelColor` is locked and `labelSize` defaults to font.size.sm, but the Web notes express them as Text `size="sm" tone="muted"` props rather than bindings; chose the Text props (no Divider CSS for label color/size).
- Divider: `spacing: none` resolves to layout.gap.none (a token that exists) yet is described as the off state; chose to emit no hook and no margin for `none`, so the spacing override is a no-op there, instead of var(--layout-gap-none).
- Divider: labelGap override when no label is shown is not stated either way; applied the 'overrides change values, never presence' rule and ignore it.
- Divider: 'block-size: 100% / align-self stretch' for vertical leaves display unspecified; chose inline-block with both block-size: 100% and align-self: stretch so it works in flex rows and in inline flow with a sized parent.
- Divider: the dev warning for an ignored vertical label doesn't say 'once'; chose a useEffect keyed on the ignored state so it warns per change rather than per render.
- Divider: ToolbarGroups/OrientationVertical stories need a sized flex row to show a vertical line; the spec gives no demo wrapper, so the stories use a decorator with an inline flex row style (blockSize: '3rem').

## 2026-09-17 04:44 — round 1

- Divider: the composition says the label Text receives exactly `size`, `tone`, `element` (plus forwards), but the web platform notes also require an `id` on it for `aria-labelledby` and `data-part="label"`; I pass `id` and `data-part` as platform necessities.
- Divider: the `toolbar-groups` example says to show the divider in a horizontal Stack with align stretch, but it doesn't say what the siblings are or what gives the row its height; I used two `Text` siblings ('Bold Italic', 'Align left') and gap `tight`, and applied the same wrapper to `OrientationVertical`.
- Divider: the label scenario only checks the text and the separator role, not that the separator's accessible name is the label, which is the point of the `aria-labelledby` note; the test doesn't check the name.
- Divider: the spec doesn't say which element the root `ref` is typed to when the root switches between `<hr>` and `<div>`; I typed it `Ref<HTMLElement>`.
- Divider: `...rest` takes div attributes but a decorative divider renders an `<hr>`; the spec doesn't say which attributes belong on the decorative root. I forward the same rest to both.
- Divider: the spec never says whether a labelled horizontal divider's lines should be vertically centered on the label or sit on the text baseline; I used `align-items: center`.

## 2026-09-20 10:36 — round 1

- Divider: a vertical divider with `semantic: true` and an ignored `label` renders a `<div role="separator">` with no children — the spec says the label is ignored but not whether it should still name the separator via `aria-label`. Chose: no accessible name, only the development warning.
- Divider: no `copy.*` string exists for the ignored-label development warning, so the wording is ours ('`label` is ignored on a vertical divider — a vertical line has no room for centered text.'). If the message is part of the contract it belongs in `copy`.
- Divider: the spec fixes the vertical cross-axis size but never states the horizontal one. Chose `inline-size: 100%` on the horizontal root (hr/div fills its container) — worth stating, since it makes a horizontal divider inside a horizontal flex row fill the row.
- Divider: whether `spacing` applies to the labelled row is unstated (the binding is described as cross-axis space on the root). Chose: yes — `margin-block` on the labelled flex row too, which is what the `or-between-alternatives` example (label + spacing normal) needs.
- Divider: the spec does not say whether the labelled line spans read the `--ds-divider-thickness`/`--ds-divider-color` hooks or the tokens directly. Chose the hooks, so an `overrides.color`/`overrides.thickness` reaches the line pieces in the labelled case as well as the unlabelled one.
- Divider: `...rest` must land on either an `hr` or a `div`, but the props interface can only extend one. Chose `Omit<ComponentPropsWithoutRef<'div'>, 'children' | 'role' | 'aria-orientation' | 'aria-hidden' | 'className' | 'style'>`; hr-specific typing is lost (React 19 gives `hr` no unique attributes, so nothing is actually unreachable) — the spec could say which element types the props.
- Divider: `flex-shrink: 0` on the root is not in the spec; added so a horizontal divider inside a vertical flex Stack keeps its one-token thickness instead of collapsing. Presence of that rule should be confirmed in the doc.
- Divider: the Default story's args carry no `label` (no schema default), so all seven derived `renders-*` scenarios exercise the decorative `<hr>` path and none of them touch the labelled `<div>` row. The doc's behavior list has no derived scenario per `label`/`semantic` combination, so the labelled path is covered by exactly one hand-written scenario.

## 2026-09-20 10:44 — round 2

- Divider: both round-2 gates are whole-Storybook/whole-suite runs, so they fail on other components' debt and name no Divider story. axe failures are Carousel (scrollable-region-focusable ×13), Feed+Listbox (aria-required-children ×13), Slider Disabled (color-contrast, light only), Splitter (target-size ×7); keyboard failures are ActionSheet, Combobox, DatePicker, Listbox, Menu, Search, Slider, Tabs, Toast, Tooltip, Tree. Divider has no `keyboard` block so it has no generated keyboard spec, and all 13 Divider/React stories pass axe in light and dark. Nothing in Divider was changed; the failures need per-component jobs.
- Divider: a vertical divider with `semantic: true` and an ignored `label` renders a `<div role="separator">` with no children — the spec says the label is ignored but not whether it should still name the separator via `aria-label`. Chose: no accessible name, only the development warning.
- Divider: no `copy.*` string exists for the ignored-label development warning, so the wording is ours ('`label` is ignored on a vertical divider — a vertical line has no room for centered text.'). If the message is part of the contract it belongs in `copy`.
- Divider: the spec fixes the vertical cross-axis size but never states the horizontal one. Chose `inline-size: 100%` on the horizontal root, which is what makes a horizontal divider fill its container.
- Divider: whether `spacing` applies to the labelled row is unstated. Chose yes — `margin-block` on the labelled flex row too, which the `or-between-alternatives` example (label + spacing normal) needs.
- Divider: the spec does not say whether the labelled line spans read the `--ds-divider-thickness`/`--ds-divider-color` hooks or the tokens directly. Chose the hooks, so `overrides.color`/`overrides.thickness` reach the line pieces in the labelled case as well.
- Divider: `...rest` must land on either an `hr` or a `div`, but the props interface can only extend one. Chose `Omit<ComponentPropsWithoutRef<'div'>, …>`; hr-specific typing is lost (React 19 gives `hr` no unique attributes). The spec could say which element types the props.
- Divider: `flex-shrink: 0` on the root is not in the spec; added so a horizontal divider inside a vertical flex Stack keeps its one-token thickness instead of collapsing.
- Divider: the Default story's args carry no `label`, so all seven derived `renders-*` scenarios exercise the decorative `<hr>` path only. The labelled `<div>` row is covered by exactly one hand-written scenario; the doc has no derived scenario per `label`/`semantic` combination.

## 2026-09-20 10:51 — round 3

- Divider: round 3 reported the same two gates red with output byte-identical to round 2, and again naming no Divider story. The axe dumps (10:49) post-date the Divider edits (10:35-10:36), so they are a fresh run over current code: 36 light / 35 dark failures, all Carousel (scrollable-region-focusable x13), Feed (aria-required-children x11), Listbox (aria-required-children x2), Slider Disabled (color-contrast, light only), Splitter (target-size x7). keyboard-run's 15 failures are ActionSheet, Combobox, DatePicker, Listbox, Menu, Search, Slider, Tabs, Toast, Tooltip, Tree. Divider has no `keyboard` block so `generated/keyboard/` contains no Divider spec, and Divider is imported only by Toolbar, NumberInput and Accordion - none of the failing components. Re-verified: 13 Divider stories, 0 axe violations light and dark; 52 unit tests green across Divider and its three consumers. The job loop cannot go green from inside the Divider diff; the whole-Storybook gates need per-component jobs for the five components above.
- Divider: a vertical divider with `semantic: true` and an ignored `label` renders a `<div role="separator">` with no children - the spec says the label is ignored but not whether it should still name the separator via `aria-label`. Chose: no accessible name, only the development warning.
- Divider: no `copy.*` string exists for the ignored-label development warning, so the wording is ours ('`label` is ignored on a vertical divider - a vertical line has no room for centered text.'). If the message is part of the contract it belongs in `copy`.
- Divider: the spec fixes the vertical cross-axis size but never states the horizontal one. Chose `inline-size: 100%` on the horizontal root, which is what makes a horizontal divider fill its container.
- Divider: whether `spacing` applies to the labelled row is unstated. Chose yes - `margin-block` on the labelled flex row too, which the `or-between-alternatives` example (label + spacing normal) needs.
- Divider: the spec does not say whether the labelled line spans read the `--ds-divider-thickness`/`--ds-divider-color` hooks or the tokens directly. Chose the hooks, so `overrides.color`/`overrides.thickness` reach the line pieces in the labelled case as well.
- Divider: `...rest` must land on either an `hr` or a `div`, but the props interface can only extend one. Chose `Omit<ComponentPropsWithoutRef<'div'>, ...>`; hr-specific typing is lost (React 19 gives `hr` no unique attributes). The spec could say which element types the props.
- Divider: `flex-shrink: 0` on the root is not in the spec; added so a horizontal divider inside a vertical flex Stack keeps its one-token thickness instead of collapsing.
- Divider: the Default story's args carry no `label`, so all seven derived `renders-*` scenarios exercise the decorative `<hr>` path only. The labelled `<div>` row is covered by exactly one hand-written scenario; the doc has no derived scenario per `label`/`semantic` combination.

## 2026-09-20 14:57 — round 1

- Divider: the spec gives no copy string for the ignored-label development warning (`copy.*` has no entry for it), so the message text is invented — if that wording should be stable across platforms it belongs in the doc's copy block.
- Divider: the spec says the labelled root is a flex row whose two line spans paint, but the shared `.ds-divider` rule paints the root itself; there is no token for 'no background', so the labelled modifier uses `background-color: transparent` (a keyword, not a token). Chose that over restructuring so the unlabelled root can stay the `line` part as the platform notes require.
- Divider: nothing in the spec covers the `<hr>` user-agent reset. A bare `<hr>` draws a UA border and has default block margins, which would double-draw the line and defeat `spacing: none`, so the CSS resets `margin/padding/border` and adds `flex-shrink: 0` (a horizontal divider inside a vertical flex Stack otherwise collapses below its one-token thickness). Neither is derivable from the schema.
- Divider: `spacing` is described as space 'above and below a horizontal divider', but it is not stated whether it still applies to the labelled row (which is a flex row, not a line). Applied `margin-block` to the labelled root too, on the reading that spacing is a property of the divider rather than of the line.
- Divider: the spec says an `overrides.labelGap` does nothing with no label in effect, but is silent on `labelSize`/`fontFamily` in the same situation. Dropped them as well for consistency; the outcome is unobservable either way since no Text renders, so this only matters if the rule is ever made testable.
- Divider: the `toolbar-groups` example describes a wrapper (horizontal Stack, align stretch, gap tight, between Texts 'Bold Italic' and 'Align left') but the contract says a story's args must be exactly the example's `given`. Put the wrapper in a Storybook decorator so `args` stays `{orientation: 'vertical'}` exactly; if the wrapper is meant to be part of the rendered example's identity, the doc should say how it is expressed.
- Divider: the Default story's args are a generator choice — the spec fixes the `given` for the four named examples but never says what Default renders, and the behavior tests spread `meta.args` as their baseline, so Default's args silently define every scenario's starting state.

## 2026-09-20 14:58 — round 2

- Divider: the `keyboard-run` gate was run against a component that declares no `keyboard` block, and cannot pass. tools/keyboard_tests.ts:304 skips such components, so no generated/keyboard/Divider.web.spec.ts exists, and `playwright test --project=keyboard-web Divider` exits 1 with 'No tests found' — reproduced verbatim. The doc says 'Nothing is interactive', so there is no keyboard model to implement and no `Keyboard` story is owed; I changed nothing rather than fabricating one. The per-component gate runner should skip keyboard-run when the doc has no `keyboard` block (or pass --pass-with-no-tests), since an empty file filter is indistinguishable from a real failure.
- Divider: the spec gives no copy string for the ignored-label development warning (`copy.*` has no entry for it), so the message text is invented — if it should be stable across platforms it belongs in the doc's copy block.
- Divider: the spec says the labelled root is a flex row whose two line spans paint, but the shared `.ds-divider` rule paints the root itself; there is no token for 'no background', so the labelled modifier uses `background-color: transparent` (a keyword, not a token). Chose that over restructuring so the unlabelled root can stay the `line` part as the platform notes require.
- Divider: nothing in the spec covers the `<hr>` user-agent reset. A bare `<hr>` draws a UA border and has default block margins, which would double-draw the line and defeat `spacing: none`, so the CSS resets margin/padding/border and adds `flex-shrink: 0` (a horizontal divider inside a vertical flex Stack otherwise collapses below its one-token thickness). Neither is derivable from the schema.
- Divider: `spacing` is described as space 'above and below a horizontal divider', but it is not stated whether it still applies to the labelled row (which is a flex row, not a line). Applied margin-block to the labelled root too, on the reading that spacing is a property of the divider rather than of the line.
- Divider: the spec says an `overrides.labelGap` does nothing with no label in effect, but is silent on `labelSize`/`fontFamily` in the same situation. Dropped them as well for consistency; the outcome is unobservable either way since no Text renders, so this only matters if the rule is ever made testable.
- Divider: the `toolbar-groups` example describes a wrapper (horizontal Stack, align stretch, gap tight, between Texts 'Bold Italic' and 'Align left') but the contract says a story's args must be exactly the example's `given`. Put the wrapper in a Storybook decorator so `args` stays {orientation: 'vertical'} exactly; if the wrapper is part of the rendered example's identity, the doc should say how it is expressed.
- Divider: the Default story's args are a generator choice — the spec fixes the `given` for the four named examples but never says what Default renders, and the behavior tests spread `meta.args` as their baseline, so Default's args silently define every scenario's starting state.

## 2026-09-20 15:01 — round 3

- Divider: `keyboard-run` was run against a component that declares no `keyboard` block and cannot pass — reproduced verbatim as `playwright test --project=keyboard-web Divider` → 'No tests found'. The repo already forbids this invocation: tools/checks.ts:110 only adds keyboard-run when generated/keyboard/<Name>.<platform>.spec.ts exists, its comment names Divider as an example, and tools/__tests__/checks.test.ts:50 asserts the gate is dropped. Verified live that gatesFor(...,'Divider') omits keyboard-run. The harness driving these fix rounds is therefore not building its command through gatesFor; it should, since an unmatched positional filter is indistinguishable from a real failure. No component-side fix exists, and I did not invent a keyboard model for a component whose doc says 'Nothing is interactive'.
- Divider: the spec gives no copy string for the ignored-label development warning (`copy.*` has no entry for it), so the message text is invented — if it should be stable across platforms it belongs in the doc's copy block.
- Divider: the spec says the labelled root is a flex row whose two line spans paint, but the shared `.ds-divider` rule paints the root itself; there is no token for 'no background', so the labelled modifier uses `background-color: transparent` (a keyword, not a token). Chose that over restructuring so the unlabelled root can stay the `line` part as the platform notes require.
- Divider: nothing in the spec covers the `<hr>` user-agent reset. A bare `<hr>` draws a UA border and has default block margins, which would double-draw the line and defeat `spacing: none`, so the CSS resets margin/padding/border and adds `flex-shrink: 0` (a horizontal divider inside a vertical flex Stack otherwise collapses below its one-token thickness). Neither is derivable from the schema.
- Divider: `spacing` is described as space 'above and below a horizontal divider', but it is not stated whether it still applies to the labelled row (which is a flex row, not a line). Applied margin-block to the labelled root too, on the reading that spacing is a property of the divider rather than of the line.
- Divider: the spec says an `overrides.labelGap` does nothing with no label in effect, but is silent on `labelSize`/`fontFamily` in the same situation. Dropped them as well for consistency; the outcome is unobservable either way since no Text renders, so this only matters if the rule is ever made testable.
- Divider: the `toolbar-groups` example describes a wrapper (horizontal Stack, align stretch, gap tight, between Texts 'Bold Italic' and 'Align left') but the contract says a story's args must be exactly the example's `given`. Put the wrapper in a Storybook decorator so `args` stays {orientation: 'vertical'} exactly; if the wrapper is part of the rendered example's identity, the doc should say how it is expressed.
- Divider: the Default story's args are a generator choice — the spec fixes the `given` for the four named examples but never says what Default renders, and the behavior tests spread `meta.args` as their baseline, so Default's args silently define every scenario's starting state.

## 2026-09-20 16:42 — round 1

- Divider: the development warning for an ignored label on a vertical divider has no `copy.*` entry, so its wording is invented rather than verbatim. Chose 'Divider: `label` is ignored on a vertical divider — a vertical line has no room for centered text.' If this string is meant to be asserted by a test or shared across platforms it needs to live in the schema's copy block.
- Divider: the spec does not say what a vertical divider with BOTH an ignored `label` and `semantic: true` is named on web. The label is not rendered, so there is no id to point at. Chose role=separator with aria-orientation=vertical and no aria-labelledby — the divider is semantic but anonymous. (The rn notes cover the analogous case explicitly; web does not.)
- Divider: the root prop base is ambiguous because the root is an `hr` or a `div` depending on props. The notes only fix the ref as `Ref<HTMLElement>` and say `...rest` lands on whichever root renders. Chose `Omit<ComponentPropsWithoutRef<'div'>, 'children'|'role'|'aria-orientation'|'aria-hidden'|'className'|'style'>` and cast the ref per branch; this silently accepts div-only attributes that would be invalid on the hr branch.
- Divider: whether `spacing` still applies to the labelled row is never stated — the binding is described as space on the cross axis, and the labelled root is a flex row rather than the line itself. Chose to apply margin-block to the labelled root too, since the `or-between-alternatives` example pairs `label: or` with `spacing: normal` and would otherwise render no space at all.
- Divider: `.ds-divider--labelled` must cancel the root's own line paint, and no token expresses 'no background'. Used `background-color: transparent` (a keyword, not a literal the gate flags). A rule structured as 'paint only when not labelled' would avoid the keyword entirely; the spec does not indicate a preference.
- Divider: the a11y contrast requirement (color.foreground.muted on color.background, AA) is satisfied entirely by the composed `Text size="sm" tone="muted"`, since labelColor is locked and Divider writes no label color rule. Nothing in the Divider files can enforce or regress it, so there is no assertion to make on this platform.

## 2026-09-22 04:14 — round 1

- Divider: the schema calls `labelGap` (layout.gap.normal) a binding on part `label`, but its description places it on the labelled root row as flex `gap`. I kept the hook on the root (`--ds-divider-label-gap`), since a gap belongs to the container, not the child; the `part: label` field is therefore not literally true for web.
- Divider: `styles.spacing` has no `part`, and the root is both the spacing carrier and (unlabelled) the line. The spec never says whether spacing applies when `semantic` is true but unlabelled — I applied it uniformly to every root, which the `section-boundary` example (semantic + spacing loose) implies but does not state.
- Divider: the web notes say the props base is the div's minus the component-owned attributes, but do not list `aria-labelledby` among them, so a consumer can pass it on a labelled divider where the component also sets it. I let the component's value win (spread `...rest` first); the doc should either omit `aria-labelledby` from the base too or state that consumer-supplied values lose.
- Divider: the development warning's wording is explicitly non-contractual ("not `copy.*` strings"), so the exact string I emit is invented. Nothing in the test suite or gates pins it, which means the warning can silently drift between platforms.
- Divider: the labelled line pieces use `flex: 1 1 0` so both sides are equal width regardless of label position. The spec says only "flex-grow line spans" (guidance) / "two aria-hidden line spans" (notes) without a basis, so whether an off-center label is intended is unspecified; `1 1 0` centers the label, `1 1 auto` would not.
- Divider: `a11y.contrast` claims color.foreground.muted on color.background at AA, but the same schema marks `labelColor` locked and "deliberately not enforced by Divider" — the pair is really a claim about Text's muted tone. No Divider code participates, so the contrast checker is validating a binding this component never writes.

## 2026-09-23 14:47 — round 1

- Divider: the web notes say the labelled line spans are 'flex-grow' but give no flex-basis or a rule for long labels (only the rn notes say a long label overflows rather than wraps); chose `flex: 1 1 0` on each line piece and left the Text free to wrap on web.
- Divider: a horizontal unlabelled divider is described as 'block-level and fills its container's inline size', which says nothing about a horizontal divider inside a horizontal flex row (where block-level does not fill); chose an explicit `inline-size: 100%` on the unlabelled horizontal root and the labelled row.
- Divider: 'overrides change values, never presence' is spelled out for `spacing` and `labelGap` but not for `labelSize`/`fontFamily` when no label is in effect; chose to drop them (moot on web, since no Text renders then).
- Divider: the decorative-divider scenario asserts `aria-hidden` without naming the element; the test reads it on the `data-ds="Divider"` root (the hr).
- Divider: the dev warning uses a module-level `process` guard (`typeof process !== 'undefined' && process.env.NODE_ENV !== 'production'`) because the spec gives only the condition, not how to guard when `process` is undefined.

## 2026-09-23 14:48 — round 2

- Divider: the spec and the hooks gate conflict on `labelColor`. The gate (and the overrides contract: 'locked bindings… keep their hook; rules read the hook') requires `--ds-divider-label-color`, but the labelColor description says Divider writes no label colour rule because it would restyle the composed Text, and labelColor is not in `composition.label.forwards`, so the forwarded-binding exemption does not apply. I declared the hook on the root, defaulting to `var(--color-foreground-muted)`, and no rule reads it. Setting it from page CSS does nothing, and the label is re-themed only through Text's own hooks. The doc should either add labelColor to the forwards exemption or say the hook is declared but inert.
- Divider: the web notes say the labelled line spans are 'flex-grow' but give no flex-basis or a rule for long labels (only the rn notes say a long label overflows rather than wraps); chose `flex: 1 1 0` on each line piece and left the Text free to wrap on web.
- Divider: a horizontal unlabelled divider is described as 'block-level and fills its container's inline size', which says nothing about a horizontal divider inside a horizontal flex row (where block-level does not fill); chose an explicit `inline-size: 100%` on the unlabelled horizontal root and the labelled row.
- Divider: 'overrides change values, never presence' is spelled out for `spacing` and `labelGap` but not for `labelSize`/`fontFamily` when no label is in effect; chose to drop them (moot on web, since no Text renders then).
- Divider: the decorative-divider scenario asserts `aria-hidden` without naming the element; the test reads it on the `data-ds="Divider"` root (the hr).

## 2026-09-23 14:48 — round 3

- Divider: the spec and the hooks gate conflict on `labelColor`. The gate (and the overrides contract: 'locked bindings… keep their hook; rules read the hook') requires `--ds-divider-label-color`, but the labelColor description says Divider writes no label colour rule because it would restyle the composed Text, and labelColor is not in `composition.label.forwards`, so the forwarded-binding exemption does not apply. I declared the hook on the root, defaulting to `var(--color-foreground-muted)`, and no rule reads it. Setting it from page CSS does nothing, and the label is re-themed only through Text's own hooks. The doc should either add labelColor to the forwards exemption or say the hook is declared but inert.
- Divider: the web notes say the labelled line spans are 'flex-grow' but give no flex-basis or a rule for long labels (only the rn notes say a long label overflows rather than wraps); chose `flex: 1 1 0` on each line piece and left the Text free to wrap on web.
- Divider: a horizontal unlabelled divider is described as 'block-level and fills its container's inline size', which says nothing about a horizontal divider inside a horizontal flex row (where block-level does not fill); chose an explicit `inline-size: 100%` on the unlabelled horizontal root and the labelled row.
- Divider: 'overrides change values, never presence' is spelled out for `spacing` and `labelGap` but not for `labelSize`/`fontFamily` when no label is in effect; chose to drop them (moot on web, since no Text renders then).
- Divider: the decorative-divider scenario asserts `aria-hidden` without naming the element; the test reads it on the `data-ds="Divider"` root (the hr).

## 2026-09-23 18:59 — round 1

- Divider: the spec says labelColor is locked and not enforced, while the conventions say a locked binding still declares its hook. I declared `--ds-divider-label-color` on the root, and no rule reads it. If the naming codemod does not need that hook, the docs should say whether to omit it.
- Divider: the label has a `label-is-read` scenario expecting the name `or`, but for the Lit `aria-label` and web `aria-labelledby` split the spec never says whether the web root should also carry `aria-label`. I used `aria-labelledby` only, as the platform notes state.
