Restyle the site header per Tony's review, 2026-09-15: full-width nav with centered, evenly-spaced links; drop the "Homepage" link and make the logo itself the home link; give the header a dark/inverse look for contrast against the page; and a site-wide link underline rule.

1. **Layout.** In `apps/website/src/components/HeaderNav.tsx` / `Header.astro`, change the nav row from its current (left logo, right-clustered links) arrangement to full width with the link group centered and evenly spaced (`justify-content: space-evenly` or equal-gap flex, not `space-between` pinned to the edges) — reference `apps/website/src/components/header.css`'s `.ds-site-header` rules.
2. **Remove "Homepage".** `generated/nav.json`'s `top` list is written by `tools/site_nav.ts` from a fixed top-level list (site-nav.ts writes `Homepage / Docs / GitHub / About`, per `website-plan.md`'s original spec) — drop `Homepage` from that generator, not just from the rendered markup, so the mobile `SidePanel` nav (job 503) stays in sync automatically. The logo mark in `Header.astro` (currently a plain image/link) becomes the home link (`href="/"`) — confirm it already has an accessible name (it's a `Link`, so check what text/label it carries; a logo-only link needs one, e.g. "Design Schema, home").
3. **Dark header.** Before inventing new tokens, check `apps/website/src/theme-switch.ts` and `themes.ts` — the site already ships light/dark builds (`tokens.light.*`, `tokens.SWzU7fps.css` in the last build), so there may already be a `[data-theme="dark"]`-scoped set of CSS custom properties this can reuse by wrapping the header in that same attribute regardless of the page's active theme. If the existing dark-mode variables are the *whole* dark palette (surfaces, borders, every semantic color) rather than just foreground/background, scoping the header to them is the smaller change and should be preferred. Only if that doesn't give an acceptable header-specific look, add the `foreground`/`background` inverse pair Tony suggested as a new token group (`color.inverse.foreground` / `color.inverse.background`) in `foundations/*.md` + `tools/theme.ts`, consumed only by the header. State which path was taken in the job log — this is a real fork, not a detail.
4. **Global link underline rule.** Site-wide (not just the header): links get `text-decoration: none` by default and `text-decoration: underline` on `:hover`/`:focus-visible`. This is a global site rule, not a `Link` component schema change (the generated `Link` component's own decoration is a separate, cross-platform concern — don't touch `packages/react/src/Link.tsx` for this; it's `apps/website`'s own stylesheet, e.g. a rule in `apps/website/src/styles/`).
Gate: `pnpm --filter website build` succeeds; the header renders full width with centered, evenly spaced links and no "Homepage" entry at any viewport, including the mobile `SidePanel` nav; the logo is a focusable, named link to `/`; the header keeps 4.5:1 text contrast against its own background at both viewport tiers; every in-page text link across the site is unstyled until hover/focus, then underlines. Do not modify `packages/react/src/Link.tsx` or any other generated component — this is `apps/website`-only styling plus, if step 3 needs it, a token addition.
