// Generated by tools/behavior_tests.ts from the `behavior` block of the Stack doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Stack } from '../../packages/react/src/Stack';
import type { StackProps } from '../../packages/react/src/Stack';
import meta from '../../packages/react/src/Stack.stories';

function setup(given: Partial<StackProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Stack {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Stack"]') ?? utils.container.firstElementChild) as HTMLElement,
    container: () => s.root(),
    rerender: (next: Partial<StackProps>) => utils.rerender(<Stack {...props} {...next} />),
  };
  return s;
}

describe('Stack', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-direction-vertical', async () => {
    const s = setup({"direction": "vertical"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-direction-horizontal', async () => {
    const s = setup({"direction": "horizontal"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gap-none', async () => {
    const s = setup({"gap": "none"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gap-tight', async () => {
    const s = setup({"gap": "tight"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gap-normal', async () => {
    const s = setup({"gap": "normal"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gap-loose', async () => {
    const s = setup({"gap": "loose"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gap-section', async () => {
    const s = setup({"gap": "section"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-start', async () => {
    const s = setup({"align": "start"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-center', async () => {
    const s = setup({"align": "center"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-end', async () => {
    const s = setup({"align": "end"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-stretch', async () => {
    const s = setup({"align": "stretch"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-justify-start', async () => {
    const s = setup({"justify": "start"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-justify-center', async () => {
    const s = setup({"justify": "center"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-justify-end', async () => {
    const s = setup({"justify": "end"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-justify-between', async () => {
    const s = setup({"justify": "between"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-div', async () => {
    const s = setup({"element": "div"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-section', async () => {
    const s = setup({"element": "section"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-nav', async () => {
    const s = setup({"element": "nav"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-ul', async () => {
    const s = setup({"element": "ul"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-ol', async () => {
    const s = setup({"element": "ol"});
    expect(s.root()).not.toBeNull();
  });
});
