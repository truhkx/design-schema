// Generated by tools/behavior_tests.ts from the `behavior` block of the Box doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Box } from '../../packages/react/src/Box';
import type { BoxProps } from '../../packages/react/src/Box';
import meta from '../../packages/react/src/Box.stories';

function setup(given: Partial<BoxProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Box {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Box"]') ?? utils.container.firstElementChild) as HTMLElement,
    surface: () => s.root(),
    rerender: (next: Partial<BoxProps>) => utils.rerender(<Box {...props} {...next} />),
  };
  return s;
}

describe('Box', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-none', async () => {
    const s = setup({"inset": "none"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-sm', async () => {
    const s = setup({"inset": "sm"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-md', async () => {
    const s = setup({"inset": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-lg', async () => {
    const s = setup({"inset": "lg"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-xl', async () => {
    const s = setup({"inset": "xl"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-block-none', async () => {
    const s = setup({"insetBlock": "none"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-block-sm', async () => {
    const s = setup({"insetBlock": "sm"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-block-md', async () => {
    const s = setup({"insetBlock": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-block-lg', async () => {
    const s = setup({"insetBlock": "lg"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-block-xl', async () => {
    const s = setup({"insetBlock": "xl"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-inline-none', async () => {
    const s = setup({"insetInline": "none"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-inline-sm', async () => {
    const s = setup({"insetInline": "sm"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-inline-md', async () => {
    const s = setup({"insetInline": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-inline-lg', async () => {
    const s = setup({"insetInline": "lg"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-inset-inline-xl', async () => {
    const s = setup({"insetInline": "xl"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-surface-none', async () => {
    const s = setup({"surface": "none"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-surface-default', async () => {
    const s = setup({"surface": "default"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-surface-subtle', async () => {
    const s = setup({"surface": "subtle"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-surface-strong', async () => {
    const s = setup({"surface": "strong"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-radius-none', async () => {
    const s = setup({"radius": "none"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-radius-sm', async () => {
    const s = setup({"radius": "sm"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-radius-md', async () => {
    const s = setup({"radius": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-radius-lg', async () => {
    const s = setup({"radius": "lg"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-radius-full', async () => {
    const s = setup({"radius": "full"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-div', async () => {
    const s = setup({"element": "div"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-section', async () => {
    const s = setup({"element": "section"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-article', async () => {
    const s = setup({"element": "article"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-aside', async () => {
    const s = setup({"element": "aside"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-header', async () => {
    const s = setup({"element": "header"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-footer', async () => {
    const s = setup({"element": "footer"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-main', async () => {
    const s = setup({"element": "main"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-nav', async () => {
    const s = setup({"element": "nav"});
    expect(s.root()).not.toBeNull();
  });
});
