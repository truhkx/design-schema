// Generated by tools/behavior_tests.ts from the `behavior` block of the Fieldset doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Fieldset } from '../../packages/react/src/Fieldset';
import type { FieldsetProps } from '../../packages/react/src/Fieldset';
import meta from '../../packages/react/src/Fieldset.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<FieldsetProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Fieldset {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Fieldset"]') ?? screen.queryByRole('group') ?? utils.container.firstElementChild) as HTMLElement,
    group: () => (screen.queryByRole('group') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<FieldsetProps>) => utils.rerender(<Fieldset {...props} {...next} />),
  };
  return s;
}

describe('Fieldset', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gap-tight', async () => {
    const s = setup({"gap": "tight"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gap-normal', async () => {
    const s = setup({"gap": "normal"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gap-loose', async () => {
    const s = setup({"gap": "loose"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('group', { name: s.props.legend })).toBeInTheDocument();
  });
  test('error-is-identified', async () => {
    const s = setup({"error": "Fix this before continuing."});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeInTheDocument();
    expect(s.group()).toHaveAttribute('aria-invalid', 'true');
  });
});
