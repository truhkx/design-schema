# Gaps reported while generating DatePicker for web

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 11:48 — round 1

- DatePicker: composition requires monthSelect/yearSelect to be Select, but Select always renders a visible block label above its trigger (no visually-hidden-label option) and has no `size` prop despite the platform notes saying 'size: sm' — the header is denser than a typical '‹ September 2026 ›' calendar header, and `size` was simply omitted since Select doesn't support it.
- DatePicker: styles.monthTitleSize/monthTitleWeight have no matching anatomy part — the visible month/year text lives inside the composed, never-restyled Select (no 'monthTitle' element exists), so these two override hooks are defined on the root for API-contract completeness but currently have no visual effect. Docs should either add a monthTitle anatomy part or drop the two tokens.
- DatePicker: yearSelect's option range isn't specified anywhere in the schema; defaulted to (current year − 100) … (current year + 50) when min/max are absent, clamped to min/max's year when given.
- DatePicker: wrapped the composed calendar content (Today/Clear/prev/next Buttons, month/year Selects) in `<FormContext.Provider value={null}>` so they don't auto-register as spurious fields or double-inherit `disabled` from an ambient enclosing Form — not called out anywhere in the spec but necessary given how FormContext propagates through composition.
- DatePicker: the range keyboard model registers the field with the Form twice (`name` and `${name}-end`), mirroring the Lit platform note ('two entries for a range, name and name-end'), since the web FormContext's FormFieldValue type has no way to carry a `{start,end}` object.
- DatePicker: Escape's 'returns focus to the calendar button' (expect: focus-trigger) is delegated entirely to the composed Popover + FocusScope's generic restore-to-previously-focused-element behavior. That matches exactly when the calendar was opened by clicking the button, but when opened via ArrowDown from the input, focus restores to the input instead — Popover's cloneElement(trigger) claims the sole ref slot and doesn't expose the trigger node back to the composing component, so DatePicker has no way to pre-focus the button before opening.
- DatePicker: onChange is only fired once a value is complete (any single-date pick, or both ends of a range) or on an explicit Clear — picking just a range's start, or typing a partial/incomplete date, updates state silently. The spec's 'fired when a complete valid date...is typed or picked' doesn't state what happens on in-progress picks, so this was the most literal reading.
- DatePicker: anatomy omits a 'weekNumber' part even though `showWeekNumbers`/`copy.weekNumber` clearly imply one; added `data-part="weekNumber"`/a week-number header cell anyway.
- DatePicker: when the locale's first day of week isn't Monday, a visual grid row spans two different ISO 8601 weeks; the week-number column shows the ISO week of the row's first (leftmost) visible day rather than resolving both.
- DatePicker: typed-date parsing is a best-effort lenient parser (digit groups split on non-digit runs, or one unbroken digit run sliced positionally by the locale pattern's field order) rather than a fully specified grammar; unusual locale separator/order combinations may not parse perfectly.
- DatePicker: no schema binding exists for the gap between label/description/field/error, or between the input(s) and the calendar button within the field row (unlike Input's `partGap`); used direct `space.1`/`space.2` tokens rather than an overridable hook.
- DatePicker: `calendarSurface` has no separate surface to paint on web — the calendar renders inside the composed Popover's own (locked) panel background, so this binding is realized by Popover's surface rather than DatePicker's own CSS.
- DatePicker.stories: there is no controlled `open` prop in the schema (unlike Popover/Menu), so the `Keyboard` story force-opens the calendar with a mount-time programmatic click on the trigger button inside a small render-function wrapper, instead of an `open` arg.

## 2026-09-10 19:08 — round 1

- DatePicker (existing implementation) was missing the `size` (sm/md), `hideLabel`, and `open` (controlled) props, the `data-ds-field` field-discovery hook, and the `paddingBlockSm`/`paddingInlineSm`/`fieldGap`/`minTargetSm`/`partGap` overridable bindings entirely — added all of these to match the schema; also the month/year Selects were rendered with a visible label instead of the spec's `hideLabel size="sm"`, now fixed.
- styles.monthTitleWeight says it 'forwards to the Selects as `overrides.fontWeight`', but Select's SelectOverridableBinding has no `fontWeight` entry (only `labelWeight`, which targets Select's own label Text, not its value text). I forward `monthTitleSize` to the Select's `fontSize` override as specified, but `monthTitleWeight` has no matching Select API to forward into, so it only sets the (currently unused) root CSS hook — Select needs a `fontWeight` override binding added to its own schema for this to be realized as documented.
- keyboard model requests 'Tab cycles within the calendar: month/year controls, the grid (one tab stop, roving over days), Today, Clear' — this is native tab order through the composed Select/Button/day-grid elements as rendered, not custom-trapped; I did not add an explicit focus trap beyond what Popover already provides for a non-modal popup, since DatePicker's a11y.role is `none` (not `dialog`) and the spec doesn't list `focus-trap`/`inert-background` in `a11y.requires` for this component.

## 2026-09-16 10:08 — round 1

- DatePicker: composition says `label: Text`, but the package convention (and the doc's own 'The label is a native <label for>') makes the label a native <label>. I rendered a native <label for> styled by labelWeight, and only description and error are Text.
- DatePicker: the web notes put aria-selected on each day <button>, but aria-selected is not allowed on role=button. I put aria-selected on the <td role="gridcell"> and kept aria-current and aria-disabled on the button.
- DatePicker: in a range, it is unstated whether the days between start and end count as selected. I gave every day from start to end aria-selected and the 'selected' status in its label; only the two ends get the selected fill.
- DatePicker: calendarInset has no declared target. Popover's own inset has the same default token (layout.inset.md), so I forward calendarInset to Popover overrides.inset only when it is overridden. That is a forward the Parts section does not list.
- DatePicker: calendarButton, prevMonthButton, nextMonthButton, monthSelect, yearSelect, todayButton and clearButton are composed components whose root data-part is overwritten by the child (Button always writes data-part="container"). Each data-part therefore sits on a wrapper <span>, and tests click the button inside it. The doc should say how composed parts carry their data-part.
- DatePicker: the calendar is portaled out of the root, so the root's --ds-date-picker-* hooks do not reach it. I declare the hook defaults on .ds-date-picker__calendar too and write overrides inline on both. The overrides contract does not mention portaled parts.
- DatePicker: size sm calls for 'small type', but there is no fontSize binding. I added an internal --ds-date-picker-font-size (font.size.md, font.size.sm at sm) for the input and label. It is not overridable.
- DatePicker: the Keyboard section says Tab 'cycles within the calendar', but the composed non-modal Popover closes on Tab-out. I trap Tab myself on the calendar wrapper (preventDefault runs before Popover's handler) and ignore Tab from a Select's own popup. Outside click still closes through Popover, although no dismiss list is declared.
- DatePicker: the notes say the Popover 'composes FocusScope', but Popover already renders its own FocusScope (trapped=false). I did not add a second one.
- DatePicker: the validation order is error, required, unparseable. When the text is present but unparseable and there is no value, strictly following it would say 'required'. I report required only when every input is empty; otherwise unparseable text gives copy.invalid, and a partial range with required gives copy.required.
- DatePicker: rangeOrder says 'End date must be after the start date', but picking lets start equal end ('picking before the start restarts'). I allow a same-day range and report rangeOrder only when end < start.
- DatePicker: whether the first range pick shows in the inputs is unstated. It stays pending in the calendar only; the inputs, the value and onChange are untouched until the second pick, and closing discards it.
- DatePicker: whether picking the already-selected day fires onChange again is unstated. A pick always fires; typing the same date again does not.
- DatePicker: Clear 'acts immediately', but it is unstated whether it closes the calendar. It does not close, and it fires onChange(undefined) even when already empty. Today follows 'acts like clicking today's cell' (closes for a single date), and it is aria-disabled when today is outside min/max or isDateDisabled.
- DatePicker: the doc does not say what fills the year Select when only one of min/max is given, or when the shown year is outside the span. Each missing bound falls back separately to −100 or +10 years, and the list always includes the shown year.
- DatePicker: 'defaults to the document/device locale' — I read document.documentElement.lang first, then the Intl default. The pattern is formatToParts with 2-digit month and day and a numeric year, so en-US shows MM/DD/YYYY.
- DatePicker: the week-number column's cell semantics are not specified. The header is <th scope=col abbr="Week"> with visually hidden 'Week', and each row's number is <th scope="row" data-part="weekNumber">.
- DatePicker: a day's 'full date' format is unstated. I used Intl dateStyle 'full' in UTC plus ', today' and/or ', selected'. The joining punctuation is mine, not copy.
- DatePicker: the web notes and Guidance give no Form value shape for web ('valueType: date-range', and 'Form registration as Input, returning the ISO string or { start, end }' is written for RN). FormFieldValue cannot hold an object, so a range registers two string fields, name and name-end. Disabled fields submit nothing.
- DatePicker: a controlled `open` that the parent flips to true without a user action does not re-aim the calendar at the value (the month is chosen only on a user open or at mount). Focus still moves to the roving day.
- DatePicker: the 'focus on a day' keyboard rules are all 'expect: manual', so no test covers Arrow, Page, Home, End or Tab cycling. Keys held with Alt, Ctrl or Meta in the grid are ignored.

## 2026-09-17 12:58 — round 1

- DatePicker: the web guidance says the footer is a `Stack` with Today and Clear, but the `footerGap` binding styles the footer part itself and no forward to Stack's `gap` is declared; I dropped the Stack and gave the footer div `gap: var(--ds-date-picker-footer-gap)`.
- DatePicker: `open` says every change to it aims the calendar at 'the value's month', but guidance also says 'typing a complete valid date moves the calendar to it'. A range with only one end typed has no value, so which month opens is undefined; I use the committed value (the end when ArrowDown comes from the end input), else today, and ignore uncommitted typed text.
- DatePicker: 'reopening focuses the end's cell when opened from the end input' is only defined for ArrowDown; I track only ArrowDown in the end input, so clicking the calendar button after focusing the end input opens on the start.
- DatePicker: the Arrow keys 'stay put when none is left before min/max', but with no min/max and an `isDateDisabled` that disables every day there is no stopping rule; I stop searching after 3660 days (about ten years) and stay put.
- DatePicker: `dayTodayBorder` and `daySelectedBackground` are the same token (`color.control.selectedBackground`), so on a selected today the ring the doc says 'stays' can't be seen against the fill; I kept the ring and noted that it has no visible effect.
- DatePicker: `weekNumberSize` says 'at the regular weight', but there is no binding for that weight; I used `var(--font-weight-regular)` directly (not overridable).
- DatePicker: the `transition` binding lists `border-color`, but no day state changes a border (today is an inset ring); I added `border-color` to the transition list anyway, where it has no effect.
- DatePicker: the Escape keyboard rule expects 'closes, then focus-trigger', which a story with a fixed `open: true` can never show; I made the Keyboard story keep `open` in state and follow onOpenChange (as Select and Combobox do). The doc doesn't say a controlled-open Keyboard story needs this.
- DatePicker: `value: ''` is 'a controlled empty field', but the doc doesn't say what `{ start: '', end: '' }` means; I treat an empty start or end as missing.
- DatePicker: the doc doesn't say whether picking the day that's already selected, or Clear on a controlled field, should rewrite text the user typed and left unparsed; I rewrite the inputs from the (unchanged) value in that case, and otherwise wait for the controlled prop to change.

## 2026-09-21 17:22 — round 1

- DatePicker: `lineHeight` is declared overridable but has no reachable target in the composed description/errorMessage Text — the forwards list names only helperSize and fontFamily, so an override to lineHeight reaches the root, input and day cells but never the helper text. Either lineHeight needs a third forward or the doc should say it does not apply to the helper Text.
- DatePicker: the keyboard block says ArrowDown on an already-open calendar focuses 'the pending range start, else the value, else today', while the `open` prop says the end input aims at the end and everything else at the start. The two disagree for ArrowDown pressed in the end input; I preferred the end value there, so ArrowDown and reopening behave the same way from the same input.
- DatePicker: copy.gridLabel's {year} is 'the displayed year as the header shows it', but the header shows it inside a Select whose option labels the doc never specifies a format for. I used plain `String(view.year)` for both the option label and the grid label, so they always agree — this means non-Latin-digit locales get Western digits in both.
- DatePicker: weekNumberSize and weekNumberWeight carry part `weekNumber`, but no binding gives week-number cells a colour — only the prose ('in weekdayColor at weekNumberWeight') does. I styled them with the weekdayColor hook; a colour binding with part weekNumber would make that a contract rather than prose.
- DatePicker: the spec fixes every other date to UTC arithmetic on YYYY-MM-DD parts and forbids `new Date(string)`, but never says which clock 'today' reads. I used the device's local calendar date (local getters, then formatted to ISO), so the today ring and the Today button follow the user's wall clock rather than UTC.
- DatePicker: nothing says what an unparseable `min`/`max` should do. I ignore such a bound when computing the year Select's range but still compare it as a string in the day-disabled test and in tooEarly/tooLate, so a malformed bound disables days rather than throwing.
- DatePicker: `container` is not in the schema. Per the package's portal convention I added it as a platform-only prop defaulting to document.body and forwarded it to the composed Popover; it never affects semantics.
- DatePicker: the doc requires the field's focus ring, Escape dismissal and focus restore, but also says the composed Popover's FocusScope is the only one and DatePicker adds none. Escape handling and focus-restore-to-trigger are therefore entirely Popover's; DatePicker only traps Tab. If Popover's restore rule changes, DatePicker's focus-restore requirement silently changes with it.

## 2026-09-21 17:27 — round 2

- DatePicker: the Escape rule says the calendar's own Popover 'is the only FocusScope, DatePicker adds none', but Escape must also work when focus is on the calendar button or in the input — neither is inside the portaled panel, so the Popover cannot hear the key. DatePicker therefore has to own an Escape handler on its root in addition to the Popover's. The doc should say Escape is DatePicker's, like Tab, rather than implying the Popover covers it.
- DatePicker: 'returns focus to the calendar button' is unconditional, so Escape pressed while typing in the input now yanks focus out of the input onto the calendar button. I implemented it literally, but for the input case leaving focus in place would be the better behaviour and the doc should say which it wants — 'returns' reads like it is about focus the calendar took, not focus the user still has.
- DatePicker: `lineHeight` is declared overridable but has no reachable target in the composed description/errorMessage Text — the forwards list names only helperSize and fontFamily, so an override to lineHeight reaches the root, input and day cells but never the helper text. Either lineHeight needs a third forward or the doc should say it does not apply to the helper Text.
- DatePicker: the keyboard block says ArrowDown on an already-open calendar focuses 'the pending range start, else the value, else today', while the `open` prop says the end input aims at the end and everything else at the start. The two disagree for ArrowDown pressed in the end input; I preferred the end value there, so ArrowDown and reopening behave the same way from the same input.
- DatePicker: copy.gridLabel's {year} is 'the displayed year as the header shows it', but the header shows it inside a Select whose option labels the doc never specifies a format for. I used plain `String(view.year)` for both the option label and the grid label, so they always agree — this means non-Latin-digit locales get Western digits in both.
- DatePicker: weekNumberSize and weekNumberWeight carry part `weekNumber`, but no binding gives week-number cells a colour — only the prose ('in weekdayColor at weekNumberWeight') does. I styled them with the weekdayColor hook; a colour binding with part weekNumber would make that a contract rather than prose.
- DatePicker: the spec fixes every other date to UTC arithmetic on YYYY-MM-DD parts and forbids `new Date(string)`, but never says which clock 'today' reads. I used the device's local calendar date (local getters, then formatted to ISO), so the today ring and the Today button follow the user's wall clock rather than UTC.
- DatePicker: nothing says what an unparseable `min`/`max` should do. I ignore such a bound when computing the year Select's range but still compare it as a string in the day-disabled test and in tooEarly/tooLate, so a malformed bound disables days rather than throwing.
- DatePicker: `container` is not in the schema. Per the package's portal convention I added it as a platform-only prop defaulting to document.body and forwarded it to the composed Popover; it never affects semantics.

## 2026-09-23 16:20 — round 1

- DatePicker: `value` says 'on blur ... the inputs show the formatted value', but that would erase unparseable text (so `copy.invalid` could never be reported on blur-validated forms) and a typed end of an incomplete range (so the range `required` message could never fire). Chose: on blur, reformat only an input whose text parses (single), or both inputs when both parse (range); unparseable text and one-sided ranges are kept.
- DatePicker: the composition gives Popover exactly `initialFocus: none`, but the web notes and the Tab cycle (prev, month, year, next, grid, Today, Clear) assume no close button, and Popover's `dismissible` defaults to true. Chose: also pass `dismissible={false}` and `placement="bottom-start"`; the composition's `props` should list both.
- DatePicker: `locale` defaults to `document.documentElement.lang`, which cannot be read in render under the SSR rule. Chose: render first with the Intl default and switch after mount (the inputs reformat on the locale change). The Intl default itself can differ between server and browser, which can still mismatch the placeholder on hydration; the doc does not say what the server should assume.
- DatePicker: calendarInset is forwarded to Popover `inset`, so per the hook rule it declares no parent hook, while the web note says DatePicker 'declares the --ds-date-picker-* hook defaults' on the calendar content. Chose: no hooks for any forwarded binding (helperSize, monthTitleSize, monthTitleWeight, calendarInset); fontFamily keeps its hook because the field reads it too.
- DatePicker: descriptionText and errorText are locked bindings on the description/errorMessage parts, but those parts are composed Text components whose colour comes from `tone`, and restyling a child is forbidden. Chose: declare the hooks (the locked-hook rule) with nothing reading them; the Text tones realise the tokens. The doc should either forward them or say the hook is inert on web.
- DatePicker: the today-ring spec says web animates the ring's border-color and that the focus ring uses the ring's geometry, while the a11y rules require an outline on :focus-visible. Chose: the today ring is a transparent-by-default border of dayTodayBorderWidth; focus is an outline inset by focusRingWidth over the same box, and the today border goes transparent while focused. If the two width tokens ever differ, the rings no longer share a geometry.
- DatePicker: `isDateDisabled` says Arrow keys stop 'before min/max, or, with no bound in that direction, ... within 3660 days' but does not say whether the bound in the travel direction or either bound counts. Chose: the bound in the direction of travel (min for backward, max for forward); either bound still stops the step if it is crossed.
- DatePicker: `tooEarly`/`tooLate` show `{min}`/`{max}` in the input's pattern, and an unparseable bound is 'still compared as a string', but the doc gives no text for an unparseable bound in the message. Chose: fall back to the raw bound string.
- DatePicker: Escape from inside the calendar with a controlled owner that ignores onOpenChange: Popover records reason `escape` but does not close, and that stale reason is reused on a later parent-driven close (focus then returns to the button even if it had left the panel). Popover-level behaviour, not addressed here.
