// Generated by tools/behavior_tests.ts from the `behavior` block of the Heading doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Heading } from '../../packages/react/src/Heading';
import type { HeadingProps } from '../../packages/react/src/Heading';
import meta from '../../packages/react/src/Heading.stories';

function setup(given: Partial<HeadingProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Heading {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Heading"]') ?? screen.queryByRole('heading') ?? utils.container.firstElementChild) as HTMLElement,
    text: () => (screen.queryByRole('heading') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<HeadingProps>) => utils.rerender(<Heading {...props} {...next} />),
  };
  return s;
}

describe('Heading', () => {
  test('level-puts-the-heading-in-the-outline', async () => {
    const s = setup({"level": "3"});
    expect(screen.getByRole('heading')).toBeInTheDocument();
  });
  test('size-does-not-change-the-outline', async () => {
    const s = setup({"level": "2", "size": "md"});
    expect(screen.getByRole('heading')).toBeInTheDocument();
  });
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-level-1', async () => {
    const s = setup({"level": "1"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-level-2', async () => {
    const s = setup({"level": "2"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-level-3', async () => {
    const s = setup({"level": "3"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-level-4', async () => {
    const s = setup({"level": "4"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-level-5', async () => {
    const s = setup({"level": "5"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-level-6', async () => {
    const s = setup({"level": "6"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-4xl', async () => {
    const s = setup({"size": "4xl"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-3xl', async () => {
    const s = setup({"size": "3xl"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-2xl', async () => {
    const s = setup({"size": "2xl"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-xl', async () => {
    const s = setup({"size": "xl"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-lg', async () => {
    const s = setup({"size": "lg"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-md', async () => {
    const s = setup({"size": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-start', async () => {
    const s = setup({"align": "start"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-center', async () => {
    const s = setup({"align": "center"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-end', async () => {
    const s = setup({"align": "end"});
    expect(s.root()).not.toBeNull();
  });
});
