Write tools/gap_digest.py, used by regen.ps1 between phases: read generated/gaps/*.md and generated/generate.lock*.json and write generated/gaps/SUMMARY.md — one section per component, newest round first, with each gap line classified by a small heuristic into: DOC (the doc was ambiguous or silent — contains "spec does not say", "unspecified", "ambiguous", "chose", "assumed", "not stated"), CODE (a pre-existing bug the model fixed or worked around — "pre-existing", "already existed", "bug"), TOOLING ("denied", "could not run", "no node_modules", "typecheck could not"), and NOISE (repeats of an earlier line in the same file, or "no changes were needed"). Collapse NOISE, list DOC first with the doc path the fix belongs in (site/src/content/docs/components/<name>.md), and end with a checklist of components whose gates failed (from the lock: gates with false) and which gate. `--phase <name>` goes in the heading. Add tests/test_gap_digest.py with a fixture gaps folder. Do not modify anything under packages/ or generated/ other than SUMMARY.md.
