// Generated by tools/behavior_tests.ts from the `behavior` block of the Dialog doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Dialog } from '../../packages/react/src/Dialog';
import type { DialogProps } from '../../packages/react/src/Dialog';
import meta from '../../packages/react/src/Dialog.stories';

function setup(given: Partial<DialogProps> = {}) {
  const events = {
    onClose: vi.fn(),
    onOpened: vi.fn(),
  };
  const props = { ...meta.args, ...given, onClose: events.onClose, onOpened: events.onOpened };
  const utils = render(<Dialog {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Dialog"]') ?? screen.queryByRole('dialog') ?? utils.container.firstElementChild) as HTMLElement,
    scrim: () => (screen.queryByRole('dialog') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<DialogProps>) => utils.rerender(<Dialog {...props} {...next} />),
  };
  return s;
}

describe('Dialog', () => {
  test('renders', async () => {
    const s = setup({"open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-sm', async () => {
    const s = setup({"size": "sm", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-md', async () => {
    const s = setup({"size": "md", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-lg', async () => {
    const s = setup({"size": "lg", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-initial-focus-first', async () => {
    const s = setup({"initialFocus": "first", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-initial-focus-title', async () => {
    const s = setup({"initialFocus": "title", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-initial-focus-close', async () => {
    const s = setup({"initialFocus": "close", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({"open": true});
    expect(screen.getByRole('dialog', { name: s.props.heading })).toBeInTheDocument();
  });
});
