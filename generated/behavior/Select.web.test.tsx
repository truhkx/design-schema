// Generated by tools/behavior_tests.ts from the `behavior` block of the Select doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Select } from '../../packages/react/src/Select';
import type { SelectProps } from '../../packages/react/src/Select';
import meta from '../../packages/react/src/Select.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<SelectProps> = {}) {
  const events = {
    onChange: vi.fn(),
    onOpenChange: vi.fn(),
  };
  const props = { ...meta.args, ...given, onChange: events.onChange, onOpenChange: events.onOpenChange };
  const utils = render(<Select {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Select"]') ?? screen.queryByRole('combobox') ?? utils.container.firstElementChild) as HTMLElement,
    label: () => (screen.queryByRole('combobox') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<SelectProps>) => utils.rerender(<Select {...props} {...next} />),
  };
  return s;
}

describe('Select', () => {
  test('renders', async () => {
    const s = setup({"open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-sm', async () => {
    const s = setup({"size": "sm", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-md', async () => {
    const s = setup({"size": "md", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-native-auto', async () => {
    const s = setup({"native": "auto", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-native-always', async () => {
    const s = setup({"native": "always", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-native-never', async () => {
    const s = setup({"native": "never", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({"open": true});
    expect(screen.getByRole('combobox', { name: s.props.label })).toBeInTheDocument();
  });
  test('control-is-focusable', async () => {
    const s = setup({"open": true});
    act(() => (s.label()).focus());
    expect(s.label()).toHaveFocus();
  });
  test('error-is-identified', async () => {
    const s = setup({"error": "Fix this before continuing.", "open": true});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeInTheDocument();
    expect(s.label()).toHaveAttribute('aria-invalid', 'true');
  });
});
