// Generated by tools/behavior_tests.ts from the `behavior` block of the Search doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Search } from '../../packages/react/src/Search';
import type { SearchProps } from '../../packages/react/src/Search';
import meta from '../../packages/react/src/Search.stories';

function setup(given: Partial<SearchProps> = {}) {
  const events = {
    onChange: vi.fn(),
    onSubmit: vi.fn(),
    onClear: vi.fn(),
  };
  const props = { ...meta.args, ...given, onChange: events.onChange, onSubmit: events.onSubmit, onClear: events.onClear };
  const utils = render(<Search {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Search"]') ?? screen.queryByRole('searchbox') ?? utils.container.firstElementChild) as HTMLElement,
    landmark: () => (screen.queryByRole('searchbox') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<SearchProps>) => utils.rerender(<Search {...props} {...next} />),
  };
  return s;
}

describe('Search', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-md', async () => {
    const s = setup({"size": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-lg', async () => {
    const s = setup({"size": "lg"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('searchbox', { name: s.props.label })).toBeInTheDocument();
  });
  test('control-is-focusable', async () => {
    const s = setup({});
    act(() => (s.landmark()).focus());
    expect(s.landmark()).toHaveFocus();
  });
});
