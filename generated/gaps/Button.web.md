# Gaps reported while generating Button for web

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 01:06 — round 1

- Button: the schema's existing Button.tsx/css/stories predated the current spec (no overrides, no inverse, no track/onTrack, no data-ds/data-part, no loading spinner) — fully regenerated to match; flagging in case the old file was mid-migration for a reason not visible in the schema.
- Button loading: 'Replaces the icon slot with a 1em ring spinner' is singular and ambiguous about leadingIcon vs trailingIcon vs both — chose to replace only leadingIcon's slot (or show the spinner there even with no leadingIcon set) and leave trailingIcon rendering unchanged during loading.
- Button loadingSpin/spinner stroke width has no token binding in the schema — used border.width.focus for the ring thickness as the closest existing border-width token; no dedicated 'stroke width' token exists.
- Button inverse+ghost backgroundHover: schema says 'hover uses a translucent inverse foreground' but gives no token/opacity value — used color-mix(in srgb, var(--color-inverse-foreground) 16%, transparent), the sanctioned color-mix literal exception; the 16% figure is a guess with no token backing it.
- Button inverse focus ring: schema names color.inverse.focus for 'the focus ring' under `inverse` generally (not scoped to ghost like background/foreground), so it's swapped for all variants when inverse is true, not just ghost — flagging since the paragraph's phrasing is slightly ambiguous about scope.
- Added Button.test.tsx covering all 13 behavior scenarios (rollout target-by-target per repo convention seen in Card/Switch/etc.) even though the Output section only explicitly named .tsx and .stories.tsx — the Behavior scenarios section's 'emit one test per scenario' instruction and existing sibling components' *.test.tsx made this seem in-scope.

## 2026-09-10 17:21 — round 1

- Button already existed with variant/size/type/disabled/iconOnly/loading/inverse/track fully implemented; added the two schema props that were missing entirely: `accessibleName` (overrides aria-label, falls back to iconOnly's label-as-name behavior) and `overflowLabel` (plain data prop, read by Toolbar.tsx's overflow-collapse logic via element.props, not rendered by Button itself).
- Fixed a spec deviation: `loading` was not hiding `trailingIcon` per the schema ('hides trailingIcon'); trailingIcon now only renders when not loading.
- `expanded` (schema prop mapping to aria-expanded) is implemented by leaving `aria-expanded` un-omitted from the native `ComponentPropsWithoutRef<'button'>` passthrough rather than adding a dedicated `expanded` boolean prop — existing composed callers (Popover, Menu, Disclosure, SidePanel, Combobox, Search) already pass `aria-expanded` directly this way, so I kept that established convention instead of introducing a second, redundant prop.

## 2026-09-10 19:45 — round 1

- Button: schema lists `spinnerStroke` (border.width.focus) as overridable, but the existing implementation hard-coded `var(--border-width-focus)` in the spinner border with no `--ds-button-spinner-stroke` hook or entry in ButtonOverridableBinding — added the hook, default, and type union entry to match the other overridable bindings.
- Button: schema says inverse-ghost hover uses `color.inverse.foreground` at 12% over the surface, but the existing CSS mixed inverse-foreground at 16% with `transparent` rather than with `color-inverse-surface` — corrected to `color-mix(in srgb, var(--color-inverse-foreground) 12%, var(--color-inverse-surface))` to match the spec's stated formula.
- Button: schema has an `expanded` prop (aria-expanded) but Button has no explicit `expanded` boolean — it relies on `aria-expanded` flowing through `...rest` from ComponentPropsWithoutRef<'button'>, which is how Menu.tsx and Popover.tsx already set it on their trigger Buttons. Kept this existing convention rather than adding a redundant typed prop, since the schema itself says consumers rarely set it directly.

## 2026-09-12 06:10 — round 1

- Button: the home page (job 504) needs two calls to action — "Read the docs" → /docs and "View on GitHub" → the repo — and website-plan.md specifies both as `Button`s, but a Button has no destination to give: it renders a <button>, so it could only navigate from script, which would leave the landing page's two actions dead without JavaScript. components/button.md's own "Do not" section already rules this out ("Do not use a Button to navigate to another page or screen; use a Link (or a Link styled as a button) so the destination is exposed to assistive technology and works with open-in-new-tab") — and the "Link styled as a button" it names does not exist: Link has `tone: default | inherit` and no appearance prop of any kind. Shipped both CTAs as plain `Link`s, which is correct semantically and works with no client JS, at the cost of the visual weight a landing page's primary action normally carries. The schema decision this needs is either an appearance prop on Link (`variant: link | button-primary | button-secondary`, the shape Primer and Spectrum both use) or an `href` on Button that switches its rendered element to an anchor. What it should not be is page CSS in apps/website restating Button.css's fill, padding and radius: that duplicate would drift from the component on the first token change, and it is exactly the ad-hoc styling the job forbids.
- Button: website-plan.md's home-page section asks for the secondary CTA as `variant="outline"`, which is not one of Button's variants (primary / secondary / ghost / danger) and appears nowhere in components/button.md. If a bordered, low-fill emphasis is wanted it is a new variant in the schema, with its own color pairs and its own contrast proof, not an alias for `secondary`. Nothing was substituted here, since the CTA ended up a Link either way.

## 2026-09-16 03:43 — round 1

- Button: `expanded` is declared as a schema prop, but platforms.web notes say "`expanded` is not a separate React prop here" while behavior scenario `expanded-is-reported` passes `expanded: true` as a prop override. Direct contradiction. Chose to add the prop (mapped to aria-expanded, deferring to `...rest`'s aria-expanded when the prop is undefined) so the scenario is renderable; the note's claim that disclosing parents set it through rest still holds.
- Button: onTrack's argument shape is undeclared. `events.onTrack` has no `payload` in the schema, the extension prose says "fires onTrack with the same pair", and the behavior scenario uses an object `with: {name, label}`. tools/behavior_tests.ts therefore emits the object fallback for web (`onTrack({name,label}, expect.anything())`), which is the Lit detail shape — job 638 states the fields are positional on web/RN/SwiftUI. Implemented positional `onTrack(name, label)` per the prose and the shipped API; `generated/behavior/Button.web.test.tsx > press-tracks` stays red until button.md declares `events.onTrack.payload: [{name: name, type: string}, {name: label, type: string}]`.
- Button: copy.loading has no declared placement on web. platforms.web.attributes lists only type/aria-disabled/aria-busy/aria-label, and only SwiftUI says where the string goes (accessibilityValue). Rendering it as visible or as name-contributing hidden text would change the accessible name from "Create account" to "Create account Loading". Chose an aria-hidden visually-hidden node referenced by aria-describedby (the accname-spec path for hidden-but-referenced text), merged with any caller-supplied aria-describedby.
- Button: `spinnerStroke` is listed under Locked in the overrides contract but was in the shipped `ButtonOverridableBinding` union. Removed it from the type (the CSS hook `--ds-button-spinner-stroke` still exists and consumers can set it from their own CSS). This is a breaking type change for anyone passing `overrides={{ spinnerStroke: … }}`.
- Button: the package convention says `...rest` never forwards `style`/`className` and that overrides are the only per-instance styling, but every existing component in packages/react/src (Icon, Meter, and the shipped Button) accepts and merges both. Kept the merge for consistency rather than making Button the only component that rejects className/style; the convention needs a package-wide migration, not a one-file divergence.
- Button: the `icon-only-in-a-toolbar` example's `given` is `{label, iconOnly, variant, size}` with no leadingIcon, so the story renders a button with no visible content. Kept the args exactly as given per the "exactly its given as args" rule; the example should name a glyph (e.g. `leadingIcon: Icon name=close`) or the doc should say icon-only examples supply their own.
- Button: the loading spinner has no anatomy entry (anatomy is container/label/leadingIcon/trailingIcon) yet `loading` says it renders "in the leading icon slot". Gave it no `data-part` so the leadingIcon slot is never present without its prop; if a gate expects `data-part="leadingIcon"` on the spinner, the anatomy needs a `spinner` part instead.
- Button: `inverse` specifies ghost hover as "color.inverse.foreground at 12% over the surface" but 12% is a bare literal with no token behind it. Used `color-mix(in srgb, var(--color-inverse-foreground) 12%, var(--color-inverse-surface))` under the sanctioned color-mix exception; a `color.inverse.backgroundHover` token would remove the literal.
- Button: a11y.requires lists target-24px (size.target.min) but the guidance also asks for 44x44 on touch platforms, and web declares no `minTargetComfortable` binding. Applied only `--size-target-min` to min-inline-size/min-block-size; the web doc never says when the comfortable target applies.
- Button: `overflowLabel` has no web-side contract — it is accepted and deliberately unused (destructured out so it never reaches the DOM), with no statement of how Toolbar reads it off a React element (props inspection? a data attribute?). Toolbar's overflow collapse cannot currently see it.

## 2026-09-17 04:14 — round 1

- Button: onPress says 'No payload', but the web name is `onClick`, and callers such as Menu and Popover triggers expect React's native signature. I kept `onClick(event: MouseEvent<HTMLButtonElement>)` rather than a no-argument handler.
- Button: inverseHoverOpacity has `computed: times 0.25`, but the overrides contract says an entry sets the hook to `var(--token)`. It doesn't say whether the multiplier applies before or after an override. I made the hook hold the base token (`--ds-button-inverse-hover-opacity: var(--opacity-disabled)`) and apply `* 0.25` where the rule reads it, so an override also gets the multiplier.
- Button: the inverseHoverOpacity opacity is a 0–1 number, but color-mix needs a percentage. The spec gives no conversion, so I wrote `calc(calc(var(--hook) * 0.25) * 100%)`.
- Button: the conventions say `...rest` never forwards `className`/`style`, but ButtonProps still extends button props that include them. I drop them silently rather than removing them from the type, which could break callers; there's no dev warning either. The doc should say which.
- Button: a disabled parent Form disables the button through `useFormContext().disabled`. That field isn't in the Form contract the digest gives (`{ name, label, id, getValue, validate, focus }`); I used the existing field.
- Button: the spinner 'takes the leadingIcon position' but 'carries no part name'. I rendered it without a data-part, in place of the leadingIcon wrapper, so there's no leadingIcon part while loading.
- Button: the `icon-only-in-a-toolbar` example gives `leadingIcon: "The close Icon"` as prose. The story renders `<Icon name="close" inline />`.
- Button: there's no `Keyboard` story because Button has no `keyboard` block; activation is native, as the scenarios say.
- Button: demo-brand/src/CtaButton.tsx still lists `backgroundHover` as overridable, which the schema now locks. I didn't touch it; `pnpm demo:naming` may need a rerun.

## 2026-09-18 18:09 — round 1

- Button: the doc gives no web CSS for the loading ring's round shape, the spin direction or the full turn. The existing CSS uses `--radius-full` and `rotate(360deg)`, and I kept both. The doc could name the radius token and say whether a one-turn rotation is a sanctioned literal.
- Button: `backgroundHover` is declared for `state: hover` but says nothing about disabled or loading. I kept the existing choice: no hover fill while `aria-disabled` or `aria-busy` is set.
- Button: the Overrides section gives every binding a hook, locked ones included, but the Lit note says locked bindings get no hook, with spinnerStroke the only exception. On web I kept hooks for locked bindings, left out of the `overrides` type. The doc should say whether web follows Lit here.
- Button: the copy.loading node is visually hidden and also `aria-hidden`, because aria-describedby still resolves hidden nodes. The doc doesn't say whether the node should be aria-hidden, which would stop a screen reader's virtual cursor reading it inline.
- Button: the doc says `iconOnly` padding is equal on every side (`space.sm`). The CSS sets only paddingInline to space.sm, which matches because paddingBlock is already space.sm. An override of paddingBlock or paddingInline then makes the sides unequal again. The doc doesn't say which should win.
- Button: `type: submit` with `iconOnly` or `loading`: the doc doesn't say whether a blocked submit should also stop Form's native submit event beyond preventDefault(). I kept preventDefault plus stopPropagation.
- Button: `accessibleName` must contain the visible label (WCAG 2.5.3), but the doc asks for no development warning when it doesn't, so I added none.
- Button: in the stories, the `given` value `leadingIcon: "Icon name=close"` is shorthand, not a literal string. I rendered it as `<Icon name="close" inline />`. The doc should define how content values in `given` map to elements.

## 2026-09-18 18:16 — round 2

- Button: the round-2 failures are all in other components. keyboard-run fails in ActionSheet, Combobox, DatePicker, Listbox, Menu, Search, Slider, Tabs, Toast, Tooltip and Tree; axe fails in Carousel, Feed, Listbox, Slider, Splitter, Menu and Tabs. Button's spec has no keyboard block, so keyboard-run has no Button test, and logs/playwright.json has no Button/React axe entry in light or dark mode. I changed no Button code because the gates name nothing to fix in it. The gate output could say which components each failure belongs to, so a single-component round isn't sent failures from the whole package.
- Button: the gate report gives no pre-change baseline, so from the report alone I can't tell whether these failures predate this generation. I also didn't check that the gates ran against a Storybook rebuilt after the round-1 Button edits.

## 2026-09-18 18:23 — round 3

- Button: rounds 2 and 3 got the same package-wide gate failures, none in Button. keyboard-run fails in ActionSheet, Combobox, DatePicker, Listbox, Menu, Search, Slider, Tabs, Toast, Tooltip and Tree; axe fails in Carousel, Feed, Listbox, Slider, Splitter, Menu and Tabs. Button's diff is one CSS line with the same result, active only on inverse-ghost hover, plus a type-only change to ButtonProps. The React package typechecks cleanly, so no composing component passes className or style. The gates need to run on the regenerated component's own stories and specs, or diff against a pre-generation baseline, so a single-component round isn't sent failures it can't fix.
- Button: eight of the failing components compose Button (Splitter, Tree, Carousel, DatePicker, Search, ActionSheet, Menu, Toast). Their failures are roving focus, Escape handling, tab order, required children, target size, a focusable scroll region and dark-mode contrast in their own markup, none in Button's contract. I didn't edit them, since this job covers Button only.

## 2026-09-19 08:40 — round 1

- Button: the Overrides section says 'Rules always read the hook, never the token directly' and gives every binding a hook, but platforms.web.notes says locked bindings get no --ds-button-* hook and are read directly (spinnerStroke excepted). I followed the platform notes, since the Declared-contracts precedence and the Lit parity argument favour them; the generic Overrides prose should say that locked bindings are the exception.
- Button: iconOnly says paddingInline takes the resolved paddingBlock, but the Overrides section only describes hooks set per modifier class, which an inline paddingInline override would beat. I wrote the rule as `padding-inline: var(--ds-button-padding-block)` on .ds-button--icon-only; the doc could state the CSS shape directly.
- Button: spinnerStroke says 'one quarter transparent' for web but only names the edge for rn (the top border). I made the block-start border transparent on web to match rn; the doc doesn't say which edge web uses.
- Button: inverse focus ring: the doc says the ring uses color.inverse.focus for every variant but not whether that should be a separate rule or a swap of the focusRing hook, now that locked bindings have no hook. I used `.ds-button--inverse:focus-visible { outline-color: var(--color-inverse-focus) }`.
- Button: outline-offset isn't specified by any binding. I kept `outline-offset: var(--border-width-focus)`, reading the locked focusRingWidth token directly.
- Button: removing the locked hooks leaves packages/react/demo-brand/renames.json and DIFF.md referencing hooks that no longer exist; `pnpm demo:naming` must be rerun (outside this job's write scope).

## 2026-09-19 08:50 — round 2

- Button: round-2 gate failures come from other components, not Button. The axe failures are Carousel, Feed, Listbox, Menu, Slider, Splitter and Tabs, and all 22 Button/React stories pass axe in light and dark. The 15 keyboard-web failures are in other components' specs, and Button has no keyboard block. Whole-Storybook gates attribute this debt to whichever component the job regenerated; the gate should be scoped to the job's component or measured against a baseline.
- Button: keyboard gate: generated/keyboard/Search.web.spec.ts looks for role=searchbox, but the Search Keyboard story renders a combobox because it has suggestions. That is a mismatch between Search's spec and its story, not something Button can fix.
- Button: keyboard gate: the Tooltip spec finds its trigger by `[aria-haspopup], [aria-expanded], [aria-controls]`. A tooltip trigger is a plain Button with only aria-describedby, which Button's spec says reports no expanded state when `expanded` is undefined, so the selector can never match. The generator's trigger locator for Tooltip should use aria-describedby. I left Button to its spec (no aria-expanded when expanded is undefined).
- Button: I couldn't run the gate against HEAD's Button.css for comparison (the sandbox refused the file swap); logs/button-keyboard-ab.mjs does it if the owner wants the before/after evidence.

## 2026-09-19 08:56 — round 3

- Button: round 3 got the same gate output as round 2. All axe failures are in Carousel, Feed, Listbox, Menu, Slider, Splitter and Tabs, and the fresh logs/playwright.json has no Button/React entry. All 15 keyboard-web failures are in other components' specs, and Button has no keyboard block. Nothing in Button can fix them without breaking the spec, so the gates need to check only the job's component or compare against a baseline, or the round loop will not converge.
- Button: keyboard gate: the Tooltip spec finds its trigger by `[aria-haspopup], [aria-expanded], [aria-controls]`. A tooltip trigger is a Button with only aria-describedby, and Button's spec says it reports no expanded state when `expanded` is undefined. The trigger locator for Tooltip should use aria-describedby. Button stays to its spec.
- Button: keyboard gate: the Search spec looks for role=searchbox, but the Search Keyboard story renders a combobox because it has suggestions. That is Search's own spec-versus-story mismatch.

## 2026-09-20 01:07 — round 1

- Button: the spec fixes the focus ring's color and width but says nothing about outline-offset; the file uses `outline-offset: var(--border-width-focus)` so the ring clears the fill without introducing a literal.
- Button: no cursor is specified for any state; the file uses `pointer`, `not-allowed` under aria-disabled and `progress` under aria-busy. These are invisible to every gate, so if they are meant to be uniform across components they need saying somewhere.
- Button: spinnerStroke describes the ring as 'a spinnerSize circle with one quarter transparent' without saying which quarter or how it is drawn on web; implemented as a 4-sided border with `border-block-start-color: transparent`, i.e. the top quarter, so the rotation reads as starting from 12 o'clock.
- Button: `iconOnly` is not said to suppress the `label` part entirely — only to hide the visible label. The file renders no `data-part="label"` node at all while iconOnly, relying on aria-label for the name; an alternative reading would keep the part as a visually-hidden node. Any gate that asserts a component's anatomy parts are all present would disagree with the choice made.
- Button: `backgroundHover` is documented for ':hover and :active' but the rule that must exclude it while disabled or loading is left to the implementation; the file gates every variant's hover rule on `:not([aria-disabled='true']):not([aria-busy='true'])`, which couples the styling to the two ARIA attributes rather than to the props.
- Button: `size` interpolates both `space.{size}` and `font.size.{size}`, which silently assumes the space and font scales are keyed sm/md/lg exactly like the size enum. They are today, but nothing in the schema declares that coupling, so a theme that renames a space step breaks paddingInline with no gate failure.
- Button: the enum-value stories need a label, and the spec only fixes args for the four named examples; VariantSecondary/Ghost/Danger and TypeSubmit therefore carry invented labels ('Cancel', 'Forgot password?', 'Delete file', 'Sign in') chosen to illustrate the variant's guidance.
- Button: the Inverse story needs an inverse surface to sit on, which no example provides; it uses a decorator with `background: var(--color-inverse-surface)`. A decorator that adds no focusable element is safe for the keyboard gate, but the spec never sanctions story-level styling, so this is the one place a story paints outside the component.

## 2026-09-20 01:20 — round 2

- Button: NO FILES WERE MODIFIED THIS ROUND. Neither reported gate failure is attributable to Button — the keyboard gate has no Button spec at all (no `keyboard` block in the schema), and the axe gate's full light and dark lists contain zero `Button/React/*` entries. A scoped rerun of the axe gate's own loop over all 22 Button/React stories in both modes produced zero violations.
- Button: GATE SCOPING — `axe` and `keyboard-run` are whole-Storybook gates, so a round-2 prompt attributes every other component's pre-existing debt to whichever component was just generated. The failures belong to Carousel (scrollable-region-focusable), Feed and Listbox (aria-required-children), Splitter (target-size), Slider (color-contrast), Form (target-size on composed Links), plus Menu/Popover/RadioGroup/Search/SegmentedControl/Slider/Table/Tabs/Toast/Tooltip/Tree in the keyboard gate. These gates need per-component filtering before their output can be used as a generation signal.
- Button: `Form/React/Failed Submit` target-size resolves to two `data-ds="Link"` error-summary links at 1222x18 with 20px safe space, not to a Button (the story's only Button measures 73.8x37). Link has no minTarget binding in its schema while Button does, so a Link used as an interactive error-summary target has no 24px floor to meet WCAG 2.5.8. That is a gap in Link's schema, and no amount of Button or Form code can close it.
- Button: the spec fixes the focus ring's color and width but says nothing about outline-offset; the file uses `outline-offset: var(--border-width-focus)` so the ring clears the fill without introducing a literal.
- Button: no cursor is specified for any state; the file uses `pointer`, `not-allowed` under aria-disabled and `progress` under aria-busy. These are invisible to every gate, so if they are meant to be uniform across components they need saying somewhere.
- Button: spinnerStroke describes the ring as 'a spinnerSize circle with one quarter transparent' without saying which quarter or how it is drawn on web; implemented as a 4-sided border with `border-block-start-color: transparent`, i.e. the top quarter, so the rotation reads as starting from 12 o'clock.
- Button: `iconOnly` is not said to suppress the `label` part entirely — only to hide the visible label. The file renders no `data-part="label"` node while iconOnly, relying on aria-label for the name; a gate asserting that every anatomy part is present would disagree with that choice.
- Button: `backgroundHover` is documented for ':hover and :active' but the rule that must exclude it while disabled or loading is left to the implementation; the file gates every variant's hover rule on `:not([aria-disabled='true']):not([aria-busy='true'])`, coupling the styling to the two ARIA attributes rather than to the props.
- Button: `size` interpolates both `space.{size}` and `font.size.{size}`, silently assuming the space and font scales are keyed sm/md/lg exactly like the size enum. They are today, but nothing declares that coupling, so a theme that renames a space step breaks paddingInline with no gate failure.
- Button: the enum-value stories need a label and the spec only fixes args for the four named examples, so VariantSecondary/Ghost/Danger and TypeSubmit carry invented labels ('Cancel', 'Forgot password?', 'Delete file', 'Sign in') chosen to illustrate each variant's guidance.
- Button: the Inverse story needs an inverse surface to sit on, which no example provides; it uses a decorator with `background: var(--color-inverse-surface)`. The spec never sanctions story-level styling, so this is the one place a story paints outside the component.

## 2026-09-20 01:33 — round 3

- Button: NO FILES MODIFIED — third consecutive round in which neither gate failed on Button. Scoped rerun of the axe gate's own loop over all 22 Button/React stories in light and dark: zero violations. The keyboard gate contains no Button spec at all (no `keyboard` block in Button's schema), so it runs zero Button tests.
- GATE SCOPING: `axe` and `keyboard-run` are whole-Storybook gates, so every round attributes eleven other components' pre-existing debt to whichever component was just generated. Round 3's axe output is byte-identical to round 2's. Until these gates are filtered per component, their output cannot function as a generation signal.
- GATE FLAKINESS: `keyboard-run` reported 29 passed in round 2 and 53 passed in round 3 from an unchanged tree, with a different failing set each time (Combobox, DatePicker, Dialog, FocusScope, Select, SidePanel, Stepper appeared; RadioGroup, SegmentedControl, Popover disappeared). The gate is timing-dependent and its failures are not reproducible.
- Carousel: SPEC vs GATE CONFLICT, unfixable in code. axe `scrollable-region-focusable` demands the scrolling `.ds-carousel__viewport` be keyboard-reachable, but generated/keyboard/Carousel.json defines Tab as 'Moves through the controls (play/pause, previous, next, picker) and then into the current slide's focusable content' — the viewport is deliberately not a tab stop. Adding tabIndex=0 would contradict the documented keyboard model. The doc must decide: either the viewport becomes a documented tab stop, or the rule is opted out per story.
- Splitter: SPEC vs GATE CONFLICT, axe blind spot. The real pointer target is `max(handleSize, minTarget)` drawn on `.ds-splitter__separator::before` (Splitter.css:121-138) and genuinely meets 24px, but axe's target-size measures the separator element's own box (`--space-1`) and cannot see pseudo-element hit areas. Satisfying axe means sizing the element itself and moving `separatorColor` onto a pseudo-element — an anatomy inversion only Splitter's doc can authorise.
- Slider/SYSTEMIC: `aria-disabled` + `opacity.disabled` is a documented cross-component rule (Button's platform notes mandate aria-disabled so the control stays focusable), but axe exempts only native `[disabled]` from color-contrast and therefore flags every aria-disabled control that dims. Slider/React/Disabled is the first to trip it; any dimmed control can. The system needs a documented position: either a disabled-state foreground token that survives the dim, or a standing per-story opt-out.
- Feed: REAL BUG, not Button's. `role="feed"`'s first direct child is `<div role="status">` (Feed.tsx:444-448); `feed` requires `article` children and `status` is not one, so all 11 Feed stories fail aria-required-children. Fix is to render the new-items live region outside the role="feed" element, which requires Feed's doc to place `newItemsButton` outside the feed container.
- Listbox: REAL BUG, not Button's. The empty state renders `<Text data-part="emptyState">` inside `role="listbox"` (Listbox.tsx:629,643) with no role="option" children, so only Empty and Empty With Message fail. Fix is to render `emptyState` as a sibling of the listbox element; Listbox's doc must state that nesting.
- Link: Form/React/Failed Submit's two target-size nodes are `data-ds="Link"` error-summary links at 1222x18 with 20px safe space. Button declares a `minTarget` binding and meets the floor (measured 73.8x37 in the same story); Link declares none, so an interactive Link has no 24px floor for WCAG 2.5.8. Link's schema needs a minTarget binding.
- Button: the spec fixes the focus ring's color and width but says nothing about outline-offset; the file uses `outline-offset: var(--border-width-focus)` so the ring clears the fill without introducing a literal.
- Button: no cursor is specified for any state; the file uses `pointer`, `not-allowed` under aria-disabled and `progress` under aria-busy. Invisible to every gate, so if these are meant to be uniform across components it needs saying somewhere.
- Button: spinnerStroke describes the ring as 'a spinnerSize circle with one quarter transparent' without saying which quarter or how it is drawn on web; implemented as a 4-sided border with `border-block-start-color: transparent`, so the rotation reads as starting from 12 o'clock.
- Button: `iconOnly` is said to hide the visible label but not to suppress the `label` part entirely; the file renders no `data-part="label"` node while iconOnly, relying on aria-label for the name. A gate asserting every anatomy part is present would disagree.
- Button: `backgroundHover` is documented for ':hover and :active' but the rule excluding it while disabled or loading is left to the implementation; the file gates each variant's hover rule on `:not([aria-disabled='true']):not([aria-busy='true'])`, coupling styling to ARIA attributes rather than props.
- Button: `size` interpolates both `space.{size}` and `font.size.{size}`, silently assuming the space and font scales are keyed sm/md/lg like the size enum. Nothing declares that coupling, so a theme renaming a space step breaks paddingInline with no gate failure.
- Button: enum-value stories need labels and the spec only fixes args for the four named examples, so VariantSecondary/Ghost/Danger and TypeSubmit carry invented labels ('Cancel', 'Forgot password?', 'Delete file', 'Sign in').
- Button: the Inverse story needs an inverse surface that no example provides; it uses a decorator with `background: var(--color-inverse-surface)`. The spec never sanctions story-level styling, so this is the one place a story paints outside the component.

## 2026-09-20 11:03 — round 1

- Button: iconGap's token is `space.2` while every other spacing binding uses the t-shirt scale (`space.{size}`, `space.sm`) — two naming conventions inside one styles block. Resolved it literally as var(--space-2) and did not try to map it onto a named step.
- Button: backgroundHover declares `state: hover`, but its own description says `:hover` and `:active`. Implemented the description — `:is(:hover, :active)`, suppressed under [aria-disabled='true'] and [aria-busy='true'].
- Button: the focus ring's outline-offset is unspecified — the spec fixes only colour (color.border.focus) and width (border.width.focus). Chose outline-offset: var(--border-width-focus) so the ring clears the fill without introducing a literal.
- Button: spinnerStroke is listed as locked (so, by the overrides contract, no `--ds-button-*` hook) and then named as the one binding that keeps its hook. Two statements that read as a contradiction until the binding description resolves them; emitted the hook but kept spinnerStroke out of ButtonOverridableBinding.
- Button: with `iconOnly`, the label part is not rendered at all — no hidden `data-part="label"` node. The spec says the visible label is hidden and becomes the accessible name, but never says whether the part hook should survive for a consumer selecting on it.
- Button: cursor is not a binding and the spec never mentions it. Chose `pointer`, `not-allowed` while disabled, `progress` while loading.
- Button: disabledOpacity is 'applied to the whole button', which dims the focus ring along with the fill. The doc does not rule on whether the 2.4.11 ring should be exempt from the dim; left it dimmed as written.
- Button: platform notes say className/style are omitted from ButtonProps so passing either 'is a type error rather than a silent drop', but say nothing about an untyped spread at runtime. Kept a runtime strip of both so the overrides contract also holds for JS callers.
- Button: the Accessibility prose says the target is '44×44 on touch platforms', while a11y.requires lists only target-24px and the minTarget description says web and Lit apply only the min floor with no coarse-pointer rule. Followed the binding description — one floor, size.target.min, no pointer media query — but the prose still contradicts it.
- Button: scenario `loading-announces-busy-and-ignores-activation` asserts the button 'keeps its height and its label in view'. jsdom has no layout, so that half of the scenario is not expressible on this platform; the test asserts the blocked press and aria-busy only.
- Button: the hover rules gate on [aria-busy='true'], which assumes `loading` is the only source of aria-busy. The spec does not say whether a caller's own aria-busy arriving through ...rest should also suppress the hover fill; as written it does.

## 2026-09-20 11:11 — round 2

- Button: round 2 required no code change — both failing gates are whole-Storybook sweeps and neither contains a Button failure. Keyboard: Button has no `keyboard` block so no Button spec is generated at all; all 15 failures are other components' specs. Axe: the untruncated lists (test-results/*/error-context.md) name only Carousel, Feed, Listbox, Slider, Splitter; all 22 Button/React stories pass in both modes, and every target-size node resolves to a Link, not a Button. The gate report attributes another component's debt to the component being generated.
- Link (not Button, found while attributing the axe failures): Link sets no min-inline-size/min-block-size, so inline links measure 374x18 and fail WCAG 2.5.8 target-size wherever they appear — 40 nodes across Splitter's stories in both modes. Link's schema has no minTarget binding to implement, unlike Button's size.target.min floor; this needs a decision in Link's doc (a floor, or a documented opt-out for inline text links, which 2.5.8's inline exception would justify).
- Button: iconGap's token is `space.2` while every other spacing binding uses the t-shirt scale (`space.{size}`, `space.sm`) — two naming conventions inside one styles block. Resolved it literally as var(--space-2) and did not try to map it onto a named step.
- Button: backgroundHover declares `state: hover`, but its own description says `:hover` and `:active`. Implemented the description — `:is(:hover, :active)`, suppressed under [aria-disabled='true'] and [aria-busy='true'].
- Button: the focus ring's outline-offset is unspecified — the spec fixes only colour (color.border.focus) and width (border.width.focus). Chose outline-offset: var(--border-width-focus) so the ring clears the fill without introducing a literal.
- Button: spinnerStroke is listed as locked (so, by the overrides contract, no `--ds-button-*` hook) and then named as the one binding that keeps its hook. Two statements that read as a contradiction until the binding description resolves them; emitted the hook but kept spinnerStroke out of ButtonOverridableBinding.
- Button: with `iconOnly`, the label part is not rendered at all — no hidden `data-part="label"` node. The spec says the visible label is hidden and becomes the accessible name, but never says whether the part hook should survive for a consumer selecting on it.
- Button: cursor is not a binding and the spec never mentions it. Chose `pointer`, `not-allowed` while disabled, `progress` while loading.
- Button: disabledOpacity is 'applied to the whole button', which dims the focus ring along with the fill. The doc does not rule on whether the 2.4.11 ring should be exempt from the dim; left it dimmed as written.
- Button: platform notes say className/style are omitted from ButtonProps so passing either 'is a type error rather than a silent drop', but say nothing about an untyped spread at runtime. Kept a runtime strip of both so the overrides contract also holds for JS callers.
- Button: the Accessibility prose says the target is '44x44 on touch platforms', while a11y.requires lists only target-24px and the minTarget description says web and Lit apply only the min floor with no coarse-pointer rule. Followed the binding description — one floor, size.target.min, no pointer media query — but the prose still contradicts it.
- Button: scenario `loading-announces-busy-and-ignores-activation` asserts the button 'keeps its height and its label in view'. jsdom has no layout, so that half of the scenario is not expressible on this platform; the test asserts the blocked press and aria-busy only.
- Button: the hover rules gate on [aria-busy='true'], which assumes `loading` is the only source of aria-busy. The spec does not say whether a caller's own aria-busy arriving through ...rest should also suppress the hover fill; as written it does.

## 2026-09-20 11:19 — round 3

- Button: round 3 required no code change, as in round 2 — neither failing gate contains a Button test. Button has no `keyboard` block so no Button keyboard spec is generated at all; the axe dumps (fresh, mtime 15:17 today) hold zero Button entries in either mode; all 22 Button/React stories pass axe in both modes; Button's five files are byte-identical to HEAD. Routing these whole-Storybook gates to a per-component generation job cannot converge and misattributes other components' debt to the component being generated.
- Keyboard gate (harness, not a component): the 17 failures are two different problems and the gate does not distinguish them. Re-running FocusScope and Tree serially with repeatEach=2 shows FocusScope passing 3/3 both repeats — its 2 failures are contention flake under fullyParallel, which is why the count moved 15->17 between rounds — while Tree's ArrowUp and Home fail deterministically both repeats. The gate needs the flaky specs serialized (or a retry policy) so a real regression is distinguishable from parallelism noise; until then the failing set is unstable between runs.
- Tree (not Button, found while splitting flake from debt): ArrowUp lands on index 2 when index 1 is expected, and Home lands on index 2 when 0 is expected — reproducible serially, twice. Tree's roving-tabindex bookkeeping is not moving focus to the computed node; this is real debt, not flake.
- Link (not Button, found in round 2 while attributing the axe failures): Link sets no min-inline-size/min-block-size, so inline links measure 374x18 and fail WCAG 2.5.8 target-size wherever they appear — all 40 flagged target-size nodes across Splitter's stories in both modes resolve to a Link, none to a Button. Link's schema has no minTarget binding, unlike Button's size.target.min floor; this needs a decision in Link's doc (a floor, or a documented opt-out for inline text links, which 2.5.8's inline exception would justify).
- Button: iconGap's token is `space.2` while every other spacing binding uses the t-shirt scale (`space.{size}`, `space.sm`) — two naming conventions inside one styles block. Resolved it literally as var(--space-2) and did not try to map it onto a named step.
- Button: backgroundHover declares `state: hover`, but its own description says `:hover` and `:active`. Implemented the description — `:is(:hover, :active)`, suppressed under [aria-disabled='true'] and [aria-busy='true'].
- Button: the focus ring's outline-offset is unspecified — the spec fixes only colour (color.border.focus) and width (border.width.focus). Chose outline-offset: var(--border-width-focus) so the ring clears the fill without introducing a literal.
- Button: spinnerStroke is listed as locked (so, by the overrides contract, no `--ds-button-*` hook) and then named as the one binding that keeps its hook. Two statements that read as a contradiction until the binding description resolves them; emitted the hook but kept spinnerStroke out of ButtonOverridableBinding.
- Button: with `iconOnly`, the label part is not rendered at all — no hidden `data-part="label"` node. The spec says the visible label is hidden and becomes the accessible name, but never says whether the part hook should survive for a consumer selecting on it.
- Button: cursor is not a binding and the spec never mentions it. Chose `pointer`, `not-allowed` while disabled, `progress` while loading.
- Button: disabledOpacity is 'applied to the whole button', which dims the focus ring along with the fill. The doc does not rule on whether the 2.4.11 ring should be exempt from the dim; left it dimmed as written.
- Button: platform notes say className/style are omitted from ButtonProps so passing either 'is a type error rather than a silent drop', but say nothing about an untyped spread at runtime. Kept a runtime strip of both so the overrides contract also holds for JS callers.
- Button: the Accessibility prose says the target is '44x44 on touch platforms', while a11y.requires lists only target-24px and the minTarget description says web and Lit apply only the min floor with no coarse-pointer rule. Followed the binding description — one floor, size.target.min, no pointer media query — but the prose still contradicts it.
- Button: scenario `loading-announces-busy-and-ignores-activation` asserts the button 'keeps its height and its label in view'. jsdom has no layout, so that half of the scenario is not expressible on this platform; the test asserts the blocked press and aria-busy only.
- Button: the hover rules gate on [aria-busy='true'], which assumes `loading` is the only source of aria-busy. The spec does not say whether a caller's own aria-busy arriving through ...rest should also suppress the hover fill; as written it does.

## 2026-09-20 12:43 — round 1

- Button: `spinnerSize` describes the ring as "1em of the label font" but its token is `font.size.{size}`, and it is explicitly its own binding so a `fontSize` override must not move it. On web those two only agree while fontSize is untouched. Used `var(--ds-button-spinner-size)` defaulting to the per-size `font.size.{size}` token, so a fontSize override leaves the ring alone — the binding contract wins over the "1em" prose.
- Button: `inverseHoverOpacity` is given twice and differently — the Style bindings section says computed `calc(var(--opacity-disabled) * 0.25)` (token read directly), the schema description says `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)` (hook read, rule multiplies). Chose the hook form, since inverseHoverOpacity is overridable and the doc states "the hook holds the base token and the rule that reads it multiplies"; the direct-token form would make the override a no-op.
- Button: `backgroundHover` declares `state: hover`, but its description says the fill covers "`:hover` and `:active`". Applied both via `:is(:hover, :active)`. The bindings contract says a binding styles only its declared `state`, so the two disagree.
- Button: `label` is listed in `anatomy` and rendered with `data-part="label"`, but `iconOnly` hides the visible label entirely, so the part element disappears from the DOM. The parts contract says overrides change values and never presence, and never says a prop may remove an anatomy part. Rendered no label element when `iconOnly` (the name moves to `aria-label`); a part-based test or override targeting `label` finds nothing.
- Button: the generation rule says the accessible name is "never both empty", but no development warning is specified for an empty `label` on web — and the doc explicitly waives warnings for the two adjacent cases (Lit's `''` initial value, and `iconOnly` with no `leadingIcon`). Emitted no warning, so an empty `label` produces a nameless button silently.
- Button: the inverse ghost hover fill is specified as `inverseBackgroundHover` at `inverseHoverOpacity` "over color.inverse.surface", implemented as a `color-mix` against that token. But `ghost` has a transparent-ish fill, so the true backdrop is whatever the Toast or panel actually paints; the mix is only correct when that surface is exactly `color.inverse.surface`.
- Button: `a11y.contrast` proves `color.inverse.link` against `color.inverse.surface`, but not the ghost foreground against the inverse *hover* fill (the color-mix result). That pair is unproven by the build's contrast check.
- Button: `expanded` and an `aria-expanded` arriving through `...rest` can both be set; the doc says the rest-borne attribute "still applies" when the prop is undefined, but does not say which wins when both are present. Chose the explicit prop (`expanded ?? ariaExpanded`).
- Button: no statement on `disabled` and `loading` being true together. Emitted both `aria-disabled="true"` and `aria-busy="true"`, and the disabled dim applies while the spinner still runs.
- Button: the focus ring specifies color (`color.border.focus`) and width (`border.width.focus`) but no `outline-offset`. Used `var(--border-width-focus)` as the offset so the ring clears the button's own fill; there is no token or binding for it.
- Button: `iconGap` uses the numeric scale (`space.2`) while `paddingInline`/`paddingBlock` use the named one (`space.{size}` / `space.sm`). Both resolve, but the doc gives no rule for which scale is canonical for a given kind of spacing.
- Button: the doc says transitions are "removed under prefers-reduced-motion" without naming the media-query form. The prior generation used `@media (prefers-reduced-motion: no-preference)`, which also strips motion where the feature is unsupported; switched to `@media (prefers-reduced-motion: reduce) { transition: none }` to match every sibling component in the package.

## 2026-09-20 12:46 — round 2

- Button: the `keyboard-run` gate fails with "No tests found" because Button declares no `keyboard` block, so tools/keyboard_tests.ts:304 writes no generated/keyboard/Button.web.spec.ts and the per-component Playwright filter matches zero files. No change inside packages/react can fix this: the gate globs the generated spec, not the story, and freshOutDir deletes hand-written specs. Made no code change. The doc should add a `keyboard` block with the two native rules (Enter and Space, `expect: manual`, `native: true`) so the generator emits them as `test.skip — native`; the gate then finds a spec and reports coverage instead of failing. Verified the suite is otherwise healthy: 245 tests in 30 files, 30 of 51 components have keyboard blocks and exactly 30 specs exist.
- Button: the doc's `enter-activates` and `space-activates` scenarios say key handling is native to `<button>`, which contradicts the gate harness's apparent expectation that every generated component yields a keyboard spec. Treated the doc as correct (wrote no key handlers) and left the gate failing rather than adding handlers the doc forbids.
- Button: `spinnerSize` describes the ring as "1em of the label font" but its token is `font.size.{size}`, and it is explicitly its own binding so a `fontSize` override must not move it. On web those two only agree while fontSize is untouched. Used `var(--ds-button-spinner-size)` defaulting to the per-size `font.size.{size}` token, so a fontSize override leaves the ring alone — the binding contract wins over the "1em" prose.
- Button: `inverseHoverOpacity` is given twice and differently — the Style bindings section says computed `calc(var(--opacity-disabled) * 0.25)` (token read directly), the schema description says `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)` (hook read, rule multiplies). Chose the hook form, since inverseHoverOpacity is overridable and the doc states "the hook holds the base token and the rule that reads it multiplies"; the direct-token form would make the override a no-op.
- Button: `backgroundHover` declares `state: hover`, but its description says the fill covers "`:hover` and `:active`". Applied both via `:is(:hover, :active)`. The bindings contract says a binding styles only its declared `state`, so the two disagree.
- Button: `label` is listed in `anatomy` and rendered with `data-part="label"`, but `iconOnly` hides the visible label entirely, so the part element disappears from the DOM. The parts contract says overrides change values and never presence, and never says a prop may remove an anatomy part. Rendered no label element when `iconOnly` (the name moves to `aria-label`); a part-based test or override targeting `label` finds nothing.
- Button: the generation rule says the accessible name is "never both empty", but no development warning is specified for an empty `label` on web — and the doc explicitly waives warnings for the two adjacent cases (Lit's `''` initial value, and `iconOnly` with no `leadingIcon`). Emitted no warning, so an empty `label` produces a nameless button silently.
- Button: the inverse ghost hover fill is specified as `inverseBackgroundHover` at `inverseHoverOpacity` "over color.inverse.surface", implemented as a `color-mix` against that token. But `ghost` has a transparent-ish fill, so the true backdrop is whatever the Toast or panel actually paints; the mix is only correct when that surface is exactly `color.inverse.surface`.
- Button: `a11y.contrast` proves `color.inverse.link` against `color.inverse.surface`, but not the ghost foreground against the inverse *hover* fill (the color-mix result). That pair is unproven by the build's contrast check.
- Button: `expanded` and an `aria-expanded` arriving through `...rest` can both be set; the doc says the rest-borne attribute "still applies" when the prop is undefined, but does not say which wins when both are present. Chose the explicit prop (`expanded ?? ariaExpanded`).
- Button: no statement on `disabled` and `loading` being true together. Emitted both `aria-disabled="true"` and `aria-busy="true"`, and the disabled dim applies while the spinner still runs.
- Button: the focus ring specifies color (`color.border.focus`) and width (`border.width.focus`) but no `outline-offset`. Used `var(--border-width-focus)` as the offset so the ring clears the button's own fill; there is no token or binding for it.
- Button: `iconGap` uses the numeric scale (`space.2`) while `paddingInline`/`paddingBlock` use the named one (`space.{size}` / `space.sm`). Both resolve, but the doc gives no rule for which scale is canonical for a given kind of spacing.
- Button: the doc says transitions are "removed under prefers-reduced-motion" without naming the media-query form. Round 1 switched the inverted `no-preference` gate to `@media (prefers-reduced-motion: reduce) { transition: none }` to match every sibling component; the doc should state the required form.

## 2026-09-20 12:48 — round 3

- Button: BLOCKING — the `keyboard-run` gate cannot pass and the fix is doc-only, so it will keep failing until a doc pass applies it. Button declares no `keyboard` block, so tools/keyboard_tests.ts:304 writes no generated/keyboard/Button.web.spec.ts and the per-component Playwright filter matches zero files. Add to button.md frontmatter: `keyboard:` with two rules — `keys: [Enter]` and `keys: [' ']`, each `action: Activates the button and fires onPress.`, `from: any`, `expect: manual`, `native: true`, `platforms: [web, lit]`. keyboard_tests.ts:226 renders manual rules as `test.skip`, which DO count as found tests, so the spec file exists and the gate exits 0. `native: true` means generators implement nothing, so no component code changes. Made no code change; nothing in packages/react can create the spec (the keyboard-web project matches only generated/keyboard/*.web.spec.ts, and freshOutDir deletes hand-written specs).
- Button: the keyboard gate's outcome vocabulary (schema/component.ts:516 KEYBOARD_EXPECTS) has no value meaning "activates" or "fires its event" — it covers only closes/opens, focus movement, toggles and selects. A component whose entire keyboard model is plain activation, like Button, therefore cannot declare a machine-checkable rule and can only use `expect: manual`. This is the structural reason Button has no keyboard block, and it will recur for every activation-only control. Consider adding an `activates` expect that asserts the component's event fired.
- Button: the doc's `enter-activates` and `space-activates` behavior scenarios say key handling is native to `<button>`, which conflicts with the gate harness's expectation that every generated component yields a keyboard spec. Treated the doc as correct (wrote no key handlers, added no Keyboard story) and left the gate failing rather than adding code the doc forbids. Confirmed a Keyboard story would not help: with all rules manual, every test is skipped and the story-navigating beforeEach never runs.
- Button: `spinnerSize` describes the ring as "1em of the label font" but its token is `font.size.{size}`, and it is explicitly its own binding so a `fontSize` override must not move it. On web those two only agree while fontSize is untouched. Used `var(--ds-button-spinner-size)` defaulting to the per-size `font.size.{size}` token, so a fontSize override leaves the ring alone — the binding contract wins over the "1em" prose.
- Button: `inverseHoverOpacity` is given twice and differently — the Style bindings section says computed `calc(var(--opacity-disabled) * 0.25)` (token read directly), the schema description says `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)` (hook read, rule multiplies). Chose the hook form, since inverseHoverOpacity is overridable and the doc states "the hook holds the base token and the rule that reads it multiplies"; the direct-token form would make the override a no-op.
- Button: `backgroundHover` declares `state: hover`, but its description says the fill covers "`:hover` and `:active`". Applied both via `:is(:hover, :active)`. The bindings contract says a binding styles only its declared `state`, so the two disagree.
- Button: `label` is listed in `anatomy` and rendered with `data-part="label"`, but `iconOnly` hides the visible label entirely, so the part element disappears from the DOM. The parts contract says overrides change values and never presence, and never says a prop may remove an anatomy part. Rendered no label element when `iconOnly` (the name moves to `aria-label`); a part-based test or override targeting `label` finds nothing.
- Button: the generation rule says the accessible name is "never both empty", but no development warning is specified for an empty `label` on web — and the doc explicitly waives warnings for the two adjacent cases (Lit's `''` initial value, and `iconOnly` with no `leadingIcon`). Emitted no warning, so an empty `label` produces a nameless button silently.
- Button: the inverse ghost hover fill is specified as `inverseBackgroundHover` at `inverseHoverOpacity` "over color.inverse.surface", implemented as a `color-mix` against that token. But `ghost` has a transparent-ish fill, so the true backdrop is whatever the Toast or panel actually paints; the mix is only correct when that surface is exactly `color.inverse.surface`.
- Button: `a11y.contrast` proves `color.inverse.link` against `color.inverse.surface`, but not the ghost foreground against the inverse *hover* fill (the color-mix result). That pair is unproven by the build's contrast check.
- Button: `expanded` and an `aria-expanded` arriving through `...rest` can both be set; the doc says the rest-borne attribute "still applies" when the prop is undefined, but does not say which wins when both are present. Chose the explicit prop (`expanded ?? ariaExpanded`).
- Button: no statement on `disabled` and `loading` being true together. Emitted both `aria-disabled="true"` and `aria-busy="true"`, and the disabled dim applies while the spinner still runs.
- Button: the focus ring specifies color (`color.border.focus`) and width (`border.width.focus`) but no `outline-offset`. Used `var(--border-width-focus)` as the offset so the ring clears the button's own fill; there is no token or binding for it.
- Button: `iconGap` uses the numeric scale (`space.2`) while `paddingInline`/`paddingBlock` use the named one (`space.{size}` / `space.sm`). Both resolve, but the doc gives no rule for which scale is canonical for a given kind of spacing.
- Button: the doc says transitions are "removed under prefers-reduced-motion" without naming the media-query form. Round 1 switched the inverted `no-preference` gate to `@media (prefers-reduced-motion: reduce) { transition: none }` to match every sibling component; the doc should state the required form.

## 2026-09-20 16:06 — round 1

- Button: the `inverseHoverOpacity` contract line gives computed as `calc(var(--opacity-disabled) * 0.25)` (token read directly), but the binding's own description mandates `calc(var(--ds-button-inverse-hover-opacity) * 0.25 * 100%)` so an override is multiplied too. Kept the hook-reading form, per the description and the overrides contract; the contracts line should be corrected to match.
- Button: a11y.requires lists only `target-24px` and the `minTarget` description says web applies that floor alone with no coarse-pointer rule, but the guidance prose says '44x44 on touch platforms'. Followed the binding (single `size.target.min` floor, no pointer media query); the prose sentence reads as a contradiction on web.
- Button: anatomy lists `label` unconditionally, but `iconOnly` hides the visible label. Chose to render no `data-part="label"` element at all under `iconOnly` (rather than a visually-hidden one), since the name comes from aria-label. The doc never says whether the part should still exist.
- Button: `iconGap` is `space.2` while `paddingInline`/`paddingBlock` use the t-shirt ramp (`space.{size}`, `space.sm`). The doc never states the relationship between the numeric and t-shirt space scales, so whether `space.2` is intentionally off-ramp or a stand-in for `space.xs` is unresolved.
- Button: `loading` says the spinner 'swaps in at the same width' as a leadingIcon, but Icon has its own `size` prop and the doc never ties Icon's rendered box to the label font size. Sized the spinner purely from its own `spinnerSize` binding (`font.size.{size}`), so the no-shift claim only holds when the caller's Icon happens to render at 1em.
- Button: `accessibleName` on a NON-iconOnly button makes aria-label replace the visible text as the accessible name. The doc requires the name to contain the visible label but explicitly ships no dev warning, so nothing enforces WCAG 2.5.3 here; trusted the caller, as instructed.
- Button: the doc supplies `given` only for its four examples, so the notable-state stories (Disabled, Loading, LoadingIconOnly, WithIcons, IconOnly, Expanded, Inverse, Tracked) needed labels and icon choices invented (e.g. 'Undo' on the inverse surface, 'Add item' with plus/chevron-right). The Inverse story also needs a dark decorator to be legible, which the parts contract otherwise discourages.
- Button: `copy.loading` is rendered into an aria-hidden node referenced by aria-describedby. This is what the doc specifies and it works, but it is unusual enough that a reviewer may read it as a bug; the doc's parenthetical justification ('a description still resolves from a hidden node') is the only thing marking it as deliberate.

## 2026-09-22 02:58 — round 1

- Button: the web notes give `not-allowed` under aria-disabled and `progress` under aria-busy, but `disabled` and `loading` are explicitly allowed to be true together and no precedence is stated. The two rules have equal specificity, so source order decides; I put the aria-busy rule last, making a disabled+loading button show `progress`. The doc should say which state owns the cursor.
- Button: the parts `leadingIcon`/`trailingIcon` are declared `kind: slot`, and nothing says whether the web rendering wraps the caller's node. I wrap each in a `span[data-part]` (aria-hidden, so the glyph is decorative per the prop descriptions) rather than cloning attributes onto the caller's element, which would restyle a child. Lit gets this for free from real `<slot>` elements, so the platforms only agree by coincidence here.
- Button: `loading`'s description says the spinner swaps in at the same width as a leadingIcon, which 'holds exactly when that icon renders at the label's font size' — but Button cannot enforce that without restyling a child, and there is no prop for it. The guarantee rests entirely on callers passing `<Icon inline />`. Either the doc should state that `inline` is required of a Button's icons, or the width claim should be softened to match the `trailingIcon` case it already accepts as a shift.
- Button: the web notes say `ButtonProps` omits `className` and `style` from the native button props, but say nothing about `children`. Button renders `label`, not children, so I omitted `children` too — passing it is a type error rather than a silently dropped node. Worth stating alongside the className/style omission.
- Button: `overrides` for a locked binding is specified as 'ignored if passed', but locked bindings are absent from `ButtonOverridableBinding`, so a literal is a compile error and only an untyped spread can reach the runtime path. The runtime skip exists (no hook in the lookup table), but 'ignored' reads as if it were a supported call shape.
