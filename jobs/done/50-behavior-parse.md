Re-land the behavior-scenario validation in tools/parse.py. It existed on 2026-09-09 and was overwritten; tests/test_behavior.py describes it and is currently skipped with `skipif(not hasattr(p, "validate_behavior"))`. schema/component.schema.json and schema/component.ts now define the `behavior` block officially ($defs/behaviorScenario: name, description?, given?, when?, then[], platforms?) — validate against that shape, do not change the schema.

Implement in tools/parse.py:
1. `validate_behavior(component: dict, path: Path) -> None` raising DocError with the messages the tests expect: unknown prop in `given` ("unknown prop 'x'"), enum value not declared, `when.click`/`when.focus` naming a part that is not in `anatomy`, `then.event` naming an event not in `events`, `then.copy` naming a key not in `copy`, `then.state` outside checked|expanded|selected|disabled|invalid|pressed|open, `platforms` entries outside web|lit|rn, duplicate scenario names.
2. `derive_behavior(component: dict) -> list[dict]`: schema-derived scenarios added to the authored ones — `renders` (default props), one `renders-<prop>-<value>` per enum value of every enum prop, `has-accessible-name` when a11y.requires has accessible-name, `is-focusable` when the component has a keyboard block or role is a widget role, `error-is-identified` when error-identification is required and an `error` prop exists. Derived scenarios come first and never override an authored name.
3. Write the merged list into generated/components.json under each component as `behavior`, and into the prompt as a "## Behavior scenarios" section listing them, so the generator writes one test per scenario (the templates already say tests follow the scenarios).
4. Call validate_behavior from the existing per-doc validation path, next to the keyboard/composition checks.

Then run `pytest -q tests/test_behavior.py tests/test_parse.py tests/test_parse_checks.py` (use `py -3 -m pytest` if `pytest` is not on PATH) and make it pass; remove the skipif once the functions exist. Run `node tools/py.mjs tools/parse.py` and confirm 0 errors across all docs. Do not modify anything under packages/ or generated/ by hand.
