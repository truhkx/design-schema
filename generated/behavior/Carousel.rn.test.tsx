// Generated by tools/behavior_tests.ts from the `behavior` block of the Carousel doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Carousel } from '../../packages/rn/src/Carousel';
import type { CarouselProps } from '../../packages/rn/src/Carousel';
import meta from '../../packages/rn/src/Carousel.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<CarouselProps> = {}) {
  const events = {
    onChange: jest.fn(),
  };
  const props: CarouselProps = { ...(meta.args as CarouselProps), ...given, onChange: events.onChange };
  const tree = (p: CarouselProps) => (
    <ThemeProvider mode="light">
      <Carousel {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Carousel') ?? screen.UNSAFE_root,
    region: () => screen.queryByRole('region') ?? s.root(),
    rerender: (next: Partial<CarouselProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Carousel', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-picker-dots', () => {
    const s = setup({"picker": "dots"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-picker-tabs', () => {
    const s = setup({"picker": "tabs"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-picker-none', () => {
    const s = setup({"picker": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('region', { name: s.props.label })).toBeOnTheScreen();
  });
});
