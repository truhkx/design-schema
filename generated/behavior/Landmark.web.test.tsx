// Generated by tools/behavior_tests.ts from the `behavior` block of the Landmark doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Landmark } from '../../packages/react/src/Landmark';
import type { LandmarkProps } from '../../packages/react/src/Landmark';
import meta from '../../packages/react/src/Landmark.stories';

function setup(given: Partial<LandmarkProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Landmark {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Landmark"]') ?? utils.container.firstElementChild) as HTMLElement,
    region: () => s.root(),
    rerender: (next: Partial<LandmarkProps>) => utils.rerender(<Landmark {...props} {...next} />),
  };
  return s;
}

describe('Landmark', () => {
  test('the-role-prop-chooses-the-landmark', async () => {
    const s = setup({"role": "navigation"});
    expect(screen.getByRole('navigation')).toBeInTheDocument();
  });
  test('search-is-the-search-landmark', async () => {
    const s = setup({"role": "search"});
    expect(screen.getByRole('search')).toBeInTheDocument();
  });
  test('main-is-the-primary-content-landmark', async () => {
    const s = setup({"role": "main"});
    expect(screen.getByRole('main')).toBeInTheDocument();
  });
  test('a-region-is-named-by-its-label', async () => {
    const s = setup({"role": "region", "label": "Related articles"});
    expect(screen.getByRole('region')).toBeInTheDocument();
    expect(s.region()).toHaveAttribute("aria-label", "Related articles");
  });
  test('an-overridden-element-still-carries-its-role', async () => {
    const s = setup({"role": "banner", "as": "div"});
    expect(s.region()).toHaveAttribute("role", "banner");
    expect(screen.getByRole('banner')).toBeInTheDocument();
  });
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-role-banner', async () => {
    const s = setup({"role": "banner"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-role-navigation', async () => {
    const s = setup({"role": "navigation"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-role-main', async () => {
    const s = setup({"role": "main"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-role-complementary', async () => {
    const s = setup({"role": "complementary"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-role-contentinfo', async () => {
    const s = setup({"role": "contentinfo"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-role-region', async () => {
    const s = setup({"role": "region"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-role-search', async () => {
    const s = setup({"role": "search"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-role-form', async () => {
    const s = setup({"role": "form"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-as-header', async () => {
    const s = setup({"as": "header"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-as-nav', async () => {
    const s = setup({"as": "nav"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-as-main', async () => {
    const s = setup({"as": "main"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-as-aside', async () => {
    const s = setup({"as": "aside"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-as-footer', async () => {
    const s = setup({"as": "footer"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-as-section', async () => {
    const s = setup({"as": "section"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-as-form', async () => {
    const s = setup({"as": "form"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-as-div', async () => {
    const s = setup({"as": "div"});
    expect(s.root()).not.toBeNull();
  });
  test.skip('has-accessible-name — then.name: role from prop \'role\' cannot be queried; name the landmark/text role in the doc', async () => {});
});
