// Generated by tools/behavior_tests.ts from the `behavior` block of the Feed doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Feed } from '../../packages/rn/src/Feed';
import type { FeedProps } from '../../packages/rn/src/Feed';
import meta from '../../packages/rn/src/Feed.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<FeedProps> = {}) {
  const events = {
    onLoadMore: jest.fn(),
    onShowNew: jest.fn(),
    onItemVisible: jest.fn(),
  };
  const props: FeedProps = { ...(meta.args as FeedProps), ...given, onEndReached: events.onLoadMore, onShowNew: events.onShowNew, onViewableItemsChanged: events.onItemVisible };
  const tree = (p: FeedProps) => (
    <ThemeProvider mode="light">
      <Feed {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Feed') ?? screen.UNSAFE_root,
    container: () => screen.queryByRole('feed') ?? s.root(),
    rerender: (next: Partial<FeedProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Feed', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-2', () => {
    const s = setup({"headingLevel": "2"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-3', () => {
    const s = setup({"headingLevel": "3"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-heading-level-4', () => {
    const s = setup({"headingLevel": "4"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('feed', { name: s.props.label })).toBeOnTheScreen();
  });
});
