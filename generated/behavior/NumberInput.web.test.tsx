// Generated by tools/behavior_tests.ts from the `behavior` block of the NumberInput doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { NumberInput } from '../../packages/react/src/NumberInput';
import type { NumberInputProps } from '../../packages/react/src/NumberInput';
import meta from '../../packages/react/src/NumberInput.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<NumberInputProps> = {}) {
  const events = {
    onChange: vi.fn(),
  };
  const props = { ...meta.args, ...given, onChange: events.onChange };
  const utils = render(<NumberInput {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="NumberInput"]') ?? screen.queryByRole('spinbutton') ?? utils.container.firstElementChild) as HTMLElement,
    label: () => (screen.queryByRole('spinbutton') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<NumberInputProps>) => utils.rerender(<NumberInput {...props} {...next} />),
  };
  return s;
}

describe('NumberInput', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-format-decimal', async () => {
    const s = setup({"format": "decimal"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-format-currency', async () => {
    const s = setup({"format": "currency"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-format-percent', async () => {
    const s = setup({"format": "percent"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-format-unit', async () => {
    const s = setup({"format": "unit"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-sm', async () => {
    const s = setup({"size": "sm"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-md', async () => {
    const s = setup({"size": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('spinbutton', { name: s.props.label })).toBeInTheDocument();
  });
  test('control-is-focusable', async () => {
    const s = setup({});
    act(() => (s.label()).focus());
    expect(s.label()).toHaveFocus();
  });
  test('error-is-identified', async () => {
    const s = setup({"error": "Fix this before continuing."});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeInTheDocument();
    expect(s.label()).toHaveAttribute('aria-invalid', 'true');
  });
});
