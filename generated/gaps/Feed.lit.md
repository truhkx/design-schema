# Gaps reported while generating Feed for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 16:45 — round 1

- FeedItem.content/actions are typed `ReactNode` in the schema; Lit has no equivalent, so they're typed `unknown` (any lit-html-renderable value: string, TemplateResult, Node), matching the precedent of DataGrid's `render?: (row) => unknown`.
- aria-labelledby for each article is not set directly by Feed: the article is a <ds-card>, and Card already labels itself via ElementInternals.ariaLabelledByElements pointing at its own internally-rendered heading (cross-shadow-root, Chromium-only best effort, documented in Card.ts). A plain aria-labelledby attribute on <ds-card> couldn't reference that heading's id anyway, since it lives in Card's own shadow root, a different tree scope.
- The anatomy lists a distinct `articleHeader` part, but composing Card means the heading row is entirely Card's own internal `header` part (heading text only, no room for the timestamp) — Feed exposes `articleBody` (timestamp + content) and `articleActions` (footer) parts but no `articleHeader` part of its own.
- copy.position ('{index} of {total}') is rendered as a visually-hidden span reinforcing aria-posinset/aria-setsize, but only when `hasMore` is false (i.e. `total`/setsize is actually known); when `hasMore` is true, aria-setsize is -1 (unknown per the web platform notes) and the position text is omitted rather than showing '{index} of -1'. Not specified in the doc either way.
- newItemsOffset ('space above the new-items button when it appears') is implemented as padding-block-start on the button's own sticky wrapper rather than a margin, since there is no prior sibling to space against (the button is always first) and the styling rules forbid inter-sibling margins.
- Ctrl+End when `hasMore` only dispatches `load-more` and does not move focus ('first triggers a load so the end is real'); the doc doesn't specify whether the user must press Ctrl+End again once loading finishes and `hasMore` becomes false, or whether Feed should auto-escape once that happens — implemented as 'press again after it loads'.
- Ctrl+Home/Ctrl+End escaping outside the feed walks `document.body` for focusable elements (shadow-piercing, same technique as FocusScope.ts's private helpers, reimplemented locally since they aren't exported) — this assumes a single top-level document and won't follow focus into iframes.
- The empty state (`items.length === 0`) shows the loading indicator instead of `copy.empty` while `loading` is true, on the assumption that mid-initial-fetch shouldn't claim 'Nothing here yet.'; not specified in the doc.

## 2026-09-10 19:15 — round 1

- Feed: FeedItem's `content`/`actions` are typed `unknown` per lit notes ("any lit-html renderable") rather than a stricter TemplateResult union — the doc gives no exact type, so `unknown` was kept as the existing stand-in for ReactNode.
- Feed: `endMessage` prop's default fallback text (copy.end) is applied at render time (`this.endMessage ?? COPY_END`) rather than as the property's default value, since `undefined` must be distinguishable from an explicit empty string; not stated either way in the spec.
- Feed: on Ctrl+End with `hasMore`, the doc says 'press again once it has loaded and hasMore is false' to escape the feed — implemented by simply re-checking live `hasMore` on each Ctrl+End press (no explicit retry/debounce state), since the spec doesn't describe what happens if the caller never flips `hasMore` to false.

## 2026-09-16 13:01 — round 1

- Feed: the doc's own behavior list has 6 scenarios, but the 10 scenarios for this platform leave out `loading-marks-the-feed-busy` (it is marked web-only, and Lit also runs on the web). I wrote only the 10 given; the host still gets aria-busy="true" while loading.
- Feed: the doc says articles have aria-labelledby, but Lit's Card names itself with aria-label because ids don't work across shadow roots. I let Card set the name and added no aria-labelledby.
- Feed: `unreadBorder` has no `part`, and drawing the bar on the Card would restyle a child. I draw it on a wrapper row Feed owns around each card. The doc should name the part that carries the bar (or give Card an accent-edge option).
- Feed: `endMessageInset` is on part endMessage, which is composed as Text, and Text has no padding. The padding is on a wrapper row, and data-part=endMessage is on the ds-text inside it. `loadingInset` has no part, so it goes on the loadingIndicator wrapper.
- Feed: `timestampColor` and `endMessageColor` are locked colours on composed Text. I used Text tone="muted" instead of setting the colour, and passed `timestampSize`/`endMessageSize`/`fontFamily` through Text's --ds-text-font-size/--ds-text-font-family hooks. The doc doesn't say whether these bindings belong to Feed or to Text's props.
- Feed: `articleInset` is 'passed to each Card as its inset', but Card's inset is a sm/md/lg choice and can't take any token. I set inset="md" and pass overrides through Card's --ds-card-padding-block/inline hooks.
- Feed: articleBody has no gap binding, so the space between the timestamp and the content isn't specified. I used ds-stack gap="tight". The doc also doesn't say where the hidden `unread` and `position` text goes; I put both at the start of articleBody.
- Feed: the doc doesn't say what to show with no items, `hasMore` true and not `loading` (the moment right after the first load-more). I show copy.empty.
- Feed: it isn't clear whether Ctrl+End with `hasMore` should fire load-more again while `loading`, or how often load-more may fire while the last article stays in view. I fire on every Ctrl+End, and the observer fires once until `items`, `hasMore` or `loading` changes.
- Feed: `onItemVisible` doesn't say whether it fires again when an item scrolls back into view. I fire once per id per element.
- Feed: the doc names no date/time style for the absolute time. I used Intl.DateTimeFormat dateStyle 'medium' for dates older than a week and dateStyle 'medium' + timeStyle 'short' for the title. Relative times don't refresh as time passes (not specified). An unparseable timestamp is shown as the raw string.
- Feed: the doc doesn't say whether the PageUp/PageDown/Ctrl commands should work from the new-items button, or where focus goes after show-new if `items` never changes. The commands only act from inside an article, and focus moves to the first article on the next `items` update after a press.
- Feed: `label` is required but has no default. I warn in development when it is empty.

## 2026-09-17 16:32 — round 1

- Feed: the generation rules require `shadowRootOptions.delegatesFocus: true`, but Feed's host is not focusable (Card gives each article tabindex=-1) and delegatesFocus would pull focus into the first article on any click in the feed's whitespace. Omitted, matching every other container in the package (Accordion, Card, Stack, Toolbar). The doc should say delegatesFocus is for focusable hosts only.
- Feed: the generated gate `generated/behavior/Feed.lit.test.ts` resolves `container` with `deep(el.shadowRoot, '[role="feed"]')`, which never inspects the host — but the lit platform notes (and the package's 'names tests can read' rule) put role=feed and aria-label on the host. It falls back to the nameless `[part=container]` div, so `has-accessible-name` fails. Duplicating role=feed on the inner div would expose two nested feeds, so I left it; the gate's locator needs to consider the host. (Same generated file is also missing the `loading-marks-the-feed-busy` scenario the doc declares.)
- Feed: the `focusRing`/`focusRingWidth` bindings have no `part` and nothing for Feed to draw — the article ring belongs to ds-card and the button ring to ds-button, and restyling a child is forbidden. Feed declares no hook for them at all. The doc should mark them child-owned/forwarded or drop them.
- Feed: `reduced-motion` is in a11y.requires but the Lit feed has no motion to remove — show-new moves focus and lets the browser scroll natively, so there is no smooth-scroll to gate. No prefers-reduced-motion block was written; the doc should say what motion (if any) Lit is meant to have.
- Feed: `timestampSize` names part `timestamp`, but the web notes put data-part="timestamp" on the `<time>` inside a composed Text, so the font-size hook cannot live on the part element — it has to be set on the wrapping ds-text (a private `.timestamp-text` class). Same shape for endMessage/emptyState, where the part is the Feed wrapper and the size hook must target its ds-text child. The doc should say the size bindings apply to the Text that wraps or sits inside the part.
- Feed: `newItemsCount` is an integer with no default and the doc never says what 0 or a negative value means. Chose: undefined or <= 0 renders no button, while the role=status row stays in the DOM unpadded so the count is announced when it first appears.
- Feed: the precedence between the loading indicator, the end message and the empty state appears only in the Behavior prose, not in props or styles. Implemented as: loading wins over everything; empty + hasMore renders nothing; empty + !hasMore renders copy.empty; otherwise !hasMore renders the end message. `endMessage` is suppressed when items is empty, per the prop description.
- Feed: the show-new focus contract says focus moves 'once the first item's id changes', but not when to disarm if the caller updates `items` without prepending. Chose to keep it armed until the first id changes (so focus stays on the button when nothing is prepended), which means a much later prepend would still take focus.
- Feed: Ctrl+End with `hasMore` and `loading` is specified as 'does nothing'. Ambiguous whether the key should still be consumed; I call preventDefault so the page does not jump to the document end, and fire nothing.
- Feed: the `article` anatomy name now covers two different elements on this platform — the Feed-owned wrapper carries part/data-part="article" and the unread bar, while the ds-card inside it is the actual role="article" element carrying aria-posinset/aria-setsize/aria-describedby. Worth stating explicitly, since a test or override that assumes the part element is the role=article element will miss.

## 2026-09-21 23:57 — round 1

- Feed: the lit notes say aria-busy is "on the host" while deferring everything else to the web notes, which put role=feed, aria-label AND aria-busy on the items column. Put all three on the column: the generated harness locates the container with deep(shadowRoot, '[role="feed"]') and never sees the host, so host placement fails has-accessible-name and loading-marks-the-feed-busy outright. The lit note should be deleted or restated.
- Feed: the web notes contradict themselves on which element is role="article" (paragraph 1 = the Card, closing paragraph = the Feed-owned wrapper). On Lit paragraph 1 is not implementable. Verified in Chromium: a negative tabindex on a shadow host takes the host's whole flat-tree subtree out of sequential focus navigation, so <ds-card focusable> (tabindex="-1") makes every Link and Button inside an article unreachable by Tab — the keyboard gate's Tab test finds no focusable at all, and it is a WCAG 2.1.1 failure, not a cosmetic one. Implemented the closing paragraph: the wrapper carries role="article", tabindex="-1", aria-label (the item heading), aria-describedby, aria-posinset and aria-setsize, and the Card is role="none". Card's schema needs a way to be a scripted focus target without a host tabindex, or the doc must stop asking shadow-DOM platforms to compose a focusable Card.
- Feed: focusRing/focusRingWidth are locked as "child-owned ... Feed has no focusable element of its own, so it declares no hook for this" — but with the wrapper as the article, Feed does own the focus target and draws the ring itself (outline of border.width.focus in color.border.focus, no offset, mirroring Card's own :focus-visible treatment). The binding's description contradicts the structure the same notes prescribe.
- Feed: no radius binding exists for the article wrapper, and Feed cannot read the composed Card's --ds-card-radius (custom properties inherit downward only), so the focus ring would be a square around a rounded card. Used radius.lg — Card's own default radius — on the wrapper. The doc should give the article wrapper a radius binding, or lock the two together.
- Feed: Card writes role="article" AND aria-label onto its own host whenever `heading` is set. Suppressing the nested article needed role="none" on the Card (Card respects a pre-existing role), but Card still writes aria-label onto that presentational host; ARIA resolves the conflict by ignoring the role, which is harmless but wrong-looking. Card should let a composing container turn its self-naming off.
- Feed: "a loading indicator is shown after the last article" reads as inside the feed, but role="progressbar" is not an article and axe reports it as an unallowed child. The same applies to the end message and the empty state, whose composed Text renders a <p> (role paragraph). Rendered all three as siblings after the role="feed" column, under "any other chrome stay outside it as siblings". NOTE: the React generation keeps endMessage and emptyState inside its role="feed" column, which should fail the same rule — the two platforms now disagree and React's CaughtUp/Empty stories look latently red.
- Feed: an empty feed is itself an axe violation the doc does not address — `feed` is absent from axe's reviewEmpty list, so a role="feed" owning no article fails aria-required-children unless aria-busy is true. Chose: the column carries role/aria-label/aria-busy only when it has at least one article OR is `loading`; with neither it is a plain container holding copy.empty, so an empty feed has no named feed region. The doc should say whether role="feed" is unconditional (accepting the violation) or drops as implemented.
- Feed: with role="feed" on the items column the doc no longer says which element is the `container` anatomy part — the column or the outer shell holding the live row and the footer chrome. Put data-part="container" on the column (matching React); data-ds="Feed" stays on the host and the override hooks and keydown handler on the host/shell.
- Feed: the Keyboard story's `given` is not declared. The generated gate walks focusables inside getByRole('feed') and asserts the first Tab lands on index 0, so anything focusable before the feed element fails it — including the new-items button, which is now a sibling outside the column, and the surrounding "before/after" links the previous Lit story used to give Ctrl+Home/End somewhere to land. Keyboard is therefore plain Default args (Reply button, a Link, Download button). Consequence the doc should record: Ctrl+Home's new-items target and Ctrl+End's escape target are both unreachable in the gate fixture.
- Feed: no copy.* or format covers the absolute date. Used Intl.DateTimeFormat(undefined, {dateStyle:'medium'}) for the >=7-day fallback and {dateStyle:'medium', timeStyle:'short'} for the <time> title; counts floored, a future timestamp shows copy.justNow, an unparseable timestamp renders as given with no title, and relative text is computed at render and does not tick. Unspecified in the doc.
- Feed: the empty-label warning wording is quoted in the `label` description without backticks; used React's exact string ("Feed: `label` is the accessible name of the feed and must not be empty.") so the two platforms warn identically. Whitespace-only counts as empty, warned once per mount.
- Feed: "the pending focus request lives until the next change of `items` and no further" does not define a change, and the literal reading (property identity) breaks the feature — every caller passing a fresh array drops the request before the prepend lands. Keyed it on the joined item ids, so it survives an identical re-set and expires on the first change of ids. Same key decides the load-more re-arm: the observer is rebuilt only on a change of the LAST id, hasMore or loading, so a prepend does not ask again.
- Feed: on Lit, Stack and Card write their data-part inside their own shadow roots, so unlike React no display:contents wrapper is needed — `articleBody` and `articleActions` sit directly on the <ds-stack> hosts, and articleBodyGap forwards through --ds-stack-gap. Only `article` and `newItemsButton` need Feed-owned wrappers. The web notes' wrapper rationale should be marked web-only.
- Feed: Text renders only p|span, so the <time datetime title> carrying data-part="timestamp" and the aria-describedby id sits inside a Text span (tone="muted" size="xs"); timestampSize is forwarded to that Text's --ds-text-font-size rather than to the part element, as the binding's own note allows. The doc gives no position for the visually-hidden copy.unread run either — placed first in articleBody, then the timestamp, then copy.position, then the content.
- Feed: the generated behavior harness clicks the part element for `when.click: newItemsButton`. On Lit the sticky row centres the Button, so the click happens to land on it and the scenario passes — the same harness fails on React, where it does not. The harness should click a part's first interactive descendant rather than relying on layout; I did not add a click handler to the row, which would make its whitespace an activation target.

## 2026-09-23 14:04 — round 1

- Feed: articleInset, articleBodyGap, timestampSize, endMessageSize, emptyStateSize and fontFamily are described as 'forwarded to the child's overrides', and the contract says a forwarded binding declares no hook of its own. But `overrides` only takes TokenRefs, so forwarding through it would lose any value set from page CSS. I kept Feed's own --ds-feed-* hooks and set the child's documented CSS hooks from them (--ds-card-padding-block/inline, --ds-stack-gap, --ds-text-font-size/-family, --ds-button-font-family), which the package conventions allow. The doc should say which of the two is meant on Lit.
- Feed: 'once per mount' has no clear meaning for a custom element, which disconnects and reconnects whenever it is moved in the DOM. I reset the label warning on every connect, but kept item-visible deduplication for the element's whole lifetime so that moving a feed never re-reports items as read. The doc should define what a mount is on Lit.
- Feed: the Copy contract says to pick plural forms with Intl.PluralRules and never concatenate a count into a sentence, but showNew/minutesAgo/hoursAgo/daysAgo/position each have one form with a {n}/{count} placeholder and no plural variants. I filled the placeholders directly, as written ('1 min ago', 'Show 3 new').
- Feed: `label` is required with no default, but a Lit accessor needs an initial value. It starts as '' (which triggers the development warning); a required label cannot be enforced as a type on a custom element.
- Feed: the doc only says an empty feed asks for its first page 'on mount and again if the caller clears items'. The load-more re-arm rules (a new hasMore value, a finished loading cycle) are only stated for the last-article observer. I applied the same re-arm rules to the empty-feed request, so an empty feed whose first page failed asks again once `loading` ends. The doc should state this for the empty case.
- Feed: the newItemsButton part is only on the live row while the button is shown; the always-present role="status" row has no part while hidden. The doc does not say whether the part should exist when no button is shown.
- Feed: the Keyboard fixture has no newItemsCount (per the Behavior section), so neither Ctrl+Home's new-items target nor Ctrl+End's escape target is exercised by the keyboard gate. Both are covered only by Feed.test.ts.

## 2026-09-23 14:05 — round 2

- Feed: timestampColor, endMessageColor and emptyStateColor are described as 'expressed as the composed Text's tone="muted", not a colour set on the Text', but the hooks gate requires a locked binding to have a hook that the rules read. I kept tone="muted" and also set Text's --ds-text-color from Feed's new hooks (--ds-feed-timestamp-color, --ds-feed-end-message-color, --ds-feed-empty-state-color), which technically does set a colour on the Text. The default resolves to the same token. The doc should say that on Lit the tone is backed by Feed's hook feeding Text's colour hook.
- Feed: articleInset, articleBodyGap, timestampSize, endMessageSize, emptyStateSize and fontFamily are described as 'forwarded to the child's overrides', and the contract says a forwarded binding declares no hook of its own. But `overrides` only takes TokenRefs, so forwarding through it would lose any value set from page CSS. I kept Feed's own --ds-feed-* hooks and set the child's documented CSS hooks from them (--ds-card-padding-block/inline, --ds-stack-gap, --ds-text-font-size/-family, --ds-button-font-family), which the package conventions allow. The doc should say which of the two is meant on Lit.
- Feed: 'once per mount' has no clear meaning for a custom element, which disconnects and reconnects whenever it is moved in the DOM. I reset the label warning on every connect, but kept item-visible deduplication for the element's whole lifetime so that moving a feed never re-reports items as read. The doc should define what a mount is on Lit.
- Feed: the Copy contract says to pick plural forms with Intl.PluralRules and never concatenate a count into a sentence, but showNew/minutesAgo/hoursAgo/daysAgo/position each have one form with a {n}/{count} placeholder and no plural variants. I filled the placeholders directly, as written ('1 min ago', 'Show 3 new').
- Feed: `label` is required with no default, but a Lit accessor needs an initial value. It starts as '' (which triggers the development warning); a required label cannot be enforced as a type on a custom element.
- Feed: the doc only says an empty feed asks for its first page 'on mount and again if the caller clears items'. The load-more re-arm rules (a new hasMore value, a finished loading cycle) are only stated for the last-article observer. I applied the same re-arm rules to the empty-feed request, so an empty feed whose first page failed asks again once `loading` ends. The doc should state this for the empty case.
- Feed: the newItemsButton part is only on the live row while the button is shown; the always-present role="status" row has no part while hidden. The doc does not say whether the part should exist when no button is shown.
- Feed: the Keyboard fixture has no newItemsCount (per the Behavior section), so neither Ctrl+Home's new-items target nor Ctrl+End's escape target is exercised by the keyboard gate. Both are covered only by Feed.test.ts.

## 2026-09-23 15:45 — round 1

- Feed: onItemVisible 'once per item id per mount' and hasMore 'on mount' do not say what a mount is for a custom element that is disconnected and reconnected (for example, moved in the DOM). I chose: reconnecting re-arms the empty-feed first-page request, the load-more observer and the label warning, but keeps the set of ids already reported, so a moved feed never fires item-visible twice for the same id.
- Feed: timestampColor/endMessageColor/emptyStateColor say 'expressed as the composed Text's tone="muted", not a colour set on the Text', but the Overrides contract says locked bindings keep a working :host hook. I chose: tone="muted" on the Text, and the Feed hook also feeds Text's --ds-text-color with the same token so the locked hook can still be themed from page CSS. The doc should say which rule wins.
- Feed: timestampSize/endMessageSize/emptyStateSize/articleInset/articleBodyGap/fontFamily say they are 'forwarded to the child's overrides.<binding>', which by the Overrides contract means Feed declares no hook of its own; yet they are listed as overridable Feed bindings with --ds-feed-* hooks. I chose: declare the --ds-feed-* hook and set the child's documented CSS hook from it (--ds-text-font-size, --ds-card-padding-block/-inline, --ds-stack-gap, --ds-text-font-family, --ds-button-font-family) rather than writing the child's `overrides` property, so page CSS overrides keep working.
- Feed: the item id is used to build the timestamp's DOM id (aria-describedby target). The shape does not say whether ids may contain whitespace or must be unique across feeds on one page; I used `feed-ts-<id>` inside the shadow root, so ids only have to be unique within one feed and must not contain whitespace.
- Feed: newItemsButton part placement while no button is shown is unspecified (the row stays in the DOM unpadded). I chose: the role="status" row always renders but carries part/data-part="newItemsButton" only while the button is shown, so a test finds the part only when it can click something.

## 2026-09-23 19:44 — round 1

- Feed: copy.showNew, copy.position and the relative-time strings are interpolated with plain template literals; the spec lists params but no plural forms, so no Intl.PluralRules is used and 'Show 1 new' stays as written.
- Feed: 'a moment' of dwell for item-visible is given as one second only in the platform notes, not in the props description; I used 1000 ms as a module constant.
- Feed: the spec says a fractional newItemsCount is interpolated as given, but a Lit `type: Number` attribute converter does not reject fractions either; I left the value untouched and did not test it.
- Feed: the article wrapper's aria-label copies the heading, but the Card also renders the same heading inside the article, so a reader hears it twice; the spec offers no way to avoid this on Lit.
- Feed: the spec doesn't say whether Ctrl+End with hasMore false and loading true should leave the feed. I chose to leave it, since 'does nothing while loading' is stated only for hasMore.
