Build the per-component doc page per website-plan.md ("Component page template").

1. `apps/website/src/pages/docs/components/[slug].astro`, statically generated for all 51 slugs from `generated/components.json` (`getStaticPaths`).
2. Header block: `Breadcrumb` (Docs / Components / `<Name>`), `Heading(level=1)` = component name, `Text(tone="muted")` = the doc's `description` frontmatter field.
3. Props section: a `Table` (not `DataGrid`) built from the component's `props`/`events` — columns Prop, Type, Default, Description, Platforms — the same fields `site/src/components/SchemaTables.astro` already renders for the internal docs, just through the generated `Table` component.
4. Accessibility section: an `Accordion` with three sections — "Role & structure" (the doc's `a11y.role`, anatomy, and `a11y.requires` prose), "Keyboard support" (a `Table` built from `generated/keyboard/<Name>.json`, Key/Action columns — components with no keyboard spec omit this section), "Contrast & motion" (the doc's contrast pairs and any reduced-motion requirement). An `Alert(tone="info")` above the accordion surfaces anything needing a deliberate consumer choice (e.g. supplying an accessible name).
5. For the 36 components with no file yet in `packages/react/src`, render an `Alert(tone="neutral")` in place of the (job 507) live example section: "Not yet generated — schema and accessibility contract below are final; a live example ships once `packages/react` regenerates this component."
Leave a marked placeholder (`<!-- EXAMPLES -->` or an empty named slot) between the header block and the props table for job 507 to fill in — do not build the examples/`Tabs` section here.
Gate: all 51 slugs build without error; the props table's row count matches each doc's prop count; the "not yet generated" alert appears on exactly the 36 components missing from `packages/react/src` at build time (recompute the list, don't hard-code it). Do not modify `packages/*/src` or `generated/*.json`.
