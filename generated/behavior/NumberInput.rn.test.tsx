// Generated by tools/behavior_tests.ts from the `behavior` block of the NumberInput doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { NumberInput } from '../../packages/rn/src/NumberInput';
import type { NumberInputProps } from '../../packages/rn/src/NumberInput';
import meta from '../../packages/rn/src/NumberInput.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<NumberInputProps> = {}) {
  const events = {
    onChange: jest.fn(),
  };
  const props: NumberInputProps = { ...(meta.args as NumberInputProps), ...given, onChangeText: events.onChange };
  const tree = (p: NumberInputProps) => (
    <ThemeProvider mode="light">
      <NumberInput {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('NumberInput') ?? screen.UNSAFE_root,
    label: () => screen.queryByRole('spinbutton') ?? s.root(),
    decrementButton: () => screen.queryByTestId('NumberInput.decrementButton') ?? s.root(),
    incrementButton: () => screen.queryByTestId('NumberInput.incrementButton') ?? s.root(),
    rerender: (next: Partial<NumberInputProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('NumberInput', () => {
  test('the-increment-button-steps-up', () => {
    const s = setup({"defaultValue": 5, "step": 1});
    fireEvent.press(s.incrementButton());
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('the-decrement-button-steps-down', () => {
    const s = setup({"defaultValue": 5, "step": 1});
    fireEvent.press(s.decrementButton());
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('typing-a-number-reports-it', () => {
    const s = setup({});
    fireEvent.changeText(s.label(), "7");
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('decrement-does-nothing-at-the-minimum', () => {
    const s = setup({"defaultValue": 0, "min": 0, "max": 10});
    fireEvent.press(s.decrementButton());
    expect(s.events.onChange).not.toHaveBeenCalled();
  });
  test('a-disabled-field-does-not-step', () => {
    const s = setup({"disabled": true, "defaultValue": 5});
    fireEvent.press(s.incrementButton());
    expect(s.events.onChange).not.toHaveBeenCalled();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-format-decimal', () => {
    const s = setup({"format": "decimal"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-format-currency', () => {
    const s = setup({"format": "currency"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-format-percent', () => {
    const s = setup({"format": "percent"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-format-unit', () => {
    const s = setup({"format": "unit"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-sm', () => {
    const s = setup({"size": "sm"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-size-md', () => {
    const s = setup({"size": "md"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('spinbutton', { name: s.props.label })).toBeOnTheScreen();
  });
  test('error-is-identified', () => {
    const s = setup({"error": "Fix this before continuing."});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeOnTheScreen();
  });
});
