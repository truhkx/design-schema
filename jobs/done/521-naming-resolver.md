Wire naming resolution into generation per claude/customization-and-naming.md ("naming.md — a sibling doc..."), building on job 520's schema.

1. `tools/generate.ts`/`tools/parse.ts` (or their `.py` equivalents if Workstream A's TypeScript port hasn't reached this tool yet — check `claude/typescript-and-currency-plan.md`'s progress first and match whichever language the tool is currently in) gain a naming-resolution step: if an active `naming.md` is present for the run, resolve it once and pass the resolved names into every platform template (web, Lit, RN, and SwiftUI once job 430 lands) for: the output file name, the exported component identifier, prop names in the generated signature, and the CSS custom-property / TokenRef prefix.
2. This step runs **after** the model receives the canonical schema and prompt (the model still reasons in canonical names) and **before** the file is written — it is a rename of the templating output, not a rewrite of what the model is asked to generate. The canonical schema, the prompt text, and every gate (contrast, keyboard-spec derivation, lint-literals, behavior tests, typecheck) keep operating on canonical names throughout; only the final written identifiers change.
3. Composite components must resolve consistently: if `Button` is renamed to `CtaButton`, every other generated component that imports/renders a `Button` (Card's `headerActions`, Form's submit control, etc.) must reference `CtaButton` too, on every platform.
Gate: regenerating `Button` with job 520's fixture `naming.md` (mapping `Button: CtaButton`, namespace `acme`) produces a file exporting `CtaButton`, with CSS custom properties prefixed `--acme-` (web) / equivalent per-platform prefix, and zero changes to the canonical `schema/component.ts`-validated docs. Regenerating with no `naming.md` present produces byte-identical output to today's generation (no naming step is a true no-op). Do not modify `packages/*/src` directly — only the generator should ever write there.
