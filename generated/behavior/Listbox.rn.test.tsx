// Generated by tools/behavior_tests.ts from the `behavior` block of the Listbox doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Listbox } from '../../packages/rn/src/Listbox';
import type { ListboxProps } from '../../packages/rn/src/Listbox';
import meta from '../../packages/rn/src/Listbox.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

type TestNode = ReturnType<typeof screen.getByTestId>;
const CONTROL_ROLES = new Set(["button", "checkbox", "switch", "radio", "textbox", "searchbox", "spinbutton", "combobox", "slider", "link", "tab", "menuitem", "menuitemcheckbox", "menuitemradio", "option", "treeitem", "scrollbar", "adjustable", "imagebutton", "togglebutton"]);
function isControl(n: TestNode): boolean {
  return CONTROL_ROLES.has(n.props.role ?? n.props.accessibilityRole) || typeof n.props.onPress === 'function';
}
const CONTAINER_ROLES = new Set(["dialog", "alertdialog", "combobox", "grid", "listbox", "menu", "menubar", "radiogroup", "tablist", "tree", "treegrid"]);
function hasOwnRole(n: TestNode): boolean {
  return CONTAINER_ROLES.has(n.props.role ?? n.props.accessibilityRole);
}
function activatable(node: TestNode, root: TestNode): TestNode {
  const hit = isControl(node) ? node : node === root || hasOwnRole(node) ? undefined : node.findAll(isControl)[0];
  if (hit === undefined) return node;
  return typeof hit.type === 'string' ? hit : (hit.findAll((n: TestNode) => typeof n.type === 'string')[0] ?? hit);
}

function setup(given: Partial<ListboxProps> = {}) {
  const events = {
    onChange: jest.fn(),
    onActiveChange: jest.fn(),
  };
  const props: ListboxProps = { ...(meta.args as ListboxProps), ...given, onChange: events.onChange, onActiveChange: events.onActiveChange };
  const tree = (p: ListboxProps) => (
    <ThemeProvider mode="light">
      <Listbox {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Listbox') ?? screen.UNSAFE_root,
    list: () => screen.queryByRole('listbox') ?? s.root(),
    option: () => screen.queryByTestId('Listbox.option') ?? s.root(),
    rerender: (next: Partial<ListboxProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Listbox', () => {
  test('click-on-an-option-selects-it', () => {
    const s = setup({});
    fireEvent.press(activatable(s.option(), s.root()));
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('a-disabled-option-cannot-be-selected', () => {
    const s = setup({"options": [{"value": "apple", "label": "Apple", "disabled": true}, {"value": "banana", "label": "Banana"}]});
    fireEvent.press(activatable(s.option(), s.root()));
    expect(s.events.onChange).not.toHaveBeenCalled();
  });
  test('the-empty-message-shows-when-there-are-no-options', () => {
    const s = setup({"options": []});
    expect(screen.getByText(new RegExp("No\\ options"))).toBeOnTheScreen();
  });
  test('a-custom-empty-message-replaces-the-default', () => {
    const s = setup({"options": [], "emptyMessage": "No fruit matches that."});
    expect(screen.getByText(new RegExp("No\\ fruit\\ matches\\ that\\."))).toBeOnTheScreen();
  });
  test('loading-replaces-the-empty-message', () => {
    const s = setup({"options": [], "loading": true});
    expect(screen.getByText(new RegExp("Loading\u2026"))).toBeOnTheScreen();
  });
  test('invalid-renders-the-invalid-copy', () => {
    const s = setup({"invalid": true});
    expect(screen.getByText(new RegExp(escapeRegExp(s.props.label) + "\\ is\\ not\\ valid\\."))).toBeOnTheScreen();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-max-visible-5', () => {
    const s = setup({"maxVisible": "5"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-max-visible-8', () => {
    const s = setup({"maxVisible": "8"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-max-visible-12', () => {
    const s = setup({"maxVisible": "12"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-max-visible-all', () => {
    const s = setup({"maxVisible": "all"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('listbox', { name: s.props.label })).toBeOnTheScreen();
  });
  test('error-is-identified', () => {
    const s = setup({"error": "Fix this before continuing."});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeOnTheScreen();
  });
});
