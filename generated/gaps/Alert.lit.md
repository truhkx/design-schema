# Gaps reported while generating Alert for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 01:57 — round 1

- Alert: the schema's spec text says the icon is 'drawn as a 1em inline shape until an Icon component exists', but ds-icon (with info/success/warning/danger status shapes and a close glyph) now exists in the package, so I composed <ds-icon> for both the tone icon and the dismiss button's glyph instead of hand-drawn SVG, per the generation rule to never draw icons by hand.
- Alert: iconSize is listed as overridable, but the composed <ds-icon> only exposes size via a fixed xs/sm/md/lg/xl enum, not an arbitrary token. I drove it through ds-icon's own documented CSS hook (--ds-icon-size) set from Alert's --ds-alert-icon-size hook on the .icon element, rather than the size attribute, so a per-instance override still works without touching ds-icon's shadow internals — the spec doesn't say how a generic TokenRef override should reconcile with a child's quantized size API.
- Alert: the previous implementation had no --ds-alert-* CSS custom-property hooks or overrides property at all (unlike the sibling components already regenerated in this pass), so I added the full Overrides contract (border, borderWidth, radius, padding, gap, partGap, iconSize, headingWeight, fontFamily, fontSize, lineHeight, dismissMargin) and a data-ds="Alert" host attribute to match current package convention; neither was called out explicitly as missing in the task, but both are required by the package-conventions doc.

## 2026-09-10 17:41 — round 1

- Alert: the existing Alert.ts had no accessible-name implementation at all (no aria-labelledby/aria-label logic), despite the platform note requiring the region be named by the heading, else the body. Implemented it via ElementInternals.ariaLabelledByElements pointing at the rendered .heading/.body element where supported, falling back to a literal aria-label attribute (built from the target slot's flattened assignedNodes text) elsewhere — following the exact 'literal attribute, not internals.ariaLabel' precedent already established in Toast.ts, since ElementInternals.ariaLabel/role aren't read by the dom-accessibility-api test harness.
- Alert: the spec's styles.icon description says the tone icon is colored by passing its token as `overrides.color` to the composed Icon ('the sanctioned way to color a composed child'), but the existing implementation (and the identical precedent in Toast.ts for its own locked `icon` binding) colors it via a `:host([tone=...]) .icon { color: ... }` rule relying on Icon's documented color-inheritance channel instead. Left this as-is for consistency with the established codebase convention rather than switching to an explicit `.overrides` object, since both are equally 'sanctioned' per the composition rule's 'or its documented --ds-<child>-* hook' clause.
- Alert: moveFocusOnward only ever searched for a focusable element after the alert in the document; the guidance requires falling back to the closest previous focusable element when none follows. Fixed to try forward first, then fall back to the nearest preceding candidate.

## 2026-09-10 20:43 — round 1

- Alert (lit): the existing Alert.ts was missing the `headingSize` overridable binding (token font.size.md, listed in the schema's styles and Overrides contract) entirely — no hook, no default, and `.heading` had no font-size rule at all (it inherited the container's `fontSize`/body font-size instead). Added the `--ds-alert-heading-size` hook defaulting to `var(--font-size-md)` and applied it to `.heading`.
- Alert (lit): the dismiss button's `part`/`@csspart` was `dismiss`, not matching its anatomy name `dismissButton` verbatim (kebab-cased, per the package's convention seen in Dialog/BottomSheet's `close-button`, AlertDialog's `cancel-button`/`confirm-button`). Renamed to `dismiss-button`.

## 2026-09-16 05:09 — round 1

- Alert: platforms.lit.notes say the role is set via ElementInternals, but the package convention says roles the tests must observe are plain host attributes (dom-accessibility-api ignores ElementInternals). Chose both: internals.role and a mirrored `role` attribute, removed when live=off.
- Alert: the accessible-name note (ariaLabelledByElements where supported, else aria-label) conflicts with the convention that names tests read must be plain attributes. Chromium supports ariaLabelledByElements, so no aria-label attribute is set there, and a name test through dom-accessibility-api would see no name. Kept the doc's behaviour; no scenario checks the name.
- Alert: iconSize is overridable and is also forwarded to Icon as overrides.size (a TokenRef). A consumer setting only the CSS hook --ds-alert-icon-size doesn't reach the Icon's size (it only moves the first-line alignment margin), because a forward through `overrides` needs a token path, not a CSS value. Chose: forward `overrides.iconSize ?? 'font.size.lg'`. The doc should say whether a forwarded binding keeps a CSS hook.
- Alert: the doc's styles say `foreground` is the heading colour and bodyColor's part is `body`, but background, border, borderWidth, radius, padding, gap and fontFamily have no `part`. Assumed they style `container`, apart from fontFamily and the tone border hook, which stay on :host so they inherit.
- Alert: the heading slot (`heading` property or named slot) is in the Lit notes but not in `parts`, which lists only `body` as a slot. Kept the named `heading` slot from the notes, with the `heading` property text as its fallback.
- Alert: the part-naming note says kebab-case (`dismiss-button`), but the Parts contract says `part`/`data-part` values are the anatomy names verbatim (`dismissButton`). Chose part="dismiss-button" and data-part="dismissButton".
- Alert: `children` is a doc prop, but in Lit it's the default slot, so the story arg `children` is rendered as slot text rather than set as a property. Examples say 'exactly its given as args'; stories inherit the Default args (tone, live, dismissible, children) for keys an example leaves out.
- Alert: live-off-renders-no-role is limited to web, yet the Lit element removes the role when live=off too. A lit test could be added.
