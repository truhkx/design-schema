// Generated by tools/behavior_tests.ts from the `behavior` block of the Toast doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Toast } from '../../packages/rn/src/Toast';
import type { ToastProps } from '../../packages/rn/src/Toast';
import meta from '../../packages/rn/src/Toast.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

type TestNode = ReturnType<typeof screen.getByTestId>;
const CONTROL_ROLES = new Set(["button", "checkbox", "switch", "radio", "textbox", "searchbox", "spinbutton", "combobox", "slider", "link", "tab", "menuitem", "menuitemcheckbox", "menuitemradio", "option", "treeitem", "scrollbar", "adjustable", "imagebutton", "togglebutton"]);
function isControl(n: TestNode): boolean {
  return CONTROL_ROLES.has(n.props.role ?? n.props.accessibilityRole) || typeof n.props.onPress === 'function';
}
const CONTAINER_ROLES = new Set(["dialog", "alertdialog", "combobox", "grid", "listbox", "menu", "menubar", "radiogroup", "tablist", "tree", "treegrid"]);
function hasOwnRole(n: TestNode): boolean {
  return CONTAINER_ROLES.has(n.props.role ?? n.props.accessibilityRole);
}
function activatable(node: TestNode, root: TestNode): TestNode {
  const hit = isControl(node) ? node : node === root || hasOwnRole(node) ? undefined : node.findAll(isControl)[0];
  if (hit === undefined) return node;
  return typeof hit.type === 'string' ? hit : (hit.findAll((n: TestNode) => typeof n.type === 'string')[0] ?? hit);
}

function setup(given: Partial<ToastProps> = {}) {
  const events = {
    onAction: jest.fn(),
    onDismiss: jest.fn(),
  };
  const props: ToastProps = { ...(meta.args as ToastProps), ...given, onAction: events.onAction, onDismiss: events.onDismiss };
  const tree = (p: ToastProps) => (
    <ThemeProvider mode="light">
      <Toast {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Toast') ?? screen.UNSAFE_root,
    region: () => screen.queryByRole('status') ?? s.root(),
    actionButton: () => screen.queryByTestId('Toast.actionButton') ?? s.root(),
    dismissButton: () => screen.queryByTestId('Toast.dismissButton') ?? s.root(),
    rerender: (next: Partial<ToastProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Toast', () => {
  test('the-dismiss-button-fires-on-dismiss', () => {
    const s = setup({"dismissible": true});
    fireEvent.press(activatable(s.dismissButton(), s.root()));
    expect(s.events.onDismiss).toHaveBeenCalled();
  });
  test('the-action-button-fires-on-action', () => {
    const s = setup({"actionLabel": "Undo"});
    fireEvent.press(activatable(s.actionButton(), s.root()));
    expect(s.events.onAction).toHaveBeenCalled();
  });
  test('danger-toasts-are-announced-assertively', () => {
    const s = setup({"tone": "danger"});
    expect(screen.getByRole('alert')).toBeOnTheScreen();
  });
  test('the-message-is-rendered', () => {
    const s = setup({"message": "3 files moved to Archive"});
    expect(screen.getByText(new RegExp("3\\ files\\ moved\\ to\\ Archive"))).toBeOnTheScreen();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-neutral', () => {
    const s = setup({"tone": "neutral"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-success', () => {
    const s = setup({"tone": "success"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-warning', () => {
    const s = setup({"tone": "warning"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-danger', () => {
    const s = setup({"tone": "danger"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-duration-short', () => {
    const s = setup({"duration": "short"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-duration-long', () => {
    const s = setup({"duration": "long"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-duration-persistent', () => {
    const s = setup({"duration": "persistent"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('status')).toBeOnTheScreen();
  });
});
