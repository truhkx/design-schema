// Generated by tools/behavior_tests.ts from the `behavior` block of the Splitter doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Splitter } from '../../packages/rn/src/Splitter';
import type { SplitterProps } from '../../packages/rn/src/Splitter';
import meta from '../../packages/rn/src/Splitter.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<SplitterProps> = {}) {
  const events = {
    onSizeChange: jest.fn(),
    onSizeChangeEnd: jest.fn(),
    onCollapseChange: jest.fn(),
  };
  const props: SplitterProps = { ...(meta.args as SplitterProps), ...given, onSizeChange: events.onSizeChange, onSizeChangeEnd: events.onSizeChangeEnd, onCollapseChange: events.onCollapseChange };
  const tree = (p: SplitterProps) => (
    <ThemeProvider mode="light">
      <Splitter {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Splitter') ?? screen.UNSAFE_root,
    container: () => screen.queryByRole('separator') ?? s.root(),
    rerender: (next: Partial<SplitterProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Splitter', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-orientation-horizontal', () => {
    const s = setup({"orientation": "horizontal"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-orientation-vertical', () => {
    const s = setup({"orientation": "vertical"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-stack-below-prose', () => {
    const s = setup({"stackBelow": "prose"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-stack-below-content', () => {
    const s = setup({"stackBelow": "content"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-stack-below-never', () => {
    const s = setup({"stackBelow": "never"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('separator', { name: s.props.label })).toBeOnTheScreen();
  });
});
