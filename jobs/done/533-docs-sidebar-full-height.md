Fix the docs sidebar's height per Tony's review, 2026-09-15: it should fill the page's height and only develop its own scrollbar if its content would overflow into the footer — not sit at content height, and not scroll independently below that.

1. In `apps/website/src/layouts/DocsLayout.astro` / `docs-layout.css`, make the two-column docs layout (sidebar + content, per `DocsSidebar.astro`) a grid or flex row that stretches to the greater of viewport height or content height, with the sidebar column set to `position: sticky` from just under the header down to just above the footer (not `100vh`, which would let it run under/behind the footer) — the sidebar's own inner list gets `overflow-y: auto` only, so it scrolls in place once its content is taller than the available space, rather than the whole page scrolling past a short sidebar.
2. Verify against both a short docs page (little sidebar content visible, e.g. near the top of Foundations) and a case where the sidebar's expanded `Disclosure` groups genuinely exceed viewport height, to confirm both the "shorter than viewport" and "needs its own scroll" cases look right.
Gate: on a tall viewport the sidebar's background/border extends the full column height down to the footer, not just to its last link; on a short viewport or with several `Disclosure` groups open, the sidebar scrolls independently once it would otherwise run under the footer, and the footer never sits underneath it. Do not modify `packages/react/src/**` — this is a page-chrome layout fix in `apps/website` only (the `SidePanel`/`Disclosure` components used inside the sidebar are unchanged).
