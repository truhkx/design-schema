// Generated by tools/behavior_tests.ts from the `behavior` block of the Landmark doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Landmark } from '../../packages/rn/src/Landmark';
import type { LandmarkProps } from '../../packages/rn/src/Landmark';
import meta from '../../packages/rn/src/Landmark.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<LandmarkProps> = {}) {
  const events = {};
  const props: LandmarkProps = { ...(meta.args as LandmarkProps), ...given };
  const tree = (p: LandmarkProps) => (
    <ThemeProvider mode="light">
      <Landmark {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Landmark') ?? screen.UNSAFE_root,
    region: () => s.root(),
    rerender: (next: Partial<LandmarkProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Landmark', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-role-banner', () => {
    const s = setup({"role": "banner"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-role-navigation', () => {
    const s = setup({"role": "navigation"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-role-main', () => {
    const s = setup({"role": "main"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-role-complementary', () => {
    const s = setup({"role": "complementary"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-role-contentinfo', () => {
    const s = setup({"role": "contentinfo"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-role-region', () => {
    const s = setup({"role": "region"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-role-search', () => {
    const s = setup({"role": "search"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-role-form', () => {
    const s = setup({"role": "form"});
    expect(s.root()).toBeTruthy();
  });
  test.skip('has-accessible-name — then.name: role from prop \'role\' cannot be queried; name the landmark/text role in the doc', async () => {});
});
