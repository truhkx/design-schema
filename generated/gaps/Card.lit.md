# Gaps reported while generating Card for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-09 20:56 — round 1

- Card: the doc's Lit note says the interactive hit-area extension needs 'the consumer's stylesheet or a small global rule from the package' to draw the ::after on the slotted light-DOM link/button, since a shadow stylesheet can't reach slotted content. Rather than leaving that as an undocumented external dependency, Card.ts injects the rule itself via a <style> tag into the card's root node (document head, or the nearest ancestor ShadowRoot) the first time an interactive card is used there — a self-contained implementation of the same idea, but worth flagging since it wasn't spelled out as code.
- Card: labelling the host article by the shadow-rendered heading ('aria-labelledby the heading id') has no standard cross-shadow-boundary mechanism. Implemented as best-effort using ElementInternals.ariaLabelledByElements (feature-detected), which as of this writing is Chromium-only; other engines get an article with no accessible name from the heading.
- Card: 'size restrained per the theme' for the header ds-heading wasn't given a value — used the smallest available HeadingSize ('md'), leaning on the theme's 'hierarchy from weight, not size' guidance, fixed regardless of headingLevel.
- Card: none of the 11 given behavior scenarios exercise headerActions, footer, or interactive — all are plain 'renders: true' checks on heading-level/inset/surface. Those paths are covered by stories (WithHeaderActions, WithFooter, InteractiveTrue) but have no corresponding test, since the doc didn't include scenarios for them.

## 2026-09-09 21:47 — round 1

- Card: spec gives no explicit size for the rendered <ds-heading> (web notes only say 'restrained per the theme'); implementation fixes size="md" for every headingLevel so visual weight stays modest regardless of level, per the theme's 'hierarchy from weight over size' guidance.
- Card: cross-shadow-boundary aria-labelledby uses ElementInternals.ariaLabelledByElements, a Chromium-only, feature-detected API; in browsers without it the host gets role="article" but no programmatic accessible name from the heading — a platform gap the spec doesn't address.
- Card: headerGap/footerGap are layout.gap tokens (loose/normal/tight) but ds-stack's own `gap` property only exposes the space.{0-12} scale, so Card sets `style="gap: var(--ds-card-*-gap)"` directly on the composed <ds-stack> rather than using its gap attribute — reaching a style into a composed child, which the composition rule otherwise forbids; treated as an application of the Overrides section's sanctioned host-CSS-specificity escape hatch since Stack's schema doesn't yet expose layout.gap tokens.
- Card: the interactive hit-area selector was broadened to `ds-link, ds-button, a[href], button` instead of only `ds-link`/`ds-button`, so a raw slotted anchor or button button still gets the extended hit area even though the spec only names the two Lit components.

## 2026-09-09 21:49 — round 2

- Card: the `parse` gate failure (checkbox.md/switch.md rejecting an unrecognized `behavior` frontmatter key) is unrelated to Card and not caused by anything in packages/lit/src/Card.ts, Card.stories.ts, or index.ts — card.md is not among the failing docs. The real fix is either adding `behavior` to schema/component.schema.json or amending the two docs, both of which are outside my permitted edit paths (schema/, site/) this round, so I left Card's files untouched rather than papering over an unrelated, out-of-scope schema/doc mismatch.

## 2026-09-09 21:49 — round 3

- Card: the `parse` gate failure (checkbox.md/switch.md rejecting an unrecognized `behavior` frontmatter key) is identical to round 2's report and still does not implicate card.md or any file in packages/lit/src/Card.*  — the fix requires editing schema/component.schema.json or the two docs, both outside my permitted edit paths (schema/, site/) and unrelated to Card's implementation. If a Card-specific gate is actually failing, its output wasn't included here; I could not find one by inspection (no literals in Card.ts/Card.stories.ts, card.md parses cleanly).

## 2026-09-09 22:21 — round 1

- Card: schema lists `actionsGap` (layout.gap.tight, gap between header-actions controls) as both a style binding and overridable, but the existing implementation omitted it entirely (no hook, no CSS). Added `--ds-card-actions-gap` and styled `slot[name='header-actions']` as `display: flex; gap: var(--ds-card-actions-gap)` so slotted controls in that named slot lay out with the token gap; updated the WithHeaderActions story to include two controls (a Link and a ghost icon Button, per the anatomy note 'at most two') so the gap is visible.
- Card: spec doesn't say how a slot with multiple assigned elements should get gap applied across the shadow boundary; chose styling the `<slot>` element itself as a flex container (assigned nodes flow as its layout children), consistent with how the rest of the file already avoids `::slotted` for layout.

## 2026-09-10 01:26 — round 1

- Card: spec's Lit notes say the interactive hit-area class is applied via 'a small global rule from the package' but doesn't say where that stylesheet lives given the light-DOM link may be in an arbitrary ancestor tree (page or another shadow root); existing implementation injects a scoped <style> into whichever root the slotted element resolves in (ensureHitAreaStyle), tracked per-root via a WeakSet to avoid duplicate injection.
- Card: spec doesn't say what happens when zero or more than one interactive child is slotted for `interactive`; existing implementation leaves the card non-interactive (no hit-area class applied) and logs a DEV-only console.warn.
- Card: header Heading size isn't specified beyond 'so a card heading reads smaller than a page heading' (web notes say size: lg, but existing Lit implementation uses size="md"); kept as previously generated since no reflected size mismatch was flagged elsewhere.

## 2026-09-10 17:28 — round 1

- Card: `focusable` is a boolean prop but is absent from `platforms.lit.reflect` (which lists only inset, surface, interactive, heading-level), unlike `interactive` which is boolean and reflected. Followed the explicit reflect list literally and left `focusable` non-reflecting (still a real boolean attribute via the default Lit boolean converter, just not reflected back to the DOM), but this is inconsistent with the general 'booleans get reflect: true' convention stated elsewhere and may be a doc omission.
- Card: a11y.role is declared as `none` at the schema level, but platforms.web notes require an `article` role labelled by the heading when `heading` is set. Implemented per the web notes (role toggles article/none via ElementInternals) since platform notes are more specific than the generic a11y block; flagging in case `role: none` was meant to apply uniformly.
- Card: no behavior scenario exercises `focusable` (all 11 scenarios only cover renders/headingLevel/inset/surface), so its tabindex/:focus-visible behavior has no generated test coverage — only a manual `FocusableTrue` story was added.

## 2026-09-16 04:26 — round 1

- Card: in Lit the enlarged hit area is drawn on the ds-link/ds-button host element, so a click there never reaches the native <a>/<button> inside its shadow root and nothing happens. The doc doesn't say how to activate the child. I chose to forward the click by calling click() on the child's inner a[href]/button, which reaches into the child's shadow root. Link and Button could instead expose a public activate() method.
- Card: 'An interactive card always reserves border.width.focus, transparent until focused' means a surface=default interactive card loses its visible border. The doc doesn't say whether the border colour should stay at the wider width. I followed React: transparent border, focus colour on :focus-within.
- Card: the doc doesn't say whether header actions and footer controls on an interactive card stay clickable above the enlarged hit area (only the RN notes say they keep their own target). I lifted the header-actions and footer rows with position: relative and z-index: 1.
- Card: the Lit notes say to find 'the single ds-link/ds-button in the default slot', but children are usually wrapped in a Stack, so the doc doesn't say whether to search only direct children or all descendants. I search all descendants of the slotted elements, matching ds-link, ds-button, a[href] and button (the web guidance).
- Card: the focus ring for an interactive card is described as ':focus-within', which also shows it on mouse focus. The focus-visible requirement suggests keyboard focus only, but :focus-visible can't be seen across the slot from the host. I used :host([interactive]:focus-within).
- Card: a focusable card with delegatesFocus would pass scripted focus on to its first focusable child. The Lit convention says to use delegatesFocus 'for anything focusable', which conflicts with this. I left delegatesFocus off.
- Card: the anatomy lists body/headerActions/footer as slots and header/footer gaps as parts, but doesn't say which element carries data-part for the footer (the slot or the row around it). I put data-part='footer' on the row div and data-part='body'/'headerActions' on the slot elements.
- Card: the doc gives a Heading size of 'lg' only in the prose platform notes, not in a parts/props contract. I used size='lg' (the old Lit element used 'md').
- Card: the Default story needs a heading so the HeadingLevel stories show something, but examples use 'exactly their given', so DenseGridCard (no heading in given) inherits Default's heading. The doc doesn't define a Default args set. I kept heading 'Notification settings' in Default.
- Card: the example given 'children: A Link to the invoice' is prose describing content, not a value. I render children as a ds-link label when interactive is true, and as ds-text otherwise.
- Card: the behavior scenario 'a-card-with-a-heading-is-an-article' and the tabindex half of 'focusable-takes-scripted-focus-only' are marked web-only, so they aren't in the Lit list. The element still implements both (role='article' plus aria-label, and tabindex='-1').
- Card: the React guidance says aria attributes passed through ...rest (role, aria-posinset…) land on the root, but for Lit the doc doesn't say how a consumer's role interacts with the card's own role='article'. I set role only when the consumer hasn't set one, and remove only a role the card itself wrote.

## 2026-09-17 04:41 — round 1

- Card: `interactive` says a disabled child disables the card, but not what counts as disabled on Lit (ds-link has no `disabled`, a raw a[href] cannot be disabled). Chose: a `disabled` attribute or aria-disabled="true" on the target, watched by a MutationObserver that writes nothing back.
- Card: the lit notes name only the `target-focus` custom state; the 'no hover background without a single target' and 'disabled target' rules need a selector too. Chose: extra custom states `has-target` and `target-disabled` for the hover rules.
- Card: 'warns once in development' for zero/several interactive children doesn't say when to judge 'zero' if the body is added after connection. Chose: no warning on the first update; the check runs again, with a warning, on the body's slotchange and when `interactive` changes later.
- Card: `focusable` says 'draws its ring as an outline … no offset' but not on which element; the host has no radius. Chose: outline on the surface part while the host matches :focus-visible, with the host's own outline removed.
- Card: the Behavior section says aria attributes passed through `...rest` land on the root, and the lit notes protect a consumer's role; neither says whether a consumer's aria-label survives a heading. Chose: the card writes and removes only an aria-label it set itself.
- Card: the lit notes put header-actions/footer rows at position: relative; z-index: 1 without saying whether that's only for interactive cards (the web notes tie it to the hit area). Chose: only under [interactive], to avoid a stacking context on plain cards.
- Card: the scenario `a-card-with-a-heading-is-an-article` expects role article 'labelled by that heading', but on Lit the name is aria-label (ids don't cross the shadow root). Test checks role="article" plus aria-label equal to the heading text.
- Card: the anatomy lists `heading` as a part but Behavior says it keeps the Heading's own hook and carries no data-part; the prior Lit output put data-part="heading" on ds-heading. Chose the Behavior rule (no data-part on the heading).

## 2026-09-20 09:23 — round 1

- Card: the prompt's global rules require `delegatesFocus: true` in shadowRootOptions for anything focusable, but platforms.lit.notes require the opposite ('the shadow root does not use delegatesFocus') so scripted focus lands on the card rather than its first focusable child. Followed the platform notes and set no shadowRootOptions; the two rules contradict each other and the doc should say the focusable-container case is exempt.
- Card: `heading` is listed in anatomy and in Parts and slots as an element part, but the guidance says it is the system Heading which 'keeps its own hook' and is located by role or text. Rendered <ds-heading> with no `data-part="heading"`; if the part-locator gate expects one, these two statements disagree.
- Card: the overrides contract says 'apply an override only where the binding is in effect' but never says which Card bindings are conditional. Derived it from the borderWidth prose ('an override of this binding therefore only changes non-interactive cards') and the surface-default border rule: border in effect on surface=default, borderWidth on surface=default and not interactive, transition only when interactive. `border` and `transition` are my inference, not stated.
- Card: `hoverBackground` declares one token (color.background.subtle) but its description adds a second value for subtle cards (color.background.strong). Wrote both as CSS token references with no hook, since the binding is locked — but a single-token binding cannot express a per-surface value, so the schema under-describes it.
- Card: `interactive` says 'A top-level Fragment is flattened, so its children count as top-level' — a React-only statement. Lit has no Fragment; a nested html`` template interpolation produces sibling nodes with no wrapper element, so slot assignment already treats them as top-level. Covered it with an `InteractiveInFragment` story using a nested template, but the doc should state the Lit equivalent.
- Card: the `dense-grid-card` example gives no `heading` and its description says 'the story clears any Default heading', but Storybook drops an `undefined` arg back to the meta default. Used `heading: ''`, which the `heading` prop documents as equivalent to omitted. The doc does not say which mechanism a story should use to clear an inherited string arg.
- Card: React's Default and every enum story carry a Stack body plus a two-Button footer, but `footer` and `headerActions` are slots on Lit, not args a story can set through the meta render. Added a story-only `footer: boolean` arg to the Lit args type to reach parity; it is not a Card prop and will appear as a stray control on the docs page. The doc has no guidance for how a slot-valued React arg becomes a Lit story arg.
- Card: `interactive` with a disabled target says 'no hover background, pressing does nothing', and the lit notes say pressing 'is already blocked by the child'. That is not true of the card's own click forwarder, which would otherwise call click() on the child's inner control; added an explicit disabled guard there. The doc should say the forwarder must re-check disabled.

## 2026-09-20 09:34 — round 2

- Card: the axe gate reports zero violations for all 21 Card/Lit stories in light and dark, and Card has no `keyboard` block so no Card keyboard spec is generated. Neither failing gate has a Card-attributable failure, so no Card code was changed this round.
- Feed (not Card): every Feed/Lit story fails axe `aria-required-children` because Feed puts its new-items `role="status"` row and its `role="progressbar"` loading row inside `role="feed"`, which only permits `article` children. Card supplies `role="article"` and `tabindex="-1"` on all three articles as the Feed doc requires. The Feed doc should say where the status/progressbar rows live relative to the feed container — outside it, or as `aria-owns`-detached siblings.
- keyboard harness (tools/keyboard_tests.ts, not Card): the generated `focusables()` helper calls `walk(el)` on the root locator and then looks for shadow roots only under `el.querySelectorAll('*')`, so it never enters the root host's OWN shadow root. For any Lit element whose content is in its shadow root the list is empty, `focusIndex()` is permanently -1, and every Tab/Arrow/Home/End rule fails regardless of the component. Measured on Feed: generated walk finds 0 focusables, the same walk including the host's shadow root finds 4. This accounts for the whole keyboard-lit failure list. Left unfixed because it is a shared harness affecting every Lit component, not Card.
- Card: the round-2 instruction says to fix the code so every gate passes, but the two named gates are whole-Storybook/whole-package runs whose failures belong to other components and to the keyboard harness. There is no change to Card that makes them pass, and the doc/gate contract does not say which component owns a cross-component gate failure. Chose to prove Card clean and report the real owners rather than alter other components from a Card job.
- Card: storybook-static for both React and Lit is stale, so `docs:examples:check` fails on `Card.stories.tsx exports InteractiveInFragment, which storybook-static/index.json does not list`. My Lit story changes (four Interactive* added, Focusable/WithFooter removed) make the Lit index stale the same way. Rebuilding is a separate build step; not done here because rebuilding Lit alone leaves the gate red on React.

## 2026-09-20 09:52 — round 3

- Card: this run's own gate artifacts (test-results/*/error-context.md, written 09:40) contain zero Card/Lit entries in light and dark, and Card has no `keyboard` block so no Card keyboard spec is generated. Neither failing gate has a Card-attributable failure; no Card code was changed. Card verifies clean: tsc, 15/15 behavior tests, 21/21 stories axe-clean both modes, lint:literals 0.
- keyboard harness (tools/keyboard_tests.ts, not Card): the generated `focusables()` helper has two bugs — `walk(el)` looks for shadow roots only under `el.querySelectorAll('*')` so it never enters the ROOT host's own shadow root (every component whose role is a plain attribute on the host gets an empty list and a permanent focusIndex of -1), and it collects light-DOM matches before descending so the order is not flattened-tree order. I fixed both and regenerated: 7 tests went green (FocusScope, Popover, RadioGroup, Stepper, Toast x3, Tree x2) but 4 that had been passing on the wrong order went red (Dialog Shift+Tab, BottomSheet Shift+Tab, Toolbar End, Toolbar ArrowLeft), 29 still failed, and the one-line change rewrote ~60 files under generated/ for BOTH platforms. Reverted — it needs its own job where those 4 components can be adjudicated against their specs.
- keyboard harness assertion model (not Card): the generated Tab tests never focus anything inside the root first, so `before` is -1 and they assert the first Tab lands on the root's first focusable. Any story with a focusable element before the component fails regardless of the component — Feed's Keyboard story renders a ds-link before the feed, so Tab 1 lands outside it. Feed's Tab rule stays red even with the traversal fixed. The docs should say whether a keyboard rule is asserted from a cold start or from a known position inside the component.
- keyboard-lit gate is non-deterministic (not Card): with identical reverted code I measured 60, 63 and 63 passing, and the round-3 prompt reported 61 — a plus/minus 3 band with no code change. Small pass-count deltas cannot attribute a regression to a job at this granularity, so per-job keyboard results need either a fixed worker count with retries or a recorded per-test baseline rather than a total.
- Feed (not Card): all 14 Feed/Lit stories fail axe `aria-required-children` because Feed places its new-items `role="status"` row and its `role="progressbar"` loading row as children of `role="feed"`, which permits only `article` children (axe: 'Element has children which are not allowed: [role=status]'). Card correctly supplies role="article" and tabindex="-1" on all three articles, and Feed/Lit/Empty fails identically with zero cards rendered. The Feed doc should say where those rows live relative to the feed container.
- Card: the round-3 instruction to make every gate pass conflicts with the instruction not to edit generated/ and with the scope of a single-component job. The remaining work is an a11y fix in six other components (Feed, Listbox, Select, Slider, DataGrid, TreeGrid) plus a shared-harness redesign. Chose to prove Card clean, attempt and measure the one shared-tooling fix, revert it when it proved net-harmful and out of bounds, and report rather than make design calls for six components whose specs are not in front of me.

## 2026-09-20 14:43 — round 1

- Card: the generated behavior harness asserts `role: article` and `attribute: tabindex` inside the shadow root, so generated/behavior/Card.lit.test.ts fails 2 of 15 (a-card-with-a-heading-is-an-article, focusable-takes-scripted-focus-only). tools/behavior_tests.ts resolves a `role:` expectation as el.shadowRoot.querySelector('[role="article"]') and an `attribute:` expectation against surface() = the shadow root's [part="surface"], while the doc's own lit notes put both role="article" and tabindex="-1" on the HOST. Pre-existing (those two assertions are unchanged in the working-tree diff; React passes 15/15 only because its surface() IS the root element). Card cannot satisfy both readings: moving role="article" onto the inner surface div strands the host's aria-label on a roleless element (axe aria-prohibited-attr) and leaves the article unnamed, and moving tabindex="-1" inside adds a focusable descendant, contradicting 'scripted focus lands on the card rather than its first focusable child'. I kept the documented contract and left the two generated tests red; the hand-written packages/lit/src/Card.test.ts covers the same two scenarios against the host and passes. The harness needs a host fallback for role/attribute expectations on Lit.
- Card: hoverBackground declares one token (color.background.subtle) but its description names a second for one enum value ('subtle cards use color.background.strong'), which the interpolation/`by` machinery cannot express — color.background.{surface} would resolve subtle to subtle. I hand-wrote the two :hover rules. The binding should carry a `by: surface` map rather than hiding the second token in prose.
- Card: a11y.contrast never pairs any foreground against color.background.strong, even though that is exactly the hover background of an interactive `surface: subtle` card. The three listed pairs only cover color.background.{surface} (default, subtle), so the hovered state of the one surface that changes on hover goes unchecked by the contrast build. I implemented the hover as documented.
- Card: borderWidth says 'An interactive card ALWAYS reserves border.width.focus', while `interactive` says a card with zero or several targets 'stays non-interactive (no hit area, no hover background, no press)' — silent on the border. I keyed the reserved width off the `interactive` attribute alone, so a fallback card on surface: subtle carries a transparent focus-width border it can never fill (and the header/footer z-index stacking context it never needs). Gating both on the has-target state is the other defensible reading.
- Card: the focusable ring contradicts itself across platforms. borderWidth scopes the reserved focus width to interactive cards, and the web and lit notes agree a focusable card 'reserves no border' and draws an outline — but the rn note says 'A focusable card reserves border.width.focus like an interactive one.' Lit follows its own note (outline on the surface part, no reserved border), so the same focusable card's ring sits outside the box on web/Lit and inside it on native.
- Card: `focusable` prescribes drawing the ring on :focus-visible, but :focus-visible after a programmatic focus() is a browser heuristic, not a guarantee — Chromium only matches it when the previously focused element already did. A Feed moving focus to a card after a pointer interaction lands focus with no visible ring, and the focusable-takes-scripted-focus-only scenario asserts only tabindex, so nothing catches it. I implemented the doc as written; a `focus-ring` custom state set from the card's own focusin handler would make the ring unconditional.
- Card: `heading` is listed both in anatomy and in 'Parts and slots' as an element, but the guidance body exempts it from data-part ('it is the system Heading, which keeps its own hook, and is located by role heading or its text'). I emitted <ds-heading> with no part/data-part, matching React. The parts list should record the exemption instead of leaving the two sections to disagree.
- Card: `children` is type `content`, yet every example's `given` is a bare string that sometimes has to become an element — 'A Link to the invoice' and 'View the September invoice' must render as a Link with href #, per whole-card-is-a-link, or an interactive card has no target at all. I encoded that in the meta render (interactive → ds-link, otherwise ds-text) and had to invent a non-schema `footer: boolean` story arg so the Lit stories could slot React's two-button footer, since a slot cannot come through an args string. Examples whose `given` spelled out the slotted content per platform would remove both guesses.
- Card: headerActions' 'At most two' and children's body-length guidance are explicitly 'a content guideline, not a runtime check', so I emitted no dev warning for either — recording it because the doc does define dev warnings for the interactive-target and focusable+interactive cases, and nothing distinguishes the two classes of limit except that sentence.

## 2026-09-20 14:48 — round 2

- Card: the keyboard-run gate demands generated/keyboard/Card.lit.spec.ts, but the Card doc has no `keyboard` block, so tools/keyboard_tests.ts never emits one and Playwright exits 1 with 'No tests found'. This is unfixable from the component: the spec is derived from doc frontmatter (which generation may not edit), and a `Keyboard` story would not create it because nothing reads stories to decide which specs exist. Card genuinely has no keyboard model — `interactive` deliberately adds no focus stop and delegates every key to its single child Link/Button, and `focusable` is explicitly 'not a tab stop', scripted focus only. The real fix is harness-side and already uncommitted in tools/checks.ts (drop keyboard-run when the component's spec is absent, and never pass a filter that matches nothing), covered by tools/__tests__/checks.test.ts. Flagging that it is uncommitted: if that change is lost, every component with no keyboard block (Button, Card, Divider, Link, …) fails this gate again on a scoped run.
- Card: the generated behavior harness asserts `role: article` and `attribute: tabindex` INSIDE the shadow root, so generated/behavior/Card.lit.test.ts fails 2 of 15 (a-card-with-a-heading-is-an-article, focusable-takes-scripted-focus-only). tools/behavior_tests.ts resolves a `role:` expectation as el.shadowRoot.querySelector('[role="article"]') and an `attribute:` expectation against surface() = the shadow root's [part="surface"], while the doc's own lit notes put both role="article" and tabindex="-1" on the HOST. Pre-existing; React passes 15/15 only because its surface() IS the root element. Card cannot satisfy both: moving role="article" to the inner div strands the host's aria-label on a roleless element (axe aria-prohibited-attr) and leaves the article unnamed, and moving tabindex="-1" inside adds a focusable descendant, contradicting 'scripted focus lands on the card rather than its first focusable child'. I kept the documented contract; packages/lit/src/Card.test.ts covers both scenarios on the host and passes. The harness needs a host fallback for role/attribute expectations on Lit.
- Card: hoverBackground declares one token (color.background.subtle) but its description names a second for one enum value ('subtle cards use color.background.strong'), which the interpolation/`by` machinery cannot express — color.background.{surface} would resolve subtle to subtle. I hand-wrote the two :hover rules. The binding should carry a `by: surface` map rather than hiding the second token in prose.
- Card: a11y.contrast never pairs any foreground against color.background.strong, even though that is the hover background of an interactive `surface: subtle` card. The three listed pairs only cover color.background.{surface} (default, subtle), so the hovered state of the one surface that changes on hover goes unchecked by the contrast build.
- Card: borderWidth says 'An interactive card ALWAYS reserves border.width.focus', while `interactive` says a card with zero or several targets 'stays non-interactive (no hit area, no hover background, no press)' — silent on the border. I keyed the reserved width off the `interactive` attribute alone, so a fallback card on surface: subtle carries a transparent focus-width border it can never fill, plus header/footer stacking contexts it never needs. Gating both on the has-target state is the other defensible reading.
- Card: the focusable ring contradicts itself across platforms. borderWidth scopes the reserved focus width to interactive cards, and the web and lit notes agree a focusable card 'reserves no border' and draws an outline — but the rn note says 'A focusable card reserves border.width.focus like an interactive one.' Lit follows its own note, so the same focusable card's ring sits outside the box on web/Lit and inside it on native.
- Card: `focusable` prescribes drawing the ring on :focus-visible, but :focus-visible after a programmatic focus() is a browser heuristic — Chromium only matches it when the previously focused element already did. A Feed moving focus to a card after a pointer interaction lands focus with no visible ring, and focusable-takes-scripted-focus-only asserts only tabindex, so nothing catches it. I implemented the doc as written; a `focus-ring` custom state set from the card's own focusin handler would make the ring unconditional.
- Card: `heading` is listed in anatomy and in 'Parts and slots' as an element, but the guidance body exempts it from data-part ('it is the system Heading, which keeps its own hook, and is located by role heading or its text'). I emitted <ds-heading> with no part/data-part, matching React. The parts list should record the exemption instead of leaving the two sections to disagree.
- Card: `children` is type `content`, yet every example's `given` is a bare string that sometimes has to become an element — 'A Link to the invoice' must render as a Link with href #, per whole-card-is-a-link, or an interactive card has no target at all. I encoded that in the meta render (interactive → ds-link, otherwise ds-text) and invented a non-schema `footer: boolean` story arg so the Lit stories could slot React's two-button footer, since a slot cannot come through an args string. Examples whose `given` spelled out the slotted content per platform would remove both guesses.

## 2026-09-20 14:51 — round 3

- Card: keyboard-run demands generated/keyboard/Card.lit.spec.ts, but card.md has no `keyboard` block so tools/keyboard_tests.ts emits none and the scoped positional filter matches nothing. Unfixable from the component: the spec derives from doc frontmatter (off-limits), and a `Keyboard` story does not create it because nothing reads stories to decide which specs exist. Card genuinely has no keyboard model — `interactive` adds no focus stop and delegates every key to its single child Link/Button, `focusable` is explicitly 'not a tab stop'. The harness fix is already on disk in tools/checks.ts (drop the gate when the component has no spec; never pass a filter matching nothing) and tested, but tools/generate.ts static-imports checks.ts and runs all fix rounds in ONE long-lived process, so ESM module caching means a harness edit landing after process start is never re-read: rounds 2 and 3 both ran the pre-fix argv and produced byte-identical output. Two harness bugs to fix separately: (a) checks.ts must be correct BEFORE a run starts, or generate.ts must re-resolve the gate argv per round rather than capturing it at import; (b) this affects every no-keyboard-block component (Button, Card, Divider, Icon, Link, Text, …), each of which burns its whole fix-round budget on a gate it cannot reach.
- Card: the generated behavior harness asserts `role: article` and `attribute: tabindex` INSIDE the shadow root, so generated/behavior/Card.lit.test.ts fails 2 of 15 (a-card-with-a-heading-is-an-article, focusable-takes-scripted-focus-only). tools/behavior_tests.ts resolves a `role:` expectation as el.shadowRoot.querySelector('[role="article"]') and an `attribute:` expectation against surface() = the shadow root's [part="surface"], while the doc's lit notes put both role="article" and tabindex="-1" on the HOST. React passes 15/15 only because its surface() IS the root element. Card cannot satisfy both: moving role="article" inward strands the host's aria-label on a roleless element (axe aria-prohibited-attr) and leaves the article unnamed; moving tabindex="-1" inward adds a focusable descendant, contradicting 'scripted focus lands on the card rather than its first focusable child'. I kept the documented contract; packages/lit/src/Card.test.ts covers both on the host and passes. The harness needs a host fallback for role/attribute expectations on Lit.
- Card: hoverBackground declares one token (color.background.subtle) but its description names a second for one enum value ('subtle cards use color.background.strong'), which the interpolation/`by` machinery cannot express — color.background.{surface} would resolve subtle to subtle. I hand-wrote the two :hover rules. The binding should carry a `by: surface` map rather than hiding the second token in prose.
- Card: a11y.contrast never pairs any foreground against color.background.strong, even though that is the hover background of an interactive `surface: subtle` card. The three listed pairs only cover color.background.{surface} (default, subtle), so the hovered state of the one surface that changes on hover goes unchecked by the contrast build.
- Card: borderWidth says 'An interactive card ALWAYS reserves border.width.focus', while `interactive` says a card with zero or several targets 'stays non-interactive (no hit area, no hover background, no press)' — silent on the border. I keyed the reserved width off the `interactive` attribute alone, so a fallback card on surface: subtle carries a transparent focus-width border it can never fill, plus header/footer stacking contexts it never needs. Gating both on the has-target state is the other defensible reading.
- Card: the focusable ring contradicts itself across platforms. borderWidth scopes the reserved focus width to interactive cards, and the web and lit notes agree a focusable card 'reserves no border' and draws an outline — but the rn note says 'A focusable card reserves border.width.focus like an interactive one.' Lit follows its own note, so the same focusable card's ring sits outside the box on web/Lit and inside it on native.
- Card: `focusable` prescribes drawing the ring on :focus-visible, but :focus-visible after a programmatic focus() is a browser heuristic — Chromium only matches it when the previously focused element already did. A Feed moving focus to a card after a pointer interaction lands focus with no visible ring, and focusable-takes-scripted-focus-only asserts only tabindex, so nothing catches it. I implemented the doc as written; a `focus-ring` custom state set from the card's own focusin handler would make the ring unconditional.
- Card: `heading` is listed in anatomy and in 'Parts and slots' as an element, but the guidance body exempts it from data-part ('it is the system Heading, which keeps its own hook, and is located by role heading or its text'). I emitted <ds-heading> with no part/data-part, matching React. The parts list should record the exemption instead of leaving the two sections to disagree.
- Card: `children` is type `content`, yet every example's `given` is a bare string that sometimes has to become an element — 'A Link to the invoice' must render as a Link with href # per whole-card-is-a-link, or an interactive card has no target at all. I encoded that in the meta render (interactive → ds-link, otherwise ds-text) and invented a non-schema `footer: boolean` story arg so the Lit stories could slot React's two-button footer, since a slot cannot come through an args string. Examples whose `given` spelled out the slotted content per platform would remove both guesses.

## 2026-09-20 16:40 — round 1

- Card: the generated behavior harness asserts `role: article` inside the shadow root and `attribute: tabindex` against surface() = the shadow root's [part="surface"], while platforms.lit.notes put both role="article" and tabindex="-1" on the HOST, so generated/behavior/Card.lit.test.ts fails 2 of 15. Card cannot satisfy both: moving role inward leaves the article unnamed (the aria-label strands on a roleless host, axe aria-prohibited-attr) and breaks Feed, whose role="feed" requires article children on the card host; moving tabindex inward adds a focusable descendant, contradicting 'scripted focus lands on the card rather than its first focusable child'. Kept the documented contract and left the two generated tests red; tools/behavior_tests.ts needs a host fallback for role/attribute expectations on Lit.
- Card: `children` says 'a plain string or number is rendered inside the system Text with its defaults', but the reason given is native-only ('a bare string cannot sit in a native View'). On Lit, wrapping a slotted text node means reparenting light-DOM children, which the package's idempotent-slot rule warns against. I left bare slotted strings unwrapped, so a bare-string body renders in the UA default face while React's is a Text. The doc should say whether the Text wrap is a native workaround or a cross-platform contract; if the latter it needs a Lit-safe mechanism, since Card must not move its slotted children.
- Card: the `dense-grid-card` example gives no `heading` and its description says 'the story clears any Default heading', but Storybook drops an undefined arg back to the meta default, and the Lit meta render needs a heading arg to interpolate. I used `heading: ''`, which the `heading` prop documents as equivalent to omitted. React needs no such arg because its meta args carry only `children`. The doc does not say how a story clears an inherited string arg, and this is the one place the Lit story args are not exactly the example's `given`.
- Card: `footer` and `headerActions` are React content props but Lit slots, and a slot cannot come through an args string, so the Lit meta carries a non-schema `footer: boolean` arg to reach parity with React's two-Button footer. It will show as a stray control on the docs page. Examples whose `given` spelled out the slotted content per platform would remove this.
- Card: `hoverBackground` declares one token (color.background.subtle) but its description names a second for one enum value ('subtle cards use color.background.strong'), which the interpolation/`by` machinery cannot express — color.background.{surface} would resolve subtle to subtle. I hand-wrote the two :hover rules. The binding should carry a `by: surface` map rather than hiding the second token in prose.
- Card: a11y.contrast never pairs any foreground against color.background.strong, even though that is the hover background of an interactive `surface: subtle` card. The three listed pairs cover only color.background.{surface} (default, subtle), so the hovered state of the one surface that changes on hover is unchecked by the contrast build.
- Card: `borderWidth` says 'An interactive card ALWAYS reserves border.width.focus', while `interactive` says a card with zero or several targets 'stays non-interactive (no hit area, no hover background, no press)' — silent on the border. I keyed the reserved width off the `interactive` attribute alone, so a fallback card on surface: subtle carries a transparent focus-width border it can never fill, plus header/footer stacking contexts it never needs. Gating both on the has-target state is the other defensible reading.
- Card: the focusable ring contradicts itself across platforms. `borderWidth` scopes the reserved focus width to interactive cards and the web and lit notes agree a focusable card 'reserves no border' and draws an outline, but the rn note says 'A focusable card reserves border.width.focus like an interactive one.' Lit follows its own note, so the same focusable card's ring sits outside the box on web/Lit and inside it on native.
- Card: `focusable` prescribes drawing the ring on :focus-visible, but :focus-visible after a programmatic focus() is a browser heuristic — Chromium matches it only when the previously focused element already did. A Feed moving focus to a card after a pointer interaction lands focus with no visible ring, and focusable-takes-scripted-focus-only asserts only tabindex, so nothing catches it. Implemented as written; a `focus-ring` custom state set from the card's own focusin handler would make the ring unconditional.
- Card: the prompt's global rules require `delegatesFocus: true` for anything focusable, but platforms.lit.notes require the opposite ('the shadow root does not use delegatesFocus') so scripted focus lands on the card rather than its first focusable child. Followed the platform notes and set no shadowRootOptions; the doc should record the focusable-container case as an explicit exemption.
- Card: `heading` is listed in anatomy and in 'Parts and slots' as an element part, but the guidance body exempts it from data-part ('it is the system Heading, which keeps its own hook, and is located by role heading or its text'). Emitted <ds-heading> with no part/data-part, matching React. The parts list should record the exemption instead of leaving the two sections to disagree.
- Card: the overrides contract says 'apply an override only where the binding is in effect' but never says which Card bindings are conditional. Derived it: `border` in effect on surface=default, `borderWidth` on surface=default and not interactive (from the borderWidth prose 'an override of this binding therefore only changes non-interactive cards'), `transition` only when interactive. The `border` and `transition` conditions are inference, not stated.
- Card: `interactive` with a disabled target says 'pressing does nothing' and the lit notes say pressing 'is already blocked by the child' — not true of the card's own click forwarder, which would otherwise call click() on the child's inner control. The forwarder carries an explicit disabled guard; the doc should say it must re-check disabled.
- Card: 'A top-level Fragment is flattened, so its children count as top-level' is a React-only statement. Lit has no Fragment; a nested html`` interpolation produces sibling nodes with no wrapper, so slot assignment already treats them as top-level. Covered by the InteractiveInFragment story, but the doc should state the Lit equivalent.
- Card: the keyboard-run gate demands generated/keyboard/Card.lit.spec.ts, but card.md has no `keyboard` block so tools/keyboard_tests.ts emits none and Playwright exits 1 with 'No tests found'. Unfixable from the component — Card genuinely has no keyboard model (`interactive` adds no focus stop and delegates every key to its single child; `focusable` is explicitly not a tab stop). The harness must drop the gate when a component has no spec.

## 2026-09-22 04:09 — round 1

- Card: the `focusable` prop's a11y field and the lit platform notes contradict each other on how the ring is drawn — a11y says a focus-ring state set in the card's own focusin handler (because ':focus-visible' misses a programmatic focus() after a pointer interaction), the lit notes say 'while the host matches :focus-visible'. Implemented both (a `focus-ring` custom state OR :focus-visible), matching React; the lit notes should be amended to name the state.
- Card: nothing says what clears the 'focusin follows a pointerdown' suppression. Chose pointerup, so a pointerdown that moved focus nowhere does not swallow the next scripted focus (React does the same, but only React's code says so — the doc does not).
- Card: React exports a story `InteractiveWithoutTarget` (zero targets: stays non-interactive but still reserves the ring's border width) that is neither an `examples` entry nor derivable from the schema. Added it for story parity; it should be an example in the doc so every platform gets it.
- Card: `children` is type `content`, so the Lit stories have to invent a body. Chose <ds-text element="p"> for non-interactive cards and <ds-link href="#invoice"> when `interactive`, plus a boolean `footer` arg as the stand-in for React's `footer` content prop. None of these are schema props and they are guesses about what the docs site should show.
- Card: `hoverBackground` is specified as 'interactive cards only, on pointer hover' with no mention of the target's state, but the `interactive` prop text says a card that fell back to non-interactive has 'no hover background' and a disabled target has 'no hover background' too. Gated hover on the `has-target` and `target-disabled` custom states; the hoverBackground binding's own description should say so.
- Card: `borderWidth` is 'rendered only with surface default', while an interactive card always reserves border.width.focus instead — so an override is in effect only when `surface === 'default' && !interactive`. The doc states the two rules separately and never states their conjunction, which is what the overrides code has to encode.
- Card: `heading` is listed in `anatomy` and in the parts table as an element, but the prop description says it carries no `data-part` because it is the system Heading keeping its own hook. Followed the prop description (no data-part on <ds-heading>); the parts table and anatomy list disagree with it.
- Card: the doc's presence rule ('the header renders when heading or headerActions is present... Present means not undefined, null or false: a 0 or an empty string is content') is written for React props. Lit has slots, not props, so presence is whether anything is assigned. Detected header-actions/footer presence from slotchange plus a ':scope > [slot=...]' query at connect; an empty-but-present slot therefore counts as absent on Lit, which the doc does not address.
- Card: the generated behavior test `generated/behavior/Card.lit.test.ts` asserts role="article" via el.shadowRoot.querySelector and tabindex on the `surface` part, but the lit notes put role/aria-label/tabindex on the host. Those two scenarios cannot pass as generated — pre-existing generator locator bug, not a component defect (the in-package src/Card.test.ts asserts them on the host and passes).
