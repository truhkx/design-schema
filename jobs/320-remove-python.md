Finish the TypeScript port: delete tools/py.mjs, tools/requirements.txt, tools/__pycache__, any remaining `.py`; remove every `node tools/py.mjs` from package.json and the PowerShell scripts; update process/generation-pipeline.md, process/component-roadmap.md and jobs/README.md command lines; CI no longer installs Python. Gate: `git grep -n "\.py\b"` returns only historical mentions in process/generation-log.md; `pnpm check`, `pnpm gates`, `pnpm build` green. Do not modify packages/*/src or generated/.
