Move @design-schema/react and the RN package's dev tree to React 19.3 (Workstream B step 3).

1. `react`, `react-dom`, `@types/react`, `@types/react-dom` to latest 19.x in packages/react (dev + peer `^19`) and packages/rn (dev; peer stays `>=19` once RN 0.87 lands in job 230).
2. Run `npx types-react-codemod@latest preset-19 ./packages/react/src ./packages/rn/src` and `npx codemod@latest react/19/replace-reactdom-render` if applicable.
3. `forwardRef` removal: React 19 passes `ref` as a prop. Apply the `react/19/remove-forward-ref` codemod; verify every component that exposed a ref still types it (`ref?: Ref<HTMLElement>` in its props).
4. `@testing-library/react` latest; `vitest.config.ts` jsdom environment unchanged.
5. prompts/templates/web.md and prompts/conventions/web.md: state "React 19: `ref` is a prop; no forwardRef; `useId`; Actions are not used by components".
Gate: `pnpm --filter @design-schema/react typecheck`, `test`, `build-storybook`, `pnpm gates:axe`, `pnpm gates:keyboard` for web. The codemods are the only permitted edits under packages/react/src; do not modify generated/.
