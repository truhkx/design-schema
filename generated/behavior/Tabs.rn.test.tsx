// Generated by tools/behavior_tests.ts from the `behavior` block of the Tabs doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Tabs } from '../../packages/rn/src/Tabs';
import type { TabsProps } from '../../packages/rn/src/Tabs';
import meta from '../../packages/rn/src/Tabs.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<TabsProps> = {}) {
  const events = {
    onChange: jest.fn(),
  };
  const props: TabsProps = { ...(meta.args as TabsProps), ...given, onChange: events.onChange };
  const tree = (p: TabsProps) => (
    <ThemeProvider mode="light">
      <Tabs {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Tabs') ?? screen.UNSAFE_root,
    tablist: () => screen.queryByRole('tablist') ?? s.root(),
    tab: () => screen.queryByTestId('Tabs.tab') ?? s.root(),
    rerender: (next: Partial<TabsProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Tabs', () => {
  test('click-selects-a-tab', () => {
    const s = setup({"defaultValue": "activity"});
    fireEvent.press(s.tab());
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('clicking-the-selected-tab-changes-nothing', () => {
    const s = setup({"defaultValue": "overview"});
    fireEvent.press(s.tab());
    expect(s.events.onChange).not.toHaveBeenCalled();
  });
  test('a-disabled-tab-cannot-be-selected', () => {
    const s = setup({"tabs": [{"id": "overview", "label": "Overview", "disabled": true}, {"id": "activity", "label": "Activity"}], "defaultValue": "activity"});
    fireEvent.press(s.tab());
    expect(s.events.onChange).not.toHaveBeenCalled();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-activation-automatic', () => {
    const s = setup({"activation": "automatic"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-activation-manual', () => {
    const s = setup({"activation": "manual"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-orientation-horizontal', () => {
    const s = setup({"orientation": "horizontal"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-orientation-vertical', () => {
    const s = setup({"orientation": "vertical"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-fit-start', () => {
    const s = setup({"fit": "start"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-fit-fill', () => {
    const s = setup({"fit": "fill"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('tablist', { name: s.props.label })).toBeOnTheScreen();
  });
});
