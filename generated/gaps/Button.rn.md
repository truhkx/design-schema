# Gaps reported while generating Button for rn

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 01:12 — round 1

- Button: the existing Button.tsx/stories predated the current schema (no `inverse`, `track`/`onTrack`, or `overrides`, and it passed `disabled` straight to `Pressable`, which removes it from the tab order — contradicting the package convention and the a11y guidance that disabled buttons stay focusable). Rewrote it in full against the current schema rather than patching around the stale version.
- Button: `inverse` — the spec says ghost's hover 'uses a translucent inverse foreground' but defines no token for that translucent color (only the locked `inverseForeground`/`inverseFocusRing` bindings). Kept the normal `backgroundHover` token for the pressed state on ghost+inverse rather than inventing an untokenized alpha value.
- Button: the spec's inverse paragraph doesn't say whether the focus-ring swap to `color.inverse.focus` applies to every variant or only `ghost`. Chose 'every variant when `inverse` is true', since the ring must read against the inverse surface regardless of the button's own fill.
- Button: 'loading … replaces the icon slot' doesn't say whether that means only `leadingIcon` or both icon slots. Chose: the spinner always occupies the leading slot (also covering `iconOnly`, whose sole visible glyph is `leadingIcon`), and `trailingIcon` is hidden while loading; the label itself stays visible throughout since only the icon is described as being replaced.
- Button: `fontSize` was previously hard-coded to `fontSizeMd` regardless of `size`, not following the schema's `font.size.{size}` interpolation. Fixed to map sm/md/lg to fontSizeSm/Md/Lg.
- Button: schema ties a `transition` binding specifically to hover/press and marks it overridable, so implemented an actual `Animated` background-color transition (using `motionDurationFast`/`motion.easing.standard`, skipped under reduced motion) instead of the instant swap used elsewhere in the package (e.g. Link's pressed-color change), since Button's own schema calls this out where Link's does not.
- Button: `loadingSpin` has no described visual (only 'one rotation'); built a custom 1em bordered-ring spinner (foreground-colored, one edge transparent) driven by that duration, replacing the previous `ActivityIndicator`, since a native `ActivityIndicator`'s spin rate can't be bound to a token.
- Button: `onTrack` payload shape isn't spelled out beyond the behavior scenario's `{ name, label }`; defined and exported `ButtonTrackEvent { name: string; label: string }` for it.

## 2026-09-10 17:21 — round 1

- Button: existing Button.tsx predated the current schema's `expanded`, `accessibleName` and `overflowLabel` props (present in the Lit implementation but missing here). Added them: `expanded` maps to `accessibilityState.expanded` (omitted, not `false`, when undefined); `accessibleName` overrides `accessibilityLabel` (falls back to `label`); `overflowLabel` is declared on the type only, since it's data read by a collapsing Toolbar parent, not rendered by Button itself.
- Button: Menu.tsx, Popover.tsx and Toolbar.tsx contain doc comments asserting 'Button has no hook to carry accessibilityState.expanded' / 'no overflowLabel metadata' as acknowledged native limits. That's now stale for `expanded` (Button supports it) and for `overflowLabel` on Menu/Popover triggers specifically, though Toolbar's overflow:menu limitation is separately caused by the lack of a ResizeObserver equivalent, not by the missing prop. Left those files untouched since this task scopes to Button only — a follow-up pass should revisit whether Menu/Popover triggers now wire up `expanded`.
- Button: no Button.test.tsx exists yet; the behavior-scenario-to-test rollout (tracked separately) hasn't reached Button, so no test file was added — stories cover the 12 scenarios' renders/props instead.

## 2026-09-10 20:11 — round 1

- Button: the existing packages/rn/src/Button.tsx already implemented the full schema (props, tokens, a11y, overrides, form/track composition) except that `spinnerStroke` (listed as overridable in the shared Overridable list) was missing from `ButtonOverridableBinding` and the spinner's `borderWidth` was hardcoded to `t.borderWidthFocus` instead of resolving an override. Added `spinnerStroke` to the type and wired it through `overrides?.spinnerStroke` with `t.borderWidthFocus` as the default, matching the pattern used for the other overridable bindings. No other changes were needed — index.ts, Button.stories.tsx, and all other bindings/a11y/composition already matched the spec.

## 2026-09-16 03:57 — round 1

- Button: `copy.loading` has no declared surface on rn. The SwiftUI notes spend it on `.accessibilityValue`, but `platforms.rn` and `a11y` never say where the string goes, and `accessibilityState.busy` already carries busy-ness. Chose `accessibilityValue={{ text: 'Loading' }}` on the root alongside `busy`. Say where rn spends it, or mark the string web/lit/swiftui-only.
- Button: `inverse` requires a ghost hover of `color.inverse.foreground` at 12%, but there is no token for that mix and no style binding for it — `styles.backgroundHover` is only `color.action.{variant}.backgroundHover`. Wrote a module constant plus a hex-to-rgba helper, both marked `literal-ok` because the literal gate rejects `rgba(` and any opacity that is not a token. Needs either an `inverseBackgroundHover` binding with its own token, or an opacity token the doc names.
- Button: `platforms.rn.notes` lists the props Button must forward so Tooltip can attach, but omits `onPressOut`, which this package's Tooltip also clones onto its child (packages/rn/src/Tooltip.tsx:255) to hide the bubble. Forwarded it anyway alongside Button's own pressed-state tracking; add it to the notes list.
- Button: precedence between `accessibleName` and an `accessibilityLabel` set by a parent is unspecified — Tooltip sets the latter when its content IS the name, and Button owns the former. Chose `accessibleName ?? accessibilityLabel ?? label`. State the order in the rn notes.
- Button: the schema declares no ref, but the rn platform rules require a component that exposes its root to declare one. Added `ref?: React.Ref<ViewInstance>`. If Button is meant to be measurable/focusable by a parent (Tooltip, Toolbar overflow), the schema should say so rather than leaving it to the platform rule.
- Button: example `icon-only-in-a-toolbar` gives `iconOnly: true` with no `leadingIcon`, and the contract says a story gets exactly its `given` as args — so `IconOnlyInAToolbar` renders a button with no glyph and no visible label, an empty box in Storybook and in any Chromatic snapshot. Add `leadingIcon` to the example's `given`, or let slot content be story-supplied.
- Button: `loading` specifies a 1em ring in `currentColor`, which does not exist on rn. Drew the ring in the resolved variant foreground and used `fontSize` as the 1em stand-in, halved (literal-ok) for the radius. The stroke is locked to `border.width.focus`, which at `size: sm` is a large fraction of a small ring; there is no spinner-size or spinner-diameter token to size it independently.
- Button: `a11y.requires` asks for `target-24px` while the guidance body asks for 44x44 on touch, and `styles` declares only `minTarget: size.target.min`. Used `size.target.min` for minWidth/minHeight and `hitSlop` up to `size.target.comfortable`, so a token in effect on every button is not declared in the styles block.
- Button: `iconOnly` with a `trailingIcon` set is undefined — `iconOnly` says it shows only `leadingIcon`. Chose to render neither the label nor the trailing icon. Same question for `loading` + `iconOnly`, where the spinner replaces the sole glyph (implemented as the description says).
- Button: `disabled-stays-focusable` is narrowed to web/lit, but the doc body claims disabled buttons stay focusable everywhere. On rn the only way to keep a Pressable in the focus order is to never pass it `disabled`, so this is a press guard plus `accessibilityState.disabled` — and a hardware-keyboard user can still focus and press it, with the press swallowed. Worth stating in the rn notes, since it reads as a bug otherwise.
- Button: `expanded` is typed `boolean` with no default, but a non-disclosing button must not be announced as collapsed. Chose to omit `expanded` from `accessibilityState` entirely when the prop is undefined, mirroring the tri-state the lit notes describe; the rn notes do not mention it.
- Button: nothing says whether a press blocked by `disabled` or `loading` should still reach analytics, nor whether a Form's own disabled state disables its submit button. Chose: no `trackPress`/`onTrack` for a blocked press, and `disabled || form.disabled`.
- Button: the events contract gives `onPress` no arguments, but rn's Pressable hands its handler a `GestureResponderEvent`. Typed it `() => void` and dropped the event, so a consumer cannot read the touch position or modifiers. Confirm that is intended cross-platform rather than an artifact of the web signature.
- Button: `styles.transition` describes 'Background/foreground transitions', but no variant changes its foreground between rest and pressed, so only the background animates. Either drop foreground from the description or name the state that changes it.
- Button: `backgroundHover` is overridable while `background` and `foreground` are locked, so an override can put an arbitrary token behind the locked foreground and break the AA pair `a11y.contrast` proves. If the pressed fill is meant to stay provable, it belongs in the locked set.

## 2026-09-17 04:18 — round 1

- Button: `loading` says the spinner takes the leading icon slot whether or not `leadingIcon` is set, and also that the layout stays unchanged. Without a leadingIcon, adding the spinner (plus iconGap) makes the button wider. I followed the slot rule, so the layout shifts in that case.
- Button: `inverseHoverOpacity` is overridable and has `computed: times 0.25`. The spec doesn't say whether the ×0.25 applies to an overriding token too. I multiply the resolved override by 0.25 as well.
- Button: `spinnerSize` defaults to font.size.{size}, but the spec doesn't say whether a `fontSize` override should move it. I kept them independent: the spinner follows only its own binding.
- Button: `onTrack` changed from an object to positional (name, label). That breaks the exported `ButtonTrackEvent` type. I removed it from index.ts, and there were no other users in packages/rn.
- Button: the generic rule says `disabled` sets accessibilityState in addition to passing `disabled`. The rn notes say never pass `disabled` to Pressable. I followed the notes: a press guard plus accessibilityState.disabled.
- Button: the React Native guidance says callers color icons through Icon's `overrides.color`, but the package digest says Icon takes a `color` prop. The stories keep using `color`.
- Button: the `icon-only-in-a-toolbar` example gives `leadingIcon: "The close Icon"` as prose, not a value. I rendered `<Icon name="close">` colored with the ghost foreground.
- Button: `loadingSpin` gives the rotation length but no easing, and motion.easing.standard is specified for `transition` only. The continuous spin uses Easing.linear.
- Button: `backgroundHover` is now locked, and the spec gives no replacement override for the non-inverse pressed fill. Feed only overrides fontFamily, so nothing broke.

## 2026-09-18 18:55 — round 1

- Button: the style-bindings contract says a `state: hover` binding may use the Pressable callback's `hovered`, but the rn notes say backgroundHover is applied to the pressed state because touch has no hover. Chose pressed only, so under react-native-web a mouse hover does not change the fill. onHoverIn/onHoverOut are only passed through to the Pressable.
- Button: the loading prop says the spinner 'carries no part name' and 'no leadingIcon part is present while loading', but the earlier generation gave the spinner wrapper testID="Button.leadingIcon". Removed it. The doc could say outright whether the spinner gets any testID of its own; chose none.
- Button: example `icon-only-in-a-toolbar` gives `leadingIcon: Icon name=close` as a string, which can't be passed literally as a ReactNode arg. The story renders <Icon name="close"> colored with the ghost variant's foreground token, because RN has no currentColor. So 'exactly its given' holds in meaning, not word for word.
- Button: inverseBackgroundHover is 'this color at inverseHoverOpacity over color.inverse.surface'. On native it is the resolved color with an alpha channel, drawn over whatever is behind the transparent ghost fill, not composited against color.inverse.surface specifically. Colors that are not #rgb/#rrggbb pass through with no alpha applied.
- Button: the spinner is 'a spinnerSize circle with one quarter transparent'. Drawn as a bordered View with a transparent borderTopColor, radius spinnerSize / 2 (marked literal-ok), because RN has no conic stroke. The doc doesn't say whether rn should use react-native-svg for it.
- Button: iconOnly with no leadingIcon (and not loading) renders an empty button with only the accessible name. The doc doesn't say whether that is a dev warning or allowed. Chose to allow it silently.
- Button: type="submit" with no enclosing Form context does nothing beyond onPress. Nothing in the doc covers this case or asks for a __DEV__ warning.
- Button: scenario loading-announces-busy-and-ignores-activation limits its aria-busy expectation to web and Lit. The rn test also asserts accessibilityState.busy (toBeBusy), because the rn notes require busy. The scenario could list an rn-side `state: busy` expectation.
- Button: overflowLabel is accepted in ButtonProps but never rendered or passed to the Pressable. Toolbar is assumed to read it from the element's props, as the web notes describe; the rn notes don't mention it.

## 2026-09-18 19:01 — round 2

- Button: the rn notes and rules name only `accessibilityState={{ disabled, busy, expanded }}`, but react-native-web 0.21 drops `accessibilityState` completely, so on web no aria-disabled, aria-busy or aria-expanded reached the DOM. axe then failed color-contrast on the Disabled story, because a dimmed button not marked disabled isn't covered by the WCAG exemption. Chose to keep `accessibilityState` and mirror it to `aria-disabled`/`aria-busy`/`aria-expanded`, which RN 0.87 merges on native and react-native-web renders. The rn platform notes (and the shared rn conventions) should require the aria-* forms, or both. The same drop likely affects every rn component that sets state only through accessibilityState (Checkbox, Input, SegmentedControl, etc.).
- Button: the loading prop says rn announces copy.loading through `accessibilityValue={{ text: copy.loading }}`. react-native-web ignores `accessibilityValue`, and its aria-valuetext equivalent isn't allowed on role=button (axe aria-allowed-attr), so on react-native-web 'Loading' isn't announced at all; only aria-busy is. Kept accessibilityValue for native and added nothing for web. The doc should say how react-native-web should announce loading, if it should.
- Button: `accessibilityRole` and `accessibilityLabel` still reach the DOM under react-native-web 0.21, so they were left as is. The doc's rule to prefer `role` on RN ≥ 0.87 doesn't say whether Button should switch to `role="button"`/`aria-label`.

## 2026-09-18 19:08 — round 3

- Button: the rn notes require a disabled button that stays focusable and is announced as disabled via `accessibilityState.disabled`, with `disabled` never passed to Pressable. Under react-native-web 0.21 this can't be done with props: `accessibilityState` is ignored, Pressable overwrites any passed `aria-disabled` with its own `disabled` prop, and any disabled signal adds the native `disabled` attribute to the <button> it renders for role=button, removing it from the tab order. Chose to set `aria-disabled` imperatively on the web DOM node in an effect, through a Button-owned root ref that `useImperativeHandle` exposes as `ref`. The doc (or the rn conventions) should name this react-native-web case and the sanctioned way to handle it, since every Pressable-based control with a focusable disabled state (Link, SegmentedControl, Checkbox, Tabs) hits the same wall.
- Button: the axe gate failed color-contrast on the Disabled story only because react-native-web never marked the button disabled. The disabledOpacity dimming itself is correct per the spec and WCAG 1.4.3 (inactive components are exempt), and was left unchanged. The doc could say that the disabled state must be visible to web accessibility tooling for that exemption to apply.
- Button: the loading prop says rn announces copy.loading through `accessibilityValue={{ text: copy.loading }}`. react-native-web ignores it, and aria-valuetext isn't allowed on role=button, so on react-native-web only aria-busy is exposed. Nothing was added for web; the doc should say how react-native-web should announce loading, if it should.
- Button: Button now owns its root ref and passes it to the caller through useImperativeHandle instead of passing `ref` straight through. The rn conventions allow this ('useImperativeHandle(ref, …) when an inner ref owns the root'), but the Button doc only says it 'exposes its root Pressable as ref'.

## 2026-09-19 09:28 — round 1

- Button: the `iconOnly` prop text says the padding is `space.sm`, but the paddingBlock binding's token is also `space.sm`, so it's unclear whether iconOnly means the literal `space.sm` or the resolved paddingBlock. I used the resolved paddingBlock, because the text also says a paddingBlock override keeps the sides equal.
- Button: `loading-announces-busy-and-ignores-activation` puts `aria-busy` on web/Lit only, while its description says the rn test checks accessibilityState.busy 'in its own file'. There is no separate file, so the test asserts `toBeBusy()` in the same scenario.
- Button: the `icon-only-in-a-toolbar` example gives `leadingIcon: Icon name=close` as a string. On rn it has to be an element whose `color` is the ghost foreground token, so the story renders a small wrapper that reads that token. `given` has no way to name a token-colored child element.
- Button: `backgroundHover` has state `hover`, but the rn notes map it to pressed. The spec never says whether `Pressable`'s `hovered` should do anything on react-native-web. It is ignored, following the binding description, so the forwarded `onHoverIn`/`onHoverOut` are passed through and don't change the fill.
- Button: `transition` is meant to animate only the background, but pressing also re-renders the label, and the spec doesn't say whether the label color may change on press. No variant changes its foreground, so only the separate animated fill layer behind the content animates.
- Button: the loading spinner's looping `Animated` timing causes React act() warnings under Jest. The scenarios can't say whether tests should render with reduced motion or fake timers. I left the tests unchanged.

## 2026-09-19 09:35 — round 2

- Button: the rn axe gate runs over the whole Storybook and fails on other components' existing violations (Toolbar, Tree, TreeGrid, Listbox, Carousel, Toast, the Preferences demo, the SettingsPage pattern and more). No Button/React Native story is in the failure list, so no change to Button can make the gate pass. The spec gives no way to scope a component job's axe gate to its own stories. I changed no code.
- Button: Toast 'With Action' reports 2 contrast failures where the other Toast stories report 1. The spec says ghost+inverse text is color.inverse.link on the transparent ghost fill over color.inverse.surface, a pair the spec's contrast list requires to pass. I couldn't run axe here to confirm the extra failure isn't the Button label; logs/button-axe-rn.mjs prints the failing HTML.
- Button: Card 'Interactive With Button' fails nested-interactive because a Button sits inside a pressable Card. Neither the Button nor the Card spec says whether a Button may go inside an interactive container. Button was left as is.

## 2026-09-19 09:41 — round 3

- Button: the rn axe gate runs over the whole Storybook and fails on other components' existing violations (Toolbar, Tree, TreeGrid, Listbox, Carousel, Card, Toast, the Preferences demo, the SettingsPage pattern). For the second round in a row no Button/React Native story fails, and Toast 'With Action' dropped from 2 contrast failures to 1, matching the other Toast stories. The spec gives no way to scope a component's axe gate to its own stories. I changed no code, because weakening Button couldn't clear failures in other components' stories.

## 2026-09-20 02:37 — round 1

- Button: `touchTarget` says the hit area is reached "without changing the visual size" but gives no geometry, and nothing for the frame before the first layout. Chose onLayout measurement with `hitSlop` per side = ceil((size.target.comfortable - measured extent) / 2), assuming size.target.min until the first layout arrives.
- Button: `inverseBackgroundHover` is overridable while `backgroundHover` is locked, and the doc only says native uses "the resolved colour with an alpha channel" — it does not define the conversion. Chose to expand #rgb/#rrggbb to rgba() at `opacity.disabled x 0.25` and pass any other colour form through untouched, per the binding's own note.
- Button: `focusRing`/`focusRingWidth` do not say whether the ring occupies layout on rn (no outline exists). Chose a border of `border.width.focus` that is always present and transparent when unfocused, so focusing never shifts the button.
- Button: `spinnerSize` resolves to `font.size.{size}`, the same token as `fontSize`, and the doc says a fontSize override must not move it — but not whether the spinner tracks the `size` prop. Chose to key it off `size` and ignore a `fontSize` override, so `size: lg` grows the ring.
- Button: the doc requires setting `aria-disabled` imperatively on the react-native-web DOM node but the package has no DOM lib and `ViewInstance` has no setAttribute. Declared a local minimal element type and cast the root ref through it, guarded by Platform.OS === 'web'.
- Button: `loading` "without one the spinner and iconGap widen the button, a shift that is accepted" — no guidance on whether to reserve the slot instead. Chose no spacer: the width shift is taken as specified.
- Button: anatomy names `label` as a part but the parts section gives it no props or forwards. Rendered as a plain react-native Text styled from the fontFamily/fontWeight/fontSize/foreground bindings rather than the package's Text, which has no binding for a variant foreground or a button font size; that is composition the doc neither requires nor forbids.
- Button: scenario `disabled-stays-focusable` is narrowed to web/lit, and @testing-library/react-native exposes no focusability assertion, so the rn guarantee (disabled never reaches Pressable, keeping it in the focus order) is covered only by inspection, not by a test.
- Button: the `transition` binding animates the background on press, but the doc does not say whether it should settle synchronously under test. The running Animated.timing emits a React act() warning in Jest on every press scenario; the tests pass, but the doc offers no reduced-motion-in-test rule that would silence it.

## 2026-09-20 02:58 — round 2

- Button: nothing in packages/rn/src/Button.tsx needed to change — all 23 Button/React Native stories pass axe in both modes (46/46 renders, 0 violations). The axe-rn gate is whole-Storybook and fails on ~1,200 violating nodes across 25 other components; a Button job cannot fix them without rewriting those components, so the gate stays red on debt Button does not own.
- Button: the only Button-named nodes in the whole sweep are 14 `Button.label` color-contrast nodes inside Toast stories, caused by axe sampling during Toast's entrance fade (toast composited at ~0.79 opacity → 3.55:1). Once the fade settles the same stories report 0 contrast violations, and the locked pair color.inverse.link on color.inverse.surface is 8.95:1. Left unchanged: recolouring the label to survive a transient blend would break the locked `inverseForeground` binding.
- theme (shared, not Button's file): `useReducedMotion()` resolves AccessibilityInfo.isReduceMotionEnabled() asynchronously and returns false on first render, so every rn-web entrance animation plays once even under prefers-reduced-motion. That is what leaves Toast mid-fade for axe, and it also means Button's own `transition` and `loadingSpin` briefly animate before the preference is known, against those bindings' "disabled under prefers-reduced-motion". Fixing it needs a synchronous matchMedia seed on web in packages/rn/src/theme.tsx — out of scope for a Button job, reported for the owner.
- Button: `touchTarget` gives no geometry and nothing for the frame before first layout; chose onLayout measurement with hitSlop per side = ceil((size.target.comfortable - measured extent) / 2), assuming size.target.min until the first layout.
- Button: `inverseBackgroundHover` is overridable while `backgroundHover` is locked, and the doc only says native uses "the resolved colour with an alpha channel" without defining the conversion; chose #rgb/#rrggbb → rgba() at opacity.disabled x 0.25, other colour forms passed through untouched.
- Button: the doc does not say whether the focus ring occupies layout on rn (there is no outline); chose an always-present border of border.width.focus that is transparent when unfocused, so focusing never shifts the button.
- Button: `spinnerSize` resolves to the same token as `fontSize` and must not move with a fontSize override, but the doc does not say whether it tracks the `size` prop; chose to key it off `size`.
- Button: the doc requires setting aria-disabled imperatively on the react-native-web node, but the package has no DOM lib and ViewInstance has no setAttribute; declared a local minimal element type and cast the root ref, guarded by Platform.OS === 'web'.
- Button: anatomy names `label` a part but gives it no props or forwards; rendered as a plain react-native Text styled from the bindings rather than the package's Text, which has no binding for a variant foreground or a button font size.
- Button: scenario `disabled-stays-focusable` is web/lit-only and @testing-library/react-native has no focusability assertion, so the rn guarantee (disabled never reaches Pressable) is covered by inspection, not a test.
- Button: the doc offers no reduced-motion-in-test rule, so the pressed-state Animated.timing settles outside act() and every press scenario logs a React act() warning; the tests pass.

## 2026-09-20 03:12 — round 3

- Button: `transition` and `loadingSpin` are specified as disabled under prefers-reduced-motion, but the shared `useReducedMotion()` resolved the preference in an effect and returned false for the first render, so both animated briefly against their own bindings. Fixed in packages/rn/src/theme.tsx by seeding the state synchronously from matchMedia on web (identical value to react-native-web's own async source, one render earlier); Button.tsx itself needed no change. This also removed the 46 Toast color-contrast nodes, 14 of which carried Button.label.
- Button: no doc says which component owns reduced motion for an entrance animation that wraps a Button — the 14 failing nodes were named Button.label but produced by Toast's fade compositing over it. Chose to fix the shared hook rather than recolour the label, since `inverseForeground` is locked and the static pair is 8.95:1.
- Button: nothing in packages/rn/src/Button.tsx needed changing for this gate — all 23 Button/React Native stories pass axe in both modes. The axe-rn gate is whole-Storybook and still fails on ~1,150 nodes across 25 other components that a Button job cannot own.
- Button: `touchTarget` gives no geometry and nothing for the frame before first layout; chose onLayout measurement with hitSlop per side = ceil((size.target.comfortable - measured extent) / 2), assuming size.target.min until the first layout.
- Button: `inverseBackgroundHover` is overridable while `backgroundHover` is locked, and the doc only says native uses "the resolved colour with an alpha channel" without defining the conversion; chose #rgb/#rrggbb → rgba() at opacity.disabled x 0.25, other colour forms passed through untouched.
- Button: the doc does not say whether the focus ring occupies layout on rn (there is no outline); chose an always-present border of border.width.focus that is transparent when unfocused, so focusing never shifts the button.
- Button: `spinnerSize` resolves to the same token as `fontSize` and must not move with a fontSize override, but the doc does not say whether it tracks the `size` prop; chose to key it off `size`.
- Button: the doc requires setting aria-disabled imperatively on the react-native-web node, but the package has no DOM lib and ViewInstance has no setAttribute; declared a local minimal element type and cast the root ref, guarded by Platform.OS === 'web'. The same DOM-lib gap now applies to matchMedia in theme.tsx.
- Button: anatomy names `label` a part but gives it no props or forwards; rendered as a plain react-native Text styled from the bindings rather than the package's Text, which has no binding for a variant foreground or a button font size.
- Button: scenario `disabled-stays-focusable` is web/lit-only and @testing-library/react-native has no focusability assertion, so the rn guarantee (disabled never reaches Pressable) is covered by inspection, not a test.
- Button: the doc offers no reduced-motion-in-test rule, so the pressed-state Animated.timing settles outside act() and every press scenario logs a React act() warning; the tests pass.

## 2026-09-20 12:00 — round 1

- Button: the doc requires both an animated `background`/`backgroundHover` and a `focusRing` border whose width is always reserved, but a Pressable's own style cannot hold an Animated value on RN. Chose a nested flex fill child that grows into the root's box, with the ring staying on the root Pressable so it paints above the fill; the doc says nothing about this layering or about the inner corner radius, for which I used `radius - border.width.focus`.
- Button: `loadingSpin` is 'disabled under prefers-reduced-motion' but the doc does not say whether the ring itself stays visible. Chose to keep the ring rendered and frozen at rotation 0, so `loading` still reads as a pending state; likewise `transition` snaps the background instantly rather than animating.
- Button: the doc gives no `useNativeDriver` guidance for `loadingSpin`. The package convention names `false` for layout props; rotation is a transform, so I used the native driver everywhere except web (react-native-web has none). The background interpolation stays on `false`, since colour is not native-drivable there.
- Button: `hitSlop` must reach `size.target.comfortable`, but the doc does not say what to use before the first `onLayout`. Chose to assume the visual is exactly `size.target.min` until measured, so the comfortable target is never under-served on the first frame.
- Button: the package convention says label typography should come from the system `Text` component, but Button's `fontFamily`/`fontWeight`/`fontSize`/`foreground` are its own bindings and the package `Text`'s `size`/`tone` enums cannot express `font.size.{size}` with an action foreground. Used React Native's `Text` with the bindings applied explicitly; if the `label` part is meant to compose the system `Text`, that component's schema needs a way to take those four bindings.
- Button: `backgroundHover` is documented as the pressed fill on rn and explicitly not read from the Pressable `hovered` state, yet `onHoverIn`/`onHoverOut` are forwarded to the root. The result is that a react-native-web preview shows no hover feedback at all on a button — correct per the doc, but the Storybook preview therefore does not demonstrate the `backgroundHover` binding for any variant.

## 2026-09-20 12:09 — round 2

- Button: the rn axe gate is whole-Storybook, so it fails on 580 nodes owned by 24 other components while Button itself is clean across all 46 story-modes. Made no change to Button; the gate cannot distinguish the component under generation from the package's standing debt, so a per-component axe run is the only signal a generation job can act on.
- Button: the doc mandates mirroring accessibilityState to aria-busy/aria-expanded and setting aria-disabled imperatively, because react-native-web drops accessibilityState. That mirror is why Button passes axe, but it is written only in Button's rn platform notes — Switch, SegmentedControl, RadioGroup, Select, Checkbox, Combobox, Slider and Meter all fail aria-required-attr for the exact same reason. The rule belongs in a shared rn platform convention rather than one component's notes.
- Button: the doc requires both an animated background and a focus ring whose width is always reserved, but a Pressable's style cannot hold an Animated value on RN. Chose a nested flex fill child carrying the padding, gap and an inner radius of `radius - border.width.focus`, leaving the ring on the root so it paints above the fill; an inset-0 overlay (the obvious reading) fills the padding box on Yoga and in CSS alike and so stops short of every edge.
- Button: `loadingSpin` is 'disabled under prefers-reduced-motion' but the doc does not say whether the ring stays visible. Chose to keep it rendered and frozen at rotation 0 so `loading` still reads as pending; `transition` likewise snaps instead of animating.
- Button: the doc gives no `useNativeDriver` guidance for `loadingSpin`. The package convention names `false` for layout props, but rotation is a transform, so I used the native driver everywhere except web; the background interpolation stays `false` since colour is not native-drivable there.
- Button: `hitSlop` must reach `size.target.comfortable`, but the doc does not say what to assume before the first `onLayout`. Chose `size.target.min` as the presumed footprint so the comfortable target is never under-served on the first frame. Worth noting react-native-web ignores hitSlop entirely, so the web preview's target is the visual box alone — it still clears the 24px floor via `minTarget`, but the 44px touch target is unverifiable by the axe gate.
- Button: the package convention says label typography should come from the system `Text`, but Button's `fontFamily`/`fontWeight`/`fontSize`/`foreground` are its own bindings and `Text`'s `size`/`tone` enums cannot express `font.size.{size}` with an action foreground. Used React Native's `Text` with the bindings applied explicitly; if the `label` part is meant to compose the system `Text`, that component's schema needs to accept those four bindings.
- Button: `backgroundHover` is documented as the pressed fill on rn and explicitly not read from the Pressable `hovered` state, yet `onHoverIn`/`onHoverOut` are forwarded to the root. A react-native-web preview therefore shows no hover feedback on any variant — correct per the doc, but the Storybook preview cannot demonstrate the binding.

## 2026-09-20 12:16 — round 3

- Button: the rn axe gate is whole-Storybook, so it reports 1116 violating nodes across 30 story titles from ~26 other components while Button is clean in all 46 of its own story-modes. Made no change to Button for the third round running; a generation job has no way to turn this gate green, and the gate gives the component under generation no isolated signal — a per-component axe run is the only actionable output.
- Button: the doc mandates mirroring accessibilityState to aria-busy/aria-expanded and setting aria-disabled imperatively because react-native-web drops accessibilityState. That mirror is why Button passes axe, but it lives only in Button's rn platform notes, and Switch, SegmentedControl, RadioGroup, Select, Checkbox, Combobox, Slider, Meter, Splitter, DataGrid and TreeGrid all fail aria-required-attr for exactly that reason — roughly half the gate's nodes. The rule belongs in a shared rn platform convention.
- Button: the Demo/Preferences 'Submitting' contrast failures show the same gap from the other side — Form-disabled dimming reduces contrast on RadioGroup/Checkbox/Switch labels, and axe cannot excuse it because those components never set aria-disabled, whereas Button's identically dimmed label is excused. Whatever doc carries the disabled-dimming rule needs to pair it with the aria-disabled mirror.
- Button: the doc requires both an animated background and a focus ring whose width is always reserved, but a Pressable's style cannot hold an Animated value on RN. Chose a nested flex fill child carrying the padding, gap and an inner radius of `radius - border.width.focus`, leaving the ring on the root so it paints above the fill; an inset-0 overlay — the obvious reading — fills the padding box on Yoga and in CSS alike and so stops short of every edge.
- Button: `loadingSpin` is 'disabled under prefers-reduced-motion' but the doc does not say whether the ring stays visible. Chose to keep it rendered and frozen at rotation 0 so `loading` still reads as pending; `transition` likewise snaps instead of animating.
- Button: the doc gives no `useNativeDriver` guidance for `loadingSpin`. The package convention names `false` for layout props, but rotation is a transform, so I used the native driver everywhere except web; the background interpolation stays `false` since colour is not native-drivable there.
- Button: `hitSlop` must reach `size.target.comfortable`, but the doc does not say what to assume before the first `onLayout`. Chose `size.target.min` as the presumed footprint so the comfortable target is never under-served on the first frame. Note react-native-web ignores hitSlop entirely, so the web preview's target is the visual box alone — it clears the 24px floor via `minTarget`, but the 44px touch target is unverifiable by the axe gate.
- Button: the package convention says label typography should come from the system `Text`, but Button's `fontFamily`/`fontWeight`/`fontSize`/`foreground` are its own bindings and `Text`'s `size`/`tone` enums cannot express `font.size.{size}` with an action foreground. Used React Native's `Text` with the bindings applied explicitly; if the `label` part is meant to compose the system `Text`, that component's schema needs to accept those four bindings.
- Button: `backgroundHover` is documented as the pressed fill on rn and explicitly not read from the Pressable `hovered` state, yet `onHoverIn`/`onHoverOut` are forwarded to the root. A react-native-web preview therefore shows no hover feedback on any variant — correct per the doc, but the Storybook preview cannot demonstrate the binding.

## 2026-09-20 13:01 — round 1

- Button: the doc requires both an animated background/backgroundHover and a focusRing whose width is always reserved, but a Pressable's own style cannot hold an Animated value on RN. Kept the nested flex fill child that grows into the root's box with the ring on the root so it paints above the fill; the doc says nothing about this layering or about the inner corner radius, for which the code uses `radius - border.width.focus`. The obvious reading (an inset-0 overlay) fills the *padding* box on Yoga and in CSS alike, so the fill stops border.width.focus short of every edge.
- Button: `touchTarget` says the hit area is reached 'without changing the visual size' but gives no geometry and nothing for the frame before the first onLayout. Kept hitSlop per side = ceil((size.target.comfortable - measured extent) / 2), assuming size.target.min until measured. react-native-web ignores hitSlop entirely, so the 44px touch target is unverifiable by the axe gate.
- Button: `inverseBackgroundHover` is overridable while `backgroundHover` is locked, and the doc only says native uses 'the resolved colour with an alpha channel' without defining the conversion. Kept #rgb/#rrggbb expanded to rgba() at opacity.disabled x 0.25, with any other colour form passed through untouched.
- Button: neither `focusRing` nor `focusRingWidth` says whether the ring occupies layout on rn (there is no outline). Kept an always-present border of border.width.focus that is transparent when unfocused, so focusing never shifts the button.
- Button: `loadingSpin` is 'disabled under prefers-reduced-motion' but the doc does not say whether the ring stays visible. Kept it rendered and frozen at rotation 0 so `loading` still reads as pending; `transition` likewise snaps the background instantly rather than animating.
- Button: the doc gives no `useNativeDriver` guidance for `loadingSpin`. The package convention names `false` for layout props, but rotation is a transform, so the native driver is used everywhere except web; the background interpolation stays `false` since colour is not native-drivable there.
- Button: the doc requires setting aria-disabled imperatively on the react-native-web DOM node, but the package has no DOM lib and `ViewInstance` has no setAttribute. Kept a locally declared minimal element type with the root ref cast through it, guarded by Platform.OS === 'web'.
- Button: anatomy names `label` a part but the parts section gives it no props or forwards, and the package convention says label typography should come from the system `Text`. Button's fontFamily/fontWeight/fontSize/foreground are its own bindings and `Text`'s size/tone enums cannot express font.size.{size} with an action foreground, so React Native's `Text` is used with the bindings applied explicitly. If the `label` part is meant to compose the system Text, that component's schema needs to accept those four bindings.
- Button: `backgroundHover` is documented as the pressed fill on rn and explicitly not read from the Pressable `hovered` state, yet onHoverIn/onHoverOut are forwarded to the root. A react-native-web preview therefore shows no hover feedback on any variant - correct per the doc, but the Storybook preview cannot demonstrate the binding.
- Button: scenario `disabled-stays-focusable` is narrowed to web/lit and @testing-library/react-native exposes no focusability assertion, so the rn guarantee (disabled never reaches Pressable, keeping it in the focus order) is covered only by inspection, not by a test.
- Button: the doc offers no reduced-motion-in-test rule, so the pressed-state Animated.timing settles outside act() and every press scenario logs a React act() warning when the file is run alone. The tests pass and the full rn suite is clean.
- Button: the doc mandates mirroring accessibilityState to aria-busy/aria-expanded and setting aria-disabled imperatively because react-native-web drops accessibilityState, but that rule lives only in Button's rn platform notes. Switch, SegmentedControl, RadioGroup, Select, Checkbox, Combobox, Slider, Meter, Splitter, DataGrid and TreeGrid all fail axe's aria-required-attr for exactly that reason - the rule belongs in a shared rn platform convention rather than one component's notes.

## 2026-09-22 03:06 — round 1

- Button: nothing in packages/rn/src/Button.tsx, .stories.tsx or .test.tsx needed to change — the committed files already implement this spec exactly (13 overridable bindings, tri-state expanded, the aria-* mirror, the nested animated fill, hitSlop geometry, the frozen ring). index.ts already carries the export pair in the existing style, so no edit there either. Verified by jest (17/17 scenarios), tsc -p packages/rn and lint:literals (0 findings).
- Button: `focusRingOffset` is locked and described as an outline offset 'so the ring clears the button's own fill instead of sitting on its edge', but the rn notes prescribe the opposite geometry — an always-present border of focusRingWidth on the root with the animated fill as a flex child grown flush inside it — so on rn the ring sits directly on the fill edge and the binding has no expression at all. The `inverse` paragraph leans on that offset for its contrast argument ('focusRingOffset puts it clear of the button's own fill, so it is proven against color.inverse.surface and needs no pair against each variant's background'), which therefore does not hold on rn: the inverse focus ring is adjacent to each variant's own fill. Either give rn a rendering for the offset (an outer transparent ring view, or root padding) or mark focusRingOffset web/lit-only and add the per-variant inverse-focus pairs to a11y.contrast.
- Button: `minTarget` is 'min-width and min-height of the button on every platform', but on rn the root also carries the always-present focus border, so minWidth/minHeight is the border box and the fill+content box is 2 x border.width.focus under the floor. Implemented as the border box (which is what WCAG 2.5.8 measures), but the doc should say which box the floor applies to now that the ring occupies layout on this platform.
- Button: the doc offers no reduced-motion rule for tests, so the pressed-state Animated.timing settles outside act() and every press scenario logs a React act() warning when Button.test.tsx runs alone (17/17 still pass). A test-time default — reduced motion on, or fake timers — would silence it without touching the component.
- Button: example `icon-only-in-a-toolbar` still cannot be rendered with 'exactly its given' as args on rn. `leadingIcon: Icon name=close` needs `color` set to the ghost foreground, and rn has no currentColor, so the story passes a small theme-reading wrapper element instead of a literal value. `given` has no way to name a token-coloured child element; the contract holds in meaning, not word for word.
- Button: `touchTarget` (size.target.comfortable via hitSlop) is unverifiable by any gate. react-native-web ignores hitSlop, so the axe-rn run only ever sees the visual box clearing the 24px floor through minTarget, and there is no native gate — the 44px guarantee rests on the onLayout arithmetic alone. Same for `disabled-stays-focusable`, which the doc narrows to web/lit because @testing-library/react-native has no focusability assertion.
- Button: the rule that react-native-web drops `accessibilityState`, so busy/expanded must be mirrored as aria-* props and aria-disabled set imperatively on the DOM node, is still written only in Button's `platforms.rn.notes`. It is a package-wide react-native-web fact — Switch, SegmentedControl, RadioGroup, Select, Checkbox, Combobox, Slider, Meter, Splitter, DataGrid and TreeGrid need the same mirror — so it belongs in a shared rn platform convention rather than one component's notes.
- Button: the doc says to set aria-busy 'true' while loading but does not say what to do otherwise. The implementation passes `aria-busy={loading}`, so every non-loading button renders aria-busy="false" in the web preview, rather than omitting the attribute the way `aria-expanded` is omitted when `expanded` is undefined. Say whether the false value should be emitted or dropped.

## 2026-09-23 13:43 — round 1

- Button: the package conventions say to mirror `accessibilityValue` as `aria-valuetext` 'always', but the `loading` prop says aria-valuetext is not allowed on a button and react-native-web should expose only aria-busy. I followed the component doc (no aria-valuetext mirror for copy.loading) and left a comment; the conventions digest should name this exception for role=button.
- Button: the rn notes say 'The role and name stay accessibilityRole/accessibilityLabel, which react-native-web still renders', which reads as though no aria-label is needed, while the conventions require `aria-label` beside every `accessibilityLabel`. I wrote both, from the same `accessibleName ?? accessibilityLabel ?? label` value; the doc should say the aria-label mirror is expected.
- Button: `disabled-does-not-fire` expects `state: disabled is: true`. On rn that is `accessibilityState.disabled`, and the web DOM gets `aria-disabled` set on the node in an effect, which Jest never exercises (Platform.OS is not web). The web mirror is covered by no test; the doc could add a react-native-web assertion or say that it is left to the axe gate.
- Button: `overflowLabel` is typed on ButtonProps but not destructured. With no rest spread it never reaches the Pressable, which is what the doc asks, but the doc does not say whether Toolbar reads it from `element.props` (it can only do that if Button keeps it on its props), so I kept it there and did not render it.
- Button: the `inverse` + non-ghost pressed fill: the doc says only ghost changes fill on inverse surfaces, so other variants use their own backgroundHover when pressed on inverse. It does not say explicitly whether inverseBackgroundHover applies to non-ghost variants; I applied it to ghost only.

## 2026-09-23 13:43 — round 1

- Button: the package digest says every accessibilityValue is mirrored as aria-valuetext, but the spec's `loading` prop says aria-valuetext is not allowed on a button and the react-native-web preview should expose only aria-busy. I followed the spec (no aria-valuetext mirror, only a comment saying why); the digest's 'both spellings, always' rule should name this exception.
- Button: the digest prefers the `role` prop over `accessibilityRole` on RN ≥ 0.87, but platforms.rn.props and the rn notes name `accessibilityRole=button`. I kept `accessibilityRole` as the spec says; the doc could say explicitly whether `role="button"` should replace it.
- Button: the digest says to mirror accessibilityLabel as aria-label, but the rn notes only say the name 'stays accessibilityRole/accessibilityLabel, which react-native-web still renders' and never mention aria-label. I added aria-label with the same resolved name; the rn notes should list aria-label among the mirrors next to aria-busy and aria-expanded.
- Button: `expanded` says 'when an aria-expanded also arrives through ...rest, the prop wins', but the RN Button has no rest spread (only the forwarded props are named), so that precedence rule is moot on rn. I added no rest spread; the doc could mark the ...rest clause as web/Lit only.
- Button: the icon-only-in-a-toolbar example gives `leadingIcon: "Icon name=close"` as a string, but on rn the Icon's `color` needs the ghost foreground token, which a Storybook arg literal cannot resolve. The story uses a small helper that reads the token from useTheme; the example could say the rn story needs a themed wrapper.
- Button: disabled-stays-focusable is web/Lit only, and the rn test for disabled-does-not-fire checks `toBeDisabled()` (from accessibilityState), not focusability. As the rn notes say, nothing tests that disabled is kept off Pressable; that guarantee rests on the code alone.
