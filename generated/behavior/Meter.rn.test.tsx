// Generated by tools/behavior_tests.ts from the `behavior` block of the Meter doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Meter } from '../../packages/rn/src/Meter';
import type { MeterProps } from '../../packages/rn/src/Meter';
import meta from '../../packages/rn/src/Meter.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<MeterProps> = {}) {
  const events = {};
  const props: MeterProps = { ...(meta.args as MeterProps), ...given };
  const tree = (p: MeterProps) => (
    <ThemeProvider mode="light">
      <Meter {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Meter') ?? screen.UNSAFE_root,
    container: () => screen.queryByRole('meter') ?? s.root(),
    rerender: (next: Partial<MeterProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Meter', () => {
  test('value-text-is-shown-and-announced', () => {
    const s = setup({"valueText": "3.2 GB of 10 GB"});
    expect(screen.getByText(new RegExp("3\\.2\\ GB\\ of\\ 10\\ GB"))).toBeOnTheScreen();
  });
  test('the-label-names-the-measurement', () => {
    const s = setup({"label": "Password strength"});
    expect(screen.getByText(new RegExp("Password\\ strength"))).toBeOnTheScreen();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-info', () => {
    const s = setup({"tone": "info"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-success', () => {
    const s = setup({"tone": "success"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-warning', () => {
    const s = setup({"tone": "warning"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-danger', () => {
    const s = setup({"tone": "danger"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('meter', { name: s.props.label })).toBeOnTheScreen();
  });
});
