// Generated by tools/behavior_tests.ts from the `behavior` block of the Container doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Container } from '../../packages/react/src/Container';
import type { ContainerProps } from '../../packages/react/src/Container';
import meta from '../../packages/react/src/Container.stories';

function setup(given: Partial<ContainerProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Container {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Container"]') ?? utils.container.firstElementChild) as HTMLElement,
    column: () => s.root(),
    rerender: (next: Partial<ContainerProps>) => utils.rerender(<Container {...props} {...next} />),
  };
  return s;
}

describe('Container', () => {
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-width-prose', async () => {
    const s = setup({"width": "prose"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-width-content', async () => {
    const s = setup({"width": "content"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-width-page', async () => {
    const s = setup({"width": "page"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-width-full', async () => {
    const s = setup({"width": "full"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gutter-narrow', async () => {
    const s = setup({"gutter": "narrow"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gutter-default', async () => {
    const s = setup({"gutter": "default"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gutter-wide', async () => {
    const s = setup({"gutter": "wide"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-gutter-none', async () => {
    const s = setup({"gutter": "none"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-center', async () => {
    const s = setup({"align": "center"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-start', async () => {
    const s = setup({"align": "start"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-div', async () => {
    const s = setup({"element": "div"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-main', async () => {
    const s = setup({"element": "main"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-section', async () => {
    const s = setup({"element": "section"});
    expect(s.root()).not.toBeNull();
  });
});
