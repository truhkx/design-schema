// Generated by tools/behavior_tests.ts from the `behavior` block of the Breadcrumb doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Breadcrumb } from '../../packages/rn/src/Breadcrumb';
import type { BreadcrumbProps } from '../../packages/rn/src/Breadcrumb';
import meta from '../../packages/rn/src/Breadcrumb.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

type TestNode = ReturnType<typeof screen.getByTestId>;
const CONTROL_ROLES = new Set(["button", "checkbox", "switch", "radio", "textbox", "searchbox", "spinbutton", "combobox", "slider", "link", "tab", "menuitem", "menuitemcheckbox", "menuitemradio", "option", "treeitem", "scrollbar", "adjustable", "imagebutton", "togglebutton"]);
function isControl(n: TestNode): boolean {
  return CONTROL_ROLES.has(n.props.role ?? n.props.accessibilityRole) || typeof n.props.onPress === 'function';
}
const CONTAINER_ROLES = new Set(["dialog", "alertdialog", "combobox", "grid", "listbox", "menu", "menubar", "radiogroup", "tablist", "tree", "treegrid"]);
function hasOwnRole(n: TestNode): boolean {
  return CONTAINER_ROLES.has(n.props.role ?? n.props.accessibilityRole);
}
function activatable(node: TestNode, root: TestNode): TestNode {
  const hit = isControl(node) ? node : node === root || hasOwnRole(node) ? undefined : node.findAll(isControl)[0];
  if (hit === undefined) return node;
  return typeof hit.type === 'string' ? hit : (hit.findAll((n: TestNode) => typeof n.type === 'string')[0] ?? hit);
}

function setup(given: Partial<BreadcrumbProps> = {}) {
  const events = {
    onNavigate: jest.fn(),
  };
  const props: BreadcrumbProps = { ...(meta.args as BreadcrumbProps), ...given, onNavigate: events.onNavigate };
  const tree = (p: BreadcrumbProps) => (
    <ThemeProvider mode="light">
      <Breadcrumb {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Breadcrumb') ?? screen.UNSAFE_root,
    nav: () => screen.queryByRole('navigation') ?? s.root(),
    link: () => screen.queryByTestId('Breadcrumb.link') ?? s.root(),
    rerender: (next: Partial<BreadcrumbProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Breadcrumb', () => {
  test('click-on-an-ancestor-reports-navigation', () => {
    const s = setup({});
    fireEvent.press(activatable(s.link(), s.root()));
    expect(s.events.onNavigate).toHaveBeenCalled();
  });
  test('an-uncollapsed-trail-shows-every-ancestor', () => {
    const s = setup({"collapse": false, "items": [{"label": "Docs", "href": "/docs"}, {"label": "Components", "href": "/docs/components"}, {"label": "Navigation", "href": "/docs/components/navigation"}, {"label": "Breadcrumb", "href": "/docs/components/navigation/breadcrumb"}, {"label": "Keyboard"}]});
    expect(screen.getByText(new RegExp("Components"))).toBeOnTheScreen();
    expect(screen.getByText(new RegExp("Navigation"))).toBeOnTheScreen();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({"label": "Accessible name"});
    expect(screen.getByRole('navigation', { name: s.props.label })).toBeOnTheScreen();
  });
});
