// Generated by tools/behavior_tests.ts from the `behavior` block of the Box doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Box } from '../../packages/rn/src/Box';
import type { BoxProps } from '../../packages/rn/src/Box';
import meta from '../../packages/rn/src/Box.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<BoxProps> = {}) {
  const events = {};
  const props: BoxProps = { ...(meta.args as BoxProps), ...given };
  const tree = (p: BoxProps) => (
    <ThemeProvider mode="light">
      <Box {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Box') ?? screen.UNSAFE_root,
    surface: () => s.root(),
    rerender: (next: Partial<BoxProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Box', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-none', () => {
    const s = setup({"inset": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-sm', () => {
    const s = setup({"inset": "sm"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-md', () => {
    const s = setup({"inset": "md"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-lg', () => {
    const s = setup({"inset": "lg"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-xl', () => {
    const s = setup({"inset": "xl"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-block-none', () => {
    const s = setup({"insetBlock": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-block-sm', () => {
    const s = setup({"insetBlock": "sm"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-block-md', () => {
    const s = setup({"insetBlock": "md"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-block-lg', () => {
    const s = setup({"insetBlock": "lg"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-block-xl', () => {
    const s = setup({"insetBlock": "xl"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-inline-none', () => {
    const s = setup({"insetInline": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-inline-sm', () => {
    const s = setup({"insetInline": "sm"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-inline-md', () => {
    const s = setup({"insetInline": "md"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-inline-lg', () => {
    const s = setup({"insetInline": "lg"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-inset-inline-xl', () => {
    const s = setup({"insetInline": "xl"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-surface-none', () => {
    const s = setup({"surface": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-surface-default', () => {
    const s = setup({"surface": "default"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-surface-subtle', () => {
    const s = setup({"surface": "subtle"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-surface-strong', () => {
    const s = setup({"surface": "strong"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-radius-none', () => {
    const s = setup({"radius": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-radius-sm', () => {
    const s = setup({"radius": "sm"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-radius-md', () => {
    const s = setup({"radius": "md"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-radius-lg', () => {
    const s = setup({"radius": "lg"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-radius-full', () => {
    const s = setup({"radius": "full"});
    expect(s.root()).toBeTruthy();
  });
});
