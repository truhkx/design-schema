The npm organization `design-schema` now exists, so the placeholder scope is the real one: @design-schema/tokens, /react, /lit, /rn. Make the four packages publishable without publishing anything (publishing is a human step):

1. Each packages/*/package.json: remove `"private": true`; add `"publishConfig": { "access": "public" }`, `"license": "MIT"` (also add a root LICENSE file, MIT, copyright the design-schema contributors), `"repository"` (type git, url from `git remote get-url origin` if one exists, else a placeholder the user fills in), `"sideEffects": false` for react/rn and `true` for lit (custom-element registration is a side effect), and a `"files": ["dist"]` list.
2. Build outputs: react, lit and rn currently point `main` at src/index.ts. Add a `build` script per package using tsup (add it as a root devDependency, pinned) emitting ESM + types to dist/, and set `"exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } }` plus `"main"`/`"types"` for older resolvers. rn: mark `react-native` and `react-native-svg` as peerDependencies; react: `react`/`react-dom` peers; lit: `lit` peer. `@design-schema/tokens` stays a normal dependency using the workspace protocol, and pnpm's `publishConfig` should rewrite it to a version range on publish (pnpm does this for `workspace:^`).
3. Root package.json: keep `private: true`; add scripts `build:packages` (runs every package's build) and `release:check` that runs `pnpm build:packages`, `pnpm -r exec npm pack --dry-run` and prints each tarball's file list, so the user can see exactly what would ship.
4. Add a short docs page site/src/content/docs/process/publishing.md: what the packages are, the exact commands to publish (`npm login`, `pnpm -r publish --access public`), that 2FA or a granular access token is needed, and that versions are bumped together with `pnpm -r exec npm version <x>` for now.
Run `pnpm release:check` and `pnpm typecheck`, make both pass, and do not run `npm publish`. Do not modify anything under packages/*/src or generated/.
