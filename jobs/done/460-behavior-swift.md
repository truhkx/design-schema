Generate Swift Testing cases from the docs' `behavior` scenarios for the swiftui platform (tools/behavior_tests gains a swiftui emitter): each scenario becomes a `@Test` that renders the component with the `given` props inside a host view, performs `when` through accessibility identifiers (tap = `.accessibilityAction`, key = `.onKeyPress` injection helper in Support/Testing.swift), and asserts `then` against the accessibility tree (label, value, traits, identifier presence). Output under packages/swiftui/Tests/DesignSchemaTests/Generated/<Name>BehaviorTests.swift. Scenarios with `platforms` excluding swiftui are skipped. Gate: Button's scenarios pass in the workflow. Do not modify packages/*/src or generated/ except the generated test files.
