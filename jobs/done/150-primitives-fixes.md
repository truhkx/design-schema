Three tooling fixes from the Primitives regeneration phase (see generated/gaps/{Icon,Text,Heading}.*.md):

1. tools/lint_literals.py: the font-stack rule matches `fontFamily: '--ds-…'` in override-hook lookup tables. Exempt string values that start with `--` (a custom-property name is not a font stack) and values that are `var(...)`. Add a fixture to tests/test_lint_literals.py.
2. tools/parse.py derived behavior scenarios: `has-accessible-name` currently has no `given`, so components whose accessible name comes from an optional prop (Icon's `label`) fail the scenario at defaults. Derive `given` from the prop the a11y notes name (the first prop whose `a11y` text mentions accessible name / aria-label / accessibilityLabel, or the required `label`/`caption`/`title` prop), using the first non-empty example from the doc's stories guidance or a placeholder string; if no such prop exists and the name is intrinsic (Heading, Button label), keep it as is. Test in tests/test_behavior.py.
3. generate.py: the run for Text.lit "did not return the JSON report block" — the model finished without the trailing report. Treat a missing report as a soft failure: log a WARN gap line "(model did not return the JSON report block)" is already recorded; additionally re-prompt once with a one-line request for the report (cheap, --resume) before counting the round, so files touched are captured in the lock. Test with the fake runner in tests.
Run `pytest -q`; do not modify anything under packages/*/src or generated/.
