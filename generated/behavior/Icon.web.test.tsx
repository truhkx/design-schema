// Generated by tools/behavior_tests.ts from the `behavior` block of the Icon doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Icon } from '../../packages/react/src/Icon';
import type { IconProps } from '../../packages/react/src/Icon';
import meta from '../../packages/react/src/Icon.stories';

function setup(given: Partial<IconProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Icon {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Icon"]') ?? screen.queryByRole('img') ?? utils.container.firstElementChild) as HTMLElement,
    glyph: () => (screen.queryByRole('img') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<IconProps>) => utils.rerender(<Icon {...props} {...next} />),
  };
  return s;
}

describe('Icon', () => {
  test('unlabelled-icon-is-hidden-from-assistive-technology', async () => {
    const s = setup({});
    expect(s.glyph()).toHaveAttribute("aria-hidden", "true");
  });
  test('label-makes-the-icon-meaningful', async () => {
    const s = setup({"name": "warning", "label": "Warning: over quota"});
    expect(screen.getByRole('img')).toBeInTheDocument();
    expect(s.glyph()).not.toHaveAttribute("aria-hidden");
    expect(screen.getByRole('img', { name: "Warning: over quota" })).toBeInTheDocument();
  });
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-check', async () => {
    const s = setup({"name": "check"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-dash', async () => {
    const s = setup({"name": "dash"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-chevron-right', async () => {
    const s = setup({"name": "chevron-right"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-chevron-down', async () => {
    const s = setup({"name": "chevron-down"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-chevron-up', async () => {
    const s = setup({"name": "chevron-up"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-chevron-left', async () => {
    const s = setup({"name": "chevron-left"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-close', async () => {
    const s = setup({"name": "close"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-plus', async () => {
    const s = setup({"name": "plus"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-minus', async () => {
    const s = setup({"name": "minus"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-info', async () => {
    const s = setup({"name": "info"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-success', async () => {
    const s = setup({"name": "success"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-warning', async () => {
    const s = setup({"name": "warning"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-danger', async () => {
    const s = setup({"name": "danger"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-external', async () => {
    const s = setup({"name": "external"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-ellipsis', async () => {
    const s = setup({"name": "ellipsis"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-search', async () => {
    const s = setup({"name": "search"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-arrow-right', async () => {
    const s = setup({"name": "arrow-right"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-arrow-left', async () => {
    const s = setup({"name": "arrow-left"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-calendar', async () => {
    const s = setup({"name": "calendar"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-menu', async () => {
    const s = setup({"name": "menu"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-list', async () => {
    const s = setup({"name": "list"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-grid', async () => {
    const s = setup({"name": "grid"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-play', async () => {
    const s = setup({"name": "play"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-pause', async () => {
    const s = setup({"name": "pause"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-folder', async () => {
    const s = setup({"name": "folder"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-name-file', async () => {
    const s = setup({"name": "file"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-xs', async () => {
    const s = setup({"size": "xs"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-sm', async () => {
    const s = setup({"size": "sm"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-md', async () => {
    const s = setup({"size": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-lg', async () => {
    const s = setup({"size": "lg"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-xl', async () => {
    const s = setup({"size": "xl"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({"label": "Accessible name"});
    expect(screen.getByRole('img', { name: s.props.label })).toBeInTheDocument();
  });
});
