// Generated by tools/behavior_tests.ts from the `behavior` block of the Listbox doc. Do not edit.
import { beforeEach, describe, expect, test, vi } from 'vitest';
import { userEvent } from 'vitest/browser';
import '../../packages/lit/src/Listbox.js';
import meta from '../../packages/lit/src/Listbox.stories.js';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function deep(root: ParentNode, selector: string): Element | null {
  const direct = root.querySelector(selector);
  if (direct) return direct;
  for (const el of Array.from(root.querySelectorAll('*'))) {
    const shadow = (el as HTMLElement).shadowRoot;
    if (shadow) {
      const found = deep(shadow, selector);
      if (found) return found;
    }
  }
  return null;
}

/** Every element that is "active" from the document down through shadow roots: the host, then the inner one.
 *  document.activeElement alone is a shadow host while focus sits inside its shadow tree. */
function activeChain(): Element[] {
  const chain: Element[] = [];
  let el: Element | null = document.activeElement;
  while (el) {
    chain.push(el);
    el = el.shadowRoot?.activeElement ?? null;
  }
  return chain;
}

async function setup(given: Record<string, unknown> = {}) {
  const el = document.createElement('ds-listbox');
  const props = { ...meta.args, ...given };
  for (const [key, value] of Object.entries(props)) {
    if (value === undefined) continue;
    // `children` is slotted light-DOM content, and `Element.children` is getter-only:
    // assigning it throws and takes the whole file down at setup. Slot a string into the
    // default slot the way the stories do; a richer value cannot be expressed here.
    if (key === 'children') {
      if (typeof value === 'string') el.append(value);
      continue;
    }
    (el as unknown as Record<string, unknown>)[key] = value;
  }
  const events = {
    onChange: vi.fn(),
    onActiveChange: vi.fn(),
  };
  el.addEventListener('change', events.onChange as unknown as EventListener);
  el.addEventListener('active-change', events.onActiveChange as unknown as EventListener);
  document.body.append(el);
  await (el as unknown as { updateComplete: Promise<boolean> }).updateComplete;
  const root: ParentNode = el.shadowRoot ?? el;  // some elements render in light DOM
  const s = {
    el,
    root,
    events,
    props,
    root_: () => el,
    list: () => (deep(root, '[role="listbox"]') ?? deep(root, '[part="list"]') ?? deep(root, '[data-part="list"]') ?? root.firstElementChild) as HTMLElement,
    option: () => (deep(root, '[part="option"]') ?? deep(root, '[data-part="option"]')) as HTMLElement,
  };
  return s;
}

beforeEach(() => {
  document.body.replaceChildren();
});

describe('ds-listbox', () => {
  test('click-on-an-option-selects-it', async () => {
    const s = await setup({});
    await userEvent.click(s.option());
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('arrow-selects-as-it-moves-when-selection-follows-focus', async () => {
    const s = await setup({"selectionFollowsFocus": true});
    s.el.focus();
    await userEvent.keyboard('{ArrowDown}');
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('arrows-only-move-when-selection-does-not-follow-focus', async () => {
    const s = await setup({"selectionFollowsFocus": false});
    s.el.focus();
    await userEvent.keyboard('{ArrowDown}');
    expect(s.events.onChange).not.toHaveBeenCalled();
    expect(s.events.onActiveChange).toHaveBeenCalled();
  });
  test('space-selects-the-active-option', async () => {
    const s = await setup({"selectionFollowsFocus": false});
    s.el.focus();
    await userEvent.keyboard(' ');
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('a-disabled-option-cannot-be-selected', async () => {
    const s = await setup({"options": [{"value": "apple", "label": "Apple", "disabled": true}, {"value": "banana", "label": "Banana"}]});
    await userEvent.click(s.option());
    expect(s.events.onChange).not.toHaveBeenCalled();
  });
  test('the-empty-message-shows-when-there-are-no-options', async () => {
    const s = await setup({"options": []});
    expect(s.el.shadowRoot!.textContent).toMatch(new RegExp("No\\ options"));
  });
  test('a-custom-empty-message-replaces-the-default', async () => {
    const s = await setup({"options": [], "emptyMessage": "No fruit matches that."});
    expect(s.el.shadowRoot!.textContent).toMatch(new RegExp("No\\ fruit\\ matches\\ that\\."));
  });
  test('loading-replaces-the-empty-message', async () => {
    const s = await setup({"options": [], "loading": true});
    expect(s.el.shadowRoot!.textContent).toMatch(new RegExp("Loading\u2026"));
  });
  test('invalid-renders-the-invalid-copy', async () => {
    const s = await setup({"invalid": true});
    expect(s.el.shadowRoot!.textContent).toMatch(new RegExp(escapeRegExp(s.props.label) + "\\ is\\ not\\ valid\\."));
    expect(s.list()).toHaveAttribute('aria-invalid', 'true');
  });
  test('renders', async () => {
    const s = await setup({});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-max-visible-5', async () => {
    const s = await setup({"maxVisible": "5"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-max-visible-8', async () => {
    const s = await setup({"maxVisible": "8"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-max-visible-12', async () => {
    const s = await setup({"maxVisible": "12"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-max-visible-all', async () => {
    const s = await setup({"maxVisible": "all"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('has-accessible-name', async () => {
    const s = await setup({});
    expect(s.list()).toHaveAccessibleName(s.props.label);
  });
  test('error-is-identified', async () => {
    const s = await setup({"error": "Fix this before continuing."});
    expect(s.el.shadowRoot!.textContent).toMatch(new RegExp("Fix\\ this\\ before\\ continuing\\."));
    expect(s.list()).toHaveAttribute('aria-invalid', 'true');
  });
});
