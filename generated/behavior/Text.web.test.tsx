// Generated by tools/behavior_tests.ts from the `behavior` block of the Text doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Text } from '../../packages/react/src/Text';
import type { TextProps } from '../../packages/react/src/Text';
import meta from '../../packages/react/src/Text.stories';

function setup(given: Partial<TextProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Text {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Text"]') ?? utils.container.firstElementChild) as HTMLElement,
    text: () => s.root(),
    rerender: (next: Partial<TextProps>) => utils.rerender(<Text {...props} {...next} />),
  };
  return s;
}

describe('Text', () => {
  test('truncated-text-keeps-the-full-string-reachable', async () => {
    const s = setup({"truncate": true, "children": "A sentence long enough to be clipped by its column."});
    expect(s.text()).toHaveAttribute("title", "A sentence long enough to be clipped by its column.");
  });
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-xs', async () => {
    const s = setup({"size": "xs"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-sm', async () => {
    const s = setup({"size": "sm"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-md', async () => {
    const s = setup({"size": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-lg', async () => {
    const s = setup({"size": "lg"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-xl', async () => {
    const s = setup({"size": "xl"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-weight-regular', async () => {
    const s = setup({"weight": "regular"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-weight-medium', async () => {
    const s = setup({"weight": "medium"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-weight-semibold', async () => {
    const s = setup({"weight": "semibold"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-weight-bold', async () => {
    const s = setup({"weight": "bold"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-tone-default', async () => {
    const s = setup({"tone": "default"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-tone-strong', async () => {
    const s = setup({"tone": "strong"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-tone-muted', async () => {
    const s = setup({"tone": "muted"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-tone-danger', async () => {
    const s = setup({"tone": "danger"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-tone-on-action', async () => {
    const s = setup({"tone": "onAction"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-start', async () => {
    const s = setup({"align": "start"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-center', async () => {
    const s = setup({"align": "center"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-align-end', async () => {
    const s = setup({"align": "end"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-p', async () => {
    const s = setup({"element": "p"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-element-span', async () => {
    const s = setup({"element": "span"});
    expect(s.root()).not.toBeNull();
  });
});
