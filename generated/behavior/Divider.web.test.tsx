// Generated by tools/behavior_tests.ts from the `behavior` block of the Divider doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Divider } from '../../packages/react/src/Divider';
import type { DividerProps } from '../../packages/react/src/Divider';
import meta from '../../packages/react/src/Divider.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<DividerProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Divider {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Divider"]') ?? screen.queryByRole('separator') ?? utils.container.firstElementChild) as HTMLElement,
    line: () => (screen.queryByRole('separator') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<DividerProps>) => utils.rerender(<Divider {...props} {...next} />),
  };
  return s;
}

describe('Divider', () => {
  test('decorative-divider-is-hidden-from-assistive-technology', async () => {
    const s = setup({});
    expect(s.line()).toHaveAttribute("aria-hidden", "true");
  });
  test('semantic-divider-is-a-separator', async () => {
    const s = setup({"semantic": true});
    expect(screen.getByRole('separator')).toBeInTheDocument();
    expect(s.line()).toHaveAttribute("aria-orientation", "horizontal");
  });
  test('label-is-read-and-makes-the-divider-semantic', async () => {
    const s = setup({"label": "or"});
    expect(screen.getByText(new RegExp("or"))).toBeInTheDocument();
    expect(screen.getByRole('separator')).toBeInTheDocument();
    expect(screen.getByRole('separator', { name: "or" })).toBeInTheDocument();
  });
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-orientation-horizontal', async () => {
    const s = setup({"orientation": "horizontal"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-orientation-vertical', async () => {
    const s = setup({"orientation": "vertical"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-spacing-none', async () => {
    const s = setup({"spacing": "none"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-spacing-tight', async () => {
    const s = setup({"spacing": "tight"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-spacing-normal', async () => {
    const s = setup({"spacing": "normal"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-spacing-loose', async () => {
    const s = setup({"spacing": "loose"});
    expect(s.root()).not.toBeNull();
  });
});
