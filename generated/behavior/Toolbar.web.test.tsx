// Generated by tools/behavior_tests.ts from the `behavior` block of the Toolbar doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Toolbar } from '../../packages/react/src/Toolbar';
import type { ToolbarProps } from '../../packages/react/src/Toolbar';
import meta from '../../packages/react/src/Toolbar.stories';

function setup(given: Partial<ToolbarProps> = {}) {
  const events = {};
  const props = { ...meta.args, ...given };
  const utils = render(<Toolbar {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Toolbar"]') ?? screen.queryByRole('toolbar') ?? utils.container.firstElementChild) as HTMLElement,
    container: () => (screen.queryByRole('toolbar') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<ToolbarProps>) => utils.rerender(<Toolbar {...props} {...next} />),
  };
  return s;
}

describe('Toolbar', () => {
  test('horizontal-is-the-reported-orientation', async () => {
    const s = setup({});
    expect(s.container()).toHaveAttribute("aria-orientation", "horizontal");
  });
  test('vertical-toolbar-reports-its-orientation', async () => {
    const s = setup({"orientation": "vertical"});
    expect(s.container()).toHaveAttribute("aria-orientation", "vertical");
  });
  test('the-toolbar-is-one-tab-stop', async () => {
    const s = setup({});
    act(() => (s.container()).focus());
    expect(s.container()).not.toHaveFocus();
  });
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-orientation-horizontal', async () => {
    const s = setup({"orientation": "horizontal"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-orientation-vertical', async () => {
    const s = setup({"orientation": "vertical"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-overflow-wrap', async () => {
    const s = setup({"overflow": "wrap"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-overflow-menu', async () => {
    const s = setup({"overflow": "menu"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-overflow-scroll', async () => {
    const s = setup({"overflow": "scroll"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-sm', async () => {
    const s = setup({"size": "sm"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-md', async () => {
    const s = setup({"size": "md"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-density-compact', async () => {
    const s = setup({"density": "compact"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-density-comfortable', async () => {
    const s = setup({"density": "comfortable"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('toolbar', { name: s.props.label })).toBeInTheDocument();
  });
});
