// Generated by tools/behavior_tests.ts from the `behavior` block of the BottomSheet doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { BottomSheet } from '../../packages/rn/src/BottomSheet';
import type { BottomSheetProps } from '../../packages/rn/src/BottomSheet';
import meta from '../../packages/rn/src/BottomSheet.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<BottomSheetProps> = {}) {
  const events = {
    onClose: jest.fn(),
    onDragDismiss: jest.fn(),
  };
  const props: BottomSheetProps = { ...(meta.args as BottomSheetProps), ...given, onClose: events.onClose, onDragDismiss: events.onDragDismiss };
  const tree = (p: BottomSheetProps) => (
    <ThemeProvider mode="light">
      <BottomSheet {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('BottomSheet') ?? screen.UNSAFE_root,
    scrim: () => screen.queryByRole('dialog') ?? s.root(),
    rerender: (next: Partial<BottomSheetProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('BottomSheet', () => {
  test('renders', () => {
    const s = setup({"open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-height-content', () => {
    const s = setup({"height": "content", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-height-half', () => {
    const s = setup({"height": "half", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-height-full', () => {
    const s = setup({"height": "full", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({"open": true});
    expect(screen.getByRole('dialog')).toBeOnTheScreen();
  });
});
