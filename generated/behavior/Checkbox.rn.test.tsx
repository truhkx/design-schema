// Generated by tools/behavior_tests.ts from the `behavior` block of the Checkbox doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Checkbox } from '../../packages/rn/src/Checkbox';
import type { CheckboxProps } from '../../packages/rn/src/Checkbox';
import meta from '../../packages/rn/src/Checkbox.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<CheckboxProps> = {}) {
  const events = {
    onChange: jest.fn(),
  };
  const props: CheckboxProps = { ...(meta.args as CheckboxProps), ...given, onChange: events.onChange };
  const tree = (p: CheckboxProps) => (
    <ThemeProvider mode="light">
      <Checkbox {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Checkbox') ?? screen.UNSAFE_root,
    control: () => screen.queryByRole('checkbox') ?? s.root(),
    label: () => screen.getByText(props.label),
    description: () => screen.getByText(props.description),
    rerender: (next: Partial<CheckboxProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Checkbox', () => {
  test('click-on-control-toggles-on', () => {
    const s = setup({});
    fireEvent.press(s.control());
    expect(s.events.onChange).toHaveBeenCalledWith(true);
    expect(s.control()).toBeChecked();
  });
  test('click-on-label-toggles', () => {
    const s = setup({});
    fireEvent.press(s.label());
    expect(s.events.onChange).toHaveBeenCalledWith(true);
    expect(s.control()).toBeChecked();
  });
  test('click-on-description-toggles', () => {
    const s = setup({"description": "One email a month about new features."});
    fireEvent.press(s.description());
    expect(s.events.onChange).toHaveBeenCalledWith(true);
    expect(s.control()).toBeChecked();
  });
  test('toggles-back-off', () => {
    const s = setup({"defaultChecked": true});
    fireEvent.press(s.control());
    expect(s.events.onChange).toHaveBeenCalledWith(false);
    expect(s.control()).not.toBeChecked();
  });
  test('indeterminate-is-announced-as-mixed', () => {
    const s = setup({"indeterminate": true});
    expect(s.control()).toHaveProp('accessibilityState', expect.objectContaining({ checked: "mixed" }));
  });
  test('disabled-does-not-toggle', () => {
    const s = setup({"disabled": true});
    fireEvent.press(s.control());
    expect(s.events.onChange).not.toHaveBeenCalled();
    expect(s.control()).not.toBeChecked();
    expect(s.control()).toBeDisabled();
  });
  test('required-is-shown-in-the-label', () => {
    const s = setup({"required": true});
    expect(screen.getByText(new RegExp("\\(required\\)"))).toBeOnTheScreen();
  });
  test('error-marks-invalid-and-is-announced', () => {
    const s = setup({"error": "Accept the terms to continue."});
    expect(screen.getByText(new RegExp("Accept\\ the\\ terms\\ to\\ continue\\."))).toBeOnTheScreen();
  });
  test('controlled-follows-prop', () => {
    const s = setup({"checked": false});
    fireEvent.press(s.control());
    expect(s.events.onChange).toHaveBeenCalledWith(true);
    expect(s.control()).not.toBeChecked();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('error-is-identified', () => {
    const s = setup({"error": "Fix this before continuing."});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeOnTheScreen();
  });
});
