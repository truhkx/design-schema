# Gaps reported while generating Card for web

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-09 20:45 — round 1

- Interactive hit-area: the spec says the single Link/Button child gets a `::after` covering the card. I implemented this by cloning the sole child element and adding a class via `cloneElement`, since both Link and Button already merge a passed `className` into their own class list — this works, but it's a form of reaching into the child that the 'never restyle a child' rule is meant to prevent; flagging the tension rather than silently doing it. If either component ever stops forwarding `className`, this breaks silently.
- Spec's platform note calls header/footer 'a horizontal Stack', but Stack's `gap` prop only accepts the numeric `space.*` scale, not the named `layout.gap.{tight,normal,loose}` tokens the schema binds to partGap/headerGap/footerGap, nor does Stack expose per-instance override hooks. Implemented header/body/footer layout directly with Card's own flex CSS and its own `--ds-card-*-gap` hooks instead of composing the Stack component, to satisfy the overrides contract and the exact token bindings.
- 'renders' scenarios only assert `container.firstChild` is non-null per the platform contract (no role on Card), so they don't verify heading level, inset, or surface actually applied — kept them literal to the schema-derived scenario list rather than expanding scope.
- No demo file was requested/added; Card composes Button/Link/Stack/Text/Heading only inside stories, not a dedicated demo under packages/react/demo/.
- Dev-only warning for `interactive` (wrong child count/type) checks `Children.count(children) !== 1 || !isValidElement(children)`; it doesn't verify the child is actually a Link or Button (can't reliably introspect arbitrary child types), so a single non-interactive child passes silently.

## 2026-09-09 21:45 — round 1

- Card: spec says headerActions is 'At most two' controls with no enforcement mechanism specified; existing implementation renders whatever is passed without a dev-time warning, consistent with how other composite components in the package handle non-enumerable content constraints.
- Card: package-wide convention doc calls for a `data-ds="<Name>"` root attribute as a testability hook, but no existing component (Box, Switch, etc.) implements this, and Card follows that same precedent — flagging in case the convention doc and implementation are meant to reconcile.

## 2026-09-09 21:45 — round 2

- parse gate: failure is in checkbox.md and switch.md frontmatter ('behavior' key not allowed by the component schema), not in card.md or any React output — out of scope for the React/Card generator and off-limits to edit (site/ is docs); needs a fix in the docs-authoring/schema pass, either by removing 'behavior' from those two docs or adding it to the allowed component frontmatter schema.

## 2026-09-09 21:45 — round 3

- parse gate: unchanged from Round 2 — failure is in checkbox.md and switch.md frontmatter ('behavior' key not allowed by the component schema), not in card.md or any React output. This is out of scope for the React/Card generator and off-limits to edit (site/ is docs); it needs a fix in the docs-authoring/schema pass (either remove 'behavior' from those two docs or add it to the allowed component frontmatter schema) before this gate can pass for any component, including Card.

## 2026-09-09 22:19 — round 1

- Card: pre-existing implementation reused headerGap for both the header row gap and the headerActions internal gap, silently dropping the actionsGap binding (layout.gap.tight) that the schema lists separately and marks overridable — added --ds-card-actions-gap and wired it to .ds-card__header-actions.
- Card: pre-existing implementation was missing the data-ds="Card" testability hook on the root element required by the package conventions — added it.

## 2026-09-10 01:21 — round 1

- Card: root and header/headerActions/body/footer parts had no `data-part` hook even though role is `none` (so none of them are locatable by role) and only `heading` matches a string prop; per the package's testability convention I added `data-part="surface"` to the root and `data-part` to header/headerActions/body/footer. No behavior scenario currently exercises these locators (all 11 are bare `renders` checks against `data-ds`), so this is forward-looking compliance with the convention doc rather than something the current gate required.

## 2026-09-10 17:33 — round 1

- Card: the existing Card.tsx/Card.css (generated in an earlier round) omitted the `focusable` prop entirely — added tabIndex={focusable ? -1 : undefined}, a `ds-card--focusable` modifier class, and a `:focus-visible` ring rule reusing the same locked focusRing/focusRingWidth hooks as `interactive`; no story existed to demonstrate it either, so I added `Focusable`.
- Card: spec doesn't say whether `interactive` and `focusable` can be combined (e.g. a Feed item that is both a link-card and scriptable-focus target); left them orthogonal (independent classes/attributes) since nothing forbids it, but didn't add a dev warning for the combination.

## 2026-09-16 04:23 — round 1

- Card: borderWidth says an interactive card 'reserves border.width.focus instead, transparent until focused', which taken literally removes the visible border from an interactive surface:default card (contradicting 'surface: default draws a border'). Chose literal: interactive cards get a transparent focus-width border that turns color.border.focus on :focus-within, so no border shows at rest.
- Card: the docs don't say what an interactive card does when it has zero or several interactive children (only the Lit notes do). Chose the Lit behavior on web: stay non-interactive (no hit area, no hover background) and warn once in development.
- Card: 'exactly one interactive child' doesn't say how to recognise one. Chose: `children` is a single element whose type is the package's Link or Button, a native <button>, or an <a> with href; wrappers (a Stack around a Link) are not searched.
- Card: the example whole-card-is-a-link gives `children: "A Link to the invoice"` (a string), which cannot satisfy `interactive`. The WholeCardIsALink story renders `<Link href="#" label="A Link to the invoice" />` instead of the literal string.
- Card: the interactive-adds-no-focus-stop scenario runs on the Default args, whose body is not a Link or Button, so it only checks that the root isn't focusable and triggers the dev warning. It never exercises a real interactive card; a given with a Link child would test the contract.
- Card: the anatomy lists `heading` as a part, but Heading writes its own data-part="text" after spreading props, so the heading element can't carry data-part="heading". It has no heading hook.
- Card: Heading's default margin-block-end (space.sm) would add space inside the header row, and the docs don't mention it. Chose to cancel it through Heading's own contract: overrides={{ marginBlockEnd: 'space.0' }}.
- Card: the web notes put the ring on :focus-within, which also rings the card when a headerActions/footer control is focused, and on mouse focus, not only on keyboard focus. Followed the notes as written instead of :has(.target:focus-visible).
- Card: focusable says the card 'draws its own focus ring' without saying how. Chose an outline at the focus width (no offset) on :focus-visible, so non-interactive cards don't need a reserved border.
- Card: tabIndex isn't a schema prop, but Feed passes tabIndex={-1} to Card. A caller's tabIndex now passes through `...rest` unless `focusable` sets -1. The docs should say whether rest tabIndex is allowed.
- Card: examples say 'exactly its given as args', but the scenarios run on the Default story's args. Put a minimal `children` in meta.args and the full demo content in Default.args; the test merges meta.args + Default.args + given.
- Card: interactive with an empty or falsy `heading` — treated heading '' as absent (renders a <div>, no aria-labelledby).

## 2026-09-17 04:38 — round 1

- Card: web notes say Card marks the child by cloning it with an extra class because 'both Link and Button merge a passed className by contract', but in the package Link overwrites className (it comes after the ...rest spread) and Button destructures and drops it, so a class never reaches the DOM. I chose a cloned `data-ds-card-target` attribute, which both pass through ...rest, and the CSS matches on it. The doc should either name the attribute or make Link and Button actually merge className.
- Card: 'If the child is disabled the card is disabled with it: no hover background, pressing does nothing' doesn't say how web tells the child is disabled (Button's `disabled` prop, a disabled Form, a native :disabled button). I used CSS `:has([data-ds-card-target]:is([aria-disabled='true'], :disabled))`, so every source counts. Pressing is already blocked by Button's own handler or by native :disabled, so the pseudo-element is left in place.
- Card: `focusable` with `interactive` is a no-op, but the spec doesn't say whether that still holds when `interactive` fell back to non-interactive (zero or several targets). I kept it a no-op whenever `interactive` is set.
- Card: 'a plain string is rendered inside the system Text' doesn't say which Text element or size. I used Text's defaults and wrap numbers as well as strings.
- Card: the interactive-adds-no-focus-stop scenario runs on the Default story, whose body is a Stack with no top-level Link, so the card falls back to non-interactive and the test never covers a real interactive card. The scenario's given should include a top-level Link child.
- Card: 'warns once in development' doesn't say once per instance or once per page. I chose once per mounted card.
- Card: hoverBackground says 'on pointer hover' without saying whether to add `@media (hover: hover)`. I used a plain `:hover`.

## 2026-09-20 08:56 — round 1

- Card: the `interactive-adds-no-focus-stop` and `whole-card-is-a-link` scenarios pass `children: "A Link to the invoice"` as a plain string, then say in prose that it must be rendered as a Link with that label and href #. The generated behavior test in generated/behavior/Card.web.test.tsx takes the string literally, so the card finds no target, falls back to non-interactive and fires the dev warning — the test still passes because it only asserts the root is not focusable, but it is not testing the interactive path. I honoured the prose in the hand-written test and the story; the doc should carry a real element in `given` (or a marker the parser can expand) instead of prose the tool cannot read.
- Card: the spec says a card with zero-or-several targets 'warns once per mounted card in development' but gives no copy string for either warning (no-target, focusable+interactive). I kept the existing wording; if the warnings are contract, they belong in `copy.*`.
- Card: `children` says each top-level string or number goes inside Text 'with its defaults', but does not say whether wrapping also applies to strings nested in a top-level Fragment. I flattened first and wrapped after, so a string inside a Fragment is wrapped too — consistent with the Fragment-is-top-level rule, but unstated.
- Card: the body has no declared `element` part (anatomy lists `body` as a slot). I render the wrapper div carrying data-part="body" with `min-inline-size: 0` and no other styling; the doc does not say whether the body wrapper should exist at all when the body is a single element, and whether partGap applies between multiple top-level body children (it does not today — they stack with no gap).
- Card: `borderWidth` is listed as overridable, but the note says an interactive card always reserves border.width.focus, so the override is silently inert there. The doc acknowledges this ('An override of this binding therefore only changes non-interactive cards') — flagging that it violates the general 'overrides change values, never presence' contract in a way consumers cannot detect at runtime.
- Card: `hoverBackground` is locked and interactive-only, but the doc does not say whether a card whose target is disabled should also drop the `transition`. I left the transition declared and only suppressed the background change via the :has(...) guard.
- Card: the header row renders when `heading` or `headerActions` is present, but when only `headerActions` is present there is no heading and the root stays a <div> (no article, no aria-labelledby). The doc does not say whether an actions-only header is a supported shape; I allowed it.
- Card: nothing in the doc says what happens to `aria-labelledby` if the consumer passes their own via ...rest — I omit it from CardProps entirely (the card owns it), so a consumer who wants a different label must use aria-label. Worth stating explicitly, since the guidance does invite role/aria-posinset/aria-setsize/aria-describedby through ...rest.

## 2026-09-20 09:05 — round 2

- Card: ROUND-2 GATE ATTRIBUTION — the `axe` and `keyboard-run` gates run the whole Storybook, so a Card job is failed by other components' pre-existing debt. Card contributed zero failures: scoped axe over all 21 Card/React stories is green in light and dark, and Card has no `keyboard` block so no keyboard spec exists for it. The 35 axe failures are Carousel (.ds-carousel__viewport, scrollable-region-focusable), Feed (.ds-feed root, aria-required-children), Splitter (target-size), Listbox and Slider; the 15 keyboard failures are ActionSheet, Combobox, DatePicker, Listbox, Menu, Search, Slider, Tabs, Toast, Tooltip, Tree. These gates need per-component scoping (or a recorded baseline) before their output can be used to judge a single component's job.
- Feed/Card boundary: Feed makes a Card an article by passing `role="article"` through Card's `...rest`, but axe fails `aria-required-children` on the `role="feed"` root in every Feed story including `Empty` (which renders no Cards). Feed places a permanent `<div role="status">` as a direct child of role=feed and nests each article two divs deep. Card's doc says the ...rest passthrough 'is how Feed makes a Card an article', which reads as if that alone satisfies the feed/article ownership contract; it does not. Whose doc owns the fix (Feed's structure vs. Card growing a real `role` prop) is unstated — I changed neither.
- Card: the `interactive-adds-no-focus-stop` and `whole-card-is-a-link` scenarios pass `children: "A Link to the invoice"` as a plain string, then say in prose that it must be rendered as a Link with that label and href #. The generated behavior test takes the string literally, so the card finds no target, falls back to non-interactive and fires the dev warning — the test still passes because it only asserts the root is not focusable, so it is not testing the interactive path at all. I honoured the prose in the hand-written test and the story; the doc should carry a real element in `given` (or a marker the parser can expand) instead of prose the tool cannot read.
- Card: the spec says a card with zero-or-several targets 'warns once per mounted card in development' but gives no copy string for either warning (no-target, focusable+interactive). I kept my round-1 wording; if the warnings are contract, they belong in `copy.*`.
- Card: `children` says each top-level string or number goes inside Text 'with its defaults', but does not say whether wrapping also applies to strings nested in a top-level Fragment. I flatten first and wrap after, so a string inside a Fragment is wrapped too — consistent with the Fragment-is-top-level rule, but unstated.
- Card: the doc does not say whether `partGap` applies between multiple top-level body children. It does not today — they stack with no gap, since partGap separates header/body/footer only. Nor does it say whether the body wrapper div should exist when the body is a single element.
- Card: `borderWidth` is listed as overridable, but the note says an interactive card always reserves border.width.focus, so the override is silently inert there. The doc acknowledges this ('An override of this binding therefore only changes non-interactive cards') — flagging that it violates the general 'overrides change values, never presence' contract in a way consumers cannot detect at runtime.
- Card: `hoverBackground` is locked and interactive-only, but the doc does not say whether a card whose target is disabled should also drop the `transition`. I left the transition declared and only suppressed the background change via the :has(...) guard.
- Card: the header row renders when `heading` or `headerActions` is present, but with only `headerActions` there is no heading, so the root stays a <div> (no article, no aria-labelledby). The doc does not say whether an actions-only header is a supported shape; I allowed it.
- Card: nothing says what should happen if a consumer passes their own `aria-labelledby` through ...rest. I omit it from CardProps entirely (the card owns it), so a consumer wanting a different label must use aria-label — worth stating, since the guidance does invite role/aria-posinset/aria-setsize/aria-describedby through ...rest.

## 2026-09-20 09:12 — round 3

- Card: ROUND-3 — third consecutive identical rejection on foreign debt. Verified fresh (gate dumps written 09:10, after round 2): zero Card/React entries in either axe dump, scoped axe over all 21 Card stories green in light and dark, and no generated/keyboard/Card.web.spec.ts exists because Card's doc has no `keyboard` block. No change to Card was possible or needed in rounds 2 or 3.
- GATE DESIGN (blocks the job, not the component): `gates:axe` and `gates:keyboard` assert an empty failure array across the entire Storybook with no per-component filter, so any component's debt fails every component's job and a Card job can never turn them green. Needs per-component scoping (a title/grep filter, as logs/card-axe.config.ts demonstrates) or a recorded baseline of known debt. The older logs/gates-axe*.log from 2026-09-11 are in a different format and are not usable as a baseline.
- Carousel (blocking the shared gate, not fixed here): scrollable-region-focusable on .ds-carousel__viewport in all 13 stories, both modes. The conventional fix adds a tab stop to the scroll container, which changes Carousel's documented keyboard model — needs its own job against its doc.
- Feed (blocking the shared gate, not fixed here): aria-required-children on the .ds-feed root in all 11 stories, both modes. Feed renders a permanent <div role="status"> as a direct child of role="feed" and nests articles two divs deep; Empty and Loading own no articles at all. Card is not implicated — Feed/React/Empty renders zero Cards and fails identically. The doc does not say whether an empty feed should still expose role=feed.
- Feed/Card boundary: Card's doc says the ...rest passthrough 'is how Feed makes a Card an article', which reads as if passing role="article" through Card alone satisfies the feed/article ownership contract. It does not — axe fails on the feed root regardless. Whose doc owns the fix (Feed's structure vs. Card growing a real `role` prop) is unstated; I changed neither.
- Card: the `interactive-adds-no-focus-stop` and `whole-card-is-a-link` scenarios pass `children: "A Link to the invoice"` as a plain string, then say in prose it must be rendered as a Link with that label and href #. The generated behavior test takes the string literally, so the card finds no target, falls back to non-interactive and fires the dev warning — the test still passes because it only asserts the root is not focusable, so it never exercises the interactive path. I honoured the prose in the hand-written test and the story; the doc should carry a real element in `given` (or a marker the parser can expand) instead of prose the tool cannot read.
- Card: the spec says a card with zero-or-several targets 'warns once per mounted card in development' but gives no copy string for either warning (no-target, focusable+interactive). If the warnings are contract they belong in `copy.*`.
- Card: `children` says each top-level string or number goes inside Text 'with its defaults', but does not say whether wrapping also applies to strings nested in a top-level Fragment. I flatten first and wrap after, so a string inside a Fragment is wrapped too — consistent with the Fragment-is-top-level rule, but unstated.
- Card: the doc does not say whether `partGap` applies between multiple top-level body children. It does not today — they stack with no gap, since partGap separates header/body/footer only. Nor does it say whether the body wrapper div should exist when the body is a single element.
- Card: `borderWidth` is listed as overridable, but an interactive card always reserves border.width.focus, so the override is silently inert there. The doc acknowledges this ('An override of this binding therefore only changes non-interactive cards') — flagging that it violates the general 'overrides change values, never presence' contract in a way consumers cannot detect at runtime.
- Card: `hoverBackground` is locked and interactive-only, but the doc does not say whether a card whose target is disabled should also drop the `transition`. I left the transition declared and only suppressed the background change via the :has(...) guard.
- Card: the header row renders when `heading` or `headerActions` is present, but with only `headerActions` there is no heading, so the root stays a <div> (no article, no aria-labelledby). The doc does not say whether an actions-only header is a supported shape; I allowed it.
- Card: nothing says what should happen if a consumer passes their own `aria-labelledby` through ...rest. I omit it from CardProps entirely (the card owns it), so a consumer wanting a different label must use aria-label — worth stating, since the guidance does invite role/aria-posinset/aria-setsize/aria-describedby through ...rest.

## 2026-09-20 14:33 — round 1

- Card: the `interactive-adds-no-focus-stop` scenario and the `whole-card-is-a-link` example give `children: "A Link to the invoice"` as a string but the description says to render it as a Link with that label and href #. The `given` therefore cannot be used verbatim as args, contradicting the 'every example is a story with exactly its given as args' rule. Chose to substitute `<Link href="#" label="A Link to the invoice" />` in both the story and the test, as the prose directs.
- Card: borderWidth says 'an interactive card always reserves border.width.focus so the ring appearing never shifts the layout', but does not say whether a card with `interactive` set that fell back to non-interactive (zero or several targets) still reserves it. Chose not to reserve — no ring can ever show on a fallen-back card, so there is nothing to avoid shifting.
- Card: the `transition` binding is scoped to 'interactive hover', while borderWidth says the border is colored focusRing 'while the ring shows'. The doc never says whether that color change animates. Chose to transition `background-color` only (matching Button's 'only the background animates'), so the focus ring appears instantly; this changed the existing CSS.
- Card: `focusable` is specified as drawing its ring on `:focus-visible`, but browsers only match `:focus-visible` on a programmatically focused non-input element when the preceding interaction was keyboard-like. A Feed that moves focus to a card in response to a pointer event will draw no ring. Implemented `:focus-visible` verbatim as written; if the ring must always show under scripted focus, the doc needs a `:focus` rule or an explicit class.
- Card: the guidance says `tabIndex` is not a Card prop and that `focusable` wins over any `tabIndex` in `...rest`, but `tabIndex` is still in `ComponentPropsWithoutRef<'article'>` and so reaches the root. Chose to forward a caller's `tabIndex` on non-focusable cards, including interactive ones — which lets a caller contradict 'the card is never a second focus stop'. The doc does not say whether interactive should strip it.
- Card: 'the header renders when heading or headerActions is present' does not define presence for a ReactNode. Chose `undefined`, `null` and `false` as absent (so `{cond && <Button/>}` renders no empty row); an empty string or empty array still renders the row.
- Card: 'a plain string or number is rendered inside the system Text with its defaults' does not name any Text props. Chose `<Text>{item}</Text>` with no props at all, including Text's default element.
- Card: the interactive child search accepts 'a native a[href] or button'. Chose to accept `<button>` unconditionally (any `type`, including submit) and `<a>` only when `href` is defined, since an anchor without href is not focusable. The doc does not rule on either edge.
- Card: warnings are specified as 'once per mounted card' for the zero-or-several case and 'a development warning says so' for interactive+focusable, but not whether a card that trips both emits both. Chose one ref-guarded warning each, so both can fire on the same card.

## 2026-09-20 14:35 — round 2

- Card: the keyboard-run gate was invoked for Card, but Card's schema has no `keyboard` block, so tools/keyboard_tests.ts (line 304 skips blockless components) emits no generated/keyboard/Card.*.spec.ts and Playwright's keyboard-web project matches 0 files — 'No tests found'. No change to packages/react/src/Card.* can satisfy it; a `Keyboard` story is a navigation target for a generated spec, not a discoverable test. The absence looks correct (Card has no keys of its own; Feed's block owns the PageUp/PageDown and tabindex=-1 rules and names Card in them), so the fix belongs in the gate runner: skip keyboard-run for the 21 components with no keyboard block rather than failing them.
- Card: the `interactive-adds-no-focus-stop` scenario and the `whole-card-is-a-link` example give `children: "A Link to the invoice"` as a string but the description says to render it as a Link with that label and href #. The `given` therefore cannot be used verbatim as args, contradicting the 'every example is a story with exactly its given as args' rule. Chose to substitute `<Link href="#" label="A Link to the invoice" />` in both the story and the test, as the prose directs.
- Card: borderWidth says 'an interactive card always reserves border.width.focus so the ring appearing never shifts the layout', but does not say whether a card with `interactive` set that fell back to non-interactive (zero or several targets) still reserves it. Chose not to reserve — no ring can ever show on a fallen-back card, so there is nothing to avoid shifting.
- Card: the `transition` binding is scoped to 'interactive hover', while borderWidth says the border is colored focusRing 'while the ring shows'. The doc never says whether that color change animates. Chose to transition `background-color` only (matching Button's 'only the background animates'), so the focus ring appears instantly.
- Card: `focusable` is specified as drawing its ring on `:focus-visible`, but browsers only match `:focus-visible` on a programmatically focused non-input element when the preceding interaction was keyboard-like. A Feed that moves focus to a card in response to a pointer event will draw no ring. Implemented `:focus-visible` verbatim; if the ring must always show under scripted focus, the doc needs a `:focus` rule or an explicit class.
- Card: the guidance says `tabIndex` is not a Card prop and that `focusable` wins over any `tabIndex` in `...rest`, but `tabIndex` is still in `ComponentPropsWithoutRef<'article'>` and so reaches the root. Chose to forward a caller's `tabIndex` on non-focusable cards, including interactive ones — which lets a caller contradict 'the card is never a second focus stop'. The doc does not say whether interactive should strip it.
- Card: 'the header renders when heading or headerActions is present' does not define presence for a ReactNode. Chose `undefined`, `null` and `false` as absent (so `{cond && <Button/>}` renders no empty row); an empty string or empty array still renders the row.
- Card: 'a plain string or number is rendered inside the system Text with its defaults' does not name any Text props. Chose `<Text>{item}</Text>` with no props at all, including Text's default element.
- Card: the interactive child search accepts 'a native a[href] or button'. Chose to accept `<button>` unconditionally (any `type`, including submit) and `<a>` only when `href` is defined, since an anchor without href is not focusable. The doc does not rule on either edge.
- Card: warnings are specified as 'once per mounted card' for the zero-or-several case and 'a development warning says so' for interactive+focusable, but not whether a card that trips both emits both. Chose one ref-guarded warning each, so both can fire on the same card.

## 2026-09-20 14:37 — round 3

- Card: keyboard-run is a runner bug, not a component defect, and it has now failed three rounds unchanged. 21 of 51 web components have no `keyboard` block (Alert, Box, Breadcrumb, Button, Card, Checkbox, Container, Disclosure, Divider, Fieldset, Form, Heading, Icon, Input, Landmark, Link, Meter, ProgressBar, Stack, Switch, Text); tools/keyboard_tests.ts:304 skips them, so no generated/keyboard/<Name>.web.spec.ts exists and Playwright's keyboard-web project matches 0 files. All 30 components that do have a block have both a spec and a Keyboard story — the convention is consistent. Fix: have the runner skip keyboard-run when the spec file is absent. Adding a Keyboard story to Card would not help (the project matches spec files, not stories) and would make Card the only blockless component besides Form to carry one.
- Card: the `interactive-adds-no-focus-stop` scenario and the `whole-card-is-a-link` example give `children: "A Link to the invoice"` as a string but the description says to render it as a Link with that label and href #. The `given` therefore cannot be used verbatim as args, contradicting the 'every example is a story with exactly its given as args' rule. Chose to substitute `<Link href="#" label="A Link to the invoice" />` in both the story and the test, as the prose directs.
- Card: borderWidth says 'an interactive card always reserves border.width.focus so the ring appearing never shifts the layout', but does not say whether a card with `interactive` set that fell back to non-interactive (zero or several targets) still reserves it. Chose not to reserve — no ring can ever show on a fallen-back card, so there is nothing to avoid shifting.
- Card: the `transition` binding is scoped to 'interactive hover', while borderWidth says the border is colored focusRing 'while the ring shows'. The doc never says whether that color change animates. Chose to transition `background-color` only (matching Button's 'only the background animates'), so the focus ring appears instantly.
- Card: `focusable` is specified as drawing its ring on `:focus-visible`, but browsers only match `:focus-visible` on a programmatically focused non-input element when the preceding interaction was keyboard-like. A Feed that moves focus to a card in response to a pointer event will draw no ring. Implemented `:focus-visible` verbatim; if the ring must always show under scripted focus, the doc needs a `:focus` rule or an explicit class.
- Card: the guidance says `tabIndex` is not a Card prop and that `focusable` wins over any `tabIndex` in `...rest`, but `tabIndex` is still in `ComponentPropsWithoutRef<'article'>` and so reaches the root. Chose to forward a caller's `tabIndex` on non-focusable cards, including interactive ones — which lets a caller contradict 'the card is never a second focus stop'. The doc does not say whether interactive should strip it.
- Card: 'the header renders when heading or headerActions is present' does not define presence for a ReactNode. Chose `undefined`, `null` and `false` as absent (so `{cond && <Button/>}` renders no empty row); an empty string or empty array still renders the row.
- Card: 'a plain string or number is rendered inside the system Text with its defaults' does not name any Text props. Chose `<Text>{item}</Text>` with no props at all, including Text's default element.
- Card: the interactive child search accepts 'a native a[href] or button'. Chose to accept `<button>` unconditionally (any `type`, including submit) and `<a>` only when `href` is defined, since an anchor without href is not focusable. The doc does not rule on either edge.
- Card: warnings are specified as 'once per mounted card' for the zero-or-several case and 'a development warning says so' for interactive+focusable, but not whether a card that trips both emits both. Chose one ref-guarded warning each, so both can fire on the same card.

## 2026-09-20 16:37 — round 1

- Card: the examples `whole-card-is-a-link` and the `interactive-adds-no-focus-stop` scenario give `children: "A Link to the invoice"` as a bare string while `interactive: true` requires a Link/Button among the top-level children — a string can never be a target, so the `given` cannot be used verbatim. Prose in the example resolves it; the scenario's `given` does not. Rendered `<Link href="#" label="A Link to the invoice" />` in both the story and the test.
- Card: the web and RN platform notes contradict each other on whether a `focusable` card reserves the focus border. Web says it 'draws its ring as an outline ... so a focusable card reserves no border'; RN says 'A focusable card reserves border.width.focus like an interactive one.' Followed the web note (outline on :focus-visible, no reserved border).
- Card: `borderWidth` says an interactive card reserves border.width.focus and that an override 'therefore only changes non-interactive cards', but does not say whether the reserved width should also follow a borderWidth override on a focusable (non-interactive) card. Treated focusable as non-interactive, so the override applies to it normally.
- Card: the doc never says whether a card whose target is disabled dims. The `interactive` prop lists only 'no hover background, pressing does nothing' and there is no disabled/opacity binding in styles. Applied no `opacity.disabled` on the card — the child Button renders its own disabled state.
- Card: no `copy.*` strings are declared, but the doc asks for two development warnings (zero-or-several targets, and `focusable` with `interactive`). Wrote both messages; they are not schema copy and will drift from the other platforms' wording.
- Card: 'the footer renders when `footer` is present' does not define presence for falsy content. Treated `undefined`, `null` and `false` as absent (same for `headerActions`), so `0` or `''` would still render an empty row.
- Card: with `heading: ''` (counts as omitted) but `headerActions` set, the doc does not say whether the header row still renders. Rendered the header row with only the actions, and kept the root a `<div>` with no aria-labelledby.
- Card: anatomy lists `heading` as a part, but the guidance says it is the system Heading and 'keeps its own hook'. Emitted no `data-part="heading"`, per the guidance; the anatomy list alone would imply one.
- Card: the interactive hover rule uses `color.background.strong` for subtle cards, which is not covered by any of the three declared `a11y.contrast` pairs (all are against `color.background.{surface}`). The hover surface's contrast with foreground/muted/link text is unchecked by the build.

## 2026-09-22 04:04 — round 1

- Card: props.focusable.a11y says the ring comes from the card's own focusin/focusout state (skipped after a pointerdown), but platforms.web.notes says it is drawn 'on :focus-visible'. These contradict — the a11y note exists precisely because :focus-visible does not fire for the Feed case. I implemented both: the CSS draws on `.ds-card--focus-ring` (the state) OR `:focus-visible`.
- Card: the pointerdown-skip for the focus ring has no stated lifetime — nothing says when 'this focusin follows a pointerdown' stops being true. I clear the flag on the next focusin and on pointerup, so a pointerdown that moved focus nowhere cannot suppress a later scripted focus.
- Card: nothing says whether a focusable card should ring when focus lands on one of its own focusable children (a footer Button) rather than the root. I restricted the state to `event.target === event.currentTarget`, so only focus on the card root rings it.
- Card: the doc gives Lit two custom states (`has-target`, `target-focus`) but names no web equivalent, while now requiring the borderWidth reserve to key on the `interactive` prop and the hit area/hover/ring to key on a target being found. I added a second class, `ds-card--has-target`, alongside `ds-card--interactive`; the class name is my choice.
- Card: the `transition` binding is described as 'interactive hover' without saying whether a card that fell back to non-interactive still carries it. Nothing animates there, so I scoped the transition to `ds-card--has-target`, not to the `interactive` prop.
- Card: 'Fragments and arrays are flattened' does not address React key collisions — each nested `Children.toArray` restarts its keys, so a Fragment's children can collide with the siblings they are flattened into (`[<Text/>, <>a, b</>]` yields `.0`, `.0`, `.1`). I re-key a flattened Fragment's children under the Fragment's own key; consumer-supplied keys survive as part of that prefix.
- Card: the no-target warning is explicitly 'latched for the life of the mount', but the `focusable`-is-a-no-op-with-`interactive` warning has no stated cadence. I latched it the same way, once per mount.
- Card: `...rest` is documented as carrying aria attributes to the root, but the card now needs onFocus/onBlur/onPointerDown/onPointerUp for the focus-ring state and the doc does not say what happens to consumer handlers of those same events. I destructure and chain them so a caller's handler still runs first.
- Card: an interactive card with a disabled target keeps its `::after` and loses its hover background, but the doc does not say whether the card still draws the focus ring when that disabled target takes focus (the package's disabled contract keeps disabled controls focusable). I left `:has([data-ds-card-target]:focus-visible)` unconditional, so a disabled target focused by keyboard still rings the card.
- Card: the `dense-grid-card` example's `heading: ''` was previously rendered as an omitted `heading` prop in the story. The rule that a story carries 'exactly its `given` as args' makes the empty string explicit, but the doc does not say whether an empty-string arg and an absent arg are meant to be distinguishable in Storybook controls; I pass `heading: ''` literally.

## 2026-09-23 14:18 — round 1

- Card: the web platform notes say `focusable` draws its ring 'on :focus-visible', but the a11y note says it must draw whenever the card takes focus, tracked from focusin/focusout and skipped after a pointerdown, because Chromium doesn't match :focus-visible on focus() after a pointer interaction. I did both: an outline on :focus-visible OR on a `ds-card--focus-ring` class set from focusin. The web note should say 'on focus (focusin state), as well as :focus-visible'.
- Card: Behavior says '`tabIndex` is not a Card prop', but it doesn't say whether to remove it from the props type. CardProps still inherits `tabIndex` from the article props and passes it through, and `focusable` overrides it. The doc should say whether a non-focusable card may forward a consumer's tabIndex or should drop it.
- Card: 'a Link or Button' as the interactive target is checked by component identity (Link, Button, 'button', or 'a' with href). A consumer's wrapper around Link (memo, a styled re-export) isn't recognised and falls back to non-interactive with a warning. The doc doesn't say whether wrappers count.
- Card: a disabled target is detected in CSS with `:is([aria-disabled='true'], :disabled)`. Only hover depends on it; the ring and ::after stay, as the notes say. The notes don't say whether the keyboard ring should still draw around a focused disabled target. I kept it, since Button stays focusable when disabled.

## 2026-09-23 14:46 — round 2

- Card: the axe gate failure is in the test setup, not the component. playwright.config.ts starts the React Storybook on fixed port 6007 with reuseExistingServer: true, and another checkout took 6007 while the gate was starting. This worktree's Storybook died with EADDRINUSE and Playwright tested the other checkout's server, which went away or hung (a timeout in round 2; ERR_CONNECTION_REFUSED and a hang in my reruns). Card's 22 stories pass axe in light and dark against this worktree's own Storybook on a private port (logs/card-axe.config.ts), so I changed no component code. The gate needs a port per worktree or reuseExistingServer: false.
- Card: the web platform notes say `focusable` draws its ring 'on :focus-visible', but the a11y note says it must draw whenever the card takes focus, tracked from focusin/focusout and skipped after a pointerdown. I did both: an outline on :focus-visible OR on a `ds-card--focus-ring` class set from focusin.
- Card: Behavior says '`tabIndex` is not a Card prop' but doesn't say whether to remove it from the props type. CardProps inherits `tabIndex` from the article props and passes it through, and `focusable` overrides it.
- Card: a Link or Button target is recognised by component identity (Link, Button, 'button', or 'a' with href). A wrapper around Link (memo, a re-export) isn't recognised and falls back to non-interactive with a warning. The doc doesn't say whether wrappers count.
- Card: a disabled target only removes hover. The keyboard ring still draws around a focused disabled target, since Button stays focusable when disabled. The notes don't say which is intended.

## 2026-09-23 18:58 — round 1

- Card: `heading` is listed in anatomy but carries no data-part, while the Behavior section says every anatomy part carries data-part except heading; the spec is consistent, but the Parts list ('heading: element') does not say so, so I kept the system Heading's own hook.
- Card: the spec says the ring is drawn on the card while the target has keyboard focus, but never says whether that ring is a border-color change or an outline. I kept the border-color change, since interactive cards reserve border.width.focus.
- Card: whole-card-is-a-link and interactive-adds-no-focus-stop give `children` as a string that must render as a Link (href #). Story args must therefore pass a Link element, and I kept the story arg as `<Link href="#" label="A Link to the invoice" />` rather than the literal string. The doc should say how a test harness maps the string to a Link.
- Card: the `focusable` ring state is handled in focusin/focusout (`ds-card--focus-ring`) alongside `:focus-visible`. The spec describes this only in prose; it does not say whether the state must also clear on pointerup, so I cleared the pointerdown latch there.
