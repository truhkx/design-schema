# Gaps reported while generating Disclosure for rn

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 01:54 — round 1

- The rn platform notes only say the trigger contains "the chevron and a Text" without naming a component; the general icon rule ("never draw an inline SVG... by hand") and Icon's own docstring (which lists "the chevron in a Disclosure" as a usage example) make clear it should be the system Icon, so I render `<Icon name="chevron-right">` rotated 90deg via Animated for the open state, since chevron-right's path rotated 90deg matches chevron-down exactly — the file previously hand-drew the chevron with bordered Views, which I replaced.
- The spec's overridable-bindings table (triggerFontSize etc.) applies to the trigger text, but Icon only accepts an enum `size` (xs/sm/md/lg/xl) tied to fixed tokens, not an arbitrary resolved number. To keep the 1em chevron in sync with an overridden triggerFontSize, I forward the same TokenRef to Icon's own `overrides.size`; the schema doesn't spell this out.
- Web platform notes call for mirroring the chevron under `[dir=rtl]`; RN platform notes say nothing about RTL, and there's a `chevron-left` glyph available but no guidance on when to use it on native. I left the chevron unmirrored (matches the RN notes as given) — flagging in case RTL support is expected there too.
- The previously-generated file was missing `overrides`/`DisclosureOverridableBinding` entirely and the root `testID="Disclosure"` hook required by package convention; both were added to match the other generated components.

## 2026-09-10 17:51 — round 1

- Disclosure: the schema's onToggle event describes a reason of pointer/keyboard/controlled and says 'Accordion relies on it', but RN's Pressable has no way to distinguish a hardware Enter/Space activation from a touch — the same limit the existing Accordion.tsx documents for its own onOpenChange. I added the reason parameter for schema fidelity and self-echo tracking so a standard open/onToggle controlled pairing doesn't double-fire (mirroring packages/react/src/Disclosure.tsx's algorithm), but 'keyboard' can never fire natively — every trigger press reports 'pointer'. Also note RN's actual Accordion.tsx does not consume Disclosure's reason at all (it derives its own 'trigger'/'exclusive'/'controlled' vocabulary independently), so the spec's 'Accordion relies on it' claim doesn't hold for this platform.
- Disclosure: the icon styles.icon binding requires RTL mirroring on native via 'I18nManager.isRTL → chevron-left', which the pre-existing implementation omitted (always rendered chevron-right). Fixed by swapping the base glyph and, because chevron-left's point sits on the opposite side of its glyph from chevron-right, using the opposite open-rotation angle so the expanded state still lands on the down-pointing shape in both writing directions — geometry not spelled out in the spec, derived from packages/rn/src/paths.ts's SVG path data and cross-checked against the web implementation's CSS cascade behavior (packages/react/src/Disclosure.css), which achieves the same visual result via scaleX(-1) + rotate override.
- Disclosure (pre-existing, unchanged): moving focus back to the trigger when the panel closes is documented as impossible on native (no focus-within notion) — left as-is per the existing platform-limit comment, matching the platform notes.

## 2026-09-16 05:06 — round 1

- Disclosure: the `icon` binding says the chevron is `Icon name="chevron-right" inline`, but the rn notes say to pass `triggerFontSize`'s token to the Icon's `overrides.size`. The package Icon ignores `size` and its override while `inline` is set, and inline outside a Text falls back to font.size.md. The two instructions can't both work, so I rendered a non-inline Icon with `overrides.size` set to the `triggerFontSize` override, or 'font.size.md' by default.
- Disclosure: `platforms.rn.props` lists `accessibilityHint`, but nothing says what the hint should contain. The only candidates are copy.expanded / copy.collapsed, and those would repeat the expanded state that `accessibilityState` already announces. I left the hint out.
- Disclosure: copy.expanded and copy.collapsed have no stated use on React Native; only the SwiftUI notes use them, as accessibilityValue. They are unused here, even though the rule says to use every copy string.
- Disclosure: the rule 'disabled uses opacity.disabled on the whole element' conflicts with 'the panel keeps its current state'. The web CSS dims only the trigger. I followed web and dimmed only the trigger.
- Disclosure: `triggerBackgroundHover` is state `hover`, described as 'pointer hover and pressed'. Pressable's style callback reports only `pressed`, so I track hover with onHoverIn/onHoverOut and apply the background for pressed or hovered while not disabled.
- Disclosure: every example passes a plain string as `children`, which React Native can't render directly inside a View. The panel wraps string or number children in the package `Text`. The spec doesn't say whether that is the component's job or the story's.
- Disclosure: the `panelColor` binding (color.foreground) can't be applied to arbitrary children on native because there is no style inheritance. Only the wrapped string case gets it, through Text's default tone.
- Disclosure: the `onToggle` reason `keyboard` can't be told apart from a tap on a Pressable, so every trigger press reports `pointer`. A controlled `open` change the component didn't cause reports `controlled`, and a controlled echo of its own press isn't reported twice.
- Disclosure: `useReducedMotion()` resolves asynchronously, so a chevron rotation can start before reduce motion is known on first render.
- Disclosure: the package digest says `toLineHeight(t.fontLineHeightNormal, t.fontSizeMd)`, but theme.tsx's signature is `toLineHeight(fontSize, multiplier)`. I followed the code.
- Disclosure: the digest says `useNativeDriver: false` for layout props. The chevron animates only a transform, so I kept the native driver on native and turned it off on web.
- Disclosure: the scenario `disabled-trigger-stays-focusable` is limited to web and Lit, and a Pressable has no focusable assertion in RNTL. Not tested on React Native.
- Disclosure: the scenarios `renders-heading-level-*` only check that it renders. Native has no heading level to assert, and the header role sits on the summary text, not the button.

## 2026-09-17 05:15 — round 1

- Disclosure: the notes say the chevron snaps 'until useReducedMotion resolves', but the package's useReducedMotion() returns a plain boolean that reads false until the OS answers, so the component can't tell when it has resolved. I made only the first effect run snap and animated every later change. A toggle that comes before the OS answers still animates even if reduce motion is on.
- Disclosure: the package digest shows toLineHeight(t.fontLineHeightNormal, t.fontSizeMd), but theme.tsx declares toLineHeight(fontSize, multiplier). I followed the real signature (font size first); the digest example has the arguments the wrong way round.
- Disclosure: the spec says keepMounted hides the closed panel with display: 'none' but doesn't say whether it also leaves the accessibility tree. I added accessibilityElementsHidden and importantForAccessibility='no-hide-descendants' while closed, to match the Accessibility section ('not in the accessibility tree either way').
- Disclosure: the Animated section says useNativeDriver: false for layout props, but the chevron animates a transform, which isn't a layout prop. I used useNativeDriver: Platform.OS !== 'web'. The spec doesn't say which to use for transforms.
- Disclosure: the triggerFontFamily/triggerFontSize/triggerFontWeight bindings style the summary text, but the Composition rule says to use the package Text, and that Text has no overrides for family, size or weight. I kept a plain react-native Text with every binding applied explicitly and the header role on it. Either Text needs those overrides, or the doc should say that a plain react-native Text is the right choice for the summary.
- Disclosure: when the component is controlled and the consumer ignores a press, the pending request is only cleared at the next open change. If that change sets open to the value the ignored press asked for, it is treated as an echo and does not fire 'controlled'. The spec's wording allows this, but it doesn't say whether that case should report 'controlled'.
- Disclosure: the platform notes say react-native-web renders testID as data-testid, but the Keyboard-story rule doesn't apply because the schema has no keyboard block. So no Keyboard story ships, and the axe/manual keyboard check on react-native-web has no story that renders the disclosure open with three focusable children. OpenWithFormFields only has text children.

## 2026-09-21 03:08 — round 1

- Disclosure: the rn notes say the summary is the package Text with accessibilityRole="header" when headingLevel is set, and make the plain-RN-Text stopgap conditional on Text not accepting the four typography overrides. Text does accept all four (TextOverridableBinding = fontFamily|fontSize|fontWeight|lineHeight) but has no accessibilityRole prop at all, so the package Text can never carry the header role. Took the stopgap for the real reason instead — a plain react-native Text with triggerFontFamily/Size/Weight/LineHeight and triggerColor applied directly. The stopgap condition in the doc should name the missing accessibilityRole, not the overrides.
- Disclosure: the notes say the summary Text's overrides receive the trigger bindings '(colour triggerColor)', but Text's color binding is locked and unreachable through overrides — its default tone happens to resolve to color.foreground, which is triggerColor, so the two agree by coincidence. Unstated whether that coincidence is the intent or Text should grow a way to be told.
- Disclosure: triggerBackgroundHover has no 'not hovered' token. Used the literal 'transparent' for the resting trigger background (and for the resting focus-ring border colour). No token expresses 'no background'.
- Disclosure: focusRing/focusRingWidth say only to draw a border of borderWidthFocus in colorBorderFocus when focused. A border that only exists while focused shifts the trigger's layout on focus, so a transparent border of the same width is always present. The doc does not say which it wants.
- Disclosure: the chevron's overrides.size is passed `overrides?.triggerFontSize ?? 'font.size.md'` — a TokenRef, since Icon.overrides takes refs, not resolved numbers. That means the default is the literal token name 'font.size.md' rather than whatever triggerFontSize's schema default is, so the two would drift if the binding's default token changed. The doc says 'the same token as triggerFontSize (default font.size.md)' without saying how to keep them in sync.
- Disclosure: minTarget is applied as both minHeight and minWidth on the trigger. With triggerPaddingBlock/Inline the visual box already exceeds size.target.min, so no hitSlop is added; the doc's target-24px requirement mentions hitSlop 'where the visual is smaller' without saying whether the min-dimensions alone satisfy it for a text trigger.
- Disclosure: the trigger is alignSelf: 'flex-start' so the hover/pressed background hugs the summary rather than spanning the parent's width. Nothing in the schema states the trigger's width; on web the button is inline-level by default, and this is the closest native reading, but it is a guess.
- Disclosure: copy.expanded/copy.collapsed are declared but the rn platform notes say they are unused here (accessibilityState announces the state). Followed the platform notes over the 'use every copy.* string verbatim' rule; the copy block is dead weight for rn.
- Disclosure: reason 'keyboard' is unreachable on rn — Pressable cannot distinguish a hardware Enter/Space from a tap — so DisclosureToggleReason includes a value this platform never emits. Already acknowledged by the schema, but it means the payload union is wider than the platform's behaviour.
- Disclosure: the controlled scenario's rerender starts the chevron's Animated.timing outside act(), which logs a React act() warning during the test (alongside the pre-existing one from theme's async reduced-motion read). Tests pass; the doc gives no guidance on whether the first animated change after a controlled open change should snap instead.
- Disclosure: with keepMounted the closed panel keeps display:'none' plus accessibilityElementsHidden/importantForAccessibility='no-hide-descendants' as instructed — but the rn convention forbids those on a View containing a Pressable, and a keepMounted panel holding form fields will usually contain controls. The doc does not reconcile the two; followed the Disclosure-specific instruction.

## 2026-09-21 03:12 — round 2

- Disclosure: the rn notes say `accessibilityState={{ expanded, disabled }}` is how the state is conveyed, but react-native-web 0.21 ignores accessibilityState entirely — under the axe gate (which runs the RN stories on react-native-web) neither state reaches the DOM. Both had to be mirrored: aria-expanded as a prop, aria-disabled written onto the node in an effect (Pressable's own `disabled` would set the native attribute and drop the trigger from the tab order). The doc should say accessibilityState alone is not sufficient on rn, as Button, Checkbox, Switch and RadioGroup already work around.
- Disclosure: disabledOpacity dims the trigger including its text, which axe reads as a contrast failure unless the trigger is marked disabled in the DOM. The doc asks for the dimming and for the trigger to stay focusable without saying that the two only coexist because WCAG exempts inactive controls — worth stating, since it is the whole reason the aria-disabled mirror is load-bearing rather than cosmetic.
- Disclosure: the rn notes say the summary is the package Text with accessibilityRole="header" when headingLevel is set, and make the plain-RN-Text stopgap conditional on Text not accepting the four typography overrides. Text does accept all four (TextOverridableBinding = fontFamily|fontSize|fontWeight|lineHeight) but has no accessibilityRole prop at all, so the package Text can never carry the header role. Took the stopgap for that reason instead — a plain react-native Text with triggerFontFamily/Size/Weight/LineHeight and triggerColor applied directly. The stopgap condition should name the missing accessibilityRole, not the overrides.
- Disclosure: the notes say the summary Text's overrides receive the trigger bindings '(colour triggerColor)', but Text's color binding is locked and unreachable through overrides — its default tone happens to resolve to color.foreground, which is triggerColor, so the two agree by coincidence. Unstated whether that is the intent or Text should grow a way to be told.
- Disclosure: triggerBackgroundHover has no 'not hovered' token. Used the literal 'transparent' for the resting trigger background (and for the resting focus-ring border colour). No token expresses 'no background'.
- Disclosure: focusRing/focusRingWidth say only to draw a border of borderWidthFocus in colorBorderFocus when focused. A border that exists only while focused shifts the trigger's layout on focus, so a transparent border of the same width is always present. The doc does not say which it wants.
- Disclosure: the chevron's overrides.size is passed `overrides?.triggerFontSize ?? 'font.size.md'` — a TokenRef, since Icon.overrides takes refs, not resolved numbers. The default is therefore the literal token name rather than whatever triggerFontSize's schema default is, so the two would drift if that default changed. The doc says 'the same token as triggerFontSize (default font.size.md)' without saying how to keep them in sync.
- Disclosure: minTarget is applied as both minHeight and minWidth on the trigger; with the trigger padding the visual box already exceeds size.target.min, so no hitSlop is added. The target-24px requirement mentions hitSlop 'where the visual is smaller' without saying whether min-dimensions alone satisfy it for a text trigger.
- Disclosure: the trigger is alignSelf: 'flex-start' so the hover/pressed background hugs the summary rather than spanning the parent's width. Nothing in the schema states the trigger's width; on web the button is inline-level by default and this is the closest native reading, but it is a guess.
- Disclosure: copy.expanded/copy.collapsed are declared but the rn notes say they are unused here (accessibilityState announces the state). Followed the platform notes over the 'use every copy.* string verbatim' rule; the copy block is dead weight for rn.
- Disclosure: reason 'keyboard' is unreachable on rn — Pressable cannot distinguish a hardware Enter/Space from a tap — so DisclosureToggleReason includes a value this platform never emits. Acknowledged by the schema, but the payload union is wider than the platform's behaviour.
- Disclosure: the controlled scenario's rerender starts the chevron's Animated.timing outside act(), logging a React act() warning during the test (alongside the pre-existing one from theme's async reduced-motion read). Tests pass; the doc gives no guidance on whether the first animated change after a controlled open change should snap instead.
- Disclosure: with keepMounted the closed panel keeps display:'none' plus accessibilityElementsHidden/importantForAccessibility='no-hide-descendants' as instructed — but the rn convention forbids those on a View containing a Pressable, and a keepMounted panel holding form fields will usually contain controls. The doc does not reconcile the two; followed the Disclosure-specific instruction.

## 2026-09-23 13:54 — round 1

- Disclosure: the web notes say `ref` resolves to the trigger (Accordion moves focus between triggers with it), but the rn rules say a component exposes its root as `ref?: Ref<ViewInstance>`. The rn notes don't say which one applies on native. I kept `ref` on the root View; a native Accordion that needs to focus triggers would have no way to reach the Pressable.
- Disclosure: `fullWidth` says 'native stretches the Pressable across its container' but doesn't say how. I used `alignSelf: 'stretch'` in place of the default `flex-start`, and left the icon and summary at the start through the row's default justify-content. The root View's own width isn't specified, so the trigger only fills a row as wide as the root's parent gives it.
- Disclosure: `triggerBackgroundHover` names `onHoverIn`/`onHoverOut` or pressed on RN. PressableStateCallbackType only reliably provides `pressed`, so hover is tracked in local state. The spec doesn't say which to prefer.
- Disclosure: no `keyboard` block in the schema, so no `Keyboard` story was added. The a11y section still requires keyboard-operable (Enter/Space), which on native is just the Pressable's default activation.
- Disclosure: the `disabled-trigger-stays-focusable` scenario is web/lit only, and the 11 rn scenarios don't include it, so the rn test doesn't check it. The `Disabled` example story's description still says 'still focusable', which RNTL can't verify.
- Disclosure: the `controlled-open-change-reports-controlled` scenario says `when: set: open: true`, which isn't an interaction RNTL has. It's written as a `rerender` with the new prop. The spec doesn't name the mechanism.

## 2026-09-23 13:55 — round 1

- Disclosure: the rn notes say react-native-web gets `aria-expanded` as a prop and `aria-disabled` set on the node in an effect, but the package rule says to mirror every accessibility prop as `aria-*`, including `aria-label` next to `accessibilityLabel`. The notes don't mention `aria-label`; I added it on the trigger.
- Disclosure: the generate prompt says `disabled` sets `disabled` as well as `accessibilityState.disabled`, but the rn notes (and the package rule) say not to pass `disabled` to Pressable because it drops the trigger from the tab order. I followed the notes: a press guard plus `aria-disabled` set on the node on web only.
- Disclosure: the rn notes don't say whether a closed `keepMounted` panel also needs `aria-hidden` for react-native-web. I relied on `display: 'none'`, which removes it from the DOM accessibility tree, plus `accessibilityElementsHidden`/`importantForAccessibility` for native.
- Disclosure: `triggerIcon` is described as a wrapper View around the Icon, but nothing says whether it should shrink or how it aligns vertically when the summary wraps. I kept it as an Animated.View, centred by the trigger's `alignItems: 'center'`.
- Disclosure: `ref` resolves to the trigger button on web (Accordion moves focus with it), but the rn rules put it on the root View. I followed the rn rules, so an rn Accordion cannot move focus between triggers through `ref`.
- Disclosure: the transition binding says later changes animate unless `useReducedMotion` reads true 'at that moment', but not what happens when the reduced-motion answer arrives mid-animation or after a toggle. The effect reruns on that change and snaps the chevron to its current target.
- Disclosure: the scenario `disabled-trigger-does-not-toggle` checks `state: disabled`. On rn that is `accessibilityState.disabled` (RNTL `toBeDisabled`), not focusability, as the notes say. `disabled-trigger-stays-focusable` is web/Lit only, so native has no test that the disabled trigger stays focusable.
