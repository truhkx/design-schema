# Gaps reported while generating Popover for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 03:06 — round 1

- heading: no level is specified in the schema for the composed <ds-heading>; chose level=3 (Popover sits one step below a typical Dialog's level 2). A future 'headingLevel' prop would remove the guess.
- trigger accessible-name fallback: since aria-controls can't cross the shadow boundary and the trigger is arbitrary slotted content, the panel's aria-label (when heading is unset) is read from the trigger's aria-label/label/textContent attributes in that order. This works for <ds-button label="..."> authored via attribute syntax (as in the stories) but not for a trigger whose accessible name is set only via a JS property binding (`.label=`) or is entirely composed inside another element's shadow root — no generic cross-shadow-boundary way to read an arbitrary element's computed accessible name exists from outside.
- trigger activation (keyboard 'Enter/Space toggles from the trigger') is implemented via a native `click` listener on the slotted trigger, matching the web platform note's 'onClick toggle' — this covers any native-activatable element (button, link) but not a non-native focusable trigger (e.g. a plain <div tabindex="0">) that doesn't itself convert Enter/Space to a click.
- 'has-accessible-name' scenario is ambiguous for a closed-by-default Popover: the host `ds-popover` itself carries no role/aria-label (only the trigger and, once open, the panel do). Implemented so the trigger always has its own accessible name; flagging in case the scenario expects the host element itself to expose one.
- arrow (showArrow): rendered as a single rotated square with a border only on its top+left edges (a common CSS shortcut for a diamond pointer), so it doesn't perfectly blend into the panel border for every placement side — a pixel-perfect per-side border was out of scope for the described 'small pointer'.
- 'inset' token governs the panel's own padding (matching Dialog's precedent for its 'inset' binding); the anatomy's separate 'body: Box' composition is used for the default-slot wrapper without setting Box's own `inset` attribute, mirroring how Dialog.ts composes its body Box.
- dismissible defaults to true and the schema's platforms.lit.reflect list does not include it, but the generic 'booleans that default to true' rule requires a negated, reflected attribute (`no-dismiss`) — implemented via a custom Lit converter since it's not in the reflect list and no existing component demonstrates this exact pattern (Dialog.ts's own `dismissible` does NOT follow this rule, reflecting a plain, non-negatable boolean instead).

## 2026-09-10 18:06 — round 1

- Popover: the existing generated Popover.ts had `heading-level` listed under platforms.lit.reflect and headingLevel in the schema (enum '2'|'3'|'4', default '3'), but the class had no headingLevel property and the panel heading was hardcoded to level="3" — added the missing @property (reflected, attribute heading-level), wired it into the <ds-heading level> binding, exported PopoverHeadingLevel from index.ts, and added the HeadingLevel2/3/4 stories plus the headingLevel arg/argType that were also missing.

## 2026-09-16 06:59 — round 1

- Popover: Shift+Tab from the first element closes the popover, but the event's reasons only include `tab-out` ('Tab moved focus past the end'). I report `tab-out` for both directions; the doc should say so or add a reason.
- Popover: overlay.dismiss lists `focus-out`, but the web notes say there's no focusout listener (the Tab/Shift+Tab handlers own keyboard exits) and the Lit notes say nothing. I added no focusout listener, so focus moved out by other means (a click on a non-focusable area is covered by outside-press; programmatic focus is not) leaves the popover open.
- Popover: the Keyboard section expects Tab and Shift+Tab to close the popover, while the Keyboard story renders it open with `open: true`, which makes it controlled, and a controlled element doesn't change state by itself. The story acts as the consumer and writes `open-change` back into `open`. The doc should say whether gate stories may do this or should use an uncontrolled way to start open (there is no defaultOpen prop).
- Popover: for Tab from the last element to 'close and move focus to the element after the trigger', the panel must be hidden before the browser moves focus. When controlled, that only happens if the consumer sets `open=false` synchronously inside the `open-change` listener. An async consumer leaves focus on the next element in the still-open panel's tab order.
- Popover: the composition gives Heading, Box, Button and FocusScope but no props for them. I pass Heading only `level` (plus tabindex=-1 so it can take focus when there's no control), give Box none, give the close Button variant=ghost size=sm icon-only with a close icon, and FocusScope trapped=modal, active=open, autoFocus=none and restoreFocus=false (the popover moves and restores focus itself). The doc should list these props.
- Popover: the docs say 'moves focus to the first control (or the heading, if there are none)' without saying whether the close button counts as a control. I try body controls first, then the close button, then the heading.
- Popover: Lit can't point aria-controls across the shadow boundary, so the trigger only gets expanded state. Setting aria-expanded on a ds-button host is ignored because the inner <button> carries the role, so for triggers with an `expanded` property I set that property instead, and aria-expanded otherwise. The Lit notes say 'aria-expanded is set on the slotted trigger'; they should mention Button's `expanded`.
- Popover: I read the panel's 'trigger text copied into aria-label' fallback from the trigger's aria-label, then accessibleName, then label, then textContent. The doc doesn't say how to find the text of a custom-element trigger whose name lives in its shadow root.
- Popover: the examples' `trigger` and `children` given values are descriptions ('A Filters Button'), not values a Lit arg can render. Each example story keeps those strings as args to meet the 'exactly its given' rule and renders real content in its own render function.
- Popover: the date-picker-panel example puts 'A DatePicker calendar' inside the panel, but Lit's ds-date-picker is a field with its own popup, so the example nests an overlay inside a popover, which the guidance forbids ('Do not nest popovers'). I rendered <ds-date-picker> as is; the doc needs a calendar-only DatePicker or another example.
- Popover: `enter` says 'a space.1 slide from the trigger side', but no binding or hook covers the slide distance. I hard-wired var(--space-1), so it can't be overridden.
- Popover: the close button's 24px target and the heading's clearance next to it rely on `--size-target-min`, which no style binding declares. I reserved inline-end padding of size.target.min + partGap when dismissible.
- Popover: the arrow has no binding for its border or fill. I reuse the panel's surface token and the border/borderWidth hooks. Centering it on the panel edge follows the web notes, since the Lit notes say nothing about the arrow.
- Popover: the has-accessible-name derived scenario doesn't say what should be named, and the Default story is closed. The test checks the trigger's native button. If it should check the panel dialog, the scenario needs `open: true`.

## 2026-09-17 10:48 — round 1

- Popover: composition says focusScope receives exactly trapped/autoFocus/restoreFocus, but the content stays mounted through the exit transition and a mounted, trapped ds-focus-scope pulls focus back on document focusin, which would undo focus-restore to the trigger. I also pass `active` bound to the open state (React's Popover.tsx does the same, `active={open}`); the doc should list `active: from open` or say the scope unmounts before the exit.
- Popover: composition lists closeButton prop `leadingIcon: close`, but Lit ds-button has no leadingIcon property, only a `leading-icon` slot; I slotted `<ds-icon name="close">` there.
- Popover: the Behavior order for the first focus target (body control, then close button, then heading) disagrees with the tab order the header row produces (close button before body), so 'the first element in the panel' for Shift+Tab is the close button when dismissible. I used DOM order [closeButton, ...body] for Tab/Shift+Tab and the doc's order for initial focus; the doc should say which 'first' the Shift+Tab rule means.
- Popover: Lit notes say Tab from the last element focuses 'the first focusable element after the host' but define neither 'focusable' (native tabbables vs ds-* hosts with delegatesFocus) nor how positive tabindex or hidden/inert elements count; I used document order over native tabbables plus the ds-* field/button/link tags, skipping tabindex=-1, inert and unrendered elements.
- Popover: the keyboard rules give no behavior for Tab/Shift+Tab when the panel has no tabbable element (focus on the heading or panel itself, dismissible false, no controls); I treated Tab as tab-out forward and Shift+Tab as tab-out to the trigger.
- Popover: the modal Escape rule assumes focus is inside, but a modal <dialog> also gets a native cancel on Escape before focus arrives, and Chrome can close it without a cancel; I report both as reason `escape` and reopen if still controlled open. The doc could state that.
- Popover: the reason type is exported as `PopoverCloseReason` (the existing index name) although it includes `trigger` for opening; siblings use `<Name>OpenChangeReason` (MenuOpenChangeReason). The doc/naming should pick one; I kept the existing name to avoid breaking the export.
- Popover: arrowSize says the arrow is 'centered on the panel edge' without saying whether the center sits on the border's outer edge or its centerline; I centered it on the border centerline (offset by half the borderWidth).
- Popover: `offset` as a facing-side margin only shifts the box for the bottom/right sides under top/left positioning; for top/left sides I subtract the read-back margin in the position math. The doc doesn't say the margin is purely informational on those sides.
- Popover: example `filter-panel` says 'A Form of filter controls' and `required-step` 'an email Input and a Save Button' but neither names the submit control's placement in ds-form; I put Apply/Save in ds-form's `actions` slot.
- Popover: the Behavior says stories needing to start open render through a wrapper that owns `open`, but examples must have 'exactly its given' as args, and none of the examples give `open`, so FilterPanel, DatePickerPanel, RequiredStep and ContextualHelp render closed (uncontrolled) — confirm that is intended.
- Popover: `has-accessible-name` (derived) doesn't say whether it targets the trigger or the panel; per the Behavior note (derived scenarios find the named panel) I test the panel's name against the Default story.

## 2026-09-21 08:03 — round 1

- Popover: the Tab keyboard rule cannot pass on Lit as generated. The gate's focusable walker (generated/keyboard/Popover.lit.spec.ts) does not descend slot assignments, so inside the panel it sees exactly one element — the close button (probe confirms: walker sees ['button:Close'], while real tab order is Filters -> Close -> 7 days -> 30 days -> 90 days -> Filters). The Tab test then focuses `count-1` (the close button) and demands the popover close, while the Shift+Tab test focuses index 0 (the same close button) and demands it close as the *first* element. Only one of the two can hold. Because the doc pins the header row (with the close button) before the body, and because on Lit the body is slotted light DOM rather than a portaled descendant as on web, the harness cannot express 'the last element in the panel'. I implemented the rule as written (Tab from the genuine last tabbable prevents default, focuses the first focusable after the host else the trigger, and reports tab-out), which the probe shows is correct, and left the gate red rather than distorting the component.
- Popover: the doc gives `maxWidth: layout.maxWidth.prose` as the only inline-size constraint, but `collision: flip-shift` cannot bring a panel wider than the viewport into view. I clamped to `min(var(--ds-popover-max-width), calc(100vw - 2 * var(--layout-gutter)))`; the gutter clamp is my addition and is not in the schema.
- Popover: the doc never says what names the panel when `heading` is absent *and* the trigger has no readable name (no aria-label, no `accessibleName`, no `label`, empty textContent). RN specifies a __DEV__ warning for exactly this case; web and Lit specify none. I render no `aria-label` at all, which silently fails the derived `has-accessible-name` scenario. Either extend RN's missing-name warning to Lit, or state a fallback.
- Popover: the doc lists `open` under `platforms.lit.reflect`, but `open` is also the controlled/uncontrolled switch (`undefined` = uncontrolled). A boolean attribute cannot express `undefined`, so once a consumer sets and then removes the `open` attribute the property is `false`, not `undefined`, and the popover is permanently controlled. I reflect it as declared and treat only a never-set property as uncontrolled; the doc should say which wins.
- Popover: `props.trigger` is `required: true` and 'exactly one focusable element', but a Lit slot cannot enforce cardinality and the doc specifies no Lit dev warning (only RN's). I added an `import.meta.env.DEV` warning when the `trigger` slot does not hold exactly one element, and another when the default slot is empty — both invented.
- Popover: story-naming rules conflict for `showArrow`. 'Stories are named after the prop and value in PascalCase' gives `ShowArrow`, but React exports `WithArrow` and the parity rule requires the same export name. I chose `WithArrow`. Same class of conflict would hit any boolean prop whose React story is named for the effect rather than the prop.
- Popover: the `date-picker-panel` example requires 'a fixed literal date label (the story does not compute today)' but does not say what the label is, so every platform must invent one and they will drift. I copied React's `16 September 2026`. Put the literal in the example's `given`.
- Popover: `focusScope` is in `anatomy` and `composition`, but the Lit notes enumerate only heading, body and close button as popover-owned wrappers carrying `data-part`. I put `data-part="focusScope"`/`part="focusScope"` directly on `<ds-focus-scope>` rather than wrapping it; the doc should say whether a composed part that is itself a wrapper component gets one too.

## 2026-09-21 08:13 — round 2

- Popover: the Tab keyboard rule cannot pass on Lit, and the cause is the gate harness, not the component. `focusables()` in tools/keyboard_tests.ts walks with querySelectorAll plus shadowRoot descent and never follows <slot> assignments, so inside a Lit panel whose content is slotted it sees only the component's own shadow-side controls — for Popover exactly one, the close button (probe: gate walker sees ["Close"]; real tab order is Filters, Close, 7 days, 30 days, 90 days, Filters). `from: first` emits focusAt(root, 0) and `from: last` emits focusAt(root, count-1), which are therefore the SAME element, and the two generated tests demand opposite outcomes from it: Shift+Tab must close (it is the genuine first, and it does — that test passes) and Tab must also close (it is not the genuine last, and correctly does not). No DOM arrangement satisfies both while the doc pins the header row, and so the close button, before the body. This is systemic: run against other slotted Lit overlays, Dialog fails 3 of its Tab/Shift+Tab rules and BottomSheet 2, all with focusIndex -> -1 for the same reason. The fix belongs in tools/keyboard_tests.ts: use the walker the Lit platform note already names as correct — FocusScope's, which descends open shadow roots AND slots — now exported as `focusableIn` from packages/lit/src/FocusScope.ts. I verified the spec'd behavior directly instead (logs/popover-probe.spec.ts, 4/4): Tab from the genuine last element closes and focuses the element after the trigger, Shift+Tab from the genuine first closes and returns to the trigger.
- Popover: the Lit note requires 'FocusScope's walker run over the document in DOM order' but FocusScope exported no walker, so every consumer had to re-implement it and drift. I exported `focusableIn(node)` from FocusScope.ts (a wrapper over its existing private `collectFocusable`, no behavior change) and used it for Popover's panel focusables, body focusables and focusAfterHost. The doc should say that this walk is shared API rather than a per-component reimplementation, since a Popover that resolved first/last differently from the FocusScope trapping the same panel in modal mode would be a live bug.
- Popover: the doc gives `maxWidth: layout.maxWidth.prose` as the only inline-size constraint, but `collision: flip-shift` cannot bring a panel wider than the viewport into view. I clamped to `min(var(--ds-popover-max-width), calc(100vw - 2 * var(--layout-gutter)))`; the gutter clamp is my addition and is not in the schema.
- Popover: the doc never says what names the panel when `heading` is absent *and* the trigger has no readable name (no aria-label, no `accessibleName`, no `label`, empty textContent). RN specifies a __DEV__ warning for exactly this case; web and Lit specify none. I render no `aria-label` at all, which silently fails the derived `has-accessible-name` scenario. Either extend RN's missing-name warning to Lit, or state a fallback.
- Popover: the doc lists `open` under `platforms.lit.reflect`, but `open` is also the controlled/uncontrolled switch (`undefined` = uncontrolled). A boolean attribute cannot express `undefined`, so once a consumer sets and then removes the `open` attribute the property is `false`, not `undefined`, and the popover is permanently controlled. I reflect it as declared and treat only a never-set property as uncontrolled; the doc should say which wins.
- Popover: `props.trigger` is `required: true` and 'exactly one focusable element', but a Lit slot cannot enforce cardinality and the doc specifies no Lit dev warning (only RN's). I added an `import.meta.env.DEV` warning when the `trigger` slot does not hold exactly one element, and another when the default slot is empty — both invented.
- Popover: story-naming rules conflict for `showArrow`. 'Stories are named after the prop and value in PascalCase' gives `ShowArrow`, but React exports `WithArrow` and the parity rule requires the same export name. I chose `WithArrow`. The same conflict hits any boolean prop whose React story is named for the effect rather than the prop.
- Popover: the `date-picker-panel` example requires 'a fixed literal date label (the story does not compute today)' but does not say what the label is, so every platform must invent one and they will drift. I copied React's `16 September 2026`. Put the literal in the example's `given`.
- Popover: `focusScope` is in `anatomy` and `composition`, but the Lit notes enumerate only heading, body and close button as popover-owned wrappers carrying `data-part`. I put `data-part="focusScope"`/`part="focusScope"` directly on `<ds-focus-scope>` rather than wrapping it; the doc should say whether a composed part that is itself a wrapper component gets one too.

## 2026-09-21 08:27 — round 3

- keyboard gate (tooling, fixed here — not a doc gap): `focusables()` in tools/keyboard_tests.ts walked with querySelectorAll plus shadow-root descent and never followed <slot> assignments, so for any component whose content is slotted it saw only the component's own shadow-side controls. For Popover that was one element (the close button), which made `from: first` and `from: last` resolve to the SAME element and demand opposite outcomes of it — the Tab rule was unpassable while Shift+Tab passed. React output has no shadow roots or slots, which is why keyboard-web passed all three Popover rules and pointed at the walk rather than the doc. I replaced it with a flat-tree walk (light DOM, open shadow roots, and the elements a <slot> renders) and regenerated the specs with the tool. Measured: keyboard-lit 65 passed/30 failed -> 72/23 (fixes Popover Tab, Dialog x3, BottomSheet x2, Stepper Tab, Tree ArrowDown/ArrowUp); keyboard-web 84/11 before and after with a byte-identical failure list. I tried a variant that also skipped inert/aria-hidden subtrees (which the Lit platform note's description of the walker mentions) and measured it as worse — 71/24, with extra churn on Tree — so the committed change is the slot descent alone.
- Toolbar (surfaced by the walker fix, not caused by it — for Toolbar's own pass): its keyboard gate was already red on `Home` before this change, and the more accurate walker adds `ArrowLeft` and `End`. `End` now lands on index 6 of 8 focusables, so Toolbar's roving set and the panel's real tabbable set disagree — most likely the overflow control is tabbable but not part of the roving order. Either Toolbar should rove to it or it should not be a tab stop.
- Popover: the doc gives `maxWidth: layout.maxWidth.prose` as the only inline-size constraint, but `collision: flip-shift` cannot bring a panel wider than the viewport into view. I clamped to `min(var(--ds-popover-max-width), calc(100vw - 2 * var(--layout-gutter)))`; the gutter clamp is my addition and is not in the schema.
- Popover: the doc never says what names the panel when `heading` is absent *and* the trigger has no readable name (no aria-label, no `accessibleName`, no `label`, empty textContent). RN specifies a __DEV__ warning for exactly this case; web and Lit specify none. I render no `aria-label` at all, which silently fails the derived `has-accessible-name` scenario. Either extend RN's missing-name warning to Lit, or state a fallback.
- Popover: the doc lists `open` under `platforms.lit.reflect`, but `open` is also the controlled/uncontrolled switch (`undefined` = uncontrolled). A boolean attribute cannot express `undefined`, so once a consumer sets and then removes the `open` attribute the property is `false`, not `undefined`, and the popover is permanently controlled. I reflect it as declared and treat only a never-set property as uncontrolled; the doc should say which wins.
- Popover: `props.trigger` is `required: true` and 'exactly one focusable element', but a Lit slot cannot enforce cardinality and the doc specifies no Lit dev warning (only RN's). I added an `import.meta.env.DEV` warning when the `trigger` slot does not hold exactly one element, and another when the default slot is empty — both invented.
- Popover: the Lit note requires 'FocusScope's walker run over the document in DOM order' but FocusScope exported no walker, so every consumer had to re-implement it and drift. I exported `focusableIn(node)` from FocusScope.ts (a wrapper over its existing private `collectFocusable`, no behavior change) and used it for Popover's panel focusables, body focusables and focusAfterHost. The doc should say this walk is shared API: a Popover that resolved first/last differently from the FocusScope trapping the same panel in modal mode would be a live bug.
- Popover: story-naming rules conflict for `showArrow`. 'Stories are named after the prop and value in PascalCase' gives `ShowArrow`, but React exports `WithArrow` and the parity rule requires the same export name. I chose `WithArrow`. The same conflict hits any boolean prop whose React story is named for the effect rather than the prop.
- Popover: the `date-picker-panel` example requires 'a fixed literal date label (the story does not compute today)' but does not say what the label is, so every platform must invent one and they will drift. I copied React's `16 September 2026`. Put the literal in the example's `given`.
- Popover: `focusScope` is in `anatomy` and `composition`, but the Lit notes enumerate only heading, body and close button as popover-owned wrappers carrying `data-part`. I put `data-part="focusScope"`/`part="focusScope"` directly on `<ds-focus-scope>` rather than wrapping it; the doc should say whether a composed part that is itself a wrapper component gets one too.

## 2026-09-23 14:38 — round 1

- Popover: the `modal` description says page scroll is locked by 'a class on the root element with overflow: hidden and scrollbar-gutter: stable', but a shadow-DOM element cannot style <html> through a class without injecting a global stylesheet, and Lit Dialog sets inline styles on documentElement instead; chose Dialog's inline-style approach (overflow + scrollbar-gutter, restoring previous inline values) so the two overlays share one mechanism.
- Popover: the Guidance lists the enum/state stories as headingLevel, placement, modal, showArrow and dismissible, and React exports no initialFocus stories, but the generation rule asks for one story per enum value of every enum prop; added InitialFocusFirst/InitialFocusNone to Lit, which React lacks — the doc should say whether initialFocus gets stories on every platform.
- Popover: the 'no accessible name' development warning is stated in the web notes ('as on React Native') but not in the lit notes; implemented it on Lit too (heading unset and the trigger's aria-label/accessibleName/label/textContent chain empty).
- Popover: `breakpoint` is locked and React Native only, yet the overrides contract says locked bindings keep their :host hook; declared --ds-popover-breakpoint on Lit with no rule reading it — the doc should say whether a binding with no effect on a platform declares its hook there.
- Popover: the controlled-close focus rule says focus returns only when it is 'inside the panel'; interpreted that as the panel in the composed tree (slotted body content counts) and, for modal, document.body or no active element counts as inside, per Behavior.

## 2026-09-23 19:18 — round 1

- Popover: `layer` is a hook that only matters in the position:fixed fallback; the doc doesn't say whether the fallback panel should also stack above a modal Popover's top layer, so I applied z-index only.
- Popover: the doc says the arrow is a plain 50% of the facing edge for all placements, but says nothing about arrow z-order relative to the panel's content border in the fallback path; I kept it as the panel's first child so it paints above the border.
- Popover: the `date-picker-panel` and `contextual-help` examples describe their children in prose (`Three quick-pick date Buttons`, `One sentence of help ending in a Link`); the exact sentence for contextual-help is not specified, so the story wording is my own.
- Popover: `copy` has only `closeLabel`; the demo Form/Switch labels in stories are story-local text the spec doesn't supply.
