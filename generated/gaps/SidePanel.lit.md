# Gaps reported while generating SidePanel for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 03:40 — round 1

- SidePanel: doc prop `title` renamed to `heading` for both the JS property and the attribute (not just mapped) to avoid colliding with `HTMLElement.title`, matching the existing precedent in Dialog.ts and BottomSheet.ts rather than keeping the doc's literal attribute name.
- SidePanel: `aria-controls` from the trigger to the panel is never set — the panel lives inside the shadow root and an id there can't be referenced by an IDREF from the light-DOM trigger, same limitation already accepted in Popover.ts. The panel is instead only named via `aria-labelledby` inside the shadow root.
- SidePanel: `swipeable` is exposed as a reflected boolean property/attribute for API parity but no swipe gesture is wired up on the web platform — the doc parenthesizes the swipe-to-open half as '(native only)' and the Lit platform notes don't describe a gesture handler (unlike BottomSheet's explicit pointer-drag implementation), so I left it inert on Lit and noted this rather than guessing at a touch implementation.
- SidePanel: copy.openLabel ('Open menu') has no natural home in the component itself since `trigger` is fully consumer-supplied slotted content (the component never renders its own trigger text) — I used it verbatim as the trigger button's label in the stories to satisfy 'use copy.* verbatim', but a real consumer would follow the guidance text ('Menu', 'Filters') instead.
- SidePanel: the example `<ds-icon name="menu">` from the platform notes can't be used — 'menu' is not in Icon's `IconName` union (packages/lit/src/Icon.ts). Stories render the trigger as a label-only button instead of guessing at a substitute glyph.
- SidePanel: 'A Link followed inside the panel closes it with reason navigation' is implemented as a click listener on the body wrapper checking for an `<a>` or `<ds-link>` in the composed path; the doc doesn't specify the exact detection heuristic (e.g. whether it should also cover a `<ds-button>` acting as a link), so this is a best-effort interpretation.
- SidePanel: safe-area padding is only applied via `env(safe-area-inset-bottom)` on the footer row (relevant since the panel is full-height); the web/React notes also mention `env(safe-area-inset-left)`/`-right`, but those are physical properties that don't map cleanly onto the `side`-based logical-property layout used here, and the Lit platform notes don't call safe-area out explicitly, so inline safe-area was left out.
- SidePanel: the non-modal scrim `<div>` shows/hides via the `hidden` attribute rather than crossfading; the modal path gets a real transition through the native `<dialog>::backdrop`, but doc doesn't specify whether the non-modal scrim should also fade, so I kept it simple.
- SidePanel: in persistent mode the landmark role is always `complementary` — the schema has no prop letting a Lit consumer choose `navigation` (the React version gets this via a `Landmark as="nav"` composition the schema doesn't expose here), so a navigation-heavy panel can't get the more specific role on this platform without an added prop.

## 2026-09-10 18:19 — round 1

- SidePanel: doc prop `role` (values complementary/navigation) renamed to `landmark` for the Lit JS property/attribute to avoid colliding with the native `Element.role` ARIA-reflection accessor, per the 'roles on the host' naming convention; it is not reflected (also absent from platforms.lit.reflect) and only drives the `role` attribute set on the shadow `<aside>` (persistent) or non-modal `<dialog>` (overlay-open) region.
- SidePanel: `hideTitle`/`hide-title` from a prior generation round (based on an earlier doc draft using `title`) renamed to `hideHeading`/`hide-heading` to match the current schema's prop name.
- SidePanel: `modal`'s stray `reflect: true` (left over from the earlier round) removed since `modal` is not listed under `platforms.lit.reflect` and no CSS reads a `:host([modal])` selector — its overlay/dialog-vs-aside branching is already handled entirely in render() logic.
- SidePanel: prior-round gaps around `aria-controls` never reaching the shadow panel, `swipeable` being inert on web (native-only gesture), and the persistent-mode `<aside>`'s role previously being hardcoded still apply as documented in generated/gaps/SidePanel.lit.md rounds 1-2, except the last is now resolved by the new `landmark` prop.

## 2026-09-16 07:39 — round 1

- SidePanel: platforms.lit.notes says 'Shadow <dialog> for overlay mode' for both overlay modes, but the role prop says a modal panel is a dialog and a non-modal one is a landmark region, and ARIA in HTML allows no landmark role on <dialog>. I render non-modal as <aside>/<nav> with `hidden` and use <dialog> + showModal() only when modal. The Lit note should separate the two modes.
- SidePanel: the Lit note says persistent mode renders an <aside>, but landmark 'navigation' can't sit on <aside> (ARIA in HTML allows no navigation role there). I render <nav> for navigation and <aside> for complementary. The docs don't say whether Lit should compose ds-landmark as web does (composition doesn't list Landmark); I don't.
- SidePanel: dismiss by outside click has no reason value (the enum lists scrim but no 'outside'). A pointerdown outside a non-modal panel with no scrim (scrim: false) reports reason 'scrim'.
- SidePanel: reason 'action' ('a footer action asked to close') has no mechanism: no method, event or attribute lets a slotted footer control ask to close, and a light-DOM form method=dialog can't reach the shadow <dialog>. Only a native close() of the modal dialog maps to 'action'; the doc should name the mechanism (e.g. a close() method or a data attribute).
- SidePanel: copy.expanded ('Expanded') has no use on Lit: aria-expanded carries the state (Button's `expanded` property on a ds-button trigger, the attribute on any other element). It is only meaningful for SwiftUI's accessibilityValue; mark it platform-scoped.
- SidePanel: the doc has no defaultOpen, so open-at-start is only possible by controlling `open`; the stories and examples (cart, detail-panel) are therefore controlled and re-set `open` in an open-change listener. An uncontrolled panel always starts closed.
- SidePanel: scrim, modal and hideHeading are not in platforms.lit.reflect; I use attributes no-scrim (negated, since scrim defaults true), modal and hide-heading without reflecting them. The `role` prop is the `landmark` property/attribute (unreflected), per the doc.
- SidePanel: the keyboard block says Escape works 'from inside' while the web note says 'anywhere inside'; it is unclear whether Escape with focus on the slotted trigger (non-modal, focus stays there on open) should close. I only handle Escape from inside the surface, so Escape on the trigger does nothing.
- SidePanel: the parts section says composed parts receive 'exactly the listed props', but none are listed for heading/body/footer/closeButton. I chose ds-heading level 2 size lg, ds-box inset lg (the `inset` binding), ds-stack horizontal gap tight justify end, and ds-button ghost icon-only with a close ds-icon.
- SidePanel: the `partGap` and `inset` bindings have no part; I apply partGap as CSS gap on the focusScope column and inset as header/footer padding plus the body ds-box inset (override forwarded as paddingBlock/paddingInline).
- SidePanel: modal initial focus is 'the first control (or the title)' without saying whether footer controls count or whether the close button comes before body content. I pick the first focusable in the body slot, then the footer, then the close button, then the heading.
- SidePanel: the exit binding describes motion.easing.exit and enter uses motion.easing.standard, but neither easing is a style binding or overridable; hard-wired to those tokens. The scrim fade reuses enter/exit durations.

## 2026-09-17 11:15 — round 1

- SidePanel: the swipe dismiss is not wired on Lit (the doc says so), yet `swipe` stays in the reason union and in overlay.dismiss; kept the value in the type with nothing that emits it.
- SidePanel: the `open-change` reasons say the scrim fires on click, the web notes say pointerdown; Lit kept `click` on the scrim (the behavior scenario says `click: scrim`) and `pointerdown` for `outside`.
- SidePanel: with `scrim: true` the fixed scrim covers the trigger, so pressing the trigger while open lands on the scrim and reports `scrim`, not `trigger`; the doc never says whether the trigger should sit above the scrim.
- SidePanel: the `navigation` reason on Lit has no router hook; chose any click in the body whose composed path holds an `<a href>` or `ds-link` (not default-prevented), without moving focus to the trigger.
- SidePanel: `action` on Lit: the doc names a slotted `form method="dialog"` but not a submitter's `formmethod="dialog"`; both are treated as `action`, and it is ignored while persistent.
- SidePanel: the doc says nothing about what happens when something other than the component closes the modal `<dialog>`; removed the old `action` report there (SidePanel never raises `action` on its own) and rely on `cancel` being prevented.
- SidePanel: the persistent sidebar's border is 'the inner edge'; the web notes say `border-inline-end` only, which is wrong for `side: end`. Lit draws it on the edge facing the content (inline-end for start, inline-start for end).
- SidePanel: the body forward lists only `inset → paddingBlock`, while the `inset` description says the same padding also goes on the header and footer; the header and footer read the hook directly, and the Box's own inline padding keeps `inset="lg"` with no override path.
- SidePanel: examples without `open` in their given (navigation-drawer, filters) would pick up the stories' `open: true` meta default; set `open: false` on them, which goes beyond their exact given. The doc should say whether the example args are merged over Default.
- SidePanel: `layer` for the non-modal scrim has no binding of its own; the scrim uses `calc(var(--ds-side-panel-layer) - 1)` so it sits under the panel.
- SidePanel: the breakpoint is read from the resolved `--layout-max-width-content/page` value on `<html>` when the element connects; a theme change after that doesn't re-read it (the doc says 'read from the theme token' but not when).

## 2026-09-21 10:44 — round 1

- SidePanel: the doc never says which state the Default story shows. Every other overlay (Dialog, BottomSheet) opens by default on both platforms, but React's SidePanel meta omits `open` so its Default renders closed behind the trigger. I followed React for story parity — Lit's meta no longer sets `open` — which changed the rendered output of Default and of all eleven enum-value stories. State it in the doc so the platforms cannot drift again.
- SidePanel: the derived `has-accessible-name` scenario has no `given`, so on Lit it lands on a closed panel whose region carries `hidden` itself; a real browser then computes an empty name (React survives it only because its `hidden` sits on the wrapper, not on the <aside>, and jsdom does not cascade display:none). The test asserts the closed region is out of the accessibility tree and then checks the name once shown. The doc should say the name requirement is about the shown panel.
- SidePanel: the Shift+Tab rule (`from: first`, `expect: focus-trigger`) cannot pass on Lit. The trigger is slotted inside the host, so the keyboard gate's first focusable within `[data-ds="SidePanel"]` is the trigger itself, and Shift+Tab from it correctly leaves the component; on web `data-ds` sits on the portaled panel, so `first` is the panel's first control. Pre-existing (both the old and the new focusable walk index the trigger at 0) and not fixable without breaking document order, which the platform note says is exactly what should carry Tab on Lit. Either the rule needs a Lit-specific `from`, or the gate needs to skip the trigger when it is inside the root.
- SidePanel: the content guidance says the current navigation Link is marked with `aria-current="page"` on web and Lit, but `ds-link` has no `current` prop and does not forward `aria-current` from the host to its inner <a>, so the Lit navigation-drawer story cannot mark it. The React story sets it through rest props. Link's schema needs the prop before Lit can honour this.
- SidePanel: the generated behavior test writes the doc's `role` prop straight onto the element (`el.role = 'complementary'`), which on a custom element is the native `Element.role` ARIA reflection rather than the Lit `landmark` property the platform note renames it to. The `renders-role-*` scenarios therefore pass without exercising the landmark choice (my hand-written test uses `landmark`). The per-platform rename needs to reach tools/behavior_tests.ts.

## 2026-09-21 10:53 — round 2

- SidePanel: `a11y.role: none` is the cause of the keyboard-lit Shift+Tab failure and is not true of the component — the non-modal panel exposes the landmark the `role` prop names and the modal one is a dialog. With `role: none`, tools/keyboard_tests.ts `resolveRole` falls back to `page.locator('[data-ds="SidePanel"]').first()`, which on Lit contains the slotted trigger, so the rule's `from: first` indexes the trigger (probe: index 0 = trigger, index 1 = the panel's close button) and Shift+Tab from it correctly leaves the component. Replace `a11y.role: none` with `a11y.roleFrom: role` — the field landmark.md already uses, and the prop's default `complementary` resolves it — so the root becomes `getByRole('complementary')` on every platform. Verified: logs/side-panel-role-root.spec.ts is the generated spec with only that root changed and all four rules pass. Same fix un-skips the generated behavior test's `has-accessible-name`, which skips itself with "role 'none' cannot be queried; name the landmark/text role in the doc".
- SidePanel: the doc never says which state the Default story shows. Every other overlay (Dialog, BottomSheet) opens by default on both platforms, but React's SidePanel meta omits `open` so its Default renders closed behind the trigger. I follow React for story parity — Lit's meta does not set `open` — which decides the rendered output of Default and of all eleven enum-value stories. State it in the doc so the platforms cannot drift again.
- SidePanel: the derived `has-accessible-name` scenario has no `given`, so on Lit it lands on a closed panel whose region carries `hidden` itself; a real browser then computes an empty name (React survives it only because its `hidden` sits on the wrapper, not on the <aside>, and jsdom does not cascade display:none). The test asserts the closed region is out of the accessibility tree and then checks the name once shown. The doc should say the name requirement is about the shown panel.
- SidePanel: the content guidance says the current navigation Link is marked with `aria-current="page"` on web and Lit, but `ds-link` has no `current` prop and does not forward `aria-current` from the host to its inner <a>, so the Lit navigation-drawer story cannot mark it (React sets it through rest props). Link's schema needs the prop before Lit can honour this.
- SidePanel: the generated behavior test writes the doc's `role` prop straight onto the element (`el.role = 'complementary'`), which on a custom element is the native `Element.role` ARIA reflection rather than the Lit `landmark` property the platform note renames it to. The `renders-role-*` scenarios therefore pass without exercising the landmark choice (my hand-written test uses `landmark`). The per-platform rename needs to reach tools/behavior_tests.ts.

## 2026-09-21 11:02 — round 3

- SidePanel: `a11y.role: none` is wrong for a component that declares a `role` prop (complementary/navigation) and lists `landmark-role` in `a11y.requires`, and it is the sole cause of the keyboard-lit Shift+Tab failure. It makes `resolveRole` return none, so tools/keyboard_tests.ts:89 falls back to `page.locator('[data-ds="SidePanel"]').first()`. That root works on web only because the doc puts `data-ds` on the portaled panel with the trigger wrapper outside it; on Lit the trigger is a slot inside the host, so the rule's `from: first` indexes the trigger (probed: 0 = trigger, 1 = the panel's close button) and Shift+Tab off it correctly leaves the component. FIX: replace `a11y.role: none` with `a11y.roleFrom: role` — the field landmark.md already uses; the prop default `complementary` resolves it and both platforms root at `getByRole('complementary')`. Proven: logs/side-panel-role-root.spec.ts is the generated spec with only that root changed and all four executable rules pass; logs/side-panel-probe.spec.ts dumps the focusable order behind it. No code-side fix exists that does not contradict the schema: moving `data-ds` to the panel breaks the stated Lit convention that the host carries it, and putting the region before the trigger slot contradicts `platforms.lit.notes` ('the shadow region follows the trigger slot, so Tab order needs no handling').
- SidePanel: fixing the role above also un-skips the generated behavior gate's `has-accessible-name` (it currently skips itself with "role 'none' cannot be queried; name the landmark/text role in the doc"), and that check will then run against the Default story — which is closed. On Lit a closed panel carries `hidden` on the region itself, so a real browser computes an empty name (React survives it only because its `hidden` sits on the wrapper and jsdom does not cascade display:none). My hand-written test asserts the closed region is out of the accessibility tree and then checks the name once shown. The doc must say the name requirement is about the shown panel, and both fixes should land together or the behavior gate turns red as the keyboard gate turns green.
- SidePanel: the doc never says which state the Default story shows. Every other overlay (Dialog, BottomSheet) opens by default on both platforms, but React's SidePanel meta omits `open` so its Default renders closed behind the trigger. I follow React for story parity — Lit's meta does not set `open` — which decides the rendered output of Default and of all eleven enum-value stories. State it in the doc so the platforms cannot drift again.
- SidePanel: the content guidance says the current navigation Link is marked with `aria-current="page"` on web and Lit, but `ds-link` has no `current` prop and does not forward `aria-current` from the host to its inner <a>, so the Lit navigation-drawer story cannot mark it (React sets it through rest props). Link's schema needs the prop before Lit can honour this.
- SidePanel: the generated behavior test writes the doc's `role` prop straight onto the element (`el.role = 'complementary'`), which on a custom element is the native `Element.role` ARIA reflection rather than the Lit `landmark` property the platform note renames it to. The `renders-role-*` scenarios therefore pass without exercising the landmark choice (my hand-written test uses `landmark`). The per-platform rename needs to reach tools/behavior_tests.ts.

## 2026-09-22 01:42 — round 1

- SidePanel: `open` is in platforms.lit.reflect, but an uncontrolled panel's state cannot be reflected — writing the attribute would define the `open` property and silently convert the element to controlled. Only a controlled `open` reflects; uncontrolled open state is invisible to `:host([open])`. The doc should either drop `open` from the reflect list or name a separate state attribute (e.g. `data-open`) for the uncontrolled case.
- SidePanel: the four `constants` (dismissDistance, dismissVelocity, dragSlop, edgeZone) have no reachable code path on Lit — the doc says Lit wires no gesture — so the rule that logic must read each constant through its token expression cannot be honoured. `swipe` likewise stays in the reason union with nothing that emits it, and `swipeable` reflects `no-swipeable` with no rule or handler reading it. Mark the constants and the reason platform-scoped.
- SidePanel: `aria-controls` from the slotted trigger to the shadow panel is impossible (an IDREF in the light DOM cannot address an id in a shadow root), so only `aria-expanded` is set and the panel is named by its heading via `aria-labelledby` inside the shadow root. The doc's a11y prose still describes the APG disclosure pair as button + aria-controls; it should state the Lit exception where it states the modal-unmount one.
- SidePanel: the `navigation` reason needs a detection heuristic the doc does not give. I close on a click inside the parts column whose composed path holds an `<a href>` or a `ds-link` and that is not default-prevented. Unspecified: whether a `ds-button` acting as a router link, or a click on a `ds-link` without `href`, should count.
- SidePanel: the navigation-drawer example asks for the current page marked with `aria-current` where the platform allows it, and names Lit as one that cannot — `ds-link` has neither a `current` property nor a forward of `aria-current` to its inner `<a>`. The Lit story leaves it unmarked. Link's own schema needs the prop before this example can match React's.
- SidePanel: tools/behavior_tests.ts writes the doc's prop name straight onto the element, so the generated spec sets `el.role = 'navigation'` — which on a custom element is the native `Element.role` ARIA reflection, not the `landmark` property platforms.lit renames it to. `renders-role-complementary` and `renders-role-navigation` therefore pass without ever exercising the landmark choice (my hand-written test uses `landmark`). The per-platform prop rename needs to reach the behavior-test generator.
- SidePanel: the width cap `min(width, 100% - edgeGutter)` is written against the fixed containing block as the doc requires, but the doc does not say what the host itself should lay out as in overlay mode — the host holds only the trigger slot there, with the panel `position: fixed`. I left `:host` at the default inline display so the trigger flows like the button it is; a doc that wants `display: contents` (as the web trigger wrapper uses) should say so, since it changes how a consumer's flex/grid parent treats `<ds-side-panel>`.
- SidePanel: the doc specifies the breakpoint probe but not the failure path. When the token resolves to an empty string or measures 0px I render the overlay presentation (the same fallback as a missing `matchMedia`); the doc should name that fallback so the platforms cannot pick different ones.
- SidePanel: `enter`/`exit` describe a slide from the edge without naming the animated property. Lit animates `inset-inline-start`/`inset-inline-end` (which keeps the slide on the writing-direction edge under RTL) rather than the `translate` from `-100%` the web notes give, so the two platforms composite differently. Worth stating one of them in the shared binding description.

## 2026-09-23 14:48 — round 1

- SidePanel: composition says `body` is a Box carrying the `body` part, but the Lit Box writes `data-part="surface"` on its own host (Box.ts connectedCallback). That overwrites a parent-assigned `data-part="body"` and collides with the panel's own `surface` part. The web notes solve this only for Button (the closeButton wrapper span). I put `data-part="body"` on the overlay-owned scroll wrapper the web notes already require, with the Box inside it. The doc should say which element carries `body` on Lit.
- SidePanel: `inset` says the forward travels through `--ds-box-padding-inline` set to `--ds-side-panel-inset` "in SidePanel's stylesheet", but Box declares that hook's default on its own `:host`, so the value can't reach it by inheritance from a wrapper. It has to be set on the ds-box element itself (`[data-part='body'] > ds-box`). The doc should name the element the rule targets.
- SidePanel: `headingGap` says the forward travels only through `--ds-heading-margin-block-end` and 'no conditional prop is needed', while the Declared contracts say each forward reaches the child's `overrides`. I kept both: the hook rule always applies, and `overrides.marginBlockEnd` is passed only when the caller overrode headingGap, matching how `inset` is described.
- SidePanel: the conventions say a binding forwarded to a child's `overrides` declares no hook of its own, but the doc names `--ds-side-panel-inset` and `--ds-side-panel-heading-gap` as the forwarding route. I followed the doc and declared both hooks.
- SidePanel: `navigation` must not move focus, but in modal mode the native `<dialog>.close()` restores focus to the element focused before `showModal()` by itself. The component can't stop that without re-focusing afterwards, so a modal panel closed by a followed Link may still land focus on the trigger. The doc doesn't say whether that is acceptable.
- SidePanel: `trigger` should warn in development for a fragment or bare string, as on web. On Lit, bare text can't be assigned to a named slot, and there are no fragments. The only reachable mistake is two elements with `slot="trigger"`, so that is the one case warned about.
- SidePanel: the web notes' `closeButton` wrapper `<span>` exists because React's Button writes its own `data-part`. The Lit Button doesn't, and the Lit notes don't ask for a wrapper, so `data-part="closeButton"` sits on the `ds-button` directly. The doc should say whether the wrapper is web-only.
- SidePanel: `open` has no default and is reflected, so a controlled `false` can't be spelled as an attribute (boolean attributes only express true). Controlled-closed is property-only on Lit; the doc doesn't mention this.

## 2026-09-23 14:49 — round 2

- SidePanel: `focusRing` / `focusRingWidth` are locked bindings with no `part` or state, so the doc doesn't say which element's focus ring they style. The panel draws only one ring itself: the heading, which becomes focusable (tabindex -1) as the last initial-focus fallback in modal mode. The close button and slotted controls draw their own rings. I applied the hooks to `[data-part='heading']:focus-visible`. The doc should name the part.
- SidePanel: `gates:hooks` exits 1 for 45 other components on web and Lit (271 locked bindings without hooks) that are outside this job. A clean SidePanel can't make the gate's exit code pass, so it should be read per component.
- SidePanel: the conventions say a binding forwarded to a child's `overrides` declares no hook of its own, but the doc names `--ds-side-panel-inset` and `--ds-side-panel-heading-gap` as the forwarding route. I followed the doc and declared both hooks.
- SidePanel: composition says `body` is a Box carrying the `body` part, but the Lit Box writes `data-part="surface"` on its own host, which overwrites a parent-assigned `data-part="body"`. I put `data-part="body"` on the overlay-owned scroll wrapper, with the Box inside it. The doc should say which element carries `body` on Lit.
- SidePanel: Box declares `--ds-box-padding-inline` on its own `:host`, so the `inset` forward can't reach it by inheritance. The rule targets the ds-box element directly (`[data-part='body'] > ds-box`). The doc should name the element the rule targets.
- SidePanel: `headingGap` says 'no conditional prop is needed', while the Declared contracts say each forward reaches the child's `overrides`. I kept both: the hook rule always applies, and `overrides.marginBlockEnd` is passed only when headingGap is overridden.
- SidePanel: `navigation` must not move focus, but a modal `<dialog>.close()` natively restores focus to the element focused before `showModal()`. A modal panel closed by a followed Link may therefore still land focus on the trigger. The doc doesn't say whether that is acceptable.
- SidePanel: the development warning for a non-single `trigger` can only fire for two elements with `slot="trigger"` on Lit, because a text node can't be assigned to a named slot.
- SidePanel: the web `closeButton` wrapper `<span>` exists because React's Button writes its own `data-part`. The Lit Button doesn't, so `data-part="closeButton"` sits on the `ds-button` directly. The doc should say whether the wrapper is web-only.
- SidePanel: controlled `open` has no default and a boolean attribute can't express false, so controlled-closed is property-only on Lit. The doc doesn't mention this.

## 2026-09-23 19:21 — round 1

- SidePanel: `swipeable` is documented as accepted for parity with no gesture on Lit, but the Lit reflect list gives it a `no-swipeable` attribute while the platform note lists only `modal`, `hide-heading`, `no-scrim` and `landmark` as non-reflected attributes. I kept the reflected negated attribute per the reflect list.
- SidePanel: `aria-controls` cannot reach the shadow panel, so only `aria-expanded` is set on the trigger. The prop text says aria-controls is set whenever the target is in the DOM, and does not say this is web-only.
- SidePanel: the keyboard Tab and Shift+Tab rules are `expect: manual` with `from: trigger`/`first`. On Lit they rely on document order, and the spec does not say how a keyboard gate should treat them.
- SidePanel: the `outside` press is a capture-phase pointerdown on `document`, and anything inside the host, panel or slotted trigger is treated as inside. The spec says 'outside the panel and trigger' without saying whether slotted children of the host count as inside; I counted them as inside.
- SidePanel: the `persistent` sidebar with no `matchMedia`, or a token that measures 0px, falls back to overlay. The spec only names jsdom for this; I applied it to both cases.
