Project Storybook's examples into the docs site per website-plan.md ("Content pipeline", "Component page template" step 4).

1. `tools/docs_examples.ts`: for each `packages/react/src/<Name>.stories.tsx`, statically analyze (do not execute — these import React/CSS) the named exports of the default-exported `Meta`'s siblings, capturing each export's name and its `args` object, plus a `sourceText` (the export's source slice, for display).
2. Cross-reference each export against `storybook-static/index.json` (the canonical story manifest `storybook build` produces — see job 510) to attach the real `storyId` Storybook itself assigned, rather than re-deriving Storybook's ID algorithm by hand. If `storybook-static/index.json` doesn't exist yet in the working tree, run `pnpm --filter react build-storybook` first (fast, static, no Chromatic token needed for this local build) — job 510's Chromatic publish is a separate, later step that reuses the same build. Write `generated/examples/<Name>.json`: `[{ "title": "Default", "args": {...}, "sourceText": "...", "storyId": "button--default" }, ...]`.
3. Fill the placeholder from job 506 in `[slug].astro`: a `Tabs` component, one tab per entry in `generated/examples/<Name>.json`, each panel rendering the live component as a React island (`client:visible`) with those `args` — this rendering is entirely self-contained and never fetches from Storybook or Chromatic, so the docs site's examples work even if the hosted Storybook is unreachable — plus its `sourceText` through job 508's code renderer, plus a `Link(external)` "Open in Storybook ↗" using `storyId` and the Chromatic project URL from job 510 (`{chromaticUrl}/?path=/story/{storyId}`). If job 510 hasn't landed yet, omit the link rather than pointing at a URL that doesn't exist.
4. Add `pnpm docs:examples` to `package.json` and call it from `pnpm check` after `pnpm parse` (it depends on `packages/react/src` being current).
Gate: every `*.stories.tsx` in `packages/react/src` produces a `generated/examples/<Name>.json` with at least one entry ("Default" at minimum) carrying a `storyId` that matches an entry in `storybook-static/index.json`; the corresponding component's docs page renders that many tabs, each showing a working live example that renders correctly with Storybook's dev server stopped. Do not modify `packages/react/src` (only reads it) or hand-author example content — every example must trace back to an existing story.
