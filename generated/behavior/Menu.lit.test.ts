// Generated by tools/behavior_tests.ts from the `behavior` block of the Menu doc. Do not edit.
import { beforeEach, describe, expect, test, vi } from 'vitest';
import { userEvent } from 'vitest/browser';
import '../../packages/lit/src/Menu.js';
import meta from '../../packages/lit/src/Menu.stories.js';

function deep(root: ParentNode, selector: string): Element | null {
  const direct = root.querySelector(selector);
  if (direct) return direct;
  for (const el of Array.from(root.querySelectorAll('*'))) {
    const shadow = (el as HTMLElement).shadowRoot;
    if (shadow) {
      const found = deep(shadow, selector);
      if (found) return found;
    }
  }
  return null;
}

/** Every element that is "active" from the document down through shadow roots: the host, then the inner one.
 *  document.activeElement alone is a shadow host while focus sits inside its shadow tree. */
function activeChain(): Element[] {
  const chain: Element[] = [];
  let el: Element | null = document.activeElement;
  while (el) {
    chain.push(el);
    el = el.shadowRoot?.activeElement ?? null;
  }
  return chain;
}

async function setup(given: Record<string, unknown> = {}) {
  const el = document.createElement('ds-menu');
  const props = { ...meta.args, ...given };
  for (const [key, value] of Object.entries(props)) {
    if (value !== undefined) (el as unknown as Record<string, unknown>)[key] = value;
  }
  const events = {
    onAction: vi.fn(),
    onOpenChange: vi.fn(),
  };
  el.addEventListener('action', events.onAction as unknown as EventListener);
  el.addEventListener('open-change', events.onOpenChange as unknown as EventListener);
  document.body.append(el);
  await (el as unknown as { updateComplete: Promise<boolean> }).updateComplete;
  const root: ParentNode = el.shadowRoot ?? el;  // some elements render in light DOM
  const s = {
    el,
    root,
    events,
    props,
    root_: () => el,
    trigger: () => (deep(root, '[role="menu"]') ?? deep(root, '[part="trigger"]') ?? deep(root, '[data-part="trigger"]') ?? root.firstElementChild) as HTMLElement,
  };
  return s;
}

beforeEach(() => {
  document.body.replaceChildren();
});

describe('ds-menu', () => {
  test('renders', async () => {
    const s = await setup({"open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-trigger-variant-ghost', async () => {
    const s = await setup({"triggerVariant": "ghost", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-trigger-variant-secondary', async () => {
    const s = await setup({"triggerVariant": "secondary", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-trigger-variant-primary', async () => {
    const s = await setup({"triggerVariant": "primary", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-trigger-icon-ellipsis', async () => {
    const s = await setup({"triggerIcon": "ellipsis", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-trigger-icon-chevron-down', async () => {
    const s = await setup({"triggerIcon": "chevron-down", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-trigger-icon-none', async () => {
    const s = await setup({"triggerIcon": "none", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-placement-bottom-start', async () => {
    const s = await setup({"placement": "bottom-start", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-placement-bottom-end', async () => {
    const s = await setup({"placement": "bottom-end", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-placement-top-start', async () => {
    const s = await setup({"placement": "top-start", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-placement-top-end', async () => {
    const s = await setup({"placement": "top-end", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('has-accessible-name', async () => {
    const s = await setup({"open": true});
    expect(s.trigger()).toHaveAccessibleName(s.props.label);
  });
});
