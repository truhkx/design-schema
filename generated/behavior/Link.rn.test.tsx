// Generated by tools/behavior_tests.ts from the `behavior` block of the Link doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Link } from '../../packages/rn/src/Link';
import type { LinkProps } from '../../packages/rn/src/Link';
import meta from '../../packages/rn/src/Link.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<LinkProps> = {}) {
  const events = {
    onPress: jest.fn(),
  };
  const props: LinkProps = { ...(meta.args as LinkProps), ...given, onPress: events.onPress };
  const tree = (p: LinkProps) => (
    <ThemeProvider mode="light">
      <Link {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Link') ?? screen.UNSAFE_root,
    anchor: () => screen.queryByRole('link') ?? s.root(),
    rerender: (next: Partial<LinkProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Link', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-default', () => {
    const s = setup({"tone": "default"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-inherit', () => {
    const s = setup({"tone": "inherit"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('link', { name: s.props.label })).toBeOnTheScreen();
  });
});
