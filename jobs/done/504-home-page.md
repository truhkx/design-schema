Build the home page per website-plan.md ("Home page").

1. `apps/website/src/assets/logo.svg`: a bespoke wordmark/mark for "Design Schema" using `currentColor` (or the theme's `color.foreground` custom property) so it re-themes automatically; this is the one hand-authored visual asset on the site — every other pixel comes from a generated component.
2. `apps/website/src/pages/index.astro`: hero — `Container(width="content")` → `Stack(direction="column", align="center", gap="loose")` with the larger logo, `Heading(level=1)` = "Design Schema: An Agentic Design System", `Text(size="lg", tone="muted")` with a 1–2 sentence statement drawn from `vision-and-decisions.md`'s Vision paragraph, and a `Stack(direction="row", gap="normal")` of two `Button`s (primary "Read the docs" → `/docs`, secondary `variant="outline"` "View on GitHub" → the repo, external).
3. Feature section below: `Container(width="page")` → a wrapped `Stack` of `Card`s, one per pillar (schema-driven, every platform, zero-token theming, accessible by construction), each with a `Heading(level=3)` + `Text`.
4. A decorative, `aria-hidden="true"` SVG background (sand/geometric-line motif per the landing-page pattern note) behind the hero `Container` — inline SVG, not an `Icon`, since this is page art, not a UI glyph.
Gate: Lighthouse accessibility ≥ 95; the hero heading and statement are present in the server-rendered HTML (view-source, no client JS required to read them). Do not modify `packages/*/src` — if a wrapped `Stack` can't achieve the feature-card layout, log the gap in `generated/gaps/` rather than adding ad hoc CSS to a generated component.
