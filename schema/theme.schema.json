{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://design-schema.dev/schema/theme.schema.json",
  "title": "Theme frontmatter",
  "description": "The decisions a theme is derived from. Lives under the `theme:` key of a theme doc's frontmatter. tools/theme.ts turns this into full DTCG token files for every mode. Derived from schema/theme.ts by tools/schema.ts — do not edit by hand.",
  "type": "object",
  "properties": {
    "theme": {
      "$ref": "#/$defs/themeDef"
    }
  },
  "required": [
    "theme"
  ],
  "$defs": {
    "themeDef": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Folder and CSS attribute value: tokens/themes/<id>, [data-theme=<id>]."
        },
        "status": {
          "default": "draft",
          "type": "string",
          "enum": [
            "draft",
            "review",
            "stable",
            "deprecated"
          ]
        },
        "tone": {
          "minItems": 2,
          "maxItems": 5,
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Adjectives that describe the feel. Used by AI tools as the tie-breaker for borderline decisions."
        },
        "not": {
          "type": "string",
          "description": "The one word this theme must never be."
        },
        "seed": {
          "type": "object",
          "properties": {
            "color": {
              "type": "string",
              "pattern": "^#[0-9a-fA-F]{6}$",
              "description": "Brand color. The brand ramp is built around it; neutrals are tinted toward its hue unless `neutral` is set. A near-achromatic color (OKLCH chroma below 0.03) is treated as ink: the primary action is the darkest neutral on a light page and the lightest on a dark one, links take the text color, and the focus ring is the brand step that reads at 3:1 on both surfaces."
            },
            "neutral": {
              "description": "Optional second seed for two-seed palettes (sand-and-black, navy-and-cream): the neutral ramp takes this color's hue and chroma (capped at 0.06) instead of a faint cast of the brand hue, the light-mode page is this color lifted (lightness min(L + 0.15, 0.96)) rather than white, and the darkest neutral keeps the hue at low chroma so dark mode is a warm black. Every contrast-chosen step is still chosen against the new surfaces.",
              "type": "string",
              "pattern": "^#[0-9a-fA-F]{6}$"
            },
            "typeface": {
              "default": "system",
              "description": "'system' or a family name. Named families are prepended to the system stack.",
              "type": "string"
            },
            "headingTypeface": {
              "description": "Defaults to typeface.",
              "type": "string"
            },
            "mono": {
              "default": "system",
              "type": "string"
            }
          },
          "required": [
            "color"
          ],
          "additionalProperties": false
        },
        "neutralTint": {
          "default": 0.2,
          "description": "How much of the seed hue bleeds into the neutral ramp. 0 = pure gray.",
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "scale": {
          "type": "object",
          "properties": {
            "base": {
              "type": "number",
              "minimum": 12,
              "maximum": 20,
              "description": "Body size in px."
            },
            "ratio": {
              "type": "number",
              "minimum": 1.1,
              "maximum": 1.5,
              "description": "Modular scale ratio. 1.2 dense/technical, 1.25 balanced, 1.333 editorial."
            }
          },
          "required": [
            "base",
            "ratio"
          ],
          "additionalProperties": false
        },
        "radius": {
          "type": "string",
          "enum": [
            "none",
            "sm",
            "md",
            "lg",
            "full"
          ],
          "description": "Corner preset: none 0/0/0, sm 2/4/6, md 4/8/12, lg 8/12/16, full pill."
        },
        "density": {
          "type": "string",
          "enum": [
            "compact",
            "comfortable",
            "roomy"
          ],
          "description": "Spacing multiplier 0.75 / 1 / 1.25 on the 4px grid."
        },
        "motion": {
          "default": "subtle",
          "description": "Transition durations: none 0ms, subtle 120/200ms, expressive 160/320ms. Loop (spinner) is always 800ms. Reduced-motion preferences override at runtime.",
          "type": "string",
          "enum": [
            "none",
            "subtle",
            "expressive"
          ]
        },
        "elevation": {
          "default": "subtle",
          "description": "Overlay shadows: flat = none (overlays rely on the scrim and border), subtle = soft (default), pronounced = deeper and darker. Scales shadow.raised / shadow.overlay in both modes.",
          "type": "string",
          "enum": [
            "flat",
            "subtle",
            "pronounced"
          ]
        },
        "layout": {
          "description": "Rhythm BETWEEN components and at page level. Everything inside a component comes from `space.*` via its own bindings; this decides gutters, section spacing, stack presets and measure.",
          "type": "object",
          "properties": {
            "rhythm": {
              "default": "normal",
              "description": "Multiplier on section and stack spacing (0.75 / 1 / 1.5) on top of density. tight for dense tools, loose for marketing and editorial.",
              "type": "string",
              "enum": [
                "tight",
                "normal",
                "loose"
              ]
            },
            "contentWidth": {
              "default": 960,
              "description": "Max width in px of the main content column (layout.maxWidth.content). Page width is 4/3 of it; prose measure is 65 characters at the body size regardless.",
              "type": "integer",
              "minimum": 480,
              "maximum": 1600
            }
          },
          "additionalProperties": false
        },
        "modes": {
          "type": "object",
          "properties": {
            "default": {
              "$ref": "#/$defs/mode"
            },
            "supports": {
              "minItems": 1,
              "type": "array",
              "items": {
                "$ref": "#/$defs/mode"
              }
            }
          },
          "required": [
            "default",
            "supports"
          ],
          "additionalProperties": false
        },
        "statusHues": {
          "type": "object",
          "properties": {
            "danger": {
              "default": 25,
              "type": "number",
              "minimum": 0,
              "maximum": 360
            },
            "success": {
              "default": 145,
              "type": "number",
              "minimum": 0,
              "maximum": 360
            },
            "warning": {
              "default": 80,
              "type": "number",
              "minimum": 0,
              "maximum": 360
            },
            "info": {
              "default": null,
              "description": "null = seed hue",
              "anyOf": [
                {
                  "type": "number",
                  "minimum": 0,
                  "maximum": 360
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        "overrides": {
          "description": "Escape hatch: explicit token values, keyed by a token's full or public path (schema/tokens.ts). `base` sets base-layer tokens (palette, type, spacing, radius, motion) before the modes are derived, so mode choices see them; `light` and `dark` set mode tokens after derivation. Each value must fit its token's type or be a `{path}` reference to a token of the same type. size.target.* and border.width.focus are accessibility floors and can't be overridden. Still contrast-checked.",
          "type": "object",
          "properties": {
            "base": {
              "type": "object",
              "propertyNames": {
                "type": "string"
              },
              "additionalProperties": {}
            },
            "light": {
              "type": "object",
              "propertyNames": {
                "type": "string"
              },
              "additionalProperties": {}
            },
            "dark": {
              "type": "object",
              "propertyNames": {
                "type": "string"
              },
              "additionalProperties": {}
            }
          },
          "additionalProperties": false
        },
        "tuning": {
          "description": "Named adjustments to base values the derivation otherwise fixes, so a theme can state them without an override. Each value left out keeps its derived default.",
          "type": "object",
          "properties": {
            "radius": {
              "description": "Corner steps in px (0–48), replacing the preset's three steps. radius.full stays 999px. No effect with radius: none.",
              "type": "object",
              "properties": {
                "sm": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 48
                },
                "md": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 48
                },
                "lg": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 48
                }
              },
              "additionalProperties": false
            },
            "lineHeight": {
              "description": "Line heights (1.0–2.2), tight ≤ normal ≤ loose. Defaults 1.2 / 1.5 / 1.7.",
              "type": "object",
              "properties": {
                "tight": {
                  "type": "number",
                  "minimum": 1,
                  "maximum": 2.2
                },
                "normal": {
                  "type": "number",
                  "minimum": 1,
                  "maximum": 2.2
                },
                "loose": {
                  "type": "number",
                  "minimum": 1,
                  "maximum": 2.2
                }
              },
              "additionalProperties": false
            },
            "fontWeight": {
              "description": "Font weights (100–900 in steps of 100), ascending. Defaults 400 / 500 / 600 / 700.",
              "type": "object",
              "properties": {
                "regular": {
                  "type": "integer",
                  "minimum": 100,
                  "maximum": 900,
                  "multipleOf": 100
                },
                "medium": {
                  "type": "integer",
                  "minimum": 100,
                  "maximum": 900,
                  "multipleOf": 100
                },
                "semibold": {
                  "type": "integer",
                  "minimum": 100,
                  "maximum": 900,
                  "multipleOf": 100
                },
                "bold": {
                  "type": "integer",
                  "minimum": 100,
                  "maximum": 900,
                  "multipleOf": 100
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      },
      "required": [
        "id",
        "tone",
        "not",
        "seed",
        "scale",
        "radius",
        "density",
        "modes"
      ],
      "additionalProperties": false
    },
    "mode": {
      "type": "string",
      "enum": [
        "light",
        "dark"
      ]
    }
  }
}
