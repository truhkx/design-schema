// Generated by tools/behavior_tests.ts from the `behavior` block of the Combobox doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Combobox } from '../../packages/react/src/Combobox';
import type { ComboboxProps } from '../../packages/react/src/Combobox';
import meta from '../../packages/react/src/Combobox.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<ComboboxProps> = {}) {
  const events = {
    onChange: vi.fn(),
    onInputChange: vi.fn(),
    onOpenChange: vi.fn(),
  };
  const props = { ...meta.args, ...given, onChange: events.onChange, onInputChange: events.onInputChange, onOpenChange: events.onOpenChange };
  const utils = render(<Combobox {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Combobox"]') ?? screen.queryByRole('combobox') ?? utils.container.firstElementChild) as HTMLElement,
    label: () => (screen.queryByRole('combobox') ?? s.root()) as HTMLElement,
    rerender: (next: Partial<ComboboxProps>) => utils.rerender(<Combobox {...props} {...next} />),
  };
  return s;
}

describe('Combobox', () => {
  test('renders', async () => {
    const s = setup({"open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-filter-starts-with', async () => {
    const s = setup({"filter": "startsWith", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-filter-contains', async () => {
    const s = setup({"filter": "contains", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-filter-none', async () => {
    const s = setup({"filter": "none", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-filter-async', async () => {
    const s = setup({"filter": "async", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({"open": true});
    expect(screen.getByRole('combobox', { name: s.props.label })).toBeInTheDocument();
  });
  test('control-is-focusable', async () => {
    const s = setup({"open": true});
    act(() => (s.label()).focus());
    expect(s.label()).toHaveFocus();
  });
  test('error-is-identified', async () => {
    const s = setup({"error": "Fix this before continuing.", "open": true});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeInTheDocument();
    expect(s.label()).toHaveAttribute('aria-invalid', 'true');
  });
});
