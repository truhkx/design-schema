# Gaps reported while generating ActionSheet for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 03:26 — round 1

- ActionSheet: the schema prop is `title`, but `HTMLElement` already defines `title` as the tooltip attribute (per this package's own convention for exactly this collision, e.g. AlertDialog/BottomSheet/Dialog's `heading`). Renamed the Lit property/attribute to `heading`, matching the rest of the overlay components, even though the anatomy composes it as `Text` rather than a real heading element.
- ActionSheet: anatomy lists no `handle`/header part, yet the `close` event's reason enum includes `drag` and the Behavior prose says 'a drag close[s] it'. Added an unlisted, undocumented decorative `.handle` bar (mirrored from `ds-bottom-sheet`'s own handle, using its same locked `color.foreground.muted` token) so the drag gesture has something to read as grabbable; the schema should either add a `handle` anatomy part with its own style binding, or state explicitly that drag applies only on native (RN) since it isn't part of ActionSheet's own anatomy.
- ActionSheet (wide/`<ds-menu>` presentation): the web platform note says the menu is 'anchored to the element that was focused when `open` became true,' but `<ds-menu>` only positions its popup from its own internal trigger button — it has no public API for an external anchor. Worked around this by placing the `<ds-menu>` host itself at the opener's screen rect (`position: fixed`, matching size, `opacity: 0`) so its own trigger inherits that position; this is an approximation (the real trigger button's intrinsic padding/min-target size can differ slightly from the original opener's), not pixel-exact anchoring. `Menu` would need its own anchor-rect prop to do this correctly.
- ActionSheet (wide/`<ds-menu>` presentation): the close reason enum is `escape | scrim | cancel | drag`, but `<ds-menu>`'s `open-change` event doesn't say why it closed (Escape and an outside pointerdown both just report `{ open: false }`), and there's no scrim or Cancel row in this presentation at all. Every non-modal dismissal in the wide presentation is reported as `close` with reason `escape` as the closest fit of the four; none of the reasons actually describe 'clicked outside a non-modal popup.'
- ActionSheet: no `dismissible`/always-dismissible note in the schema (unlike BottomSheet's explicit `dismissible` prop) — assumed the sheet is always dismissible via Escape/scrim/Cancel since no such prop is listed.

## 2026-09-10 18:13 — round 1

- ActionSheet: `dismissible`'s wide-screen (Menu) equivalent is unspecified — the doc says only 'clicking outside closes' for the wide presentation and doesn't say whether that outside-click close should be suppressed when dismissible is false. Kept it unconditional (mapped to reason 'escape', which the doc says always reports) since Menu has no scrim/cancel row to gate.
- ActionSheet: anatomy lists 'handle' but a11y/copy give it no description; mirrored BottomSheet's implementation (aria-hidden decorative pill, color.foreground.muted) since the doc's own platform notes say 'the same 25%/1.5px/ms rule' as BottomSheet's drag gesture.

## 2026-09-16 07:23 — round 1

- ActionSheet: the web and Lit platform notes say Menu 'takes no external anchor' and ask for an invisible trigger placed over the opener's rect, but the Lit ds-menu now has an `anchor` property that drops the trigger and positions from the element. I used `anchor` set to the opener (document.body when nothing had focus), which is the doc's intent without the hidden-trigger workaround; the notes should be updated.
- ActionSheet: `open` is `required` and 'Controlled visibility', but the Controlled state contract asks for an uncontrolled mode as well. The only change event is `close`, and there is nothing to report a choice as a close (`action` must not fire onClose), so an uncontrolled sheet could never close after a choice. I implemented controlled only.
- ActionSheet: `dismissible: false` does not say whether the Cancel row still renders or what it does. A Cancel that does nothing is a trap and one that closes ignores the prop, so I hide the Cancel row and its divider when not dismissible, as BottomSheet hides its close button.
- ActionSheet: overlay.dismiss lists `close-button` and `swipe`, but onClose's reasons are `cancel` and `drag`. I emitted `cancel` and `drag`; the overlay block should use the event's reason names.
- ActionSheet: the `titleSize` binding has no part, and the heading Text sets its own size, so a host hook can't reach it. It has no --ds-action-sheet-title-size hook and is forwarded to the heading Text's overrides.fontSize, alongside fontFamily and lineHeight as their descriptions say. `titleColor` (locked) is Text tone="muted"; the doc should name the heading part and the Text size/tone it maps to.
- ActionSheet: the doc doesn't say which overrides reach the wide ds-menu. I forwarded the bindings that share a name (shadow, radius, itemPaddingBlock, itemPaddingInline, itemGap, fontFamily, fontSize, lineHeight, enter) plus divider → Menu's `separator`; scrim, header*, handle*, exit, layer and maxWidth have no Menu equivalent. The Parts section says to 'add no other' forward but lists none for Menu.
- ActionSheet: `maxWidth` is both an overridable hook and the presentation breakpoint, and a media query can't read a per-element custom property. I read the host's computed --ds-action-sheet-max-width when connected, and again when overrides.maxWidth changes, and build matchMedia('(width > …)') from it, as ds-bottom-sheet does.
- ActionSheet: shadow, radius, surface and itemHover have no `part`, and divider/dividerWidth have none either (the doc describes a 'divider' element that is not in the anatomy). I applied shadow and radius to the surface (top corners only), and drew the dividers as undocumented elements: role=separator inside the menu before the danger group, aria-hidden above the cancel row.
- ActionSheet: the doc gives no height cap for the sheet ('height: content' from BottomSheet is implied only on web). I capped it at 90dvh with a literal-ok comment, and the list scrolls inside.
- ActionSheet: headerPaddingBlock covers 'the header and the cancel row', but no binding sets the header's or cancel row's inline padding. I reused itemPaddingInline for both.
- ActionSheet: the keyboard rule 'Enter/Space chooses and closes' (expect: closes) contradicts the controlled contract, where the consumer closes. The element fires `action` through the native button's own activation, and the Keyboard story's consumer closes on `action` and `close` so the gate can see it close.
- ActionSheet: a hovered row doesn't say whether it takes focus (ds-menu moves focus on hover). Following 'implement every key → action and nothing else', hover only paints itemHover and focus stays on the roving item.
- ActionSheet: 'the-cancel-row-is-named-from-copy' and 'has-accessible-name' don't say which element is checked. The tests read the cancel ds-button's label and its inner button's accessible name, and the role=menu list's accessible name (the dialog also gets the same aria-label).
- ActionSheet: 'escape-fires-on-close' and 'renders' use the Default story's args, which are not given in the doc, so the Default story is the photo-actions example (open, heading Photo.jpg, four actions). The schema has no enum props of its own, so the stories are Default, Closed, DismissibleFalse, the three examples and Keyboard.
- ActionSheet: behavior scenarios run only in the narrow presentation (the test viewport is below maxWidth); the wide Menu presentation, its reason mapping and focus return have no scenario.

## 2026-09-17 11:06 — round 1

- ActionSheet: the Lit note says 'the forwarded ref resolves to the shadow <dialog>, null while closed', but Lit has no forwarded ref and no other Lit overlay exposes one. I added a public `dialog` getter (HTMLDialogElement | null; null while closed and in the wide presentation); the doc should name the property or drop the sentence for Lit.
- ActionSheet: the 'part on an overlay-owned wrapper' rule (cancelButton on the row wrapping the Button, itemIcon a span wrapping Icon) is only in the web notes. I applied it to Lit too for parity, so `[data-part=cancelButton]` is the row, not the ds-button; the Lit notes should say so.
- ActionSheet: 'a close that accompanies a chosen action never fires onClose, whatever order the two arrive in' has no mechanism for Lit, where ds-menu dispatches open-change and action synchronously and a focus-out can come from the same press. I delay the mapped close by a microtask, drop it if an action arrived, and report at most one close per task (a setTimeout resets that). The doc should state the ordering Menu guarantees, so there's nothing to delay.
- ActionSheet: the focusScope composition lists exactly trapped/restoreFocus/autoFocus. The previous element also paused the scope during the exit transition (`active=false`); I removed that to follow 'add no other', so the trap stays on through the exit animation. The doc doesn't say whether the trap should be paused while closing.
- ActionSheet: constants dismissDistance (0.25) and dismissVelocity (1.5 px/ms) have no token expression, so 'read each constant through its token expression' can't be followed; they stay module constants.
- ActionSheet: the `exit` binding covers both the slide-down (motion.easing.exit) and the below-threshold spring-back (motion.easing.standard), one token with two easings. I used the exit hook for both durations and separate easing tokens per case.
- ActionSheet: the schema has no enum props (tone is inside the `actions` shape), so 'a story per enum value' produces none. Stories stay Default, Closed, DismissibleFalse, the three examples and Keyboard.
- ActionSheet: the behavior scenarios run only in the sheet presentation (the test viewport is below layout.maxWidth.prose). The wide Menu presentation, its close-reason mapping, the action/close suppression and the layer/divider forwards have no scenario or test.

## 2026-09-21 09:41 — round 1

- ActionSheet: the doc says focus returns to the opener "at the start of the exit transition", but the sheet is a showModal() <dialog> whose inert background refuses focus to anything outside it until close(). Implemented `active` following `open` (so the scope never pulls focus back during the exit) and attempt the restore at the start; under showModal it actually lands when the scope unmounts at the end of the exit. The doc should say which of the two it wants.
- ActionSheet: the `layer` binding names part `surface`, but the surface is inside the <dialog> and inside the top layer z-index is inert — the only element that can honour it is the <dialog> itself (the position:fixed fallback the doc mentions). Put `z-index: var(--ds-action-sheet-layer)` on the <dialog>; on `surface` it would be dead.
- ActionSheet: titleSize says Text's hook is "set to --ds-action-sheet-title-size in the sheet's stylesheet" but does not say on which element. Text sets --ds-text-font-size on its own `:host([size="sm"])`, which beats plain inheritance, so the declaration has to target the ds-text element from the sheet's shadow stylesheet (outer-tree wins over :host). Chose `.heading ds-text { --ds-text-font-size: … }`, same for fontFamily and lineHeight.
- ActionSheet: behavior scenario `the-cancel-row-is-named-from-copy` is not expressible on Lit the way the generated harness checks it (copy string present in the sheet's shadowRoot.textContent): ds-button has no default slot, so the Cancel label is a `label` property rendered inside the button's own shadow root. The hand-written test asserts the accessible name instead; the generated one fails (pre-existing). Either the scenario needs a name-based expectation or Button needs a slotted label.
- ActionSheet: `dismissible: false` with no `heading` drops the header, but the doc does not say what replaces its block padding — the surface gains no top inset, so the first row's own itemPaddingBlock becomes the top spacing. Chose that (no compensating padding); if the surface should keep headerPaddingBlock at the top edge, say so.
- ActionSheet: the Enter/Space keyboard rule carries `expect: closes` while its own action text says the sheet does not close itself and the story's consumer does. Implemented as fire-only (native button activation, nothing when the row is disabled); the `expect` value looks wrong for the sheet presentation.
- ActionSheet: `open` is documented as "controlled only — there is no uncontrolled mode", but the Controlled state contract still says "uncontrolled from its initial state when omitted". Implemented controlled-only: `open` defaults to false and the element never flips it.
- ActionSheet: the guidance says example stories "start from blank args, never from Default's or meta args", which CSF cannot express — meta args always merge into every story, and emptying meta.args would break both the Default-is-photo-actions rule and the generated behavior harness, which reads `meta.args`. Mirrored React exactly instead: meta carries open/heading/actions and each example restates its whole `given` (UnnamedSheet passing `heading: undefined`).

## 2026-09-23 14:44 — round 1

- ActionSheet: titleSize and itemIconSize are described as having 'no host hook of its own', yet the delivery text sets Text's/Icon's hook to `--ds-action-sheet-title-size` in the sheet's stylesheet, so a host hook must exist; the digest rule says a forwarded binding declares no hook. Chose to declare both hooks on :host (default to their tokens) so consumer CSS works as the titleSize text requires.
- ActionSheet: the Keyboard story's trigger icon is not named anywhere in the doc (no copy key, no icon). Chose `ellipsis` to match the React story; the doc should name the glyph and the 'More actions' label's source.
- ActionSheet: the wide-Menu mapping says the focus-out 'Menu raises when the window itself loses focus' reports nothing, but Menu's open-change detail does not distinguish a window blur from an in-page focus loss. Chose `document.hasFocus() === false` at dispatch time as the discriminator; Menu's detail could carry this instead.
- ActionSheet: the doc does not say how to de-duplicate Menu's `outside` followed by the `focus-out` of the same press (both map to scrim). Chose one close report per task (a flag cleared by setTimeout).
- ActionSheet: Chromium closes a modal <dialog> on Escape without a cancelable `cancel` when there is no user activation; the doc's escape-dismiss contract does not cover this. Chose to report `close` {reason: 'escape'} from the native `close` event (unless `cancel` already reported it) and reopen, since the consumer owns `open`.
- ActionSheet: the story wrapper is told to 'first call the story's own onAction/onClose args'; Lit stories have no function args for events (they log via parameters.actions.handles). Chose the handles plus a close-on-event listener; the doc could state the Lit equivalent.
- ActionSheet: under the `open`-attribute fallback (showModal missing or throwing) Escape does nothing, because no native cancel fires. The doc says the fallback exists only so tests can render, so no keydown Escape handler was added.

## 2026-09-23 14:45 — round 2

- ActionSheet: maxWidth is locked and the spec says the breakpoint is 'read from the theme token, not per instance', but the hooks gate requires a `--ds-action-sheet-max-width` hook, and a hook exists to be re-themed per element. Chose to declare the hook (defaulting to layout.maxWidth.prose) so the codemod can rename it, while the matchMedia query keeps reading the root theme token; setting the hook on one sheet does not move its breakpoint. The doc should say whether the hook is meant to take effect (reading it from the host's computed style) or is declared only for renaming, as Menu's phoneBreakpoint is.
- ActionSheet: titleColor is 'realized by the composed heading Text's tone="muted"; Text owns its color', yet the gate requires a sheet hook for it. Chose to declare `--ds-action-sheet-title-color` and pass it to Text through its documented `--ds-text-color` hook from the outer-tree rule `.heading ds-text` (the same path titleSize uses), with tone="muted" still set. By default the rendered color does not change. The doc should say whether a sheet-level hook may override the color Text owns.
- ActionSheet: minTarget, focusRing and focusRingWidth have no `part` in the doc. Chose the item rows (min-block-size and :focus-visible outline). The Cancel row's target size and focus ring come from the composed ds-button's own hooks, so the sheet's hooks do not reach it.
- ActionSheet: the spec gives the surface no foreground binding. The surface text color reuses `--ds-action-sheet-item-color` (color.foreground) instead of a separate token.

## 2026-09-23 19:20 — round 1

- ActionSheet: the spec says the Keyboard story accepts `given` args from the story URL, but no keyboard rule declares any `given` args, so I added no URL-arg handling.
- ActionSheet: the photo-actions example uses `icon: danger` on the delete action, which is a status-shape glyph, not a destructive-action icon. I kept it verbatim, but the example may be a typo for `close` or `trash`.
