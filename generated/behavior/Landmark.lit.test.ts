// Generated by tools/behavior_tests.ts from the `behavior` block of the Landmark doc. Do not edit.
import { beforeEach, describe, expect, test, vi } from 'vitest';
import { userEvent } from 'vitest/browser';
import '../../packages/lit/src/Landmark.js';
import meta from '../../packages/lit/src/Landmark.stories.js';

function deep(root: ParentNode, selector: string): Element | null {
  const direct = root.querySelector(selector);
  if (direct) return direct;
  for (const el of Array.from(root.querySelectorAll('*'))) {
    const shadow = (el as HTMLElement).shadowRoot;
    if (shadow) {
      const found = deep(shadow, selector);
      if (found) return found;
    }
  }
  return null;
}

/** Every element that is "active" from the document down through shadow roots: the host, then the inner one.
 *  document.activeElement alone is a shadow host while focus sits inside its shadow tree. */
function activeChain(): Element[] {
  const chain: Element[] = [];
  let el: Element | null = document.activeElement;
  while (el) {
    chain.push(el);
    el = el.shadowRoot?.activeElement ?? null;
  }
  return chain;
}

async function setup(given: Record<string, unknown> = {}) {
  const el = document.createElement('ds-landmark');
  const props = { ...meta.args, ...given };
  for (const [key, value] of Object.entries(props)) {
    if (value === undefined) continue;
    // `children` is slotted light-DOM content, and `Element.children` is getter-only:
    // assigning it throws and takes the whole file down at setup. Slot a string into the
    // default slot the way the stories do; a richer value cannot be expressed here.
    if (key === 'children') {
      if (typeof value === 'string') el.append(value);
      continue;
    }
    (el as unknown as Record<string, unknown>)[key] = value;
  }
  const events = {
  };
  document.body.append(el);
  await (el as unknown as { updateComplete: Promise<boolean> }).updateComplete;
  const root: ParentNode = el.shadowRoot ?? el;  // some elements render in light DOM
  const s = {
    el,
    root,
    events,
    props,
    root_: () => el,
    region: () => (deep(root, '[part="region"]') ?? deep(root, '[data-part="region"]') ?? root.firstElementChild) as HTMLElement,
  };
  return s;
}

beforeEach(() => {
  document.body.replaceChildren();
});

describe('ds-landmark', () => {
  test('the-role-prop-chooses-the-landmark', async () => {
    const s = await setup({"role": "navigation"});
    expect(s.el.shadowRoot!.querySelector('[role="navigation"]')).not.toBeNull();
  });
  test('search-is-the-search-landmark', async () => {
    const s = await setup({"role": "search"});
    expect(s.el.shadowRoot!.querySelector('[role="search"]')).not.toBeNull();
  });
  test('main-is-the-primary-content-landmark', async () => {
    const s = await setup({"role": "main"});
    expect(s.el.shadowRoot!.querySelector('[role="main"]')).not.toBeNull();
  });
  test('a-region-is-named-by-its-label', async () => {
    const s = await setup({"role": "region", "label": "Related articles"});
    expect(s.el.shadowRoot!.querySelector('[role="region"]')).not.toBeNull();
    expect(s.region()).toHaveAttribute("aria-label", "Related articles");
  });
  test('renders', async () => {
    const s = await setup({});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-role-banner', async () => {
    const s = await setup({"role": "banner"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-role-navigation', async () => {
    const s = await setup({"role": "navigation"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-role-main', async () => {
    const s = await setup({"role": "main"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-role-complementary', async () => {
    const s = await setup({"role": "complementary"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-role-contentinfo', async () => {
    const s = await setup({"role": "contentinfo"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-role-region', async () => {
    const s = await setup({"role": "region"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-role-search', async () => {
    const s = await setup({"role": "search"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-role-form', async () => {
    const s = await setup({"role": "form"});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test.skip('has-accessible-name — then.name: role from prop \'role\' cannot be queried; name the landmark/text role in the doc', async () => {});
});
