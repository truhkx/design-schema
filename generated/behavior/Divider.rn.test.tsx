// Generated by tools/behavior_tests.ts from the `behavior` block of the Divider doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Divider } from '../../packages/rn/src/Divider';
import type { DividerProps } from '../../packages/rn/src/Divider';
import meta from '../../packages/rn/src/Divider.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<DividerProps> = {}) {
  const events = {};
  const props: DividerProps = { ...(meta.args as DividerProps), ...given };
  const tree = (p: DividerProps) => (
    <ThemeProvider mode="light">
      <Divider {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Divider') ?? screen.UNSAFE_root,
    line: () => screen.queryByRole('separator') ?? s.root(),
    rerender: (next: Partial<DividerProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Divider', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-orientation-horizontal', () => {
    const s = setup({"orientation": "horizontal"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-orientation-vertical', () => {
    const s = setup({"orientation": "vertical"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-spacing-none', () => {
    const s = setup({"spacing": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-spacing-tight', () => {
    const s = setup({"spacing": "tight"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-spacing-normal', () => {
    const s = setup({"spacing": "normal"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-spacing-loose', () => {
    const s = setup({"spacing": "loose"});
    expect(s.root()).toBeTruthy();
  });
});
