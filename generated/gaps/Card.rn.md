# Gaps reported while generating Card for rn

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-09 21:04 — round 1

- interactive: the spec asks the child's own Pressable/Text node to render 'accessible={false}' when collapsed into the surrounding Pressable, but Button/Link don't forward unknown props to their inner element (they destructure a fixed prop list), so that prop would be silently dropped. Instead I wrap the located Button/Link in a `View` with `pointerEvents="none"` + `accessibilityElementsHidden` + `importantForAccessibility="no"`, which collapses touch and most screen-reader traversal into the outer Pressable but isn't guaranteed to remove it from a hardware-keyboard/tab order on every platform (notably react-native-web) the way a true single DOM node would. Flagging in case the schema wants Button/Link to grow an official 'render inert' escape hatch instead.
- interactive: searched only `children` (the body) for the single required Link/Button, not `headerActions` or `footer`, since combining 'exactly one interactive child' with those other anatomy slots isn't specified and risks double targets; a Button/Link placed in `footer` or `headerActions` on an interactive card is not neutralized and stays independently focusable/pressable.
- interactive border: the static border (surface default) and the locked focus ring use different token widths (`border.width.thin` vs `border.width.focus`). To avoid the ring toggle shifting layout (matching Button's own technique), interactive cards always reserve `border.width.focus` for their border, so `overrides.borderWidth` has no visible effect while a card is interactive — it only affects non-interactive, surface:default cards.
- transition: there is no continuous pointer hover on native to animate between (same limitation Button/Link already document for their hover tokens), so `overrides.transition` is accepted by the type but has no runtime effect; `hoverBackground` instead styles the Pressable's momentary `pressed` state, swapped instantly with no animation.
- header/footer/body rows are built as plain `View`s styled directly from `layout.gap.*` tokens rather than composed from the `Stack` component, because `Stack`'s `gap` prop only accepts the `space.*` scale ('0'..'12'), not the `layout.gap.*` tokens (`loose`/`normal`/`tight`) the schema specifies for `partGap`/`headerGap`/`footerGap`. This mirrors how Alert/Disclosure already lay out internal rows in this package without going through Stack.
- no token binding is given for the gap between multiple `headerActions` items (only `headerGap`, between the heading and the whole actions group, is specified); used `space.2` as a reasonable small gap and flagged it here rather than inventing a new named binding.
- Default story's `headerActions` demo uses a plain `ghost`/`sm` Button rather than the icon-only ghost Button the guidance describes, since Card's schema names no icon and the package has no Icon glyph specified for this use; kept it label-only to avoid inventing iconography.
- Heading's `size` is left at its default per-level size; the web platform note says header size should be 'restrained per the theme' but gives no concrete size or token, so no additional restraint was applied on native.

## 2026-09-09 21:51 — round 1

- Card: transition (motion.duration.fast) has no runtime effect on native since Pressable's pressed style swaps instantly and there's no continuous hover state to animate between — kept the overridable binding for API parity but documented it as inert in the docstring, per the existing implementation's choice.
- Card: header/footer are hand-built horizontal View rows using layout.gap.* tokens rather than the Stack component, since Stack's `gap` prop only accepts the space.* scale, not layout.gap.* — noted in the docstring rather than growing Stack's API.
- Card: headerActions 'at most two' is a content guideline, not enforced in code (no runtime check on children count), consistent with how other components treat soft content limits.
- Link: added exported LINK_EXTERNAL_SUFFIX purely so Card's interactive hit-area collapsing can reproduce Link's accessible name (including the external suffix) verbatim instead of duplicating the copy string — this was a pre-existing bug in the already-generated Card.tsx, now fixed.

## 2026-09-09 21:52 — round 2

- parse gate: failure is unrelated to Card — checkbox.md and switch.md carry a `behavior` frontmatter block (from the prior behavior-scenarios rollout) that schema/component.schema.json rejects under additionalProperties: false; card.md has no such key. Fixing this needs an edit to site/components/{checkbox,switch}.md or schema/component.schema.json, both outside my permitted paths (site/, schema/) — reporting rather than editing forbidden files. No Card code change addresses this gate.

## 2026-09-09 21:52 — round 3

- parse gate: unchanged from round 2 — failure is in checkbox.md (line 113) and switch.md (line 89), which retain a `behavior` frontmatter block from the earlier behavior-scenarios rollout that schema/component.schema.json rejects (additionalProperties: false). card.md has no `behavior` key and Card's generated files are unaffected (typecheck and all 11 Card.test.tsx tests pass). This requires editing site/src/content/docs/components/{checkbox,switch}.md or schema/component.schema.json, both outside my permitted paths (site/, schema/); no change to packages/rn/src/Card.* can fix it.

## 2026-09-09 22:23 — round 1

- Card: spec's borderWidth binding says border is 'Rendered only with surface default', but the interactive Pressable always reserves borderWidth at t.borderWidthFocus (transparent border color when subtle/unfocused) to avoid layout shift when focus toggles — kept this pre-existing, documented interpretation rather than making focus-ring width surface-conditional, since reserving space is necessary to prevent jank and the spec doesn't address focus-ring layout stability.

## 2026-09-10 01:23 — round 1

- Card: schema's platforms.rn.notes says header/footer are 'plain row Views styled from this component's gap bindings, not Stack' while the Guidance/Platform-notes prose says 'header and footer are horizontal Stacks' — the existing implementation follows the schema's own notes (plain Views using layoutGap* tokens directly) since Stack's gap enum only accepts the space.* scale, not Card's own overridable gap bindings.
- Card: 'interactive' a11y note says the card is 'not focusable' but the implementation makes the Pressable itself the sole focusable/accessible element (with the child Link/Button hidden via accessibilityElementsHidden) — interpreted as 'the card doesn't add a second focus stop beyond its single child target', consistent with the anatomy's intent, not literally non-focusable.
- Card: no explicit prop for aria-labelledby equivalent since RN has no id/labelledby mechanism — accessibilityLabel is only set on the Pressable in the interactive path (taken from the child's label), not on the plain View wrapper, since a non-interactive article-like grouping has no direct RN analog; the Heading itself remains the discoverable name via accessibilityRole="header".

## 2026-09-10 17:42 — round 1

- Card: the schema's `focusable` prop (tabindex=-1 scripted focus, own ring) was entirely missing from the previously-generated Card.tsx despite being in the current schema and despite Feed already composing Card — added it now. Implemented as: `React.forwardRef<View, CardProps>` exposing the root's native View (RN's `NativeMethods.focus()/.blur()`) so a container can move focus to it by script; `tabIndex={-1}` on the root, which is a real DOM tabIndex=-1 (scriptable, no tab stop) under react-native-web where this package's stories/tests run, but on native Android RN's own `tabIndex` typing documents `-1` as simply not-focusable, so the 'scriptable via -1' semantics are a react-native-web-only guarantee, not a cross-platform one.
- Card: RN core's `View` TypeScript type has no `onFocus`/`onBlur` (Flow types support them; DataGrid/Tree instead use `Pressable`, which is typed, for focus tracking) even though the runtime and react-native-web support them — added a local `FocusableViewProps` cast (`View as unknown as ForwardRefExoticComponent<...>`) rather than reaching for `any`, but this means the ring-drawing half of `focusable` is only verified to work on react-native-web, not asserted against real native builds.
- Card: no component in this package currently calls the new ref to drive scripted focus — Feed.tsx (the consumer the schema names: 'so a container (Feed) can move focus to it by script') renders a native FlatList with no equivalent of the web doc's PageUp/PageDown scripted-paging interaction, so `focusable` is wired up (prop, ref, ring) but has zero current callers on this platform. Wiring Feed itself to use it was out of scope for this Card-only generation pass.
- Card: added a `__DEV__` warning when `interactive` and `focusable` are both set (interactive wins, focusable is a no-op) since the schema doesn't say what should happen for that combination — treated it as a documented no-op rather than a runtime error, matching the codebase's existing soft-guard style (e.g. the interactive child-count warning already in this file).
- Card: no behavior-scenario test covers `focusable` (the 11 generated scenarios predate this prop), so its addition is untested by the generated Card.test.tsx; only manually verified via `tsc --noEmit` and the existing 11 `renders: true` tests, which still pass unchanged.

## 2026-09-16 04:28 — round 1

- Card: `hoverBackground` is a `state: hover` binding, but native touch has no hover; I showed it on Pressable hover (react-native-web and iPad pointer) and on press, following the `transition` note that 'the pressed style swaps instantly'.
- Card: the RN notes say the Pressable takes accessibilityRole and accessibilityLabel from the single child, but don't say what happens with zero or several Button/Link children. I leave the role and label unset and pressing does nothing; the children are still made inert and a __DEV__ warning fires.
- Card: nothing says whether a disabled child Button makes the interactive card disabled. I copied `disabled` into the Pressable's accessibilityState and guard the press.
- Card: `focusable` needs tabIndex={-1} and focus events on a View. RN core's View type has no onFocus/onBlur and on Android tabIndex -1 means not focusable, so scripted focus only works on react-native-web. I typed onFocus/onBlur through a local alias; the doc doesn't acknowledge this limit.
- Card: `borderWidth` says an interactive card reserves border.width.focus 'transparent until focused', but a `surface: default` card also needs its border. I use color.border (or the `border` override) as the resting color on default cards and transparent on subtle ones, and the same for `focusable` cards.
- Card: `heading-hierarchy` requires the header role and says headingLevel only affects typography, but the Guidance fixes the heading at `size: lg`, so on RN the level changes nothing visible. I pass both to Heading.
- Card: the `dense-grid-card` example's `given` has no `heading`, but the Default story args include one. I set heading: undefined in the story so it matches the example.
- Card: example children are prose strings ('A Link to the invoice', 'What the plan includes'). I rendered them as a Link and as Text, since a bare string can't sit in a View on RN.
- Card: the spec gives `actionsGap` no `part` (only headerGap/footerGap name one). I applied it to the headerActions row view.
- Card: the `transition` binding has no runtime effect on RN (per its own description). I accept it in overrides but don't read it.

## 2026-09-17 04:43 — round 1

- Card: The rules say `disabled` sets accessibilityState plus the `disabled` prop, but the package conventions say never pass `disabled` to Pressable (it removes focus). I followed the package: accessibilityState.disabled plus a press guard.
- Card: `focusable` is a no-op when `interactive` is set, but the spec doesn't say what happens when `interactive` finds zero or several targets and the card falls back to non-interactive. I let `focusable` apply then, since the card has no target. The warning still fires whenever both props are set.
- Card: 'top-level children' is not defined for React Fragments. A Link inside a top-level `<>...</>` is not found (only arrays are looked into).
- Card: the spec wraps a plain string body in Text but doesn't say what happens to strings in an array body. I wrap each top-level string or number too.
- Card: the notes say the card takes the child's accessibilityRole and accessibilityLabel but don't say which of Button's label sources wins (`accessibleName`, `accessibilityLabel`, `label`), or whether pressing the card runs Button's `track`/`onTrack`, `type=submit` and `loading` handling. I copied Button's own order and press handler, including the form's disabled state.
- Card: the heading part has no testID (the guidance says it keeps its own hook), and neither the anatomy nor the rn notes give the surface a `Card.surface` hook. The root keeps `testID="Card"`.
- Card: the `dense-grid-card` example's `given` has no heading, but the Default args set one. 'Exactly its given as args' cannot hold without clearing it, so the story sets `heading: undefined`.
- Card: the `transition` binding is overridable but has no runtime effect on native (the pressed style swaps instantly), so the override is accepted and ignored.
- Card: no rn behavior scenario covers interactive or focusable (those scenarios are web/lit only), so the new target logic has no test on this platform.

## 2026-09-20 10:11 — round 1

- Card: `disabled` in the rules says the whole element takes `opacity.disabled`, but Card has no disabled binding and a disabled child Button already dims itself. I left the card's opacity untouched (only accessibilityState.disabled, a press guard and no hover background) rather than double-dimming the child.
- Card: the flattened top-level list is re-rendered as an array, so element children written without keys now sit in an array. I key each entry with a wrapping Fragment at its flattened index, which means a reordering body remounts its children; the doc says nothing about identity across the interactive/non-interactive fallback.
- Card: on web and Lit the headerActions/footer rows are lifted above the hit area with position:relative + z-index:1. Native has no equivalent — the nested Button inside the wrapping Pressable wins the touch by the responder system, so a header action still works, but that is RN responder behavior rather than anything the spec states.
- Card: `focusable` still needs `tabIndex={-1}` plus onFocus/onBlur on a View. RN core types View without those and Android treats -1 as not focusable, so the ring and scripted focus are react-native-web-only; I keep the local `FocusableView` cast. The rn notes acknowledge the platform limit but not the typing hole.
- Card: `transition` is overridable but, per its own description, has no runtime effect on native, so the override is accepted by the type and never read.
- Card: no rn behavior scenario covers `interactive` or `focusable` (both scenarios are web/lit-only), so the target scan, the Fragment flattening and the focusable no-op ship untested by the generated Card.test.tsx.
- Card: with zero or several candidates the rn notes do not say what the Pressable should announce. The card renders a plain View instead — no role, no label, no press — and warns once under __DEV__.
- Card: the `dense-grid-card` example's `given` has no `heading` while the Default args set one, so "exactly its given as args" cannot hold; the story passes `heading: undefined` to clear it (unchanged from the previous round).

## 2026-09-20 10:24 — round 2

- Card: the rn notes prescribe neutralising the interactive target with `pointerEvents="none"`, `accessibilityElementsHidden` and `importantForAccessibility="no"`, but react-native-web implements none of those three, so on the platform the axe gate actually runs the collapsed Button keeps its tabindex and role and fails `nested-interactive`. I added `inert` (which RNW forwards and axe honours) alongside them. The doc should name the web half of the neutralisation, since `accessible={false}` is not available and the native props are native-only.
- Card: even with the target inert, react-native-web renders both the card's Pressable and the collapsed Button as real `<button>` elements, so React logs 'In HTML, <button> cannot be a descendant of <button>' for the interactive-with-Button stories. Nothing Card can do about it without reaching into Button; the doc's 'wrap and hide the child' model assumes a host that can be made non-semantic.
- Card: Feed/React Native fails `aria-required-children` on every story (role=feed with no article children) because the rn notes give Card no article role — `a-card-with-a-heading-is-an-article` is scoped to web and lit. I did not add one: it would collide with the interactive card taking its child's role, and the schema's a11y.role is `none`. The doc needs to say what a Card contributes to a Feed on this platform.
- Card: the axe gate is whole-Storybook, so a Card job's pass/fail is decided by other components' debt (~350 failures per mode in the rn run, 2 of them Card's). Per-component evidence has to be produced by hand each round.
- Card: `disabled` in the rules says the whole element takes `opacity.disabled`, but Card has no disabled binding and a disabled child Button already dims itself. I left the card's opacity untouched (accessibilityState.disabled, a press guard, no hover background) rather than double-dimming.
- Card: on web and Lit the headerActions/footer rows are lifted above the hit area with position:relative + z-index:1. Native has no equivalent — a nested Button wins the touch by the responder system — so that part of the contract is unstated for rn.
- Card: `focusable` still needs `tabIndex={-1}` plus onFocus/onBlur on a View, which RN core's type omits and Android treats as not-focusable, so it is a react-native-web-only capability typed through a local `DomView` cast. The rn notes acknowledge the platform limit but not the typing hole.
- Card: `transition` is overridable but, per its own description, has no runtime effect on native, so the override is accepted by the type and never read.
- Card: no rn behavior scenario covers `interactive` or `focusable` (both are web/lit-only), so the target scan, the Fragment flattening, the focusable no-op and this round's `inert` fix ship untested by the generated Card.test.tsx.
- Card: the `dense-grid-card` example's `given` has no `heading` while the Default args set one, so 'exactly its given as args' cannot hold; the story passes `heading: undefined` to clear it.

## 2026-09-20 10:30 — round 3

- Card: the axe gate is whole-Storybook, so a Card job is failed by other components' debt. The refreshed dumps carry 337 findings across 26 components (Listbox 44, Carousel 30, Table 29, Toolbar 23, Tree 20, …) and zero from Card; round 2's `inert` fix cleared Card's only two. A component job cannot make this gate green, and its truncated tail gives no signal about the component under generation — the gate needs to be scoped per component, or its report needs to separate the job's own findings from the pre-existing set.
- Card: Feed/React Native fails `aria-required-children` on every story, including `Empty` where no Card is rendered, so role=feed's article children are Feed's problem and not Card's. The doc still does not say what a Card contributes to a Feed on this platform — `a-card-with-a-heading-is-an-article` is scoped to web and lit, and the rn notes give Card no article role.
- Card: the rn notes prescribe neutralising the interactive target with `pointerEvents="none"`, `accessibilityElementsHidden` and `importantForAccessibility="no"`, but react-native-web implements none of the three, so the doc's neutralisation is a no-op on the platform the gate runs. The code adds `inert` alongside them; the doc should name the web half, since `accessible={false}` is not available on Button or Link.
- Card: even with the target inert, react-native-web renders both the card's Pressable and the collapsed Button as real `<button>` elements, so React logs 'In HTML, <button> cannot be a descendant of <button>' for the two interactive-with-Button stories. No axe rule catches it and Card cannot avoid it without reaching into Button; the doc's wrap-and-hide model assumes a host that can be made non-semantic.
- Card: `disabled` in the rules says the whole element takes `opacity.disabled`, but Card has no disabled binding and a disabled child Button already dims itself. I left the card's opacity untouched (accessibilityState.disabled, a press guard, no hover background) rather than double-dimming.
- Card: on web and Lit the headerActions/footer rows are lifted above the hit area with position:relative + z-index:1. Native has no equivalent — a nested Button wins the touch by the responder system — so that part of the contract is unstated for rn.
- Card: `focusable` needs `tabIndex={-1}` plus onFocus/onBlur on a View, which RN core's type omits and Android treats as not-focusable, so it is a react-native-web-only capability typed through a local `DomView` cast. The rn notes acknowledge the platform limit but not the typing hole.
- Card: `transition` is overridable but, per its own description, has no runtime effect on native, so the override is accepted by the type and never read.
- Card: no rn behavior scenario covers `interactive` or `focusable` (both are web/lit-only), so the target scan, the Fragment flattening, the focusable no-op and the `inert` fix ship untested by the generated Card.test.tsx — they are only covered by the throwaway jsdom harness in logs/.
- Card: the `dense-grid-card` example's `given` has no `heading` while the Default args set one, so 'exactly its given as args' cannot hold; the story passes `heading: undefined` to clear it.

## 2026-09-20 14:54 — round 1

- Card: platforms.rn.props lists accessibilityRole and accessibilityLabel, but the only scenario that names them (a-card-with-a-heading-is-an-article) is web+lit, and the rn notes attach both to the interactive Pressable only. I left a non-interactive card's root View unlabelled and unroled — labelling it would collapse the whole card into one accessibility node and hide the Heading, body and footer from swipe navigation, which contradicts 'screen-reader users reach each card by swiping, its Heading being a header'. So on RN a heading gives the card no name at the container level.
- Card: the rn notes say the interactive Pressable runs 'the single child Link/Button's own press behavior', but Link's own behavior on react-native-web is the browser navigating a real anchor (Linking is never called). The Card's Pressable is not an anchor, so I mirrored Link's native path only: with no onPress (as in the WholeCardIsALink example, href '#') pressing an interactive card calls Linking.openURL, which under react-native-web opens a new window instead of navigating in place. I did not reach for window.location — no component in the package touches DOM globals — so interactive Link cards behave differently on the web build than a bare Link does.
- Card: `transition` is listed as overridable, but the schema also says the binding 'has no runtime effect' on RN. I kept it in CardOverridableBinding for API parity and never read it, so overrides.transition is silently ignored — the only override in the union that changes nothing.
- Card: the borderWidth description ('an interactive card always reserves border.width.focus instead') mentions only interactive cards, while the rn notes add 'a focusable card reserves border.width.focus like an interactive one'. I reserved the ring width for both, which means an override of borderWidth is a no-op on focusable cards as well as interactive ones — the description says only interactive.
- Card: hoverBackground's description gives the subtle-surface value (color.background.strong) in web/Lit terms. I applied the same per-surface lookup on RN for the pressed and pointer-hover state; the schema never states it for native explicitly.
- Card: `focusable` maps to tabIndex=-1, which RN core's View type does not declare and which Android treats as not focusable. I typed it through a local DomView alias (also used for `inert`, since accessibilityElementsHidden/importantForAccessibility are dropped by react-native-web) and documented it as a react-native-web-only capability. The schema asks for a capability no native platform has, so on iOS/Android `focusable` renders nothing observable and the card-focused-by-a-feed example is web-only in practice.
- Card: `interactive` says the child is searched among 'top-level children of the body' with 'a top-level Fragment is flattened'. It does not say whether a bare string inside such a Fragment is a top-level body child for the wrap-in-Text rule; I flatten Fragments and arrays for both purposes, so those strings get the system Text too.
- Card: behavior scenario heading-is-rendered-as-a-heading asserts `role: heading` for web only, leaving no RN assertion beyond the text. I asserted the text alone; the header role is Heading's own and is not re-checked by Card's test.
- Card: the doc says the development warning for zero-or-several targets fires 'once per mounted card', but not whether a card that later gains a valid single target should warn again if it goes back to zero. I latch the warning per mount (a ref), so a card that flips invalid → valid → invalid warns only the first time.

## 2026-09-22 04:12 — round 1

- Card: the flatten-and-wrap rule for bare strings/numbers is specified for `children` only, but the Behavior section calls a `0` in any slot content that renders its row — and a bare string or number in a native View throws. I applied the same `renderSlot` flatten-and-wrap to `headerActions` and `footer`; the doc should say whether those slots share the body's wrap contract.
- Card: a11y.role is `none` and platforms.rn.props lists accessibilityRole, but no value is given for the interactive Pressable. I take it from the target's kind — `link` for a Link child, `button` for a Button child — since the notes say role is copied from the child.
- Card: the ring is specified to show 'only while that target has keyboard focus', but on native the target is collapsed into the Pressable and RN exposes no focus-visible distinction (only onFocus/onBlur, and only under react-native-web). I draw the ring on any Pressable focus, so a pointer-initiated focus would ring the card there — the web/Lit ':focus-visible only' rule has no native equivalent.
- Card: neutralising the target with `pointerEvents="none"` + `accessibilityElementsHidden` + `importantForAccessibility="no"` does not work under react-native-web — it drops both native-only props, so the child Link/Button stays a tab stop and a second target, breaking 'exactly one focus stop' in Storybook and the axe gate. I added `inert` on the neutralising wrapper (typed through a local widened-View alias). The doc prescribes only the native trio.
- Card: `transition` is overridable but documented to have no runtime effect on native. The override is therefore accepted and silently ignored; the doc does not say whether a dev warning is wanted for a caller who passes it.
- Card: the accessible name for a Link target is unspecified when that Link is `external`. I reproduced Link's own resolved name (`accessibilityLabel ?? label + copy.externalSuffix`), which required Link to export `LINK_EXTERNAL_SUFFIX` so the copy string is reused rather than rewritten — the doc should state that the card inherits the suffix.
- Card: `hoverBackground` with a disabled target — the prose says 'no hover background', but native shows this binding on press, and nothing says whether the pressed background is suppressed too. I suppressed both.
- Card: `focusable` needs `tabIndex`/`onFocus`/`onBlur` on a View, which RN 0.87's core types do not declare (react-native-web supports them). I widened View through a local prop alias rather than casting at each use; the doc concedes the feature is inert on iOS/Android but not that it is untyped.
- Card: `border` override on a subtle card is documented as a no-op, but an interactive or focusable subtle card still reserves a border colored `transparent` at rest. I kept the override a no-op there (transparent wins), which the doc implies but does not state.
- Card: behavior scenarios `interactive-adds-no-focus-stop`, `focusable-takes-scripted-focus-only` and `a-card-with-a-heading-is-an-article` are scoped to web/lit and have no RNTL expression (no tab order, no tabindex attribute, no article role in the native tree), so the rn test file has 12 tests, not 15.

## 2026-09-23 18:57 — round 1

- Card: the whole-card-is-a-link example gives `children` as a plain string but says to render a Link labelled with it; the story renders `<Link href="#" label="A Link to the invoice" />` by hand rather than with exactly its `given` args, so a generator has to guess the string-to-Link translation.
- Card: the spec doesn't say whether the interactive Pressable's aria-* mirrors (aria-label, aria-disabled) are expected, since the `rn.props` list only names accessibilityRole and accessibilityLabel; I mirrored them per the package convention.
- Card: the `heading` anatomy part has no testID of its own and the spec says it keeps Heading's own hook; header, headerActions, body and footer testIDs (`Card.header` etc.) are inferred from the anatomy-names rule, not stated for this component.
- Card: the `focusable` behavior scenarios (tabindex -1, focusable: true) are web/lit only, so nothing tests the rn `focusable` path (tabIndex -1, focus ring) in Jest.
