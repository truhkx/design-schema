# Gaps reported while generating BottomSheet for rn

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 03:01 — round 1

- BottomSheet: the drag gesture's threshold ("released past the threshold") and velocity aren't quantified in the spec; I chose 25% of measured sheet height (matching web's stated 25%) and a 1.5 px/ms release velocity, and used `Animated.decay` for the post-release motion since the spec's "continues at the drag velocity" has no native-specific guidance on the mechanism.
- BottomSheet: the RN notes say the ScrollView's scrollY-at-0 'hands the gesture to the pan responder,' implying the body itself can also initiate a drag-dismiss when scrolled to top. I implemented drag only on the header/handle (the PanResponder is not attached to the ScrollView) since arbitrating responder ownership between a ScrollView and a PanResponder without a new gesture dependency is unreliable; dragging must start on the header, not from within scrolled-to-top body content.
- BottomSheet: no `initialFocus` prop exists on this component (unlike Dialog), so focus placement on open relies entirely on FocusScope's default `autoFocus="first"`, which on native resolves to the scope's own wrapper (not a real first-focusable descendant) — the same acknowledged limit FocusScope documents for itself.
- BottomSheet: when rendered as `Dialog` above `maxWidth` (tablet/wide), `hideTitle` has no effect since Dialog always shows its title — the title is visually present in the wide presentation even when `hideTitle` is true, though it remains the accessible name either way.
- BottomSheet: `scroll-lock` (a11y.requires) has no native equivalent — there's no page scroll for a modal `Modal` to suppress — so it isn't implemented, matching Dialog's existing documented limitation.
- BottomSheet: `accessibilityRole="dialog"` (a11y.role) is not set explicitly on the surface, matching the existing Dialog/AlertDialog implementations in this package, which rely on `accessibilityViewIsModal` plus the native `Modal` instead — following established precedent rather than the schema's literal `role: dialog` value, since RN's `accessibilityRole` enum support for 'dialog' is inconsistent across versions.

## 2026-09-10 18:24 — round 1

- BottomSheet: the drag gesture's dismiss threshold ('released past the threshold') and velocity aren't quantified for native; I used 25% of measured sheet height (matching the web note's stated 25%) and 1.5 px/ms release velocity, with Animated.decay for the post-release motion since the spec's 'continues at the drag velocity' names no native mechanism.
- BottomSheet: the RN notes say the ScrollView's scrollY-at-0 'hands the gesture to the pan responder,' implying the body itself can start a drag-dismiss when scrolled to top. I attached the PanResponder only to the header/handle, not the ScrollView, since arbitrating responder ownership between a ScrollView and a PanResponder without a new gesture dependency is unreliable — dragging must start on the header.
- BottomSheet: there is no `initialFocus` prop on this component (unlike Dialog), so focus placement on open relies entirely on FocusScope's default `autoFocus="first"`, which on native resolves to the scope's own wrapper rather than a real first-focusable descendant — the same acknowledged limit FocusScope documents for itself.
- BottomSheet: `scroll-lock` (a11y.requires) has no native equivalent — there's no page scroll for a modal `Modal` to suppress — so it isn't implemented, matching Dialog's existing documented limitation.
- BottomSheet: `accessibilityRole="dialog"` (a11y.role) is not set explicitly on the surface, matching the existing Dialog/AlertDialog implementations in this package, which rely on `accessibilityViewIsModal` plus the native `Modal` instead of RN's inconsistently-supported 'dialog' accessibilityRole value.
- BottomSheet: renaming `title`/`hideTitle`/`draggable` to the current spec's `heading`/`hideHeading`/`dragToDismiss` breaks four existing callers outside this file's scope (Combobox.tsx, DataGrid.tsx, Select.tsx, TreeGrid.tsx) that still pass the old prop names — left unfixed as out of scope for a single-component regen; they need their own regen pass or a follow-up patch before `tsc` is clean package-wide.
- BottomSheet: the 5 behavior scenarios (renders x4, has-accessible-name) have no corresponding *.test.tsx in this package (unlike Dialog/AlertDialog/Menu) — no test file was in scope per the Output section, so scenario coverage is not yet automated for this component on RN.

## 2026-09-10 18:25 — round 2

- BottomSheet: the typecheck gate requires the whole `rn` package to compile, which meant fixing four consumers (Combobox.tsx, DataGrid.tsx, Select.tsx, TreeGrid.tsx) still passing the old `title` prop to BottomSheet — renamed each to `heading` to match this component's current spec; none of them used the old `hideTitle`/`draggable` names so no further changes were needed there.
- BottomSheet: the drag gesture's dismiss threshold and release velocity aren't quantified for native; used 25% of measured sheet height (matching the web note's stated 25%) and 1.5 px/ms, with Animated.decay for post-release motion since the spec names no native mechanism for 'continues at the drag velocity'.
- BottomSheet: the RN notes say the ScrollView's scrollY-at-0 'hands the gesture to the pan responder,' implying the body can also start a drag-dismiss when scrolled to top. PanResponder is attached only to the header/handle, not the ScrollView, since arbitrating responder ownership between the two without a new gesture dependency is unreliable.
- BottomSheet: there is no `initialFocus` prop (unlike Dialog), so focus placement on open relies entirely on FocusScope's default `autoFocus="first"`, which on native resolves to the scope's own wrapper rather than a real first-focusable descendant — the same acknowledged limit FocusScope documents for itself.
- BottomSheet: `scroll-lock` (a11y.requires) has no native equivalent — no page scroll for a modal `Modal` to suppress — so it isn't implemented, matching Dialog's existing documented limitation.
- BottomSheet: `accessibilityRole="dialog"` (a11y.role) is not set explicitly, matching the existing Dialog/AlertDialog implementations, which rely on `accessibilityViewIsModal` plus native `Modal` instead of RN's inconsistently-supported 'dialog' accessibilityRole value.
- BottomSheet: the 5 behavior scenarios (renders x4, has-accessible-name) have no corresponding *.test.tsx in this package (unlike Dialog/AlertDialog/Menu) — no test file was in scope per the Output section, so scenario coverage is not yet automated for this component on RN.

## 2026-09-16 07:15 — round 1

- BottomSheet: `open` is `required: true` but the Controlled state contract asks for an uncontrolled mode from a default when omitted — impossible with a required prop; kept it required and controlled-only, as Dialog does.
- BottomSheet: reason `action` ('a footer action asked to close') has no mechanism — footer is an opaque ReactNode and the schema declares no context or callback a footer Button could use; the type includes 'action' but the component never emits it.
- BottomSheet: `dismissible: false` — the schema says 'the close button and Escape always exist' (dragToDismiss) but also 'only the footer actions close it'; followed Dialog and did not render the close button, disabled the scrim and drag, and kept onRequestClose reporting 'escape'.
- BottomSheet: `maxWidth` is described as 'read once from the theme (the breakpoint is not per-instance overridable)' yet is listed as overridable; honored the override.
- BottomSheet: the Guidance says only `inset`, `radius`, `partGap`, `footerGap` are forwarded to Dialog's overrides above the breakpoint, but `scrim`, `shadow`, `layer`, `enter`, `exit` also match Dialog bindings by name; forwarded all nine shared names and dropped handleHeight/handleWidth/maxWidth.
- BottomSheet: 'the always-present wrapper carries data-ds=BottomSheet in both presentations' conflicts with `closed-sheet-renders-nothing`, and on RN a wrapper View around a Modal would take layout space in the caller's tree; the wide presentation renders Dialog alone (its root testID is `Dialog`, scrim `Dialog.scrim`), so the scenarios only hold below maxWidth. The test sets a phone-sized window because Jest's default 750-wide window is above layout.maxWidth.prose.
- BottomSheet: header spacing is unbound — no binding names the header's top padding, the handle-to-heading gap or the heading-to-close-button gap; used t.spaceSm, t.layoutGapTight and t.layoutGapNormal (Dialog's headerGap default).
- BottomSheet: no spec for the release animation when a drag is below threshold (spring back) — schema says 'springs back' but forbids momentum physics and names no token; used a timing animation with `exit` duration and motion.easing.standard, instant under reduced motion.
- BottomSheet: a drag dismiss holds the release position so the exit plays 'from wherever the finger left', but if the consumer keeps `open` true the spec doesn't say what happens; the sheet springs back after the update renders.
- BottomSheet: the drag start threshold (px moved before the header claims the responder so taps on the close button still work) is not in the schema; used 4 px with literal-ok.
- BottomSheet: `minTarget` (size.target.comfortable on closeButton) cannot be applied without restyling the child and Button has no override for it; relies on Button's own hitSlop, which already extends to sizeTargetComfortable.
- BottomSheet: `layer` (layer.sheet) has little meaning inside a native Modal, which is its own window; applied as zIndex on the anchor view.
- BottomSheet: Guidance says drag begins 'when the body is at its scroll top' but the RN note says the PanResponder lives only on the header/handle so no arbitration is needed; followed the RN note (header-only, no scroll-top check).
- BottomSheet: safe-area bottom inset — SafeAreaView from react-native is iOS-only and the no-dependency rule rules out react-native-safe-area-context; Android gets no bottom inset.
- BottomSheet: example givens are prose descriptions ('A Form of filter controls', 'Clear and Apply Buttons'); RN cannot render bare strings in a View, so stories realize them as real Inputs/Buttons/Links rather than the literal strings.

## 2026-09-17 11:01 — round 1

- BottomSheet: `inset` is described as inline padding of header/body/footer plus block-end padding of the last part, but the composed body is a Box whose only symmetric option is `inset="lg"` (padding on all four sides), so the body also gets block-start padding on top of `partGap`; chose Dialog's existing Box inset="lg" composition for parity and forwarded the `inset` override to Box paddingBlock/paddingInline.
- BottomSheet: the header's block-end padding is not specified (only top via headerPaddingTop/inset and inline via inset); chose none, relying on partGap.
- BottomSheet: `SafeAreaView` is named as the inset source but is deprecated in core RN since 0.81 and pads every unsafe edge it intersects, not only the bottom; kept it, wrapping the last part (footer, or the body when there is no footer).
- BottomSheet: dragSlop is claimed via onMoveShouldSetPanResponderCapture so a drag that starts on the heading or close button still works past the slop; the spec says the header claims the move but not whether it may take it over from a child Pressable.
- BottomSheet: dismissVelocity sample timing uses `nativeEvent.timestamp`; the spec says 'between the last two move samples' without naming a clock.
- BottomSheet: `full` height is the window height minus `layout.gutter` and `half` is window height × 0.5 — the spec's web notes give 100dvh - gutter and 50dvh, and the rn notes only say 'a fraction of useWindowDimensions().height'; with statusBarTranslucent the full sheet can reach under the status bar when the gutter is smaller than it.
- BottomSheet: the heading row's alignment when hideHeading is true (close button alone) is unspecified; chose flex-end.
- BottomSheet: the spring-back after a below-threshold release, and after a drag dismiss the consumer did not honor, animates only the drag offset; the spec does not say whether a spring-back should also reset an interrupted enter animation.
- BottomSheet: the `Keyboard` story's Tab/Shift+Tab wrap is FocusScope's on react-native-web; native has no Tab model, so the keyboard rules other than Escape (onRequestClose / onAccessibilityEscape) cannot be expressed here and have no rn test.

## 2026-09-21 09:09 — round 1

- BottomSheet: composition names `heading: Heading` but no heading level, and the doc says the level/id wiring is 'wiring every platform passes'. Chose `level="2"` to match Dialog; the doc should state the level so the three platforms cannot drift.
- BottomSheet: `inset` says the block edges are padded once on 'the column that holds header, body and footer (the surface)', and the rn notes say the safe-area SafeAreaView is 'the last child of the surface column' whose 'only edge it adds is its bottom-edge padding as height' — but that column's gap is `partGap`, which puts a `partGap` above the empty SafeAreaView. The doc does not say which wins. Chose `marginTop: -partGap` on the SafeAreaView to cancel it, so bottom clearance is exactly inset + safe-area inset.
- BottomSheet: `minTarget` says the closeButton wrapper's extra area activates the Button via 'the Button's hitSlop covering the extra area', but rn Button takes no `hitSlop` prop — it computes its own to `size.target.comfortable` internally and nothing can be passed in. The wrapper is sized to the token and Button's own slop is what covers it; the doc should either say that or Button's schema needs a way to accept the wrapper's dimensions.
- BottomSheet: `dismissVelocity` prescribes measuring between the last two move samples via `nativeEvent.timestamp`, but RN's PanResponder already supplies `gestureState.vy` in px/ms, which is what an RN author would reach for and which is smoothed differently. Followed the doc's sampling literally; the doc should say whether `vy` is an acceptable rn source, since the two disagree on a flick that ends with one slow sample.
- BottomSheet: `dragSlop` and `handleHeight` are both `space.1`, but only `handleHeight` is overridable. The doc does not say whether overriding `handleHeight` should move the slop. Chose to read `t.space1` for the slop regardless, treating it as the constant it is declared to be.
- BottomSheet: the footer is 'pinned to the bottom of the sheet' but with `height: content` the column has no free space to pin against. Chose `flexGrow: 0` on the body for `content` and `flexGrow: 1` for `half`/`full`, so the footer only pins where a fixed height creates slack; the doc should say what pinning means at content height.
- BottomSheet: anatomy lists `surface` and `focusScope` as parts, and the convention gives every part `testID="BottomSheet.<part>"`, but the rn notes name only `BottomSheet.closeButton` and the root `testID` convention wants `BottomSheet` on the root. Chose the Dialog precedent — the surface carries the root `testID="BottomSheet"` and nothing carries `BottomSheet.surface`; `focusScope` gets none, since FocusScope owns its wrapper. Ambiguous for any test that looks for the surface part by name.
- BottomSheet: the `keyboard` block's Tab / Shift+Tab wrap rules have no rn implementation path — they are delegated wholesale to FocusScope `trapped`, which on native cannot walk descendants, so the rules are only really exercised under react-native-web. The doc marks no keyboard rule as `native`, so it reads as if rn implements them.

## 2026-09-23 14:18 — round 1

- BottomSheet: web says the body Box takes its part hook directly, but rn Box accepts no testID (it always writes testID="Box"), and partGap says the scroll element has no hook. I put testID="BottomSheet.body" on a sheet-owned View between the ScrollView and the Box; the doc should name that wrapper on rn the way it names the heading/closeButton/footer wrappers.
- BottomSheet: 'a spring-back also finishes an interrupted enter animation' does not say whether a drag that starts mid-enter pauses the enter. I stop the enter (progress.stopAnimation) when the drag is granted and have the spring-back animate it to 1 over `exit` with motion.easing.standard; a dismissing release leaves it paused until the consumer's render decides.
- BottomSheet: the racing case (a dismissing release after `open` already went false) says 'is a spring-back', but the exit is already playing then. I return only the drag offset to 0 and leave the exit fade/slide running, rather than animating the sheet back to fully open.
- BottomSheet: nothing covers a reopen while a held drag offset is still exiting. The enter animation brings the drag offset back to 0 alongside progress, so the surface does not rise with a stale offset.
- BottomSheet: the example givens are prose ('A Form of filter controls', 'Clear and Apply Buttons'), not renderable args. The stories render stand-in content (Inputs, Buttons, Links) matching each description.
- BottomSheet: the minTarget wrapper relies on Button's internally computed hitSlop to cover the wrapper's extra area. That is taken from the doc and not verified against Button.tsx here; if the sm icon-only Button's hitSlop is smaller than the 44-unit wrapper, a tap on the wrapper edge activates nothing.
- BottomSheet: the velocity sample rule does not say whether the move that crossed dragSlop (the grant) counts as a sample. The grant only records the origin; samples start with the first onPanResponderMove, so a one-move flick has velocity 0 and distance alone decides.
- BottomSheet: 'The body scrolls within the sheet' plus Dialog's KeyboardAvoidingView — the sheet doc does not say whether the sheet should avoid the soft keyboard (a Filters form with Inputs). Not added, since it is not in the spec; a focused Input near the bottom can be covered on iOS.

## 2026-09-23 14:18 — round 1

- BottomSheet: the job wrote no files — another process was editing packages/rn/src/BottomSheet.tsx and BottomSheet.stories.tsx during the run (last writes 18:17:58Z and 18:18:04Z), so overwriting them would have clobbered its work.
- BottomSheet: the body part is a Box, but rn Box always writes testID="Box" and the spec says the scroll element has no part hook, so there is nowhere to put testID="BottomSheet.body" without an extra wrapper View (the on-disk edit adds one); the doc should say which element carries it on rn.
- BottomSheet: the spec does not say whether the rn sheet avoids the on-screen keyboard; Dialog wraps its surface in KeyboardAvoidingView (padding on iOS), and without it a Filters or New expense form in a sheet at the bottom is covered by the keyboard. I would have added it to match Dialog.
- BottomSheet: dismissVelocity says to measure between the last two move samples but not whether the sample is the raw gesture dy or the offset from where the slop was crossed, clamped at 0; I chose raw dy, since the clamp can zero out a real downward flick.
- BottomSheet: 'a spring-back also finishes an interrupted enter animation' does not say what happens when the release comes after open went false (the racing close); the on-disk edit returns only the drag offset and lets the exit keep playing, which seems right but is not stated.
- BottomSheet: the non-dismissible-still-reports-escape scenario is web/lit only, but rn does report Escape (onRequestClose, onAccessibilityEscape) when not dismissible; RNTL 13 cannot fire a Modal's onRequestClose directly, which is presumably why rn is excluded — the doc could say so.

## 2026-09-23 19:10 — round 1

- BottomSheet: the spec says the body is a Box inside an unnamed scroll element with the body part hook, but Box writes its own testID on rn; I put `testID="BottomSheet.body"` on a wrapper View inside the ScrollView. The doc should say which element owns the rn body testID.
- BottomSheet: the spec does not say where `BottomSheet.header`, `BottomSheet.heading`, `BottomSheet.handle` and `BottomSheet.scrim` testIDs go on rn; I put them on the header View, a wrapper View around Heading, the handle View and the scrim Pressable.
- BottomSheet: `drag` and the derived scenarios cannot be exercised in Jest, because PanResponder gestures are not driven by testing-library; the spec says nothing about how to test the drag on rn, so no drag test exists.
- BottomSheet: the spec gives no rn-specific reduced-motion behaviour for the drag release beyond 'instant'; I set the offset to 0 without animation.
