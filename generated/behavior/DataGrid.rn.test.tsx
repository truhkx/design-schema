// Generated by tools/behavior_tests.ts from the `behavior` block of the DataGrid doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { DataGrid } from '../../packages/rn/src/DataGrid';
import type { DataGridProps } from '../../packages/rn/src/DataGrid';
import meta from '../../packages/rn/src/DataGrid.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<DataGridProps> = {}) {
  const events = {
    onSortChange: jest.fn(),
    onSelectionChange: jest.fn(),
    onCellChange: jest.fn(),
    onEditStart: jest.fn(),
    onRangeNeeded: jest.fn(),
    onColumnResize: jest.fn(),
  };
  const props: DataGridProps = { ...(meta.args as DataGridProps), ...given, onSortChange: events.onSortChange, onSelectionChange: events.onSelectionChange, onCellChange: events.onCellChange, onEditStart: events.onEditStart, onEndReached: events.onRangeNeeded, onColumnResize: events.onColumnResize };
  const tree = (p: DataGridProps) => (
    <ThemeProvider mode="light">
      <DataGrid {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('DataGrid') ?? screen.UNSAFE_root,
    container: () => screen.queryByRole('grid') ?? s.root(),
    rerender: (next: Partial<DataGridProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('DataGrid', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-none', () => {
    const s = setup({"selectable": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-row', () => {
    const s = setup({"selectable": "row"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-cell', () => {
    const s = setup({"selectable": "cell"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-selectable-range', () => {
    const s = setup({"selectable": "range"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-density-compact', () => {
    const s = setup({"density": "compact"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-density-comfortable', () => {
    const s = setup({"density": "comfortable"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-height-content', () => {
    const s = setup({"height": "content"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-height-viewport', () => {
    const s = setup({"height": "viewport"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-height-fixed', () => {
    const s = setup({"height": "fixed"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('grid', { name: s.props.caption })).toBeOnTheScreen();
  });
});
