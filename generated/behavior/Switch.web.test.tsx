// Generated by tools/behavior_tests.ts from the `behavior` block of the Switch doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Switch } from '../../packages/react/src/Switch';
import type { SwitchProps } from '../../packages/react/src/Switch';
import meta from '../../packages/react/src/Switch.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

const FOCUSABLE = 'input, button, select, textarea, a[href], [tabindex]';
/** Focus the element a key press lands on. `when.key` is pressed on the primary part, but a composite that
 *  manages a roving tabindex (a tablist, a radiogroup, a toolbar) is not focusable itself: .focus() on it is a
 *  no-op and the key would go to document.body instead of the component. Focus what it delegates to — the
 *  element carrying tabindex="0", else the first focusable descendant. An already-focusable part focuses itself. */
function focusInto(el: Element | null): void {
  if (el === null) return;
  const target = el.matches(FOCUSABLE) ? el : (el.querySelector('[tabindex="0"]') ?? el.querySelector(FOCUSABLE) ?? el);
  (target as HTMLElement).focus();
}

function setup(given: Partial<SwitchProps> = {}) {
  const events = {
    onChange: vi.fn(),
  };
  const props = { ...meta.args, ...given, onChange: events.onChange };
  const utils = render(<Switch {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="Switch"]') ?? screen.queryByRole('switch') ?? utils.container.firstElementChild) as HTMLElement,
    track: () => (screen.queryByRole('switch') ?? s.root()) as HTMLElement,
    label: () => screen.getByText(props.label),
    description: () => screen.getByText(props.description),
    rerender: (next: Partial<SwitchProps>) => utils.rerender(<Switch {...props} {...next} />),
  };
  return s;
}

describe('Switch', () => {
  test('click-on-track-toggles-on', async () => {
    const s = setup({});
    await s.user.click(s.track());
    expect(s.events.onChange).toHaveBeenCalledWith(true, expect.anything());
    expect(s.track()).toBeChecked();
  });
  test('click-on-label-toggles', async () => {
    const s = setup({});
    await s.user.click(s.label());
    expect(s.events.onChange).toHaveBeenCalledWith(true, expect.anything());
    expect(s.track()).toBeChecked();
  });
  test('click-on-description-toggles', async () => {
    const s = setup({"description": "Sends a daily summary at 9:00."});
    await s.user.click(s.description());
    expect(s.events.onChange).toHaveBeenCalledWith(true, expect.anything());
    expect(s.track()).toBeChecked();
  });
  test('space-toggles', async () => {
    const s = setup({});
    act(() => focusInto(s.track()));
    await s.user.keyboard('[Space]');
    expect(s.events.onChange).toHaveBeenCalledWith(true, expect.anything());
    expect(s.track()).toBeChecked();
  });
  test('enter-is-ignored', async () => {
    const s = setup({});
    act(() => focusInto(s.track()));
    await s.user.keyboard('{Enter}');
    expect(s.events.onChange).not.toHaveBeenCalled();
    expect(s.track()).not.toBeChecked();
  });
  test('toggles-back-off', async () => {
    const s = setup({"defaultChecked": true});
    await s.user.click(s.track());
    expect(s.events.onChange).toHaveBeenCalledWith(false, expect.anything());
    expect(s.track()).not.toBeChecked();
  });
  test('disabled-does-not-toggle', async () => {
    const s = setup({"disabled": true});
    await s.user.click(s.track());
    expect(s.events.onChange).not.toHaveBeenCalled();
    expect(s.track()).not.toBeChecked();
    expect(s.track()).toHaveAttribute('aria-disabled', 'true');
  });
  test('disabled-stays-focusable', async () => {
    const s = setup({"disabled": true});
    act(() => (s.track()).focus());
    expect(s.track()).toHaveFocus();
  });
  test('controlled-follows-prop', async () => {
    const s = setup({"checked": false});
    await s.user.click(s.track());
    expect(s.events.onChange).toHaveBeenCalledWith(true, expect.anything());
    expect(s.track()).not.toBeChecked();
  });
  test('controlled-updates-on-set', async () => {
    const s = setup({"checked": false});
    s.rerender({"checked": true});
    expect(s.track()).toBeChecked();
  });
  test('description-is-rendered', async () => {
    const s = setup({"description": "Sends a daily summary at 9:00."});
    expect(screen.getByText(new RegExp("Sends\\ a\\ daily\\ summary\\ at\\ 9:00\\."))).toBeInTheDocument();
  });
  test('label-at-the-end-still-toggles-the-row', async () => {
    const s = setup({"labelPosition": "end"});
    await s.user.click(s.label());
    expect(s.events.onChange).toHaveBeenCalledWith(true, expect.anything());
    expect(s.track()).toBeChecked();
  });
  test('renders', async () => {
    const s = setup({});
    expect(s.root()).not.toBeNull();
  });
  test('renders-label-position-start', async () => {
    const s = setup({"labelPosition": "start"});
    expect(s.root()).not.toBeNull();
  });
  test('renders-label-position-end', async () => {
    const s = setup({"labelPosition": "end"});
    expect(s.root()).not.toBeNull();
  });
  test('has-accessible-name', async () => {
    const s = setup({});
    expect(screen.getByRole('switch', { name: s.props.label })).toBeInTheDocument();
  });
  test('control-is-focusable', async () => {
    const s = setup({});
    act(() => (s.track()).focus());
    expect(s.track()).toHaveFocus();
  });
});
