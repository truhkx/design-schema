Extend tools/lint_literals.ts with a Swift matcher: bare numeric literals inside `.padding(`, `.frame(`, `.cornerRadius(`, `.lineSpacing(`, `.font(.system(size:`, `Color(`, `.opacity(`, `.animation(`, `.zIndex(` in packages/swiftui/Sources/DesignSchema/**.swift are violations unless the line carries `// literal-ok: reason`; `0`, `1`, `2` and fractions (0.5) are allowed as on the other platforms. Run it from `pnpm lint:literals`. Gate: a fixture file with `.padding(12)` fails and with `.padding(theme.spaceMd)` passes. Do not modify packages/*/src or generated/.
