// Generated by tools/behavior_tests.ts from the `behavior` block of the SidePanel doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { SidePanel } from '../../packages/rn/src/SidePanel';
import type { SidePanelProps } from '../../packages/rn/src/SidePanel';
import meta from '../../packages/rn/src/SidePanel.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<SidePanelProps> = {}) {
  const events = {
    onOpenChange: jest.fn(),
  };
  const props: SidePanelProps = { ...(meta.args as SidePanelProps), ...given, onOpenChange: events.onOpenChange };
  const tree = (p: SidePanelProps) => (
    <ThemeProvider mode="light">
      <SidePanel {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('SidePanel') ?? screen.UNSAFE_root,
    trigger: () => screen.queryByRole('complementary') ?? s.root(),
    scrim: () => screen.queryByTestId('SidePanel.scrim') ?? s.root(),
    closeButton: () => screen.queryByTestId('SidePanel.closeButton') ?? s.root(),
    rerender: (next: Partial<SidePanelProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('SidePanel', () => {
  test('close-button-fires-on-open-change', () => {
    const s = setup({"open": true});
    fireEvent.press(s.closeButton());
    expect(s.events.onOpenChange).toHaveBeenCalled();
  });
  test('the-close-button-works-without-the-swipe', () => {
    const s = setup({"open": true, "swipeable": false});
    fireEvent.press(s.closeButton());
    expect(s.events.onOpenChange).toHaveBeenCalled();
  });
  test('non-dismissible-scrim-tap-does-nothing', () => {
    const s = setup({"open": true, "dismissible": false});
    fireEvent.press(s.scrim());
    expect(s.events.onOpenChange).not.toHaveBeenCalled();
  });
  test('the-heading-is-rendered', () => {
    const s = setup({"open": true, "heading": "Your cart"});
    expect(screen.getByText(new RegExp("Your\\ cart"))).toBeOnTheScreen();
  });
  test('renders', () => {
    const s = setup({"open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-side-start', () => {
    const s = setup({"side": "start", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-side-end', () => {
    const s = setup({"side": "end", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-width-narrow', () => {
    const s = setup({"width": "narrow", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-width-default', () => {
    const s = setup({"width": "default", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-width-wide', () => {
    const s = setup({"width": "wide", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-persistent-never', () => {
    const s = setup({"persistent": "never", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-persistent-content', () => {
    const s = setup({"persistent": "content", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-persistent-page', () => {
    const s = setup({"persistent": "page", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-role-complementary', () => {
    const s = setup({"role": "complementary", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-role-navigation', () => {
    const s = setup({"role": "navigation", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({"open": true});
    expect(screen.getByRole('complementary')).toBeOnTheScreen();
  });
});
