# Gaps reported while generating Form for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 01:24 — round 1

- Form: anatomy lists separate 'fields' and 'actions' parts, but `children` is a single content prop with no dedicated actions slot — implemented one `part="fields"` slot covering both fields and actions; there is no distinct DOM node for 'actions'.
- Form: the DsFormField interface (per doc) declares `error`/`validationMessage` as required strings, but the already-existing `ds-switch` (a field type Form must collect) has neither property since a switch never validates — kept the interface as documented and read both defensively (`?? ''`) at runtime via `as unknown as DsFormField` casts, since Switch can never satisfy the interface structurally.
- Form: 'blur' validation for ds-radio-group ('validates when focus leaves the whole group') isn't spelled out mechanically — implemented by comparing a focusout event's `relatedTarget === target`, since composed-event retargeting collapses both to the `ds-radio-group` host when focus moves between its own radios, but stays distinct when focus truly leaves the group; this is my interpretation, not something stated in the docs.
- Form: nothing requires a consumer's field to carry an `id`, but the error summary must link to one — used `name` as documented ('the base of generated ids') to auto-assign `${idBase}-${field.name}` to any field missing an id at submit time.
- Form: `labelledBy` (aria-labelledby across the shadow boundary) is wired through the newer `ElementInternals.ariaLabelledByElements`, feature-detected exactly like `Card.ts` already does — Chromium only today; there is no fallback for engines lacking it, so `labelledBy` currently has no effect there (same pre-existing gap as Card).
- Form: the 'control-is-focusable' scenario (derived, lit/web) doesn't fit cleanly — Form is a container of externally-slotted fields, not a single control, so its own shadow root has no focusable element unless the error summary is showing; relied on the browser's `delegatesFocus` fallback (focuses the host itself when no inner focusable target exists) to satisfy `el.focus()` moving `document.activeElement`.

## 2026-09-10 17:25 — round 1

- Form: schema requires `actions` as a separate content prop with its own named slot and `actions` anatomy part, distinct from `children`/`fields` — the pre-existing Form.ts only had one default slot mixing fields and the button row. Added a named `actions` slot (`<slot name="actions" part="actions">`) and updated Form.stories.ts to slot the action row separately; the flex-column `gap` on the native `<form>` already applies uniformly between the fields slot and the actions slot, matching the styles.gap description ('between fields and between fields and actions').
- Form (lit platform notes) is self-contradictory about field discovery: it first states 'Form discovers fields by the data-ds-field attribute ... never by a tag list' (matching the package conventions doc's 'Form discovers fields by that attribute, never by a tag or component list'), then in the same paragraph says 'Instead it collects light-DOM fields by tag (ds-input, ds-checkbox, ds-switch, ds-radio-group)'. Kept the existing tag-based querySelectorAll implementation since the actual sibling field components (e.g. Input.ts) only set `data-ds` (component name), not `data-ds-field` — switching to attribute-based discovery would silently stop collecting every existing field until Input/Checkbox/Switch/RadioGroup are also updated, which is out of scope for a Form-only change.
- index.ts was missing the `FormOverridableBinding` type export (every sibling component exports its `<Name>OverridableBinding` type) even though Form.ts already defined and used it — added it to the type export list.

## 2026-09-16 04:15 — round 1

- Form: the lit notes require discovery by `data-ds-field` only, but in the Lit package only Input.ts sets that attribute; Checkbox, Switch, RadioGroup, Select, NumberInput and DatePicker do not, so a ds-form no longer collects them until they are regenerated with it. I followed the spec (attribute only) instead of keeping the old tag list.
- Form: `form.discovery: context` has no Lit counterpart; the lit notes describe attribute discovery over the light-DOM subtree, which is what I implemented.
- Form: the guidance says Checkbox/Switch validate on change under `blur`, but attribute-only discovery gives no way to tell a field's kind. I detect it from the event's origin (a native input[type=checkbox] or an element with role=switch); the docs should say how a field declares that it has no blur moment (e.g. a `data-ds-field="change"` value).
- Form: the lit notes say Enter submits 'in a ds-input', while the Lit guidance says 'on Enter in any field'. I chose any `data-ds-field` field, skipping a textarea, a native button or a link as the event's origin, since Enter should not submit from a multi-line field.
- Form: the notes say `disabled` goes to 'the fields it found', while the `submitting` example says 'every field and action disabled'. I disable found fields plus `ds-button` descendants (found by tag, since actions have no discovery attribute).
- Form: `copy.invalidSummary` ('This form has errors.') has no stated use on web or Lit (only SwiftUI announces it); the role=alert summary already announces the heading, so it is unused on Lit.
- Form: `copy.summaryHeadingOne` duplicates the `one` form of `copy.summaryHeading.plural`; I used it as that `one` form. The plural locale is not specified; I use the nearest [lang] ancestor, falling back to navigator.language.
- Form: `errorSummary` defaults true, so per convention its attribute is the negated `no-error-summary`; the doc names no attribute, and this breaks the old `error-summary` attribute.
- Form: the summary item is described as 'a link' but Link is not in Related. I composed ds-link with tone=inherit so the locked danger text color reaches it, and preventDefault its click to focus the field instead of changing the URL hash.
- Form: `DsFormField.currentValue` stays `string | boolean | null`, as the submit payload is `Record<string, string | boolean>`, but the Lit guidance widens the field value contract to `string | number | boolean | string[] | [number, number]`; the event payload and that contract disagree.
- Form: the four examples give `children`/`actions` as prose ('An email Input and a password Input'), which cannot be story args; I passed the scalar givens as args and built the described content in each story's render.
- Form: the behavior scenario `label-names-the-form-landmark` is web-only, so there is no Lit test for it, though the host does carry role=form and aria-label.
- Form: the error summary's internal padding, radius, border width and item spacing have no style bindings; I used space.md padding, radius.md, border-width.thin, layout.gap.tight between items and space.sm/space.lg list padding.

## 2026-09-17 04:31 — round 1

- Form: the package digest types `DsFormField.currentValue` as `string | boolean | null`, but the doc's Lit notes say it uses the full onSubmit value contract; I widened it to `string | number | boolean | string[] | [number, number] | null` (null = contributes no key). The digest should be updated.
- Form: 'an empty field contributes no key' does not define empty for a field; Form omits `null` and `''` but not an empty array, so an empty multi-select Listbox would submit `[]`. Chose field-reported null plus the empty string.
- Form: `errorSummaryGap` is 'gap between the heading and the list and between list items' with the list as a Stack, but Stack's gap is an enum and its override lives in Stack; I forwarded it by setting Stack's `--ds-stack-gap` hook from `--ds-form-error-summary-gap` on the composed `ds-stack`s. The doc should say which forwarding mechanism is sanctioned (the child's `overrides` property or its hook).
- Form: the summary's outer arrangement (heading + list) is not named as a part or element; I used a `<ds-stack gap=tight>` inside the `errorSummary` div.
- Form: `role="alert"`, `tabindex="-1"` and the Link `href` for summary items are given only in the Web notes; Lit reuses them (`href="#<field id>"`, with the click default prevented so the hash never changes). If there is no field id, the href is `#`.
- Form: in `validate: change` mode the doc does not say whether fields also validate on blur; I validate only on change (plus blur and change after a failed submission).
- Form: the doc gives no text for an item whose field has an empty message AND an empty `label`; I fall back to the field `name`.
- Form: the behavior scenario `label-names-the-form-landmark` is checked through host `role`/`aria-label` attributes rather than a computed accessible name; the lit tests have no accessible-name helper.
- Form: example `long-form-validated-on-blur` names the fields but not input types (phone/email); stories use the default Input type for them, following the `given` literally.

## 2026-09-18 22:16 — round 1

- Form: the generic Overrides section says every overridable binding becomes a --ds-form-* hook and lists errorSummaryGap as overridable, but the errorSummaryGap binding says Form forwards it to both summary Stacks' `overrides.gap` and has no hook. I followed the binding: it stays in FormOverridableBinding but has no hook and is only forwarded.
- Form: the Lit event names `submit` and `invalid` are native event names, which conflicts with the rule against dispatching a CustomEvent under a native name. I kept them because platforms.lit names them. The shadow <form>'s native submit is non-composed and preventDefault'd, and a field's native `invalid` (from ElementInternals.checkValidity) does not bubble, so neither reaches a listener on ds-form. A consumer listening in the capture phase on a field's ancestor can still see a native `invalid`.
- Form: the doc doesn't say whether the summary returns when blur or change validation finds an error after a successful submission. I reset the failed-submission state on success, so the summary stays hidden until the next failed submit, and `validate: submit` stops re-validating on blur and change.
- Form: the doc doesn't say whether Form or the field makes a field show its own error when blur or change validation runs. Form calls field.checkValidity() and relies on the field to render its error.
- Form: a single bare action in the flex-column container would stretch to full width. The doc says a single action renders bare but gives no alignment. I made the actions slot a start-aligned flex row, which is a layout choice the doc doesn't specify.
- Form: the scenario `label-names-the-form-landmark` expects role form with name 'Sign in'. Role and aria-label are plain attributes on the host; the <form> in the shadow root carries no accessible name. The doc doesn't say which element is the landmark on Lit, so the shadow <form> is also exposed as an unnamed form in the accessibility tree.
- Form: the sign-in examples say 'in a Stack' but give no gap for it. I used gap=normal for the field Stack in the stories.

## 2026-09-18 22:39 — round 2

- Form: the axe gate can't pass for any Lit component. tests/gates/axe.spec.ts checks every Lit story in light and dark, one at a time (about 840 stories x 2 modes x ~1.3 s, roughly 36 minutes), against a 900 s limit. Form's 10 stories were axe-clean in both modes when run alone, and so was the failed-submit error-summary state. No Form change can make the gate pass.
- Form: the keyboard-run gate fails on other components' specs (Tabs, SegmentedControl, Tree, Toast, Select, Combobox, Menu, Dialog). Run one at a time, the same 20 tests fail every time, so this isn't load flakiness. Form has no keyboard block and no keyboard spec. I left those components alone as out of scope for this Form job.
- Form (carried from round 1): the generic Overrides section says errorSummaryGap is a --ds-form-* hook, but the binding says it is forwarded to the summary Stacks' `overrides.gap` with no hook. I followed the binding.
- Form (carried from round 1): the Lit events `submit` and `invalid` share names with native events, which conflicts with the rule against using native event names. I kept them because platforms.lit names them.
- Form (carried from round 1): the doc doesn't say whether the error summary comes back when blur or change validation finds an error after a successful submission. I reset the failed-submission state on success, so it doesn't.
- Form (carried from round 1): a single bare action would stretch to full width in the flex column, and the doc gives no alignment. I made the actions slot a start-aligned flex row.

## 2026-09-18 22:56 — round 3

- Form: the axe gate fails only on other components' stories (DataGrid, Feed, Listbox, NumberInput, Select, Slider, Tabs, TreeGrid). logs/playwright.json has no Form/Lit, Input/Lit, Button/Lit, Link/Lit, Stack/Lit, Text/Lit or demo entries, and Form's 10 stories passed axe in both modes, including the failed-submit summary state. The gate covers the whole Storybook, so a Form job can't turn it green.
- Form: the keyboard-run gate fails only in specs for other components (Tabs, SegmentedControl, Tree, Toast, Select, Combobox, Menu, Dialog, DatePicker, Feed, Listbox, Popover, Search, SidePanel, Slider, Stepper, Toolbar, Tooltip). Run one at a time, they fail every time. Form has no keyboard block and no spec.
- Form (carried): the generic Overrides section says errorSummaryGap is a --ds-form-* hook, but the binding says it is forwarded to the summary Stacks' `overrides.gap` with no hook. I followed the binding.
- Form (carried): the Lit events `submit` and `invalid` share names with native events, which conflicts with the rule against using native event names. I kept them because platforms.lit names them.
- Form (carried): the doc doesn't say whether the error summary comes back when blur or change validation finds an error after a successful submission. I reset the failed-submission state on success, so it doesn't.
- Form (carried): a single bare action would stretch to full width in the flex column, and the doc gives no alignment. I made the actions slot a start-aligned flex row.

## 2026-09-19 12:36 — round 1

- Form: errorSummaryBorder is `locked: false` in styles and listed as Overridable, but platforms.web.notes calls it one of 'the locked summary bindings (errorSummaryText, errorSummaryBackground, errorSummaryBorder)' with no --ds-form-* hook. Chose the styles block: --ds-form-error-summary-border exists and is overridable.
- Form: an error-summary entry whose field has unregistered still needs the label fallback (field message empty → label → name), but the unregistered field can no longer be asked for its label. Chose to remember each errored field's label when it is recorded (at submit and at later blur/change validation).
- Form: 'the locale of the nearest `lang` ancestor' does not say whether that lookup crosses shadow roots. Lit uses this.closest('[lang]'), which stops at the host's shadow root, then falls back to the runtime default.
- Form: the FailedSubmit story 'submits the sign-in example empty in its play function', but on Lit the shadow <form> owns no fields, so requestSubmit() does nothing. Lit's play function calls the element's public submit() instead; the doc should say how Lit triggers it (submit(), or a press on the ds-button).
- Form: the prop is `labelledBy`, but the Lit attribute is not stated. Lit's default is the lowercased `labelledby`, and the stories use that; the doc could name the attribute (e.g. `labelled-by`) explicitly.
- Form: behavior scenario label-names-the-form-landmark checks role and name through the host's plain role/aria-label attributes (as the conventions require). The derived 'renders' scenarios only check that the container part exists, because the doc gives no observable beyond 'renders: true'.
- Form: the doc does not say whether ds-form should stop the submit ds-button's `press` event once it has handled it (the conventions stop a child's press only when the composite dispatches its own event). Chose to let `press` keep bubbling, since Form dispatches `submit`/`invalid`, not a press.

## 2026-09-19 12:44 — round 2

- Form: both rejecting gates cover the whole Lit Storybook, and neither names Form. keyboard-run fails only on other components' specs (Form has no keyboard block, so there is no Form keyboard spec). The axe output shown names only Tabs (dark color-contrast) and TreeGrid (aria-hidden-focus, target-size); no logs/playwright*.json exists for this run, so the full list could not be searched for Form. A Form-only axe run (logs/form-axe.config.ts) passes all 10 Form/Lit stories in light and dark. That run did not confirm that FailedSubmit's play function rendered the error summary before axe checked. No Form code was changed; the gates need a per-component scope, or the other components need fixing, before a Form job can go green.

## 2026-09-19 12:52 — round 3

- Form: the whole-Storybook axe and keyboard gates fail only on other components, so a Form job cannot pass them. The complete axe list (test-results/*axe-lit/error-context.md, 168 failures) names Carousel, DataGrid, Feed, Listbox, NumberInput, Select, Slider, Splitter, Tabs and TreeGrid, never Form. Every keyboard-run failure is in another component's spec (Form declares no keyboard block, so it has no spec). The last Form-only axe run (round 2, logs/form-axe.config.ts) passed all 10 Form/Lit stories in light and dark, and Form code has not changed since. Nothing was changed; the gates need a per-component scope, or those components need fixing separately.

## 2026-09-20 06:30 — round 1

- Form: the Lit contract says ds-form 'calls the field's checkValidity(), and the field renders its own error', but checkValidity() on DsInput/DsCheckbox/DsRadioGroup only syncs ElementInternals and never sets `invalid`, so nothing renders. Every field's own doc says `invalid` is 'usually set by the Form', yet the doc's enumerated DsFormField members (name, label, required, disabled, error, currentValue, id, focus(), checkValidity(), validationMessage) omit `invalid`. I added `invalid?: boolean | undefined` as an optional interface member and Form now sets it from the validation result. Decide which side owns this: either add `invalid` to the documented interface, or make each field render on its own internals `invalid` event.
- Form: `invalid` is specified both as an input to validation ('fields with an `error` prop are invalid, and `invalid` fields are invalid') and as the thing Form sets — which is circular, because DsInput.syncInternals reports customError whenever `invalid` is true, so a Form-set flag is self-sustaining and the field can never become valid again. I broke the cycle by tracking which fields Form marked (invalidByForm) and clearing only those before re-measuring, mirroring the documented `disabled` propagation. The doc should state this precedence explicitly.
- Form: the doc says 'When a Form has an error summary, the Form announces errors and Inputs stay silent', but Input renders its error text with role="alert" unconditionally and the declared DsFormField interface gives Form no way to silence a field. With the summary on, an error is announced twice (summary + each field). Needs either a field-side suppression (e.g. Input checking for an enclosing ds-form with a summary) or a documented interface member.
- Form: contradiction on errorSummaryBorder. The `styles` block marks it `locked: false` and the Overrides section lists it as Overridable, but the web platform note calls it one of 'the locked summary bindings (errorSummaryText, errorSummaryBackground, errorSummaryBorder)' that 'get no --ds-form-* hook on web or Lit'. I followed the contract sections: it is overridable with a --ds-form-error-summary-border hook. Fix the platform note.
- Form: generated/behavior/Form.lit.test.ts asserts `el.shadowRoot.querySelector('[role="form"]')` is not null, which contradicts the Lit platform note that 'The landmark is the host: role="form" ... sit on ds-form, and the shadow <form> is unnamed, so it is not a second landmark'. The scenario is unexpressible as generated for Lit and fails against a correct implementation; the generator for behavior tests needs to read the role off the host on Lit. I left generated/ untouched as instructed.
- Form: the a11y `contrast` pair (color.foreground.danger on color.background.subtle) is declared, but errorSummaryBackground is color.background.subtle while the summary sits on whatever surface the consumer placed the Form on. The summary box paints its own background so the pair holds, but nothing in the doc constrains the Link focus ring or the rest of the form against that surface.
- Form: `name` is documented as 'the base of generated ids' with 'An unnamed form bases its ids on a generated unique id (useId on web)'. Lit has no useId, so I used a module-level counter (`ds-form-N`). The doc should name the Lit fallback, since the id is user-visible in the summary link hrefs.
- Form: the doc says 'two forms given the same `name` on one page is an authoring error Form does not detect' and gives no dev warning, yet it does ask for a dev warning nowhere else either. I kept one existing DEV warning for the explicitly forbidden case ('Never nest inside a native form'). Confirm whether that warning is wanted, since the doc never asks for it.
- Form: it is unspecified whether Form should stop the submit ds-button's composed `press` event once it dispatches its own `submit` (the package convention says a composite stops an inner button's press). Because the action button is consumer-slotted rather than composed by Form, I let `press` continue to propagate so consumers can still listen on their own button.
- Form: the errorSummary description says the plural locale is 'read at the failed submit' from 'the nearest `lang` ancestor (web and Lit)'. I used `this.closest('[lang]')`, which includes the host itself; the doc does not say whether a `lang` on the ds-form element counts as 'nearest ancestor' or whether the lookup should start at the parent.

## 2026-09-20 06:40 — round 2

- Form: BOTH round-2 gates failed on other components' pre-existing debt, not on Form, so there was nothing in Form to fix. The axe and keyboard gates run over the whole Lit Storybook, so any component's debt fails the job that happens to be running. Verified: Form has no `keyboard` block and therefore no generated/keyboard/Form.lit.spec.ts (the full keyboard-lit run is 32 failures across ActionSheet, Combobox, DatePicker, Dialog, Feed, Listbox, Menu, Popover, Search, SegmentedControl, Select, SidePanel, Slider, Stepper, Tabs, Toast, Toolbar, Tooltip, Tree — zero Form); the untruncated axe arrays in test-results/*/error-context.md are Feed 28, TreeGrid 6, Listbox 4, Select 2, DataGrid 2, Slider 1 — zero Form. All 10 Form/Lit stories pass axe in light and dark with 0 violations, including Failed Submit. These gates need to be scoped per component (or filtered by the job's component) so a generation job is judged on its own output.
- Form: the Lit contract says ds-form 'calls the field's checkValidity(), and the field renders its own error', but checkValidity() on DsInput/DsCheckbox/DsRadioGroup only syncs ElementInternals and never sets `invalid`, so nothing renders at the field. Every field's own doc says `invalid` is 'usually set by the Form', yet the doc's enumerated DsFormField members omit `invalid`. I added `invalid?: boolean | undefined` as an optional interface member and Form sets it from the validation result. Decide which side owns this: add `invalid` to the documented interface, or make each field render on its own internals `invalid` event.
- Form: `invalid` is specified both as an input to validation ('fields with an `error` prop are invalid, and `invalid` fields are invalid') and as the thing Form sets — which is circular, because DsInput.syncInternals reports customError whenever `invalid` is true, so a Form-set flag is self-sustaining and the field can never become valid again. A browser test caught this: a corrected field stayed in error forever. I broke the cycle by tracking which fields Form marked (invalidByForm) and clearing only those before re-measuring, mirroring the documented `disabled` propagation. The doc should state this precedence explicitly.
- Form: the doc says 'When a Form has an error summary, the Form announces errors and Inputs stay silent', but Input renders its error text with role="alert" unconditionally and the declared DsFormField interface gives Form no way to silence a field. With the summary on, an error is announced twice (summary + each field). Needs either a field-side suppression (e.g. Input checking for an enclosing ds-form with a summary) or a documented interface member.
- Form: contradiction on errorSummaryBorder. The `styles` block marks it `locked: false` and the Overrides section lists it as Overridable, but the web platform note calls it one of 'the locked summary bindings (errorSummaryText, errorSummaryBackground, errorSummaryBorder)' that 'get no --ds-form-* hook on web or Lit'. I followed the contract sections: it is overridable with a --ds-form-error-summary-border hook. Fix the platform note.
- Form: generated/behavior/Form.lit.test.ts asserts `el.shadowRoot.querySelector('[role="form"]')` is not null, which contradicts the Lit platform note that 'The landmark is the host: role="form" ... sit on ds-form, and the shadow <form> is unnamed, so it is not a second landmark'. The scenario is unexpressible as generated for Lit and fails against a correct implementation; the behavior-test generator needs to read the role off the host on Lit. I left generated/ untouched as instructed.
- Form: `name` is documented as 'the base of generated ids' with 'An unnamed form bases its ids on a generated unique id (useId on web)'. Lit has no useId, so I used a module-level counter (`ds-form-N`). The doc should name the Lit fallback, since the id is user-visible in the summary link hrefs.
- Form: the doc never asks for a dev warning, but the implementation keeps one for the explicitly forbidden nesting case ('Never nest inside a native form'). Confirm whether that warning is wanted.
- Form: it is unspecified whether Form should stop the submit ds-button's composed `press` event once it dispatches its own `submit` (the package convention says a composite stops an inner button's press). Because the action button is consumer-slotted rather than composed by Form, I let `press` keep propagating so consumers can still listen on their own button.
- Form: the errorSummary description says the plural locale is 'read at the failed submit' from 'the nearest `lang` ancestor (web and Lit)'. I used `this.closest('[lang]')`, which includes the host itself; the doc does not say whether a `lang` on the ds-form element counts as 'nearest ancestor' or whether the lookup should start at the parent.

## 2026-09-20 06:59 — round 3

- Form: NEITHER round-3 gate implicates Form, so there was nothing in Form to fix. `keyboard-run`'s failure list is byte-identical to round 2's (32 failures across ActionSheet, Combobox, DatePicker, Dialog, Feed, Listbox, Menu, Popover, Search, SegmentedControl, Select, SidePanel, Slider, Stepper, Tabs, Toast, Toolbar, Tooltip, Tree) and Form has no `keyboard` block, so no generated/keyboard/Form.lit.spec.ts exists and Form cannot appear in that gate at all. `axe` produced no per-story results whatsoever this round — it timed out at 900s — so it named no component, Form included.
- Form: the axe gate is at a structural capacity ceiling, not failing on any one component. `gates:axe` runs axe-web + axe-lit + axe-rn, each sweeping every story twice (light and dark): I measured 864 web + 843 lit + 822 rn stories = 5,058 render+analyse passes, executed as six serial loops. At the ~270ms/story I measured on Form's own stories, a single Lit mode is already ~230s and the slowest loop lands at the 900s cap. Form contributes 10 of 843 Lit stories (1.2%), measured in round 2 at 6.0s for both modes with 0 violations, so no change to Form can move this gate. The gate needs to be scoped per component (or per platform), given a budget above the sweep's real cost, or run incrementally; otherwise every future generation job will be rejected by it regardless of that job's output. I launched a per-story timing sweep (logs/lit-axe-timing.spec.ts) to distinguish a genuine story-level hang from the capacity ceiling and to name the slowest components, but the background run was stopped before it wrote results, so I cannot report which components dominate the sweep.
- Form: the Lit contract says ds-form 'calls the field's checkValidity(), and the field renders its own error', but checkValidity() on DsInput/DsCheckbox/DsRadioGroup only syncs ElementInternals and never sets `invalid`, so nothing renders at the field. Every field's own doc says `invalid` is 'usually set by the Form', yet the doc's enumerated DsFormField members omit `invalid`. I added `invalid?: boolean | undefined` as an optional interface member and Form sets it from the validation result. Decide which side owns this: add `invalid` to the documented interface, or make each field render on its own internals `invalid` event.
- Form: `invalid` is specified both as an input to validation ('fields with an `error` prop are invalid, and `invalid` fields are invalid') and as the thing Form sets — which is circular, because DsInput.syncInternals reports customError whenever `invalid` is true, so a Form-set flag is self-sustaining and the field can never become valid again. A browser test caught this: a corrected field stayed in error forever. I broke the cycle by tracking which fields Form marked (invalidByForm) and clearing only those before re-measuring, mirroring the documented `disabled` propagation. The doc should state this precedence explicitly.
- Form: the doc says 'When a Form has an error summary, the Form announces errors and Inputs stay silent', but Input renders its error text with role="alert" unconditionally and the declared DsFormField interface gives Form no way to silence a field. With the summary on, an error is announced twice (summary + each field). Needs either a field-side suppression (e.g. Input checking for an enclosing ds-form with a summary) or a documented interface member.
- Form: contradiction on errorSummaryBorder. The `styles` block marks it `locked: false` and the Overrides section lists it as Overridable, but the web platform note calls it one of 'the locked summary bindings (errorSummaryText, errorSummaryBackground, errorSummaryBorder)' that 'get no --ds-form-* hook on web or Lit'. I followed the contract sections: it is overridable with a --ds-form-error-summary-border hook. Fix the platform note.
- Form: generated/behavior/Form.lit.test.ts asserts `el.shadowRoot.querySelector('[role="form"]')` is not null, which contradicts the Lit platform note that 'The landmark is the host: role="form" ... sit on ds-form, and the shadow <form> is unnamed, so it is not a second landmark'. The scenario is unexpressible as generated for Lit and fails against a correct implementation; the behavior-test generator needs to read the role off the host on Lit. I left generated/ untouched as instructed.
- Form: `name` is documented as 'the base of generated ids' with 'An unnamed form bases its ids on a generated unique id (useId on web)'. Lit has no useId, so I used a module-level counter (`ds-form-N`). The doc should name the Lit fallback, since the id is user-visible in the summary link hrefs.
- Form: the doc never asks for a dev warning, but the implementation keeps one for the explicitly forbidden nesting case ('Never nest inside a native form'). Confirm whether that warning is wanted.
- Form: it is unspecified whether Form should stop the submit ds-button's composed `press` event once it dispatches its own `submit` (the package convention says a composite stops an inner button's press). Because the action button is consumer-slotted rather than composed by Form, I let `press` keep propagating so consumers can still listen on their own button.
- Form: the errorSummary description says the plural locale is 'read at the failed submit' from 'the nearest `lang` ancestor (web and Lit)'. I used `this.closest('[lang]')`, which includes the host itself; the doc does not say whether a `lang` on the ds-form element counts as 'nearest ancestor' or whether the lookup should start at the parent.
- (report recovered after a second request)

## 2026-09-20 14:05 — round 1

- Form: the behavior scenario `label-names-the-form-landmark` is generated as `el.shadowRoot.querySelector('[role="form"]')` plus an accessible name on that shadow node, but the doc's lit platform notes say the opposite — the landmark is the host (`role="form"` + aria-label on ds-form) and the shadow <form> is deliberately unnamed so it is not a second landmark. I followed the platform note, so generated/behavior/Form.lit.test.ts fails that one test (the hand-written Form.test.ts asserts the same scenario on the host and passes). This is the same generator limitation that already fails 4 of Landmark's generated tests; the behavior-test generator needs a way to express 'role/name on the host'.
- Form: the React package exports a `Keyboard` story but the schema has no `keyboard` block for Form, so the doc never states Form's keyboard model or the story's args. I added a Lit `Keyboard` story mirroring React's (three required Inputs in a Stack plus the submit Button) for story parity; the doc should either grow a `keyboard` block (Tab through fields, Enter submits) or drop the React story.
- Form: the doc describes submission triggers (submit Button, Enter) but names no imperative API, while the `FailedSubmit` story it mandates needs one to submit from `play`. I exposed a public `submit(): void`. It should be in the schema as a method if it is part of the contract.
- Form: `errorSummaryGap` is specified as forwarded to both summary Stacks' `overrides.gap` with no `--ds-form-*` hook, but the doc never says what those Stacks' own `gap` prop should be. Chose `gap="tight"`, matching the binding's token (layout.gap.tight), so an absent override still renders the documented default.
- Form: the onSubmit value contract lists null, empty string and empty array as contributing no key, but does not say whether a Switch that is `false` contributes. Chose to include it (strict equality, so `false` and `0` are values), since the doc says 'Switch a boolean'.
- Form: the summary's plural locale is 'the locale of the nearest `lang` ancestor'. Ambiguous whether a `lang` on ds-form itself counts. Chose `this.closest('[lang]')`, which includes the element itself.
- Form: the doc says an entry whose field 'has since unregistered' stays as plain danger Text with no Link, but Lit discovery is a live DOM query with no registration step, so 'unregistered' has no definition here. Chose: the field is no longer returned by the light-DOM `[data-ds-field]` query at render time (removed from the DOM, or now inside a closed ds-disclosure without keep-mounted).
- Form: the package convention says a composite stops an inner ds-button's `press` when it dispatches its own event, but here the submit Button is consumer-slotted light DOM, not a shadow child. Chose to let `press` keep propagating past ds-form (only `submit`/`invalid` are Form's own events); the doc should say which.
- Form: `delegatesFocus: true` is required by the generation rules for anything focusable, but Form is a container whose only shadow-focusable node is the error summary, which is focused programmatically. With delegatesFocus, a click on the container's blank area delegates focus into the first slotted field — and the package's other containers (Card, Fieldset) set no shadowRootOptions at all. Kept it per the rule; worth deciding explicitly for containers.
- Form (tooling, not the doc): adding the `Keyboard` story makes packages/lit/storybook-static/index.json stale, so `tools/docs_examples.ts` will flag `form-lit--keyboard` until `pnpm --filter @design-schema/lit build-storybook` runs. That gate is already red for an unrelated pre-existing reason: packages/react/src/Card.stories.tsx exports `InteractiveInFragment`, which the React storybook index does not list.

## 2026-09-20 14:09 — round 2

- Form: the `keyboard-run` gate cannot pass and no component code can make it. The doc has no `keyboard` block, so tools/keyboard_tests.ts:304 never emits generated/keyboard/Form.{lit,web}.spec.ts, and a positional filter naming that file makes Playwright exit 1 with 'No tests found'. tools/checks.ts:101-114 already guards this (scoped to Form it does not schedule keyboard-run at all, and any filter it emits carries --pass-with-no-tests, which exits 0) — the failing round used the unguarded shape. Identical to the Link rounds recorded in logs/link-lit-kbgate.mjs and logs/link-kbgate-round3.mjs. Doc fix: give Form a `keyboard` block. The Accessibility prose already promises 'keyboard operable, including submission with Enter (2.1.1)' and the element implements it (Enter in any data-ds-field field submits, unless the keydown originates in a textarea, button or link), but as prose it is untestable. The Lit `Keyboard` story is already in place and correctly named (`form-lit--keyboard`, four focusable children), so a spec would bind to it with no further change.
- Form: story parity and the keyboard rule contradict each other. The rules say 'a component with a `keyboard` block ships a story exported as `Keyboard`' — Form has no such block — while the parity rule says every React story has a Lit twin, and React's Form.stories.tsx exports `Keyboard`. I kept the Lit `Keyboard` story (parity, and it passes axe in both modes), but it is doc-unbacked on both platforms. Resolve by adding the `keyboard` block rather than deleting the stories.
- Form: the behavior scenario `label-names-the-form-landmark` is generated as `el.shadowRoot.querySelector('[role="form"]')` plus an accessible name on that shadow node, but the doc's lit platform notes say the opposite — the landmark is the host (`role="form"` + aria-label on ds-form) and the shadow <form> is deliberately unnamed so it is not a second landmark. I followed the platform note, so that one test fails inside generated/behavior/Form.lit.test.ts (the hand-written Form.test.ts asserts the same scenario on the host and passes). Same generator limitation that already fails 4 of Landmark's generated tests; the behavior-test generator needs a way to express 'role/name on the host'.
- Form: the doc describes submission triggers (submit Button, Enter) but names no imperative API, while the `FailedSubmit` story it mandates needs one to submit from `play`. I exposed a public `submit(): void`. It should be in the schema as a method if it is part of the contract.
- Form: `errorSummaryGap` is specified as forwarded to both summary Stacks' `overrides.gap` with no `--ds-form-*` hook, but the doc never says what those Stacks' own `gap` prop should be. Chose `gap="tight"`, matching the binding's token (layout.gap.tight), so an absent override still renders the documented default.
- Form: the onSubmit value contract lists null, empty string and empty array as contributing no key, but does not say whether a Switch that is `false` contributes. Chose to include it (strict equality, so `false` and `0` are values), since the doc says 'Switch a boolean'.
- Form: the summary's plural locale is 'the locale of the nearest `lang` ancestor'. Ambiguous whether a `lang` on ds-form itself counts. Chose `this.closest('[lang]')`, which includes the element itself.
- Form: the doc says an entry whose field 'has since unregistered' stays as plain danger Text with no Link, but Lit discovery is a live DOM query with no registration step, so 'unregistered' has no definition here. Chose: the field is no longer returned by the light-DOM `[data-ds-field]` query at render time (removed from the DOM, or now inside a closed ds-disclosure without keep-mounted).
- Form: the package convention says a composite stops an inner ds-button's `press` when it dispatches its own event, but here the submit Button is consumer-slotted light DOM, not a shadow child. Chose to let `press` keep propagating past ds-form (only `submit`/`invalid` are Form's own events); the doc should say which.
- Form: `delegatesFocus: true` is required by the generation rules for anything focusable, but Form is a container whose only shadow-focusable node is the error summary, which is focused programmatically. With delegatesFocus, a click on the container's blank area delegates focus into the first slotted field — and the package's other containers (Card, Fieldset) set no shadowRootOptions at all. Kept it per the rule; worth deciding explicitly for containers.
- Form (tooling, not the doc): the `Keyboard` story makes packages/lit/storybook-static/index.json stale, so tools/docs_examples.ts flags `form-lit--keyboard` until `pnpm --filter @design-schema/lit build-storybook` runs. That gate is already red for an unrelated pre-existing reason: packages/react/src/Card.stories.tsx exports `InteractiveInFragment`, which the React storybook index does not list.

## 2026-09-20 14:11 — round 3

- Form: `keyboard-run` is unsatisfiable from component code and the failure is harness-side. Root cause proven this round: tools/checks.ts is uncommitted (`git diff`), HEAD's copy schedules keyboard-run repo-wide with no filter and the working-tree copy guards on existsSync plus --pass-with-no-tests — neither produces the reported error. Only an intermediate copy (filter, no --pass-with-no-tests) does, which the long-running generator holds in memory from startup; that is why rounds 2 and 3 are byte-identical. Identical to the Link loop recorded in generated/gaps/Link.lit.md, which consumed ~8 rounds before concluding the same. Harness fix: restart the runner, or commit tools/checks.ts. Doc fix that clears it regardless of harness version (verified): give Form a `keyboard` block carrying the Enter→submit rule — tools/keyboard_tests.ts emits even manual/native rules as collected `test.skip` entries (generated/keyboard/Feed.lit.spec.ts:78-81), so a Form spec would exist and the filter would match. The Accessibility prose already promises 'keyboard operable, including submission with Enter (2.1.1)' and the element implements it (Enter in any data-ds-field field submits, unless the keydown originates in a textarea, button or link), but as prose it is untestable. The `Keyboard` story is already named correctly for it (`form-lit--keyboard`, four focusable children).
- Form: story parity and the keyboard rule contradict each other. The rules say 'a component with a `keyboard` block ships a story exported as `Keyboard`' — Form has no such block — while the parity rule says every React story has a Lit twin, and React's Form.stories.tsx exports `Keyboard`. I kept the Lit `Keyboard` story (parity, and it passes axe in both modes), but it is doc-unbacked on both platforms. Resolve by adding the `keyboard` block rather than deleting the stories.
- Form: the behavior scenario `label-names-the-form-landmark` is generated as `el.shadowRoot.querySelector('[role="form"]')` plus an accessible name on that shadow node, but the doc's lit platform notes say the opposite — the landmark is the host (`role="form"` + aria-label on ds-form) and the shadow <form> is deliberately unnamed so it is not a second landmark. I followed the platform note, so that one test fails inside generated/behavior/Form.lit.test.ts (the hand-written Form.test.ts asserts the same scenario on the host and passes). Same generator limitation that already fails 4 of Landmark's generated tests; the behavior-test generator needs a way to express 'role/name on the host'.
- Form: the doc describes submission triggers (submit Button, Enter) but names no imperative API, while the `FailedSubmit` story it mandates needs one to submit from `play`. I exposed a public `submit(): void`. It should be in the schema as a method if it is part of the contract.
- Form: `errorSummaryGap` is specified as forwarded to both summary Stacks' `overrides.gap` with no `--ds-form-*` hook, but the doc never says what those Stacks' own `gap` prop should be. Chose `gap="tight"`, matching the binding's token (layout.gap.tight), so an absent override still renders the documented default.
- Form: the onSubmit value contract lists null, empty string and empty array as contributing no key, but does not say whether a Switch that is `false` contributes. Chose to include it (strict equality, so `false` and `0` are values), since the doc says 'Switch a boolean'.
- Form: the summary's plural locale is 'the locale of the nearest `lang` ancestor'. Ambiguous whether a `lang` on ds-form itself counts. Chose `this.closest('[lang]')`, which includes the element itself.
- Form: the doc says an entry whose field 'has since unregistered' stays as plain danger Text with no Link, but Lit discovery is a live DOM query with no registration step, so 'unregistered' has no definition here. Chose: the field is no longer returned by the light-DOM `[data-ds-field]` query at render time (removed from the DOM, or now inside a closed ds-disclosure without keep-mounted).
- Form: the package convention says a composite stops an inner ds-button's `press` when it dispatches its own event, but here the submit Button is consumer-slotted light DOM, not a shadow child. Chose to let `press` keep propagating past ds-form (only `submit`/`invalid` are Form's own events); the doc should say which.
- Form: `delegatesFocus: true` is required by the generation rules for anything focusable, but Form is a container whose only shadow-focusable node is the error summary, which is focused programmatically. With delegatesFocus, a click on the container's blank area delegates focus into the first slotted field — and the package's other containers (Card, Fieldset) set no shadowRootOptions at all. Kept it per the rule; worth deciding explicitly for containers.
- Form (tooling, not the doc): the `Keyboard` story makes packages/lit/storybook-static/index.json stale, so tools/docs_examples.ts flags `form-lit--keyboard` until `pnpm --filter @design-schema/lit build-storybook` runs. That gate is already red for an unrelated pre-existing reason: packages/react/src/Card.stories.tsx exports `InteractiveInFragment`, which the React storybook index does not list.

## 2026-09-20 16:29 — round 1

- Form: the doc requires a `FailedSubmit` story that 'submits the sign-in example empty in its play function' but never declares an imperative entry point. Chose a public `submit(): void` method on the element; both events stay non-cancelable since the doc marks neither.
- Form: the doc says an unnamed form 'bases its ids on a generated unique id (useId on web)' but names no Lit equivalent. Chose a module-level counter producing `ds-form-<n>`, used as the base for the `{name}-{field.name}` ids assigned at submit time.
- Form: who owns a field's `invalid` flag is only implied ('usually set by the Form' in the field docs). Chose: Form sets `invalid` from `checkValidity()` but clears only flags it set itself, so an `invalid` the consumer set survives re-validation; a blanket clear would erase consumer state and a blanket set would be sticky forever.
- Form: `onInvalid`'s payload is `Record<string, string>` but the doc only specifies the label/name fallback for the *summary* text. Chose: the event carries the field's raw `validationMessage` including an empty string, so the fallback is presentation-only and consumers see what the field actually reported.
- Form: 'RadioGroup validates when focus leaves the whole group' is stated with no Lit mechanism. Chose: the `focusout` handler skips validation when `relatedTarget` is the field host or inside it, so moving between two radios in one group is not a blur.
- Form: the doc says the `gap` binding spaces 'the direct children of the fields part' without saying whether Lit should wrap the default slot in its own column. Chose to leave the slot at its UA `display: contents` so assigned nodes are flex items of the container directly — which is what makes 'a Stack given as children spaces its own fields with its own gap' true — and to set `display: flex; align-items: flex-start` only on the actions slot so a single bare action keeps its natural width.
- Form: `disabled` propagation is specified for the fields 'it found', with nothing about fields added while disabled is already true, or about a control the consumer disables independently afterwards. Chose: a `childList`-only MutationObserver re-runs propagation while disabled, and re-enabling touches only controls in the remembered set.
- Form: nothing is said about submitting a form with no collected fields. Chose: `submit` fires with `values: {}` rather than being suppressed.
- Form: 'Never nest inside a native form' is a rule with no stated diagnostic. Chose to emit a one-line `import.meta.env.DEV` warning when the host has a `form`/`ds-form` ancestor; the doc requests no warning, so this is an addition.
- Form: `validate` is not listed under `platforms.lit.reflect`, so it is a non-reflected property, yet every example passes it as an attribute. That works (attributes still set non-reflected properties), but the doc never states whether `validate` should be styleable from outside. Chose not to reflect it.
