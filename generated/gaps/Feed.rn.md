# Gaps reported while generating Feed for rn

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 16:48 — round 1

- Feed: composition names `ProgressBar` for loadingIndicator, but @design-schema/rn has no ProgressBar yet (only web/lit have it). Built a private `FeedLoadingIndicator` (indeterminate sliding-fill track, reduced-motion aware) as a stand-in; replace it once ProgressBar ships for RN.
- Feed: platform notes say each article is 'a Card with accessible and an accessibilityLabel from heading + relative time + unread', but Card has no accessible/accessibilityLabel prop, and collapsing the article into one accessible node (as `accessible` does on RN) would hide its action Buttons and any Link in `content` from individual focus. Chose to leave articles un-collapsed (each child keeps its own accessibility) and instead add visually-hidden Text runs for 'unread' and position — so no single composed accessible name exists per article on this platform.
- Feed keyboard model (Tab/PageDown/PageUp/Ctrl+Home/Ctrl+End) has no React Native hardware-keyboard equivalent — Views/FlatList don't receive Page or Ctrl key events on phones/tablets — so none of it is implemented; screen-reader users get the platform's native swipe/browse gestures instead, with no visible-control substitute added.
- Feed: the web spec exposes the absolute timestamp via `<time title>`; there's no native equivalent surfaced here (Card doesn't forward extra accessibility props, and adding a long-press tooltip would mean growing Card's own schema), so the absolute time is not exposed to sighted users or assistive tech on RN — only the relative string is shown.
- Feed: exact relative-time wording/thresholds beyond the one example ('3 min ago') aren't specified; implemented 'just now' (<60s), '{n} min ago' (<60m), '{n} hr ago' (<24h), else '{n} d ago' — a guess.
- Feed: `aria-posinset`/`aria-setsize` have no RN prop equivalent; substituted a hidden `copy.position` text per article, rendered only when `!hasMore` (total known), mirroring the web's `-1`/unknown sentinel by omitting the hint instead of showing an unbounded total.
- Feed: `newItemsOffset` ('space above the new-items button') was applied as the button row's own top padding since nothing render above the Feed by default; unclear whether the intent was a margin the surrounding layout should contribute instead.

## 2026-09-10 20:08 — round 1

- Feed: the hand-built loading-indicator stand-in referenced 'the not-yet-generated ProgressBar' — ProgressBar now exists in the package, so I swapped the footer to render it directly (indeterminate, visible copy.loading label) instead of the ad-hoc Animated track.
- Feed: styles.endMessageInset is a distinct overridable binding from loadingInset in the schema, but the prior implementation reused loadingInset for both the loading footer and the end message; I split them so endMessageInset actually resolves and overrides independently.
- Feed: hasMore's prop description requires firing onEndReached once on mount when items is empty and not loading (FlatList has no last row to observe in that case); this was missing, so I added a mount-only effect that fires it under that condition.
- Feed: endMessage's description says it only shows 'when hasMore is false (and items is not empty)' — the prior code showed it whenever !hasMore regardless of an empty list; added the items.length > 0 guard.
- Feed: the doc's Behavior section says 'while loading with no items the loading indicator shows, not copy.empty', but FlatList renders ListEmptyComponent and ListFooterComponent simultaneously when data is empty, so both would have appeared together; now ListEmptyComponent is suppressed while loading.
- Feed: anatomy lists articleBody, articleActions and timestamp as parts, but only container/article/loadingIndicator/endMessage/newItemsButton/emptyState carried testIDs; added Feed.timestamp/Feed.articleBody/Feed.articleActions wrappers (Text/Card don't expose a testID prop, so each needed a wrapping View, matching the pattern ProgressBar itself uses for its own label/value parts).
- Feed (native constraint, not fixed): the RN platform notes ask for copy.position 'after the heading', but Card only accepts a heading string and a footer slot — there's no injection point between them — so the hidden position/unread text stays a sibling before the Card, as it was already structured; flagging since it can't literally sit after the heading without growing Card's schema.
- Feed: the keyboard block (Tab/PageDown/PageUp/Ctrl+Home/Ctrl+End) is explicitly a web-only APG feed model per the doc's own RN platform notes ('no hardware-keyboard feed model on native'), so no Keyboard-specific interaction logic was implemented on this platform — the existing Keyboard story only exercises focusable children for the axe gate, not the key bindings themselves.

## 2026-09-16 13:03 — round 1

- Feed: the Guidance's React Native section says items are `Card`s with `accessible` and a composed `accessibilityLabel`, but platforms.rn.notes says Cards stay un-collapsed (no `accessible`) so the action Buttons and Links stay focusable; I followed the platform notes (the Declared contracts section says sections win) and the Guidance paragraph should be corrected.
- Feed: the examples and scenarios give `content` as a plain string, but React Native crashes on a bare string outside `Text`; Feed wraps string/number content in the package `Text`. The schema should say whether `content` strings are wrapped or must be nodes.
- Feed: no binding covers the space between the timestamp and the content in articleBody; I used `Stack gap="tight"` (the web Guidance's value for the actions row). Add an `articleBodyGap` binding or name the gap.
- Feed: emptyState has no inset or size binding (unlike endMessage and loadingIndicator); I rendered it as a muted `Text` with no padding. The schema should declare its inset, size and tone.
- Feed: the schema doesn't say whether `fontFamily` reaches the composed children; I passed it to every Text (timestamp, end message, empty state, hidden unread/position runs, string content) and to the new-items Button's `fontFamily` override, but not to Card's heading, because Card has no fontFamily binding.
- Feed: `articleInset` is 'passed to each Card as its inset', but Card's `inset` is an enum (sm/md/lg) and a TokenRef can't map onto it; the default uses `inset="md"` and an override goes to Card's `paddingBlock` and `paddingInline`.
- Feed: the relative-time thresholds don't say whether to round or floor, or what a future timestamp shows; I floor and clamp future times to `justNow`, and the absolute date uses `Intl.DateTimeFormat(undefined, { dateStyle: 'medium' })` because the date style isn't given.
- Feed: `onItemVisible` 'when it has been substantially visible for a moment' doesn't say whether it fires again when the item scrolls out and back; I fire on each transition to viewable (from `changed`), so the caller must be idempotent.
- Feed: `onLoadMore` 'once on mount' when empty doesn't say what happens if the caller later clears `items` to empty while `hasMore` stays true; I don't fire again, and the list's own `onEndReached` is ignored while items are empty, so that feed stalls until it gets new props.
- Feed: platforms.rn.props names `accessibilityRole=list` and the web feed is `aria-busy`; I set `accessibilityState.busy` from `loading`. The loading-marks-the-feed-busy scenario is web-only, so nothing tests busy on native.
- Feed: the convention puts `testID="Feed"` on the root, while the anatomy's `container` is the feed list itself; the outer View (holding the new-items button and the list) carries `Feed` and the FlatList carries `Feed.container`.
- Feed: the `Keyboard` story rule (open with its trigger and at least three focusable children) doesn't fit a feed, which has no open state; the story shows the new-items button plus the default items' Reply button and Open ticket link.
- Feed: `newItemsOffset` is 'padding-block-start of the sticky new-items row', but React Native has no sticky header outside the list; the button sits above the FlatList (per the platform notes) with `paddingTop`, so it never scrolls away. The schema doesn't say whether it should be inside the list as a sticky header instead.

## 2026-09-17 16:36 — round 1

- Feed: `newItemsLayer` is in the overridable list and its description scopes it to "web and Lit", while the rn notes put the new-items row above the FlatList as a sibling View, where nothing overlaps. Applied it as `zIndex` on the row so the override resolves to something, but it has no visual effect on native — either drop it from the rn overridable set or say it is inert there.
- Feed: the rn notes make the new-items row itself the `accessibilityLiveRegion="polite"`, but the row only mounts when `newItemsCount > 0`. A region that mounts together with its text is not reliably announced on Android — which is exactly why the web notes keep the role="status" row always rendered and pad it only while shown. Followed the rn notes as written; the reliable form would be an always-rendered row on native too.
- Feed: `onItemVisible` says "fires once per item id per mount" in the event description, but the rn notes describe only the raw `onViewableItemsChanged` with 50%/1s, which re-fires on re-entry. Implemented the once-per-id dedupe with a Set ref (event description wins) — the rn notes should say so.
- Feed: `onShowNew` says focus moves to the first article once the first item's id changes. Not implemented on native: Card's `focusable` is a react-native-web-only capability (no container can focus a View by script on iOS/Android), and `AccessibilityInfo.setAccessibilityFocus` would need a per-Card ref the spec does not ask for. The rn notes mention `maintainVisibleContentPosition` but say nothing about focus — they should state the focus move is web-only.
- Feed: `headingLevel` has no observable effect on rn. Card renders its heading at a fixed `size="lg"` and RN has no heading levels, so all three enum values render identically and the three derived `renders-heading-level-*` scenarios cannot distinguish them. Passed the level through to Card for parity and documented it as typography-only, which is itself inaccurate here — it controls nothing.
- Feed: `unreadBorder` is specified as a bar "drawn by that wrapper over the Card's start edge". On rn a border on the Feed-owned wrapper sits beside the Card, not over it — overlaying would need a negative offset (a literal the gate rejects) or absolute positioning. Drew `borderStartWidth`/`borderStartColor` on the wrapper, which shifts the Card inward by the bar's width instead of overlapping it.
- Feed: the entire `keyboard` block (PageDown, PageUp, Ctrl+End, Ctrl+Home) is unimplementable on native — no Page or Ctrl keys reach a FlatList, and the rn notes acknowledge it. The rule "a component with a keyboard block ships a story exported as Keyboard" still applies, so the story exists for the react-native-web axe gate only; none of the four rules is testable on this platform.
- Feed: no `emptyState` behaviour is defined for `items: []` with `hasMore: true` — guidance forbids `copy.empty` there and there is no article to show, so the feed renders an empty list with no placeholder at all while the first page is requested. Left it blank as specified; a `loading` companion state would avoid the dead screen.
- Feed: `items[].timestamp` says the absolute date is shown "from seven days" but lists `daysAgo` as "under seven days", leaving day 7 exactly to interpretation, and does not say whether the relative text should update. Used `elapsed < WEEK` for `daysAgo` and compute at render only (the doc says it does not tick), so an on-screen article's time goes stale until the next render.

## 2026-09-22 00:01 — round 1

- Feed: the schema's a11y role is `feed`, but React Native 0.87 has no `feed` value in either `AccessibilityRole` or `Role` (checked types_generated/Libraries/Components/View/ViewAccessibility.d.ts — no match). The rn platform notes say `accessibilityRole=list`, which is what I render, so the doc is self-consistent; but the generated behavior test `generated/behavior/Feed.rn.test.tsx` asserts `getByRole('feed', { name: label })` for `has-accessible-name`, which can never pass on this platform. The generator should emit `getByRole('list')` for rn, or the a11y block should declare a per-platform role.
- Feed: the generated rn behavior test presses the part wrapper directly (`fireEvent.press(screen.queryByTestId('Feed.newItemsButton'))`) for the `pressing-show-new-asks-for-the-newer-items` scenario. On RN the `newItemsButton` part is a wrapping View with no `onPress` (the pressable is the composed Button inside it, and RNTL's press bubbles to ancestors, not descendants), so that assertion cannot pass. The hand-written `src/Feed.test.tsx` works around it with `within(part).getByRole('button')`. Either the part hook should sit on the Button, or the generator needs a rule for parts that wrap a composed control.
- Feed: `hasMore` says the feed asks 'at most once per change of the last item's id, `hasMore` or `loading`', but never says what resets the latch when a fetch fails — `loading` going true then false with the same last id. I read the literal wording (a change of `loading` permits a new request) and reset on any change of the tuple, so a failed fetch can be retried when the reader is still at the end. The alternative reading (key only on last id) would silently wedge the feed after a failed page.
- Feed: `newItemsCount` is `type: integer` with 'Undefined, zero or a negative count shows no button', but nothing says what to do with a fractional value a JS caller can still pass. I removed the previous `Math.trunc` and interpolate the value as given, on the rule that an integer prop is a contract on the caller, not something the component repairs. If the intent is defensive truncation, the doc should say so.
- Feed: the style binding `newItemsLayer` (`layer.raised`) is documented as 'Inert on React Native... it is still applied so an override resolves.' I apply it as `zIndex` on the row, which is not inert — on react-native-web it genuinely stacks, and on native it affects sibling ordering within the root View. Harmless here (the row has no overlapping sibling), but 'inert' overstates it and a reader may be surprised that an override has a visible effect under react-native-web.
- Feed: the `article` part wrapper draws the unread bar as `borderStartWidth`/`borderStartColor`, which the doc acknowledges 'insets the Card by the bar's width'. That means unread and read articles are not aligned with each other — read articles have no compensating start padding. The doc does not say whether that misalignment is accepted or whether read articles should reserve a transparent bar of the same width (as Card reserves its focus ring). I left it as the doc describes, so unread rows sit `border.width.focus` narrower.
- Feed: the `timestamp` part is described for web as sitting on the `<time>` element inside the Text, with `timestampSize` applying to the wrapping Text. On RN, Text takes no testID, so I wrap it in a View that carries `testID="Feed.timestamp"` while `size="xs"` and the `overrides.fontSize` forward go to the Text inside. The part element and the styled element are therefore different nodes on this platform; the doc covers the web/Lit split but does not state the rn one.
- Feed: `articleActions` has no stated behavior when an item has no `actions`. I render the wrapper View (and Card's `footer`) only when `actions != null`, so the part is absent rather than empty for items without actions. A test that queries `Feed.articleActions` unconditionally would fail on the example items, none of which declare actions.
- Feed: the rules require a `Keyboard` story 'rendering it open with its trigger and at least three focusable children', but the rn platform notes state none of the four feed commands exists on this platform and the story is 'a visual and axe fixture here rather than a keyboard one'. The existing `Keyboard` story sets `newItemsCount: 3, hasMore: false` against sample items that include a Reply Button and an Open-ticket Link; I left it. It is unclear whether the axe gate should scope this component at all given there is no keyboard model to check.

## 2026-09-22 00:06 — round 2

- Feed: the rn platform notes give the FlatList `accessibilityRole=list` but never say what role the rows carry. Under react-native-web that renders <ul role="list"> and axe's aria-required-children descends through the roleless content-container and cell wrappers, so every article's heading/Button/Link became an unallowed child of the list and an articleless feed had no required child — the axe gate failed all 14 stories in both modes. I added `role="listitem"` to the Feed-owned `article` wrapper. The doc should state that the `article` part is the list row on rn, since `article` is not a role RN has and `list` forces `listitem` on its children.
- Feed: the rn notes put the loading indicator and end message in `ListFooterComponent` and (by implication) the empty state in `ListEmptyComponent`, both of which render *inside* the FlatList — i.e. inside role="list". The doc does not say whether they are rows of the list or chrome. I gave all three wrappers `role="listitem"`, which is what makes role="list" valid and also supplies the required child for the Empty and LoadingEmpty stories. The consequence, unstated in the doc, is that a screen reader counts the loading/end/empty row as a list item ("list, 4 items" for 3 articles plus "Loading more"). If that is wrong, the footer has to move out of the FlatList and `ListFooterComponent` should come off `platforms.rn.props`.
- Feed: the schema's a11y role is `feed`, but RN 0.87 has no `feed` value in either `AccessibilityRole` or `Role` (verified against types_generated/.../ViewAccessibility.d.ts: Role has 65 values, AccessibilityRole 40, neither includes `feed`). The rn notes' `accessibilityRole=list` is the substitute, so a feed on this platform is announced as a plain list and loses the APG feed semantics — worth stating outright in the doc rather than leaving the reader to infer it. The generated `generated/behavior/Feed.rn.test.tsx` still asserts `getByRole('feed', { name: label })`, which can never pass here.
- Feed: the generated rn behavior test presses the part wrapper directly (`fireEvent.press(screen.queryByTestId('Feed.newItemsButton'))`) for `pressing-show-new-asks-for-the-newer-items`. On rn that part is a wrapping View with no `onPress` — the pressable is the composed Button inside it, and RNTL's press bubbles to ancestors, not descendants — so the assertion cannot pass. The hand-written `src/Feed.test.tsx` works around it with `within(part).getByRole('button')`. Either the part hook belongs on the Button, or the generator needs a rule for parts that wrap a composed control.
- Feed: `hasMore` says the feed asks 'at most once per change of the last item's id, `hasMore` or `loading`' but does not say what resets the latch when a fetch fails — `loading` going true then false with the same last id. I reset on any change of that tuple, so a retry is possible while the reader is still at the end; the alternative reading (key on last id alone) would wedge the feed permanently after one failed page.
- Feed: `newItemsCount` is `type: integer` and the doc covers undefined, zero and negative, but not a fractional value a JS caller can still pass. I dropped the previous `Math.trunc` and interpolate the value as given, on the rule that an integer prop is a contract on the caller rather than something the component repairs. Say so if defensive truncation is intended.
- Feed: `newItemsLayer` (`layer.raised`) is documented as 'Inert on React Native ... still applied so an override resolves.' I apply it as `zIndex` on the row, which is not inert under react-native-web — it genuinely stacks there. Harmless (the row has no overlapping sibling) but 'inert' overstates it.
- Feed: the `article` wrapper draws the unread bar as a start border, which the doc admits 'insets the Card by the bar's width'. Read articles get no compensating start padding, so unread and read rows are misaligned by `border.width.focus`. The doc does not say whether that is accepted or whether read rows should reserve a transparent bar the way Card reserves its focus ring; I left it as described.
- Feed: `timestampSize` is documented for web as applying to the wrapping Text while the part hook sits on the inner `<time>`. On rn, Text takes no testID, so the part hook is on a wrapping View and `size="xs"` plus the `overrides.fontSize` forward go to the Text inside it — part element and styled element are different nodes. The doc covers the web/Lit split but not the rn one.
- Feed: nothing states whether `articleActions` renders when an item has no `actions`. I render the wrapper (and Card's `footer`) only when `actions != null`, so the part is absent rather than empty — a test querying `Feed.articleActions` unconditionally fails on the schema's own example items, none of which declare actions.
- Feed: the rules require a `Keyboard` story 'rendering it open with its trigger and at least three focusable children', while the rn notes state none of the four feed commands exists on this platform and the story is 'a visual and axe fixture here rather than a keyboard one'. The existing story (`newItemsCount: 3, hasMore: false` over sample items containing a Button and a Link) is kept as an axe fixture, but whether the keyboard gate should scope this component on rn at all is unresolved.

## 2026-09-23 15:04 — round 1

- Feed: 'A loading cycle that ends with the same last id re-arms the request' — the feed resets its once-only guard when `loading` changes, but FlatList only calls onEndReached again after the content length changes. Re-arming therefore depends on the loading footer appearing and disappearing to change that length, and a failed page with no footer change may not be asked for again until the reader scrolls. I relied on the footer toggle and added no timer or scroll listener.
- Feed: articleRadius on RN 'only rounds the ends of the unread bar', but the spec doesn't say whether read rows (no bar) should get the radius too. I apply it to every article wrapper; with no border it draws nothing.
- Feed: the Keyboard story needs 'at least three focusable children' with no newItemsCount, but the Default story's sample items have only two (Reply, Open ticket). I gave the third item an 'Undo' action in that story; the spec doesn't list the fixture's items.
- Feed: onItemVisible's rn name is `onViewableItemsChanged`, which is also the FlatList prop name, but Feed's version takes an id where FlatList's takes { viewableItems, changed }. The handler is typed (id: string) => void and dedupes once per id per mount; a caller who expects FlatList's signature will be confused.
- Feed: fontFamily is described as 'the root font'. RN has no inheritance, so the override reaches only the composed Texts and the Button, as the forwarding list says. Caller content passed as a ReactNode doesn't get it.

## 2026-09-23 15:04 — round 1

- Feed: articleRadius on React Native 'only rounds the ends of the unread bar', but the spec doesn't say whether read rows (which have no bar) get the radius too. The file currently applies it to every article wrapper, where it has no visible effect on read rows.
- Feed: the doc gives no rule for when onEndReached fires on first render if the list is shorter than a screen and hasMore is true. FlatList calls onEndReached for such a list, and the component asks once per change of the last id, hasMore or loading. The doc should say whether that counts as a 'user' request.
- Feed: onItemVisible's fires: [user] can't be checked on native. onViewableItemsChanged also fires on mount for items already on screen, with no user action.
- Feed: headingLevel is passed to Card for parity only. On native no test can see a difference, so the renders-heading-level-* scenarios only prove that it renders.
- Feed: the Keyboard story's 'at least three focusable children' has to come from the story's own items, because the schema's examples have no actions. I chose Default's items with an Undo button added to the third item.
- Feed: the doc gives no rule for whether visually-hidden Text runs (unread, position) should be clipped Views (position absolute, width/height 1, overflow hidden). That is what's used here, since native Text has no visually-hidden option; Text's schema should provide one.

## 2026-09-23 19:47 — round 1

- Feed: the spec says copy strings are used verbatim but gives no i18n mechanism on rn, so the copy is hard-coded in a local COPY table; showNew, position and the relative-time strings are plain template literals, not Intl.PluralRules selections, because the spec lists one form for each.
- Feed: the `behavior` scenario `loading-marks-the-feed-busy` is limited to web and lit, so there is no rn test for `accessibilityState.busy`; I left it untested rather than invent an rn expectation.
