// Generated by tools/behavior_tests.ts from the `behavior` block of the DatePicker doc. Do not edit.
import { describe, expect, test, vi } from 'vitest';
import { act, render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { DatePicker } from '../../packages/react/src/DatePicker';
import type { DatePickerProps } from '../../packages/react/src/DatePicker';
import meta from '../../packages/react/src/DatePicker.stories';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<DatePickerProps> = {}) {
  const events = {
    onChange: vi.fn(),
    onOpenChange: vi.fn(),
  };
  const props = { ...meta.args, ...given, onChange: events.onChange, onOpenChange: events.onOpenChange };
  const utils = render(<DatePicker {...props} />);
  const user = userEvent.setup();
  const s = {
    ...utils,
    user,
    events,
    props,
    root: () => (document.querySelector('[data-ds="DatePicker"]') ?? utils.container.firstElementChild) as HTMLElement,
    label: () => s.root(),
    rerender: (next: Partial<DatePickerProps>) => utils.rerender(<DatePicker {...props} {...next} />),
  };
  return s;
}

describe('DatePicker', () => {
  test('renders', async () => {
    const s = setup({"open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-sm', async () => {
    const s = setup({"size": "sm", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test('renders-size-md', async () => {
    const s = setup({"size": "md", "open": true});
    expect(s.root()).not.toBeNull();
  });
  test.skip('has-accessible-name — then.name: role \'none\' cannot be queried; name the landmark/text role in the doc', async () => {});
  test('error-is-identified', async () => {
    const s = setup({"error": "Fix this before continuing.", "open": true});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeInTheDocument();
    expect(s.label()).toHaveAttribute('aria-invalid', 'true');
  });
});
