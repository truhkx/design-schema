// Generated by tools/behavior_tests.ts from the `behavior` block of the Combobox doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Combobox } from '../../packages/rn/src/Combobox';
import type { ComboboxProps } from '../../packages/rn/src/Combobox';
import meta from '../../packages/rn/src/Combobox.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

type TestNode = ReturnType<typeof screen.getByTestId>;
const CONTROL_ROLES = new Set(["button", "checkbox", "switch", "radio", "textbox", "searchbox", "spinbutton", "combobox", "slider", "link", "tab", "menuitem", "menuitemcheckbox", "menuitemradio", "option", "treeitem", "scrollbar", "adjustable", "imagebutton", "togglebutton"]);
function isControl(n: TestNode): boolean {
  return CONTROL_ROLES.has(n.props.role ?? n.props.accessibilityRole) || typeof n.props.onPress === 'function';
}
const CONTAINER_ROLES = new Set(["dialog", "alertdialog", "combobox", "grid", "listbox", "menu", "menubar", "radiogroup", "tablist", "tree", "treegrid"]);
function hasOwnRole(n: TestNode): boolean {
  return CONTAINER_ROLES.has(n.props.role ?? n.props.accessibilityRole);
}
function activatable(node: TestNode, root: TestNode): TestNode {
  const hit = isControl(node) ? node : node === root || hasOwnRole(node) ? undefined : node.findAll(isControl)[0];
  if (hit === undefined) return node;
  return typeof hit.type === 'string' ? hit : (hit.findAll((n: TestNode) => typeof n.type === 'string')[0] ?? hit);
}

function setup(given: Partial<ComboboxProps> = {}) {
  const events = {
    onChange: jest.fn(),
    onInputChange: jest.fn(),
    onOpenChange: jest.fn(),
  };
  const props: ComboboxProps = { ...(meta.args as ComboboxProps), ...given, onChange: events.onChange, onInputChange: events.onInputChange, onOpenChange: events.onOpenChange };
  const tree = (p: ComboboxProps) => (
    <ThemeProvider mode="light">
      <Combobox {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Combobox') ?? screen.UNSAFE_root,
    label: () => screen.queryByRole('combobox') ?? s.root(),
    chipRemove: () => screen.queryByTestId('Combobox.chipRemove') ?? s.root(),
    clearButton: () => screen.queryByTestId('Combobox.clearButton') ?? s.root(),
    toggleButton: () => screen.queryByTestId('Combobox.toggleButton') ?? s.root(),
    rerender: (next: Partial<ComboboxProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Combobox', () => {
  test('typing-reports-the-input-text', () => {
    const s = setup({"open": false});
    fireEvent.changeText(s.label(), "ap");
    expect(s.events.onInputChange).toHaveBeenCalled();
  });
  test('the-toggle-button-opens-the-list', () => {
    const s = setup({"open": false});
    fireEvent.press(activatable(s.toggleButton(), s.root()));
    expect(s.events.onOpenChange).toHaveBeenCalled();
  });
  test('a-closed-combobox-is-not-expanded', () => {
    const s = setup({"open": false});
    expect(s.label()).not.toBeExpanded();
  });
  test('an-open-list-reports-the-expanded-state', () => {
    const s = setup({"open": true});
    expect(s.label()).toBeExpanded();
  });
  test('the-clear-button-clears-the-value', () => {
    const s = setup({"open": false, "defaultValue": "apple", "clearable": true});
    fireEvent.press(activatable(s.clearButton(), s.root()));
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('multiple-shows-the-selection-as-chips', () => {
    const s = setup({"open": false, "multiple": true, "defaultValue": ["apple"]});
    expect(screen.getByText(new RegExp("Apple"))).toBeOnTheScreen();
  });
  test('removing-a-chip-reports-the-new-value', () => {
    const s = setup({"open": false, "multiple": true, "defaultValue": ["apple"]});
    fireEvent.press(activatable(s.chipRemove(), s.root()));
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('a-disabled-combobox-does-not-open', () => {
    const s = setup({"open": false, "disabled": true});
    fireEvent.press(activatable(s.toggleButton(), s.root()));
    expect(s.events.onOpenChange).not.toHaveBeenCalled();
  });
  test('renders', () => {
    const s = setup({"open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-filter-starts-with', () => {
    const s = setup({"filter": "startsWith", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-filter-contains', () => {
    const s = setup({"filter": "contains", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-filter-none', () => {
    const s = setup({"filter": "none", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-filter-async', () => {
    const s = setup({"filter": "async", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({"open": true});
    expect(screen.getByRole('combobox', { name: s.props.label })).toBeOnTheScreen();
  });
  test('error-is-identified', () => {
    const s = setup({"error": "Fix this before continuing.", "open": true});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeOnTheScreen();
  });
});
