// Generated by tools/behavior_tests.ts from the `behavior` block of the Combobox doc. Do not edit.
import { beforeEach, describe, expect, test, vi } from 'vitest';
import { userEvent } from 'vitest/browser';
import '../../packages/lit/src/Combobox.js';
import meta from '../../packages/lit/src/Combobox.stories.js';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function deep(root: ParentNode, selector: string): Element | null {
  const direct = root.querySelector(selector);
  if (direct) return direct;
  for (const el of Array.from(root.querySelectorAll('*'))) {
    const shadow = (el as HTMLElement).shadowRoot;
    if (shadow) {
      const found = deep(shadow, selector);
      if (found) return found;
    }
  }
  return null;
}

/** Every element that is "active" from the document down through shadow roots: the host, then the inner one.
 *  document.activeElement alone is a shadow host while focus sits inside its shadow tree. */
function activeChain(): Element[] {
  const chain: Element[] = [];
  let el: Element | null = document.activeElement;
  while (el) {
    chain.push(el);
    el = el.shadowRoot?.activeElement ?? null;
  }
  return chain;
}

async function setup(given: Record<string, unknown> = {}) {
  const el = document.createElement('ds-combobox');
  const props = { ...meta.args, ...given };
  for (const [key, value] of Object.entries(props)) {
    if (value === undefined) continue;
    // `children` is slotted light-DOM content, and `Element.children` is getter-only:
    // assigning it throws and takes the whole file down at setup. Slot a string into the
    // default slot the way the stories do; a richer value cannot be expressed here.
    if (key === 'children') {
      if (typeof value === 'string') el.append(value);
      continue;
    }
    (el as unknown as Record<string, unknown>)[key] = value;
  }
  const events = {
    onChange: vi.fn(),
    onInputChange: vi.fn(),
    onOpenChange: vi.fn(),
  };
  el.addEventListener('change', events.onChange as unknown as EventListener);
  el.addEventListener('input-change', events.onInputChange as unknown as EventListener);
  el.addEventListener('open-change', events.onOpenChange as unknown as EventListener);
  document.body.append(el);
  await (el as unknown as { updateComplete: Promise<boolean> }).updateComplete;
  const root: ParentNode = el.shadowRoot ?? el;  // some elements render in light DOM
  const s = {
    el,
    root,
    events,
    props,
    root_: () => el,
    label: () => (deep(root, '[role="combobox"]') ?? deep(root, '[part="label"]') ?? deep(root, '[data-part="label"]') ?? root.firstElementChild) as HTMLElement,
    chipRemove: () => (deep(root, '[part="chipRemove"]') ?? deep(root, '[data-part="chipRemove"]')) as HTMLElement,
    clearButton: () => (deep(root, '[part="clearButton"]') ?? deep(root, '[data-part="clearButton"]')) as HTMLElement,
    toggleButton: () => (deep(root, '[part="toggleButton"]') ?? deep(root, '[data-part="toggleButton"]')) as HTMLElement,
  };
  return s;
}

beforeEach(() => {
  document.body.replaceChildren();
});

describe('ds-combobox', () => {
  test('typing-reports-the-input-text', async () => {
    const s = await setup({"open": false});
    await userEvent.type(s.label(), "ap");
    expect(s.events.onInputChange).toHaveBeenCalled();
  });
  test('the-toggle-button-opens-the-list', async () => {
    const s = await setup({"open": false});
    await userEvent.click(s.toggleButton());
    expect(s.events.onOpenChange).toHaveBeenCalled();
  });
  test('a-closed-combobox-is-not-expanded', async () => {
    const s = await setup({"open": false});
    expect(s.label()).toHaveAttribute('aria-expanded', 'false');
  });
  test('an-open-list-reports-the-expanded-state', async () => {
    const s = await setup({"open": true});
    expect(s.label()).toHaveAttribute('aria-expanded', 'true');
  });
  test('enter-commits-the-active-option', async () => {
    const s = await setup({"open": true});
    s.el.focus();
    await userEvent.keyboard('{Enter}');
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('escape-closes-the-list', async () => {
    const s = await setup({"open": true});
    s.el.focus();
    await userEvent.keyboard('{Escape}');
    expect(s.events.onOpenChange).toHaveBeenCalled();
  });
  test('the-clear-button-clears-the-value', async () => {
    const s = await setup({"open": false, "defaultValue": "apple", "clearable": true});
    await userEvent.click(s.clearButton());
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('multiple-shows-the-selection-as-chips', async () => {
    const s = await setup({"open": false, "multiple": true, "defaultValue": ["apple"]});
    expect(s.el.shadowRoot!.textContent).toMatch(new RegExp("Apple"));
  });
  test('removing-a-chip-reports-the-new-value', async () => {
    const s = await setup({"open": false, "multiple": true, "defaultValue": ["apple"]});
    await userEvent.click(s.chipRemove());
    expect(s.events.onChange).toHaveBeenCalled();
  });
  test('a-disabled-combobox-does-not-open', async () => {
    const s = await setup({"open": false, "disabled": true});
    await userEvent.click(s.toggleButton(), { force: true });
    expect(s.events.onOpenChange).not.toHaveBeenCalled();
    expect(s.label()).toHaveAttribute('aria-disabled', 'true');
  });
  test('renders', async () => {
    const s = await setup({"open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-filter-starts-with', async () => {
    const s = await setup({"filter": "startsWith", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-filter-contains', async () => {
    const s = await setup({"filter": "contains", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-filter-none', async () => {
    const s = await setup({"filter": "none", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('renders-filter-async', async () => {
    const s = await setup({"filter": "async", "open": true});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('has-accessible-name', async () => {
    const s = await setup({"open": true});
    expect(s.label()).toHaveAccessibleName(s.props.label);
  });
  test('control-is-focusable', async () => {
    const s = await setup({"open": true});
    s.el.focus();
    expect(activeChain()).toContain(s.el);
  });
  test('error-is-identified', async () => {
    const s = await setup({"error": "Fix this before continuing.", "open": true});
    expect(s.el.shadowRoot!.textContent).toMatch(new RegExp("Fix\\ this\\ before\\ continuing\\."));
    expect(s.label()).toHaveAttribute('aria-invalid', 'true');
  });
});
