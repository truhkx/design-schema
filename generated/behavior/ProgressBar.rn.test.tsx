// Generated by tools/behavior_tests.ts from the `behavior` block of the ProgressBar doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { ProgressBar } from '../../packages/rn/src/ProgressBar';
import type { ProgressBarProps } from '../../packages/rn/src/ProgressBar';
import meta from '../../packages/rn/src/ProgressBar.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<ProgressBarProps> = {}) {
  const events = {};
  const props: ProgressBarProps = { ...(meta.args as ProgressBarProps), ...given };
  const tree = (p: ProgressBarProps) => (
    <ThemeProvider mode="light">
      <ProgressBar {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('ProgressBar') ?? screen.UNSAFE_root,
    container: () => screen.queryByRole('progressbar') ?? s.root(),
    rerender: (next: Partial<ProgressBarProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('ProgressBar', () => {
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-neutral', () => {
    const s = setup({"tone": "neutral"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-success', () => {
    const s = setup({"tone": "success"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-tone-danger', () => {
    const s = setup({"tone": "danger"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-announce-none', () => {
    const s = setup({"announce": "none"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-announce-milestones', () => {
    const s = setup({"announce": "milestones"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-announce-complete', () => {
    const s = setup({"announce": "complete"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('progressbar', { name: s.props.label })).toBeOnTheScreen();
  });
});
