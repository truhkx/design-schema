# Gaps reported while generating Stepper for rn

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 19:18 — round 1

- Stepper: schema has one `transition` binding but four discrete indicator states (complete/current/upcoming/error) plus a connector; animating all of it unambiguously wasn't specified, so only the connector cross-fades between `connector` and `connectorComplete` over `transition` — the indicator itself switches instantly, same as this package's other multi-state (not binary) indicators.
- Stepper: when a step's explicit `status: 'error'` overrides the position that equals `current`, the doc doesn't say whether accessibility 'selected' state and the compact single-label reveal should follow the id match or the derived status. Chose id match (`step.id === current`) for `accessibilityState.selected`/compact-reveal, and the derived `status` for indicator color/icon and the status word (so 'has an error' wins over 'current step' in that edge case).
- Stepper: automatic `compact` on narrow viewports reuses `layout.maxWidth.prose` per the platform notes, but the exact comparison (`<` vs `<=`) isn't specified; used `windowWidth < t.layoutMaxWidthProse`, matching Table/Tabs/Menu/Select's existing pattern in this package.
- Stepper: horizontal connector placement is approximated with a fixed `marginTop` (indicatorSize/2 - connectorWidth/2) since the connector is a decorative sibling, not a measured/absolute-positioned element; there's no CSS-cascade equivalent on native to align it exactly with the indicator's center for arbitrary label heights.
- Stepper: `stepHover` ('hover and press background of a navigable step') is applied only on Pressable's `pressed` state — there is no native hover, same acknowledged limit as Button/Tabs in this package.
- Stepper: the schema's anatomy lists `description` generally (not orientation-scoped), so it renders under the label in both `horizontal` (non-compact) and `vertical`, even though the guidance prose frames descriptions as mainly a vertical-stepper concern.
- Stepper: `copy.stepOf`/`copy.stepLabel` use `{n}`/`{total}`/`{label}` placeholder syntax in the doc; implemented as small formatting functions rather than literal template substitution, matching this package's existing convention for parameterized copy (e.g. RadioGroup's `COPY.required`).

## 2026-09-16 09:48 — round 1

- Stepper: no binding sets the ring color of a complete indicator (there are complete background/foreground, current border and error border, but no complete border). I used `indicatorBorder` (color.border.strong) around the `indicatorCompleteBackground` fill; `indicatorCompleteBackground` as the ring may be what was meant.
- Stepper: `connectorComplete` has no rule for when it applies. I used position (every connector after a step before `current`), so the connector after an errored step that was already passed still shows as complete; the old code keyed it on the preceding step's status being `complete`.
- Stepper: the web renders a `nav` landmark around the list, but React Native has no nav role. The rn platform props only list `accessibilityRole=list` + `accessibilityLabel`, so the label (`copy.navLabel`) goes on the list View and there is no landmark.
- Stepper: `compact` says hidden labels are 'clipped, not removed, so they stay reachable by a screen reader'. On native each step's accessibilityLabel already carries the label, so the visible label Text is simply not rendered. The doc should say whether that satisfies the rule on rn.
- Stepper: in compact mode it's unclear whether 'Step n of m' replaces the current step's description or appears as well. I render it in the description position, replacing the description.
- Stepper: automatic compact on rn compares `useWindowDimensions().width` against `layout.maxWidth.prose`, but the stepper's own width isn't measured (the web version uses a container query). A narrow stepper inside a wide window won't collapse.
- Stepper: the only stated rule for combining the status word with the label is 'copy.stepLabel plus the status word'. There's no separator in copy and no pattern for joining them, so I join with ', ' ("Step 2: Payment, current step", as in the Accessibility prose). The doc should give a template or a join rule.
- Stepper: the status word for an explicit `status: 'current'` on a step whose id isn't `current` is unspecified. I gave it copy.current but no selected state. Upcoming steps get no status word, and copy has none for them.
- Stepper: `indicatorFontSize` sizes the numeral, but nothing says what sizes the check/danger Icon. I pass Icon size `sm` and forward an `indicatorFontSize` override to Icon's `size` override.
- Stepper: anatomy part `label`/`description` are composed Text, which takes no testID; to expose `Stepper.label`/`Stepper.description` I wrapped each in a View carrying the testID.
- Stepper: `stepHover` has state `hover` only, but its description says 'Hover and press'. I apply it on Pressable pressed or hovered (onHoverIn/onHoverOut).
- Stepper: the behavior scenario says `click: indicator` without saying which step's indicator. The tests press the first indicator; since it is aria-hidden, the query needs `includeHiddenElements: true`, and the press bubbles up to the step's Pressable.
- Stepper: `compact` is a boolean, and naming stories `<Prop><Value>` gives `CompactTrue`; I kept the existing name. The Keyboard story uses navigable all / current review so four steps can take focus, because the keyboard block gives no `given`.

## 2026-09-17 12:44 — round 1

- Stepper: composition gives the label/description/count Text `element: span`, but the RN Text has no `element` prop; omitted.
- Stepper: platforms.rn makes the root View the list, yet `count` is 'one muted Text after the list'. On RN there is no outer nav to hold it, so the count sits inside the root list View after an inner layout View holding the steps; the spec does not say where it goes or what spaces it from the steps — chose a `gap` of `stepGap` (layout.gap.normal).
- Stepper: 'A composed part receives exactly the listed props' drops Text `align`, so a wrapped label in a horizontal step is start-aligned inside a centred column; the spec does not say how a horizontal label aligns.
- Stepper: the label's `weight` is not in the composed props, only forwarded fontWeight overrides; the Text default weight is regular, so the forwards are passed with their binding tokens as defaults ('font.weight.medium' / 'font.weight.semibold') rather than only when overridden.
- Stepper: indicatorCompleteForeground/indicatorErrorForeground are 'passed as the Icon's color override', but the RN digest says pass the foreground to Icon's `color` prop; followed the spec (overrides.color with the token ref).
- Stepper: stepPadding is 'block padding too when vertical', but connectors fill only `stepGap`, so a vertical connector does not span the padding above and below each step; the spec does not say whether the line reaches the neighbouring indicators.
- Stepper: automatic compact measures the stepper's own width, but if a parent shrink-wraps the stepper (alignItems center) switching to compact narrows it further; the spec gives no hysteresis or width source for that case.
- Stepper: stepper-level `fontFamily` has no root style on RN (View takes no fontFamily); it resolves for the indicator numeral and is forwarded as the ref to each Text only when overridden, relying on Text's own default otherwise.
- Stepper: the dev warning's text for an unmatched `current` is not in copy; wrote a developer-only message.

## 2026-09-21 16:43 — round 1

- Stepper: `fontFamily` is declared on the `list` part and described as "set on the root", but a React Native View carries no text style and there is no inheritance. I applied it to the indicator numeral (the only text Stepper draws itself) and forwarded it to every composed Text's `fontFamily` override; nothing is set on the root View.
- Stepper: `ref` — the schema's rn element is the View with accessibilityRole=list, but the notes put the count outside the list. I attached `ref` to the plain root View (list + count) rather than the list View, so the ref measures the whole component; the doc should say which one the ref is.
- Stepper: `minTarget` says "navigable step control", so non-navigable steps get no minWidth/minHeight. To keep the two kinds of step aligned in one list I still give every step the transparent `focusRingWidth` border; the doc does not say whether a display-only step should occupy the same box as a navigable one.
- Stepper: the focus ring is drawn as a border, which on RN is inside the box (no `outline`), so `focusRingWidth` permanently consumes that much of every step control and the connector geometry has to compensate for it. The doc describes a ring without saying inset or outset.
- Stepper: `stepGap`'s connector geometry is given as `calc(stepPadding + (indicatorSize - connectorWidth) / 2)`, which RN cannot express. I centre the line by making the connector track exactly `indicatorSize` on the cross axis, inset by the focus border (plus `stepPadding` on the vertical axis), with `boxSizing: 'content-box'`. Horizontal connectors flexGrow from a basis of `stepGap`; vertical ones stay a fixed `stepGap` tall, since the doc's "grows with free space" has no meaning in a column of auto-height steps.
- Stepper: behaviors `step-status-is-said-in-words` and `an-errored-step-says-so` are scoped to web+lit and so are absent from the derived rn scenario list, yet on RN the status word is the only status signal a screen reader gets (it is inside each step's accessibilityLabel). The doc should scope those two to rn as well, asserting the accessibilityLabel, or say explicitly why not.
- Stepper: the keyboard rule "Tab moves between navigable steps in order; non-navigable steps are not focusable" needs no rn code (react-native-web gives Pressables tab order, non-Pressable steps none) but is also unverifiable in Jest. I kept the `Keyboard` story (navigable: all) for the axe/manual pass and wrote no code for it.
- Stepper: RN has a `list` role but no `listitem` that pairs with it, so steps are `accessible` Views/Pressables with no item role and there is no "item 2 of 5" announcement — which is what `copy.stepLabel` compensates for. Worth stating in the rn notes so a later pass does not add a role that does not exist.
- Stepper: the doc does not say whether `compact: true` set by hand on a vertical stepper should still render the `count` Text. I read "the prop does nothing there" literally: vertical never renders the count.
- Stepper: the __DEV__ warning for an unmatched `current` is specified as "a developer-only English message, not copy", with no text given. I wrote `Stepper: current "<id>" matches no step id; nothing is selected.` — if the wording should match the web/Lit generators byte for byte, the doc needs to carry it.

## 2026-09-21 16:46 — round 2

- Stepper: `platforms.rn.props` names only `accessibilityRole=list`, and nothing in the rn notes says what the steps are inside it. On react-native-web a `role="list"` whose children are buttons fails axe `aria-required-children`, so each step had to become a `role="listitem"` View wrapping the control (the web `<li>` shape). RN's `AccessibilityRole` union has no `listitem`, so this must be the `role` prop; the doc should state the wrapper and which element carries `testID="Stepper.step"` (I put it on the listitem, since the doc calls the `step` part the list item).
- Stepper: on web the connector `<span aria-hidden>` lives inside the `<li>`; on RN it has to stay a sibling of the steps, because a horizontal connector must flex-grow in the list row and centre on the indicator's axis, which it cannot do from inside a step whose control is a column. It is hidden from the accessibility tree, so axe does not count it as an unowned child, but the anatomy differs from web and the doc should say so.
- Stepper: `fontFamily` is declared on the `list` part and described as "set on the root", but a React Native View carries no text style and there is no inheritance. I applied it to the indicator numeral (the only text Stepper draws itself) and forwarded it to every composed Text's `fontFamily` override; nothing is set on the root View.
- Stepper: `ref` — the schema's rn element is the View with accessibilityRole=list, but the notes put the count outside the list. I attached `ref` to the plain root View (list + count) rather than the list View, so the ref measures the whole component; the doc should say which one the ref is.
- Stepper: `minTarget` says "navigable step control", so non-navigable steps get no minWidth/minHeight. To keep the two kinds of step aligned in one list I still give every step control the transparent `focusRingWidth` border; the doc does not say whether a display-only step should occupy the same box as a navigable one.
- Stepper: the focus ring is drawn as a border, which on RN is inside the box (no `outline`), so `focusRingWidth` permanently consumes that much of every step control and the connector geometry has to compensate for it. The doc describes a ring without saying inset or outset.
- Stepper: `stepGap`'s connector geometry is given as `calc(stepPadding + (indicatorSize - connectorWidth) / 2)`, which RN cannot express. I centre the line by making the connector track exactly `indicatorSize` on the cross axis, inset by the focus border (plus `stepPadding` on the vertical axis), with `boxSizing: 'content-box'`. Horizontal connectors flexGrow from a basis of `stepGap`; vertical ones stay a fixed `stepGap` tall, since "grows with free space" has no meaning in a column of auto-height steps.
- Stepper: behaviors `step-status-is-said-in-words` and `an-errored-step-says-so` are scoped to web+lit and so are absent from the derived rn scenario list, yet on RN the status word is the only status signal a screen reader gets (it is inside each step's accessibilityLabel). The doc should scope those two to rn as well, asserting the accessibilityLabel, or say explicitly why not.
- Stepper: the keyboard rule "Tab moves between navigable steps in order; non-navigable steps are not focusable" needs no rn code (react-native-web gives Pressables tab order, non-Pressable steps none) and is unverifiable in Jest. I kept the `Keyboard` story (navigable: all) for the axe/manual pass and wrote no code for it.
- Stepper: RN has a `list` role but the step's accessible name gets no "item 2 of 5" from it — which is what `copy.stepLabel` compensates for. Worth stating in the rn notes so a later pass does not assume the listitem wrapper supplies the ordinal.
- Stepper: the doc does not say whether `compact: true` set by hand on a vertical stepper should still render the `count` Text. I read "the prop does nothing there" literally: vertical never renders the count.
- Stepper: the __DEV__ warning for an unmatched `current` is specified as "a developer-only English message, not copy", with no text given. I wrote `Stepper: current "<id>" matches no step id; nothing is selected.` — if the wording should match the web/Lit generators byte for byte, the doc needs to carry it.

## 2026-09-23 14:44 — round 1

- Stepper: the rn notes and conventions say to mirror accessibilityState.selected as aria-selected, but react-native-web 0.21 ignores accessibilityState entirely and aria-selected is not an allowed attribute on role=button (axe aria-allowed-attr). Chose aria-current="step" (matching the web contract) as the DOM mirror, passed via a Record spread because RN's View/Pressable types omit aria-current; the doc should name aria-current, not aria-selected, as Stepper's rn web mirror.
- Stepper: a display-only step is an `accessible` View with accessibilityLabel and no role; on react-native-web that is a role-less div with aria-label, which axe can flag as aria-prohibited-attr (reported as incomplete when the div has text content). The spec gives no role for the non-navigable control; kept it role-less as written. The doc should decide whether it takes a role (e.g. group) or drops the label on web.
- Stepper: the status-word scenarios (step-status-is-said-in-words, an-errored-step-says-so) are scoped to web/lit and the doc says the rn package's own tests assert the accessibilityLabel instead, but gives no scenario for it. Added two extra tests asserting 'Step n: <label>, <status word>' via getByLabelText; the doc could declare these as rn-scoped scenarios so they are generated rather than hand-added.
- Stepper: the 'click: indicator' scenarios target a part that is aria-hidden/importantForAccessibility=no on native, so the test must query it with includeHiddenElements and rely on the press bubbling to the step's Pressable; the scenarios would be more portable as 'click: step'.
- Stepper: the `indicatorFontSize` forward to the check/danger Icon's size override is a font-size token driving an icon box; the doc acknowledges it, but Icon's overrides type must accept a font.size.* TokenRef for `size`. It typechecks today only because TokenRef is the full token union.

## 2026-09-23 14:44 — round 1

- Stepper: `selected-state` has to be mirrored in the DOM, but the RN digest's mirror for `accessibilityState.selected` is `aria-selected`, which ARIA doesn't allow on a button (the navigable Pressable) or on an element with no role (the display-only View). I used `aria-current="step"` from the web contract instead, passed through an untyped prop object because RN's View/Pressable types have no `aria-current`. The rn notes should name the DOM mirror.
- Stepper: a display-only step is an `accessible` View with an `aria-label` and no role. On react-native-web that's a role-less div with `aria-label`, which ARIA 1.2 prohibits and axe may flag. The doc should say whether that step needs a role (e.g. `text`/`group`) or whether its name should come from content instead.
- Stepper: in vertical orientation the description sits inside a step whose `accessibilityLabel` is copy.stepLabel plus the status word. On native, an accessible element with a label reads only that label, so screen readers never hear the description. The web version pairs it through aria-describedby, but the rn notes give no equivalent (`accessibilityHint`? an `aria-describedby` via nativeID?). I added nothing, so descriptions are visual-only on RN.
- Stepper: the composed Text parts have `element: span`, but RN Text has no `element` prop, so it is omitted. The same note appears in earlier rounds and the rn notes still don't say to drop it.
- Stepper: the `Keyboard` story still uses navigable: all / current: review so four Pressables can take focus. The keyboard block has no `given`, so those args are my choice.
- Stepper: a boolean story named `<Prop><Value>` becomes `CompactTrue`; kept.
