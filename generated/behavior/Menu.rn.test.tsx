// Generated by tools/behavior_tests.ts from the `behavior` block of the Menu doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Menu } from '../../packages/rn/src/Menu';
import type { MenuProps } from '../../packages/rn/src/Menu';
import meta from '../../packages/rn/src/Menu.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function setup(given: Partial<MenuProps> = {}) {
  const events = {
    onAction: jest.fn(),
    onOpenChange: jest.fn(),
  };
  const props: MenuProps = { ...(meta.args as MenuProps), ...given, onAction: events.onAction, onOpenChange: events.onOpenChange };
  const tree = (p: MenuProps) => (
    <ThemeProvider mode="light">
      <Menu {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Menu') ?? screen.UNSAFE_root,
    trigger: () => screen.queryByRole('menu') ?? s.root(),
    rerender: (next: Partial<MenuProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Menu', () => {
  test('renders', () => {
    const s = setup({"open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-trigger-variant-ghost', () => {
    const s = setup({"triggerVariant": "ghost", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-trigger-variant-secondary', () => {
    const s = setup({"triggerVariant": "secondary", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-trigger-variant-primary', () => {
    const s = setup({"triggerVariant": "primary", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-trigger-icon-ellipsis', () => {
    const s = setup({"triggerIcon": "ellipsis", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-trigger-icon-chevron-down', () => {
    const s = setup({"triggerIcon": "chevron-down", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-trigger-icon-none', () => {
    const s = setup({"triggerIcon": "none", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-bottom-start', () => {
    const s = setup({"placement": "bottom-start", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-bottom-end', () => {
    const s = setup({"placement": "bottom-end", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-top-start', () => {
    const s = setup({"placement": "top-start", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('renders-placement-top-end', () => {
    const s = setup({"placement": "top-end", "open": true});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({"open": true});
    expect(screen.getByRole('menu', { name: s.props.label })).toBeOnTheScreen();
  });
});
