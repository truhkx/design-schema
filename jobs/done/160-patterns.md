Make pattern pages generatable, starting with site/src/content/docs/patterns/settings-page.md (read it and site/src/content/docs/process/generation-pipeline.md first):

1. tools/parse.py: read site/src/content/docs/patterns/*.md as a second doc kind. A pattern has no `component:` block; its contract is the "## Structure" code block (a tree of system components with props) plus the "## Behaviors the page must show" section. Validate that every component named in the structure exists in generated/components.json and every prop named exists on that component (report unknown ones as DocError with the pattern file). Emit generated/prompts/Pattern.<Name>.<platform>.md using a new prompts/templates/pattern-<platform>.md (write these three: they say the page lives in packages/<pkg>/demo/<Name>.tsx|ts, composes only system components, uses no tokens directly, no margins, a story `Patterns/<Name>`, and must be identical across themes with no theme branching; carry the same conventions digest as components).
2. tools/generate.py: accept `--pattern <Name>` (targets Pattern.<Name>.<platform>) with the same gates minus parse-of-self; keyboard/axe gates run against the `Patterns/<Name>` story when `--with` is given.
3. regen.ps1: add a final phase "Patterns" with `--pattern SettingsPage` (name from the doc's title with spaces removed).
4. tests/test_patterns.py: structure parsing, unknown component/prop errors, prompt emission.
Do not generate anything; do not modify packages/*/src or generated/ other than prompts. Run `pytest -q` and `node tools/py.mjs tools/parse.py`.
