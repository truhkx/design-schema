# Gaps reported while generating Slider for lit

Each entry is a place the doc made the generator guess. Fix the doc, re-run parse, regenerate.

## 2026-09-10 10:51 — round 1

- Slider: `step` has a schema default of 1, so a Lit property can't distinguish 'step omitted' from 'step explicitly 1' to satisfy 'marks... snap to marks when step is omitted' during pointer drag/click. Implemented: step always governs drag/click snapping; when `marks` is set, PageUp/PageDown jump to the next/previous mark instead of ±10 steps (that part of the spec is unambiguous).
- Slider: `value`/`defaultValue` are typed `number | [number, number]` (shape given but base `type: number`, not `array`/`object`). Implemented as `@property({ attribute: false })` per the array/object convention, so they are JS-only properties, not settable as HTML attributes — flag if attribute-level control is expected.
- Slider: range mode's aria-labelledby uses hidden spans whose text is the full `copy.minimumLabel`/`copy.maximumLabel` template evaluated with `label` (e.g. 'Volume minimum'), rather than concatenating the visible label element with a bare 'minimum' node. This satisfies both 'aria-labelledby on each thumb' and 'use copy.* verbatim', but the doc doesn't show the exact DOM shape, so it's a judgment call.
- Slider: no format is specified for the beside-label/bubble text of a range's *combined* display (each thumb's own aria-valuetext/bubble is per-thumb and unambiguous). Chose `formatValue(lo) – formatValue(hi)` (en dash) for the `showValue: always` row; not in the spec.
- Slider: `ds-form` (Form.ts) collects fields via a hardcoded `FIELD_SELECTOR`/`FIELD_TAGS` list (`ds-input, ds-checkbox, ds-switch, ds-radio-group`) that does not include `ds-slider`, and `DsFormField.currentValue` is typed `string | boolean | null` vs. Slider's `number | [number, number]`. Slider is form-associated via `ElementInternals` directly (native `<form>` sees it, `FormData` gets two entries under `name` for a range), but `<ds-form>` won't auto-collect/validate it without a separate change to Form.ts, which is out of this generation's scope.
- Slider: schema has no `required` prop (unlike Input/RadioGroup), so it doesn't fully implement `DsFormField` (missing `required`). Kept the schema's prop set exactly; didn't invent a `required` prop.
- Slider: the bubble's corner radius (`radius.sm`) has no declared style binding in the schema (only `bubbleSurface`/`bubbleText` are defined) — used a fixed, non-overridable `--radius-sm` purely for shape, matching Tooltip's popup shape.
- Slider: pointer-drag value math (`positionToValue`) is LTR-only (`clientX - rect.left`), matching the pattern of other components in this package that don't special-case `:dir(rtl)` for pointer math (only Switch handles RTL, purely via a CSS transform on a discrete thumb, not continuous drag).
- Slider: the package-wide testability rule requires a `Keyboard` story with 'at least three focusable children' — Slider's `range` mode tops out at two thumbs (its whole APG model), so the `Keyboard` story renders the range+marks configuration with two thumbs; a third focusable child isn't structurally possible for this component.

## 2026-09-10 18:40 — round 1

- Slider: platforms.web.attributes doesn't list aria-required, but copy.required implies a required state must be identified — added aria-required on each thumb to match the pattern used by other required form fields (Input, RadioGroup) in this package.
- Slider: 'must have a value other than the default to submit' doesn't define what 'the default' is when defaultValue is unset — chose the same fallback the value prop itself documents (min, or [min, max]) so required and value share one notion of default.
- Slider: platforms.lit.reflect lists only range/disabled/show-value, so required and snapToMarks are plain (non-reflected) boolean properties, consistent with the explicit list rather than the broader convention some other fields use.

## 2026-09-16 09:16 — round 1

- Slider: form.valueType is number-range and the doc says a Form value is a number or [low, high], but the Lit DsFormField interface types currentValue as string | boolean | null; chose currentValue: number | [number, number] and did not declare `implements DsFormField` (ds-form collects it at runtime by data-ds-field).
- Slider: `marks` says 'Values snap to marks when `step` is omitted' while `step` has `default: 1`, so omission is undetectable from the default; chose a getter/setter that tracks whether step was set, snapping drag/click to marks when it was not (keys still use 1).
- Slider: the marks shape `{ value: number; label?: string }[]` used verbatim conflicts with exactOptionalPropertyTypes (a `label: undefined` entry is rejected); typed it as the exported SliderMark[] with `label?: string | undefined`.
- Slider: `errorText` is overridable, but errorMessage is not in `composition` and the Lit convention says error text is Text, whose colour binding is locked; rendered ds-text tone=danger inside the role=alert errorMessage part and routed the hook by re-scoping --color-foreground-danger on that wrapper (the bubble does the same for bubbleText via --color-foreground). The doc should say how a parent's colour binding reaches a composed Text.
- Slider: `mark`, `markSize`, `markLabelColor`, `markLabelSize`, `valueColor`, `valueSize`, `partGap`, `fontFamily`, `fontSize`, `helperSize`, `errorText`, `minTarget`, `focusRing`, `focusRingWidth`, `disabledOpacity` and `transition` have no `part`; applied them to tickMarks, valueText, the label row, host, description/error text, the thumb hit area, the thumb's focus ring and the track area by judgment.
- Slider: the bubble has no position or padding tokens (offset above the thumb, inline/block padding); chose thumbSize above the track centre and space.1/space.2 padding.
- Slider: no token says how far mark labels sit below the track or how much room they need; used trackPaddingBlock for both and add markLabelSize when any mark has a label.
- Slider: a11y.requires lists label-association, but the thumbs are divs, which cannot take a native <label for>; the Lit convention asks for a native label on fields, yet composition says label is Text. Used a ds-text label with an id and aria-labelledby on the thumb (a range points at hidden minimum/maximum copy spans).
- Slider: the label is Text, but no Text size is named for it (fontSize is font.size.md with labelWeight medium); used size=md weight=medium and forwarded fontFamily/fontSize/labelWeight to it.
- Slider: `required` gives no validity flag, and validation order (required, then invalid) differs from which message the error region shows; valueMissing carries copy.required, but the visible region shows only `error` or copy.invalid, never copy.required, until a Form reports it.
- Slider: 'disabled sliders are readable and focusable but inert' is not stated for a slider disabled by a form or fieldset (formDisabledCallback); treated both the same and submit no value while disabled.
- Slider: whether change-end fires when a key or drag leaves the value unchanged (at a bound) is not stated; it fires once per interaction regardless.
- Slider: the Keyboard story's Tab rule targets a range, but the doc gives no range args for it; used the price-range example's args (range, defaultValue [20, 80]).

## 2026-09-17 12:20 — round 1

- Slider: the error region should show 'the message a Form (or validate) has reported', but Lit ds-form keeps errors in its own summary and never sets a field's error or calls reportValidity(); chose to show the message from the field's own last reportValidity(), so a Form-reported copy.required never reaches the Slider's error region on Lit.
- Slider: Behavior says validate: blur validates when an interaction ends (pointer release, key-up), but ds-form only validates on focusout (or on every `change` for data-ds-field="change"); there is no change-end hook in the form contract. Kept data-ds-field="" (focusout validation).
- Slider: errorText is listed as overridable yet described as 'an override of it has no effect'; kept the --ds-slider-error-text hook and type entry but it styles nothing. Either lock it or say how it reaches the danger Text.
- Slider: the error setter also sets invalid = Boolean(error) (the package's field convention and the error-is-identified test), so clearing `error` clears an `invalid` the consumer set separately; the doc does not say whether error and invalid are independent.
- Slider: the doc does not say whether the bubble (showValue: hover) is removed or only hidden when inactive; to let the `transition` binding animate its appearance it stays rendered (aria-hidden) at opacity 0 and fades in while the thumb is pressed or focused.
- Slider: markLabelGap measures from 'the bottom of the track area', but the dots must sit on the track centre line inside the same tickMarks part; chose a tickMarks block after the track area with the dots positioned back up by trackPaddingBlock + trackHeight/2 and the labels in flow below, padded by markLabelGap (matching web).
- Slider: a controlled slider whose owner never updates `value` re-emits `change` with the same target on every pointermove and key repeat, because changes are compared against the displayed value; the doc's 'only when the value actually changed' is ambiguous for controlled mode.
- Slider: `currentValue` is null while disabled so Form submits nothing ('no value is submitted'); the doc does not say whether currentValue itself or only submission should be empty.

## 2026-09-21 15:22 — round 1

- Slider: `value` and `defaultValue` are unions (`number | [number, number]`), which no attribute converter can express unambiguously — both are `attribute: false` properties (`.value` / `.defaultValue`), so plain-HTML authors cannot write `<ds-slider default-value="30">` the way NumberInput allows `default-value`. The doc should say whether a scalar attribute form (and a syntax for a pair) is wanted.
- Slider: the composition table makes `errorMessage` the Text itself, while the lit platform note puts the Text 'inside the role=alert errorMessage wrapper'. Chose the Text carrying `part`/`data-part="errorMessage"` inside an unparted `<div role="alert">` — matching Input/NumberInput, where the part is the Text. Same question for `label`, `valueText` and `description`, which the web note also describes as wrappers.
- Slider: `invalid`'s description says setting or clearing `error` never changes the `invalid` prop, which contradicts both the package convention ('setting `error` updates `invalid` and validity at once') and the derived scenario `error-is-identified`, whose `state: invalid is true` is how every other field asserts `el.invalid === true`. Followed the schema: `error` leaves the prop alone, `aria-invalid` is true when either is set, and the test asserts `aria-invalid="true"` on the thumb instead of the prop.
- Slider: `error` is not in `form.validation` (`required`, `invalid`), but a field showing an error message must not submit. Chose `setValidity({ customError: true }, error)` with precedence error → required → invalid, mirroring NumberInput.
- Slider: `trackPaddingBlock` (space.3 = 12px) cannot contain the 44px `minTarget`, so each thumb's hit area overflows the track area by ~8px top and bottom and overlaps the label row and the mark-label row. Presses on the mark labels are safe (they paint above the thumbs and the pointer handler is on the track area only, as the doc requires), but a press on the label row directly above a thumb still grabs it. Either minTarget or trackPaddingBlock needs to give.
- Slider: the doc describes one bubble on the active thumb, but a range has two thumbs. Rendered one bubble per thumb, each shown only while its own thumb is pressed or focused. The doc also says only the *inactive* bubble is aria-hidden; the active one is aria-hidden here too, since the thumb's `aria-valuetext` already announces the same value and an announced duplicate would double-speak.
- Slider: the keyboard table gives no right-to-left rule. ArrowRight/ArrowUp increase in both directions (implemented exactly as listed) even though 'pointer math is logical (mirrored in right-to-left)' — so in RTL the pointer and the arrow keys disagree about which end of the track is 'more'. APG leaves this to the author; the doc should pick.
- Slider: `snapToMarks: true` with no `marks` is undefined — PageUp/PageDown fall back to ten steps and drag to the step grid, i.e. the flag is inert. Assumed that is right, since the flag is documented as 'with `marks`'.
- Slider: snapping anchors the step grid at `min` (`min + round((raw - min)/step) * step`), so when `(max - min)` is not a whole number of steps, `max` is unreachable by drag or click and only End reaches it. The doc does not say whether the last partial step should snap to `max`.
- Slider: 'a consumer-supplied `id` lands there too' has no Lit reading — the consumer's id is on the host, not on a thumb. Chose: the low thumb's shadow-root id is the host's `id` when one is set and `thumb` otherwise, and the high thumb carries none; `focus()` reaches the low thumb through `delegatesFocus` either way.
- Slider: `formStateRestoreCallback` is not specified. Restores a single value from the string and a range from two same-name FormData entries, matching what `setFormValue` submits.
- Slider: `partGap`, `disabledOpacity` and `transition` carry no `part` in the styles table; applied them to the unparted root wrapper (grid gap, opacity, and the halo/bubble transition duration respectively).

## 2026-09-21 15:28 — round 2

- Slider: UNFIXABLE FROM THE COMPONENT — the keyboard gate's Tab rule. tools/keyboard_tests.ts builds the gate root as page.getByRole('<a11y.role>').first(), assuming the role is on the component root, but Slider's role is on the `thumb` part, so the root IS the low thumb: 0 focusable descendants and focusIndex -1 forever. Measured with the gate's own helpers rooted at [data-ds="Slider"]: 2 focusables, before=0 ('Price range minimum'), afterTab=1 ('Price range maximum'), afterShiftTab=0 — the rule's behaviour is correct. The only shape that redirects the locator is role="slider" on a container wrapping both thumbs, which is nested-interactive (axe wcag2a) and breaks the `the-thumb-is-the-slider` scenario. Fix in the doc: give the Tab rule `expect: manual` with `native: true` (Tab between two tab stops is browser behaviour); or fix in the tool: fall back to [data-ds="<Name>"] when the resolved role belongs to an anatomy part rather than the root. Same conflict recorded for web.
- Slider: `disabledOpacity` dims the whole root (label row, value text, marks, messages), but the doc lists `aria-disabled` only among the thumb's attributes. Dimmed text that no disabled control references via aria-labelledby then fails axe color-contrast even though WCAG 1.4.3 exempts inactive components — the value text is the one node that fails in light mode. Chose to also set aria-disabled="true" on the shadow root wrapper (the element that dims) so the exemption is machine-visible. The doc should either say this or say the value text keeps full opacity; the trap applies to every component whose disabledOpacity dims text.
- Slider: `value` and `defaultValue` are unions (`number | [number, number]`), which no attribute converter can express unambiguously — both are `attribute: false` properties (`.value` / `.defaultValue`), so plain-HTML authors cannot write `<ds-slider default-value="30">` the way NumberInput allows `default-value`. The doc should say whether a scalar attribute form (and a syntax for a pair) is wanted.
- Slider: the composition table makes `errorMessage` the Text itself, while the lit platform note puts the Text 'inside the role=alert errorMessage wrapper'. Chose the Text carrying part/data-part="errorMessage" inside an unparted `<div role="alert">` — matching Input/NumberInput, where the part is the Text. The same question applies to `label`, `valueText` and `description`, which the web note also describes as wrappers.
- Slider: `invalid`'s description says setting or clearing `error` never changes the `invalid` prop, contradicting both the package convention ('setting `error` updates `invalid` and validity at once') and the derived scenario `error-is-identified`, whose `state: invalid is true` is how every other field asserts `el.invalid === true`. Followed the schema: `error` leaves the prop alone, aria-invalid is true when either is set, and the test asserts aria-invalid="true" on the thumb instead of the prop.
- Slider: `error` is not in `form.validation` (`required`, `invalid`), but a field showing an error message must not submit. Chose setValidity({ customError: true }, error) with precedence error → required → invalid, mirroring NumberInput.
- Slider: `trackPaddingBlock` (space.3) cannot contain the 44px `minTarget`, so each thumb's hit area overflows the track area and overlaps the label row. Presses on the mark-label row are safe (it paints above the thumbs and the pointer handler is on the track area only, as the doc requires), but a press on the label row directly above a thumb still grabs it. Either minTarget or trackPaddingBlock needs to give.
- Slider: the doc describes one bubble on the active thumb, but a range has two. Rendered one bubble per thumb, each shown only while its own thumb is pressed or focused. The doc also says only the *inactive* bubble is aria-hidden; the active one is aria-hidden here too, since the thumb's aria-valuetext already announces the same value.
- Slider: the keyboard table gives no right-to-left rule. ArrowRight/ArrowUp increase in both directions (implemented exactly as listed) even though 'pointer math is logical (mirrored in right-to-left)', so in RTL the pointer and the arrow keys disagree about which end is 'more'. APG leaves this to the author; the doc should pick.
- Slider: `snapToMarks: true` with no `marks` is undefined — PageUp/PageDown fall back to ten steps and drag to the step grid, i.e. the flag is inert. Assumed that is right, since the flag is documented as 'with `marks`'.
- Slider: snapping anchors the step grid at `min` (`min + round((raw - min)/step) * step`), so when `(max - min)` is not a whole number of steps, `max` is unreachable by drag or click and only End reaches it. The doc does not say whether the last partial step should snap to `max`.
- Slider: 'a consumer-supplied `id` lands there too' has no Lit reading — the consumer's id is on the host, not on a thumb. Chose: the low thumb's shadow-root id is the host's `id` when one is set and `thumb` otherwise, the high thumb carries none; focus() reaches the low thumb through delegatesFocus either way.
- Slider: `formStateRestoreCallback` is not specified. Restores a single value from the string and a range from two same-name FormData entries, matching what setFormValue submits.
- Slider: `partGap`, `disabledOpacity` and `transition` carry no `part` in the styles table; applied them to the unparted root wrapper (grid gap, opacity, and the halo/bubble transition duration).

## 2026-09-21 15:32 — round 3

- Slider: UNFIXABLE FROM THE COMPONENT — the keyboard gate's Tab rule, unchanged in round 3 (no code edit this round). tools/keyboard_tests.ts:89 roots the gate at page.getByRole('<a11y.role>').first(), assuming the role sits on the component root, but Slider's role is on the `thumb` part, so root IS the low thumb: 0 focusable descendants and focusIndex -1 forever. Measured on lit with the gate's own helpers: getByRole('slider') matches 2 (the thumbs), 0 focusables inside .first(), 2 inside [data-ds="Slider"], where before=0 ('Price range minimum'), afterTab=1 ('Price range maximum'), afterShiftTab=0 — the behaviour is correct. Both redirecting shapes were measured too: role="slider" on the host and on the track each make the locator match 3 with 2 focusables inside, and each adds nested-interactive + aria-required-attr + aria-input-field-name (wcag2a), failing the axe gate that currently passes. Fix in the doc: `expect: manual` + `native: true` on the Tab rule. Or in the tool: rootLocator() already falls back to [data-ds="<Name>"] for NON_QUERYABLE_ROLES (schema/component.ts:323) — that fallback must also cover a role that belongs to an anatomy part rather than the root. Both files are outside this job's writable set.
- Slider: `disabledOpacity` dims the whole root (label row, value text, marks, messages), but the doc lists `aria-disabled` only among the thumb's attributes. Dimmed text that no disabled control references via aria-labelledby then fails axe color-contrast even though WCAG 1.4.3 exempts inactive components — the value text was the single failing node in light mode. Fixed in round 2 by also setting aria-disabled="true" on the shadow root wrapper (the element that dims), which axe's isDisabled resolves up the ancestor chain. The doc should either say this or say the value text keeps full opacity; the trap applies to every component whose disabledOpacity dims text.
- Slider: `value` and `defaultValue` are unions (`number | [number, number]`), which no attribute converter can express unambiguously — both are `attribute: false` properties (`.value` / `.defaultValue`), so plain-HTML authors cannot write `<ds-slider default-value="30">` the way NumberInput allows `default-value`. The doc should say whether a scalar attribute form (and a syntax for a pair) is wanted.
- Slider: the composition table makes `errorMessage` the Text itself, while the lit platform note puts the Text 'inside the role=alert errorMessage wrapper'. Chose the Text carrying part/data-part="errorMessage" inside an unparted `<div role="alert">` — matching Input/NumberInput, where the part is the Text. The same question applies to `label`, `valueText` and `description`, which the web note also describes as wrappers.
- Slider: `invalid`'s description says setting or clearing `error` never changes the `invalid` prop, contradicting both the package convention ('setting `error` updates `invalid` and validity at once') and the derived scenario `error-is-identified`, whose `state: invalid is true` is how every other field asserts `el.invalid === true`. Followed the schema: `error` leaves the prop alone, aria-invalid is true when either is set, and the test asserts aria-invalid="true" on the thumb instead of the prop.
- Slider: `error` is not in `form.validation` (`required`, `invalid`), but a field showing an error message must not submit. Chose setValidity({ customError: true }, error) with precedence error → required → invalid, mirroring NumberInput.
- Slider: `trackPaddingBlock` (space.3) cannot contain the 44px `minTarget`, so each thumb's hit area overflows the track area and overlaps the label row. Presses on the mark-label row are safe (it paints above the thumbs and the pointer handler is on the track area only, as the doc requires), but a press on the label row directly above a thumb still grabs it. Either minTarget or trackPaddingBlock needs to give.
- Slider: the doc describes one bubble on the active thumb, but a range has two. Rendered one bubble per thumb, each shown only while its own thumb is pressed or focused. The doc also says only the *inactive* bubble is aria-hidden; the active one is aria-hidden here too, since the thumb's aria-valuetext already announces the same value.
- Slider: the keyboard table gives no right-to-left rule. ArrowRight/ArrowUp increase in both directions (implemented exactly as listed) even though 'pointer math is logical (mirrored in right-to-left)', so in RTL the pointer and the arrow keys disagree about which end is 'more'. APG leaves this to the author; the doc should pick.
- Slider: `snapToMarks: true` with no `marks` is undefined — PageUp/PageDown fall back to ten steps and drag to the step grid, i.e. the flag is inert. Assumed that is right, since the flag is documented as 'with `marks`'.
- Slider: snapping anchors the step grid at `min` (`min + round((raw - min)/step) * step`), so when `(max - min)` is not a whole number of steps, `max` is unreachable by drag or click and only End reaches it. The doc does not say whether the last partial step should snap to `max`.
- Slider: 'a consumer-supplied `id` lands there too' has no Lit reading — the consumer's id is on the host, not on a thumb. Chose: the low thumb's shadow-root id is the host's `id` when one is set and `thumb` otherwise, the high thumb carries none; focus() reaches the low thumb through delegatesFocus either way.
- Slider: `formStateRestoreCallback` is not specified. Restores a single value from the string and a range from two same-name FormData entries, matching what setFormValue submits.
- Slider: `partGap`, `disabledOpacity` and `transition` carry no `part` in the styles table; applied them to the unparted root wrapper (grid gap, opacity, and the halo/bubble transition duration).

## 2026-09-22 01:47 — round 1

- Slider: the doc says the error region shows `error`, else the message a Form (or `validate`) has reported, else `copy.invalid` — but the Lit form contract has no channel for ds-form to push a message back into a field, so the middle case is unreachable. I render `error` then `copy.invalid`, and `copy.required` reaches consumers only through `validationMessage` / `setValidity`, never as visible text.
- Slider: `name` says a consumer-supplied `id` 'lands on' the low thumb, but the lit platform note says a consumer's `id` stays on the host and never enters the shadow root (the low thumb's id is always `thumb`). These contradict; I followed the platform note, so on Lit an outside `aria-describedby`/`label for` cannot reference the thumb directly.
- Slider: `label` and `name` are `required: true`, but a Lit property cannot be required and the doc only asks to warn on `max <= min`. I defaulted both to '' and emit no warning when they are missing, so a label-less slider silently ships with an empty accessible name.
- Slider: `value`/`defaultValue` are called out as `attribute: false` unions, but `formatValue` and `marks` have exactly the same problem and the doc is silent about them. I made both `attribute: false` too, so a plain-HTML author can set neither.
- Slider: with `snapToMarks`, PageUp/PageDown 'go to the next mark, and past the last mark to max/min'. The doc does not say what happens when the value already sits past the last mark in that direction; I return the bound (so the key is a no-op once there).
- Slider: `snapToMarks` says arrow keys 'still move by step', so with marks an arrow key can land the value off every mark, and the next drag snaps it back to a mark. Implemented literally, but this makes keyboard and pointer reach different value sets — worth confirming it is intended.
- Slider: `required` compares the resolved value against the default purely by value, so a controlled slider re-bound to its default becomes 'missing' again after having been moved. The doc does not say whether required should latch once satisfied.
- Slider: `showValue: hover` says any focus counts and the bubble shows while pressed or focused, but does not say which wins when a drag on one thumb continues after focus has moved. I show the bubble when a thumb is pressed OR focused, so during a drag two bubbles can be visible at once on a range.
- Slider: `thumbActiveScale` is bound to `opacity.disabled` and its own description says it is not a scale. Reusing the disabled-opacity token for the pressed-thumb halo means any theme that retunes disabled dimming silently retunes the halo; the binding wants its own token and a name that is not 'scale'.
- Slider: the SwiftUI notes reference a `largeStep` (Shift-step) that exists in no prop, no keyboard rule and no constant. I implemented no large step on Lit; either the prop is missing from the schema or the SwiftUI note is stale.

## 2026-09-23 15:11 — round 1

- Slider: `markLabelSize` is described as forwarded to each mark label Text's fontSize only (so, by the overrides rule, no hook of its own), but `markLabelGap` says the slider grows by markLabelSize × font.lineHeight.normal. The mark labels are absolutely positioned, so that row height has to read the value somewhere: I kept a `--ds-slider-mark-label-size` hook for the height and also forward the value to the Texts. The doc should say whether this binding declares a hook.
- Slider: the web platform note only says 'the track click moves the nearest thumb'. That a press inside a thumb's own hit area grabs that thumb and drags from its current value comes from the RN note and the onChange rule 'a click on the thumb fires nothing'. The tie rule (before → low, after → high, on → low) says 'every platform' but is written only in the RN notes. I implemented both on Lit with a value offset kept for the drag; the web/Lit notes should say so.
- Slider: the error region shows 'the message a Form (or validate) has reported', but ds-form only sets `invalid = true` on the field and never passes a message. So when `invalid` is true, Lit shows the field's own validation message in validation order: copy.required when the value is still the default and `required` is set, otherwise copy.invalid. A consumer who sets `invalid` on a required slider still at its default therefore sees copy.required, not copy.invalid. The doc should say which one wins in that case.
- Slider: DsFormField.currentValue in the digest is `string | boolean | null`, but a range submits two strings. Form.ts already widens it to `string[]`, so the digest's interface is out of date.
- Slider: `validate: blur` on Lit validates on focusout (per the Lit note), while the Behavior section says every platform validates on interaction end. The two contradict each other for Lit; I followed the Lit note.

## 2026-09-23 15:12 — round 2

- Slider: `markLabelColor`, `valueColor`, `descriptionText` and `errorText` are described as 'realised by the composed Text's tone; no hook of its own', which contradicts the overrides rule that a locked binding keeps its `:host` hook (the hooks gate enforces the rule). I declared the hooks and had them reach the child only through Text's `--ds-text-color` hook set on the ds-text element, as ds-input does. The doc should drop 'no hook of its own' for these four, or name the `--ds-text-color` path.
- Slider: `markLabelSize` is described as forwarded to each mark label Text's fontSize only (so, by the overrides rule, no hook of its own), but `markLabelGap` says the slider grows by markLabelSize × font.lineHeight.normal. The mark labels are absolutely positioned, so that row height has to read the value somewhere: I kept a `--ds-slider-mark-label-size` hook for the height and also forward the value to the Texts. The doc should say whether this binding declares a hook.
- Slider: the web platform note only says 'the track click moves the nearest thumb'. That a press inside a thumb's own hit area grabs that thumb and drags from its current value comes from the RN note and the onChange rule 'a click on the thumb fires nothing'. The tie rule (before → low, after → high, on → low) says 'every platform' but is written only in the RN notes. I implemented both on Lit with a value offset kept for the drag; the web/Lit notes should say so.
- Slider: the error region shows 'the message a Form (or validate) has reported', but ds-form only sets `invalid = true` on the field and never passes a message. So when `invalid` is true, Lit shows the field's own validation message in validation order: copy.required when the value is still the default and `required` is set, otherwise copy.invalid. A consumer who sets `invalid` on a required slider still at its default therefore sees copy.required, not copy.invalid. The doc should say which one wins in that case.
- Slider: the prompt digest types DsFormField.currentValue as `string | boolean | null`, but a range submits two strings. Form.ts already widens it to `string[]`, so the digest's interface is out of date.
- Slider: `validate: blur` on Lit validates on focusout (per the Lit note), while the Behavior section says every platform validates on interaction end. The two contradict each other for Lit; I followed the Lit note.

## 2026-09-23 19:30 — round 1

- Slider: the spec says a Form-reported message shows in the error region, but the Lit form contract does not say how ds-form hands one to a field. I assumed ds-form sets the field's `error` property and left it at that.
- Slider: the `Keyboard` story only has the two range thumbs, so the usual three-focusable rule does not apply. The Guidance section says so, but the Keyboard section and the generator rules still ask for three stops.
- Slider: the spec does not say whether a `<ds-slider>` outside a `<ds-form>` should call `checkValidity()` when it is created. I sync validity to ElementInternals on every update and leave it at that.

## 2026-09-23 20:14 — round 1

- Slider: fontFamily is both forwarded to every composed Text's overrides and a root hook (--ds-slider-font-family, applied to .root). The spec says a forwarded binding declares no hook of its own, but also that fontFamily is overridable; I kept both (harmless duplication). Say which is intended.
- Slider: the Lit value for `required` compares against `defaultValue` normalized (min or [min,max]) but the spec's 'the default' for a controlled `value` slider is ambiguous; I compare the displayed value with the normalized defaultValue in both modes.
