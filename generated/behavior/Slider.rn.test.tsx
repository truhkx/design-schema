// Generated by tools/behavior_tests.ts from the `behavior` block of the Slider doc. Do not edit.
import * as React from 'react';
import { fireEvent, render, screen } from '@testing-library/react-native';
import { Slider } from '../../packages/rn/src/Slider';
import type { SliderProps } from '../../packages/rn/src/Slider';
import meta from '../../packages/rn/src/Slider.stories';
import { ThemeProvider } from '../../packages/rn/src/theme';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function setup(given: Partial<SliderProps> = {}) {
  const events = {
    onChange: jest.fn(),
    onChangeEnd: jest.fn(),
  };
  const props: SliderProps = { ...(meta.args as SliderProps), ...given, onValueChange: events.onChange, onSlidingComplete: events.onChangeEnd };
  const tree = (p: SliderProps) => (
    <ThemeProvider mode="light">
      <Slider {...p} />
    </ThemeProvider>
  );
  const utils = render(tree(props));
  const s = {
    ...utils,
    events,
    props,
    root: () => screen.queryByTestId('Slider') ?? screen.UNSAFE_root,
    label: () => screen.queryByRole('slider') ?? s.root(),
    rerender: (next: Partial<SliderProps>) => utils.rerender(tree({ ...props, ...next })),
  };
  return s;
}

describe('Slider', () => {
  test('invalid-renders-the-invalid-copy', () => {
    const s = setup({"invalid": true});
    expect(screen.getByText(new RegExp(escapeRegExp(s.props.label) + "\\ is\\ not\\ valid\\."))).toBeOnTheScreen();
  });
  test('renders', () => {
    const s = setup({});
    expect(s.root()).toBeTruthy();
  });
  test('renders-show-value-always', () => {
    const s = setup({"showValue": "always"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-show-value-hover', () => {
    const s = setup({"showValue": "hover"});
    expect(s.root()).toBeTruthy();
  });
  test('renders-show-value-never', () => {
    const s = setup({"showValue": "never"});
    expect(s.root()).toBeTruthy();
  });
  test('has-accessible-name', () => {
    const s = setup({});
    expect(screen.getByRole('slider', { name: s.props.label })).toBeOnTheScreen();
  });
  test('error-is-identified', () => {
    const s = setup({"error": "Fix this before continuing."});
    expect(screen.getByText(new RegExp("Fix\\ this\\ before\\ continuing\\."))).toBeOnTheScreen();
  });
});
