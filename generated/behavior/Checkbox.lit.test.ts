// Generated by tools/behavior_tests.ts from the `behavior` block of the Checkbox doc. Do not edit.
import { beforeEach, describe, expect, test, vi } from 'vitest';
import { userEvent } from 'vitest/browser';
import '../../packages/lit/src/Checkbox.js';
import meta from '../../packages/lit/src/Checkbox.stories.js';

function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function deep(root: ParentNode, selector: string): Element | null {
  const direct = root.querySelector(selector);
  if (direct) return direct;
  for (const el of Array.from(root.querySelectorAll('*'))) {
    const shadow = (el as HTMLElement).shadowRoot;
    if (shadow) {
      const found = deep(shadow, selector);
      if (found) return found;
    }
  }
  return null;
}

/** Every element that is "active" from the document down through shadow roots: the host, then the inner one.
 *  document.activeElement alone is a shadow host while focus sits inside its shadow tree. */
function activeChain(): Element[] {
  const chain: Element[] = [];
  let el: Element | null = document.activeElement;
  while (el) {
    chain.push(el);
    el = el.shadowRoot?.activeElement ?? null;
  }
  return chain;
}

async function setup(given: Record<string, unknown> = {}) {
  const el = document.createElement('ds-checkbox');
  const props = { ...meta.args, ...given };
  for (const [key, value] of Object.entries(props)) {
    if (value === undefined) continue;
    // `children` is slotted light-DOM content, and `Element.children` is getter-only:
    // assigning it throws and takes the whole file down at setup. Slot a string into the
    // default slot the way the stories do; a richer value cannot be expressed here.
    if (key === 'children') {
      if (typeof value === 'string') el.append(value);
      continue;
    }
    (el as unknown as Record<string, unknown>)[key] = value;
  }
  const events = {
    onChange: vi.fn(),
  };
  el.addEventListener('change', events.onChange as unknown as EventListener);
  document.body.append(el);
  await (el as unknown as { updateComplete: Promise<boolean> }).updateComplete;
  const root: ParentNode = el.shadowRoot ?? el;  // some elements render in light DOM
  const s = {
    el,
    root,
    events,
    props,
    root_: () => el,
    control: () => (deep(root, '[role="checkbox"]') ?? deep(root, '[part="control"]') ?? deep(root, '[data-part="control"]') ?? root.firstElementChild) as HTMLElement,
    label: () => (deep(root, '[part="label"]') ?? deep(root, '[data-part="label"]')) as HTMLElement,
    description: () => (deep(root, '[part="description"]') ?? deep(root, '[data-part="description"]')) as HTMLElement,
  };
  return s;
}

beforeEach(() => {
  document.body.replaceChildren();
});

describe('ds-checkbox', () => {
  test('click-on-control-toggles-on', async () => {
    const s = await setup({});
    await userEvent.click(s.control());
    expect(s.events.onChange).toHaveBeenCalledTimes(1);
    expect(s.events.onChange.mock.calls[0]?.[0]?.detail?.checked).toEqual(true);
    expect(((s.control()) as HTMLInputElement).type === 'checkbox' ? ((s.control()) as HTMLInputElement).checked : (s.control()).getAttribute('aria-checked') === 'true').toBe(true);
  });
  test('click-on-label-toggles', async () => {
    const s = await setup({});
    await userEvent.click(s.label());
    expect(s.events.onChange).toHaveBeenCalledTimes(1);
    expect(s.events.onChange.mock.calls[0]?.[0]?.detail?.checked).toEqual(true);
    expect(((s.control()) as HTMLInputElement).type === 'checkbox' ? ((s.control()) as HTMLInputElement).checked : (s.control()).getAttribute('aria-checked') === 'true').toBe(true);
  });
  test('click-on-description-toggles', async () => {
    const s = await setup({"description": "One email a month about new features."});
    await userEvent.click(s.description());
    expect(s.events.onChange).toHaveBeenCalledTimes(1);
    expect(s.events.onChange.mock.calls[0]?.[0]?.detail?.checked).toEqual(true);
    expect(((s.control()) as HTMLInputElement).type === 'checkbox' ? ((s.control()) as HTMLInputElement).checked : (s.control()).getAttribute('aria-checked') === 'true').toBe(true);
  });
  test('space-toggles', async () => {
    const s = await setup({});
    s.el.focus();
    await userEvent.keyboard(' ');
    expect(s.events.onChange).toHaveBeenCalledTimes(1);
    expect(s.events.onChange.mock.calls[0]?.[0]?.detail?.checked).toEqual(true);
    expect(((s.control()) as HTMLInputElement).type === 'checkbox' ? ((s.control()) as HTMLInputElement).checked : (s.control()).getAttribute('aria-checked') === 'true').toBe(true);
  });
  test('enter-is-ignored', async () => {
    const s = await setup({});
    s.el.focus();
    await userEvent.keyboard('{Enter}');
    expect(s.events.onChange).not.toHaveBeenCalled();
    expect(((s.control()) as HTMLInputElement).type === 'checkbox' ? ((s.control()) as HTMLInputElement).checked : (s.control()).getAttribute('aria-checked') === 'true').toBe(false);
  });
  test('toggles-back-off', async () => {
    const s = await setup({"defaultChecked": true});
    await userEvent.click(s.control());
    expect(s.events.onChange).toHaveBeenCalledTimes(1);
    expect(s.events.onChange.mock.calls[0]?.[0]?.detail?.checked).toEqual(false);
    expect(((s.control()) as HTMLInputElement).type === 'checkbox' ? ((s.control()) as HTMLInputElement).checked : (s.control()).getAttribute('aria-checked') === 'true').toBe(false);
  });
  test('indeterminate-is-announced-as-mixed', async () => {
    const s = await setup({"indeterminate": true});
    expect(s.control()).toHaveAttribute('aria-checked', 'mixed');
  });
  test('disabled-does-not-toggle', async () => {
    const s = await setup({"disabled": true});
    await userEvent.click(s.control(), { force: true });
    expect(s.events.onChange).not.toHaveBeenCalled();
    expect(((s.control()) as HTMLInputElement).type === 'checkbox' ? ((s.control()) as HTMLInputElement).checked : (s.control()).getAttribute('aria-checked') === 'true').toBe(false);
    expect(s.control()).toHaveAttribute('aria-disabled', 'true');
  });
  test('disabled-stays-focusable', async () => {
    const s = await setup({"disabled": true});
    s.el.focus();
    expect(activeChain()).toContain(s.el);
  });
  test('required-is-shown-in-the-label', async () => {
    const s = await setup({"required": true});
    expect(s.el.shadowRoot!.textContent).toMatch(new RegExp("\\(required\\)"));
  });
  test('error-marks-invalid-and-is-announced', async () => {
    const s = await setup({"error": "Accept the terms to continue."});
    expect(s.el.shadowRoot!.textContent).toMatch(new RegExp("Accept\\ the\\ terms\\ to\\ continue\\."));
    expect(s.control()).toHaveAttribute('aria-invalid', 'true');
    expect(s.el.shadowRoot!.querySelector('[role="alert"]')).not.toBeNull();
  });
  test('hidden-label-is-still-the-accessible-name', async () => {
    const s = await setup({"hideLabel": true});
    expect(s.control()).toHaveAccessibleName(s.props.label);
  });
  test('renders', async () => {
    const s = await setup({});
    expect(s.el.shadowRoot ? s.root.childElementCount > 0 : s.el.isConnected).toBe(true);
  });
  test('control-is-focusable', async () => {
    const s = await setup({});
    s.el.focus();
    expect(activeChain()).toContain(s.el);
  });
  test('error-is-identified', async () => {
    const s = await setup({"error": "Fix this before continuing."});
    expect(s.el.shadowRoot!.textContent).toMatch(new RegExp("Fix\\ this\\ before\\ continuing\\."));
    expect(s.control()).toHaveAttribute('aria-invalid', 'true');
  });
});
